# Local environment + tool caches (Python reference stack — extend for yours)
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
.ruff_cache/

# OS filesystem metadata (macOS Finder writes these anywhere you browse)
.DS_Store

# Generated traceability views — regenerated by scripts/trace.py on every run
# (CI uploads them as artifacts; committing them just churns diffs). The report
# and the HTML map are composite renderings of the registries; review the CSVs,
# not these (process.md §3 "Reviewability").
docs/test/report.md
docs/test/report.html
# NOT ignored, and never glob this directory: docs/test/evidence is the COMMITTED
# test-evidence record (scripts/record_test_evidence.py) that the DevStg-Release
# rung is derived from. Ignoring it would make the top rung unreachable and, worse,
# would make it reachable only on the machine that ran the suite.

# Performance comparator artifacts (process.md §9). perf-report.md is a
# regenerated composite; perf-metrics.json is the product-emitted raw
# measurement. Both are gitignored — the tracked source of truth is
# performance-budgets.csv, and the committed golden is perf-baseline.json
# (which is NOT ignored: a regression is accepted by committing its diff).
docs/test/perf-report.md
docs/test/perf-metrics.json

# No-stub / substance detector report (process.md §4 DevStg-Impl) — a regenerated
# composite from scripts/check_stubs.py; review the code, not this list.
docs/test/stub-report.md

# Unattended-coordinator raw session streams (scripts/agent_loop.py). The
# durable record is tracked: docs/iteration/ (size-bounded per-session logs)
# + docs/iteration_index.md; the raw unbounded stream is local debug only
# (process-options.md "Unattended operation").
out/run-logs/
# Per-worktree coordinator lock (scripts/agent_loop.py) — a runtime pid file
# guarding against two coordinators in one checkout; never committed
# (process-options.md "Parallel tracks").
out/agent-loop.lock
out/integrate.lock
# PreToolUse spawn-gate decision log (scripts/subagent_gate.py) — a
# best-effort, append-only trail of each verdict; an untracked cache, so
# truncating it loses history and nothing else.
out/subagent-gate.log
