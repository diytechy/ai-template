# Local environment + tool caches (Python reference stack — extend for yours)
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
.ruff_cache/

# Generated traceability report — regenerated by scripts/trace.py on every run
# (CI uploads it as an artifact; committing it just churns diffs)
docs/test/report.md
