# DERIVED STAGE — generated by scripts/derive_stage.py (do not hand-edit).
#
# NOT YET DERIVED. A fresh scaffold ships this placeholder rather than a made-up
# record, because every field of the real file is a fact about YOUR registries:
# the effective stage, the per-phase breakdown, and a SHA-256 fingerprint of the
# declared derivation inputs. A placeholder carrying invented values would be a
# claim about a spine that does not exist yet, and the fingerprint would be a
# claim that could never be true.
#
# So `derive_stage.py --check` treats a file in this form as "not yet in derived
# form" and passes with a note — the same smooth-transition path `docs/gate` has
# always given a legacy hand-set marker — until the first real derivation lands.
#
# Run it whenever you like; `trunk_step.py --regen` runs it for you at the trunk
# regeneration points:
#
#     python scripts/derive_stage.py
