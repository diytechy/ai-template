# CLAUDE.md

The agent guide for this repo is **[`AGENTS.md`](AGENTS.md)** — read it first.
It is the single source of truth for how we build here: the gated,
requirement-traced process, the readability conventions, and the working
agreement. This stub exists only so Claude Code (which reads `CLAUDE.md`
natively) is pointed at that guide; don't add content here — keep it in
`AGENTS.md`.
