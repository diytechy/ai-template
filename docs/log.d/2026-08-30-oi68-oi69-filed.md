## 2026-08-30 — open-items: OI-68 and OI-69 filed, two rows queued, the plans of record copied in

Deferred open items: OI-68, OI-69

**Summary.** The three plan folders the owner kept outside the repo
(`C:/Projects/ai-template-plans/`) were reviewed read-only and given three
fates. The complexity-pushback program is registry-ready: its phase-0 brief
is filed verbatim as **`OI-68`** (four questions: does the cognitive-complexity
sensor replace the module-size line ratchet, does it census `tests/`, armed
here or report-only, the two metric conventions and the threshold), and its
three work items follow the ruling. The adjudicator study reduced, after a
cross-family review, to an opt-in session-retention layer; its five settings
that need the owner's word are filed as **`OI-69`**, with one correction the
driver made on reading the record: the layer retains a session TRANSCRIPT a
bounded process replays, not a resident actor, so the no-daemon doctrine is
confirmed rather than reversed. The knowledge-pack review is a research pack,
not a work item; its small byte-paid edits and two kit findings are one
queued row, its UI and texture items wait on rulings and a licence check, and
its "route to plans" items stay plans.

**Filed.** `OI-68` and `OI-69` pending in `docs/requirements/open-items.toml`
(the watermark `OI` 67 → 69); two rows queued with the watermark `WI` 534 →
536 — the adjudicator telemetry-first row (`quick`; session id and
occupancy / window / percent per family, dial off, the stale
`opencode-go/grok-4.5` slug) and the agent-brief-and-scope row (`medium`;
the synthesis's items 4, 5, 6, 7, 13, 14 and its §6 findings), each with a
`specref` that resolves path and anchor. Four plans of record copied
verbatim into `docs/plans/` with a one-line provenance comment: the
complexity sensor plan (the path its own phase-1 draft names as `specref`),
the adjudicator session-retention layer, the parked persistent-session study
the brief cites, and the knowledge-pack synthesis. The prototype, the source
reports, the drafts and the review transcripts stay outside the repo, as the
owner asked.

**The driver's input, on record in the briefs' recommendations.** On `OI-68`
the driver departs from the folder's own recommendation on two of four: (1b)
demote the line ratchet to advisory for one measured window before retiring
it (today's sitting re-stamped it six times for docstring-only growth — it
measures prose — but its 2,055 lines of reviewed rationale are a one-way
door), and (3b) report-only on this repo for one window before arming (the
false-positive rate is owed before any claim, and every armed sensor is
another re-stamp per session on a bar already measured heavy); (2a) census
`tests/` — the 304-second test lives there — and (4a) stand. On `OI-69`: (a1)
confirm the no-daemon reading, (b1) `reset_on_same_artifact = false` with the
fork hardening banked, (c1) no keep-warm pings across the blackout, (d1)
`process.toml`, (e1) dedicated CLI homes.

**Not done, by the owner's call:** the bar's hotspots (the 304 s
`test_traj_graph` wire check, the nine dispatch lanes) get a row when the
grinding is ready to start, not before.

**Deviations from spec:** none — a filing sitting.

**Byte deltas on budgeted files:** none touched.

**pytest totals:** smoke tier under Git Bash **1378 passed, 6 skipped in 109.59 s** — the budget read **110.4 s vs 60 s → OVER** on the loaded box, environmental, recorded, not waived; `gen_open_items --check`: current, two pending
cards; `check_trajectory --strict`: exit 0 (both queued rows' `specref` resolve,
path and anchor); `check_docs --stale`: 0 broken.
