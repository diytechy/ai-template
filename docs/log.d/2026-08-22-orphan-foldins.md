## 2026-08-22 — The orphan debt gets owners: five fold-ins, one stray row

Deferred open items: none — an assignment act executing the owner's
directive ("Sure, please do") after the orphan-vs-frontier mapping.

The seven undecomposed SRs plus the one TC-less LLR — the debt that holds
the ladder at `DevStg-LLReqs` now that every registry is approved — are
fully assigned:

| Orphan | Assigned to | How |
|---|---|---|
| SR-151, SR-152 (hosted CI runs the bar; the verdict is the harness's own) | the test-evidence-carrier row | fold-in note + `sr_refs` — the carrier's CI-lane producer IS their subject; mint the LLR/TC as part of the design |
| SR-162 (boundary references resolve against the declared frame) | the wi455 lane | fold-in note + `sr_refs` — decomposed when the counterpart→consumers transform executes, mirroring the Hat-Refs resolution pattern |
| SR-163 (every shipped file maps to a stakeholder outcome) | the remap program row | fold-in note + `sr_refs` — the blind derivation IS this SR's verification exercise; decompose as slice 1's framing act |
| SR-177 (fan-out utilisation from the run's own telemetry) + LLR-164's missing TC (the prompt catalogue) | the session-continuity row | fold-in note + `sr_refs` — same telemetry surface; the template review regenerates the catalogue anyway |
| SR-160 (front-door launchers), SR-164 (declared SN scope) | **WI-510**, minted this act | the two strays no queued row owns; decompose-or-honestly-reclass per the row's Context |

Mechanically safe by design: the mints land Drafted, the effective stage
excludes drafts, so selection does not move; the ladder climbs as the
mints approve. Watermark `WI` 509 → 510; `docs/status.md`'s orphan
sentence corrected from the stale "nine undecomposed SRs" to the measured
seven-plus-one; surfaces regenerated.
