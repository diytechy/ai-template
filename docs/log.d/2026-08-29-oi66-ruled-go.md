## 2026-08-29 — the owner rules OI-66 (a) GO: build the contract header

Deferred open items: none — `OI-66` was the last row on the decision surface,
and this ruling clears it.

The owner: *"I'm good with option a for OI-66."*

What is ruled: the component-side contract header is BUILT — the structured
docstring body, the committed generated reference it harvests into, that
reference's freshness gate, the 21 missing anchors, the harvester fix, and the
adopter migration. Execution row: **WI-527**.

The price this was ruled against is `WI-525`'s analogue, not an estimate:
`WI-512` built the same shape of mechanism for **+2,024 / −156 across 36 files**,
and the header build is that shape with a larger cell pass (71 rows against 27),
21 modules needing an anchor first, a broken harvester to fix, and a smaller
mechanism because the anchor already exists.

**The required guard is binding and sequenced:** the harvester fix lands BEFORE
any cell moves. `gen_arch_map.module_contracts` currently reads `handback.py` as
declaring `IF-080` although its docstring says *"No `Contracts:` line,
deliberately"*, and a mechanism that reads a negated declaration as a
declaration cannot be the thing 71 rows point at.

**The one number still not taken** is the authoring cost of the 71-row cell
pass. Option (d) — sample five rows first — was not the option ruled, so the
number is measured as the pass runs and reported at close rather than before it.

The owner also directed that this session take its own decisions without
consulting them, escalating anything high-risk to a cross-family reviewer and
arbitrating a disagreement with an independent Opus agent, and close by filing
the decisions that need review as their own document.
