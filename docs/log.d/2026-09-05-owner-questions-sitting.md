## 2026-09-05 — owner questions sitting: the answers report, and terminal history relocated into the archive

The owner asked seven questions during the review sitting (item 1 was already
answered). The answers, the evidence behind each, and the pick-up checklist are
in [../decisions-for-review-2026-09-05.md](../decisions-for-review-2026-09-05.md).
Nothing here is a ruling; the open-item dispositions in its §6 are the owner's
to apply.

**What this session executed.** One thing only: the sixteen terminal work-item
specs that had accreted under `docs/work/complete/` (twelve) and
`docs/work/partial/` (four) between 2026-08-26 and 2026-09-05 were relocated
into `docs/archive/work/` with the link-aware `spec_move.py` ritual — two
inbound links re-pointed, nothing else touched. Both directories now hold only
their README pointers. The registry never lost a row:
`kitlib.registry.read_spec_rows` reads both roots as ONE registry, which is
exactly why the drift went unnoticed for two weeks.

**The cause, and what it leaves owed.** WI-504 (OI-55 ruled (a)) moved terminal
history under the archive, but two of the three mechanical writers never
followed: `handback.close_partial` writes `docs/work/partial/` and
`handback._close_adjudication_one` writes `docs/work/complete/`. Only
`consolidate.archive_absorbed` and the worker's own clean close file into the
archive. Fixing the writers also requires fixing the reader —
`kitlib.verdict.mechanical_close_attestation` recognises a mechanical close only
by additions under `docs/work/complete/`, so a writer fixed alone would stop
every mechanical close from peeling and stale its own round (the WI-586 failure
mode). Nineteen test modules pin the old path. That work is drafted as a WI in
the report's §5 and is NOT minted yet.

**The owner's stated intent behind item 5, recorded because it is doctrine and
not preference:** the archive is to hold ALL history, so that only the spine
defines living expectations and no agent or human reads a four-month-old closed
item as a statement of how the system works today.

**Findings that need no work here.** The id watermark was not hand-edited — it
is regenerated by `trace.py --bump-ids` and enforced by `watermark_findings`
inside the always-on `--strict-integrity` floor, which adopters get. The
`docs/if-tc-coverage-allow` additions ARE hand edits by design, and the
allowlist's promotion-to-error, per-entry reason requirement and hygiene report
all ship downstream. What does not ship is the pytest that pins the 118-id seed
set, which tests the kit rather than the adopter's repo — the intended split.

Green at this tip: `check_doc_refs.py --strict` OK (0 dangling),
`trace.py --strict-integrity` integrity=0 orphans=0.
