# 2026-08-25 — the OI-63 legibility round: cross-family simplify + second opinion

Owner-requested in session: *"simplify the Open-items.toml text"* (what was
found, the blast radius, the categories, the exact options and what they
expose) plus *"a cross-review at the same time just to get a second opinon on
the findings"*. Provider: **`OPENAI-SOL`** (`gpt-5.6-sol`, `codex exec`) — the
declared, enabled row; probed live; two read-only runs. Full record, raw
returns and per-finding adjudication:
[reviews/2026-08-25-oi63-brief-round/RESUME.md](../reviews/2026-08-25-oi63-brief-round/RESUME.md).

**What changed.** OI-63's five cells rewritten at ~55% of their size with the
verdict taxonomy DEFINED ONCE (restatement / remainder classes A–I /
non-crossing M,X), the numbers in the registry's bullet form, and each
option's FOR/AGAINST kept whole. Two defects the round caught are fixed
rather than carried: option (b)'s residual `21` (the one site `057d47e9`
missed — corrected to the derived 19-pure / 37-zero-remainder form) and the
blast-radius sentence that called (a) the only option inside the kit line
while (d)'s own case says it needs no kit change. The draft's pipe-table /
definition-list formatting was transposed to `md_block`'s grammar (`### `,
`- `) so the owner surface renders structure, not walls.

**The second opinion, in one line.** The measurement STANDS as a basis for
ruling: arithmetic re-summed exact, both rot exhibits (IF-055, IF-117)
re-verified in-tree, 12 of 15 spot-checked rows AGREE. Qualifications now
riding with the record: `IF-050`/`IF-061`/`IF-098` flagged for
re-adjudication (IF-050's false universal verified against
`kitlib/stage.py` at adjudication), the consumer-side-obligation claim
softened from "structurally impossible" to "statable provider-side, not
synchronizable there", and class F read as an ownership axis. Banked, not
fixed: an 8-character discrepancy inside the WI-512 dossier's own `new`
column (2,605 vs its reported 2,613) — historical fragment, no OI-63 figure
touched.

OI-63 stays `pending`; nothing here rules it.
