## 2026-08-18j — byte-budget-guard: the stamp history, evicted from the skill table

The guard skill's budget table carried its whole re-stamp history as nested
parentheticals inside two table cells (55 markers, 87% of the file). The
table now carries only the live baseline per row; the superseded chains are
preserved here verbatim (as committed at ff03d323 — the stamps added on
2026-08-18 before the eviction are recorded in this day's own log entries:
PROCESS.md 81,763 -> 81,499; PROCESS_OPTIONS.md 172,037 -> 172,047 ->
173,375; reasons in the shipped-docs-prose and csv-reference entries).

| `project-trajectory/PROCESS.md` | **watched** (baseline **81,763** as of 2026-08-18/one-vocabulary rename (**+997**: the `DevBar-*` prefix RETIRES — §4's stage-ladder block is rewritten onto the one vocabulary the owner ruled: the same `DevStg-*` token names both readings and the VERB carries the axis (a repo is IN a stage and CLEARS a stage), the three clearable rungs are marked in the diagram as events naming what closed and what the project enters next, and the two prior failures are recorded so neither is re-proposed — the `G*` tags could say neither "met" nor "next" on one axis, while the retired Release bar and the `DevStg-Release` rung sat three letters apart meaning a strictness level and a per-release milestone. The +997 is load-bearing: the prefix swap is NOT 1:1 (the Release bar -> `DevStg-Impl`, because it closed the Impl rung and `DevStg-Release` is not clearable at all), and a core that showed the ladder without the verb would leave every reader deriving the wrong mapping. Log 2026-08-18d; earlier 80,766 as of 2026-08-18/requirement quality + EARS (**+2,664**: §3 gains the requirement-quality block the whole tier had never stated in one place — simple technical English, the EIGHT characteristics (necessary, singular, unambiguous, complete, verifiable, feasible, conforming, traceable) as a table, the FIVE EARS statement patterns as a grammar table, and the what-a-checker-settles paragraph that names Necessary/Complete/Feasible as the review's with no proxy metric offered. The +2,664 is load-bearing and belongs in the core rather than an expansion home for two reasons the kit's own rules force: §3 already asserted 'Conforming' by implication — the one-`shall` bullet gated a statement pattern the file never wrote down, so an adopter was held to a form they could not read — and the new EARS advisory in `trace_text.py` needs a stated rule to be an enforcer OF something rather than a checker inventing policy. Paid for in part by collapsing the old one-`shall` bullet into a two-line pointer (its enumeration now lives once, in the new block). Log 2026-08-18a; earlier 78,102 as of 2026-08-17/registry status unification (**-1**: §5's defect-flow diagram and its off-spine bullet re-worded onto the unified vocabulary — the CMP round-trip reads `Founded -> Drafted+has-gap -> Founded` where it read `verified -> has-gap -> verified`, because `state` SPLIT into `status` (the one spine enum) plus `standing` (the lifecycle axis it was conflating). Net negative, so nothing is owed beyond this stamp; the re-word is load-bearing because the core taught two retired spine words as live CMP values, which is the retired-vocabulary regeneration SR-149 exists to prevent. Plan docs/plans/2026-08-16-registry-status-unification.md, log 2026-08-17u; earlier 78,103 as of 2026-08-17/owner rulings 2026-08-17m (+277 on the 77,826 stamp: §4's amend-and-flip teaching rewritten from the retired chain reading — "the SR is the attestation unit — flip it whenever its chain changes" — onto the ruled CELL reading: a `Status` flips only when the row's own text changes, a child (LLR/TC) amendment never flips the parent SR, child changes surface through the snapshot-drift arm and the gate, and the chain-completeness claim belongs to the derived `Founded` state (D-9), never the signature's. The +277 is load-bearing: the checkers, skills and reference doc move to the cell reading in the same commit, and leaving the core teaching the opposite would be the single-source inversion the stamps below record. Log 2026-08-17m; earlier 77,826 as of 2026-08-17/WI-471 shipped-docs resync (+1,600 on the 76,226 stamp: §4's gate-authority paragraph rewritten from the retired three-word enum onto the 0–4 `human_ratification_through` ordinal with `attended`/`single-ratify`/`autonomous` named as translate-only `--gate-policy` presets (SN-029); §1's domain-hats prose re-anchored on the shipped hats roster (`requirements/hats.toml` — `applies_when`/`asks`/`listens_for`, owner text tailored at `DevStg-Boundary`, obligations entering as labelled derived SRs); §4 gains the frame-is-a-registry paragraph naming `external.toml`'s three row kinds + `boundary_refs`; the re-attest paragraph gains the `docs/archive/last_approved/` byte-copy baseline (mechanism only — the first seed and UNANCHORED arming stay sitting-pending); the §1 hat-table paths, the retired CSV/markdown-carrier tokens, the DevStg-Release `Stable` token and §8's illegal `draft` approval token re-worded onto the live vocabulary, paid for in part by trimming the superseded domain-hats prose. Log 2026-08-17j; earlier 76,226 as of 2026-08-16/Sol round F20 (+93 on the 76,133 stamp: §3's R2 bullet opens in the rationale-gated form — "must not name a concrete artifact unless its rationale records why constraining that artifact is necessary" — replacing the absolute "never", on the cross-family reviewer's finding that an absolute rule beside a waiver-aware checker reads as a contradiction to a first-time adopter, and aligning with the standards research (every surveyed body gates on recorded justification, none bans). Log 2026-08-16o; earlier 76,133 as of 2026-08-16/re-tier v2 S1 (+1,597 on the 74,536 stamp: §3 gains the two one-decision tiering bullets — **R1** one decision per row / one home per method, with the warn-first fan-out detector (declared bound, default 7 direct LLR children, per-row re-stamp with reason, deliberately not a cap), and **R2** a requirement cell never names a concrete artifact (capability/artifact-class voice at SR tier; concrete filenames only in `AcceptanceCriteria` as rewritable current-carrier evidence or at the LLR tier; the allocation question answered at the binding homes). The +1,597 is load-bearing: the owner's 2026-08-15p rulings SUPERSEDE sitting-1 2.7(a), and until the master states the new rules the registries and the trace advisories carry a model the load-bearing core contradicts — the same single-source inversion the WI-442 and interface-rework stamps below record. Owner rulings log 2026-08-15p; earlier 74,536 as of 2026-08-15/D-9 migration step 5 (+120 on the 74,416 stamp: §4's status paragraph states the CLOSED three-value ladder — `Drafted`/`Approved`/`Modified` — says an out-of-vocabulary value is an always-on integrity finding, and says `Approved` makes NO claim that tests passed. The +120 is load-bearing twice over: the file previously promised an OPEN vocabulary, which the closure makes false, and it previously taught `Modified`→`Planned` as the evidence-invalidated path, which the fold deletes — leaving either would have the load-bearing core teaching a vocabulary the always-on floor refuses. Paid for in part by deleting the `Planned` sentences the fold retired. Owner rulings OI-30 D1/D2, log 2026-08-15m; earlier 74,416 as of 2026-08-15/interface rework (+597 on the 73,819 stamp: §8 states the RULED interface model — `Owner` as the one row answerable for a seam (an `SR-###` or a design-tier id, exactly one), `Direction` restated as FLOW AND COVERAGE rather than ownership (a `Provides` implies orientation but not that the seam is directional; a `Consumes` declares that a cross-component edge is intended and that this row discharges it), the `Owner`'s side closing the final read in place of the `Provides` side, and `CarriedBy` naming the bundle a constituent rides. The +597 is the load-bearing part: §8's old sentence taught `Provides`-drives-ownership, so leaving it would have left the core teaching the opposite of what the owner ruled (Q1/Q2/Q3, 2026-08-15a) while the registry and the checkers carried the new model — the single-source rule inverted, which is the same failure the WI-442 stamp below records. Owner rulings 2026-08-15a, plan §6; earlier 73,819 as of 2026-08-14/WI-451 act 6 (+202 on the 73,617 stamp: §1's domain-hat paragraph re-anchors the slice identity on the LLR `Module`/component after `Area` RETIRED for the closed `Aspect` vocabulary (sitting-2 decision 10, owner ruling 2026-08-14h), and NAMES `Aspect` while stating what it is not — a cross-cutting label, never an ownership tag. The +202 is that distinction: without it a reader finds the old tag gone and reasonably assumes the new one replaces it, which is the exact confusion the ruling's "REVIEW grouping, not an ownership claim" wording exists to prevent; -5 of it was paid back by the four re-anchored references); earlier 73,617 as of 2026-08-14/WI-442 REVIEW-A round 2 (+13 on the 73,604 stamp: §8's OPENING field enumeration still taught the retired `stability` after the same WI's re-word of the maturity sentence below it — the round-2 reviewer's finding; re-worded onto `Approval`; earlier 73,604 as of 2026-08-14/WI-442 (+184 on the 73,420 stamp: §8's one-maturity-field sentence re-worded from the retired `Stability` onto `Approval` (`draft`/`approved`) and gains the directional tie-back rule — `interface_from_external`/`interface_to_external` appear ONLY on a row that realizes a declared boundary crossing, and a row with neither is an internal seam. The retirement was landing in the registry and the scripts while the load-bearing core still declared the dead column, which is the single-source rule inverted; earlier 73,420 as of 2026-08-13w/post-sitting reconcile (RECONCILED to what the tree actually carries — the 68,197 stamp had drifted +5,223 un-flagged across the WI-444/WI-445/WI-447 doc landings; earlier 68,197 as of 2026-08-13/WI-443 (+829 on the 67,368 stamp: §8 gains the ruled OI-14 part B INTERFACE-ONLY contract — `Contract` states only what crosses plus its `Signal` (`discrete`/`variable`), the why moves to `Rationale`, the four warn-first form rules (no WI id, no decision citation, no rationale connective, a 500-character ceiling) are named, and `Stability` is declared the row's ONE maturity field now that `Status` retires; the field list and the machine-consumed paragraph re-worded in place (`Status` dropped from the `plan_briefs` surface); earlier 67,368 as of 2026-08-13/WI-440 (+311 on the 67,057 stamp: §8 gains one paragraph stating that `plan_briefs.IF_SURFACE_COLUMNS` feeds the IF row's surface — `Contract` and `Status` included — VERBATIM into the dual-plan LLM planning briefs, so a cell is consumed as authority rather than merely read (OI-14's first do-not-wait); earlier 67,057 as of 2026-08-12/stage-gate ruling (+2,591 on the 64,466 stamp: §4 gains the ruled **"Stages and gates — state vs. certified boundary"** subsection — the 0–5 stage ladder with the gates between its rungs, the uniform next-gate rule, the kept "a gate is not a pure function of stage" caveat, and the floor-not-achievement reading of the derived value; the §4/§7 gate-as-state sentences reworded in place. Owner ruling 2026-08-12, `docs/archive/plans/2026-08-11-stage-gate-semantics.md`; earlier 64,466 as of 2026-08-11/WI-432 (unchanged by that WI, which did not touch this file; the number is RECONCILED to what the tree actually carries — the 64,451 stamp had drifted +15 un-flagged. Historical reasons kept: −9 on the 64,460 stamp: −9 on the 64,460 stamp: §4's gate-authority and §7's push-authority pointers moved from the retired one-word files to their `docs/process.toml` dials, which reads shorter; earlier +141 on the 64,319 stamp: the §4 phased-delivery note tightened to numeric-only Phase and gained the ruled phase-boundary sentence (confirmed scope change, never the raw gate drop) + the `--next-phase` pointer)); keep flat — re-stamp this number, every tracked skill copy, when a flagged growth lands) | convention + the WI log's byte-delta report |
| `project-trajectory/PROCESS_OPTIONS.md` | **watched** (baseline **172,037** as of 2026-08-18/one-vocabulary rename (**-54**: the `DevBar-*` tokens re-spelled onto `DevStg-*` throughout, which reads shorter; no layer's content changed. Log 2026-08-18d; earlier 172,091 as of 2026-08-17/desk-round disposition (**-15**: the reviewer-tripwire sentence stopped documenting `AGENT_STATUS_WARN_BYTES` as a live dial — the env var and its `status_size_warning` reader retired with the serial driver (WI-210), so a shipped, scaffolded doc was teaching every adopter a symbol that exists nowhere in the kit (2026-08-17 desk round, F6). The pruning obligation itself is unchanged and the question of whether the tripwire should be REBUILT is deliberately left open for the owner. Net negative, so nothing is owed beyond this stamp. Log 2026-08-17w; earlier 172,106 as of 2026-08-17/registry status unification (**+80**: the "Component layer" row description re-stated on the SPLIT CMP schema — `Status ∈ Drafted|Approved|Founded` (the one spine vocabulary, and CMP is the one off-spine tier that reaches `Founded`) beside a new optional `Standing ∈ active|has-gap|deprecated`, omit = active. The +80 is the second field: `State` was carrying maturity AND lifecycle on one axis, and a layer doc that lists the row's columns while omitting one teaches a schema the checker refuses. Paid down from +133 by tightening the same paragraph. Also the §"Specs" seam sentence: `approval = "drafted"` -> `status = "Drafted"`. Plan docs/plans/2026-08-16-registry-status-unification.md, log 2026-08-17u; earlier 172,026 as of 2026-08-17/WI-471 shipped-docs resync (+52: the retired-carrier/vocabulary tokens re-worded onto the live forms — the `docs/work/` WI registry for the live-voice `work-items.csv`, `interfaces.toml`/`components.template.toml`/`open-items.template.toml`/`test-cases.toml` for the `.csv` spellings, `approval = "drafted"` for the dead `Status=Proposed`/`Active` seam values, `req_refs` for `SR-Refs`, the `buildtier` frontmatter key for the `BuildTier` column; the legacy-CSV dual-read paragraph deliberately untouched. Log 2026-08-17j; earlier 171,974 as of 2026-08-15/D-9 migration step 5 (+105 on the 171,869 stamp: the "Artifact states" bullet re-states the SR/LLR/TC row on the CLOSED ladder and records that **DevStg-Impl is no longer reachable from a cell** — `sr_bar` ceilings at DevStg-Tests and the derived line renders `DevStg-Tests (Release: pending harness driver)` (OI-30 D2). The +105 is that second sentence: the layer's whole job is telling an adopter what the derived value means, and a ceiling that is not written down reads as a regression the first time someone sees a mature spine stop at DevStg-Tests. Log 2026-08-15m; earlier 171,869 as of 2026-08-14/WI-451 act 6 (-47 on the 171,916 stamp: the `Lifecycle` tag's "mirroring `Area`" analogy STRUCK rather than re-pointed — `Lifecycle` is an OPEN project-named set and `Aspect` is CLOSED, so re-pointing would have taught the opposite of the rule — and the multi-module grouping re-anchored on `Module`/component); earlier 171,916 as of 2026-08-13w/post-sitting reconcile (RECONCILED — the 171,289 stamp had drifted +627 un-flagged); earlier **171,289** as of 2026-08-13/WI-437+WI-440 (+316: the OI-25 sweep — the ten instructional lines that taught the RETIRED `gate_policy` enum rewritten onto the live vocabulary (`human_ratification_through`, the `human-held`/`loop-held` SESSION HOLD); the `--gate-policy` flag and `docs/gate-policy.md` keep their names — live interface, not residue; plus +355: the "Component layer" cross-CMP paragraph states the new WI-440 multi-membership overlap ADVISORY — warn-only, never the exit code — so the new WARN an adopter sees is documented in the layer that emits it; both on the 170,618 stamp, composed at the serial merge; earlier 170,618 as of 2026-08-12/stage-gate ruling (+9 on the 170,609 stamp — net flat: the "Derived gate model" section stops calling itself "the working summary", repoints its dead `docs/specs/derived-gate-model.md` deference at the archived spec AND at process.md §4 as the ruled authority, and its applies-when row stops claiming an always-on layer; the false "the repo is at gate G iff" claim is gone, and four now-duplicated restatements were trimmed to pay for the additions. Owner ruling 2026-08-12; earlier 170,609 as of 2026-08-11/WI-433 (+212 on the 170,397 stamp: the "Unattended operation" blackout sentence now states the ruled SHIPPED-DISABLED default and the asymmetry that decided it, replacing one clause that named a value; earlier −62 on the measured 170,459 at WI-432: −62 on the measured 170,459 this file actually carried: the "Where the dials live" note and five layer opt-out sentences re-point from the six one-word files to `[checks] <key> = false`, which reads shorter. NOTE the stamp it replaced said 170,452 while the tree held 170,459 — a +7 drift that landed un-flagged before this WI, reconciled here rather than carried; earlier +1,314 on the 169,138 stamp: +1,314 on the 169,138 stamp: the "Where the dials live" note — the one policy home `docs/process.toml`, its keyed-pure-sh shape, the `--migrate-config` conversion and the refusal of a mixed config, plus the three kinds of declared file that deliberately stay outside it — and the per-dial rewrites from the retired one-word files to their `[section] key` names; earlier +13 on the 169,125 stamp: the layer table gained its row for the dispatcher split; earlier +115 on the 169,010 stamp: the "Signed measurements" *Grammar* sentence narrowed to whole-token placeholders — a metacharacter inside a longer value is command text and the marker is judged, closing the silent over-approximation of WI-392 REVIEW-A round-2 finding 4; earlier +788 on the 168,222 stamp: "Phased delivery" — the numeric-only rule with its literal-join rationale and grandfathering note replacing the free-form-string blessing, plus the ruled phase-boundary paragraph and the `--next-phase` helper; earlier +1,908 on the 166,314 stamp: +1,570 for "Signed measurements" part 3 — the opt-in `fig:` declared-figure marker grammar with the population and derived-figure bars, the truth/presence enforcer split, and rung 2 recorded as deliberately not built — plus +338 at the REVIEW-A rework: the *Grammar* sentence (placeholder values declare nothing, per-marker judgment, bare-or-quoted rev=) and two fig-ok exemptions so the convention text ships scaffold-clean); growth is allowed but must be *flagged* with a delta + reason — re-stamp this number, every tracked skill copy, when a flagged growth lands) | convention + the WI log's byte-delta report + the doc's own *Applies-when index* note |
