## 2026-09-06 — Anchor the OI-85/P9R resync recipe

Implementation commit: `cde260dd0aea68b92469c5b45fdde14c3dd78d59`. Its hooks passed, and the working
tree was clean after committing. This follow-on entry gives existing adopters
the exact source for the renderer-package migration, canonical need-reference
checks, source-identity preflight and review-record guidance.

Terra reviewed the recipe against the bootstrap manifest and actual source.
The corrections keep bootstrap as target-kit tooling, explicitly include the
package initializer and shared readers, preserve filled adopter records, and
verify the new facade before and after deleting obsolete kit-owned emitters.
Earlier entries still govern a wider upgrade range. This meta-repo's approval
snapshot and local test selector are not shipped as adopter state or policy.

The execution/P9R records now name the committed source and actual validation.
The control preflight and incomplete SR-161/TC-211 sample remain visible; no
queue or approval state changed in this documentation step. The implementation's
full suite remains the broad evidence; this documentation-only follow-on runs
the required commit bar and docs checks. No push.

Deferred open items: none.

Validation:

```text
.venv/bin/python -m pytest -q -n auto -m smoke
1680 passed, 4 skipped in 58.82s

.venv/bin/python scripts/check_smoke_budget.py --mode enforce
1680 passed, 4 skipped in 61.52s (0:01:01)
smoke wall-clock budget: 61.7s vs 60s budget -> OVER
```

The separate enforcer exited 1 on timing alone. The standing owner exception
applies; the budget and test membership are unchanged from the implementation
commit. `check_docs.py --root . --stale` passes with zero broken links and the
existing report orphan warning. `git diff --check` passes. Installed commit
hooks remain enabled.
<!-- fig: cmd=".venv/bin/python -m pytest -q -n auto -m smoke" rev=cde260dd+resync; out/run-logs/oi85-resync-smoke.txt -->
<!-- fig: cmd=".venv/bin/python scripts/check_smoke_budget.py --mode enforce" rev=cde260dd+resync; out/run-logs/oi85-resync-smoke-budget.txt -->

The first commit attempt was refused by the open-items freshness hook. The
implementation log's declaration spoke only for its final subsection; it now
sits in the file-level top matter, and the owner page is regenerated. This
corrects declaration scope without adding a deferral or changing an OI ruling.
The same installed hook is rerun; no bypass is used.
