## 2026-08-20 — D-9 steps 7+8 land as one act: `Modified` retires, `Founded` arms, both snapshot rules become integrity ERRORs

The sitting's mechanical follow-on (review-package §5 step 4; spec of record
`docs/plans/2026-08-15-d9-migration-plan.md` rows 7–8), built by an Opus
worker and verified before commit. One act, because the enum must equal the
live-predicate set at every commit: `is_modified` DELETED (with `is_founded`
minted as its F5 pair), `STATUS_VALUES = {Drafted, Approved, Founded}`,
`SPINE_MATURITY` re-keyed, the `modified=` basis counter retired from the
emitter while `check._BASIS_RE` keeps HONOURING it (a downstream gate file
mid-migration keeps `window_open`'s conclusive arm), `intake._apply_flips`'
silent `continue` resolved into a refusal that names the row and quotes its
status, and `baseline_snapshot.unanchored_findings` +
`check_trajectory.staged_snapshot_findings` both promoted to the always-on
`--strict-integrity` integrity floor. `Founded` becomes legal and recognized
for the four spine tiers; nothing writes it (D-9 consequence 2 stays open).
`tests/test_rule_sync.py` gains the negative assertion plus by-value pins on
the two retired-word readers the grep cannot see. Shipped surfaces swept
(PROCESS.md §4, PROCESS_OPTIONS ×6, EXAMPLE, README, the SN template, three
tri-copied skills, the machinery reference) + a RESYNC entry. Ratchets
re-stamped with reasons; `derive_gate.py` trimmed back UNDER its threshold
rather than re-stamped up.

**Two deviations from the plan, recorded in place and here** — (1)
`--ratify modified` keeps its name (a CLI word, not a Status value; the
coupling the plan feared was cut at step 4); (2) the `drifted=N` basis field
is NOT built (it needs a derive_gate→baseline_snapshot edge that is a
module-level cycle plus a filesystem read inside a pure derivation — out of
scope for a mechanical step). **One owner call surfaced:**
`intake._apply_flips` now writes NOTHING — with `Modified` retired there is
no mechanically-flippable state, and widening mechanical ratification
authority (write to Drafted, or re-bless a drifted Approved row) is a policy
decision, not a migration step; both candidates are stated in its docstring.

Riding the same commit, truth-matched: the meta phase-derivation test's
independent model learned DECOMPOSITION (its "orphans=0 ⇒ every SR is
decomposed" simplification died at the signing — nine SRs are Approved with
no LLR and no TC, so phases 4/5 honestly read DevStg-Reqs), and the R-F
error the WI-464 close introduced cleared (a terminal WI's SpecRef is empty;
the retier plan is no longer cited by any open WI and archives in a later
bookkeeping pass).

Verification: full suite by the worker 2589 passed / 1 pre-existing failure
(the phase-model test above, now fixed — the derive_gate module re-run reads
32 passed); smoke 1205 passed / 5 skipped; `trace.py --strict-integrity`
integrity=0; `check_trajectory.py --strict` down to exactly ONE error, the
`hats -> spine_carrier` seam WI-474 owns next.
<!-- fig: cmd="python -m pytest -q -n auto -m smoke" rev=2d51f140 -->
