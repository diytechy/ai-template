## 2026-08-28 — OI-64 and OI-65 executed: the protocol stated, the lint built, the ten passages cleared, the header priced

Deferred open items: OI-66 — the header build's go/no-go, now priced.

Four work items closing two rulings, all on `contract_split`.

### WI-523 — the lint arm nobody had built

`IF_REASON_CELLS` gains `Rationale`; `_WI_TOKEN_RE` gains `re.IGNORECASE`. The
predictions made before the ruling both held: **0 findings** from the 37 live
`rationale` cells, and exactly **3** from the case fix — `wI-280` in `IF-082`,
`IF-083`, `IF-084`, invisible to three prior rounds of the same arm. All three
were fixed by dropping the provenance frame rather than silenced, and each cell's
standing reason stood alone without it. Two tests added.

### WI-524 — the ten passages

Eight provenance or duplicate spans deleted; `IF-117` and `IF-061` corrected
against the tree rather than against their own cells. `contract` over the
135-row registry **40,472 → 40,137 (−335)**.

`IF-117`'s three false claims each re-verified before rewriting:
`docs/architecture.md` is absent (retired at WI-455); `load_symbol_oracle`
derives from `gen_arch_map.scan_inventory` and its own docstring says the
committed block is retired; and "149 live LLR rows" is stale against 187.
`IF-061` described the work-items CSV dual-write as live, which
`plan_artifacts._write_spec_rows` contradicts in its own comment.

Two of the eight resolved to nothing reachable and were replaced by what they
meant — `IF-090`'s "ruled decision 2" read off `intake.adjudication_action`'s
docstring — rather than merely cut.

### WI-525 — the price, and the claim it overturns

`OI-63` and `OI-65` both recorded that option (b)'s precondition was unbuilt and
cited `WI-512` as working *because* a committed `docs/cli-reference.md` already
existed. **It did not.** `git log --diff-filter=A -- docs/cli-reference.md`
returns `c54ddd10` — WI-512's own commit. The precedent built its reference from
nothing inside one work item.

fig: `git log --oneline --diff-filter=A -- docs/cli-reference.md` @ c54ddd10

So the price is an analogue, not a guess: WI-512 cost **+2,024 / −156 across 36
files**, and the header build is the same shape with a larger cell pass (71 rows
against 27), 21 modules needing an anchor first, a broken harvester to fix, and a
smaller mechanism because the anchor already exists. **Order 2,000 lines across
~35 files — one work item, not a program.** Filed on `OI-66` with the authoring
judgement inside the cell pass explicitly left unpriced.

A defect found while pricing and recorded rather than fixed:
`gen_arch_map.module_contracts` reads `handback.py` as declaring `IF-080`
although its docstring says "No `Contracts:` line, deliberately" — a negated
declaration admitted by a guard that only checks for the word.

### WI-526 — the reporting protocol, stated once

`IF-144`. A finding names its location; it carries a severity from the closed
set OK / SKIP / WARN / FAIL; an advisory never reaches the exit code; a declared
strict flag is the only promotion; an absent optional input exits zero and names
the absence. `owner = SR-157`, `req_refs` naming all ten restating rows, and no
existing row edited — (b) is state-and-sweep-nothing.

**The shape question answered on the evidence, not by a second ruling.** It read
first as a blocker: one `provider` or one `component` per row, against fourteen
providers splitting five and five across `CMP-006`/`CMP-007` with `SR-181` in
neither. Wrong axis. The protocol is what the harness presents at its package
boundary, and **all ten restating rows cite `B-05` unanimously**; 28 rows already
carry `interface_to_external = "B-05"`. The owner's direction had anticipated it
in saying an external far side is a boundary-line interface.

**Two clauses deliberately absent, both on the row's `rationale`.** *Every
degrade is named, never silent* is not stated: `SR-181` permits a silent degrade
where `SN-008` forbids one, so stating it would red an `Approved` row on day one.
The closed set is four dispositions, not the twelve tokens the checkers spell
them with — the row states the contract they honour, not a rename they do not.

**The row honours the rules it states.** First draft tripped four of the
registry's own advisories — 732 characters against the 500 ceiling, an argument
in `contract`, a date stamp in `notes`. Thinned to **466**, argument moved to
`rationale`, provenance dropped. The one remaining advisory is the accepted
directory-provider join `IF-025`/`IF-026` already carry.

### Gates

`trace.py` exits 0, integrity 0; IF advisories **43 before, 43 after** across
both registry-touching items. `check_docs --stale` OK over 1,112 docs and 1,447
links, 0 broken.

Full unfiltered suite: **3,060 passed, 15 skipped, 0 failed** (12m45s).
Commit bar: smoke **1,363 passed, 6 skipped in 23.95s**; wall-clock budget
**22.5 s against 60 s, within**.

**Two defects this session introduced and the suite caught, both fixed at their
cause rather than by moving a baseline:**

- `trace.py` grew 5 lines past its ratchet because the comment written for a
  one-line change was seven lines. The comment was shortened back to two; the
  module sits at its 5,819 baseline exactly. The baseline was not bumped.
- Python text-mode writes on Windows silently converted five source and registry
  files from LF to CRLF, which `test_old_kit_resync` caught byte-for-byte
  (`--force left a stale kit-owned checker in place: scripts/trace.py`). All were
  normalised back to LF; `git ls-files --eol` now shows no non-`ps1/cmd/bat`
  file as `w/crlf` apart from two pre-existing review artifacts from the
  2026-08-25 round. This is exactly the hazard the session protocol's
  "measure on a tree whose line endings match the index" rule names.

**A correction to the previous commit's message.** `d0f16357` recorded the
smoke wall-clock budget as breaching on this box, "pre-existing (HEAD also
breaches: 86.6 s)". That reading was **contention**, not a breach: those timings
were taken while a full unfiltered suite ran concurrently. Measured on a quiet
box the tier is **22.5 s** against its 60 s budget. There is no budget problem
here and none at HEAD.
