## 2026-08-29 — WI-530: the cell pass on the new shape (OI-67 slice 3)

Deferred open items: none — the questions this pass surfaced are recorded
below as slice-4 and slice-6 worklist, each with its row, not as rulings owed.

**Summary.** Every interface definition now lives beside the code. 132 of 136
rows have their contract stated in the owner's header — a module docstring, a
registry or config file's `#` header, a Markdown file's leading HTML comment,
a git hook — and the legacy `contract` cell is gone from those 132; every
`Contracts:` marker in the tree declares exactly the rows the registry owns
to its file; the owner-exact check warns on ONE row. The reference reads
**68 sources declare 132 seams; 132 carry a stated contract** (from 137
declarations / 2 stated at OI-66's close).

**How it ran.** Four Opus workers in parallel over disjoint owner-file
batches (the round is recorded whole at
[reviews/2026-08-29-oi67-slice3/](../reviews/2026-08-29-oi67-slice3/README.md):
brief, worklist, batches, reports, fold script). Each worker read the owner's
code before writing its bodies, trimmed the owner's marker to the registry's
id set, removed from other files' markers the ids the registry does not own
to them (50 files), proposed `data` where a finite alphabet exists, and
reported per row without touching the registry; every proposed `data` cell
was driven through `trace.if_data_advisories` by its worker — 0 findings.
The fold applied the four reports serially. Headers created where none
existed: `derive_stage.py`, `kitlib/station.py`, `acceptance_record.py`,
`handback.py` (its "declares no seam" paragraph was false — it owns IF-080),
`docs/requirements/open-items.toml`, `docs/runtime-flows.md`; READMEs created
as the declaration home of five directory owners (`docs/`,
`docs/requirements/`, `docs/reviews/`, `project-trajectory/registries`,
`project-trajectory/scripts`), the two existing ones headed. Marker trims
retired 24 + 11 + 17 stale declarations across 50 modules.

**Confirmed, not merely seeded.** The workers corrected 7 channels (`IF-010`
call → cli; `IF-139`/`IF-086`/`IF-121` stdout → exit-code — `check.py` reads a
step's return code, never its stdout; `IF-018` stdout → file; `IF-061`
bytes/consumers → call/requestors) and 4 far sides (`IF-097`, `IF-058`,
`IF-065`, `IF-053` — a stale importer list each). Moot reason cells deleted:
11 notes, 3 rationales — every one an argument about which requirement id
owned the row.

**The four rows still carrying a legacy `contract`, and why.** `IF-031`'s
owner is `docs/requirements/performance-budgets.csv`, and a `#` header at
the top of a CSV is read by `csv.DictReader` as the header ROW — five loaders
would lose `PB-ID`. Slice 2 shipped the CSV carrier in the grammar with no
comment-skipping loader; the root-cause fix is the loaders' (slice 6). The
other three are owned by `external:` parties (`IF-032` git, `IF-036` upstream
docs, `IF-041` the agent CLI): an external's surface is not ours to state in
a header, and the rule for where our READING of it is recorded (the
consuming module's docstring, or `data`) is slice 6's to set. The legacy
advisory counts exactly these four.

**Worklist for slice 4 (the split), from the workers' notes.** Duplicate
pairs that collapse to one row with two requestors: `IF-075`/`IF-127`
(`trace.reattest_model`), `IF-101`/`IF-116` (the watermark pair). Two-way
rows: `IF-079`, `IF-047` (the owner is written as well as read). Rows
bundling several media whose bodies state only the owner's own:
`IF-021`, `IF-022`, `IF-023`, `IF-025`, `IF-037`, `IF-045`. Undeclared arms
a row should exist for: `trace`'s exit code to `scripts/check` and its
`report.md`; `subagent_gate`'s stdin; `run_menu`'s argv and exit code;
`IF-004`/`IF-005`/`IF-012`'s exit-code halves; `IF-060`'s three kinds;
`IF-080`'s argv. Far sides that name the WRITTEN ARTIFACT rather than a
reader (`IF-019`, `IF-074`, `IF-140`) — systemic to generated-file rows, the
medium belongs in `data`, a ruling on the shape. `IF-037`'s readers are of
`docs/status.md`'s excerpt, not of `process.toml`. Six modules import
`spine_carrier` with no row (`acceptance_record`, `check_trajectory`,
`gen_arch_map`, `plan_coverage`, `spine_rules`, `traj_status`);
`census`/`dispatch`/`intake` import `schedule` with none; `integrate` calls
`score_reviews.parse_verdict` with none. The channel family
`IF-002`/`IF-006` (exit-code) vs `IF-001`/`IF-008`/`IF-069` (stdout) is
inconsistent for one consumer that reads both.

**Two latent defects found by writing headers, not fixed here.**
`gen_okf._doc_title_and_summary` skips only the first line of a leading HTML
comment, so a Markdown owner's contract header becomes its process-guide
summary (inert in this repo — `okf_export = false`); and the legacy cells of
`IF-055` (named retired `SCHED_*` constants), `IF-074` (a retired stamp and
mask) and `IF-103` (claimed the converter refuses to write on a lossy round
trip — it writes, then reports) were FALSE, and the bodies state current
behaviour instead.

**Ratchets.** Nine modules grew, docstring-only, each re-stamped with the
reason: `trace.py` +55, `check_trajectory.py` +36, `gen_arch_map.py` +39,
`agent_common.py` +16, `intake.py` +14, `bootstrap.py` +13, `agent_loop.py`
+8, `check.py` +8, `integrate.py` +8. Two new READMEs are reachable through
`docs/README.md`, now linked from `docs/status.md`.

**Measured at close — the number OI-66 left untaken:** the pass cost four
workers of roughly 210–280k tokens each (about 950k in all, 15–20 minutes
wall in parallel) plus the coordinator's fold; 136 rows, 68 owner files, 5
READMEs, 50 marker trims.

**Deviations from spec:** the smoke budget breached at close on a box whose HEAD timings breached identically (recorded above, not waived); the four legacy rows above stay, stated rather than
forced; `notes`/`rationale` were pruned only where a worker judged the cell
wholly moot (the trims of half-moot cells are slice-6 authoring).

**Byte deltas on budgeted files:** none touched.

**pytest totals:** full suite `python -m pytest -q -n auto`: 3075 passed, 15 skipped, 2 failed in 759.47s — both failures were fixtures keyed on IF-001's deleted `contract` cell (`test_trace_briefs`, `test_gen_open_items`), re-keyed onto its `data` cell and re-run green, plus one forward-only guard hit (a done id in `status.md`, scrubbed); smoke tier: 1366 passed, 6 skipped. **The smoke budget read OVER on this box at close — 98.9 s, 113.7 s, 117.5 s against 60 s — and the reading is ENVIRONMENTAL, not this change's:** the three slowest tier tests take 32 s at HEAD and 33 s here, `bootstrap`/`check_trajectory`/`trace`/`check_docs` time identically at HEAD (4.7/10.7/7.2/2.7 s vs 5.2/10.5/7.1/3.2 s), and the full suite that ran 9:41 an hour earlier ran 12:39 now on the same box. One machine, one data point; the budget is not moved, and the next session's first act is to re-measure on a quiet box. `check.py` harness: PASS; `check_docs --stale`: 0 broken.
