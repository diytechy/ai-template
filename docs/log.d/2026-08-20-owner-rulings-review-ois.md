## 2026-08-20 — The owner rules the five review-era briefs: the pending queue empties, five execution rows queue behind it

The owner ruled all five pending open items in one message (2026-08-20); each
row flipped `pending -> ruled` with the ruling recorded at the top of its
recommendation cell, and every ruling that owes a build got its executing WI
in the same commit. The pending band of
[open-items.html](../open-items.html) is now legitimately empty — and, per
OI-41's own founding evidence, "empty" is a claim this fragment backs by
declaring what was deferred: **nothing new was deferred by this session; all
five briefs closed to rulings**, four with execution rows and one with
nothing to build.

| OI | Ruling | Execution |
|---|---|---|
| OI-32 | **(d) THE GENERATED VIEW** — concern/hat refs on SR and LLR rows, effective sets DERIVED never copied, the component view generated, `detail_doc` retired. Owner's sequencing note recorded: the new field is NOT anticipated to be an attested cell, so it can be added AFTER the sitting without re-opening anything signed. Field name ("hats_ref" vs the brief's "concern_refs") is Phase 0's reconciliation. | **WI-484** (strong, spine, deliberately post-sitting) |
| OI-41 | **(e) as amended** — the three field-or-count arms (allow-file `OI-###` grammar hard at birth; the session-log deferral declaration, weak-by-design on record; the vacuity check re-aimed to NAME the contradicting entry) plus the folded-in ALWAYS-ON direction, its "always-on is not populated" caveat standing. | **WI-485** |
| OI-42 | **(b) + (e) + THE CAMPAIGN** — harvester tightened to literal `Implements:` first (shared `gen_arch_map` landing with WI-478; stale enforcement-audit row corrected in the same change; shipped guide re-worded onto the declared dial), reverse-coverage scanner report-only with the dial in `docs/process.toml` and 50% recorded as the target, then the campaign writes the tags with a code-review pass and raises the dial to a bar the tree clears; (c)'s revisioned marker RE-CONSIDERED at campaign close as the decay answer. | **WI-486** then **WI-487** (hard edge) |
| OI-43 | **(a) as recommended** — IF-seam TC citations promote to ERRORS from DevStg-Tests onward, migration allowlist seeded at the measured 115 with a burn-down expectation, landed BEHIND the wi455/WI-469 contract re-authoring; the shipped prose stays, the promotion makes it true. | **WI-488** (soft edges ~WI-469, ~WI-455) |
| OI-44 | **(c), DEFER TO THE PUBLICATION EVENT, trigger NAMED** — the first act that would publish this repository's history beyond the owner's own machines (public remote push, release, or a clone handed to a third party); re-answer the row before that act. Recorded beside it: (d) the fresh-history export is the preferred shape if the identities must not publish, and (b)'s rewrite decays with every SHA-citing session. **Documented so it does not resurface:** the ruled row is the record, and `docs/process.toml`'s `privacy_check` line now carries a pointer to OI-44 — a future review finding "privacy off + identities in history" resolves there. | none — nothing to build |

Bookkeeping in the same commit: watermark `WI` 483 → 488 via
`trace.py --bump-ids`; `docs/status.md`'s hand-authored sitting bullet
corrected (it still said "three pending briefs" — stale since OI-43/44 were
minted on 2026-08-19, and now superseded by the rulings); open-items.html,
the dashboard and the status block regenerated. The owner also directed a
readability review of the spine text ahead of signing — that runs as its own
sweep with per-row before/after in its own fragment, not here.
