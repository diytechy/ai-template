## 2026-08-29 — the owner rules OI-67 (a): one row, one direction, one kind — the cells go

Deferred open items: none — the one pending row was ruled by this entry, and
the decision surface is empty again.

The owner, on the filed brief: *"Sounds good, feel free to start implementing
the OI-67 recommendation."* That is (a) — the owner's own shape, sequenced as
the recommendation laid it out, with the driver directed to proceed and to
take the decisions the ruling leaves open (the same standing directive OI-66's
ruling carried: decide, escalate the high-risk ones to a cross-family
reviewer, file what needs review as its own document).

**What is ruled.** An interface row is ONE OWNER, its CONSUMERS, and a TYPED
statement of the information — nothing else. `provider`, `req_refs` and the
prose `contract` leave the row; `signal`/`signal_note` are replaced by a closed
`channel` and an optional short `data` alphabet; `owner` names the providing
THING (a module path, a file path, or an `external:` party) rather than a
requirement id; the definition of what the owner promises lives beside the
code, in the `Contract IF-###:` body WI-527 built, and nowhere else. Two-way
rows split. The 71-row cell pass ruled at OI-66 does not run in its old form —
its authoring is spent on the new shape instead.

**How it is executed.** As a program of six rows, sequenced so nothing is
removed before its replacement exists:
[plans/2026-08-29-if-row-shape-plan.md](../plans/2026-08-29-if-row-shape-plan.md)
is the plan of record and names the slices; the rows are filed under
`docs/work/queued/`. The shape in code lands first (readers, checks, the
kit's own registry converted mechanically with the legacy `contract` cell
kept readable until its content has a home); the header scanner widens to
the 31 non-module providers; the cell pass and the row split follow; the
adopter migration ships with the templates and `PROCESS.md` §8; the gate arms
last, once the kit's own tree is clean.

**Decisions the ruling leaves to the driver, stated so they can be reviewed
at close:** the `kind` vocabulary and the `data` cell's form rules; whether a
generated file's definition is declared by the module that writes it (yes —
the writer is the one scannable home); how a bundled row is split against
`carried_by`; and the converter's reach for adopters. Each is recorded in the
plan with the alternative it beat.
