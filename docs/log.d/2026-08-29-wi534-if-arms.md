## 2026-08-29 — WI-534: the arms the split surfaced — nine rows minted and stated (OI-67 follow-on)

Deferred open items: none — nothing here needs a ruling; the decisions the
program took alone stay filed in
[../decisions-for-review-2026-08-29-slices-4-6.md](../decisions-for-review-2026-08-29-slices-4-6.md).

**Summary.** The worklist the WI-531 and WI-533 records left standing is
done: nine rows (`IF-165`–`IF-173`) minted from the watermark and stated
beside their owners, **154 → 163 rows, the reference at 74 sources / 163
seams / 163 stated**, and `gen_arch_map` runs every target one invocation
names. Every far side was MEASURED before a row was written, and three of the
worklist's own assumptions did not survive the measurement (below).

**How it ran.** Two Opus measurement workers, read-only, over disjoint
families — A the harness family (`derive_stage`, `trace`, `check_perf`,
`gen_arch_map`), B the coordination family (`docs/log.d/`, `subagent_gate`,
`schedule`, `integrate`) — each returning a report in the slice-4 schema plus
the drafted bodies; the coordinator folded the registry with the slice-4 fold
script (each new row minted directly after the row it splits from) and then
two apply workers, in isolated worktrees while the full suite ran on the main
checkout, wrote the bodies and the code corrections; their diffs were applied
on top. The reports, bodies, worklist and fold are in the session's
scratchpad, not the tree — the registry rows and the headers ARE the record.

**What was minted, and what the measurement changed.** `IF-165`
`derive_stage`'s exit code: THREE consumers, not one — `check` (the
derived-stage step), `trunk_step` (its write-mode regen, refusing the regen
point on a nonzero code) and `kitlib/stage` (`--print` under
`derive_via_subprocess`); alphabet 0 and 1 alone, reproduced on scratch roots.
`IF-166` `docs/test/report.html`: the kit's own harness DOES write it
(`check.py`'s traceability step passes `--html` at `DevStg-Impl`), no kit
module reads it — `check_vocab` skips it, `integrate` deletes it as residue
— so the adopter alone, `B-05`. `IF-167` `docs/test/perf-report.md`, absent
here (this repo emits no perf metrics) and so a declared absence; the shipped
`gitignore.template` ignored it where this repo's `.gitignore` did not — both
lines added. `IF-168` `docs/log.d/`'s write-and-delete arm: NO kit module
writes a fragment — every one is hand-authored — so the requestors are
`trunk_step` (the unlink, all-or-nothing with the append, the deletion riding
the caller's refresh commit) and the adopter's session; `IF-156` lost its
deletion clause. `IF-169` the PreToolUse verdict payload on stdout, `B-04`
with the exit-code row; the fail-open arm prints NOTHING (its reason lives
only in the log) — a fact `IF-020`'s body had wrong. `IF-170` the decision
log under `out/`: it HAS a kit reader (`agent_loop`'s run banner counts its
lines and its fail-open entries), a defer never logs, and it was not
gitignored downstream — the template now ignores it and the module's prose
says "untracked cache". `IF-171` `schedule.py`'s argv and `IF-172` what it
prints; no exit-code row, because the answer is the listing and the code
alphabet is argparse plumbing. `IF-173` `integrate`'s in-process surface:
`dispatch`, `handback`, `lane` — `handback` does NOT call `branch_outcomes`
(a docstring mention only) and `lane` takes one symbol; seven `_`-prefixed
names cross the seam and are stated as part of it.

**Corrections that rode along.** `gen_arch_map.main` dispatched into
`sys.exit` on the first of `--backlink-coverage` / `--cli-doc` /
`--contracts-doc` it found, so one invocation naming both references reported
"CLI reference up to date", exit 0, over a stale interface reference — every
named mode runs, in order, and the verdict is the worst code (C901 21 → 19;
test added). `schedule simulate --jobs 0` raised an uncaught `ValueError`
(exit 1) — refused with 2 through argparse now — and the module's Usage put
`--root` after the subcommand, where argparse rejects it.

**Surfaced, not done.** Four of the seven private names crossing `IF-173`
(`_merge_ready`, `_claimed_specs`, `_claimed_wi_ids`, `_spec_frontmatter`)
carry the dispatcher's own decisions and read as the seam's real API — a
rename to file, not made here. `tests/integrate_fixtures.py`'s docstring
still says the shipped gitignore covers two `out/` paths (it covered three
before this slice and covers four now). A repo-wide `ruff check tests/` is red
on two pre-existing F401s (`test_agent_loop.py`, `test_trace_hats.py`) that
the floor's `format --check` does not see. The filing commit (`fb482e95`)
left the queued row without a `specref` for the one commit it was open —
`check_trajectory --strict` read R-E red there and clean again at this close.

**Ratchets.** Docstring-only bumps, each re-stamped with the reason:
`trace.py` 5998 → 6005, `integrate.py` 2612 → 2626; `gen_arch_map.py`
2223 → 2230 (+7, the composition and its comment) and its `main` re-stamped
DOWN 21 → 19. The seam-TC allowlist grew by nine reasoned entries (30 past
the seed; `if_tc_allow_hygiene_findings` reports the burn-down every run).
`tests/test_traj_parse.py`'s pin of this repo's untied `external:` rows widened
seven → nine (`IF-168`, `IF-171`: information coming IN from the adopter's
session, the decision-4.4 shape).

**Deviations from spec:** none — the row's title names the nine arms and the
`gen_arch_map` fix, all delivered.

**Byte deltas on budgeted files:** none touched.

**pytest totals:** smoke tier under Git Bash **1378 passed, 6 skipped in 88.12 s** (1384 collected against the 1390 stamp) — the budget read **88.8 s vs 60 s → OVER** on a box other sessions held at 50–80 %: environmental, recorded, not waived, the quiet re-measure still owed; the targeted runs the apply workers made in their worktrees: 96 passed (`schedule`, `subagent_gate`, `derive_stage`, `check_perf`), 141 passed (`gen_arch_map`, the two ratchets, `bootstrap`, `subagent_gate`), and the coordinator's 393 passed, 1 skipped over the pins the fold touches after the untied-rows pin was widened. Full suite `python -m pytest -q -n auto` on this tree: **3108 passed, 15 skipped in 1705.84s (0:28:25)**, exit 0 — and on the correction commit `7fc42a5a` before it, 3106 passed, 15 skipped in 2675.71s (0:44:35), exit 0, both on the loaded box. `check_trajectory --strict`: exit 0;
`trace --strict`: `interfaces=163 interface-findings=0`, the three standing
reds only; `check_docs --stale`: 0 broken; every generated document current.
