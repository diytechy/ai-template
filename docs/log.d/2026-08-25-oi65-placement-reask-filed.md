## 2026-08-25 — OI-65 filed: the placement re-ask gets a row, and OI-64 gets its sequencing

Deferred open items: **OI-65** — filed by this sitting and pending, the placement
re-ask with its two sub-decisions. **OI-64** — still pending; the measurement
filed earlier today did not rule it, and this sitting only added its sequencing
against OI-65 and named the one family the measurement did not sweep.

**Summary.** Owner-directed, no WI. Checking whether OI-64 carried its full
context after the (c) measurement turned up a gap that was not in OI-64 at all:
**OI-63's re-ask had no row.** OI-63 is `ruled`, so its card is off the decision
surface; WI-522 closed declaring `Deferred open items: none`; and the re-ask
survived only as prose inside a ruled brief and a numbered list in
`docs/status.md`. The one live pending row — OI-64 — then deferred to a question
nothing carried. That is verbatim the failure OI-62 named: *"a measured number
with no row to rule on is how a deferred question becomes a forgotten one."*
The measured number was 43,995 → 37,859.

**Filed: OI-65**, the re-ask on cleaned cells, in three parts.

1. **PLACEMENT.** Does the irreducible remainder move to a component-side
   contract header or stay in the cell — now asked on cells holding only
   crossing content. It carries what changed since OI-63 (the cells; the
   owner's file-far-side correction, which retires option (c)'s central plank;
   OI-64's one-definer rule) and what did not (the sizes — **8,975 characters
   of remainder over 71 rows**, **28,305 of restatement**, anchors on **57 of
   76** modules — because WI-522 moved only non-crossing content; the unbuilt
   target; the IF-055/IF-117 rot exhibits). A fifth option is minted: **(b')**,
   rule the header in principle and PRICE the generated reference and its gate
   before any cell moves, which is the honest form of (b) given that its
   precondition has now stood unbuilt through two rulings.
2. **THE DELETION QUEUE**, ten rows named: `IF-080`/`IF-088`/`IF-089`/`IF-136`/
   `IF-137` (bare plan-doc citations), `IF-090`/`IF-094` (citations resolving to
   nothing a reader can reach), `IF-117`/`IF-061` (rot, needing correction not
   deletion), `IF-127` (a row-internal duplicate).
3. **THE LINT ARM.** `trace.py`'s `IF_REASON_CELLS` is `("Notes", "SignalNote")`
   — nothing reads an IF `rationale`, on the cell that just went from 1 user to
   37. Riding with it: `IF-082`/`IF-083`/`IF-084` carry `wI-280`, which the
   citation-frame detector misses because `_WI_TOKEN_RE` is case-sensitive.

**The sequencing claim, stated so it can be disagreed with.** OI-65 recommends
being ruled **before** OI-64: OI-64's mint target is *"wherever OI-63's re-ask
puts contracts"*, so ruling it first mints a protocol contract into a home the
other ruling may move — and the one-definer rule makes that passage worse than
harmless. Nothing in OI-65 depends on OI-64's outcome, so the reverse order
costs nothing. Recorded on both rows.

**Also surfaced on OI-64, not acted on:** of the four restatement families it
bundles, the measurement swept three. **"Every degrade is named, never silent"**
(8 rows, SN-008's honesty property) was not swept, and what the drive saw
incidentally was ragged — one class of environment absence resolved three ways.
A crude shape count finds 69 `except` handlers in the checker family, roughly
half swallowing and continuing with a default; stated as a population estimate,
not a read, and only to say the clause is not obviously green.

**Deliverables.** `OI-65` filed; `OI-63` given a forward pointer to it (a ruled
row that defers a question should name where the question went); `OI-64` given
its sequencing note and its unmeasured-family note; `docs/id-watermark` bumped
`OI 64 → 65`; `docs/open-items.html` regenerated; `docs/status.md`'s owed list
re-pointed at the row that now carries it. **Deviations from spec:** none — the
sitting's instruction was to populate OI-65 as proposed.

**Byte deltas on budgeted files:** none touched.
