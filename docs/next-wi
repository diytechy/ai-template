# Next WI (process-options.md "Unattended operation") — the WI id the driver
# expects the coordinator to pick up next, in the docs/run-phase idiom (comment
# lines + one value on the last line). The managed BUILD branch reads it and, if
# that WI's row in docs/requirements/work-items.csv carries a BuildTier, uses it
# as the session's STARTING tier in place of the phase default (WI-126). The
# value may be a `;`-joined ordered DEV-SLICE BATCH of independent off-spine WIs
# — one session, one review round, strongest-member pin (WI-133); an
# absent/empty file, an unknown id, or an empty BuildTier = the phase default.
# The DRIVER maintains this alongside docs/status.md Next action.
# NOTE: WI-144 is `active`. Build ROUND 2 LANDED (log.md 2026-07-14). The fresh
# re-critique had already fired (048-CRITIQUE, CHANGES-REQUESTED, findings=5) plus a
# 046-REVIEW-A; round 2 shipped every buildable finding, all verified:
#  - [BLOCKER A1] leaf drill blocks now tabindex="0" (keyboard can open WI/module detail)
#  - [MAJOR T1]   the landing hero names the active WI (id+title) — zero tab switches
#  - [MINOR U3]   knowledge .kedge shares the drill --muted stroke token (light-mode parity)
#  - 046-REVIEW-A the <=3-tier flat dag_svg fallback now glyph-prefixes the .wid label
# test_gen_trajectory.py 75 passed; smoke 593 passed; gen_trajectory --check fresh.
# DEFERRED / OWNER-GATED (unchanged): [MINOR T2] Knowledge-tab density (a .knode/
# knowarrow re-spec, its own pass); the U5 anchor (048 refines it to "no cross-
# vocabulary colour collision") + phase-hue de-collision; the TC-HARDEN cases — OI-12,
# ratify at the phase-g2 close.
# NEXT IN THE LOOP: a FRESH re-critique of the rebuilt PROJECT_STATE.html (SR-047's
# loop, never the authoring build's self-assessment). It judges the round-2 fixes.
# WI-144 stays open until the critique APPROVEs; after that confirms round 2, only the
# owner ratification of U5 + TC-HARDEN remains, then the full gate bar / check.py
# --gate at close, and the whole spine rejoins G3.
WI-144
