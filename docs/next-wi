# Next WI (process-options.md "Unattended operation") — the WI id the driver
# expects the coordinator to pick up next, in the docs/run-phase idiom (comment
# lines + one value on the last line). The managed BUILD branch reads it and, if
# that WI's row in docs/requirements/work-items.csv carries a BuildTier, uses it
# as the session's STARTING tier in place of the phase default (WI-126). The
# value may be a `;`-joined ordered DEV-SLICE BATCH of independent off-spine WIs
# — one session, one review round, strongest-member pin (WI-133); an
# absent/empty file, an unknown id, or an empty BuildTier = the phase default.
# The DRIVER maintains this alongside docs/status.md Next action.
# NOTE: WI-144 (the v3 campaign closer) is `active` but PAUSED — its SR-047
# critique loop has EXHAUSTED the AGENT_CRITIQUE_MAX=3 budget still
# CHANGES-REQUESTED (042/048/052; log.md 2026-07-14). Under single-ratify the
# budget failure_action pauses that WI + surfaces the block for the phase-v3
# g2-close ratification sitting (OI-12) but KEEPS the loop RUNNING on non-dependent
# work — so next-wi moves OFF WI-144 to the next actionable non-dependent unit
# (WI-144 resumes for its final build round after the sitting rules OI-12).
# 052-CRITIQUE disposition (OI-12): BUILDABLE rubric-meeting [MAJOR A4] queued
# .detail .badge 2.56:1 · [MINOR A4] focus ring 2.05:1 · [MAJOR T4] .blab overflow
# · U4/U3/U1 polish; OWNER-GATED (recurring) U5 palette taxonomy · [MAJOR T2]
# Knowledge-tab density · 3 TC-HARDEN. Recommend the sitting ratify the TC-HARDEN
# first so one final build round mechanizes contrast instead of another hand pass.
#
# NEXT: WI-147 (coordinator graceful pause). WI-146 (ratification package -
# trace.py --ratify view + the warn-first brief lint + gate-advance/reviewer-prompt
# wiring + the [g2]-authors-IF recipe line) SHIPPED 2026-07-14 (log.md). The loop
# continues the actionable owner-intake backlog in id order: WI-147 (graceful
# pause) -> WI-148 (weekday blackout) -> WI-149 (lowest-gate-first warn) -> WI-150
# (planner-assigned BuildTier) -> WI-151 (throughline pointer). WI-144 (the v3
# closer) is `active` but PAUSED - its
# SR-047 critique loop EXHAUSTED AGENT_CRITIQUE_MAX=3 still CHANGES-REQUESTED
# (042/048/052); single-ratify's budget failure_action pauses it + surfaces the
# block for the phase-v3 g2-close sitting (OI-12) but keep_nondependent KEEPS the
# loop RUNNING on non-dependent work. With the buffer gone, that non-dependent work
# IS the owner-intake backlog (WI-146…151): all hard predecessors done (WI-145, the
# 2026-07-14 sitting, is done), so the trajectory --strict check treats them as
# actionable and a NEEDS-HUMAN park would be a STALE end-state it rejects. So the
# loop proceeds through them in id order. The g2-close sitting is DUE in PARALLEL
# (rule OI-12, sequence the campaigns) - if the owner would rather the loop hold for
# it, they rule OI-12 / reorder next-wi. Research-knowledge WI-152+ is a coherent
# campaign the owner sequences at the sitting: do NOT auto-start it ahead of the
# owner-intake WIs. WI-144 resumes for its final build round after the sitting rules
# OI-12, then the full gate bar and the spine rejoins G3.
WI-147
