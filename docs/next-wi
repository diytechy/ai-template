# Next WI (process-options.md "Unattended operation") — the WI id the driver
# expects the coordinator to pick up next, in the docs/run-phase idiom (comment
# lines + one value on the last line). The managed BUILD branch reads it and, if
# that WI's row in docs/requirements/work-items.csv carries a BuildTier, uses it
# as the session's STARTING tier in place of the phase default (WI-126). The
# value may be a `;`-joined ordered DEV-SLICE BATCH of independent off-spine WIs
# — one session, one review round, strongest-member pin (WI-133); an
# absent/empty file, an unknown id, or an empty BuildTier = the phase default.
# The DRIVER maintains this alongside docs/status.md Next action.
# NOTE (2026-07-15): WI-144 CLOSED on the OWNER's manual critique APPROVE
# (docs/reviews/074-CRITIQUE.md) — the automated critique loop had no browser
# backend (sessions 070-073 churned on it), so the owner rendered + judged
# PROJECT_STATE.html directly (the strongest Critique form). SR-052/053/054 ->
# Verified, the derived gate is back at G3, the v3 dashboard-ux campaign is
# complete. `docs/gate-policy` is now `autonomous` (owner directive) — no
# g2-close sitting for future campaigns.
# NEXT: the DAG-actionable off-spine backlog — WI-162 (parallel-dispatch
# design spec, strong) ->
# WI-165 (Process-tab circular loops, now
# unblocked). WI-159 (Knowledge-tab density) stays deferred.
# THEN (owner GREENLIT 2026-07-15): flow straight into the research-knowledge
# campaign WI-152..157 + WI-164 — no stop between it and the backlog; the DAG
# edges sequence it (WI-152 first, predecessor WI-145 done). Only the deferred
# main-decomposition (WI-080->WI-081) stays parked — do not auto-start it.
# Codex Sol builds are LIVE (codex on PATH + logged in 2026-07-15), so BUILD
# draws OPENAI-SOL at the strong tier (tier-up/cooldown still self-heal to Fable).
# NOTE (2026-07-15): the WI-161 review round fired the implementer-touched-
# review-path tripwire (the build's owner-ordered docs/agents-enabled edit);
# the autonomous design-check ruled GRIND-THROUGH (docs/reviews/076-DESIGN-
# CHECK.md) — run-phase is back to BUILD, WI-167 filed for the tripwire's
# meta-repo path-coverage gap (joins the backlog after WI-165).
WI-162
