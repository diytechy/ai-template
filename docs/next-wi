# Next WI (process-options.md "Unattended operation") — the WI id the driver
# expects the coordinator to pick up next, in the docs/run-phase idiom (comment
# lines + one value on the last line). The managed BUILD branch reads it and, if
# that WI's row in docs/requirements/work-items.csv carries a BuildTier, uses it
# as the session's STARTING tier in place of the phase default (WI-126). The
# value may be a `;`-joined ordered DEV-SLICE BATCH of independent off-spine WIs
# — one session, one review round, strongest-member pin (WI-133); an
# absent/empty file, an unknown id, or an empty BuildTier = the phase default.
# The DRIVER maintains this alongside docs/status.md Next action.
# NOTE: WI-144 is `active`. Build round 1 LANDED (commit "WI-144: …round 1",
# log.md 2026-07-14): five of the six rubric-meeting 042-CRITIQUE findings shipped
# and are verified — A4 (contrast, WCAG scan 0/8 fills fail), U4 (dead When detail
# panel rewired), A3 (status glyph), U3 (How-SW legend + #sw-detail), U1 (shared
# --nlabel/--nsub type scale). Full suite 742 passed; gen_trajectory --check fresh.
# DEFERRED one round: [MAJOR T2] Knowledge-tab density (249 flat nodes) — a faithful
# start-collapsed grouping is a .knode/knowarrow re-spec that breaks 4 tests + drops
# cross-type edges; it wants its own focused pass, not a rushed late-round change.
# OWNER-GATED (unchanged): the U5 uniformity anchor + phase-hue de-collision + the 3
# TC-HARDEN change-intake cases — ratify at the phase-g2 close (OI-12).
# NEXT IN THE LOOP: a FRESH re-critique of the rebuilt PROJECT_STATE.html (SR-047's
# loop, never the authoring build's self-assessment). It judges the five landed fixes
# and re-surfaces T2. WI-144 stays open until the critique APPROVEs; then the full
# gate bar / check.py --gate at close, and the whole spine rejoins G3.
WI-144
