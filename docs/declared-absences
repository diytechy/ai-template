# Paths this repo deliberately does NOT carry, each with the reason (WI-308).
#
# One line per entry: `<path> — <reason>`. Blank lines and `#` comments ignored.
#
# Two readers share it, which is the point — the fact is stated once:
#   - `tests/test_dogfood_sync.py` — the scaffold-omission declaration: a
#     `bootstrap.py` MAPPING destination that is absent here must be listed, and
#     an entry whose path MATERIALIZES must be removed (a test enforces both).
#   - `check_doc_refs.py` — prose naming one of these is UNTRACED, not dangling.
#     Before WI-308 those references were 12 of this repo's 22 dangling findings,
#     each of them a fact already stated here.
#
# This is a DECLARATION, not a suppression list: every line carries its reason,
# a materialized path fails the honesty test, and nothing here is exempt from
# the checker — it is reclassified, counted, and printable with
# `--show-untraced`. A path that is simply ROT does not belong here; it belongs
# fixed (`docs/next-wi`, retired by WI-180, is the worked example).
#
# A reason beginning `LIFECYCLE:` declares a path whose PRESENCE is a legal,
# documented state (a claim dir while work is claimed, a pause marker while
# paused): the honesty test exempts it from the materialize-guard — asserting
# absence deadlocked the §2.3 parallel-claims queue, since every composed tree
# holds the OTHER branches' claim dirs (WI-369) — while `check_doc_refs.py`
# still classifies prose naming it when absent. Everything unmarked keeps the
# exact guard, including every retired surface.
#
# Contracts: IF-072 — the interface seam this file declares (process.md §8; row of
# record in docs/requirements/interfaces.toml).
#
# Contract IF-072: the paths this repo deliberately does not carry, one
#     `<path> — <reason>` entry per line, with blank lines and `#` comments
#     ignored. A reference in prose to a listed path is reclassified as untraced
#     with the declared reason quoted back, in place of dangling. Fail-soft in the
#     safe direction: an absent file, and a line carrying no separator, declare
#     nothing and leave the path dangling, so a malformed entry can never silence
#     real rot. A reason opening `LIFECYCLE:` declares a path whose presence is a
#     legal state and is exempt from the materialize guard; any other listed path
#     that exists on disk is a finding.

docs/requirements/work-items.csv — retired at the Phase 2c authority flip (concurrency-restructure §2/§7): the work-item registry is the docs/work/ spec folder; prose naming the CSV before 2026-07-29 is history, and a migrating adopter converts theirs with scripts/wi_convert.py.
docs/work/archive/ — retired at WI-384's state-model flip (concurrency-v2 §B2): the folder held BOTH terminal states and so needed a `disposition` attribute to tell them apart, and splitting it into docs/work/complete/ and docs/work/cancelled/ is what deleted that attribute, its validator and both raise paths. Prose naming archive/ before 2026-08-01 is history. Deliberately NOT marked LIFECYCLE: this path must never materialize again, so the honesty guard here is the same reconciliation signal the loaders enforce — a spec closed into archive/ by a branch cut before the flip is a named, loud error, not a silent skip.
docs/archive/specs/complete/ — a destination concurrency-v2 §B2 NAMES but this repo deliberately does not carry: §B2 asked the spec-of-record home to mirror the terminal folders WI-384 gave the registry. WI-391 measured that mirror and REFUTED it (2026-08-01, review-confirmed): the spec→folder mapping is not total (92 of 111 archived specs resolve to complete, 3 to cancelled, 16 to nothing), it is contradictory for at least one file (research-knowledge.2026-07-29.md has citers in both terminal states), it has no regenerator to gate freshness against, and for every attributable spec docs/work/ already answers shipped-or-cancelled by location. So this path is absent BY DECISION, not by omission. OI-11 RULED 2026-08-01: RESTATED — §B2 now records the measurement in place (a decision about the DESIGN TEXT only; WI-391 itself is CANCELLED, and this path stays absent under the ruling). Do NOT delete this line to tidy up — the WI-391 spec's own title names this path, so removing the declaration turns it into a dangling reference (measured: +2 under check_doc_refs --strict). Deliberately unmarked, so the exact materialize-guard still applies: if the mirror is ever built, removing this line is the deliberate act that records it.
docs/archive/specs/cancelled/ — the second half of the same §B2 destination pair; see the line above for the measurement, the OI-11 referent and the reason neither line may be deleted while WI-391's spec text names them.
docs/dupes-allow — retired 2026-08-11 at the D-7 census teardown (owner ruling, repo-lock §2 D-7; executed WI-426): the duplication census, its detector `check_dupes.py` and the spine chain SR-039/LLR-036/TC-039 were DELETED, not disabled — one real catch at the one-time triage and none since, structurally blind to a DIVERGED copy, and 93% of its lines registering accepted idioms. Prose naming this file before 2026-08-11 is history and keeps its token; the ruling's forwarding pointer is docs/log.md's Decisions entry of 2026-08-11. F5 duplication is unbounded again by that ruling, with duplicated POLICY owing a behavioural pin in tests/test_rule_sync.py.
project-trajectory/scripts/check_dupes.py — the duplication census's detector, deleted at the D-7 teardown 2026-08-11 (see docs/dupes-allow above for the ruling and its evidence). Kit-relative prose writes it `scripts/check_dupes.py`; both forms name the same retired script, and an adopter who chooses to keep the check pins the last shipped copy out of git history (project-trajectory/ADOPTING.md, "Migration recipes").
tests/test_check_dupes.py — deleted with its subject at the D-7 teardown (18 tests; see the line above). A test module has no meaning without the module it tests, so this is the same retirement, not a separate loss of coverage.
tests/test_dupes_census_audit.py — deleted with its subject at the D-7 teardown (12 tests): a meta-audit over the census's own PROSE, whose 12-test cost against one real catch is part of the evidence the ruling was made on.
docs/requirements/open-items.csv — retired 2026-08-11 at the batch-2 carrier cutover (repo-lock §8.1, executed WI-431): the owner decision registry moved to docs/requirements/open-items.toml on the carrier OI-12 ruled for the spine, and the CSV was DELETED in the same commit — both homes at once is refused, not resolved by precedence. Prose naming the CSV before 2026-08-11 is history and keeps its token; the live registry is one suffix away and every kit reader resolves either. An adopter converts theirs with scripts/migrate_carrier.py (project-trajectory/ADOPTING.md, "Migration recipes").
docs/agents.csv — retired 2026-08-11 in the same cutover: the model routing registry is docs/agents.toml. Its three `#` comment lines survived byte-for-byte — TOML's comment is the same convention the CSV used — which is why the `# tag-rank:` line agent_route.load_tag_rank parses still resolves. Prose naming the CSV before 2026-08-11 is history.
docs/requirements/interfaces.csv — retired 2026-08-13, the LAST batch of the same carrier migration (OI-14 part B, executed WI-443): the seam registry is docs/requirements/interfaces.toml, and it waited for this ruling precisely so it converted ONCE, carrying its new columns (`signal`, `signal_note`, `rationale`) and its retired one (`status` — undeclared since it shipped, overlapping `stability`) in the same pass. The CSV was DELETED in the same commit; both homes at once is refused, not resolved by precedence. Prose naming the CSV before 2026-08-13 is history and keeps its token; an adopter converts theirs with scripts/migrate_carrier.py (project-trajectory/ADOPTING.md, "Migration recipes").
docs/requirements/components.csv — retired 2026-08-13 in the same batch: the component registry is docs/requirements/components.toml, carrier-only (no column changed). It waited on the partition ruling for the same reason — a registry converted before the ruling that rewrites what its rows ARE converts twice.


# --- Scaffold destinations a meta-repo has no use for -------------------------
GEMINI.md — no Gemini agent in use; OpenCode/KIMI/GROK read AGENTS.md natively
docs/process.md — the master PROCESS.md lives in project-trajectory/ (status.md non-goal)
docs/process-options.md — the master PROCESS_OPTIONS.md lives in project-trajectory/ (status.md non-goal)
docs/plan.md — superseded by the trajectory layer + parallel dispatch (WI-252 mutual exclusion)
docs/interfaces.md — cross-project contract index; skip for a standalone deliverable (its own header)
docs/requirements/procurement.csv — a meta-repo purchases no parts (opt-in, off-spine)
docs/requirements/assets.csv — a meta-repo ships no binary assets (opt-in, off-spine)
run.cmd — no product to launch (status.md non-goal; the launchers here are agent-resume.*)
run.sh — no product to launch (status.md non-goal)
run.command — no product to launch (status.md non-goal)
.githooks/pre-push — privacy-checked-repo backstop; docs/privacy-check declares off (owner-ruled out, WI-251)
.github/workflows/check.yml — test.yml's gate job runs the same check.py at the derived gate
src/ — the kit's product is project-trajectory/ + tests/, not an application tree
docs/kit-license — a stamp bootstrap.py writes into a SCAFFOLD; the kit itself is the source
docs/kit-profile — likewise a scaffold stamp, recording which kit rung a repo adopted
docs/kit-version — likewise a scaffold stamp; this kit checkout is the versioned source rather than an adoption of itself
docs/release-checklist.md — a scaffolded artifact for a repo that ships releases
docs/releases/ — gen_release_checklist.py --version files the stamped, signed checklist here; a meta-repo performs no DevStg-Release, so the directory is created by the first one that ships (see the line above)

# --- Retired surfaces: gone on purpose, and prose still names them -----------
# A retired path is a deliberate absence with a reason, so it belongs here rather
# than as a scatter of `path-ok` comments. Listing it also earns a guard for
# free: test_scaffold_omissions_list_is_current fails if the path ever
# MATERIALIZES, so recreating a retired surface trips a test.
docs/open-items.md — RETIRED by WI-322; the owner surface is docs/requirements/open-items.csv rendered to docs/open-items.html
docs/blackout — RETIRED by SN-028; the weekday blackout window is the `[policies] blackout` dial in docs/process.toml, and prose naming the one-word file describes the legacy home the migrator converts
docs/gate-policy — RETIRED by SN-028 with the rest of the one-word policy idiom; SN-029 then retired the enum itself; the dial is `[attestation] human_approval_through` (0-4) plus `keep_nondependent` and `final_review` in docs/process.toml (the one policy home). Design history, the SN-028 plan and closed WI specs legitimately name the legacy file — it is the vocabulary `bootstrap.py --migrate-config` converts, and a repo carrying BOTH homes is refused rather than resolved by precedence.
docs/push-policy — RETIRED by SN-028 with the rest of the one-word policy idiom; the dial is `[policies] push` in docs/process.toml (the one policy home). Design history, the SN-028 plan and closed WI specs legitimately name the legacy file — it is the vocabulary `bootstrap.py --migrate-config` converts, and a repo carrying BOTH homes is refused rather than resolved by precedence.
docs/review-policy — RETIRED by SN-028 with the rest of the one-word policy idiom; the dial is `[policies] review_rounds` in docs/process.toml (the one policy home). Design history, the SN-028 plan and closed WI specs legitimately name the legacy file — it is the vocabulary `bootstrap.py --migrate-config` converts, and a repo carrying BOTH homes is refused rather than resolved by precedence.
docs/privacy-check — RETIRED by SN-028 with the rest of the one-word policy idiom; the dial is `[policies] privacy_check` in docs/process.toml (the one policy home). Design history, the SN-028 plan and closed WI specs legitimately name the legacy file — it is the vocabulary `bootstrap.py --migrate-config` converts, and a repo carrying BOTH homes is refused rather than resolved by precedence.
docs/secrets-scan — RETIRED by SN-028 with the rest of the one-word policy idiom; the dial is `[policies] secrets_scan` in docs/process.toml (the one policy home). Design history, the SN-028 plan and closed WI specs legitimately name the legacy file — it is the vocabulary `bootstrap.py --migrate-config` converts, and a repo carrying BOTH homes is refused rather than resolved by precedence.
docs/privacy-review — RETIRED by SN-028 with the rest of the one-word policy idiom; the dial is `[policies] privacy_review` in docs/process.toml (the one policy home). Design history, the SN-028 plan and closed WI specs legitimately name the legacy file — it is the vocabulary `bootstrap.py --migrate-config` converts, and a repo carrying BOTH homes is refused rather than resolved by precedence.
docs/guardrails-policy — RETIRED by SN-028 with the rest of the one-word policy idiom; the dial is `[policies] guardrails` in docs/process.toml (the one policy home). Design history, the SN-028 plan and closed WI specs legitimately name the legacy file — it is the vocabulary `bootstrap.py --migrate-config` converts, and a repo carrying BOTH homes is refused rather than resolved by precedence.
docs/trajectory-check — RETIRED by the 2026-08-11 overturn of WI-423 (owner: absence-as-declaration is confusing); the dial is `[checks] trajectory_check` in docs/process.toml. Design history, closed WI specs and the checkers' own migration-window fallback legitimately name the legacy file — it is the vocabulary `bootstrap.py --migrate-config` converts, and a repo carrying BOTH homes is refused rather than resolved by precedence.
docs/interfaces-check — RETIRED by the 2026-08-11 overturn of WI-423 (owner: absence-as-declaration is confusing); the dial is `[checks] interfaces_check` in docs/process.toml. Design history, closed WI specs and the checkers' own migration-window fallback legitimately name the legacy file — it is the vocabulary `bootstrap.py --migrate-config` converts, and a repo carrying BOTH homes is refused rather than resolved by precedence.
docs/components-check — RETIRED by the 2026-08-11 overturn of WI-423 (owner: absence-as-declaration is confusing); the dial is `[checks] components_check` in docs/process.toml. Design history, closed WI specs and the checkers' own migration-window fallback legitimately name the legacy file — it is the vocabulary `bootstrap.py --migrate-config` converts, and a repo carrying BOTH homes is refused rather than resolved by precedence.
docs/okf-export — RETIRED by the 2026-08-11 overturn of WI-423 (owner: absence-as-declaration is confusing); the dial is `[checks] okf_export` in docs/process.toml. Design history, closed WI specs and the checkers' own migration-window fallback legitimately name the legacy file — it is the vocabulary `bootstrap.py --migrate-config` converts, and a repo carrying BOTH homes is refused rather than resolved by precedence.
docs/live-status — RETIRED by the 2026-08-11 overturn of WI-423 (owner: absence-as-declaration is confusing); the dial is `[checks] live_status` in docs/process.toml. Design history, closed WI specs and the checkers' own migration-window fallback legitimately name the legacy file — it is the vocabulary `bootstrap.py --migrate-config` converts, and a repo carrying BOTH homes is refused rather than resolved by precedence.
docs/subagent-gate — RETIRED by the 2026-08-11 overturn of WI-423 (owner: absence-as-declaration is confusing); the dial is `[checks] subagent_gate` in docs/process.toml. Design history, closed WI specs and the checkers' own migration-window fallback legitimately name the legacy file — it is the vocabulary `bootstrap.py --migrate-config` converts, and a repo carrying BOTH homes is refused rather than resolved by precedence.
docs/gate — RETIRED at the stage unification's slice 5 (WI-493, 2026-08-18; docs/log.md "Slice 5 — one vocabulary; docs/gate retires"): the one-word derived-gate file was DELETED with the bar axis it published, and the derived surface is `docs/stage`, written by scripts/derive_stage.py from the artifact states and never hand-set. The design record (spine-restructure-2026-08-08, concurrency-restructure, concurrency-v2), the repo-lock decisions and the spine-approval records legitimately name the retired file — they describe the machinery as it was — and a repo carrying both surfaces is refused rather than resolved by precedence.
docs/specs/derived-gate-model.md — ARCHIVED 2026-07-20 to docs/archive/specs/derived-gate-model.2026-07-20.md when the derived-gate design spec stopped being spec-of-record (repo-lock §D-11; the deference was re-pointed at the archived copy and at process.md §4 as the ruled authority, owner 2026-08-12). Prose naming the pre-archive path is history and resolves one directory away; the un-archiving spine-restructure §S5 proposed was never performed.
docs/requirements/stakeholder-needs.md — retired 2026-08-11 at the D-5 carrier cutover (`bb69a622`, "four tiers to TOML, sources deleted"): the SN registry is docs/requirements/stakeholder-needs.toml. Prose naming the .md form before that commit is history and keeps its token; an adopter converts theirs with scripts/migrate_carrier.py.
docs/guardrails/core.md — the model-keyed guardrails prepend's SOURCE file, absent here because the `[policies] guardrails` dial in docs/process.toml is `off` (dormant, repo-lock §worker-prompt): the matcher and its prepend still ship, so prose naming the destination describes the shipped layer's input, not a promise that THIS repo carries it. Deliberately unmarked, so flipping the dial on and writing the file is the deliberate act that removes this line.

# --- Declared policy files where ABSENT is a legal, documented state ----------
# Naming one of these in prose is CORRECT: the reader treats absence as the
# stated default, so the file exists only when a repo overrides that default.
docs/review-cadence — absent = `slice` (per-slice review), the default cadence
# Phase 5 retirements (concurrency-restructure §6/§7): design prose and the
# §5.6/§2.3 contracts legitimately name these; their ABSENCE is the design.
docs/run-state — retired at Phase 5 with the dispatcher, its only writer; the stop banner + exit codes carry the outcome
docs/pause — the legacy untracked pause marker; retired at Phase 5 (the tracked docs/work/pause is the one home)
docs/work/pause — LIFECYCLE: present only while a pause is DECLARED; absence = not paused, the documented default
docs/work/active/ — LIFECYCLE: holds per-branch claim directories only while work is claimed; empty/absent at a drained stop
docs/work/active/wi508-architectural-remap — LIFECYCLE: one of the per-branch claim directories the line above describes; named in docs/handoff-2026-08-31.md as the claim that lane held, and absent once the lane drained.
docs/work/active/wi484-concern-refs-component-view — LIFECYCLE: the same, for the WI-484 lane (docs/handoff-2026-08-31.md).
docs/test/report.md — LIFECYCLE: the traceability matrix trace.py writes on demand (gitignored, never committed — .gitignore "Generated reports"); prose naming it describes the generator's destination, and absence until a run writes it is the documented state
docs/test/report.html — LIFECYCLE: the html render of the same matrix, written when the bar's check.py passes --html to the trace step at DevStg-Tests/DevStg-Impl (gitignored, never committed — the same .gitignore block); declared tool-residue since WI-407 (measured holding the wi-402 lane at unload, 2026-08-02), and absence until a bar run writes it is the documented state
docs/test/perf-report.md — LIFECYCLE: the perf comparator's report (scripts/check_perf.py), rewritten by any run that reaches a comparison; this repo emits no docs/test/perf-metrics.json, so every run SKIPs before the write and the absence is the documented state until a product measurement step is wired
docs/test/evidence — LIFECYCLE: the harness-written test-evidence record (scripts/record_test_evidence.py), the ONE input to the DevStg-Release rung via `kitlib.stage.evidence_verdict`. It is written only on a green harness exit and is bound by value to the tree it was measured on, so its absence — no green recorded against this tree — is the ordinary state, and prose naming it (docs/enforcement-audit.md, docs/registry-machinery-reference.md) describes the writer's destination.
tools/wi_convert.py — the converter's pre-2c meta-repo home, named as history in the spec; it moved to project-trajectory/scripts/wi_convert.py
# The dispatcher split (concurrency-v2 SA4.2): drive.py renamed dispatch.py
# with lane.py extracted; historical records (closed WI specs) and the
# not-yet-regenerated trunk surfaces legitimately name the old paths until
# the connectivity batch (SA9.1) retires them.
scripts/drive.py — renamed scripts/dispatch.py (+ scripts/lane.py) at the dispatcher split; closed specs name the old kit path as history
project-trajectory/scripts/drive.py — renamed project-trajectory/scripts/dispatch.py; LLR-143's Module pointer follows in the SA9.1 connectivity batch, after the trunk-side arch-map regeneration drops the old inventory entry
tests/test_drive.py — renamed tests/test_dispatch.py with the module it pins; closed specs name the old path as history
prompts/ — the SCAFFOLD destination for the kit's prompt templates (plan §8), absent HERE by construction. `scripts/prompts.py` resolves templates script-relatively as `KIT/prompts/`, where KIT is the parent of the scripts dir: downstream that is `<repo>/prompts/` (which bootstrap.py's MAPPING rows create), and in this meta-repo it is `project-trajectory/prompts/` — the kit's own home, which IS present and IS what every test and every session here loads. A copy at this repo's root would be a second home for the same files with nothing keeping the two in step, i.e. exactly the drift the one-home rule exists to prevent. The downstream path is not left unproven: tests/test_prompts.py bootstraps a scaffold and asserts every shipped template lands and loads from it.
prompts/README.md — see the `prompts/` line above; the authoring rules ship to the scaffold's prompts/ dir, and this repo reads them at project-trajectory/prompts/README.md.
prompts/worker.template.md — see the `prompts/` line above (kit home: project-trajectory/prompts/worker.template.md).
prompts/reviewer.template.md — see the `prompts/` line above (kit home: project-trajectory/prompts/reviewer.template.md).
prompts/critique.template.md — see the `prompts/` line above (kit home: project-trajectory/prompts/critique.template.md).
prompts/adjudicate-amendment.template.md — see the `prompts/` line above (kit home: project-trajectory/prompts/adjudicate-amendment.template.md).
prompts/adjudicate-first-approval.template.md — see the `prompts/` line above (kit home: project-trajectory/prompts/adjudicate-first-approval.template.md).
prompts/adjudicate-disposition.template.md — see the `prompts/` line above (kit home: project-trajectory/prompts/adjudicate-disposition.template.md).
prompts/adjudicate-red-tc.template.md — see the `prompts/` line above (kit home: project-trajectory/prompts/adjudicate-red-tc.template.md).
prompts/adjudicate-consolidate.template.md — see the `prompts/` line above (kit home: project-trajectory/prompts/adjudicate-consolidate.template.md). The fifth routed adjudication brief, SHIPPED by WI-583.
prompts/dual-plan-planner.template.md — see the `prompts/` line above (kit home: project-trajectory/prompts/dual-plan-planner.template.md).
prompts/dual-plan-critic.template.md — see the `prompts/` line above (kit home: project-trajectory/prompts/dual-plan-critic.template.md).
prompts/dual-plan-arbiter.template.md — see the `prompts/` line above (kit home: project-trajectory/prompts/dual-plan-arbiter.template.md).
# --- A shipped layer this repo turned OFF for itself --------------------------
# Not a retirement: the code, its check step and its documentation all still
# ship. What is absent is this repo's own INSTANCE of the generated artifact,
# because the dial that produces it is off here.
docs/okf/ — the OKF knowledge bundle `gen_okf.py` writes, absent here since 2026-08-18 because this repo set `[checks] okf_export = false` in docs/process.toml (owner directive): 551 generated files, 27% of the tracked tree, answering every spine grep twice. The LAYER is unchanged and still shipped — gen_okf.py, check.py's `okf` step, trunk_step's `okf` regen (which self-skips on `docs/okf/ absent`) and the dashboard's Knowledge tab (which self-omits when the bundle is absent, LLR "a source-less repo remains byte-stable") — so kit prose, the README generated-artifacts table, closed WI specs and the design record all legitimately name this path: they describe the shipped generator's destination, not a promise that THIS repo carries it. Deliberately NOT marked LIFECYCLE, so the exact materialize-guard still applies: with the dial off nothing regenerates the bundle, and a `docs/okf/` that reappears is either a hand-added tree no freshness check gates or the deliberate act of flipping the dial back — in which case deleting this line is part of that act. `docs/stack.ini`'s `[generated]` row for the path stays: it declares OWNERSHIP (the trunk lane owns it when it exists), and its enforcer census (tests/test_generated_freshness_wiring.py) reads the declaration, not the disk.
docs/archive/last_approved/README.md — LIFECYCLE: the `last_approved` snapshot's prose stamp, scaffolded into every new project by bootstrap's MAPPING (owner directive 2026-08-15; docs/plans/2026-08-15-baseline-snapshot-design.md §A3). THIS repo has not signed yet, so the directory does not exist here: `copy_live` refuses to CREATE it, and the first snapshot rides the owner's signing commit via `intake.py snapshot --seed` — after every pending row has been ruled, so nothing blesses text nobody read. Marked LIFECYCLE because its presence is the ordinary post-signing state; the moment this repo signs, the path materializes and stays.

# WI-455 (sitting-2 decision 8, owner 2026-08-13u): the architecture way-station
# retired. The authored narrative (Shape of the product + Runtime flows) moved
# to docs/runtime-flows.md; the structure derives live from the registries +
# source AST into PROJECT_STATE.html. Records citing the old path stay verbatim.
docs/architecture.md — retired at WI-455; narrative moved to docs/runtime-flows.md, structure derives into PROJECT_STATE.html

# --- Session-log fragments: docs/log.d/ is a STAGING dir, not a home ----------
# A fragment lives in docs/log.d/ only until the trunk step folds it into
# docs/log.md, which is why docs/log.d/ is deliberately NOT a record prefix in
# check_doc_refs.py: it is judged strictly while its author can still edit it.
# The consequence is that a CITATION written against a fragment outlives the
# fragment. Each line below names a fragment that was written, compiled, and
# removed; the record it carried is in docs/log.md, so the citing spec still
# points at a real record and re-pointing every one of them would edit closed
# WI specs to say nothing new. A fragment that was NEVER written is rot and is
# fixed in place instead (three were, at WI-568/WI-571/WI-508).
docs/log.d/2026-08-22-wi484-hatrefs-backfill.md — compiled into docs/log.md and removed; cited by docs/work/partial/WI-484-concern-refs-component-view.md
docs/log.d/2026-08-23-wi484-writer.md — compiled into docs/log.md and removed; cited by docs/work/partial/WI-484-concern-refs-component-view.md
docs/log.d/2026-08-23-wi484-component-view.md — compiled into docs/log.md and removed; cited by docs/work/partial/WI-484-concern-refs-component-view.md
docs/log.d/2026-08-23-wi511-hats-optional-keys.md — compiled into docs/log.md and removed; cited by docs/work/partial/WI-484-concern-refs-component-view.md
docs/log.d/2026-08-23-wi484-amend-guard.md — compiled into docs/log.md and removed; cited by docs/work/partial/WI-484-concern-refs-component-view.md
docs/log.d/2026-08-28-owner-rulings-oi64-oi65.md — compiled into docs/log.md and removed; cited by docs/work/partial/WI-508-architectural-remap-program.md. The ruling's durable home is the `OI-64`/`OI-65` rows in docs/requirements/open-items.toml (executed as `IF-144` via `WI-523`–`WI-526`); the surviving citation here is a plain code span, not a link.
docs/log.d/2026-08-30-owner-rulings-oi68-oi69.md — compiled into docs/log.md and removed; cited by docs/work/partial/WI-540-adjudicator-retention-layer.md
docs/log.d/2026-08-31-owner-rulings-oi74-75.md — compiled into docs/log.md and removed; cited by docs/work/queued/WI-557-delegated-decisions-record.md

# --- Synthetic paths inside test fixtures -------------------------------------
# A fixture that builds a fake git listing names paths that are DATA, not files.
# Adjudication rows quoting the fixture quote those names verbatim.
src/widget.py — a synthetic path inside tests/test_verdict_record.py's fake `<mode> <type> <sha>\t<path>` listing fixture, not a file; quoted verbatim by the WI-586/WI-587 adjudication rows that read the fixture. `src/` itself is already declared above (the kit's product is project-trajectory/ + tests/).


# --- Kit package paths written without their `scripts/` segment ---------------
# `kitlib/` is a package INSIDE the kit's script dir, so its portable form is
# `scripts/kitlib/<mod>.py` (which resolves kit-relative) and a bare
# `kitlib/<mod>.py` resolves nowhere. Live prose is corrected in place; the one
# entry below is in a DATED APPROVAL BRIEF, which `check.py --approval-immutable`
# forbids editing once minted — so the fact is declared here instead.
kitlib/spine.py — the sn_cited_ids one-home module, written package-relative in the immutable dated brief docs/ratify/2026-08-23-spine-approval.md; its real home is project-trajectory/scripts/kitlib/spine.py and the portable spelling is scripts/kitlib/spine.py. This path can never materialize at the repo root: a copy there would be the second home the one-home rule exists to prevent.
