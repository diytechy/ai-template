# Handoff 2026-09-03 — supervise the mechanized loop from the paused frontier

Written at trunk `73479cf3` (branch `contract_split`, the integration trunk),
tree clean, nothing pushed (`push = "human"`). The frontier is PAUSED at
`docs/work/pause` (commit `319f374a`); the loop process is stopped; no lane
worktrees or `wi-*` branches exist.

## 1. State you inherit

- **Merged today, by the loop plus hand steps:** WI-578 (amendment adjudication
  over LLR-158/203/204; MEANING on all three, re-anchor found blocked, no act
  taken) at `69a83efa`, and WI-579 (the verdict carrier, tree identity,
  `adjudication_review` dial) at `b5735bb8` after eleven review rounds.
- **Minted at those merges:** WI-584 (spine — the snapshot's scoped writer vs
  its unscoped refusal; WI-578's successor), WI-585 (amendment adjudication over
  LLR-045/140), WI-586 (first-approval adjudication over LLR-207/208).
- **Landed out of band since 2026-09-02:** the `restructured` terminal state,
  list-valued `supersedes`, the backlog restructure (WI-579..583 minted,
  eight rows archived under `docs/archive/work/restructured/`), three
  adversarial review rounds applied, and — owner ruling this date — a
  committing DESIGN-CHECK now arms the review round itself (`87421a12`).
  Records: `docs/log.d/2026-09-02-*.md`, `docs/log.d/2026-09-03-*.md`, plan of
  record `docs/plans/2026-09-02-backlog-restructure-and-consolidation.md`.
- **Frontier when unpaused** (`schedule.py ready --explain`): WI-585 and WI-586
  (adjudications; the dispatcher admits judgements first, exclusive, one lane
  each) → WI-584 (spine, exclusive) → WI-580 (P8, the worker/reviewer briefs)
  → WI-551 → WI-541 → WI-581 → WI-570 → WI-583 → WI-582 → the P2/P3 rows →
  WI-545 last.

## 2. Owner rulings of 2026-09-03 (do not re-litigate)

1. **No round cap yet.** A loop-held page-human re-arms DESIGN-CHECK and resets
   its tally (`agent_loop.page_consequence`, `RoutingState.apply_decision`);
   only the 40-session budget stops a lane. Owner: leave it; watch for a
   builder capitulating to INVALID reviewer feedback — that is the concern.
2. **The `implementer-touched-review-path` tripwire** fires when a lane writes
   generated files under `docs/reviews/` (WI-579's rollup did). Meta-repo
   artifact; ignore while a supervisor watches.
3. **The blackout is per SESSION** (`agent_loop.wait_out_blackout` in
   `after_session`), so it stops an in-flight lane's next session, not only
   new claims. Does not match the owner's intent; not touched yet.
4. **Applied:** DESIGN-CHECK → REVIEW, no BUILD session in between (`87421a12`).

## 3. Launch

```
cd /Users/diytechy/Documents/ai-template
git status --short            # must be empty
git rm docs/work/pause && git commit -m "unpause: <reason>"   # reviewed deletion; regenerate open-items/status first if the hook reds
./agent-resume.sh > <scratch>/loop-<date>.log 2>&1 &            # NEVER in a foreground compound command (2-min tool timeout killed it once)
```

Regenerate before the unpause commit if the hook's `open-items`/`status-map`
steps red: `gen_open_items.py`, `gen_trajectory.py --status`, `gen_trajectory.py`.
The loop survives the harness's 10-minute background limit (verified today).
Locks in `out/` from a dead loop are stale and ignored.

Watch the log with a filter anchored to the loop's own lines, or the reviewer
transcripts flood it:

```
tail -n 0 -f <log> | grep -E --line-buffered "^(=== (session|worker)|dispatch: |integrate: |handback: |intake: |session [0-9]+: outcome|review round: |loop exit|Traceback)"
```

## 4. What to expect, and what to do

- **WI-585 / WI-586 are the first adjudications under the NEW gate.** Under
  `[attestation] adjudication_review = "when-minting"` they owe a round only if
  the verdict drafts a spine/high-risk successor. If the merge slot still
  refuses one for a missing `docs/reviews/WI-<n>-REVIEW-A.md`, the migration
  window in `integrate._verdict_gate` accepts a legacy rollup: compile it from
  the round files with the governing line LAST, commit it as the lane's newest
  commit, relaunch. Precedent: `docs/reviews/WI-578-REVIEW-A.md`.
- **A refresh conflict on generated/record files** (PROJECT_STATE.html,
  docs/stage, docs/status.md, docs/log.md, docs/id-watermark): on the lane,
  `git merge <trunk sha>`, keep BOTH sides of every `docs/log.md` hunk, take
  the per-space MAX in `docs/id-watermark`, `git checkout --theirs` the three
  generated ones, regenerate (`trace.py --bump-ids`, `derive_stage.py`,
  `gen_trajectory.py --status`, `gen_trajectory.py`, `gen_verdict_rollup.py`),
  commit, re-note the rollup so it is newest, then from the TRUNK root:
  `.venv/bin/python project-trajectory/scripts/integrate.py --root . integrate`
  (`--root` before the subcommand; never run it from inside a lane).
- **A finished branch that the run did not drain** (the run exited on a
  refusal): same integrate command from the trunk root once the loop is dead.
- **UNLOAD INCOMPLETE because the lane has a real `.venv/`:**
  `git worktree remove --force <lane> && git branch -d <branch>`. Check with
  `readlink` first — a SYMLINK `.venv` must not be followed.
- **Provider outages:** OpenAI (Sol/Terra) usage limit resets 2026-09-06 ~22:40
  UTC-adjusted per the banner; opencode-go returned server errors all day.
  Rounds then run Opus-on-Opus with `heterogeneity=relaxed`, recorded per round.
- **Stop and page the owner** (write the tracked pause, let the lane finish) if:
  a lane passes ~5 CHANGES-REQUESTED rounds with findings that do not converge
  or that the rework keeps declining; a reviewer's findings are not reproducible
  and the builder still capitulates; a refresh reds on product code (not
  generated files); anything writes `docs/archive/last_approved/` from a lane;
  the dispatcher claims a batch and the worker banner names one id (the WI-580
  defect until it lands).
- **Known reds to ignore:** `check_trajectory --strict` has one pre-existing
  ERROR (schedule→trace seam, WI-582's); `check_complexity --mode enforce` is
  red on `agent_common.commit_telemetry` (21) and `agent_loop.worker_endstate`
  (18), introduced by the WI-579 merge, armed only from DevStg-Impl.

## 5. Traps this Mac has

Bare `python` does not exist — use `.venv/bin/python`. The lane pre-commit hook
prints a loud ruff format-SKIP; harmless. `spec_move.py` and `integrate.py`
must run from the trunk root. `intake.py sweep` is structurally unusable
(sweeps every terminal folder). A worker session that runs the full suite
in-turn takes ~10 min and can read as a stall.

## 6. Kickoff prompt for the next session

> Read `docs/handoff-2026-09-03.md` end to end, then `CLAUDE.md` and the
> `session-protocol` skill. You are the supervising session for the mechanized
> loop: unpause the frontier with a reviewed deletion of `docs/work/pause`
> (reason: the design-check change landed at 87421a12; resume at the generated
> frontier), launch `./agent-resume.sh` in the background with its output to a
> scratchpad log, arm the anchored monitor from §3, and hold the loop's hand
> per §4 — hand-compile a legacy rollup only when the gate asks for one,
> resolve refresh conflicts on generated files the §4 way, drain from the trunk
> root when a run exits without merging, and stop with a tracked pause when
> any §4 stop condition fires. Report each merge and mint as it lands, with the
> round count and heterogeneity of its review. Do not re-litigate the §2
> rulings. Never push.
