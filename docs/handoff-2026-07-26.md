# Handoff — 2026-07-26 (launch a new session from here)

**Branch `dualplan-routing-fix`. Not pushed. Working tree clean except
`docs/pause` (untracked, machine-local, and it must STAY — see §3).**

Read this, then [status.md](status.md). This file is the *session* handoff: what
just happened, what is half-done, and the traps. `status.md` stays the
forward-only working surface and the registries stay authoritative — where they
disagree with this file, **they win** and this file is stale.

> **Updated after a second session the same day** (the `WI-318/319/320/321`
> batch + `123-CRITIQUE`). §§1–3 are current; §2 now carries both sessions and
> §4 the frontier as it stands.

---

## 1. Start here (the 60-second version)

```
python project-trajectory/scripts/check.py --gate G2 --jobs 0   # expect PASS
python -m pytest -q -n auto -m smoke                            # expect green
```

**`perceptual-stale` is CLEAR** — `123-CRITIQUE` post-dates the last render
change. It re-reds the moment `gen_trajectory.py` or `shoot.mjs` changes again;
that is budgeted, not a regression (§3).

**Where things live now** — one thing moved this session, so check yourself here
before searching for a file that no longer exists:

| You want | It is |
|---|---|
| pending owner decisions + what owes a ratification/re-attest | **`docs/open-items.html`** (generated) |
| ...their source | `docs/requirements/open-items.csv` |
| what's next / the frontier | `docs/status.md` (generated block + hand-written intent) |
| what happened and why | `docs/log.md` (session record + the Decisions log) |
| ~~`docs/open-items.md`~~ | **retired** — WI-322. It is gone; don't recreate it. |

## 2. What this session did

Seventeen commits. In order of what a successor most needs to know:

- **WI-317** `5a9c17a` — the dashboard's descend arrow was 1.06:1 against the
  fill it was drawn on. Fixed by giving it the same per-fill `--ring` ink the
  focus ring uses. **Design fact worth keeping:** an SVG `<marker>` renders from
  the `<defs>` tree and *cannot* see the referencing node's custom properties —
  hence one marker per ink, not one shared marker.
- **WI-314** `ebc9b0f` — T6 theme-lock bound to `LLR-117`/`TC-122`; it leaves the
  critique. Live critique anchors are now **T2, T4, T5, T8**.
- **WI-308** `85a1744` — dangling doc refs 22 → 0, then `[step:doc-refs]` wired
  at G3. Introduced `docs/declared-absences` (paths this repo deliberately does
  not carry, each with its reason) — read by both `check_doc_refs.py` and
  `tests/test_dogfood_sync.py`.
- **121-CRITIQUE** `8484f34` — one render critique on the batched work, via
  **`codex`/OpenAI** (OpenCode-Go was dead again). **CHANGES-REQUESTED f=3** →
  filed as WI-318/319/320, the current `P1` frontier.
- **WI-322** `41b228a` + `910edac` + `faa3cba` — the big one: `open-items.md`
  retired for a registry + a generated view. Then an adversarial review found
  **10 findings including a BLOCKER**, all reworked. See §5.
- **WI-321** — *filed, not done*: a rule that SR text must be stand-alone (no WI
  ids, no process-doc citations). SR-049/SR-054 were cleaned; SR-026/SR-060 are
  still dirty and are `Verified`, so cleaning them flips rows `Modified`.

### The second session (same day): the 121-CRITIQUE batch, closed out

- **WI-318** — drill sub-labels are now FITTED to the box they are drawn in
  (`_fit_lines`): 15 arch-root SN blocks had been rendering their whole
  stakeholder need on one unwrapped line, starting outside the left edge.
  Column right-sizing could never absorb it — it clamps at `MAX_TIER_COL`.
- **WI-319** — the next-work card stopped budgeting titles at 60 characters;
  the clause renders whole and wraps, and a residue past the declared bound
  discloses through a **native `<details>`** (no script, so the affordance is
  assertable from markup rather than needing the browser harness).
- **WI-320** — T8's **objective** half bound (`LLR-120`/`TC-125`): 264 shipped
  wires, 0 through an unrelated node box. Its perceptual half was split off as
  **WI-323** rather than proxied.
- **WI-321** — the rule shipped (`standalone_sr_advisories` + `PROCESS.md` §3).
  The row-cleaning half is **WI-324**, deliberately not done — see §4.
- **123-CRITIQUE** — one dispatch, `codex`/OpenAI again. **CHANGES-REQUESTED
  findings=1**: T2/**T4**/T5 pass, T8's one finding **is WI-323, already
  filed**. Three findings became one and T4 flipped to a pass. `perceptual-stale`
  clear.
- **WI-325** filed (`P1`): the committed re-attest brief has **no freshness
  gate** and went stale twice in one day. Both catches were a human noticing.

## 3. Three standing constraints — do not undo these by accident

1. **`docs/pause` stays.** A render WI delivers `SR-054`
   (`Verification=Critique`), so each train the dispatcher reserves needs its own
   CRITIQUE APPROVE — while a whole-document critique also judges anchors the
   *other* render rows own. Proven live. Work render rows **attended, as one
   batch**, dispatch **one** critique after the last render commit, then resume.
   The ruled way out is decomposition, not a gate exception — and it is working:
   the render frontier is now **one row** (WI-323), the residue **two clauses**,
   not two anchors.
2. **`perceptual-stale` is CLEAR** — `123-CRITIQUE` post-dates the render change.
   It re-reds on the next `gen_trajectory.py` / `shoot.mjs` change; budget a
   re-dispatch with every render fix, and never silence the check. **Verify it
   AFTER committing the critique** — the staleness check passes silently and
   vacuously on an uncommitted one.
3. **The re-attest window is open with 8 rows** (`SR-049/052/053/054`,
   `LLR-105/TC-108`, `SR-055/LLR-056`). The derived gate reads **G2 on purpose**
   because of it. The G3-only steps drop out while it is open — don't let it
   sprawl, and don't "fix" the G2 by flipping rows without the owner. **This is
   why WI-324 exists and is not done**: cleaning `SR-026`/`SR-060`/`TC-055`
   flips three more rows into a window the owner has not yet sat with.

## 4. What is owed, and by whom

**Owner only** (the loop will not do these):

- **The re-attestation sitting.** Read `docs/open-items.html` §2 (or the brief at
  `docs/ratify/2026-07-26-reattest.md`), then flip each `Modified` row to
  `Verified` (bless) or `Planned` (evidence no longer verifies) in a reviewed
  commit. The gate re-derives G3 by itself. **Do not regenerate the view with a
  different baseline before reading it** — it self-stamps `--since a5052a913`
  and reuses that; overriding it is what the review's BLOCKER was about. The
  brief was **refreshed on that same baseline** at the end of this session (it
  was missing six chain rows: `LLR-118`/`TC-123` and `LLR-119`/`LLR-120`/
  `TC-124`/`TC-125`) — 89 insertions, **zero deletions**, so depth grew and
  nothing collapsed. **WI-325** exists so the next agent does not have to catch
  that by eye a third time.
- **WI-324** is yours to schedule, not the loop's: it flips three more rows
  `Modified`, so land it **at** the sitting.
- **Merge to `main`** for `dualplan-routing-fix` (+ decide
  `guardrails-fable-method`), and **push** (push-policy: human).
- **OI-7** (review cadence) — parked by its own recommendation.

**Next agent session** — the frontier, in build order:

| WI | What | Note |
|---|---|---|
| **WI-325** `P1` | The re-attest brief has no freshness gate (stale twice in one day) | `--check` must compare against the baseline **the file declares**, never re-derive one — re-deriving is the WI-322 BLOCKER |
| **WI-323** | Roadmap DAG lane separation (T8's perceptual clause) | render — attended, then ONE critique. Read `LLR-120`'s Detail first: it carries the two measured starting points, and the row forbids mechanizing the clause into a crossing-count proxy |
| **WI-324** | Clean `SR-026`/`SR-060`, de-triplicate `TC-055` | **owner-scheduled** — three `Modified` flips, so it lands at a sitting |

Next free id: **WI-326**. Next review number: **124**.

## 5. WI-322 in detail (the part most likely to bite you)

`docs/open-items.md` is retired. `docs/requirements/open-items.csv` is the source
and `gen_open_items.py` renders it — plus **every `Draft`/`Modified` spine row's
per-cell word-level before/after** — into `docs/open-items.html`. New `open-items`
step in `check.py` and in the pre-commit floor.

**Invariants you must not break:**

- **One computation, two renderers.** `trace.reattest_model` owns the git
  archaeology and the cell comparison; `reattest_lines` renders markdown, the
  view renders HTML. `gen_open_items` also imports `gen_trajectory.pending_block`
  rather than re-deriving what is pending. If the view and `trace.py --ratify`
  ever disagree, **the brief is authoritative and the view is the bug**.
- **The view self-stamps its baseline** and *reuses it* when a later run passes
  no `--since`. This is not a nicety: without it the unattended loop's
  regeneration silently collapsed 43 chain-row diffs to 18 and `--check`
  certified the loss. `--since ""` is the explicit opt-back-to-auto.
- **An empty attestation section means CHECK THE BASELINE**, never "nothing
  changed". A pre-regime amendment (one that landed while the row still read
  `Verified`) sits *before* the auto-derived baseline.
- **OI ids have been reused historically** (`OI-8`, `OI-9` each name two
  different decisions). Ruled history is therefore **not** backfilled into the
  registry — pending rows plus forward tombstones; the Decisions log is the
  record.

**The adversarial review ([122-REVIEW-A](reviews/122-REVIEW-A.md))** returned
CHANGES-REQUESTED findings=10; all are fixed and nine guards are mutation-proven.
Two lessons generalize beyond this WI:

1. **A guard that reads a constant instead of the artifact tests your intention,
   not your output.** My theme "drift guard" compared against a dict nothing
   rendered from — nine of twelve emitted tokens were rewritten and it stayed
   green. Second occurrence in two days (WI-293 was the first).
2. **Two fixes for one defect can mask each other.** The CRLF bug needed three
   seams (strip at source, LF-only write, normalized compare); with all three in,
   *no* single mutation turned any test red until I split the test so each
   isolates one fix.

## 6. Traps this session paid for

- **The commit bar runs the WARN tier.** A module can land in the arch map, in no
  component, with an undeclared cross-component import, and the pre-commit floor
  stays green. Run `check.py --gate <gate>` before closing any module-adding WI.
- **Never `--no-verify`.** The floor caught three of my own errors in one
  session (a malformed CSV row, IF rows declared-but-unfiled, a stale dashboard).
- **`docs/pause` cannot stop a running worker** — it is a *dispatcher*-scope
  control. Only killing the worker stops one.
- **Check a branch for orphaned FILES before deleting it**, not just for merged
  work — the last cleared train held two `docs/iteration/` logs that existed
  nowhere else.
- **A `Modified` section with no cells is a signal to check the baseline**, never
  a row that is free to bless. A bad `--since` fabricates a brief; no `--since`
  empties one. Both fail silently in front of a human about to attest.
- **`refs/llm/` is empty and the 2026-07-26 train worktree is cleared.** Older
  `../ai-template-trains/` worktrees from 2026-07-24 are still on this machine,
  deliberately untouched.

### Traps the SECOND session added

- **Measure escaped text as glyphs.** A geometry guard that counts the emitted
  string raw reads `&#x27;` as six characters and `&gt;` as four, and invents
  overflow on every apostrophe in the registry. Unescape before measuring.
- **A guard that cannot fail is not a guard, and one that passes both before and
  after may still be right.** Of the four T4 guards, three fail against the
  pre-fix emitter and the fourth passes both ways *by design* — it guards
  over-application, not the defect. Say which is which, or a later reader
  "fixes" the wrong one.
- **The artifact claim is not the algorithm claim.** `_detour_d` returns a
  least-bad fallback when no lane clears, so unit tests proving the router
  detours do **not** prove nothing shipped through a box. Sweep the artifact.
- **A ratchet asking for a simplification often has one available.** The
  complexity ratchet tripped on `render_console`; five per-pipe console loops
  collapsing into one over their concatenation fixed it, rather than a bump.
- **The re-attest brief has gone stale twice.** Until WI-325 lands, regenerate it
  (on its declared `--since`) whenever spine rows change, and check the diff is
  insertions-only.

## 7. Verification state at `faa3cba`

```
full suite ......... 1579 passed, 7 skipped
smoke .............. 434 passed
check.py --gate G2 .. PASS except `trajectory` (perceptual-stale only)
trace --strict --strict-integrity ... SN=25 SR=110 LLR=116 TC=119
                                      orphans=0 integrity=0 interfaces=73
check_docs ......... OK (268 docs, 802 links, 0 broken)
check_doc_refs --strict ... exit 0 (0 dangling)
registry ........... 320 WIs · 304 done · 3 retired · 4 queued · 8 Modified rows
```
