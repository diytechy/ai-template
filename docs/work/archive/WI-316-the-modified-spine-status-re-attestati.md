+++
id = "WI-316"
title = "The Modified spine status - re-attestation as registry state, not commit-message prose (owner-ruled 2026-07-26). Nothing durable tracks that a re-attestation is OWED: the flag is the phrase RE-ATTESTATION PENDING in commit messages and log headings, invisible to every checker - the escalated-in-conversation-filed-nowhere failure mode one layer down. Ruling: the marker belongs in the requirements registries (the current state of the WHAT), not the WI layer (work over time). A third recognized Status value, Modified, canonized so both Modified and Draft surface to the owner and pull the derived gate back for re-attest. The gate-pull is already free - derive_gate computes a decomposed non-Verified SR as G2 today - so the work is the predicate (is_modified, F5-mirrored, test_rule_sync-pinned), the modified=N basis count, the pending-projection source (e) line per Modified SR, three warn-tier consistency guards (orphaned-child flag, llr_status_advisories exemption, staged amend-without-flip), the doc statements (PROCESS.md within budget, templates, gate-advance skill, enforcement-audit row), and the backfill: SR-052/053/054 flip to Modified as the first live use, phase 3 honestly derives G2 until the owner sitting flips them back. Accepted trade stated in the spec: a Modified window runs the G2 bar, exactly like a Draft window - re-attest promptly. SURFACING (owner requirement at filing, 2026-07-26): the pending projection gains one pointer line per Draft SR (ratification owed) AND per Modified SR (re-attest owed) - both classes, per the ruling's own sentence - each naming the brief command; the depth lives in a new trace.py --ratify modified scope (the WI-146 machinery extended, not a new surface) emitting a per-row per-cell BEFORE/AFTER diff, before = the row at the most recent revision where it read Verified, git-derived, grouped by owning SR - a sitting cannot bless a delta it cannot see. Proven on the backfill batch's real 2026-07-26 diffs. Rides IF-050/IF-051/IF-052/IF-021; no new seam."
workstream = "process"
sr_refs = ["SR-049"]
buildtier = "strong"
priority = 1
safety_class = "spine"
order = 313
+++

## Deliverable

Delivered end to end 2026-07-26 (spec archived: docs/archive/specs/WI-316.2026-07-26.md). is_modified in trace.py+derive_gate.py (F5-mirrored, test_rule_sync-pinned); modified=N on the compared basis line; pending-projection source (e) - one pointer line per Draft SR (ratification owed) and per Modified SR (re-attest owed) in the freshness-gated pure region; three warn-tier guards (Modified exempt from llr_status_advisories; modified_chain_advisories for an unflagged owning SR; staged_spine_findings amend-without-flip warn in the hook, suppressed on same-commit SR flip); trace.py --ratify modified - the per-cell before/after re-attestation brief, git-derived last-Verified baseline + --since, flip excluded from the diff, off-git degrade; docs: PROCESS.md section 7 canonical semantics (+754, re-stamped), PROCESS_OPTIONS phase-cadence pointer (+230, re-stamped), gate-advance both flips, registry-hygiene fix-its, enforcement-audit row upgrade, README pull-back line. SPINE: SR-049 chain amended + flipped Modified same-commit (first live use); BACKFILL SR-052/053/054 Modified - derived gate G2 deliberately (per-phase 1=G2;3=G2, modified=4) until the owner sitting; committed brief docs/ratify/2026-07-26-reattest.md reproduces the real 8fcf530-forward diffs from git. RE-ATTESTATION PENDING commit-message prose superseded by the marker.
