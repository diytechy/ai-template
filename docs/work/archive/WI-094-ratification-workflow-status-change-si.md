+++
id = "WI-094"
title = "Ratification workflow - Status-change sign-off"
workstream = "docs"
needs = ["WI-091"]
order = 93
+++

## Deliverable

Docs/skill only (spec §6/§10.6): ratification is now a reviewed Status-change commit, not a docs/gate bump. The gate-advance skill's 'active-gate mechanism' section rewritten - docs/gate is GENERATED by derive_gate.py (computed from artifact states), regenerated after a ratification, freshness-guarded by the derived-gate step - plus a new 'Ratification = a reviewed Status-change commit' section: the acceptor marks a batch ratified (Draft->Planned on the SR, or moving an SN out of the draft section) and that commit IS the sign-off; composes with the gate-authority levels (attended = each batch; single-ratify = once at the phase [g2] close; autonomous = a fresh-context reviewer's verdict); an agent may make the ratifying commit governed by the level. The 'Sync before you bump' section retitled/reworded to 'ratify' (no more bump). gate-policy.template + the meta docs/gate-policy comment rewritten: gates are derived, the level governs WHO makes the ratifying commit, and the policy value itself stays hand-set (docs/gate is generated, never hand-edited). Skill source edited -> .claude/.agents fan-out re-synced byte-identical (bootstrap --sync) + INDEX.csv regenerated (the description changed). No spine change, no re-attestation impact, no byte-budgeted file touched.
