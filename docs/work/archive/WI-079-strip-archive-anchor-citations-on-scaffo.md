+++
id = "WI-079"
title = "Strip archive-anchor citations on scaffold"
workstream = "scripts"
needs = ["WI-010"]
order = 78
+++

## Deliverable

WI-079 (2026-07-13): the 2026-07-12 review's M7 - kit scripts carry comment/docstring provenance citations into this meta-repo's docs/archive/ REVIEW docs (THREAD_<n>_REVIEW.md, REVIEW_GRIND_A/B/FULL.md) tagged with a finding code (A5, C7, F4), which resolve HERE but dangle for a downstream reader whose scaffold copies the scripts WITHOUT docs/archive/. Owner-ruled: bootstrap drops them as it copies (provenance stays in the kit; downstream gets the copy-ready comment). Implemented strip_provenance(text) in bootstrap.py - four+ anchor-SCOPED regexes (each requires an archive-doc name, which never appears in code, so a `foo()` call is never touched; REVIEW_GRIND_[A-Z]+ deliberately excludes the REVIEW_PHASES identifier and the hyphenated REVIEW-A/REVIEW-B phase names) covering every citation SHAPE found: whole-parenthetical, leading-clause-in-paren (keeps prose: '(C5; verbatim across the kit)'->'(verbatim across the kit)'), trailing-clause-in-paren, a citation wrapped across one comment-continuation line, a whole comment line that is only the citation (line dropped), a citation opening a comment line before prose ('# (F4). Kept...'->'# Kept...'), and a bare 'See\\n# THREAD_52_REVIEW.md F5.'. Wired into the copy branch for .py sources via write_bytes (keeps LF byte-for-byte on Windows, unlike write_text; 3.8-safe). Verified: all 25 anchors across 11 scripts stripped (0 survive), REVIEW_PHASES / REVIEW-A/REVIEW-B untouched, every transformed script still compiles, no punctuation debris on changed lines. Two new tests in test_bootstrap.py (unit: every shape + the must-not-touch cases; integration: scaffolded scripts carry no anchor while kit source still does + compile). SCOPE held to the owner-ruled REVIEW_*/THREAD_* review-finding anchors: the design-doc references (AGENT_ROLES, IMPROVEMENT_PLAN) are a different, out-of-scope class, surfaced for owner triage (candidate fold into WI-098). No downstream migration; the meta-repo's own scripts are unchanged (it does not scaffold its own scripts). Full derived G3 gate PASS (tests 697 passed/3 skipped at 91.54%, dupes/derived-gate/trajectory all green; arch-map+dashboard regenerated for the new strip_provenance symbol). No new SN/SR/LLR/TC (proceed at G3), no byte-budgeted file touched.
