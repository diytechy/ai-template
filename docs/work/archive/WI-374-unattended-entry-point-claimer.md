+++
id = "WI-374"
title = "Restore the scheduling front end the Phase 5 dispatcher deletion took with it: nothing invokes `integrate.py claim` (its only other occurrence in scripts/ is bootstrap.py:1289, the MAPPING row that ships the file), so the walk-away loop is automatic at the END (integrate.py integrate drains unattended - it merged WI-372 through the full 40-step bar with no human step) and MANUAL at the FRONT - a human must pick the next WI, claim it, and launch the worker. A plain agent-resume double-click runs agent_loop.py with no role and exits 2 with a map (driven 2026-07-31 with a no-op AGENT_CMD so nothing could launch). Build the THIN driver that joins the four existing, independently-proven parts - schedule.py ready (IF-053) -> integrate.py claim (IF-080) -> agent_loop.py --wi (IF-015) -> integrate.py integrate (IF-080) - one serial lane, adding ORDERING only and no new authority: every refusal stays where it already lives (tracked pause, dirty trunk, the SpecRef/status-prose claim rungs, the composed-tree bar, the RULING-7 verdict gate, push-policy). Explicitly NOT rebuilt: worktree pools, reservations, train grouping, docs/run-state - the deleted 4,042-line dispatcher is the worked example of what this must not become, and approaching its shape is an escalation, not a build."
workstream = "scripts"
buildtier = "strong"
priority = 2
safety_class = "ordinary"
+++

## Deliverable

`scripts/drive.py` (397 lines by the size-ratchet's splitlines method, stdlib only) — the serial claim->build->integrate driver — plus a 15-line delegation in `agent_loop.py`: a plain launch (no role flag) now takes the coordinator lock and runs the drive loop instead of refusing with the map (IF-015 amended to v3). Each cycle re-derives the ready frontier from the registry (so a WI filed MID-RUN is picked up in the same run), claims the next queued WI in build order via `integrate.py claim`, launches one worker session (`agent_loop.py --worktree <wt> --wi <id>`) on the claimed branch's worktree, and drains the serial merge queue at the full gate bar. A parked claim from an interrupted run resumes on relaunch; an empty frontier drains residue and exits 0 with the drained banner; the tracked pause is re-checked at the top of EVERY cycle (exit 8); any composed refusal (claim rung, red bar, verdict gate) stops the run loudly; NEEDS-HUMAN propagates as exit 7 with the claim left parked; the driver's own stall guard aborts when cycles leave the trunk unmoved; an unwired AGENT_CMD refuses BEFORE anything is claimed — but only when work actually needs a worker, so an inert scaffold with an empty queue still drains to 0; a dirty trunk refuses at the top of every cycle (the claim rung's refusal, hoisted so the parked-resume path meets it too); a half-completed claim (active specs with no branch ref) is a named fail-closed refusal, never a false "queue drained". Never pushes — no flag exists to ask it to. Shipped in bootstrap MAPPING + README kit-contents + test_bootstrap lists; 10 new tests in tests/test_drive.py (slow tier), including end-to-end against the REAL composed-tree bar green and red. Net new production code ~280 lines against the 4,042 the dispatcher deletion removed.
