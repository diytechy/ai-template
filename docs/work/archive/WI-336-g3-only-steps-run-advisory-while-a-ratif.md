+++
id = "WI-336"
title = "G3-only steps run ADVISORY while a ratification window is open (OWNER RULING 2026-07-27). A first cut IS LANDED but 127-REVIEW-A refuted it on three points, so this row is queued, not done. (BLOCKER 4) It never runs the promised stronger --require-verified traceability variant: the advisory list is built from steps() already specialized to the CURRENT gate, and the new test asserts traceability is NOT advisory, entrenching it - build the list from the HIGHER gate's step table so gate-dependent command variants are used. (MAJOR 5) window_open() fires on drafts>0, which is ordinary G0/G1 state, contradicting the 'a project genuinely at G1 is not nagged' claim in the code comment, status.md and the log - either discriminate a real attestation window or narrow the claim. (MAJOR 6) module-coverage runs advisory while its producer tests+coverage is excluded, and _clear_stale_coverage_report only clears when the producer is in the GATING plan, so it can grade a stale coverage.json as current evidence. The RULING itself stands and includes --require-verified: the owner overruled the driver's carve-out, correctly, because a Planned row also fails that check and that is real signal."
workstream = "scripts"
buildtier = "medium"
priority = 1
safety_class = "ordinary"
order = 333
+++

## Deliverable

DONE 2026-07-27, on the second attempt: the first cut landed as CODE while its row stayed queued, because 127-REVIEW-A refuted three of its claims. All three fixed. BLOCKER 4 - the advisory plan is now built by ASKING steps() for each higher gate's OWN table (advisory_plan(gate, plan, steps_at)) instead of filtering the current gate's. The step table is specialized to the gate it was built for, so traceability built at G2 carries no --require-verified and no filter over it could ever produce the stronger variant the owner explicitly ruled IN; the old code therefore ran it NOWHERE while a test asserted traceability was not advisory, entrenching the defect. A step already gating is now skipped only when its COMMAND is identical, so the G3 form runs advisory alongside the G2 gating one; higher gates are walked highest-first with one entry per step name, which also fixed a duplicate traceability at G1 (G2 and G3 forms both queued). MAJOR 5 - window_open no longer fires on any drafts>0, which is ordinary G0/G1 state and contradicted its own docstring. modified>0 is conclusive alone (Modified is DEFINED as a post-attestation amendment, so it cannot exist pre-ratification); drafts>0 additionally requires some phase in the per-phase breakdown to sit ABOVE the computed level, which is what distinguishes a mature repo opening a new phase from a project that has never ratified anything. An unphased repo with drafts is treated as ordinary - conservative on purpose, since a warn tier nobody wants is one everybody learns to ignore, and the case that motivated the ruling is a modified window, caught unconditionally. MAJOR 6 - module-coverage follows its producer out of the advisory pass: it grades coverage.json, which only the excluded tests+coverage writes, and _clear_stale_coverage_report only clears a stale report when the GATING plan has the producer, so advisory module-coverage could grade an unrelated earlier run's report as current evidence. Three new/rewritten guards, each mutation-proven against the shipped source and each reddening exactly the test that names it: reverting to the skip-if-name-gating filter reds the traceability-variant guard, firing on any draft reds the early-drafting guard, and dropping module-coverage from ADVISORY_EXCLUDE reds the selection guard. The owner ruling behind the WI is unchanged and was correct. || AMENDED 2026-07-27 by 128-REVIEW-A. MAJOR 3: the MAJOR-5 fix still fired on an early MULTI-PHASE repo (computed=G0 per-phase=1=G1;2=G0 - some phase above computed, but nothing had reached the G2/G3 bar the advisory tier reports). Fixed here: the discriminator now requires max(per-phase) >= G2 AS WELL AS above computed, with the counterexample pinned as a guard and mutation-proven. The residual FALSE NEGATIVE is structural and filed as WI-341 (a Draft in a previously-mature SINGLE-phase repo erases the evidence the rule reads). MINOR 5 fixed: --list printed only the gating plan while the same invocation executed the advisory tier, so it now lists the advisory steps separately, marked not-gating, with a scaffold-driven guard asserting --require-verified is visible there and that the section disappears when the window closes.
