+++
id = "WI-338"
title = "Correct the FALSE CLAIMS 127-REVIEW-A refuted (BLOCKER 1 + BLOCKER 3). (1) The CPU cap's 'every concurrent run shares ONE ceiling' is asserted unconditionally in conftest.py's docstring, docs/status.md, docs/log.md, the archived WI-335 spec and the WI-335 Deliverable - but the code has THREE outcomes: compatible trees share the job, a FOREIGN process tree falls back to its own private 50% (so two runs can total ~100%), and an API failure runs uncapped; POSIX gets os.nice(5), which is not a cap at all. The reviewer's own run hit the fallback. Either implement a real machine-wide ceiling or narrow every assertion to what the code delivers. (2) WI-334's 'MOST of the blocks are the deliberate CLI preamble' is numerically false - reconstructed 34 CLI-preamble vs 50 other drifts - and 164 sanctions were signed on that premise, with the spec's own audit boxes left unchecked. Needs a truthful per-block classification, which WI-337 must land first (the counts are meaningless until the fingerprints are environment-independent)."
workstream = "docs"
needs = ["WI-337"]
buildtier = "medium"
priority = 1
safety_class = "ordinary"
order = 335
+++

## Deliverable

DONE 2026-07-27. BOTH refuted claims corrected at every place they were signed. (1) THE CPU CAP. Measured rather than reasoned about: with a first process tree holding the named job, two independent probes both landed in a PRIVATE job while a CHILD of the first landed in the SHARED one. So Windows' rule (assignment is permitted only into a job that is EMPTY or already in the process's own parent job chain) means concurrent runs MULTIPLY the cap rather than total it - the parallel-train-worktree case the promise was written for is exactly the case it does not cover. What IS delivered is a TREE-WIDE ceiling for one run, which still answers the original problem (a worker count bounds workers, not the subprocess-per-test slow tier). Narrowed in conftest.py's docstring (now opening with an explicit what-this-does-and-does-not-guarantee block naming all four outcomes incl. the uncapped API-failure path and POSIX's os.nice(5) being a priority bump, not a cap), docs/status.md, docs/log.md's WI-335 entry, the archived WI-335 spec, and the WI-335 Deliverable. MAJOR 2 closed too: the live-process test's OR over shared/private is KEPT (it asserts the RATE, which is right) but its comment no longer claims to check both, and the guard that was missing now exists - test_a_second_independent_run_gets_its_own_ceiling asserts first-tree SHARED, child SHARED, second-independent-tree PRIVATE *and warning*, with the first assertion explicitly present so a fall-back-everywhere bug cannot satisfy the rest vacuously. Mutation-proven twice: forcing a private job reds the SHARED assertions, silencing the fallback warning reds only the warned=1 assertion. (2) THE CENSUS CLASSIFICATION, which WI-337 unblocked. All 208 blocks classified by READING them - the classifier refuses to finish while any block is UNCLASSIFIED, and it took four passes to get there. True distribution: cli 77 (37%), intra-module 64 (31%), spine-loader 21, markdown-table 11, declared-file 11, git-wrapper 9, wi-registry 5, module-path 4, okf-row 2, import-fallback 2, graph-walk 1, skills-materialize 1. So the CLI preamble IS the largest single class but is 37%, NOT 'MOST' - and nearly a THIRD of the census is not F5 duplication at all, it is intra-module debt WI-280 owns, which is the finding the original claim hid. The audit is recorded IN docs/dupes-allow, grouped, one section per class with its count and the reason the duplication is accepted, because that is where the next reader of a sanction looks; the header states plainly that --emit-census emits UNGROUPED lines so a future re-stamp must re-derive the grouping or say it didn't. || CORRECTED 2026-07-27 by 128-REVIEW-A, which refuted TWO of this row's claims. (a) BLOCKER 1: the CPU-cap correction above was ITSELF wrong, in the opposite direction. 'A second run from a different PROCESS TREE gets its own ceiling' is not the rule - AssignProcessToJobObject succeeds when the process is in NO job, or the target job is empty or in its own parent job chain - so an UNJOBBED second run joins the named job and really does share, while one that something has already jobbed cannot. The guard written for it was ambient-dependent: it passed here (every process is already jobbed) and FAILED on the reviewer's host, reporting SHARED. FIXED IN THIS BRANCH: the claim now states the condition in conftest.py/status.md/log/spec, and the test CONSTRUCTS the topology (a probe joins its own anonymous job first) so it asserts only what is universal - first tree SHARED, child SHARED, already-jobbed process PRIVATE with a warning AND a verified hard rate of 0x5/5000, which the previous guard never checked. (b) BLOCKER 2 is NOT fixed and is filed as WI-340: the 208-block audit reached 0 unclassified partly by bucketing all 64 same-file pairs as 'intra-module / WI-280 owns it', which is derivable from two path strings without reading the block and is the same reach-zero-by-broad-class failure this WI was correcting. The classification of the 67 CROSS-module blocks stands; the 64 intra-module ones are metadata, not a rationale.
