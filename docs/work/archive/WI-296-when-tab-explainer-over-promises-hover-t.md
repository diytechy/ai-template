+++
id = "WI-296"
title = "When-tab explainer over-promises hover-to-highlight in TIERED mode (narrowed from a WRONG premise - read the whole row before acting) - HTML_TEMPLATE emits 'Hover a work item to highlight its neighbourhood' UNCONDITIONALLY, but the neighbourhood controller reads `.wi`/`.edge` nodes that only the FLAT-DAG emitter (dag_svg) produces. Any project above the SR-089 >3 rule (>3 phases OR >3 workstreams - this repo has 12 workstreams) renders the TIERED drill view, where wiNodes/wiEdges are empty and hover does nothing of the kind; drill blocks have their own `.hl` ring but no neighbourhood logic. THE ORIGINAL 'DEAD CODE' PREMISE WAS WRONG AND MUST NOT BE ACTED ON: 117-CRITIQUE's note observed class=\"wi\" = 0 occurrences in THIS repo's artifact and concluded the `.wi` emitter/CSS/JS was retired dead code to delete. Verified 2026-07-24 that it is NOT - rendering a minimal scaffold (<=3 phases and <=3 workstreams) emits 8 `class=\"wi\"` nodes, and test_when_view_below_thresholds_returns_none_flat_dag + test_a3_flat_dag_fallback_also_prefixes_the_status_glyph both assert that path. Because the kit SHIPS downstream and a freshly-scaffolded adopter has few phases and workstreams, the flat DAG is the DEFAULT render for exactly the copy-ready starting state the kit exists to provide - deleting it would break the When tab for every new downstream project. The JS is also already guarded (`dag ? [...] : []`), so nothing errors in tiered mode. REAL FIX (small): make the explainer sentence conditional on which emitter ran, or implement neighbourhood highlighting for the drill blocks so the promise holds in both modes. Open question for the owner: an explainer promising absent behaviour is arguably an SR-054 usability defect; SR-Refs left empty so re-gating it as critique-verified stays a deliberate choice, not a side effect."
workstream = "dashboard"
buildtier = "medium"
safety_class = "ordinary"
order = 293
+++

## Deliverable

Fixed 2026-07-24 - as the NARROWED defect, not the row's original premise. The When explainer's interaction sentence is now chosen by which emitter actually ran ($dag_interaction): the flat DAG keeps 'Hover a work item to highlight its neighbourhood; click for its detail' (true there - the controller walks the `.wi`/`.edge` nodes dag_svg emits), while the tiered drill view gets 'Double-click a container - or focus it and press Enter - to descend a layer, and the breadcrumb returns to any ancestor; click a work item for its detail', which is what that view actually does. Above the SR-089 >3 rule the old copy promised an interaction the render could not perform. NOTHING WAS DELETED: the original 'dead flat-DAG wiring, drop it' premise was verified WRONG before any work started - a minimal scaffold emits 8 `class="wi"` nodes and 0 `class="drill"`, two tests assert that path, and since the kit ships downstream where a freshly scaffolded repo has few phases/workstreams, the flat DAG is the DEFAULT render for exactly the copy-ready starting state the kit exists to provide; deleting it would have broken the When tab for every adopter while this repo's suite stayed green. One test asserts BOTH branches and was confirmed to fail against the unconditional copy; it also asserts the flat emitter is still live, so a future session cannot re-derive the deletion. Size ratchet 4598 -> 4615 (mostly the comment recording why the flat path stays). Full suite 1481 passed, 7 skipped. Expected consequence: this touches gen_trajectory.py, so the path-based perceptual-stale check re-fires until a fresh critique post-dates it.
