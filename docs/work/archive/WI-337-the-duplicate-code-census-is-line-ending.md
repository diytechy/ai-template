+++
id = "WI-337"
title = "The duplicate-code census is LINE-ENDING-SENSITIVE and the census committed in ffa7377 now FAILS on any LF checkout, including CI (found 2026-07-27 reconciling 127-REVIEW-A's counts against mine). check_dupes fingerprints a token window and the tokenizer sees CR bytes, so the same file hashes differently on a CRLF checkout than an LF one - docs/dupes-allow cannot be valid on Windows and Linux at once. Measured: 18 blocks (LF worktree @1b65c4d, old census) vs 85 (this CRLF tree, same census) vs 128 (LF worktree @ffa7377, the RE-STAMPED census). The re-stamp inverted which environment is broken instead of fixing either. P1 because the branch is unpushed and the first CI run reds."
workstream = "scripts"
buildtier = "medium"
priority = 1
safety_class = "ordinary"
order = 334
+++

## Deliverable

DONE 2026-07-27. check_dupes.normalized_source() decodes with the declared encoding (detect_encoding, so the coding cookie and BOM still apply) and collapses CRLF and lone CR to LF before tokenizing, so a fingerprint keys the CODE, not the checkout. The tree was not merely CRLF, it was MIXED - 28 files under scripts/ LF and 9 CRLF - and since two files with different endings cannot match each other at all, and find_duplicates anchors a duplicate on the first file it sees, the committed census was a DIFFERENT AND WRONG SET, not a mis-fingerprinted right one: 164 blocks where the same corpus under uniform endings has 208 (measured 208 under uniform LF AND uniform CRLF pre-fix - the count never depended on which, only the fingerprints did). Census regenerated on the normalized basis: 208 entries, verified IDENTICAL in this working tree and in a fresh LF worktree, and re-verified from the commit rather than the working copy. It is a RESTORATION, and the numbers say so: 191 of the 208 were already sanctioned by the pre-WI-334 census at 1b65c4d (the LF basis hosted CI uses); only 17 are drift since, and read rather than counted they are the established F5 families (CLI preamble, load_csv/refs, the _git wrapper, the stack-profile reader) plus known WI-280 intra-module debt. NOT claimed: a classification - WI-334's refuted 'MOST are the CLI preamble' is REMOVED from the census header rather than restated, and the truthful per-block audit stays WI-338's. Three guards in tests/test_check_dupes.py, mutated against the shipped source before being believed: significant_tokens and the emitted census agree across line endings (fixtures written with write_bytes - the existing ones use write_text, which emits the platform default and so could never observe this), plus a mutation proof IN the suite that swaps the normalization out and asserts the census diverges. Reverting the normalization reds all three. Also restored 67 tracked files that were CRLF on disk while the index and .gitattributes both declare LF (edit residue, invisible to git status because the clean filter normalizes on the way in) - the same defect produced 127-REVIEW-A's false byte-budget delta and had put project-trajectory/hooks/pre-commit, which .gitattributes warns must be LF or #!/bin/sh breaks, into CRLF. Each restore refused unless its LF form was byte-identical to its HEAD blob, so it could only be a no-op: git diff after it names exactly the three files this WI edits, and the corrected byte figures (+771 edit, +571 pre-existing overage) then re-measure exactly. Audited the other kit checkers: check_stubs/gen_arch_map go through ast.parse on read_text (universal newlines; an AST has no line endings) and the size/complexity ratchets count splitlines(), so all are clean - but check_vendored.py sha256s a vendored file's RAW BYTES against the fetched upstream and has the same flaw, filed as WI-339.
