+++
id = "WI-326"
title = "A green suite silently hides 47 skipped tests when no POSIX shell is on PATH - make the environment gap ANNOUNCE itself (SN-008: a green never hides a skipped check). Measured 2026-07-26 on Windows: the same tree, the same commit, run twice. With `C:\\Program Files\\Git\\bin` absent from PATH: 1540 passed, 54 skipped. With it present: 1587 passed, 7 skipped, zero failures - so 47 tests were not RUN and nothing said so above the -rs line nobody passes. The 47 are not marginal: 36 are `needs a POSIX shell and git on PATH` in test_pre_commit_hook.py, i.e. the tests guarding the COMMIT FLOOR itself, plus the dev-setup and agent-loop shell paths. Git for Windows ships sh.exe at Git\\bin and Git\\usr\\bin but only puts Git\\cmd on PATH, so this is the DEFAULT Windows developer state, not an exotic one - and the handoff's recorded '1579 passed, 7 skipped' vs a successor's '1540 passed, 54 skipped' is two sessions reporting incomparable greens without either noticing. Options, cheapest first: (a) a conftest session-header line stating N environment-gated skips and the one PATH entry that would close them; (b) the same, but FAIL the gate tier (check.py) when the count exceeds a declared floor, since a gate bar that skips its own floor tests is the dishonest-green case; (c) have dev-setup.sh --check report it. Prefer (a)+(b): announce always, gate at the gate. Do NOT auto-inject a PATH entry - silently repairing the environment hides the same fact one layer down."
workstream = "scripts"
sr_refs = ["SR-026"]
buildtier = "medium"
priority = 1
safety_class = "ordinary"
order = 323
+++

## Deliverable

DONE 2026-07-28. Option (a)+(b) as the row prefers, plus the single-source-of-truth move that makes the count honest. ONE declaration, conftest.ENV_GATES: each gate names its probe, what its absence COSTS, whether it is `required`, and a remedy computed for this machine - unreachable_posix_shell() finds an sh.exe that EXISTS but is off PATH and names the directory, so the message reads `put C:\Program Files\Git\bin on PATH` rather than an unactionable `no POSIX shell`. (a) ANNOUNCE: pytest_sessionstart prints the predicted banner on stderr before the first test (controller only, every run including -m smoke), and a new pytest_terminal_summary prints the MEASURED env-gated skip count after the last, with the explicit note that a pass total from this machine is not comparable with one from a fully-gated machine - the line whose absence let 1579/7 and 1540/54 be recorded as comparable greens. (b) GATE: test_prereq_toolchain.test_every_required_environment_gate_is_satisfied FAILS on any required gate; that module is SLOW-tiered, so the hard stop lands in the full suite - check.py's tests+coverage step and CI - while the commit bar stays passable on an unprovisioned box and keeps the banner. Same shape the interpreter floor already used, so no new tier and no new config surface. THE ENABLING CHANGE: the gate condition was four inline copies of shutil.which(sh) across test_pre_commit_hook (8 sites), test_onboard_devsetup (5), test_commit_msg_wi_trailer and test_pre_push_hook (module-level skipif) - the skips were real and the total was nobody's, which is exactly how 47 went uncounted. All now route through skip_without_env_gates / env_gate_skipif, which emit one shared reason string, and test_no_test_skips_on_a_hand_rolled_shell_probe scans tests/ to stop the copies growing back. NOT DONE, per the row: no PATH auto-injection - a harness that repairs its own environment hides the same fact one layer down. NOT COUNTED, deliberately: an os.name platform skip has no remedy, so counting it would make the number noise; test_count_ignores_an_unrelated_skip pins that. FOUND WHILE VERIFYING: running with PATH scrubbed showed test_end_to_end_through_git_commit_in_a_linked_worktree RED with a bare FileNotFoundError instead of skipping - the same confusion in the opposite direction - now gated. Guards: 20 in-process units in tests/test_env_gates.py, each driving a CONSTRUCTED gate table rather than the host's real PATH, plus a subprocess test that runs real pytest with PATH reduced to one empty directory and asserts both hooks fire and the printed count equals the reported skip count, with a provisioned-machine twin asserting neither line appears. Four mutants (banner suppressed, summary suppressed, count forced to zero, skipif reason drifted) each fail the suite. Smoke ratchet re-stamped 480 -> 530 for the 20 new in-process tests, reason in docs/stack.ini. Bar: smoke 490 passed / 31.3 s; the 16 non-test G3 steps all PASS.
