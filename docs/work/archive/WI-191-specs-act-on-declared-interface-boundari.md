+++
id = "WI-191"
title = "Specs act on declared interface boundaries - required Interfaces section citing resolvable IF-IDs; new seams filed as Proposed rows with a nearest-existing-IF rationale; vacuous-until-armed checker + critique-rubric near-duplicate anchor"
workstream = "docs"
needs = ["WI-064"]
buildtier = "medium"
order = 198
+++

## Deliverable

WI-191 (2026-07-16): specs-of-record now act on declared interface boundaries. THE RULE (single-sourced in PROCESS_OPTIONS Spec-of-record + echoed in the specs/ README template + the WI-000 spec template): a spec whose WIs cross a module boundary carries an ## Interfaces section citing each seam as an IF-### that resolves in interfaces.csv (the one seam home, PROCESS.md §8); a NEW seam is filed Status=Proposed at filing with the citation naming the nearest existing IF and why it does not suffice (the forced anti-duplication search - search before invention, never a speculative seam); single-module WIs state the intra-module escape in one line and cite nothing. MECHANIZED: check_trajectory.spec_interface_findings (+ _spec_interfaces_section/_proposed_rationale_present) verifies cited IFs resolve and a Proposed citation carries a non-empty rationale - WARN plain / ERROR under --strict (G2+), vacuous-until-armed (only a spec carrying the section is checked; the specs/ README and the -000 example are skipped); an armed section citing no IF and no intra-module escape is itself a finding. interfaces.template.csv documents the Proposed vocab + rationale. THE HONEST GAP is reviewer-tier (enforcement-audit.md finding 4 + Process-disciplines row): whether a rationale truly names the nearest seam and whether a Proposed contract near-duplicates an existing IF are judgment calls (check_dupes works on code, not contract prose) - backed by the new docs/rubrics/spec-interface-hygiene.md B1 anchor (which WI-190's plan rubric imports). DOGFOODED: WI-191 + WI-190 specs retrofitted with ## Interfaces sections that EXTEND the existing IF-023 (check_trajectory's consume of the trajectory/spec surface) rather than mint a near-duplicate - the exact outcome the rule produces; IF-023 contract text extended for accuracy. SPINE: SR-044 Requirement+AC extended (declared interfaces are the authority, now enforced at the spec layer too) + LLR-068 + TC-068 (the WI-064 same-file precedent) - SN=25 SR=65 LLR=68 TC=68, derived gate holds G3. 7 new tests in test_trajectory.py. Byte delta: PROCESS_OPTIONS.md 151,921 -> 153,448 (+1,527 B, flagged; the spec-of-record Interfaces contract), baseline re-stamped x3; AGENTS.template.md + PROCESS.md untouched. Discovered a PRE-EXISTING dupes-gate red (WI-188's in_phase helper in trace.py == gen_release_checklist.py, uncensused) - filed WI-193, not fixed inline (out of scope). Full suite 951p/2s; check_trajectory --strict + trace --strict green.
