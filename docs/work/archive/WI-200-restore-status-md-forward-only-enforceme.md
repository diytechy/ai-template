+++
id = "WI-200"
title = "Restore status.md forward-only enforcement - mode-aware done-id rule in check_trajectory (hand-edited status: WARN/strict-ERROR; generated-marker status: freshness check) so the WI-180 retirement's promise is real before the prose rule stands down; R-B/R-C stay retired"
workstream = "scripts"
needs = ["WI-180"]
buildtier = "medium"
order = 197
+++

## Deliverable

WI-200 (2026-07-16, opus build / fable integrate): check_trajectory.status_forward_only_findings - a done WI id token (word-bounded) in a hand-edited docs/status.md is a finding, WARN plain / ERROR --strict (the pre-WI-180 R-D severity); only registry-done ids flag; stands down when status.md carries the generated-block marker (<!-- BEGIN GENERATED - the arch_inventory sentinel), where the regenerate-and-byte-compare freshness check is the SPECIFIED-not-built successor (no status generator exists); shares the trajectory-check opt-out + placeholder/absent-registry vacuity; a repo-state rule so the invariant holds at every integrator-published tree. Docstring retirement paragraph amended (R-B/R-C stay retired; R-D RESTORED mode-aware); SR-037 Requirement+AC de-staled in the same pass (they still described R-B/R-C as live). 7 tests incl. the real-meta-repo probe; enforcement-audit gains the Harness row; STATUS.template.md names the enforcer. Spine LLR-075/TC-075 under SR-037 Phase 1.
