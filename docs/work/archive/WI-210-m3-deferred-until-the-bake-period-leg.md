+++
id = "WI-210"
title = "M3 (deferred until the bake period): legacy-path retirement - absent --jobs resolves to the dispatcher (--jobs 1 = serial dispatcher, not the legacy loop), the serial resume driver + --track plumbing retired (SR-029/SR-030 re-scoped or retired through a reviewed spine amendment), the judgment duties re-homed once in PROCESS_OPTIONS, ADOPTING section 6 + downstream-resync carry the recipe, and the per-path guard inventory single-pathed. DEFER REASON: the dispatcher must first earn trust as the live default - un-defer trigger: >= 3 real multi-WI dispatcher runs on this repo with no incident the serial path would have avoided, then an owner un-defer"
workstream = "unattended"
needs = ["WI-208", "WI-209"]
buildtier = "strong"
order = 209
+++

## Deliverable

One engine, one selection path (owner-directed un-defer 2026-07-17, superseding the bake trigger): a plain launch IS the dispatcher (absent --jobs/AGENT_JOBS resolves to 2, held at 1 until the section-14 audits); the legacy serial resume driver is DELETED outright (owner ruled (a)) - DEFAULT_PROMPT/read_ask/status_size_warning/rework-wi/run-state ladder/pause-boundary/dual_only_frontier_ask retired; --track + lane plumbing + tracks/id-blocks scaffolding retired (bootstrap --tracks gone; templates deleted); judgment duties re-homed once in PROCESS_OPTIONS Unattended operation; ADOPTING section 6 + downstream-resync carry the upgrade recipe; launchers drop the AGENT_PROMPT slot; ~130 engine-behavior tests migrated to the worker path, lock tests ported (SR-029/030 evidence re-homed); spine amendments: SR-026/029/030/060/066 + LLR-026/061/076 + TC-026/029/030/061/076; per-path guard sweep recorded in the log
