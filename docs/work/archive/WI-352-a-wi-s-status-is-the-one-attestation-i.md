+++
id = "WI-352"
title = "A WI's `Status` is the one ATTESTATION in this repo with no reconciler, so it can disagree with the evidence indefinitely and nothing says so. Widened 2026-07-28 from the single symptom that exposed it (WI-328 sat `queued` for a day with all six Done-when boxes ticked, the LLR Rationale column and the IF v2 bumps shipped, and SafetyClass=spine serializing the whole project behind finished work) to the general rule the symptom is an instance of. THE DESIGN QUESTION, ASKED AND ANSWERED (owner, 2026-07-28): should completion be DERIVED instead, like the gate? No - and the reason is already in this registry. The evidence a deriver would read is the `WI:` commit trailer, which agent_common.latest_trailer_evidence ALREADY folds into built/blocked sets for the dispatcher; but a trailer means 'a commit CLAIMS this WI', not 'the work is right'. WI-336 is the counterexample: its code landed while its row stayed `queued`, and that was CORRECT, because a review had refuted three of its claims - a deriver would have flipped it to done and been wrong. `deferred`/`blocked`/`retired` are not derivable at all, being decisions not outcomes. Completion is an attestation, and attestation is where the derivation chain bottoms out: derive_gate computes the gate FROM declared Status cells, so deriving those in turn just moves the turtle. WHAT IS MISSING is not derivation but RECONCILIATION - the same shape every other declared-vs-computed pair here already has (derive_gate --check recomputes and fails on drift; every generator carries --check). Build one reconciler in check_trajectory that compares the declared cell against every completion signal available and reports DISAGREEMENT, never auto-flipping: (a) an OPEN row whose SpecRef resolves to a spec with >=1 `- [x]` and zero `- [ ]` left; (b) the mirror - a `done` row whose spec still has unticked boxes (40c92f6 exists because S5 was ticked before it was true); (c) an OPEN row named by a `WI:` trailer on an integrated commit; (d) a `done` row with an empty Deliverable (currently zero, so it ships as a zero-to-zero guard). WARN plain, ERROR under --strict, matching R-E/R-F, and vacuous-until-armed on a repo with no specs or trailers. Fail LOUDLY and let a human resolve - an auto-flip would re-create exactly the false completion WI-336 and 40c92f6 were both corrections of."
workstream = "scripts"
buildtier = "medium"
priority = 2
safety_class = "ordinary"
order = 349
+++

## Deliverable

DONE 2026-07-28. completion_reconciliation_findings(root, wis) compares the declared Status cell against the completion evidence and returns (kind, message) pairs so the caller tiers them without pattern-matching prose. Three signals: spec-says-finished (an OPEN row whose own spec has >=1 ticked Done-when box and none left - the WI-328 shape), spec-says-unfinished (a done row whose own LIVE spec still has unticked boxes and which no open WI cites - 40c92f6's shape), and trailer-claims-it (an OPEN row named by a WI: trailer reachable from HEAD). Never auto-flips: an auto-flip would re-create exactly the false completion WI-336 and 40c92f6 were both corrections of. THREE SCOPING DECISIONS, EACH MEASURED RATHER THAN ASSUMED. (1) The done side reads LIVE specs only. Run over the archive it produced 38 findings and neither class was actionable - some are a self-referential terminal box (`Commit bar green; row done, spec archived`, untickable at the moment you would tick it), the rest are closes that never ticked; for a WI closed weeks ago whose Deliverable and log entry carry the record the only available action is cosmetic, and a check whose recommended action is nothing is the wall of warns WI-308 recorded as how a check earns its own ignore. The moment that IS actionable gets its own check: staged_completion_findings warns at the closing commit, where the spec is still live and both homes can be made to agree in one change. (2) A WI's spec is NAME-IDENTIFIED (docs/specs/WI-###.md, then the archived form), never whatever SpecRef points at - measured before this rule existed, following SpecRef reported WI-324's work FINISHED out of ticks WI-321 had made, because WI-324 cites the shared WI-321 effort doc. (3) The Done-when tally is a SECTION, bounded by heading LEVEL: a subdivided Done-when keeps its boxes, a sibling heading ends it - WI-321.md carries WI-324's remaining boxes under a sibling `## Split off, deliberately`, and folding those in attributes one WI's unfinished work to another. Migration checklists are steps, not evidence, and do not count. DEVIATION FROM THE ROW, taken on the row's own argument and pinned by a test: the trailer signal WARNS and never joins the exit code, not even under --strict. The row rules that Status stays an attestation precisely because a trailer CLAIMS a WI rather than attesting it, and cites WI-336 - code landed, row correctly left queued, a review having refuted three of its claims. Erroring there blocks the G3 gate for the length of that rework with no honest way out but a false close. Spec evidence is different in kind: a ticked box IS an attestation, so its disagreement with the row is a contradiction between two homes for one fact and gates. The fourth signal the row asked for - a done row with an empty Deliverable - is deliberately NOT reimplemented: R-A already makes it a hard ERROR at every run, strictly stronger than this tier, and a second weaker copy of a live rule is the duplication the working agreement forbids. Pinned by a test so the omission reads as a decision, not a gap. Guards: 18 tests, every signal driven against a constructed spec and every one paired with the twin that must stay silent. Six mutants (no-ticked-box reads as done, standing check reads the archive, any heading ends Done-when, open-citer suppression removed, trailer joins the exit code, migration checklists count) each fail the suite. Complexity: main() 21 -> 22, a reviewed +1, with the tier DECISION extracted to tier_completion_findings rather than written as two more branches. Module size 2135 -> 2495, reviewed bump - and the fifth consecutive one on this module, so check_trajectory.py is named as WI-280's concrete next slice with its measured number. Bar: smoke 490 passed / 24.8 s; all 17 non-test G3 steps PASS.
