+++
id = "WI-333"
title = "The G3 lint step is red on 20 ruff findings, ALL of them landed AFTER the 2026-07-25 all-17-green - a regression the open Modified window HID (dated 2026-07-27 during the re-attestation sitting). docs/stack.ini [step:lint] runs `ruff check {src} {tests}`, a G3-ONLY step: while the re-attest window held the derived gate at G2 the step dropped out of the bar, so 20 findings accumulated across 2026-07-26/27 with every commit green. 13 F402 (`html` module shadowed by a `for label, html in ...` loop variable) entered at 44024b3 WI-318; 2 F821 (undefined `pytest` - tests/test_trace.py calls pytest.skip twice WITHOUT importing pytest, so on a git-less machine the skip guard raises NameError instead of skipping) entered 2026-07-26; 5 F401 unused imports across 41b228a/faa3cba/c714e1e. Triage F821 FIRST: it is a latent defect, not a style nit. The process finding is the bigger one - a lowered bar during an attestation window is a hole regressions fall into, so consider whether the G3-only steps should still run advisory-only at G2."
workstream = "quality"
buildtier = "quick"
priority = 1
safety_class = "ordinary"
order = 330
+++

## Deliverable

DONE 2026-07-27. ruff check over project-trajectory/scripts + tests is now CLEAN (20 -> 0). F821 (2): tests/test_trace.py called pytest.skip('needs git on PATH') twice WITHOUT importing pytest - the branch is live, not dead, so the import was added and the skip path was EXERCISED (PATH stripped of git -> 'SKIPPED needs git on PATH'; mutation-proven: without the import the same run raises NameError). F402 (13): the `for label, html in ...` loop variable shadowed the `html` MODULE in tests/test_gen_trajectory.py. Verified first that no module-style html.* call sits inside any of the 13 loop bodies, so this was a latent hazard not a live bug; loop var renamed to `page` (151 tests still pass). F401 (5): each confirmed to appear exactly once (its own import line) before removal - io in gen_open_items.py, sys in test_gen_open_items.py, ROOT/SCRIPTS/run_py in test_gen_trajectory_pending.py. TWO DEFECTS IN THE ROOT conftest.py FOUND WHILE DOING IT, both shipped by the previous commit and both fixed with mutation-proven guards: (1) pytest_xdist_auto_num_workers was declared unconditionally, so ANY run without xdist (-p no:xdist, or an env that never installed it) died with PluginValidationError before collecting - fixed with @pytest.hookimpl(optionalhook=True); (2) AssignProcessToJobObject's ERROR_ACCESS_DENIED was swallowed as 'already in this job', so a run launched from a different process tree than the named job's owner ran COMPLETELY UNCAPPED while reporting success - now falls back to a private capped job and says so. The PROCESS half of this WI (should G3-only steps run advisory during an open window?) is NOT decided here - it is an owner ruling and remains open.
