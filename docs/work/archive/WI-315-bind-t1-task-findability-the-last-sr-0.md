+++
id = "WI-315"
title = "Bind T1 task findability - the last SR-054 anchor whose mechanizable half has no owner (only T2/T3/T5 have child rows; the WI-300 ruling called T1's core assertable against the nav graph and then bound everything except it, the same mechanizable-but-unbound hole F12 found for T6/WI-314). The 2026-07-26 owner ruling reworded the anchor off the word 'obvious' onto its own operational gloss, so the bar is now checkable: each of the three core reading tasks reachable within one tab switch from the landing view, its entry point a LABELLED nav control or a named landing surface. Bind it as a child LLR + Automated=Yes TC under SR-054 per the WI-300 (f) pattern. Design constraint that makes the test worth writing: assert against the RENDERED artifact carrying real registry data, not the nav skeleton - 119-CRITIQUE's MAJOR was a next-work surface that exists in the emitter (the hero 'sub nowat' line, already covered by test_t1_hero_names_the_active_work_item) but rendered nothing because it fires only on status=active and the registry had zero active rows, a state test_t1_hero_active_line_absent_when_nothing_is_active explicitly blesses; a structural nav check passes on exactly the artifact the critic failed. Gated behind WI-305 (the open T1 defect): binding an anchor while its defect is open is the lax classification the WI-300 ruling forbids."
workstream = "dashboard"
sr_refs = ["SR-054"]
needs = ["WI-305"]
buildtier = "medium"
safety_class = "ordinary"
order = 312
+++

## Deliverable

Delivered 2026-07-26 - built in-flight by the same train and reconciled per the session-protocol residue rule after the worker was stopped (verified against the spec's Done-when, kept because complete and green). LLR-115 + TC-120 bind T1's mechanizable bar under SR-054 per the option-(f) pattern, Automated=Yes. TC-120's method is the constraint the spec insisted on: generate from a ZERO-ACTIVE/all-queued fixture (the exact 119-CRITIQUE failure condition) and resolve each task's entry point from the RENDERED artifact carrying real registry data, never the nav skeleton - a structural check passes on precisely the artifact the critic failed. The live-anchor set moved on all three surfaces in step (rubric header + T1 anchor, TC-055.Expected, SR-054.AcceptanceCriteria): T2, T4, T5, T6, T7, T8.
