+++
id = "WI-334"
title = "The duplicate-code census is 90 blocks behind, and the open Modified window hid it (surfaced 2026-07-27 by the re-attestation sitting - the first check.py --gate G3 run since 2026-07-25, when [step:dupes] was GREEN). dupes is a G3-ONLY step, so from 2026-07-26 the window's G2 fallback dropped it from the bar and the trace_text decomposition (WI-329), the render batch and the gen_open_items build-out never met it. The 90 are a MIXTURE that only per-block triage separates: genuine new duplication (confirmed by reading one - gen_open_items.py:748 == :769, the BASELINE_RE baseline-reuse pair, real and extractable) versus census DRIFT (the census is fingerprinted per WI-276, so editing one token in a sanctioned block invalidates its line). Hard constraint: a census line IS acceptance of the duplication, so a blanket re-stamp is forbidden - the standing rule from the 2026-07-25 close is that a sanction is never reached for to green a step."
workstream = "quality"
buildtier = "medium"
priority = 1
safety_class = "ordinary"
order = 331
+++

## Deliverable

DONE 2026-07-27. check_dupes exits 0 (86 reported blocks -> 0). Triaged by READING, not counting, and the census records the classification: (a) MOST of the blocks are the kit's per-script CLI preamble - _utf8_console() plus the argparse skeleton - repeated across ~10 scripts, which is DELIBERATE under the F5 ruling that rejected a shared _kitcommon.py (kit scripts must stay independently copy-able and stdlib-only; check_stubs.py says 'verbatim across the kit' in its own code). Sanctioned WITH that reason. (b) The intra-file blocks in agent_dispatch/gen_trajectory/trace/check_trajectory are the known WI-280 decomposition debt, previously sanctioned, whose fingerprints moved because the blocks were EDITED - re-stamped as drift. (c) ONE was genuine, new and intra-module, so it was EXTRACTED rather than sanctioned: gen_open_items read its attestation-baseline stamp in two places (the --check compare and the write path's baseline reuse) -> stamped_baseline(); a divergence there would have split the freshness gate from the generator silently, and gen_open_items --check confirms the emitted page is unchanged. 43 committed lines that matched nothing were pruned. No blanket re-stamp: the standing rule is that a census line IS acceptance of the duplication. || CORRECTED 2026-07-27 (WI-337 + 127-REVIEW-A). Two numbers and one claim above are FALSE, kept rather than rewritten so the correction is traceable. (a) '86 reported blocks' was never the checker's count of anything - it was a census-transition reconstruction measured on a MIXED working tree (28 LF / 9 CRLF files under scripts/), where the tokenizer saw the line endings; the same corpus reports 208 blocks under uniform endings, and the census this row re-stamped red a fresh LF worktree with 128 unsanctioned blocks - a CI break that had not fired only because the branch was never pushed. (b) 'MOST of the blocks are the CLI preamble' is numerically false: reconstructed 34 CLI-preamble against 50 other drifts, plus a new cross-module _git duplicate this row does not mention. The F5 ruling may still cover the 164 sanctions, but the premise they were signed on was wrong; the truthful per-block audit is WI-338's. (c) The one EXTRACTION (stamped_baseline) stands. WI-337 restored the census to the line-ending-normalized basis.
