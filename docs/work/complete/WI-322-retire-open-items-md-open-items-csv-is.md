+++
id = "WI-322"
title = "RETIRE open-items.md: open-items.csv is the source, a generated open-items.html is the only reading surface - OI-10 RULED option (b), owner 2026-07-26 (Decisions log). The owner's framing settles the driver's prose-in-cells objection: the CSV is a machine source the generator pulls from, read raw exactly as often as work-items.csv is - the human reads the HTML. Same registry-to-generated-view pattern the repo already runs on, and OI state becomes queryable data. The generated block is a POINTER: it names `trace.py --ratify modified` rather than carrying the amendment, so an owner sitting down to re-attest cannot act from the surface whose own header promises the context needed to rule. Two things underneath it, one a design choice and one not: the four-lines-for-six-rows shape is CORRECT (the SR is the attestation unit, a Modified LLR/TC rides its owning SR line) but nothing on the line says so; and the depth that makes a re-attest readable is a WORD-LEVEL DIFF, which markdown cannot express - hence a format ruling rather than a content fix. Blast radius measured before filing, and it is KIT-WIDE: OPEN_ITEMS.template.md is a bootstrap MAPPING destination (every adopting repo has one), 11 kit-doc references describe it, 7 test modules assert it, one SR/LLR/TC row names it, and agent_dispatch treats it as a status-map marker AND refreshes its pending block every dispatch loop - so this is a kit version bump with a documented migration, not a cleanup. The thing the build must not get wrong: this file is TWO things wearing one name, a generated projection and hand-authored briefs, and a generated view needs a source - so the source decision (OI-10) lands before the renderer, or the aggregate file grows back. Options and recommendation (a: an HTML view over per-item markdown sources, retiring markdown as the READING surface while keeping it as the authoring format) are in the OI-10 brief. Hazard to carry into whichever build wins: the baseline is the whole ballgame - `--ratify modified` with no `--since` returns an EMPTY section for a row amended while it still read Verified (SR-052/053 today) and a bad `--since` fabricates one, so a rendered view must print the baseline it diffed against and make an empty section read as check-the-baseline, never as nothing-changed."
workstream = "docs"
order = 319
+++

## Deliverable

Delivered 2026-07-26. docs/open-items.md is GONE - here and from the kit scaffold. gen_open_items.py renders docs/requirements/open-items.csv plus every Draft/Modified spine row, with per-cell word-level before/after, into docs/open-items.html; a new open-items harness step + pre-commit floor entry gate its freshness with the machine-local region masked (the M-10 rule it inherits). ANTI-DUPLICATION was the design centre: trace.reattest_model is SPLIT OUT of reattest_lines so one computation feeds two renderers (markdown brief + HTML view), markdown output proven byte-identical across the refactor on both baselines; the view imports gen_trajectory.pending_block rather than re-deriving what is pending, and says on the page that the brief wins if they ever disagree. Theme tokens are the one deliberate copy, held by a drift guard rather than an extraction that would edit gen_trajectory and re-red perceptual-stale. The view is SELF-DESCRIBING about its baseline (it stamps the revision it rendered against; --check re-renders with that one), because otherwise --since would be a flag no freshness gate could reproduce - and the pre-regime streak makes an auto-baseline render an EMPTY section, which the page states as check-the-baseline, never as nothing-changed. FINDING that changed the design: OI ids have been REUSED historically (OI-8 and OI-9 each name two different decisions, 2026-07-14 vs 07-25), so ruled history is NOT backfilled - a registry keyed on a non-unique id would be broken from birth. Pending rows plus forward tombstones; the Decisions log stays the record. Three defects the tests caught mid-build, each fixed rather than accommodated: no UTF-8 console guard; changed_percent counted whitespace so four-words-replaced read 57% on a label saying -of the words-; and bootstrap left __pycache__ in every fresh scaffold once a scaffolded generator imported its siblings (fixed with -B, which also seeds the view so a fresh scaffold passes its own gate). Migration: template swapped for registries/open-items.template.csv, bootstrap MAPPING + the scaffolded OI-3 brief now a csv.writer row, 11 kit-doc references, ADOPTING section 6 note, the dispatch status-map marker + disposition regen, check_trajectory's brief lint, check_docs S-3. Seven test modules retargeted; twelve splice-only tests retired with the code they guarded, successors in tests/test_gen_open_items.py. SR-055/LLR-056 amended and flipped Modified, so the window holds 8 rows. Seams IF-073 (registry read) + IF-074 (view emit).
