+++
id = "WI-323"
title = "Roadmap DAG lane separation: the T8 residue that stays perceptual (121-CRITIQUE MAJOR, second clause) - several dependency edges share long coincident horizontal corridors between the phase-2, phase-3, unphased and phase-1 groups, then converge into dense port fans, so a reader cannot attribute a given line to one source and one target. Seen in 1680px-light-dag-full.png and 1280px-dark-dag-full.png. WI-320 bound the OBJECTIVE half (no edge through an unrelated node box, LLR-120/TC-125, measured clean at 264 wires) and deliberately left this one: 'crossings minimized, and where unavoidable in open space' is the reader-experience clause the WI-300 (f) ruling scoped out of mechanization, so this is a DESIGN slice judged by before/after shots, not a test to write. Two measured starting points from the WI-320 binding, neither of which is the whole finding: (1) ONE shipped wire rides within the router's own 3px clearance of WI-043's edge - _detour_d exhausted _MAX_LANES candidates and returned its least-bad fallback, so the lane search giving up is a real and reachable state; (2) _port_fan spreads co-port wires over min(row_h*0.6, (n-1)*6) px ordered by the other endpoint's ROW, which is a local anti-knot rule with no notion of a shared corridor. Do not mechanize this into a crossing-count proxy to green the gate - the standing rule. Read LLR-120's Detail before starting."
workstream = "dashboard"
sr_refs = ["SR-054"]
buildtier = "strong"
safety_class = "ordinary"
order = 320
+++

## Deliverable

DONE 2026-07-29 as the design slice it was scoped to be. gen_trajectory's router gained a shared-corridor ledger (_detour_d prefers box-clear AND corridor-free lanes; the ledger can only move a wire between fully-clear lanes, so the T8 objective floor cannot regress through it) and stacked lane candidates (_LANE_STACK 3 rungs at _LANE_SEP 10px, dialled against the shipped render, not theory). Measured on the artifact: coincident pairs within 6px 27 -> 0, within 10px 16 -> 0; total coincident length 8110px -> 3280px. Objective sweep green (274 wires, 0 through a box; byte-deterministic regen). Honest residue, recorded: port-fan convergence unaddressed (needs a two-phase pass — a rewrite, not a refinement); 13 pairs at 10-14px; the WI-257 edge clipping pre-existing; and the WI's own premise corrected — no least-bad fallback is reachable in the shipped render (instrumented both sides). No crossing-count proxy added (standing rule); acceptance stays the periodic advisory critique, and a same-family advisory critique ran on the before/after matrix at close (degraded-legal mode): VERDICT IMPROVED — overlap ink 45.8% -> 5.3%, tightest long-haul pair 6.5 -> 10 CSS px, measured from rendered pixels (docs/reviews/WI-323-CRITIQUE.md). The critic also proved two port-fan sites got WORSE (one newly fused stretch at block 1 — a regression this change introduced) and re-attributed the canvas-clip stubs as pre-existing; both filed per change-intake as WI-366 and WI-367.
