+++
id = "WI-325"
title = "The committed re-attestation brief has no freshness gate, and has now gone stale TWICE in one day - give `trace.py --ratify modified` a --check mode and wire it into the floor. Every other generated surface in this repo is freshness-gated (gen_trajectory --check, the status snapshot, gen_okf --check, gen_open_items --check); docs/ratify/*.md is generated the same way and gated by nothing, so it silently drifts behind the registry it summarizes. Observed 2026-07-26 twice: at 121-CRITIQUE it was missing LLR-105/TC-108 and SR-054's later amendment (an owner would have blessed six rows having seen four), and again at 123-CRITIQUE it was missing LLR-118/TC-123 plus LLR-119/LLR-120/TC-124/TC-125 (a chain three rows shorter than the registry's). Both were caught by a human noticing, which is exactly the enforcement tier docs/enforcement-audit.md calls the weakest. DESIGN CONSTRAINT that makes this harder than the sibling checks and must not be skipped: the brief SELF-STAMPS its baseline and reuses it, so --check must compare against the baseline the file already declares, NOT re-derive one - re-deriving is precisely the WI-322 review BLOCKER (a regeneration that silently collapsed 43 chain-row diffs to 18 while --check certified the loss). A stale brief is read by a human about to attest, so this fails closed."
workstream = "scripts"
sr_refs = ["SR-049"]
buildtier = "medium"
priority = 1
safety_class = "ordinary"
order = 322
+++

## Deliverable

DONE 2026-07-28. `trace.py --ratify modified --check` re-renders the brief and compares it against the committed file, failing closed on a difference - a stale brief is read by a human ABOUT TO ATTEST, so a false green there means an owner blesses rows they were never shown. THE DESIGN CONSTRAINT THE ROW INSISTED ON, and the reason this is not a copy of its siblings: the brief SELF-STAMPS its baseline, so the compare reuses the baseline the FILE declares and NEVER re-derives one. declared_since() reads only `- from --since` stamps, because a section baselined by DERIVATION pins nothing a re-derivation could move; two different --since stamps cannot come from one run, so that is reported as a hand-edit rather than resolved - guessing which is current is exactly the silent substitution the check exists to prevent. DOUBLY SELF-ARMING, so a downstream repo pays nothing: silent when there is no docs/ratify/ brief, and silent when no SR is Modified - the window is CLOSED, so the committed brief is a historical record of a finished sitting, not a surface that can go stale, and checking it against a registry whose rows have since been blessed would fail forever, which is how a check earns its own ignore. The live brief is DERIVED (newest docs/ratify/*.md by stamped NAME, not mtime - a checkout rewrites mtimes and this check exists precisely not to trust the working tree) rather than configured, because a fixed path would need editing at every sitting and would silently gate the wrong file when it was not. WIRED at both tiers per the row: docs/stack.ini [step:ratify-fresh] at G2+ AND the pre-commit floor's batched freshness run - the sitting can happen between gate runs. Shipped LIVE in stack.ini.template too, not commented, because it costs a project without a re-attestation practice exactly nothing and forces no migration. Classified in the disposition tripwire as floor-CHECKED but never REGENERATED: regenerating an attestation surface from a dispatcher disposition would silently rewrite what the owner is about to attest to. Guards (9, tests/test_trace.py) - one per Done-when clause plus the parser: a current brief passes; BOTH drift directions bite (a chain row added after it was written - the 121-CRITIQUE shape - and a changed cell); the no-op cases are no-ops, not failures; and THE LOAD-BEARING ONE proves it does not re-derive, constructed so the two baselines genuinely disagree (the brief is stamped --since <first commit> while an intervening Verified commit makes the auto-derived baseline a later one, and the check is then run with no --since at all). Found by the tests: main() is called BARE at the bottom of trace.py, so a plain return sets no exit status - the check reported STALE and exited 0 until it used sys.exit like the analyze path. Module size 2706 -> 2848, reviewed. Bar: smoke 509 passed / 24.2 s; all 18 non-test G3 steps PASS.
