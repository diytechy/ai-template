+++
id = "WI-329"
title = "Extract the spine-row TEXT concern out of trace.py (the WI-280 decomposition, applied to the pocket WI-328 grew). trace.py took a +196 reviewed bump (2812 -> 3008) for the requirement-form tier, and the module-size ratchet's standing instruction is DECOMPOSE, DO NOT BUMP - so the bump is a deferral, recorded as one. The extractable unit is cohesive and already named: provenance_findings, form_findings, paraphrase_advisories, ac_advisories and their compiled patterns are one concern - 'is this spine row readable and decidable on its own' - and they are PURE text predicates with no I/O, which is the clean-core/shell split the process asks for. The precedent is agent_common.py + plan_runner.py split out of agent_loop, so a sibling module is idiomatic here and NOT the shared-_kitcommon shape that was ruled out for breaking independent copy-ability: this is one module's own core, imported by one module. DEFERRED DELIBERATELY, not forgotten: the extraction moves the scaffold surface (bootstrap MAPPING, README kit-contents, test_bootstrap file lists, ADOPTING re-sync note) and WI-328 landed inside an open attestation window, where widening blast radius is the drive-by this repo keeps recording. Done-when: trace.py re-stamps DOWNWARD past 2812, the new module carries its own baseline or stays under the 1500 threshold, and the scaffold surface lists it."
workstream = "scripts"
needs = ["WI-328"]
buildtier = "medium"
safety_class = "ordinary"
order = 326
+++

## Deliverable

scripts/trace_text.py (383 lines, under the 1500 THRESHOLD so it needs no baseline of its own): the four PURE text predicates - provenance_findings and form_findings (gating), ac_advisories and paraphrase_advisories (warn-only) - plus the row primitives refs/is_example/is_draft, homed in the LOWER layer so trace.py imports them back and the dependency runs one way with no cycle. trace.py 3026 -> 2703, which is 109 lines BELOW the pre-WI-328 baseline of 2812: the ratchet TIGHTENS rather than merely stops growing, and this is the decomposition escape it documents and prefers over the two bumps WI-328 took. PROVEN behaviour-preserving, not asserted: the three golden report files are byte-identical across the split, which is a stronger guarantee than a passing suite because the finding STRINGS are the contract. Sequenced BEFORE the sitting on the owner's reasoning, which corrected the driver's: 4 LLR rows name a moving symbol (LLR-004 Verified plus LLR-133/134/135, which WI-330 had just added), so landing after would have re-attested three rows days apart and taken LLR-004 through a G3->G2->G3 round trip for one pointer column. Scaffold surface: bootstrap MAPPING copies the pair together (a scaffold with one and not the other ImportErrors on its first check - caught live by the golden scaffold test, not predicted), bootstrap docstring, ADOPTING section 6 migration note. IF-076 declared for the seam, on the IF-065/IF-066 agent_common precedent. Reviewed bump: bootstrap.py 2003 -> 2007 for the MAPPING entry.
