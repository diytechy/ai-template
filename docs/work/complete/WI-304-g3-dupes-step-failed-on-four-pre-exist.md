+++
id = "WI-304"
title = "G3 `dupes` step FAILED on four pre-existing duplicate blocks in agent_dispatch.py (832==1983, 1439==1983, 1440==1626, 1624==1982). Verified pre-existing at 69b11fd, the 2026-07-25 session's starting commit. FOUND ONLY 2026-07-25 because the G3 harness had never been RUNNABLE on the owner's machine - the .venv was Python 3.9.6, below the 3.11 floor - so check.py --gate G3 could not execute and this step's failure was invisible. NB the DERIVED gate (docs/gate, from artifact states) was unaffected and stayed G3; what failed was the G3 HARNESS. CORRECTION 2026-07-25: this row originally carried a soft edge to WI-280 and called decomposition 'WI-280's subject'. That was WRONG and is retracted. Reading the blocks showed they are not architectural at all but two boilerplate patterns - the 5-kwarg subprocess.run capture block (7 sites) and the run-and-report failure verdict (2 sites) - i.e. extract-two-helpers work needing nothing from WI-280's typed-value-object program. Deferring behind WI-280 would have parked a red G3 step indefinitely for an unrelated reason."
workstream = "scripts"
buildtier = "medium"
priority = 2
safety_class = "ordinary"
order = 301
+++

## Deliverable

Fixed 2026-07-25 by EXTRACTION, not sanction. `_run_captured(argv, cwd=None, **extra)` states the subprocess capture contract once for 7 call sites; `_regen_failure(proc, label, what, budget)` states the regen family's failure verdict once for 2. check_dupes now reports 'OK - no duplicate blocks in 35 file(s)', so the G3 dupes step passes and NOTHING was added to docs/dupes-allow - the census stays a record of legitimate repetition rather than absorbing copy-paste. agent_dispatch.py RATCHETED DOWN 3889 -> 3885. LATENT DEFECT FOUND AND FIXED EN ROUTE: the five raw `tail = (...).strip()[-N:]` slices were doing the naive thing this codebase had already learned was wrong. agent_common._failure_tail exists (and is used ~20x here) precisely because blind tail-truncation hid the real error behind a leading PASSING banner in the WI-229 blocked-disposition loop; it prefers the last '  FAIL  <step>' block and degrades to the same tail bound otherwise. REWORKED 2026-07-25 after adversarial review REFUTED the 'strict improvement' claim: _failure_tail returns the block ENDING at the last FAIL line, which is right for check.py (FAIL is a trailing summary) and WRONG wherever FAIL is a HEADER with the diagnostic after it. Measured: a jest failure truncated to 'FAIL src/sum.test.js' losing Expected/Received, and `go test` returned a PASSING test's block. _run_combined_bar - the ONE site that runs the DOWNSTREAM repo's declared command (ADOPTING.md: npm test / gradle check / cargo clippy), whose grammar the kit does not own - is reverted to a raw bounded tail. The four regen sites KEEP _failure_tail: they run the kit's own gen scripts, which emit no FAIL line, so the helper takes its plain tail path. Rule now stated in both docstrings: _failure_tail only where the kit owns the output grammar. Regression test added (test_combined_bar_red_detail_keeps_the_diagnostic_not_just_the_fail_header) and verified to bite - the previous red-bar test ran sys.exit(3) with NO output, so the changed line was executed but never asserted. The 400/300/200 variance the breakdown flagged is preserved deliberately, not normalised: each bound belongs to its own call site's output shape.
