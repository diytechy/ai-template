+++
id = "WI-280"
title = "Bounded core decomposition — characterize state transitions, introduce typed session/route value objects, isolate pure decisions from Git/subprocess effects, and split gen_trajectory.py into parse/graph/view-model/render modules; the module-size ratchet (WI-279-adjacent) is the growth sensor while this is deferred; subsumes the retired WI-082 (bootstrap.py main() decomposition) as a concrete first slice; Modules owned (named so docs/dupes-allow can charge blocks here and tests/test_dupes_census_audit.py can verify it): gen_trajectory.py's graph/view-model/render layer, its split siblings traj_graph.py / traj_parse.py / traj_render.py / traj_views.py / traj_panels.py / traj_status.py, and bootstrap.py's main(). RE-SPECIFIED 2026-07-29 (owner ruling): the train half of the original scope — agent_dispatch.py's disposition arms and the typed train objects — was DELETED whole at concurrency-restructure Phase 5, so it left this row's scope as removal, not decomposition; measured sizes at the re-spec, by the size-ratchet's own splitlines method (an earlier draft of this row used a blank-stripping counter and understated all three): gen_trajectory.py 4967 / bootstrap.py 2078 / agent_loop.py 3006 lines (whether agent_loop joins the owned set is the claimer's first design call)."
workstream = "scripts"
needs = ["~WI-226"]
buildtier = "strong"
safety_class = "ordinary"
order = 277
+++

## Deliverable

gen_trajectory.py 5,274 -> a 949-line facade over six new stdlib-only siblings: traj_parse (sources + the one git/subprocess effects seam), traj_graph (pure layout/routing), traj_render (SVG/HTML primitives, theme tokens, the drill renderer), traj_views (What/When/How-SW), traj_panels (Knowledge/Process/Next-work) and traj_status (the --status snapshot) - all under the 1,500-line threshold, so gen_trajectory.py's size-ratchet entry RETIRED rather than merely ratcheting down. The facade keeps the CLI, HTML_TEMPLATE, build_html and the guarded sibling-import self-heal, and re-exports every consumer-read name, so gen_open_items is untouched and the test suite needed only four monkeypatch sites repointed (gt._detour_points/gt._lane_candidates -> gt.traj_graph.*, two gt.subprocess -> gt.traj_parse.subprocess) - a re-export binds a VALUE, not the module a moved function resolves in. Render preservation was proven double-sided at every slice: the pre-split generator and the post-split generator emit byte-identical PROJECT_STATE.html (1,787,126 bytes, identical even before the as-of strip).

bootstrap.py main() (380 lines, complexity 41) decomposed into nine phase functions plus ScaffoldPlan/CopyOutcome dataclasses, with main() a 48-line sequencer; output identity proven three ways (the byte-compare scaffold suites, a pre/post --dry-run stdout diff across six flag variants including two ap.error refusal paths, and a full scaffold-tree diff). The complexity entry for bootstrap main was DELETED.

Census: the graph-layout class (10 blocks) dissolved by GraphGeom + route_graph/flat_graph; tier-node-build dissolved by TierSpec; the cli clique shrank 87 -> 85. Complexity paid down rather than re-stamped: arch_icicle 23 -> 19, sw_containment 28 -> 17, when_view deleted (under limit). Spine: 19 LLR Module cells re-homed to the modules that now own their symbols, the 11 owning SRs flipped Modified and then RATIFIED back to Verified on the recorded round-2 review verdict under gate-policy autonomous (brief: docs/ratify/2026-07-31-wi280.md), four new seams IF-082..085 filed for the cross-component edges the split moved off the facade, and check_trajectory's render-surface watch extended to the traj_* family with mutation-proven tests.

Two defects the review caught and this WI fixed: the split redded ruff check with seven F401s in the facade, invisible only because the WI's own SR flip had made lint advisory at G2 - ruff check is now in the signed close set; and ratify-fresh redded the composed tree on a stale brief, diagnosed as pre-existing trunk rot (reproducible at trunk by flipping any SR) that this WI was simply the first to expose, fixed by generating this window's own brief. Post-ratification composed tree: 20/20 PASS, exit 0.
