+++
id = "WI-321"
title = "A system requirement must be stand-alone: no work-item ids, no process-doc citations in its normative text (owner-raised 2026-07-26 at the first re-attestation sitting). SR-049 had grown `(process.md section 7)` and `(WI-316)` inside its Requirement - provenance a reader cannot resolve without this repo's history, in the one row that is supposed to be readable without it. Measured over all 110 SRs before filing: only FIVE rows carry the defect (WI ids in SR-026/049/054/060, a process-doc citation in SR-049), while 65 name a script and 6 name an artifact path - and those are LEGITIMATE, because this kit's product IS its scripts, so the name is the system under specification, not a citation. That measurement is what makes a narrow categorical rule affordable instead of a mass edit. SR-049 and SR-054 were cleaned when raised (both already sat in the open Modified window, so the fix cost no new attestation debt); SR-026 and SR-060 are Verified, so cleaning each flips it Modified and joins a sitting - the owner's to schedule. Also collapses the live-anchor triplication (rubric header + TC-055.Expected + SR-054.AcceptanceCriteria): the rubric is the instrument a critic reads, so it is the single home and the other two point at it."
workstream = "docs"
buildtier = "medium"
priority = 1
safety_class = "ordinary"
order = 318
+++

## Deliverable

The RULE, warn-first: standalone_sr_advisories flags a WI id or a process-doc citation in an SR's Requirement/AcceptanceCriteria, in its OWN advisory pipe and counter (sr-provenance-advisories) rather than folded into the AC lint whose name would then be a lie. Narrow BY MEASUREMENT, and the test carries the negative half - 65 SRs name a script, 6 an artifact path, 5 a rubric, and none of those trips it, because this kit's product IS its scripts so the name is the system under specification. Stated once in PROCESS.md section 3 (+560 bytes; baseline re-stamped 61,264 -> 61,824 in all three skill copies) so a downstream adopter inherits the standard and not just the checker. Fires on exactly the two rows the filing measured: SR-026 and SR-060 (3 cells). Their cleaning, and TC-055's de-triplication, are SPLIT OFF as WI-324 - each flips a Verified row to Modified and the window is open, which the spec and status.md both say is the owner's to schedule.
