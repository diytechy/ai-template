+++
id = "WI-358"
title = "A WI closing through the integrator reds R-D on the composed tree when status.md hand prose names it: test_forward_only_unit_over_the_real_meta_repo runs inside the merge bar, and the branch cannot scrub trunk-owned status.md. The Phase 4 acceptance pre-scrubbed by hand (commit 55777cc); the structural home is the claim step (refuse or warn when the claimed id appears in status.md hand prose) so the debt is paid before the branch exists, not discovered at merge."
workstream = "scripts"
buildtier = "medium"
priority = 2
safety_class = "ordinary"
+++

## Deliverable

DONE 2026-07-29. `integrate.py claim` refuses when the claimed id appears as a WI token in docs/status.md hand prose (generated block exempt; sentinel state machine kept byte-identical to check_trajectory's R-D so claim-time and merge-time agree — the reviewer could not split them). Refusal quotes line numbers + remedy. Review fixes folded: ids normalized at the CLI boundary (Windows glob casefolds), an unreadable status.md is a refusal, never a traceback. Accepted residuals (review findings 10/14, shared with R-D by design): tokens match inside code fences/URLs; prose added AFTER the claim still reds at merge.
