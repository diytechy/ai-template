+++
id = "WI-165"
title = "Process tab - render loops A/B as true circular loops intersecting at the shared LLM_Agent node"
workstream = "dashboard"
sr_refs = ["SR-070"]
needs = ["WI-144"]
buildtier = "strong"
order = 164
+++

## Deliverable

Process-tab loops A/B now render as closed responsive racetracks intersecting at one LLM_Agent junction; TC-056 asserts both closed cycles, the single shared node's per-loop degree, return-side placement, and deterministic/link behavior.
