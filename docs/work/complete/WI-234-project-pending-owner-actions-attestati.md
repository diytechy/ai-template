+++
id = "WI-234"
title = "Project pending owner actions (attestation blocks + conflict asks + quarantines) into a generated open-items section (owner directive 2026-07-19)"
workstream = "unattended"
sr_refs = ["SR-059"]
needs = ["~WI-232"]
buildtier = "medium"
safety_class = "high-risk"
order = 231
+++

## Deliverable

gen_trajectory.py --status now splices a SECOND marker block — `<!-- BEGIN GENERATED PENDING -->` at the END of docs/open-items.md, below the hand-authored OI briefs (byte-untouched by regeneration) — projecting every DURABLE pending-owner action, one line/pointer each, from durable state ONLY (never the out/dispatch journal, §11): (a) blocked WI rows carrying a BlockRef, with the `git show <train>:<path>` read path when the ratification doc lives only on a train branch (WI-229 shape; _blocked_pending/_train_carrying_path scan refs/heads/llm/train/*); (b) refs/llm/conflict/* records (WI-232) naming train + conflicted paths (_conflict_pending); (c) quarantined trains re-derived from the durable refs/llm/reservations/* — unreadable metadata or a missing train branch, the reconcile conditions, NOT the journal (_quarantine_pending); (d) the docs/run-state `ask:` line when NEEDS-HUMAN (_runstate_pending). Freshness rides the existing harness status-map step: `--status --check` byte-compares BOTH the status snapshot and the pending block (run_pending, mirroring run_status/_splice_status), so a stale projection fails the G3 gate. The dispatcher regenerates at its terminal decision — _regenerate_pending runs `gen_trajectory --status` best-effort after every terminal _write_runstate (_finish_dispatch review-ask + normal terminal, the diverged-head NEEDS-HUMAN, and the idle needs-human), journaling pending-regen-failed on any generator error so the terminal path never crashes. OPEN_ITEMS.template.md gains the block; bootstrap inserts the stack OI-3 brief ABOVE the marker (OI_PENDING_ANCHOR). 12 regressions in test_gen_trajectory_pending.py (real temp git repos): train-only read path + resolution, dev-tree plain path, conflict record + clear, quarantine reason + retire, readable-reservation-not-flagged, NEEDS-HUMAN ask vs RUNNING, --check trips on a hand-staled block, hand-authored region byte-untouched, vacuous without markers, dispatcher terminal regen + non-adopter no-op. C901 baseline unchanged; stdlib-only; Windows+POSIX. REWORK (review of c6e1601, 1 CRITICAL+6 minors): added source (a') _stranded_pending — a reserved WI whose row stays queued but whose present train carries a Blocked-WI trailer + frozen ratify doc (the WI-229 shape; git's trailer parser drops the blank-line-separated trailer, so _train_blocked_trailers line-regexes the raw %B bodies of base..tip) projects an attestation line with the train read path via _attestation_pointer, deduped against (a); unreadable refs/llm/conflict records now surface (not skipped); _splice_pending matches markers as exact full lines (tolerates quoted/indented markers), fails closed on inverted/duplicated markers, and preserves CRLF (newline=''); _regenerate_pending subprocess bounded at timeout=120. Live: the meta's own open-items.md now projects the WI-229 attestation line. +8 regressions.
