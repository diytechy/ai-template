+++
id = "WI-349"
title = "Nothing enforces the one-physical-line rule that check_trajectory's staged-close scan DEPENDS on. check_trajectory.py:1478 documents the assumption in its own docstring - 'a WI row is one physical line (no embedded newlines)' - because it compares `git diff --cached --name-only` output and HEAD's registry text line-wise. No check asserts it. Demonstrated 2026-07-28: a WI row was written with a literal newline inside a quoted Title cell, the full trajectory validator reported CLEAN, and the only thing that surfaced it was git warning about mixed line endings in the working tree - i.e. luck. A row like that also renders as a broken cell in the dashboard's detail JSON and makes the row invisible to any line-based tool downstream. Fix: an integrity check in check_trajectory's validate() that fails on any cell containing CR or LF, with the WI id and column named, plus a test that writes such a row and asserts it is caught. Cheap, and it closes the gap between a documented assumption and an enforced one - the enforcement-audit question asked of a rule the code already relies on."
workstream = "scripts"
buildtier = "quick"
priority = 1
safety_class = "ordinary"
order = 346
+++

## Deliverable

DONE 2026-07-28. check_trajectory.cell_integrity_errors(rows) reads the RAW registry cells before anything reasons about the graph they describe, and returns a hard error for any cell holding a CR or an LF, naming the WI id and the COLUMN - a bare 'row 47 is malformed' against a 352-row registry is not actionable, the cell is what the author has to find. Wired into main() beside load_wis's malformed/duplicate-id errors, which is the same class (a row the loader cannot be trusted to have read) rather than the warn-first coherence tier (a question about a row it read fine) - so it is an ERROR at every run, no --strict needed. Scoped deliberately to the WI registry: the line-wise HEAD comparison in staged_findings is what CREATES the requirement and it reads only this file; the spine registries are trace.py's. Kept OUT of load_wis so tests/test_wi_loader_sync.py's schedule-vs-check_trajectory drift lock is untouched. A broken id cell reports by ROW NUMBER, not a truncated id, because a truncated id is worse than none - it looks real and matches nothing. Guards (tests/test_trajectory.py, 5 tests): the offending registry is written through csv.writer rather than by hand-embedding a newline, so it exercises the real quoting path that made the defect invisible, and the test asserts the premise first (one CSV row really does span two physical lines). LF caught, CR caught, broken id cell reported positionally, a mutation twin with the newline removed asserted CLEAN, and - the finding itself pinned - a test that load_wis, validate and ssot_findings ALL pass on that row, so the guard cannot start silently riding on some unrelated rule. Four mutants (check unwired, CR dropped from the detected set, column name dropped from the message, row-number fallback reverted to the truncated id) each fail the suite. Found while writing the guard: Path.read_text(newline=) is 3.13+, so a 3.11-floor kit must use open() - the test caught it. Module-size ratchet re-stamped 2063 -> 2135, reviewed bump with the reason in the log; check_trajectory.py has now taken FOUR upward bumps with no decomposition between them, which is a cost the WI-280 deferral is accruing. Bar: smoke 490 passed / 27.9 s; the 16 non-test G3 steps PASS.
