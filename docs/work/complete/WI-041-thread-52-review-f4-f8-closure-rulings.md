+++
id = "WI-041"
title = "THREAD_52_REVIEW F4-F8 closure (rulings 2026-07-09)"
workstream = "scripts"
sr_refs = ["SR-037", "SR-038"]
needs = ["WI-031"]
order = 40
+++

## Deliverable

F4 iterative _cycles + _dag_ranks (deep-chain tests; icicle bounded-by-construction, documented); F5 (a) guarded sibling import + load_script shim + in-process + self-heal tests + convention comment; F6 softened PROCESS_OPTIONS opener to Builds-on-§7; F7 STATUS.template opt-out rewording; F8 accepted-as-is (ruling only, no code). Specs = the review findings + RULED notes; all RESOLVED in THREAD_52_REVIEW.md.
