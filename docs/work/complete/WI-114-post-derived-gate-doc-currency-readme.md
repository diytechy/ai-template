+++
id = "WI-114"
title = "Post-derived-gate doc currency - README config table + re-sync skills/state-reconciliation guidance"
workstream = "docs"
needs = ["WI-096", "~WI-109"]
order = 113
+++

## Deliverable

Owner-directed 2026-07-12 (three asks). (1) ADOPTING sec 6 re-sync guidance extended: the 'Re-weigh the opt-in layers' paragraph now directs adopters to recheck what's NEW across the sync range - diff skills/INDEX.csv for new/updated skills (materialize --agents / refresh --sync), scan process-options.md for opt-in layers added since the pinned commit, revisit the vendorable packs (guardrails/efficiency/knowledge); and the derived-gate migration bullet gains the STATE-RECONCILIATION recipe - before trusting the migrated value, reconcile Status/SN-sections against the registries as of the last gate-bump commit (git log -- docs/gate then git diff <sha>..HEAD -- docs/requirements docs/test), staging post-attestation additions per the new model (new SNs -> Draft-needs section; un-re-reviewed SRs -> Status=Draft) so derive_gate reproduces the gate the history actually attests; residual disagreement = the finding, resolve before relying. downstream-resync skill gains the two matching compact bullets (source edited -> .claude/.agents fan-out re-synced byte-identical). (2) Root README gains 'Configuration at a glance (defaults vs. this repo)': a 15-row map of every docs/* runtime knob - fresh-scaffold default (verified against a live bootstrap: gate=derived G1, gate-policy=attended, push=human, review=1, privacy=false, agents.csv inert, everything else absence-defaulted), toggle direction (opt-in vs opt-out), and THIS repo's setting (single-ratify+register, managed routing on 6 rows/2 families fable-led, run-phase BUILD, guardrails off, all absence-defaults on) - each file's own header stays the canonical doc, the table is the map; closing pointers to bootstrap --stack/--omit (kit-profile) and PROCESS_OPTIONS for layer semantics. (3) Doc-currency audit for the derived gate: shipped surfaces were already clean (WI-095/096); the root README's 'gates at a glance' was accurate-but-silent - it now states the active gate is DERIVED (computed+cached by derive_gate.py, advanced by ratifying in a reviewed Status-change commit). Also fixed: docs/specs/WI-109.md orphan (warn-only since its commit) linked from its log entry. Docs only - no kit-script change, no spine change, byte-budgeted files untouched.
