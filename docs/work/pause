# Tracked pause (concurrency-restructure §5.6): the dispatcher stops CLAIMING at
# the next session boundary; the in-flight lane finishes and merges. Unpausing is
# a reviewed deletion commit of this file.
reason = "Supervisor stop 2026-09-03 (handoff §4 stop condition: batch claim, one-id banner). After the WI-586 merge the dispatcher claimed WI-589;WI-584;WI-587;WI-588 as one exclusive batch on wi-589-two-verified-defects-around-th, and the worker's session-001 record names `wi: WI-589` alone (docs/iteration/wi-589-two-verified-defects-around-th-001-20260903-181248.log). The worker argv and the session prompts carry all four ids, and session 002 closed WI-584 and began WI-587, so the batch IS being worked - but every session record and telemetry commit attributes that work to WI-589 (the WI-580 brief defect). The lane runs to its merge; claiming holds until the owner rules whether a four-row spine batch with misattributed session records may stand and whether WI-580 must land before the next claim. Also merged this run: WI-585 (no round owed) and WI-586 (six cross-family rounds, legacy rollup compiled because the mechanical close staled the APPROVE round's tree - a structural defect to rule on). Delete this file in a reviewed commit to resume at the generated frontier."
since = "2026-09-03"
