# Tracked pause (concurrency-restructure §5.6): the dispatcher stops CLAIMING at
# the next session boundary; the in-flight lane finishes and merges. Unpausing is
# a reviewed deletion commit of this file.
reason = "Supervisor stop 2026-09-03, two §4 conditions. (1) REFRESH RED ON PRODUCT CODE, inherited from the TRUNK: the batch lane wi-589-two-verified-defects-around-th (WI-589;WI-584;WI-587;WI-588, all four built with trailers and closed, round 011 gpt-5.6-terra APPROVE findings=0 with no findings body, four supervisor-compiled legacy rollups at 4d7a92b8 cleared the gate) was refused at the refresh because the bar runs check.py --stage DevStg-Tests (the rows' own bar; the adjudication lanes took the derived 12-step read) and that tier is RED on trunk 175c68ec itself, every red reproduced at the trunk root: lint (3 ruff errors: tests/test_agent_loop.py:16, tests/test_trace_hats.py:38, tests/test_trajectory_holdban.py:121), backlink-coverage 45.8% < 50%, figures (53 of 289 without provenance), doc-refs --strict (65 dangling on trunk, 61 on the lane), complexity --mode enforce (agent_common.commit_telemetry 21, agent_loop.worker_endstate 18, gen_verdict_rollup.main 16, integrate._verdict_gate 19), trace.py --strict (SR-181 orphan, LLR-197 citation frame), check_trajectory (the schedule->trace seam ERROR, WI-582's), and tests/test_check_docs.py::test_meta_repo_has_zero_unexplained_orphans over docs/handoff-2026-09-03.md (1 failed, 3374 passed). No lane at DevStg-Tests can merge until the trunk is greened at that tier; the lane worktree and branch are left in place, merge-ready otherwise. (2) The batch-claim condition (WI-580 one-row brief), and three further multi-row-lane defects it exposed: the per-session walk passes a row on its trailer before its close ritual runs (WI-589 stayed in active/), dispatch._close_done_adjudication dies reading a spec already moved to complete/ before deciding the lane is not an adjudication (fatal exit 2), and worker preflight refuses a resumed batch with terminal rows, after which the dispatcher's partial-close arm commits residue as-is and dies (b0be72c7). Also to rule: an adjudication lane's mechanical close stales its own APPROVE round (WI-586 merged on a compiled rollup for that reason alone); round 011 approving a four-row spine train with an empty findings body. Merged this run: WI-585 (9dfbc971, no round owed), WI-586 (767d6bdf, six cross-family rounds; minted WI-587/588/589). Delete this file in a reviewed commit to resume at the generated frontier."
since = "2026-09-03"
