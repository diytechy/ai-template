# Tracked pause (concurrency-restructure §5.6): the dispatcher stops CLAIMING at
# the next session boundary; the in-flight lane finishes and merges. Unpausing is
# a reviewed deletion commit of this file.
reason = "Supervisor stop 2026-09-03 (handoff §4 stop condition: batch claim, one-id banner). After the WI-586 merge the dispatcher claimed WI-589;WI-584;WI-587;WI-588 as one exclusive batch on wi-589-two-verified-defects-around-th; the worker argv carries all four, and the session records walk the batch one row per session (001 wi=WI-589, 002 wi=WI-584, 003 wi=WI-587 under docs/iteration/), so attribution is NOT wrong. What fires is the WI-580 brief defect the handoff names: each session's brief renders ONE row and opens 'this assignment is your whole scope', so the model working WI-587 is never told WI-588 shares its lane and no session judges the batch as a whole. Session 003 (WI-587) ended TIMEOUT at 5114 s with registry + test edits left uncommitted; session 004 inherits the residue. The lane runs to its merge; claiming holds until the owner rules whether a four-row spine batch built one-row-blind may stand and whether WI-580 must land before the next claim. Also merged this run: WI-585 (no round owed) and WI-586 (six cross-family rounds; a legacy rollup had to be compiled because the mechanical close staled the APPROVE round's tree - a structural defect to rule on). Delete this file in a reviewed commit to resume at the generated frontier."
since = "2026-09-03"
