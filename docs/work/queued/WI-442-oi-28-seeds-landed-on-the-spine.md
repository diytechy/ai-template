+++
id = "WI-442"
title = "OI-28 execution beyond the boundary declaration WI-441 carries: land the spine rows — the boundary-actor declaration naming the direct-session agent (the unattended loop reading as a mechanized instance of the same actor class), one SR declaring the shipped template set a traced product artifact class with test_dogfood_sync as its named verification (never an LLR per template — proportionality), and regularize the two accidental 'agent CLI' IF rows into the declared-complete external set during part B's schema pass. All rows Draft on arrival; no re-attest window."
specref = "docs/requirements/open-items.toml"
workstream = "lock-program"
sr_refs = []
needs = ["WI-441"]
buildtier = "medium"
safety_class = "spine"
priority = 2
+++
