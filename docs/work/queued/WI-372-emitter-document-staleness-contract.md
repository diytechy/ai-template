+++
id = "WI-372"
title = "tests/test_gen_trajectory.py's _every_emitter_document mixes two documents with different truth-times: most call sites read the COMMITTED PROJECT_STATE.html (asserting properties of markup generated by an older renderer), while the WI-367 sweep had to regenerate and exclude the shipped copy with a local reason to avoid a standing red - a latent trap where an invariant-tightening change reds tests via the stale committed artifact rather than via the code under test (the WI-367 builder's finding: test_t8_no_wire_passes_through_an_unrelated_node_box and test_a2_the_repos_own_shipped_dashboard_holds_the_invariant survive only because their invariants happen to hold in the older markup). Make the contract explicit in the helper: a caller declares shipped-or-fresh, the shipped readers are named as compatibility pins, and a fresh-emitting fixture is shared instead of per-test regeneration."
workstream = "tests"
specref = "docs/reviews/WI-367-REVIEW-A.md"
buildtier = "medium"
priority = 3
safety_class = "ordinary"
+++
