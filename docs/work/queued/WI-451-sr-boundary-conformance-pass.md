+++
id = "WI-451"
title = "The SR-tier boundary conformance pass (owner-raised 2026-08-13, sitting decision 2.7): a system requirement states what a scope provides at its boundary, yet 75 of 148 live SRs name a .py script in requirement text — ~25 plausibly naming the boundary port itself (entry points the outside invokes), ~50 naming internal modules, which is implementation in the system tier. Slice 1, the CENSUS (safe pre-sitting): classify each of the 75 against the boundary inventory and the IF registry — port (the named artifact is a declared external crossing), internal (re-statement candidate), or mixed — and size the re-statement per row. Slice 2, gated on the sitting ruling decision 2.7(a): re-state the internal class against declared interfaces (SN-037's acceptance is the mechanization: every SR input/output references a declared interface, unresolved references become findings), demote genuinely design-level content to the LLR tier, and NARROW SR-126's script-name carve-out to boundary-crossing artifacts in the same act — one coordinated window, the WI-444 machinery re-used. Do not begin slice 2 without the sitting's ruling; do not weaken any obligation in re-statement (the WI-444 token-verification bar applies)."
specref = "docs/plans/2026-08-13-sitting-2-boundary-and-context.md#54-wi-451--sr-tier-boundary-conformance-pass"
workstream = "lock-program"
sr_refs = []
needs = []
buildtier = "strong"
safety_class = "spine"
priority = 2
+++
