+++
id = "WI-441"
title = "OI-14 part A program (ruled A3 + A6, 2026-08-13): derive the component partition from the system's declared inputs and outputs. In order: (1) declare the SYSTEM BOUNDARY — what is outside, what crosses, each crossing typed, the set declared complete — carrying OI-28's two seeds (the LLM-agent-in-a-direct-session actor whose crossings are instructions in and artifact edits out through the git-enforced hook floor, SR-019's rationale becoming the row's rationale; and the shipped template set as a traced product artifact class anchored at one SR with test_dogfood_sync as its verification); (2) first-cut functional decomposition from the needs, as signals; (3) score candidate partitions with ONE HOME PER BEHAVIOUR as a hard constraint and cut-count as tie-breaker; (4) produce a RANKED SHORTLIST with per-candidate rework sizing and behaviour-straddle lists; (5) PROVISIONALLY ADOPT the top candidate warn-first (owner's confirmed execution mode — Component is a traced cell, no re-attest opens) and package the shortlist for ratification at the owner's return. Also rule SR.Area explicitly (derive from Component or retire — never leave it authored-and-unread), and confirm the containment rule actually covers the 46 IF rows cross_component_findings is vacuous for. The boundary-rung enforcement lands as A6: applies-when, warn-first."
specref = "docs/requirements/open-items.toml#OI-14"
workstream = "lock-program"
sr_refs = []
needs = ["WI-440"]
buildtier = "strong"
safety_class = "spine"
priority = 1
+++
