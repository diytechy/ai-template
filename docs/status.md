# Meta-Repo Status — Blackboard

The **working surface** for developing the kit itself — the same `status.md`
pattern the kit scaffolds downstream, self-applied. This file is **forward-only**:
only what must happen **next** lives here. Everything backward-looking has a
home elsewhere — don't restate it here:

- **What shipped / verdicts / session record:** [log.md](log.md).
- **Owner decision briefs:** [open-items.md](open-items.md) — one `## OI-N`
  section per pending decision (blast radius, options, recommendation); a
  ruling appends to the log's Decisions and the section is removed.
- **The WI registry (every backlog + deferred item, with its reason):**
  [work-items.csv](requirements/work-items.csv) — the dashboard is the root
  [`PROJECT_STATE.html`](../PROJECT_STATE.html).
- **Spec-of-record + rubric conventions:** [specs/README.md](specs/README.md) ·
  [rubrics/README.md](rubrics/README.md) (dogfooded scaffold boilerplate).
- **Design history:** [archive/](archive/README.md).
- **Process (kit source):** [PROCESS.md](../project-trajectory/PROCESS.md) ·
  [PROCESS_OPTIONS.md](../project-trajectory/PROCESS_OPTIONS.md) (this repo has
  no scaffolded `docs/process.md`; the masters are the reference).
- **Working rules:** [CLAUDE.md](../CLAUDE.md) + the `session-protocol` skill.

---

## Current State

<!-- BEGIN GENERATED STATUS -->
_Derived facts — regenerated by `python project-trajectory/scripts/gen_trajectory.py --status`; do not hand-edit (the forward-only intent below is hand-authored)._

- **Active gate:** derived **G3** (per-phase `1=G3;2=G3;3=G3;4=G3`, derived current **phase=4**) — the harness at the derived gate is the bar; [`derive_gate.py`](../project-trajectory/scripts/derive_gate.py) computes it, cached to [`docs/gate`](gate).
- **Spine:** **SN=25 SR=109 LLR=97 TC=100** (0 drafts) · 65 seams · 5 components.
- **Open items** _(projected from [open-items.md](open-items.md) — each item's blast radius, options, and recommendation live there):_
  - **OI-4** — rule WI-097 (LICENSE + public/private intent) — no rec; needs the owner's public/private intent first.
  - **OI-7** — rule WI-123 (review cadence) — rec: wait for ≥ 2 phases of medium-BUILD evidence before switching from per-slice review.
<!-- END GENERATED STATUS -->

- **Bar (per commit):** `python -m pytest -q -n auto -m smoke` (~47 s) +
  `python project-trajectory/scripts/check_docs.py --root . --stale`, both green.
  At slice/phase close: the full suite `pytest -q -n auto` and `check.py` at the
  derived gate — every G3 step, including the G3-only `lint`, `dupes`, and
  `--require-verified`, the `--strict` trajectory step, and the `status-map`
  freshness gate on the snapshot above. The forward-only intent lives **below**
  the marker; the derived snapshot above is generated (never hand-edited).
- **Run-state:** [run-state](run-state) holds the declared value (don't
  paraphrase it here); when it reads NEEDS-HUMAN its `ask:` line is the
  canonical one-line summary the stop banner headlines.

- **Open-items policy** _(`OI-N` ids are stable and never renumbered;
  ratification history lives in [log.md](log.md) Decisions; `docs/gate-policy`
  is **`autonomous`** — owner directive 2026-07-15 — so the loop does **not**
  pause on the open items projected above; each item's depth is in
  [open-items.md](open-items.md))._
- **Deferred backlog** _(first-class `deferred` rows; each carries its reason in
  the registry — read it there, not here):_ **WI-060 · WI-061 · WI-062 ·
  WI-063 · WI-065 · WI-082 · WI-097 · WI-108 · WI-123 · WI-158 · WI-159 ·
  WI-187** in [work-items.csv](requirements/work-items.csv)
  (WI-097/WI-123 are the OI-4/OI-7 subjects above).
- **External follow-up** _(not this repo's work):_ guardrails content enrichment
  is owner-ruled to live in `TheColliny/FableClaudeMDForOpus` (vendored downstream).
- **Next action:** **WI-229 landed** 2026-07-19 (the M-05 spine migration:
  SR=108 LLR=96 TC=99, zero strict-trace findings, REVIEW-A APPROVE +
  consumption, integrated by manual re-review merge after the dispatcher's
  blocked-exit dead-end; see [log.md](log.md)). Of the three kit defects that
  forced the manual path, all three have now landed 2026-07-19: **WI-240**
  (park/quarantine details carry the failing step via `agent_common._failure_tail`,
  not the first 200 chars), **WI-238** (a blocked disposition adopts an absent
  BlockRef column in-transaction — the WI-229 registry-extension precedent — or
  fails loudly naming it, ending the parked-error loop), and **WI-239** (a later
  `WI:` completion supersedes an earlier `Blocked-WI:` so a cured blocker is
  survivable — shared latest-trailer-wins evidence + a `refs/llm/blocked/<train>`
  resume-once record give a reserved blocked train one worker resume per
  integration-head advance instead of discarding finished work). WI-241
  (the adversarial reviewer-prompt hardening + rubric) landed the same day.
  **WI-242 landed** 2026-07-19 (dogfood-sync): the live work-items.csv adopted
  the full 17-column template schema in template order (behaviour-neutral —
  every consumer proven identical old-vs-new), and `tests/test_dogfood_sync.py`
  now gates the structural surfaces (registry headers as ordered template
  supersets, the `agent-resume` launcher engine-line + exported-var set,
  stack.ini sections) against drift while owner dial VALUES stay free.
  **Dashboard-quality workstream (2026-07-20, build + independent-opus-review loop):**
  - **WI-244 DONE** — taste-skill legibility distillation → `dashboard-usability`
    rubric anchors T5-T7; REVIEW-A opus **APPROVE**.
  - **WI-245 DONE** — `project-trajectory/EXTERNAL_SKILLS.md`, the curated
    third-party-skill index; REVIEW-A opus **APPROVE** (all 12 license/trust
    claims primary-source-verified).
  - **WI-243 DONE** — the perceptual re-fire, **FAIL-CLOSED** (owner ruled
    2026-07-20). The critique TCs name the `shoot.mjs` PNG matrix; a stale render
    surface is WARN at the commit bar but **ERROR under `--strict` (G2/G3)** — it
    cannot reach a green gate (agent-proof), while a plain commit stays warn-first.
    Downstream-safe (vacuous without a `Verification=Critique` SR — none ship;
    opt-out via `docs/trajectory-check`). Proven end-to-end (fired → 075 critique
    confirmed the tab + WI-159 deferred → WI-159 fixed it → 076 re-judged →
    cleared). REVIEW-A APPROVE (1 pre-existing/out-of-scope MINOR accepted). Lands
    green now (`--strict` exit 0); reddens on the next render change until
    re-critiqued.
  - **WI-159 DONE** — the Knowledge (OKF)-tab density fix: the tab now opens
    **start-collapsed** (6 OKF type-blocks, SN→SR→LLR→TC spine, descend for the
    concepts) — no wall of nodes, no clipped labels. **The hard-to-read tab is
    fixed.** Built by opus, REVIEW-A opus APPROVE (findings=4, all MINOR);
    pixel-verified both themes by builder + reviewer + me.
  - **WI-246 / WI-247 / WI-248 DONE** (2026-07-20, traincar build + independent
    opus REVIEW-A). The 3 minor render nits 075-CRITIQUE surfaced: How-SW CMP
    labels now wrap id/name instead of truncating (WI-246); the DAG phase-accent
    palette is now 8 hue-separated, dataviz-validated colors (WI-247 — REVIEW-A's
    first pass caught a MAJOR, the new palette byte-collided with
    `STATUS_FILL`/`TIER_FILL`, reworked and re-verified collision-free); the
    What-icicle 390px scroll affordance was independently re-verified
    already-working, no code change needed (WI-248). [077-CRITIQUE.md](reviews/077-CRITIQUE.md)
    confirms all three in the re-rendered PNGs; `check.py --gate G3 --jobs 0`
    RESULT: PASS (16/16), full suite 1238p/1s @ 90.92% coverage.
  **Dashboard-quality workstream COMPLETE** (WI-243/244/245/159/246/247/248 all
  done, opus build+review loop end to end — every 075-CRITIQUE finding closed).
  **Queued next: WI-251** (2026-07-20 owner directive) — spec-lifecycle
  close-side fix: absorb-verify sweep of the stale done-WI specs + fail-closed
  R-F in `check_trajectory`; scope in [specs/WI-251.md](specs/WI-251.md).
  Remaining owner surfaces: OI-4 (license/public-private intent) and OI-7
  (review cadence) under `gate-policy: autonomous`; the deferred backlog as
  listed above; the merge-to-main sitting for `dualplan-routing-fix` (and
  `guardrails-fable-method`) remains a deliberate owner decision (push-policy:
  human). OPENCODE-KIMI/GROK are ENABLED (pathway live-verified 2026-07-19);
  builder preference is Anthropic-led per tier (Fable strong / Opus medium).

## Scope

- **Goal:** keep the kit **maintainable and trustworthy** — the
  `PROJECT-VISION:` tag opening [README.md](../README.md) is canonical.
- **Supported platforms:** Windows + POSIX; kit scripts stdlib-only on
  Python 3.8+.
- **Non-goals (self-application boundary):** no `run.*` product launchers (the
  kit's "product" is `project-trajectory/` + `tests/`); no scaffolded
  `docs/process.md` (the masters live in `project-trajectory/`).
