# Meta-Repo Status — Blackboard

The **working surface** for developing the kit itself — the same `status.md`
pattern the kit scaffolds downstream, self-applied. This file is **forward-only**:
only what must happen **next** lives here. Everything backward-looking has a
home elsewhere — don't restate it here:

- **What shipped / verdicts / session record:** [log.md](log.md).
- **Owner decision briefs:** [open-items.md](open-items.md) — one `## OI-N`
  section per pending decision (blast radius, options, recommendation); a
  ruling appends to the log's Decisions and the section is removed.
- **The WI registry (every backlog + deferred item, with its reason):**
  [work-items.csv](requirements/work-items.csv) — the dashboard is the root
  [`PROJECT_STATE.html`](../PROJECT_STATE.html).
- **Spec-of-record + rubric conventions:** [specs/README.md](specs/README.md) ·
  [rubrics/README.md](rubrics/README.md) (dogfooded scaffold boilerplate).
- **Resuming in a new session / closing the template out:**
  [wrap-up-plan.md](wrap-up-plan.md) — sequencing, the deferred-backlog review with
  queue/retire recommendations, and the standing hazards. A pointer doc: this file
  and the registry stay authoritative.
- **Design history:** [archive/](archive/README.md).
- **Process (kit source):** [PROCESS.md](../project-trajectory/PROCESS.md) ·
  [PROCESS_OPTIONS.md](../project-trajectory/PROCESS_OPTIONS.md) (this repo has
  no scaffolded `docs/process.md`; the masters are the reference).
- **Working rules:** [CLAUDE.md](../CLAUDE.md) + the `session-protocol` skill.

---

## Current State

<!-- BEGIN GENERATED STATUS -->
_Derived facts — regenerated by `python project-trajectory/scripts/gen_trajectory.py --status`; do not hand-edit (the forward-only intent below is hand-authored)._

- **Active gate:** derived **G3** (per-phase `1=G3;2=G3;3=G3;4=G3`, derived current **phase=4**) — the harness at the derived gate is the bar; [`derive_gate.py`](../project-trajectory/scripts/derive_gate.py) computes it, cached to [`docs/gate`](gate).
- **Spine:** **SN=25 SR=110 LLR=107 TC=110** (0 drafts) · 69 seams · 5 components.
- **Open items** _(projected from [open-items.md](open-items.md) — each item's blast radius, options, and recommendation live there):_
  - **OI-7** — rule WI-123 (review cadence) — rec: wait for ≥ 2 phases of medium-BUILD evidence before switching from per-slice review.
- **Ready frontier** _(dependency-ready WIs in build order — generated from the scheduler; a closed WI drops out automatically, so this list is never stale and never names a `done` id):_
  - **WI-300** `P1` — Critique/test boundary
  - **WI-305** — Dashboard has no next-work surface (119-CRITIQUE T1)
  - **WI-306** — Dashboard landing (What) tab does not start-collapsed (119-CRITIQUE T2)
  - **WI-307** — Dashboard SVG emitters do not reflow at their declared viewport widths (119-CRITIQUE T7)
  - **WI-308** — Triage the 22 dangling doc references WI-062's tiering exposed, then wire [step:doc-refs]…
<!-- END GENERATED STATUS -->

- **Bar (per commit):** `python -m pytest -q -n auto -m smoke` (~3.3 min) +
  `python project-trajectory/scripts/check_docs.py --root . --ignore docs/test/report.md --stale`,
  both green. At slice/phase close: the full suite `pytest -q -n auto`
  (~4.2 min) and `check.py` at the derived gate — every G3 step, including the
  G3-only `lint`, `dupes`, and `--require-verified`, the `--strict` trajectory
  step, and the `status-map` freshness gate on the snapshot above. The
  forward-only intent lives **below** the marker; the derived snapshot above is
  generated (never hand-edited).
- **Run-state:** [run-state](run-state) holds the declared value (don't
  paraphrase it here); when it reads NEEDS-HUMAN its `ask:` line is the
  canonical one-line summary the stop banner headlines.
- **The G3 harness RUNS here and is GREEN** — `check.py --gate G3 --jobs 0`
  **RESULT: PASS, all 17 steps** (close record in [log.md](log.md)). Read that
  as a *fresh* green, not a standing one: `perceptual-stale` is path-triggered,
  so it re-reds the moment `gen_trajectory.py` or `shoot.mjs` changes again and
  clears only with a critique that post-dates the change — budget a
  re-dispatch with every render-touching commit. Two standing rules came out of
  getting here: a census sanction (`docs/dupes-allow`) **is** accepting the
  duplication, so never reach for one to green a step (the four
  `agent_dispatch.py` blocks were *extracted*, not sanctioned); and **never
  revert a real fix, or sanction a check, to green a step**. The harness had
  never executed on this machine before 2026-07-25 — the toolchain floor was
  unmet — so treat any earlier "all passing" claim as a partial run.

- **Open-items policy** _(`OI-N` ids are stable and never renumbered;
  ratification history lives in [log.md](log.md) Decisions; `docs/gate-policy`
  is **`autonomous`** — owner directive 2026-07-15 — so the loop does **not**
  pause on the open items projected above; each item's depth is in
  [open-items.md](open-items.md))._
- **Ready frontier:** see the generated **Ready frontier** list in the snapshot
  above — derived from the scheduler in build order (`Priority 1` rows first);
  a closed WI drops out automatically, so this surface never strands a done id. The dashboard-render rows
  (`Verification=Critique`) each need a fresh, family-heterogeneous render
  critique after the change (the `render-dashboard-critique` skill), not just a
  passing test. The 2026-07-23 agent-resume run's outcome — what integrated,
  what parked, and the defects it surfaced — is in [log.md](log.md).
- **Deferred backlog** _(first-class `deferred` rows; each carries its reason in
  the registry — read it there, not here):_ **WI-061 · WI-063 · WI-108 ·
  WI-123 · WI-158 · WI-187 · WI-271 · WI-277 · WI-280** in
  [work-items.csv](requirements/work-items.csv). WI-123 is the OI-7 subject
  above; WI-280 is the `strong`-tier H-2 decomposition — a deliberate design
  program, not a cleanup — and WI-277 is hard-gated behind it. The 2026-07-25
  review of this list (recommendations, and what the owner acted on) is
  [wrap-up-plan.md](wrap-up-plan.md) §5.
- **Perceptual debt gates the two render trains.** The critique judges the whole
  document and APPROVE requires **every** anchor, while the fail-closed
  perceptual gate needs a CRITIQUE APPROVE at the reviewed head — so a render
  train cannot close while any unrelated anchor fails, including anchors it
  never touched:
  - **The semantic-tabs train integrated 2026-07-25** once the owner attested
    its SR-084 dispatch — so **A1 is no longer blocked**, and what SR-052 still
    needs is A1, A3 and A4's broader arithmetic core **bound** to child LLR/TCs
    (A2 is done: LLR-101/TC-104). The close record, including the two mechanical
    merge conflicts and how they were resolved, is in [log.md](log.md).
  - **SR-053's uniformity set is now fully bound** — U5/U3/U1
    (LLR-102/103/104, TC-105/106/107) and, 2026-07-25, **U2 and U4**
    (LLR-106/107, TC-109/110), the two anchors no defect WI owned. Close record
    in [log.md](log.md).
  - From the second round, both A2/A4 splits were **ruled** (2026-07-24) by
    verifying against the artifact rather than by a third dispatch. The A2 half
    is **closed** — the children-presentational `role="img"` cause is fixed and
    bound (LLR-101/TC-104); the close record is in [log.md](log.md). The focus-
    ring half is also **closed** — **1.00:1 — invisible — on the phase-3 block**
    fixed with a palette-independent per-fill ring, bound (LLR-105/TC-108).
    **A4's broader arithmetic core (every text/fill pair, beyond just the ring)
    still has no dedicated child LLR** — the existing A4 tests still cite the
    coarse `LLR-053`.
  - **The CRITIQUE/TEST BOUNDARY is RULED — OI-9 / WI-300, option (f)**
    (2026-07-25; ruling + alternatives passed over in [log.md](log.md)'s Decisions).
    **Decompose the LLR/TC layer** rather than add an anchor-classification field:
    each mechanizable anchor becomes a **child LLR bound to a TC with
    `Automated: Yes`**, and an SR keeps `Verification=Critique` only while a
    *perceptual* child remains under it. The critique was never a parallel system —
    `TC-053` is `Verifies: SR-052;LLR-053`, `Level: Critique`, `Automated: No`, the
    same columns `TC-051` fills with `Automated: Yes`. The defect is that
    `LLR-053/054/055` each near-verbatim restate their SR, so all four A-anchors
    collapse into one all-or-nothing TC: **that paraphrase, not critique policy, is
    why a train had to pass the whole rubric.** Per-anchor pass
    ([specs/WI-300.md](specs/WI-300.md), the spec of record): **13 of 17 mechanize**
    and the residue is **entirely inside SR-054** — SR-052 and SR-053 have none, so
    both flip to `Verification=Test` and all seven queued/blocked render rows stop
    owing a CRITIQUE. Needs **no gate change** (an SR drops out of
    `_load_critique_srs` on its own) and **no new checker** (`trace.py` coverage
    already enforces "the anchor names its test").
  - **HARD SEQUENCING on (f):** land every child TC with real `Evidence` **before**
    flipping an SR's `Verification` — never the reverse. A flip ahead of its tests
    retires an anchor nothing enforces, which is the one way this ruling fails.
  - **⚠ THE FLIP IS AN OWNER CALL, NOT A BUILD STEP — and it is now the only
    thing between the render rows and their exit from the critique chain.**
    Reading the delivered bindings before flipping shows the spec's "SR-052 and
    SR-053 have no perceptual residue" is not what the builds actually recorded:
    **every** child LLR ends by delegating a residue upward to its coarse parent
    — LLR-102 "near-duplicate but non-identical hues", LLR-103 "spacing, exact
    visual weight", LLR-104 "read as visually uniform", LLR-101 "reads as
    well-named" — and 120-CRITIQUE reported LLR-102's residue as **live**. By the
    ruling's own rule (`Verification=Critique` only while a perceptual child
    remains) neither SR may flip today. The ruling anticipated exactly this and
    reserved the call: *"if the owner judges the narrowing too lossy, those two
    stay perceptual."* What is new is that the residue is **wider than the U3/U4
    narrowing the ruling discussed**. Decide, then flip — don't flip first.
    Full argument + evidence in [log.md](log.md).
  - **Remaining binding work:** SR-052 still needs **A1**, **A3**, and **A4's
    broader arithmetic core** bound (A2 is done, LLR-101/TC-104; the owning tests
    largely exist and cite the coarse `LLR-053`). SR-053's set is complete.
    A child row cannot
    land ahead of its test: `Draft` escapes `--require-verified` but `derive_gate`
    returns **G0** for a draft, so one would drop the gate off G3.
  Evidence: [115](reviews/115-CRITIQUE.md) (SR-054, APPROVE f=0),
  [116](reviews/116-CRITIQUE.md) (SR-052, f=1), [117](reviews/117-CRITIQUE.md)
  (SR-053, f=7), [118](reviews/118-CRITIQUE.md) (SR-052 re-fire, f=1).
  **Any fix to `gen_trajectory.py` re-reds the perceptual gate** until a fresh
  critique post-dates it — budget a re-dispatch with every render fix. Never
  revert a real fix to green a checker.
- **Retired:** **WI-082** 2026-07-22 (decompose `bootstrap.py main()`) —
  superseded by WI-280, which now owns it as a concrete first slice. And
  the `--border` boundary-contrast row 2026-07-24 — retired rather than
  deleted so an over-claimed critic finding and its refutation stay
  traceable; that is evidence about critique reliability, not dead weight.
- **External follow-up** _(not this repo's work):_ guardrails content enrichment
  is owner-ruled to live in `TheColliny/FableClaudeMDForOpus` (vendored downstream).
- **Next action:** the next `agent-resume` launch works the **Ready frontier**
  above top-down (`Priority 1` rows first — each row's spec carries the why and
  the dependency it unblocks; a spec link lives on its `work-items.csv` row).
  **OI-4 and OI-8 are ruled** (2026-07-25 — Apache-2.0, and hosted CI on every
  branch push; rulings in [log.md](log.md)'s Decisions), leaving the owner
  exactly one standing call the loop won't make: **merge-to-`main`** for
  `dualplan-routing-fix`. OI-7 (review cadence) stays parked by its own
  recommendation. The remaining archive-anchored rows **WI-061 / WI-063**
  (deferred) still want per-row re-specification versus the now-available
  `retired` disposition. The two cheap
  growth sensors from the review tail already landed as tests — the per-module
  size ratchet (`tests/test_module_size_ratchet.py`) and the dashboard size
  budget (`tests/test_dashboard_size_budget.py`).
- **Merge-to-main** for `dualplan-routing-fix` (+ `guardrails-fable-method`)
  stays a deliberate owner decision (push-policy: human). OPENCODE-KIMI/GROK
  enabled; builder preference Anthropic-led per tier (Fable strong / Opus
  medium).

## Scope

- **Goal:** keep the kit **maintainable and trustworthy** — the
  `PROJECT-VISION:` tag opening [README.md](../README.md) is canonical.
- **Supported platforms:** Windows + POSIX; kit scripts stdlib-only on
  Python 3.11+.
- **Non-goals (self-application boundary):** no `run.*` product launchers (the
  kit's "product" is `project-trajectory/` + `tests/`); no scaffolded
  `docs/process.md` (the masters live in `project-trajectory/`).
