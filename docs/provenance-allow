# Reviewed exceptions to the citation-frame advisory (trace.py, warn-only).
#
# One entry per line: `<ROW-ID> <Cell> <token>`, then ` — OI-###: <reason>`.
# A line with no ` — ` separator, or with fewer than three fields before it,
# declares NOTHING and is dropped.
#
# THE `OI-###` IS REQUIRED, AND IT IS THE FIRST TOKEN OF THE REASON (OI-41 ARM 1,
# ruled 2026-08-20 — hard at birth). An exception is a DEFERRED DECISION: the
# entry says a rule this repo mechanizes is knowingly not satisfied, so it must
# NAME the open-items row that carries the question. An entry with no id, or one
# naming a row `docs/requirements/open-items.toml` does not have, is an INTEGRITY
# finding on the always-on `--strict-integrity` floor — a field, so it has no
# false positives, and the bad state is unrepresentable rather than merely
# detected. The 19 entries this replaced said the same thing in prose ("owes an
# open-item row at the sitting"), nineteen times, and produced zero rows: the
# announcement and the queue were two unconnected artifacts. Position, not
# mention — an id further into the sentence is prose, not the field.
#
# TOKEN-SCOPED, and that is the whole design. An entry silences the ONE token it
# names — spelled as the finding reports it, whitespace-collapsed and
# case-insensitive — never the cell around it. The earlier row/cell key was
# measured wrong on 2026-08-18: 15 entries here read "unsigned derived-requirement
# label", which is one parenthetical, while suppressing every frame in the same
# `Rationale`; re-running detection with the list ignored exposed 67 tokens over
# 22 rows that nobody had adjudicated, `RE-VOICED 2026-08-17` and
# `Minted at the owner's 2026-08-15` among them — the banned shape itself, riding
# a carve-out written for a label. So a frame beside an allowed one still
# reports, and re-wording an allowed cell re-opens the question rather than
# laundering a new frame through an old entry.
#
# THIS FILE IS NOT A SECOND HOME FOR PROVENANCE. Every entry below is one shape:
# the token is the ONLY record that a question is OPEN — a contradiction between
# two rows, an obligation whose carrier is gone, a label nothing mechanical
# signs. Stripping those would delete the repo's only note that the question is
# unresolved, which is worse than the frame. Each row below NAMES the open-item
# row its question sits in; when that ruling's execution lands, the token leaves
# the cell and the entry goes with it (a dead entry fails tests/test_trace.py).
#
# THE HELD POPULATION THIS FILE COULD NOT LIST — EXPIRED 2026-08-23, EXECUTED.
# The hold recorded here from 2026-08-19 (OI-36, option (b)) covered the IF
# `Contract` cells: findings from a DIFFERENT detector
# (`trace.if_contract_advisories`: `Contract names WI-###` and `Contract cites
# decision`), which get no entries below because that detector takes no `allow`
# parameter at all — an entry naming them would declare nothing and silence
# nothing. The hold was bounded by two work items and by nothing else: WI-455's
# interface lane rewrites those cells for other reasons, and its remainder was
# blocked behind WI-469 (`Consumes` rows that name the medium). WI-469 landed,
# so the pass ran as WI-455 slice 4 — the provenance clause deleted from each
# live cell, or the cell re-stated plainly where the citation was carrying the
# contract. The detector now reports ZERO on both arms; it stays warn-only, so
# a new citation re-opens the question by reporting rather than by an entry
# here. Recording the expiry rather than deleting the clause is the point of
# stating the hold on the surface a reader meets: the population was measured
# at 49 when the hold was written and at 48 when it executed (one citation left
# with WI-469's re-authoring), and only a pinned surface can show that.
#
# The eight citation FRAMES that were the other half of OI-36 were swept
# 2026-08-19, so the frame detector reports zero and this file's entries are the
# only exceptions.

# NO ACTIVE ENTRIES. Every question this file held open has been ruled AND
# executed, so each marker left its cell and its entry went with it — the
# intended end state for an entry here, not an accident, and the retirement
# records below are the standing account of what each one covered. An empty list
# is NOT a clean bill of health for the citation-frame rule: it says only that
# no rule is KNOWINGLY unsatisfied today, and the next one that is adds its line
# back, carrying the `OI-###` the grammar above requires. The IF `Contract`
# hold that the header used to except from this list is executed as of
# 2026-08-23 and excepts nothing any more.

# RETIRED ENTRIES, recorded rather than silently dropped (2026-08-18):
#
#   IF-123 / IF-127 / IF-130 Notes — each read "PROVISIONAL, standing for the
#   review sitting to overturn; the marker is the only signal the seam is
#   unratified". That marker is not a citation token and the detector never
#   reported it, so the entries were suppressing OTHER frames — `OI-30`,
#   `RESOLVED 2026-08-17`, `RE-AFFIRMED 2026-08-17`, two ruling dates — that no
#   reviewed reason had ever named. They surface now, for the interface lane to
#   sweep or for the sitting to adjudicate on their own merits.
#
# RETIRED BY THEIR RULINGS (2026-08-19) — the intended end for an entry here: the
# question closed, so the marker and the entry went together.
#
#   SR-040 Rationale x2 (`THE TRIPWIRE'S CARRIER IS RETIRED`, `investigated
#   2026-08-18`) and SR-040's `added 2026-08-16` derived-requirement label —
#   OI-38 ruled (b) STRIKE. The resume-surface size tripwire is gone from the
#   shall and the acceptance; its two hat lenses are answered in the rationale
#   rather than deleted with it (their premise, a session inheriting a growing
#   resume surface, does not survive the one-engine coordinator). The label went
#   with the paragraph that carried it — nothing is left unsigned because nothing
#   is left derived.
#
#   LLR-037 Detail / TC-040 Method (`corrected 2026-08-18` / `CORRECTED
#   2026-08-18`) — OI-40 ruled (b) CORRECT SHUT, tracking OI-38. Both cells now
#   state only what the module does and what the tests perform; the three
#   function-granular evidence tests pass, and TC-040's `expected` no longer
#   carries the "unverified BY CONSTRUCTION" clause because the clause it
#   referred to no longer exists.
#
#   IF-117 Notes (`REVERTED TO THE SR 2026-08-17`) — swept under OI-36's ruling
#   on the 8 frames. The candidate-gap the token marked is now OI-39's subject
#   (no SR states the CodeSymbol discharge obligation), so the note states the
#   gap as a standing property of the seam instead of as an edit history.
#
# RETIRED BY THEIR RULINGS (2026-08-20) — the same intended end, and the act that
# emptied the list.
#
#   SR-043 Rationale (`resolved here (2026-08-16`) — OI-37 ruled RELAX SN-006:
#   the contradiction was between the row's fail-open arm and an absolute the
#   need had reached for, so the need's text gave way and the row's behavior did
#   not. With nothing left unresolved, the OPEN QUESTION paragraph the token
#   framed is replaced by the standing reason the fail-open arm is deliberate.
#   Two questions the same ruling declined to settle are NOT covered by this
#   retirement and owe rows of their own: an unparseable policy file reading as
#   gate-OFF in one of three twins, and a fail-open record nothing counts.
#   (The licence attribution — stop-subagent-fanout, MIT — is a legal notice,
#   carries no citation token, and stays regardless.)
#
#   SR-024 / 033 / 043 / 052 / 053 / 054 / 111 / 112 / 129 / 144 / 146 / 147 /
#   149 / 167 / 175 / 176 / 177 Rationale, the derived-requirement label —
#   OI-34 ruled (c) KILL THE LABEL: an SR carries the HAT(S) it was derived from
#   instead, and the sixteen `added <date>` tokens were the parenthetical
#   `(Derived-requirement label, added <date> — PROVISIONAL, unsigned.)` and
#   nothing else. Each entry existed because deleting that parenthetical while
#   the concept stood would have silently promoted an unsigned claim to a
#   ratified one; the ruling retires the claim itself, so nothing is promoted —
#   there is no longer a signature state to hold. SR-053's signed variant
#   (`charter ruled, row retained`) carried no citation token and so never had an
#   entry, but it went in the same act, because leaving one row labelled would
#   have left the concept alive on a population of one. What each row KEEPS is
#   the `Hat-derived (hat.NAME)` prose already in the same cell, which is the
#   deriving lens the label was pointing at and the carrier until the hat field
#   lands.
