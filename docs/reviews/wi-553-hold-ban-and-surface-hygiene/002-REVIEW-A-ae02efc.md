- [MAJOR] docs/requirements/low-level-requirements.toml:606 -> Approved LLR-058 still requires a queued WI carrying `blockref` to read as blocked, but this diff removes `BlockRef` from the registry schema and deletes the scheduler derivation -> re-point LLR-058 (and its covered test method) to the terminal-partial disposition, or restore the specified behavior -> @owner
- [MAJOR] docs/requirements/low-level-requirements.toml:1430 -> Approved LLR-144 still specifies `schedule`'s queued-plus-`blockref` anti-livelock mechanism after this diff removes it -> revise the LLR to state that the terminal `partial/` move alone is the anti-livelock mechanism -> @owner
- [MAJOR] docs/requirements/low-level-requirements.toml:1981 -> Approved LLR-198 still requires the `blocked_pending` symbol and its blockref-backed owner-action projection, but this diff deletes both -> update the CodeSymbol and Detail to the two remaining pending sources and their actual owner-surface contract -> @owner
- [MAJOR] docs/test/test-cases.toml:1348 -> Approved TC-138 still requires a declared `blockref` to survive a partial close even though the work-item field was removed -> replace the obsolete assertion with the terminal-partial contract that now verifies LLR-144 -> @owner
- [MAJOR] docs/test/test-cases.toml:1864 -> Approved TC-194 still requires a blocked-row pointer projection and all four former facade names, while this diff removes the blocked source and `blocked_pending` -> revise the Method and evidence to test only the remaining spine/pause projections and names -> @owner
VERDICT: CHANGES-REQUESTED findings=5
