### REVIEWER — G3 — Round 1 — 2026-08-01
Verdict: CHANGES-REQUESTED
Findings:
- [MAJOR] project-trajectory/scripts/check_trajectory.py:67 -> The SHIPPED module contract still promises the window this row closed. Lines 67-69 (inside the module docstring, AST-bounded 2-128) read "the **amend-without-flip** warn (WI-316): a staged diff changing content cells of a `Verified` spine row without setting the `Modified` re-attest marker" — "content cells" is the exact phrase §A5 quotes as the defect, and it is now false. Driven with the shipped script on a synthetic git repo, with a liveness control in the same fixture: an LLR-001 whose `Module`/`CodeSymbol`/`TestRefs`/`Component`/`Phase` all move (content cells, the literal WI-280 shape) gives `rc=0`, `stderr=''`; the identical fixture with `Detail` amended gives `WARN - LLR-001: ratified cell(s) Detail amended while Status stays Verified …`. The FUNCTION docstring 2,575 lines below (2643-2652) was rewritten — "scoped by WI-380 to RATIFIED cells only" — so the omission is a miss, not a scope call; and this file is kit-shipped, so the false contract reaches every adopter through `downstream-resync`, in the one place that states what the warn watches without opening the function. A successor who trusts it will believe a moved `Module` pointer still arms the marker: precisely the "missed window is seen by nobody" hazard the row's own residual rule is built to avoid. -> Narrow lines 67-69 to the ratified half of the §A5.1 split (citing the ruling), and say the traced half is carried structurally by `staged_spine_amendments`. -> @owner
- [MINOR] project-trajectory/scripts/check_trajectory.py:2541 -> The WI-388 seam's RECORD is consumable; its SCAN is not callable at the trigger §A5.2 rules, and the docstring over-claims which records are WI-388's — two drives, one temp repo. (a) With an `SN-Refs` re-point STAGED, `staged_spine_amendments` returns `[{'ratified': {}, 'traced': {'SN-Refs': ('SN-001', 'SN-009')}, 'registry': 'docs/requirements/system-requirements.csv', 'id': 'SR-001'}]` and `staged_spine_findings` returns `[]` — the pairing the row claims, and it holds. But `git commit` the identical change and the same call returns `[]`, because the scan is INDEX-vs-HEAD (`git diff --cached`, `git show :path` vs `git show HEAD:path`), while §A5.2 and `docs/log.md`'s Decisions both rule the trigger as "a trunk COMMIT that changes a … cell of a `Verified` row causes the dispatcher to write …". WI-388's dispatcher runs after the commit lands and will get nothing; it needs a rev-range variant, and the reusable pieces are not lifted out for it — `_split_changed_cells` is private and the `Verified`/`Verified` filter, the `-000` skip and the owning-SR suppression are all welded inside the index scan. (b) The docstring's "A record may carry a traced change with NO ratified change — that is the WI-388 case" is wider than the ruling: a `Module`-only edit yields `{'ratified': {}, 'traced': {'Module': ('src/d.py', 'src/moved/d.py')}, …}`, and §A5.1 routes only `SN-Refs`/`Verifies` to adjudication — the rest are simply silent, not pending. -> State in the docstring that the scan is index-vs-HEAD (so WI-388 plans a rev-range parameter instead of discovering it), and qualify "the WI-388 case" to the `SN-Refs`/`Verifies` subset of `traced`. -> @owner
- [MINOR] project-trajectory/scripts/check_trajectory.py:2493 -> The deviation is CLASSIFIED right but HANDED OFF nowhere the successor reads. Keeping LLR `SR-Refs` and SR `SupersededBy` ratified is the defensible call: §A5.1's table does not name them, and narrowing an unruled cell is exactly the missed-window failure this row exists to remove, so "today's behaviour, deliberately NOT narrowed" is correct even though §A5's principle ("traceability … is traced") arguably reaches LLR `SR-Refs`; and it is stated where a maintainer of this code meets it (the comment at 2489-2493, the archived spec :20, the log fragment :73-79). What does not hold is the sentence that disposes of it — "it is WI-388's to put, not this row's". Driven: `grep -n "SR-Refs\|SupersededBy" docs/work/queued/WI-388-adjudication-mechanical-scope-judgement.md` returns 0 hits (its TOML `title` is the full scope statement and carries neither column), and in `docs/concurrency-v2.md` `SupersededBy` returns 0 hits while the single `SR-Refs` hit is :704, about `trace.py` testing it only for existence — unrelated. WI-388's builder works from that spec plus §A5.1/§A5.2, so the question is asserted as delegated while living only in this row's own code comment and log fragment, and dies with them. -> Add one sentence to WI-388's spec (or to §A5.1) naming LLR `SR-Refs` and SR `SupersededBy` as the two columns WI-380 left ratified pending a per-cell ruling. -> @owner
- Re-measured, all held: the §A5.1 table is implemented EXACTLY — all 28 ruled cells driven through `spine_cell_class`, 0 mismatches; every column of the three live registries AND the three shipped blank templates classifies (`Lifecycle` is declared but ships in neither header, harmless). Fail-safe BROKEN both ways: appending `Owner` to the live `system-requirements.csv` reds `test_spine_cell_split_classifies_every_shipped_column` ("column(s) ['Owner'] are classified by NEITHER half"), and so does appending it to `project-trajectory/registries/test-cases.template.csv` — so the residual's silent hazard really is closed at the ruling. Both mutations re-driven at HEAD: `spine_cell_class` -> `return "ratified"` gives exactly `2 failed, 7 passed` (`…traced_cells_do_not_arm…`, `…expose_the_traced_half…`); the allowlist-only residual reds `…unknown_column_falls_to_ratified` on `stderr=''` — the missed window, as recorded. Ratchets: base `check_trajectory.py` 3098 lines -> HEAD 3191 (+93, exact); base `staged_spine_findings` C901 = 20, HEAD `staged_spine_amendments` = 20 with `staged_spine_findings` gone from the census, and the counterfactual is true — re-inlining `_split_changed_cells` measures exactly 23. Nothing was greened by re-stamping. Bars at HEAD `8c02349`: full `2 failed, 1712 passed, 8 skipped in 508.43s` (the recorded 1712/8/2 match; wall differs only because a sibling worktree's suite ran concurrently), smoke `1 failed, 556 passed in 16.70s` (recorded 552+4 skipped — same 557, the 4 are env-gated skips), `ruff check .` *All checks passed!* + `ruff format --check .` *146 files already formatted*, `check_trajectory --root . --strict` *clean (388 work item(s), 363 done (94%), 16 retired, graph acyclic)* exit 0 with the 8 pre-existing WARNs, `check_docs … --stale` FAIL on 2 broken links / 0 orphans / 107 allow-listed. Both full-suite reds are independently pre-existing: `git archive 54312cfa` run under the test's exact argv reproduces the same two `docs/concurrency-v2.md:10` / `:979 -> work/deferred/` FAILs at exit 1 with 106 allow-listed orphans (so the fragment's "106 -> 107" is right), and `check._work_branch(<worktree>)` returns `'wi-380'` by construction. `gen_arch_map --check --strict-parse --src project-trajectory/scripts` says *code map up to date*; the three budgeted files are untouched; no spine registry, no dial, no adjudication kind, no scope creep. -> no remedy owed -> @owner
VERDICT: CHANGES-REQUESTED findings=3

### REVIEWER — G3 — Round 2 — 2026-08-01
Verdict: APPROVE
Findings:
- All three round-1 findings are FIXED and each was re-driven at `dcddef0a`; one new MINOR below, and it is the only thing standing between this and a clean approve. (1) MAJOR/module contract: `check_trajectory.py:67-74` now reads "a staged diff changing the **ratified** cells", cites the §A5.1 ruling by date, names `Module`/`CodeSymbol`/`TestRefs`/`SN-Refs`/`Verifies` as not arming the marker, and points at `staged_spine_amendments` as the traced half's home — every one of those five re-verified as `traced` through `spine_cell_class`, and the sentence no longer contradicts the shipped behaviour I drove in round 1. (2) MINOR/seam: the rev pair works and the index default is genuinely untouched. Loading the round-1 module and the round-2 module side by side over FIVE staged scenarios (ratified amend, traced-only, both halves, amend+`Modified` flip, nothing staged), `staged_spine_amendments(root)` and `staged_spine_findings(root)` are IDENTICAL in all five, and the production call site is still the bare `staged_spine_findings(root)` at :3070. On a real commit: default post-commit `[]`, `(root, "HEAD~1", "HEAD")` returns `('SR-001', ratified=[], traced=['SN-Refs'])`, the next commit returns `ratified=['Requirement']`, `HEAD~2..HEAD` and the raw-SHA pair both return both halves, and a bogus rev degrades silently to `[]` — the round-1 module raises `TypeError` on the same call, which is the gap that closed. The one behaviour-relevant edge I checked because the round-2 change added an argument (`diff --cached --name-only HEAD`, which errors on an unborn HEAD where the bare form does not): old and new both return `[]` there, so the degrade is unchanged. The docstring over-claim is narrowed correctly — a `Module`-only edit still yields a record (driven: `traced={'Module': ('src/d.py','src/moved/d.py')}`) and the text now says that is silent, not a WI-388 case. (3) MINOR/hand-off: WI-388's spec carries the intake, `3 × SR-Refs` + `2 × SupersededBy` as claimed, and the edit is APPEND-ONLY (common prefix through char 3758 of the title, +1500 chars) with `needs`/`specref`/`safety_class`/`buildtier`/`workstream` byte-identical. It is intake, not scope creep: the row is `queued`, the append asks for a ruling rather than making one ("`SupersededBy` is likely to stay ratified … but it should be ruled"), and it matches the trunk precedent at `54312cfa`, which appended re-validation text to eight queued rows' titles. It still parses and still schedules: `parse_spec_frontmatter` yields 7 keys with `needs = ["WI-380", "WI-381"]`, `check_trajectory --root . --strict` is clean, and `schedule.py ready` / `simulate` are byte-identical to `b2093871` (frontier `WI-384 WI-386 WI-383 WI-378`; rounds 1-5 with WI-388 still in round 4). -> no remedy owed -> @owner
- [MINOR] docs/log.d/WI-380-ratified-vs-traced-cell-split.md:127 -> The mutation ledger went stale in the same commit that added the mutation it now under-counts — the WI-372 defect class, repeated. The entry reads "reverting `spine_cell_class` to the pre-WI-380 … reds the traced-cell test and the seam test (`2 failed, 7 passed`)". Re-driven at `dcddef0a`: that revert now gives **`3 failed, 7 passed`**, and the third red is the round-2 test itself — `test_staged_spine_amendments_read_a_commit_range_not_only_the_index` also asserts the traced half (`ranged[0]["traced"] == {"SN-Refs": …}`), so adding it widened the mutation's blast radius and neither the figure nor the two-test enumeration survives. The pass count coincidentally stayed 7 (the selection went 9 -> 10), which is exactly why it reads plausible. Same defect in the archived spec at docs/work/archive/WI-380-ratified-vs-traced-cell-split.md:20, whose "MUTATION-PROVEN, not asserted:" paragraph still closes "Both mutations were run and both reds observed" while the fragment two paragraphs above correctly says three. The substance is unharmed — the revert reds MORE than recorded, never fewer, and I re-drove all three mutations at this tip: M1 `3 failed, 7 passed`, M2 reds `…unknown_column_falls_to_ratified` on `stderr=''`, M3 (index-only `_spine_revs`) reds the commit-range test on exactly the recorded `assert [] == [('docs/requirements/system-requirements.csv', 'SR-001')]` with `1 failed, 9 passed`, which also proves the rev plumbing left the other nine green. Only the record is wrong, in the surface compiled into `log.md` as the permanent account of what shipped. -> Re-stamp to `3 failed, 7 passed` naming the third test, and change the archived spec's "Both mutations" to three. -> @owner
- Re-measured at `dcddef0a`, all held, nothing re-stamped to green: module size `check_trajectory.py` **3191 -> 3230 (+39)** exact, and 3230-3098 = the "+132 total" the Deliverable claims. Complexity is NOT a re-stamp and can be proved so twice over — `tests/test_complexity_ratchet.py` is ABSENT from the round-2 diff (the baseline row `("check_trajectory.py", "staged_spine_amendments"): 20` is untouched), and a live isolated ruff C901 census measures `staged_spine_amendments` at exactly **20** with `_spine_revs` under the limit and absent, so the extraction really did absorb the rev-pair branch. Test count 5 -> **6** (file 32 -> 33 defs; the six WI-380 tests enumerated at :406/:443/:460/:479/:520/:540). Bars: full `pytest -q -n auto` -> **2 failed, 1713 passed, 8 skipped in 481.33s** (the recorded 1713/8/2 match exactly; wall differs because two sibling worktrees were running their own suites concurrently), smoke `1 failed, 556 passed in 21.70s` (the fragment's un-refreshed `552 passed, 4 skipped` is the same 557 — env-gated skips, and the new test is in a `SLOW_MODULES` module so smoke could not move), `ruff check .` *All checks passed!* + `ruff format --check .` *146 files already formatted*, `check_trajectory --root . --strict` *clean (388 work item(s), 363 done (94%), 16 retired, graph acyclic)* exit 0, `check_docs … --stale` 2 broken links / 0 orphans / **108** allow-listed (so "106 -> 107 -> 108" is right end to end), `gen_arch_map --check --strict-parse` *code map up to date* and the round-2 arch-map diff is `1 1` = the one line claimed. Both reds remain pre-existing and the branch-side one is the cause named: the branch is NOT an ancestor-descendant of `22e66e51` (`merge-base --is-ancestor` false; that commit is the `ConcurrencyTrainRewrite` tip), the branch reproduces only the same two `docs/concurrency-v2.md:10`/`:979 -> work/deferred/` FAILs and nothing new, and a `git archive` of `22e66e51` run under the test's exact argv exits **0** — so the red clears at merge, as recorded. Budgeted files untouched; the §A5.1 tables, `spine_cell_class` and `_split_changed_cells` are byte-identical to `b2093871` (2,420 chars), so round 1's 28-cell drive still stands unretested here. -> no remedy owed -> @owner
- Deliberately NOT re-examined this round (a bounded round vouches only for what it was pointed at): I did not re-drive the 28-cell §A5.1 table, the two fail-safe registry/template breaks, or the `_split_changed_cells` counterfactual — round 1 drove all three and the code they cover is byte-identical since; I did not re-audit the parts of `check_trajectory.py` outside the spine-amendment block, the other 32 kit scripts, or any test module other than `tests/test_trajectory_staged.py`; and I did not evaluate the merge itself (trunk has moved four commits ahead, and whether `22e66e51` plus two live sibling claims integrate cleanly with this branch is the integrator's bar, not this round's). -> no remedy owed -> @owner
VERDICT: APPROVE findings=1
