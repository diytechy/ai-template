# 052-CRITIQUE — SR-052/SR-053/SR-054 rubric adjudication of PROJECT_STATE.html

Independent critique session (fresh context, provider-heterogeneous, no implementer
notes read). Artifact produced from the recipe: `python
project-trajectory/scripts/gen_trajectory.py` → `PROJECT_STATE.html` (698 KB, state
as of commit e781a72 · 2026-07-14). **Method disclosure:** this critic could not
visually render the page; it judged the emitted markup/CSS/JS directly — WCAG
contrast ratios computed numerically from the actual fill/text hexes, label
overflow computed from rect widths vs measured character counts at the declared
font sizes, and every interaction handler read from the emitted scripts. Anchors
that survive that inspection are treated as satisfied; the findings below are the
ones that fail on the artifact's own numbers.

What passes: A1 (all 708 interactive SVG nodes carry `tabindex="0"` with
focus-reveals-detail or Enter/Space-descend handlers; tabs and breadcrumbs are
native buttons), A2 (every focusable node has a `<title>`; descend blocks add
`role="button"` + `aria-label`), A3 (status carries ✓/●/○ glyphs + words, tiers
carry ID prefixes + lane headers, phases carry text labels), T1 (project state and
next-WI `● WI-144` are on the landing hero at 0 switches; module map is 1 labelled
tab away), T3 (drill breadcrumbs + inline detail asides keep context). The
findings:

- [MAJOR] A4 -> detail-panel status badge: `.detail .badge` hard-codes `color:#fff` while `statusColor.queued = #94a3b8` — clicking any of the 26 queued work items in the When tab renders the word "queued" white-on-slate at **2.56:1**, far below the declared 4.5:1 floor (badge text is .68rem ≈ 10.9px, not large) -> give the queued badge a dark ink (e.g. the same `#0f172a` the queued SVG nodes already use for their text) or darken the queued badge background; every other measured pair passes (worst survivor: white on #b45309 at 5.02:1) -> @owner
- [MAJOR] T4 -> drill block main labels (`.blab`, 10px bold) are emitted **untruncated** while sub-labels are truncated with `…`: `docs/requirements/system-requirements.csv` (41 chars ≈ 220px) is centered in a 172px-wide block in the How-SW descend layers, overrunning the block edge, its ports, and the incoming-wire arrowheads by ~25px per side; same for 38-char campaign names in the When descend layers (`working-surface-restructure-2026-07-11`) and marginally for `CMP-004 — Unattended loop & floor` at the How-SW **top view** (default render) -> apply the same width-fitting truncation-with-`…` (the full text already lives in the block's `<title>`) to `.blab` that `.bsub` already gets -> @owner
- [MAJOR] U2 -> one palette color carries two or three different meanings depending on view: `#047857` = TC tier (What/Knowledge) = `done` status (When legend) = phase `v3` accent (When phase blocks); `#b45309` = `active` status = phase `v2+v3` = Process Guide type (Knowledge); `#0e7490` = SR tier = `unphased` phase = module (How-SW); `#7c3aed` = phase `v2` = Interface (Knowledge) = file hub (How-SW). Worst case is **within the When tab itself**: green/amber phase-accent blocks render directly above a status legend declaring green=done, amber=active — a reader who has learned the status vocabulary misreads the phase blocks -> separate the phase-accent and type palettes from the status palette (distinct hues for phase accents at minimum), keeping the status trio reserved; per the accumulation rule this names a new failure mode — propose **U5: one concept per color — a palette color maps to at most one concept across the status/phase/type vocabularies that render in the same document** -> @owner
- [MAJOR] T2 -> the Knowledge (OKF) view opens as a flat wall of **249 nodes + ~500 edges in one 812×3338px SVG** inside a 660px scroll box — no start-collapsed grouping at all, while the When and How-SW views correctly apply the `>3` drill rule (5 phases / 5 components at top). A first-time reviewer sees ~20% of an undifferentiated graph and scrolls blind -> give the Knowledge view the same tiered treatment (collapse by OKF `type` — its 6 types are natural containers — or by spine section) so it opens at a legible density -> @owner
- [MINOR] U4 -> container-block click behavior diverges between the two drill views: in How-SW every `.block[data-node]` (component/module/file/external) opens the detail panel on click/focus, but in the When drill only leaf `.block[data-wi]` work items do — clicking or focusing a phase/workstream/campaign container in When silently does nothing (its only affordance is double-click-to-descend) -> bind the same click/focus→detail to When container blocks (the drill already has titles/counts to show) so the same structure carries the same idiom -> @owner
- [MINOR] U3 -> two different legend idioms coexist, both in the same When tab: the phase-accent legend is a `p.tierlegend` (.82rem text, .55rem square swatches, inline flow) while the status legend below it is a `div.legend` (.85rem text, .8rem swatches, flex layout with different gaps); the What/Knowledge/How-SW tabs use only the second -> converge on the one `div.legend` idiom for the phase-accent row -> @owner
- [MINOR] U1 -> the HTML type scale is ad-hoc at the small end: near-duplicate sizes serve the same role across sections — caption/legend text at .82rem (`tierlegend`), .83rem (`.detail .meta`), and .85rem (`.legend`, `.asof`, `.top-sub`); micro-labels at .68rem (`.detail .badge`), .7rem (`.pflow .g`), .72rem (`.entry span`, loop caption), .75rem (`.tile span`, `.pflow .n`) — 21 distinct font-size declarations overall, against the SVG side which correctly shares `--nlabel`/`--nsub` -> collapse the near-duplicates to one small declared scale (e.g. .7 / .75 / .82 / .85 stays; .68/.72/.83 fold into neighbors) -> @owner
- [MINOR] A4 -> the focus/highlight indicator is the sole focus cue (native outlines are suppressed via `outline:none` on `.cell/.wi/.drill .block:focus`) and its amber `#f59e0b` 2.5px stroke measures **2.05:1** against the white surface and ~2.5:1 against the green/violet node fills — below the declared 3:1 graphical/UI-boundary threshold -> use a darker focus ring (e.g. `#b45309` ≈ 3.2:1 on white, or a two-tone ring) or restore a high-contrast outline -> @owner
- [TC-HARDEN] Add a generated-artifact contrast test: extract every emitted text-fill/background pair (SVG node text × rect fill; each `statusColor`/`tierColor`/`d.fill` value × the badge's ink) and assert ≥ 4.5:1 (≥ 3:1 for the focus-ring stroke vs surface and vs each node fill) — the queued-badge failure was reachable by pure computation and should never wait for a critique session. (routes via change-intake, process.md §5)
- [TC-HARDEN] Add a label-fit test on the drill emitters: for every emitted `.blab`/`.bsub` tspan, estimated advance (chars × 0.62 × font-size, bold) must be ≤ rect width − 2×padding, else the emitter must have truncated with `…`; assert zero untruncated overflows in a bootstrap-scaffold render. (routes via change-intake, process.md §5)
- [TC-HARDEN] Add a palette-bijection test: collect the (concept → hex) map from all emitted legends + the `tierColor`/`statusColor`/phase-accent dicts and assert no hex maps to more than one concept name within the document. (routes via change-intake, process.md §5)

VERDICT: CHANGES-REQUESTED findings=8
