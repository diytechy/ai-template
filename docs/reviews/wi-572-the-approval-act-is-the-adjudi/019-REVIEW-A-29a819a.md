- [MAJOR] project-trajectory/scripts/integrate.py:1177 -> the all-`adjudication` exemption returns before reading `Adjudicates` or comparing it with the branch's `staged_approval_acts`, so an adjudication minted over one Drafted row can flip any other row and copy that registry into `last_approved`; the prompt labels the extra row out of scope, but the actual merge admits it (`adjudication_lane= True`, `approval_refusal= None` on a flipping adjudication fixture with no scope) -> at this merge trust boundary, derive the delta's approval acts once and refuse an empty, out-of-scope, or registry-widening act; cover a permitted scoped flip and a rejected unrelated flip/snapshot -> antidote: this is validation at the genuine Git merge boundary, because arbitrary file edits cannot be made unrepresentable by a stricter in-memory type -> @owner
VERDICT: CHANGES-REQUESTED findings=1
