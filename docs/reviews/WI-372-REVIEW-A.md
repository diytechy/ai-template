### REVIEWER — G3 — Round 1 — 2026-07-30
Verdict: CHANGES-REQUESTED
Findings:
- [MAJOR] tests/test_gen_trajectory.py:3576 -> The contract states "It is simply ABSENT in a checkout with no committed dashboard, so a caller that reads it must still be meaningful fresh-only", and 3593 frames all fourteen keepers as compatibility pins whose invariants merely "hold in the older markup TODAY" — but T6 is not a pin, it is LOAD-BEARING on the shipped document. Driven: with the module's `ROOT` pointed at a dashboard-less directory the helper returns the seven fresh labels and no `shipped` (absence itself is clean), 13 of the 14 keepers pass, and `test_t6_theme_lock_has_one_mechanism_and_no_mixed_family_pair` FAILS `vacuous — only 33 node pair(s) classified` against its own `assert nodes >= 50` (line 2764); with the shipped copy present the same test passes (full suite re-run: 1 failed / 1691 passed / 7 skipped, sole failure the standing work-branch red). So a successor who follows this docstring's own prescribed remedy — "filter at the call site", `lb != "shipped"` — reds T6 immediately, on a floor that has nothing to do with the emitter they changed: the one caller the contract most needs to warn is the one it silently mis-describes. -> Name the exception in the same paragraph: T6 currently clears `nodes >= 50` only WITH the shipped document in the sweep (33 without), so excluding it there needs a replacement fixture (the `WRAPAROUND_WIS` move) before the filter, not after. -> @owner
- [MINOR] tests/test_gen_trajectory.py:3597 -> The triage rule "**When one of them reds, read the failing LABEL first:** `shipped` means regenerate the dashboard on trunk … any other label means the emitter really regressed" does not hold for T6, and T6 is exactly where a stale-shipped disagreement lands. Driven on the REAL test with a doctored copy of the committed dashboard (its `#dag .wi text` fill moved `#fff` -> `var(--text)`, i.e. precisely "an older renderer wrote this markup"): the failure is `AssertionError: ('#dag .wi text', {'varying', 'invariant'})` — no label anywhere. T6's post-loop assertions (2758-2767) judge `text_fills`, a dict accumulated ACROSS the sweep by `_collect_css_text_fills`, whose own docstring cites `#ice .cell` "in the shipped artifact" as a shape only the shipped copy contributes; the label is discarded before the assert. The other thirteen keepers do format `label` into every per-document message (checked at each of the fourteen call sites). -> Add the qualifier: the label triage applies to the per-document assertions; T6's cross-document accumulators assert by SELECTOR, so a shipped-vs-fresh disagreement surfaces there as `(selector, {families})` with no label. -> @owner
- [MINOR] 059e7e5 (close commit message) -> The close records a mechanism that is false in both halves: "The emptied claim directory is left on disk deliberately - WI-357's recorded workaround, since check.py's lane detection keys on docs/work/active/<branch>/ EXISTING, and removing it would arm the trunk-only freshness gates on the very commit that closes the WI." Driven: `ls docs/work/active/` -> "No such file or directory" (the directory is not on disk at all), and `check._work_branch('C:\\Projects\\ai-template')` still returns `'wi-372-emitter-truth-times'` — through WI-357's git-history fallback (check.py:1095-1116), which exists precisely because "§2.3 step 3 closes a WI by MOVING the claim out of active/". `check._work_branch_skip('trajectory-map', root)` returns `('SKIP', "work branch 'wi-372-emitter-truth-times' — generated freshness is the trunk lane's, concurrency-restructure §5.2", …)`, so the gates are correctly relaxed regardless; only the recorded reason is wrong, in the same session that documents this very mechanism to successors. -> No rewrite: add one line to `docs/log.d/WI-372-emitter-truth-times.md` recording that lane detection survives the claim move via the WI-357 history fallback, so the compiled log carries the true mechanism. -> @owner
- [MINOR] docs/work/archive/WI-372-emitter-document-staleness-contract.md:45 -> The re-open path the owner's ruling preserved is a live link to the wrong file: "its evidence remains in `[WI-367-REVIEW-A](../../reviews/WI-367-REVIEW-A.md)` if it ever re-opens" (repeated at docs/log.d/WI-372-emitter-truth-times.md:44). That file's three findings are the port-fan 7.90 px figure, the sw-0 / layer-count record figures, and the `_path_xs` Q gap; `git show <rev>:docs/reviews/WI-367-REVIEW-A.md | grep -c "_every_emitter_document\|truth-time"` returns 0 for EVERY version of it (one commit, c8750e7), and no other doc outside WI-372's own files records the finding. The evidence in fact survives only in this WI's intake title (line 3, which carries the builder's finding verbatim) and in the new docstring — so a successor re-opening the shared-fixture option follows the pointer to a file that never held it. -> Point the re-open path at this archived spec's own title (or copy the two-sentence finding into WI-367-REVIEW-A) so the citation resolves to the evidence. -> @owner
VERDICT: CHANGES-REQUESTED findings=4

### REVIEWER — G3 — Round 2 — 2026-07-30
Verdict: APPROVE
Findings:
- none against the four round-1 findings — all four re-driven at `8cc4058` and all four hold, none carried. (1) MAJOR: re-ran the dashboard-less-`ROOT` sweep — helper still yields the seven fresh labels and no `shipped`, 13 keepers pass, `test_t6_theme_lock_has_one_mechanism_and_no_mixed_family_pair` still fails `vacuous — only 33 node pair(s) classified`, so the docstring's "33 node pairs / floor `nodes >= 50`" describes what the test actually does; placement is right — the qualifier sits in the `shipped` bullet itself ("with one MEASURED exception, T6, called out below") and the exception has its own bolded paragraph stating the forced order (replacement fixture first, filter second), so a caller meets it at the claim, not in a footnote. (2) label triage: re-doctored the committed dashboard (`#dag .wi text` fill `#fff` -> `var(--text)`) and the real test still fails `('#dag .wi text', {'invariant', 'varying'})` — selector, no label, exactly as now documented; and the new "the PER-DOCUMENT assertions … all format `label` into their message" is not an over-claim — derived over the AST of all 15 call sites, every in-loop assertion in all 14 keepers carries the loop label (0 missing), and of the unlabelled post-loop assertions only T6's can be red by the shipped copy (T8's `not through` prints label-bearing tuples; the rest are coverage floors a stale shipped can only mask, and all pass without it). (3) lane mechanism: the account is TRUE, not merely different — `docs/work/active/` is absent from disk, `check._work_branch(ROOT)` still answers `wi-372-emitter-truth-times`, `_work_branch_skip` still returns SKIP for `trajectory-map`/`status-map` and None for `trajectory`, `git log -- docs/work/active/wi-372-emitter-truth-times` shows the add at `ba72429` still reachable, check.py:1095-1116 is that fallback and `project-trajectory/` is untouched by this branch; even the parenthetical holds (temp-repo probe: `git rm` of the last file prunes both emptied directories, and so does a checkout across it). (4) re-open pointer: the intake title still carries the builder's finding verbatim (grep count 1), `docs/reviews/WI-367-REVIEW-A.md` has exactly one commit (`c8750e7`) with 0 mentions of `_every_emitter_document`/truth-time, so "re-open from here, not from the review file" now resolves to the evidence. Bars re-measured at the new tip: full `1 failed, 1691 passed, 7 skipped in 649.38s`, smoke `1 failed, 555 passed in 11.70s` (sole failure the standing WI-357 work-branch conditional in both), `check_docs` OK 325 doc(s) / 930 link(s) / 0 broken, `check_trajectory --strict` clean (371 items, 355 done), and the AST-with-docstrings-stripped comparison is byte-identical `ba72429` -> HEAD. -> no remedy owed -> @owner
- [MINOR] docs/log.d/WI-372-emitter-truth-times.md:97 -> The byte-delta record went stale in the same commit that refreshed the bar totals beside it: it still reads "`tests/test_gen_trajectory.py` (+36 lines, all docstring)", but the round-1 fixes grew the docstring further — `git diff --numstat ba72429..HEAD` is `58  0  tests/test_gen_trajectory.py` and the file went 5287 -> 5345 lines. "All docstring" and "the ratchet does not move" both still hold (AST with docstrings stripped is byte-identical base -> tip, 546213 chars each; `tests/test_module_size_ratchet.py` censuses `conftest.SCRIPTS` only), so only the number is wrong — in the surface that gets compiled into `log.md` as the permanent record of what shipped. -> Re-stamp the figure to +58 lines when the fragment is next touched. -> @owner
- [MINOR] docs/log.d/WI-372-emitter-truth-times.md:113 -> The new "measurement lesson" mis-diagnoses the measurement it is drawn from. It records that the earlier 2-failure run's second red, `test_check_docs.py::test_meta_repo_has_zero_unexplained_orphans`, was "an artifact of the run itself: the review file existed as an unlinked orphan during the window in which this fragment had not yet grown its link to it" — but `docs/orphans-allow` allow-lists `docs/reviews/*` precisely so a review file never has to be linked ("A matched orphan is EXPECTED — … never a finding (even under --strict-orphans)"). Driven on the shipped checker over a synthetic tree: an unlinked, allow-listed `docs/reviews/*.md` gives `check_docs … --strict-orphans` exit **0** with `note - 1 expected live-orphan(s) matched docs/orphans-allow`; the SAME file carrying round 1's relative link gives exit **1** with `FAIL - …:3 broken link -> ../../reviews/WI-367-REVIEW-A.md (target not found)` — which is what fails that test's `assert proc.returncode == 0`, and is the failure the round-1 reviewer was actually asked to repair. The general rule the entry draws ("a live working tree is part of the measurement") survives intact — the tree really was being edited mid-run — but the stated cause does not, and the fragment's own link to the review file is recorded as the fix when it was never load-bearing. -> Re-state the cause as the review file's broken relative link (since repaired), keeping the lesson; note that `docs/reviews/*` is an expected-orphan class. -> @owner
VERDICT: APPROVE findings=2

### REVIEWER — G3 — Round 3 — 2026-07-30
Verdict: APPROVE
Findings:
- none — the three scoped edits at `67fa945` all verify, nothing else reviewed. (1) Byte delta: `git diff --numstat ba72429..HEAD -- tests/test_gen_trajectory.py` is `59  0` and the file is 5287 -> 5346 lines, exactly the "+59 lines … 5,287 → 5,346" now recorded; the arithmetic behind the sentence reconciles too — the build commit alone is `git diff --numstat 2a9357a~1..2a9357a` = `36  0` (the stale +36), the round-1 fixes carried it to +58, and this round's wording edit is `3  2` = net +1. "All docstring" still holds at this tip: the AST with every docstring stripped is byte-identical `ba72429` -> HEAD (546213 chars each), so the branch still carries zero behaviour change. (2) Measurement lesson: every load-bearing clause is true. `docs/orphans-allow:26` still carries `docs/reviews/*`, and re-driving the shipped checker over a synthetic tree at this tip reproduces both halves — an unlinked, allow-listed `docs/reviews/*.md` gives `check_docs --strict-orphans` exit 0 with `note - 1 expected live-orphan(s) matched docs/orphans-allow`, while the same file carrying the round-1 relative link gives exit 1 with `FAIL - …:3 broken link -> ../../reviews/WI-367-REVIEW-A.md (target not found)`; the path asymmetry is as stated (`posixpath.normpath` on `../../reviews/WI-367-REVIEW-A.md` gives `docs/reviews/WI-367-REVIEW-A.md` from `docs/work/archive`, but `reviews/WI-367-REVIEW-A.md` — no such target — from `docs/reviews`); the test it names is the right one, since `test_meta_repo_has_zero_unexplained_orphans` asserts `proc.returncode == 0` (tests/test_check_docs.py:1069) and so reds on the broken link but never on allow-listed orphan-hood; and "passes in ~1.1 s" re-measures at `1 passed in 1.10s` (1.05 s call). The entry now names the cause that can actually produce the failure and keeps the lesson that survived it. (3) T6 ordering sentence: "the same order the worked example above uses" matches the code — `test_svg_viewbox_contains_every_routed_wire` builds its replacement fixture at :4946-4950 (`load_script`, `wrap.mkdir`, `make_repo(wrap, WRAPAROUND_WIS)`, `assert gen(wrap).returncode == 0`) and only then filters at :4951 (`… if lb != "shipped"`), so fixture-first/filter-second is exactly what it does, and the remaining contrast ("it already HAD a fixture to hand and T6 does not") is a judgement the code supports — `WRAPAROUND_WIS` is a declared module constant, while T6 has no candidate for the 17 node pairs it would have to replace. `check_docs --stale` re-run with this block appended: OK — 325 doc(s), 930 intra-repo link(s), 0 broken (this block adds no markdown link, so the count is unchanged). -> no remedy owed -> @owner
VERDICT: APPROVE findings=0
