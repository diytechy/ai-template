- [MAJOR] project-trajectory/scripts/agent-resume.template.sh:51 -> The prescribed opt-in migration says to “blank these slots”, but the following `AGENT_JOBS="${AGENT_JOBS:-2}"` immediately turns a blank back into `2` (and the Windows twin similarly defaults a blank through `if not defined`). `resolve_coordinator_dials` then gives that exported env value precedence over `[agent-loop] jobs`; against this repo's real `jobs = 1`, the driven resolver returned `2`, silently defeating the declared single home for downstream adopters -> make the launcher/template migration mechanically leave `AGENT_JOBS` absent when the config home is selected (and state the exact Windows/POSIX edit), then add a scaffolded-launcher regression proving `[agent-loop] jobs` wins after the documented migration -> @owner
VERDICT: CHANGES-REQUESTED findings=1
