- [MAJOR] scripts/dev-setup.ps1:68 -> `$venvExists` tests only `.venv\Scripts\python.exe`, so an existing broken `.venv` (the spec's live base-interpreter-uninstalled case) is neither reported nor offered the consented recreate; an isolated Git scratch run with an empty `.venv` accepted the ordinary create prompt, then exited at line 228 trying to invoke the nonexistent interpreter and left the venv in place -> detect the `.venv` directory independently of its interpreter, route broken and sub-floor venvs through the consented-recreate branch, and add a behavioral PowerShell regression for the broken-venv case -> @owner
- [MAJOR] project-trajectory/scripts/agent-resume.template.sh:51 -> the downstream launcher template still declares `AGENT_JOBS=2` while the new `stack.ini.template` only comments out `[agent-loop]`; the three shipped launcher templates therefore retain the old dial home and the required template/downstream disposition and migration record are absent -> migrate all shipped launcher templates to the declared config (including an actual default `[agent-loop]` section) or explicitly defer all template changes with the required recorded rationale and migration guidance -> @owner
VERDICT: CHANGES-REQUESTED findings=2
