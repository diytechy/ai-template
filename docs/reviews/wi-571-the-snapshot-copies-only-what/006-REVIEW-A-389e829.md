### REVIEW-A — WI-571 — Round 006 — 2026-09-01

Independent review of `contract_split...HEAD`. Worst failure classes this change
admits, hunted first: (1) silent wrong content — a spine-only approval still
dragging live off-spine drift into the record (the very defect WI-571 exists to
kill); (2) fail-open — an unauthorised copy sailing past the refusal; (3) data
loss — a genuinely-approved row failing to reach the snapshot and stranding as
an `unanchored_findings` ERROR. All three held under driven scenarios on scratch
scaffolds:

- Spine flip copies ONLY the flipped registry; an off-spine registry that merely
  drifted keeps its seed bytes and still differs from live, so the census
  survives (driven + `test_a_spine_flip_LEAVES_the_offspine_snapshot_bytes_UNTOUCHED`).
- An off-spine (interfaces) `Drafted→Approved` transition authorises copying its
  own registry with a BARE `intake.py snapshot`, and a brand-new already-`Approved`
  row does too — driven both; `unanchored_findings` stays `[]` in each, so no
  approval is stranded. `SNAPSHOT_TIERS` correctly excludes SN, which is also
  outside `unanchored_findings`, so not copying it on a spine act is harmless
  (matches the parked-SN design at `baseline_snapshot.py:440`).
- The de-approval regression (round-004 MAJOR) is genuinely fixed: driving the
  pre-fix predicate (any Status difference = flip) puts the de-approved row in
  `flips` and the refusal returns "" — the fix's regression test
  `test_a_DEAPPROVAL_cannot_authorise_an_unrelated_approved_amendment` fails on
  pre-fix behaviour and passes on the fix.
- The mirror-green claim is sound: `committed_snapshot_findings` pins each
  snapshot file to live at ITS OWN writing commit (`acceptance_record.py:745`),
  so an untouched off-spine file stays at its seed commit where it matched —
  verified in code and `test_a_SCOPED_refresh_leaves_the_UNTOUCHED_offspine_mirror_GREEN`.
- `parse_approves`/`resolve_registry` raise on every malformed pair, empty ref
  and unknown registry, and resolve a rel / filename / stem — driven, all correct.

Harness (run once each, my own runs): `check.py --jobs 0` — Check summary PASS on
every step except `doc-navigability` (broken link `docs/log.md:56390 ->
log.d/plans/2026-09-01-approval-act-adjudicator-only.md`, introduced by trunk
commit `ccc69963`, an ancestor of `contract_split`, in a file excluded from this
scope — pre-existing, not this diff). `trace.py --strict-integrity` — `integrity=0`.
The two spine-stand-alone/provenance findings (LLR-197 cites WI-448) are on rows
this diff does not touch. Affected test files (`test_baseline_snapshot`,
`test_trace_briefs`, `test_module_size_ratchet`) — 78 passed.

- [MINOR] project-trajectory/scripts/baseline_snapshot.py:253 -> the refusal core of named scoping — `resolve_registry` raising on an unknown name and `parse_approves` raising on a pair with no `=`/empty ref, the exact "silently matched nothing → muted no gate while reading as authorised" false-authorisation the docstring names — has no direct regression test; only the happy path is exercised (the CLI test's `system-requirements.toml=sitting-4`). Driven here and correct in every case, so not a defect -> add unit coverage for the three raise paths and the rel/filename/stem forms -> @owner. No antidote clause owed: the construction is ALREADY the sound one — `resolve_registry` makes a silent no-match unrepresentable by raising rather than returning a default — so the gap is a missing pin on that guarantee, not a missing guard.
- [MINOR] project-trajectory/scripts/baseline_snapshot.py:709 -> `_authorised_registries` (new, local cognitive ~19 > threshold 15) and `copy_live` (local 19→21 over its `docs/complexity-baseline` row of 19) carry no baseline row / re-stamp, though the sibling SLOC ratchet WAS updated this commit (intake.py 1177→1179) — the `docs/complexity-baseline` ledger's "only-ever-tightens, reason in the same commit" rule wants the new over-threshold function recorded when introduced; the local instrument also reds ~11 trunk-untouched functions, so the exact score is not trustworthy from here -> re-measure on the authoritative environment and, if a cognitive score crossed, add the reasoned baseline row/re-stamp -> @owner. Non-blocking: the `[step:complexity]` gate is armed at DevStg-Impl (enforcement-audit.md:64) and this work sits at DevStg-LLReqs, so it is not the active bar. No antidote clause owed: the remedy edits a debt ledger, not code.

VERDICT: APPROVE findings=2
