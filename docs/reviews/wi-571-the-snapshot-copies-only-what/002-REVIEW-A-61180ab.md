## 2026-09-01 — WI-571 independent review A — 61180ab

- [MINOR] project-trajectory/scripts/baseline_snapshot.py:849 -> A refresh authorised solely by a `Status` move copies the scoped registry but never calls `_record_approval`, despite the new contract requiring the stamp to name every copied registry and whether a ref or Status move authorised it; the real CLI scenario produced `snapshot: 1 registry file(s)` and no `README.md`, leaving that approval act unauditable in the promised stamp -> record every non-seed refresh with copied registries, passing `{}` when no named ref was supplied so the entry says `Status move` -> @owner
- [MINOR] docs/work/queued/WI-569-wi-508-spine-reseal-one-clean.md:42 -> The new triage says a bare WI-569 snapshot will copy LLR-203/LLR-204 because its approval commit flips them, but this same spec says both rows are already `Approved` and the successor only confirms them; the shipped scoped path therefore copies zero registries on that bare command, so the planned regeneration does not occur -> amend the WI-569 stand instructions to name the intended spine registries with `--approves` (or explicitly add the required Status move) before relying on a snapshot refresh -> @owner
VERDICT: CHANGES-REQUESTED findings=2
