# WI-542 adjudication — the close of WI-521 (lane wi521-decomposition-debt-owner)

The lane closed `WI-521` **partial**, reason "worker exit 4", and left the
keep/discard split explicitly OWED to the adjudicator (`split_decided_by =
"adjudicator"`) — the correct move for a lane whose worker died before it could
judge its own work. The four questions, answered from the commit range
`efcde754aa..378e90005b` and the review telemetry, not from the report's prose.

## 1 — Is the claimed outcome right? YES, PARTIAL.

`WI-521` is a **standing debt owner** by its own scope ("THIS ROW IS A STANDING
DEBT OWNER, NOT A ONE-SITTING TASK … closed only when the debt below is paid or
re-homed"). A clean scoped slice landed (slice 3: `test_trace.py`'s IF-seam tier
split to `tests/test_trace_interfaces.py`), and substantial debt remains
un-paid, so the close is neither COMPLETE (the debt is not paid) nor CANCELLED
(real, verified work survives on trunk). PARTIAL stands.

## 2 — Is the keep/discard split honest? It was OWED; I decide it: KEEP ALL, DISCARD NONE.

The build committed (`e3c820cc` BUILD COMMITTED) and slice 3's two work commits
(`c9203f47`, `adfc1204`) are independently re-verified clean by the REVIEW-A
session that reached furthest before dying (session 003, log 489 lines): node-id
set equality vs the pre-split file at `56e7e52b` (92 ids, empty diff, no id
added/dropped/merged), the moved IF-tier bodies byte-identical to the original,
only the two documented `_report`-inline edits changed in the core module,
`91 passed / 1 skipped`, `-m smoke` membership unchanged, `trace.py --strict`
clean but for the pre-existing `SR-181` orphan owned elsewhere. The three
REVIEW-A ERROR/TIMEOUT sessions are an EXTERNAL infrastructure failure — the
Codex reviewer hit its usage limit ("try again at 8:34 AM") twice and timed out
at 7200s once — never a defect in the shipped diff. Nothing on trunk in this
range is unsafe; the remaining commits are the station refresh, the log
fragment, and session telemetry. Whole range is KEEP.

## 3 — Should a successor exist? YES, one.

The standing-owner role cannot ride a disposed row: `WI-521`'s spec now lives in
`docs/work/partial/`, and a closed row is never revived. A successor
(drafted below, `supersedes = "WI-521"`) carries the un-paid debt forward: the
three remaining fusion heads (`agent_loop`, `agent_common`, `bootstrap`), the
rest of `check_trajectory` (4,327 lines, `main` at complexity 24), and M-06's
two remaining monoliths (`test_trajectory_arch.py` 2,290, `test_agent_loop.py`
1,640). It does NOT carry §3's sensor/axis question — OI-68 re-homed that to
`WI-537`/`WI-538` on 2026-08-30, as slice 3 recorded.

## 4 — What does the successor cost? buildtier = strong, planmode single.

The remaining heads are the harder ones (`agent_loop` — twice-decomposed
already, `agent_common`, `bootstrap`, and `check_trajectory.main` at 24), each
slice re-measuring a blind derivation and touching the spine on module moves —
the original's `strong` tier is right. The work is a build (decomposition
slices), not a design fork, so no `planmode = "dual"`.

## Findings

- [MAJOR] the module-size ratchet still names the disposed owner -> `tests/test_module_size_ratchet.py` names `WI-521` as the debt owner (module docstring "THE DEBT OWNER IS NAMED ONCE … AND IT IS `WI-521`", the `"decompose (WI-521)"` finding message, and the baseline-entry comments), but `WI-521`'s spec is now in `docs/work/partial/` — a growth sensor whose commentary names a terminal item is the exact dead-owner defect this row was filed to make unreachable -> when the machinery mints the successor drafted below, move the ratchet pointer (docstring, the finding message, and the baseline-comment references) to the successor id in that same commit, per the rule this row inherited ("if this row closes, move the ratchet pointer in the same commit") -> @owner
- [MINOR] the keep/discard split was empty at close and is resolved here, not by the lane -> a dispatcher closing a crashed worker has no view of the work, so `keep=[]`/`discard=[]` was correctly deferred (`split_decided_by = "adjudicator"`); left unresolved it would leave unjudged commits on trunk, the defect this rung exists to catch -> recorded ruling: KEEP all of `56e7e52b..378e90005b`, DISCARD none — slice 3 is independently re-verified clean and the REVIEW-A failures are external infra (Codex usage-limit / 7200s timeout), not code -> @owner

OUTCOME: PARTIAL successors=1
