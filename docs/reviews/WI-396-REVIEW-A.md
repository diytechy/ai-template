### REVIEWER — G3 — Round 1 — 2026-08-01
Verdict: APPROVE
Findings:
- none against the defect claim, which I REPRODUCED rather than inherited, because this row's evidence is a differential and a differential is exactly the kind of claim that survives being wrong. At the branch's base (`19fddb7a`) the predicate answers asymmetrically on two byte-identical halves: `is_path_shaped('project-trajectory/work/WI-000.template.md:40-41')` is **False** while `is_path_shaped('docs/work/queued/WI-000-example.md:40-41')` is **True**. The consequence is the sharper half of the claim and it holds on the real tree: a probe doc naming both halves reports `1 dangling · 928 untraced`, and the clean run of the same tree reports `928 untraced` — the same number, so the kit half was not merely excused, it was **counted in no bucket at all**. The mechanism the row names is the one I found: `:40-41` defeats `endswith(PATH_EXTS)`, so every suffixed token turns on `PATH_PREFIXES`, and that tuple lists `registries/`, `skills/` and `ci/` *without* the `project-trajectory/` prefix they actually live under here — it is the DOWNSTREAM layout, which is why no entry for the kit tree was ever missing from it. -> no remedy owed -> @owner
- none against the choice of fix, and the choice is the thing this row was left to make. Stripping the suffix is preferred to a tenth `PATH_PREFIXES` entry because it fixes every prefix at once and leaves that tuple a list of *layout conventions* rather than a patchwork of trees somebody happened to cite; it is also the direction `docs/archive/history/backlog-plan-2026-08-01.md:61` records. The choice DETERMINES the verdict, and the row is explicit that a test asserting only equality would be satisfied by three different fixes: strip yields **BOTH CLEAN**, adding the prefix would have yielded **BOTH DANGLING**. `test_a_suffixed_pair_reaches_the_same_named_verdict_both_clean` pins the named outcome and — the part I checked hardest — also asserts a **zero** untraced count, which is what discriminates CLEAN-because-it-resolves from CLEAN-because-it-was-excused-as-kit-relative. Without that assertion a later change that let the kit half pass by exemption would keep the test green while re-hiding the tree the row exists to protect. -> no remedy owed -> @owner
- none against the two call sites, which is the half a wrong answer would have been silent about, and I DROVE the half-fix rather than citing WI-394's measurement of it. Reverting `path_findings` to derive `clean` from the original token (leaving the strip only in the predicate) reds all three of the new differential tests, and on this repo reads **971 untraced** against **832** for the full fix — 142 live files reclassified as explained-missing, and only the record-surface reason keeps them out of DANGLING here, which is luck of this repo's citation habits rather than a property of the fix. The finding is real and the guard covers it: the trap is not that the half-fix looks wrong, it is that on THIS tree the half-fix still exits 0. -> no remedy owed -> @owner
- none against the mutation twin, which is the row's own DONE-WHEN and the one place a green could have been vacuous. `test_the_suffixed_pair_diverges_when_either_half_is_broken` deletes each half of the pair IN TURN and asserts the pair DIVERGES (`DANGLING` for the broken half, `CLEAN` for the intact one). Both directions matter and I checked why: a rule that examined only one side would still pass a single-sided mutation, so a one-directional twin would have ossified half the bug. I watched the tests fail before I trusted them — against the unfixed tool at `19fddb7a`, `pytest -k "suffix or suffixed"` gives **3 failed, 1 passed**, and the one that passes (`…missing_file_still_gates`) is honest about what it guards: `scripts/deleted.py:40` was already path-shaped via the `scripts/` prefix, so that test defends against the *alternative* fix ("a suffixed token is never a path") rather than against the old behaviour, and the row's Deliverable does not claim otherwise. -> no remedy owed -> @owner
- none against the blast radius, which I probed at the predicate rather than reasoning about it. `tests/x.py::test_it:40` is still **not** path-shaped — the `::` guard fires before the strip, so WI-394's deliberate, comment-guarded decision about pytest node ids is untouched and this row does not quietly pre-empt the sibling it sits beside. `docs/x.md:` and `docs/x.md:40-` are NOT stripped (the regex demands digits) and were already shaped via the `docs/` prefix, so they are unchanged. `a/b:1` strips to `a/b` and remains unshaped. The one class that genuinely widens is the absolute path: `C:/Projects/x.md:40` is now shaped where it was invisible — but `C:/Projects/x.md` *without* a suffix was already shaped and already dangling, so the fix makes the two forms agree rather than inventing a new false-positive class, and the repo carries zero instances today. -> no remedy owed -> @owner
- none against the record, checked figure by figure against the commands that produced them. The Deliverable's real-repo numbers are re-derived here as a SAME-TREE A/B, which is stricter than the before/after the row needed: with the tree fixed at `bfa6cd3e`, the pre-fix tool (`git show 19fddb7a:…/check_doc_refs.py`) reads `0 dangling · 931 untraced` and the shipped tool reads `0 dangling · 832 untraced`. Zero dangling on both sides is the load-bearing part — **no prose repair is owed anywhere in this repo**, so this fix does not smuggle in a doc-churn sweep. The 99-token drop is suffixed citations to LIVE files that had been counted as explained-missing, which is an accuracy gain in a number the tool asks people to watch. The Deliverable quotes `928 → 829` at the code commit `ec825942` and this review quotes `931 → 832` at the close tip; both are true of their own tree and the difference is this row's own added prose, which is the sort of drift a figure without its revision would have hidden. -> no remedy owed -> @owner
- none against scope, read against the row's explicit guard ("do NOT widen this into a general path-token rewrite"). The core delta is one compiled regex, one four-line private helper and two call-site lines; `PATH_PREFIXES`, `RECORD_PREFIXES`, the `::`/`;`/`,` guards, the placeholder rules and the `path-ok` escape are all untouched. `_strip_line_suffix` is private deliberately — it is an internal normalization, and a public name would have entered the generated module map for no reader's benefit (`gen_arch_map.py --check --strict-parse --src project-trajectory/scripts` reads `code map up to date`). The one non-code edit is the kit-contents row in `project-trajectory/README.md`, and I think it is owed rather than optional: this is DOWNSTREAM-VISIBLE behaviour — an adopting repo citing a deleted file *with a line number* now reds under `--strict` where it was silent — and CLAUDE.md asks that anything forcing a downstream migration be flagged where adopters read. `LLR-038`'s `Detail` describes the shape rule without the suffix clause and is correctly left alone: it is not FALSE (a stripped token still needs an extension or a prefix), and amending it is spine work for WI-390's batch, not an ordinary row's to take. -> no remedy owed -> @owner
- none, and I confirmed the row's three PROSE findings by reproducing each, since a finding filed in place of a WI id (owner ruling R1) is only worth the discipline if it is true. (1) A standalone `check_doc_refs --strict` really cannot pass on a fresh lane worktree: `docs/test/report.md` is generated and gitignored, `README.md:154` and `README.md:187` cite it, and this worktree read **3 dangling** until `trace.py` had run — invisible in the primary checkout (the file is there) and invisible inside `check.py` (the trace step runs first), so only a lane builder running the step alone ever meets it. (2) The R-F ritual bit for real: with `specref` still set on a `complete/` row, `check_trajectory --root . --strict` exits **1** with `R-F WI-396: status=done but SpecRef … is still set`, and the FULL pytest suite passed while that was true — the suite carries no meta-assertion that this repo's own work registry survives `--strict`, so the only thing standing between that red and trunk is the G3 bar (`check.py` appends `--strict` at G2/G3, `check.py:479`). Clearing the key is what WI-397 did at `c25272c1`, so this row follows the precedent rather than inventing one. (3) The claim did refuse on a single uncommitted editor-preference line in `.vscode/settings.json` — parked in a named stash and restored after the merge, not discarded. -> no remedy owed -> @owner
- none — BARS at `bfa6cd3e`, every one foreground and read, `git status --short` empty. Full `pytest -q -n auto` **1797 passed, 8 skipped in 301.63s**; `pytest -q -n auto -m smoke` **577 passed in 14.13s**; `check_trajectory --root . --strict` rc=**0**, `clean (398 work item(s), 370 done (93%), 17 cancelled, graph acyclic)`; `check_doc_refs --root . --strict` rc=**0**, `no dangling path or sym: references · 832 untraced`; `check_docs --root . --ignore docs/test/report.md --ignore "docs/work/*" --stale` **OK - 349 doc(s), 1013 intra-repo link(s), 0 broken**; `ruff format --check` 146 files already formatted, `ruff check` All checks passed. TWO REDS WERE CAUGHT AND FIXED RATHER THAN WAIVED, and both were mine: the log fragment linked its own not-yet-written review (`test_meta_repo_has_zero_unexplained_orphans`, one broken link — the link became prose), and an in-place Python rewrite of the spec file flipped the WORKING COPY to CRLF while `git status` stayed clean under `text=auto`, which `test_the_live_registry_round_trips_in_whichever_home_is_authoritative` caught as `does not start with a +++ frontmatter fence`; restored from the blob, and the committed content matches its neighbours byte-style for byte-style. SCOPE, stated rather than implied: this covers the BRANCH, not the composed tree — trunk is not yet an ancestor, so the station refresh's bar speaks for the composition. NOT COVERED: the `sym:` tier is untouched and unexercised by this change; the strip is not applied to markdown LINK targets (that is `check_docs.py`'s surface and deliberately out of scope); and a citation form this row did not take on, `path:line:col`, remains unstripped and therefore unshaped unless it carries a known prefix. **THIS IS AN APPROVE:** the defect is reproduced rather than inherited, the fix is the smallest rule that closes every prefix at once, BOTH call sites are changed and the half-fix was driven to prove why, the guard is a two-directional mutation twin rather than an equality that cannot fail, the real-repo effect is measured as a same-tree A/B with zero prose repair owed, and the three lane-discovered findings are filed as prose under R1 with no id minted anywhere on this branch. -> no remedy owed -> @owner
VERDICT: APPROVE findings=0
