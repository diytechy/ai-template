ADVERSARIAL REVIEW of a completed build. You are running inside a throwaway git WORKTREE of the repo `c:\Projects\ai-template`, checked out at commit `8599f2b0` (branch `contract_split`, its tip); the worktree is your current directory. Your job is to BREAK the build, not to praise it. Assume the implementer was over-confident. The repo's Python is the MAIN checkout's interpreter — `C:\Projects\ai-template\.venv\Scripts\python.exe` (this worktree has no `.venv`) — run it from the worktree root. NEVER touch the main checkout at `C:\Projects\ai-template`. You MAY plant a change in THIS worktree to test a gate, provided you restore the file byte-exact afterwards (`git checkout -- <path>`) and never commit; report every plant you made. Put every reproduction you attempt in your answer. A claim without a reproduction is worth less than a reproduced one.

WHAT WAS BUILT (work item WI-533, OI-67 slice 6, "arm the gate"; the ruling: `docs/log.d/2026-08-29-oi67-ruled-a.md`; the plan of record: `docs/plans/2026-08-29-if-row-shape-plan.md` §3 "Slice 6" and §4; the decisions the slice took alone: `docs/decisions-for-review-2026-08-29-slices-4-6.md` §6). This slice claims:
- THE GATE. `check_trajectory.contract_body_findings` — WARN plain, ERROR under `--strict` — fails (a) a row its owner DECLARES (`Contracts:` marker) but does not state (no `Contract IF-###:` body), (b) an `external:`-owned row that no far-side kit module states, (c) a source declaring another owner's row (a stray declaration). Vacuous in files-mode or with no scan root; the seam-TC promotion's idiom and its `[checks] interfaces_check` opt-out. BY DECISION (6.2) an owner that declares NOTHING stays the owner-exact reverse check's WARN — the dodge "never declare, never owe a body" is visible but not gated.
- THE RETIRED CELLS are strict findings in `trace.interface_findings`, both spellings (`Contract`/`contract`, `Provider`/`provider`, `Req-Refs`/`req_refs`, `Signal`/`signal`, `SignalNote`/`signal_note`); `contract` left `kitlib.spine.SPINE_TIER_KEYS`, the carrier map, the template, the INTERFACES table, `PROCESS.md` §8 and the OKF description fallback; the legacy `contract` advisory (19 lines) is deleted; `migrate_carrier --if-shape` reports every row still carrying `contract` and never drops the cell.
- ONE CSV READER. `kitlib.spine.csv_body` / `csv_reader` / `csv_rows` — comment-skipping — and every kit reader of a CSV goes through it: `load_csv`, `check_perf.load_budgets`, `gen_release_checklist.load_csv`, `wi_convert.load_csv` (its header check now sees the real header), `spine_carrier`'s two CSV-carrier readers, `migrate_carrier`'s legacy source read, `check_trajectory.read_rows`, `gen_okf.read_rows`, `schedule.load_rows`, `agent_common._read_csv_rows`, `plan_artifacts`' id scan, and `trace.structure_findings`' raw-line column counter (which otherwise read the header's sixteen comment lines as ten one-column rows). So `IF-031`'s owner, the kit's `performance-budgets.csv`, now declares in its own `#` header.
- THE EXTERNAL RULE (decision 6.1). The three `external:`-owned rows are stated by their FAR SIDE — `IF-032` in `check_privacy`, `IF-036` in `check_vendored`, `IF-041` in `agent_session` — as "our reading of" the surface (what is read or sent, what is assumed, what a failure does); a module that is not the far side may not state an external row (a strict finding).
- ALL 154 ROWS STATED: `docs/interface-reference.md` reads "74 sources declare 154 seams; 154 carry a stated contract".
- ALSO: `gen_okf._doc_title_and_summary` skips a leading HTML comment WHOLE (it skipped only the first line — live for `docs/status.md` since slice 4); sixteen half-moot `reason` cells in `docs/if-tc-coverage-allow` trimmed (ownership / nearest-seam arguments dropped, honesty valves and measured reader sets kept; zero IF citation-frame warnings remain); the slice-4 cross-family round's six cheap findings APPLIED (`IF-159` / `IF-160` bodies made true, `IF-102`'s `rows_seq_from_text` clause removed, `IF-163` narrowed to the hand-authored bytes outside the marker pair so it no longer overlaps `IF-164`, the `trunk_step.fragment_paths` README exemption case-folded, the WI-531 fragment's far-side count corrected to nine) and five DEFERRED with reasons (`docs/reviews/2026-08-29-oi67-slice4/README.md` Dispositions).
- PROVED, it says, by tests on scaffolds and by planting on the kit's own tree: a `contract` cell on `IF-069` reds `trace --strict`; `IF-069`'s body deleted from `check_coverage` reds `check_trajectory --strict`; both restored byte-exact.
- RATCHETS: `check_trajectory.py` 4480 → 4638 lines, `trace.py` 5898 → 5912, `agent_common.py` +1, `kitlib/spine.py` 676; `PROCESS.md` 87,651 → 87,782 bytes.

Read these:
- `docs/log.d/2026-08-29-wi533-arm-the-gate.md` — what it claims it did; `docs/decisions-for-review-2026-08-29-slices-4-6.md` §6.
- `project-trajectory/scripts/check_trajectory.py` (`contract_body_findings` and the helpers it decomposed into, `_owner_exact_findings`, `_declared_seam_pairs`, the `--strict` plumbing and the `interfaces_check` opt-out), `project-trajectory/scripts/trace.py` (`interface_findings`, `structure_findings`), `project-trajectory/scripts/kitlib/spine.py` (`csv_body`, `csv_reader`, `csv_rows`, `SPINE_TIER_KEYS`, the carrier map), `project-trajectory/scripts/migrate_carrier.py` (`--if-shape`), `project-trajectory/scripts/gen_okf.py` (`_doc_title_and_summary`), `project-trajectory/scripts/check.py` (the step that runs `check_trajectory` — which flags, and whether the strict arm is what the harness runs).
- `docs/requirements/interfaces.toml` (the header and every row); `docs/interface-reference.md`; the kit's `performance-budgets.csv` under `docs/` and the shipped `performance-budgets.template.csv`; the headers of `project-trajectory/scripts/check_privacy.py`, `check_vendored.py`, `agent_session.py`, `check_coverage.py`; `docs/if-tc-coverage-allow`.
- The tests that pin the gate and the reader — find them by grepping `tests/` for `contract_body`, `csv_body`, `csv_rows`, `retired`, `_doc_title_and_summary`.
- `docs/reviews/2026-08-29-oi67-slice4/README.md` — the Dispositions table this slice wrote.
- `git show 87c1fc38 --stat` and `git show 8599f2b0 --stat` — the two commits of the slice.

FIND, in priority order:

1. **A GATE THAT LIES — FALSE GREENS.** Run `python project-trajectory/scripts/trace.py --root . --strict` and `python project-trajectory/scripts/check_trajectory.py --root . --strict` at HEAD and record their EXIT CODES (not only the printed lines — a WARN line and exit 0 is a pass). Then plant, one at a time, restore between, and report which stay green (exit 0): (a) delete a declared row's body from a MODULE owner (`Contract IF-069:` in `check_coverage.py`), from a `#`-header owner (`IF-031` in `performance-budgets.csv`), from a README owner (`IF-156` in `docs/log.d/README.md`'s HTML comment), from a config owner (a `docs/process.toml` row); (b) keep the `Contract IF-069:` line but empty it (nothing after the colon; one word; a body that is only whitespace or a `#`); (c) declare a row on a source that is not its owner (add its id to another module's `Contracts:` line, with and without a body); (d) an `external:`-owned row: delete `IF-032`'s body from `check_privacy`; then state `IF-032` in a module that is NOT on its far side — does the gate red the stray, and does it still count the row as stated?; (e) a row whose far side is ITSELF external (is there one? if not, plant `owner = "external:x"`, `consumers = ["external:y"]`) — who must state it, and what does the gate say?; (f) the five retired cells, both spellings, planted on a live row — and spellings the code might miss (`Contract` as a key inside a `notes` cell; a `contract` key in a nested sub-table; a `provider` key with an empty value); (g) the opt-outs: `[checks] interfaces_check` off in `docs/stack.ini`, files-mode, no scan root — what does the gate do, is the harness step (`check.py`) running the ARMED mode, and can an adopter disarm it by accident?; (h) the undeclared-owner dodge (decision 6.2): remove an owner's whole `Contracts:` marker AND its bodies — confirm strict exit stays 0 and say exactly where the dodge is visible (the WARN text, the reference's summary line) and whether anything downstream — the dashboard, the approval brief — would show it.

2. **THE CSV READER.** Is any kit CSV reader still raw — `csv.reader(open(...))`, `csv.DictReader(fh)` on a file handle, `read_text().splitlines()` over a `.csv`, a `Path.open` in a `csv` context — outside `kitlib.spine`? Grep `project-trajectory/scripts/` and count sites against the fragment's twelve. Feed `csv_rows` / `csv_body`, in a temp dir: a `#` inside a quoted cell; a data row whose first cell begins with `#`; a UTF-8 BOM; CRLF endings; a blank line; a comment line in the MIDDLE of the file; an empty file; a header-only file; a comment line that ends with a backslash. Does `trace.structure_findings`' reported line number still count the FILE's own lines with a sixteen-line header present (plant a wrong-column-count row after the header and read the number)? Does `wi_convert.load_csv`'s header check pass on a headed file and fail on a wrong header? Does the shipped headerless template read identically to the kit's headed copy?

3. **THE EXTERNAL RULE (decision 6.1).** Read the three bodies. Is each "our reading" — what is read or sent, what is assumed, what a failure does — and is every claim TRUE of the code beside it (`check_privacy` against git's actual output on this tree; `check_vendored` against the upstream-docs read; `agent_session` against how it actually invokes the CLI)? When a row names SEVERAL far sides (an `external:` row with a two-element consumers list), does one far side stating suffice, must all, and does the reference say which stated? Is the rule stated where an adopter will read it (`PROCESS.md` §8, the template header, `interface-reference.md`'s own preamble)?

4. **THE SIX APPLIED, THE FIVE DEFERRED.** For each APPLIED disposition (#1 `IF-159`, #2 `IF-160`, #5 `IF-163` vs `IF-164`, #7 `IF-102`, #10 the README case-fold, #11 "nine"): is it applied on THIS tree, and is the applied text TRUE of the code (read `wi_convert._self_verify`, `score_reviews.record_round`, `trunk_step.fragment_paths`, the `IF-163`/`IF-164` bodies against `traj_status`)? For each DEFERRED (#3 `IF-156`'s deletion arm, #4 the three fragments opening with `#`/`###`, #6 `IF-020`'s stdout and log kinds, #9 `TC-161`'s prose): does the tree still carry the defect exactly as described, and is the deferral reasoning sound? #8 ("strict allows invalid ownership/body states") was dispositioned ADDRESSED BY THIS SLICE — is it, or does a state the slice-4 reviewer named still pass?

5. **`gen_okf._doc_title_and_summary`.** Feed it: a leading HTML comment with no closing marker; a comment closing mid-line with text after `-->` on the same line; two leading comments; a comment after a blank line; a document that is ONLY a comment; a comment containing a `#` heading. Then the sixteen trimmed `reason` cells: run whatever lint reads them (`check_trajectory --strict`, `trace --strict`) and diff the cells against `git show 87c1fc38^:docs/if-tc-coverage-allow` — did any trimmed cell lose the honesty valve or the measured reader set the fragment says was kept?

6. **CITATION ROT for the retired cells.** Grep the WHOLE tree — `project-trajectory/` templates and docs (`PROCESS.md`, `PROCESS_OPTIONS.md`, `EXAMPLE.md`, `ADOPTING.md`, `RESYNC_PACK.md`, `AGENTS.template.md`), `docs/` (`registry-machinery-reference.md`, `enforcement-audit.md`, `interface-reference.md`), `tests/`, fixtures, `.claude/skills/` and `project-trajectory/skills/`, generated files — for `contract =`, `req_refs`, `provider =`, `signal_note`, `Req-Refs`, `SignalNote`. Classify each hit: a record (log, review, archive — fine), live prose that still describes a cell the code no longer reads (a finding), a test that pins the old shape, a generated file that should have been regenerated.

7. **THE MIGRATION.** Build a legacy registry in a temp dir (the shipped template plus one row carrying `contract`, `provider`, `req_refs`) and run `migrate_carrier --if-shape` on it: does it report the lingering `contract` and never drop it; run twice — idempotent; run it on the shipped template itself. Do the two commands the RESYNC entry names actually list what will red?

8. **THE RECORD vs THE TREE.** Every number in the fragment: 74 / 154 / 154 (recompute from `interfaces.toml` and the reference); `check_trajectory.py` 4480 → 4638, `trace.py` 5898 → 5912, `agent_common.py` +1, `kitlib/spine.py` 676 (`git show 87c1fc38^:<path> | wc -l` vs HEAD); `PROCESS.md` 87,651 → 87,782 bytes (mind CRLF: measure the blob, not the working-tree file); sixteen reason cells; twelve reader sites; the smoke-tier membership stamp 1377 → 1390; the "six applied / five deferred" against the Dispositions table. Report any that does not reproduce.

Report as a severity-ranked list: `[CRITICAL|MAJOR|MINOR]` — location — what is wrong — the reproduction you ran — the concrete change. End with one line: `VERDICT: APPROVE` or `VERDICT: CHANGES-REQUESTED findings=N`.
