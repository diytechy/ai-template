## 2026-08-30 — REVIEW-A WI-546 round 4 (`8c487423`)

Scope: `git diff HEAD~1 HEAD` changes only `tests/test_hats.py`; no SN/SR/LLR/TC
registry row changed, so no historical-registry conflict sweep or approval-batch
check was triggered.

Worst failure classes attacked, in order: silent wrong content (an out-of-pack
path accepted as knowledge); fail-open (an absolute, traversal, or non-pack value
accepted); data loss (not applicable to this test-only change).

Evidence observed:

- `python -m pytest -q tests/test_hats.py` — `57 passed in 0.62s`.
- `python -m pytest -q tests/test_hats.py -k "knowledge_guard or live_roster_knowledge"` — `2 passed, 55 deselected in 0.10s`.
- Replaying the pre-fix predicate against `docs/knowledge/../status.md` printed
  `old_accepts=True`; the new negative assertion then raised `AssertionError`.
- `python project-trajectory/scripts/trace.py` — exit 0; existing advisories and
  the pre-existing LLR-197 provenance finding were reported.
- `python project-trajectory/scripts/check.py` — `RESULT: PASS`.
- `git diff --check HEAD~1 HEAD` — exit 0.

Spec Done-when coverage:

- Every live `knowledge` value resolves to an existing Markdown target below
  `docs/knowledge`, and drafted packs retain `DRAFT`: covered by
  `test_live_roster_knowledge_values_resolve_and_the_drafted_packs_say_DRAFT`.
- Traversal, absolute, and non-pack values are rejected even when a target
  exists: covered by
  `test_the_knowledge_guard_refuses_traversal_absolute_and_non_pack_paths`;
  demonstrated RED against the pre-fix predicate.
- The historical 17 approved-cell Rationale attribution deletions: UNCOVERED by
  a direct identity/count test in this test-only diff; it is not modified here.

No findings.

VERDICT: APPROVE findings=0
