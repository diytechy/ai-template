### REVIEWER — G3 — Round 1 — 2026-07-31
Verdict: CHANGES-REQUESTED
Findings:
- [MAJOR] project-trajectory/scripts/gen_trajectory.py:76,79,111,113,114,115,138 -> **The split breaks `ruff check`, and the WI's own gate move is what hides it.** Driven at the tip with the repo `.venv`: `ruff check project-trajectory/scripts tests` = **7 errors, all F401, all in the facade** — `math` (:76) and `subprocess` (:79) are genuinely dead there (`grep "math\.\|subprocess\."` over gen_trajectory.py = 0 hits; the code that used them moved to the siblings), and the five bare `import traj_views/traj_render/traj_panels/traj_parse/traj_graph` lines (:111,:113,:114,:115,:138) carry no `# noqa: F401` while the `from traj_* import (...)` blocks beside them do. Real trunk (`c:\Projects\ai-template`, ConcurrencyTrainRewrite) is **"All checks passed!"** and its declared bar is **exit 0 at G3**. Bisected across the branch — 7d38b1c **0**, 234debc 1, 2158f08 3, 259b95a 4, 25b9ce6 5, bd32d93 5, d600969 **7**, and 7 at every commit since: introduced by S2 and grown by each split slice. Why no signal fired: `lint` is a `[product] [G3]` step, so at the derived **G2** this branch now sits at, `check.py --tier smoke` prints `FAIL lint exit 1 [advisory — not gating]` and still exits **0** — which is exactly what I measured on the work branch. I then simulated the state my own ratification produces (11 SRs back to `Verified`, `trunk_step --regen`, gate re-derived to **G3**, non-work branch): `check.py --tier smoke` = **exit 1, `FAIL lint`, now gating**. So the repo's own bar goes red the moment the eleven rows are blessed. This is the named recurring defect in its purest form — the close set signs `ruff format --check` (formatting) and never `ruff check` (lint), and the one step it omits is the one its change reds. -> Delete the two dead stdlib imports; mark the sibling module imports the facade deliberately keeps reachable with `# noqa: F401` (the marker the neighbouring from-imports already use — `gt.traj_graph`/`gt.traj_parse` are load-bearing, tests/test_gen_trajectory.py:5072,5329 patch them), or drop any that nothing needs. Then add `ruff check` to the WI's signed close set. -> @owner
- [MAJOR] docs/ratify/2026-07-29d-phase5.md (surfaced by docs/requirements/system-requirements.csv, the 11-row flip) -> **The composed-tree bar reds on a second, unrecorded gating step: `ratify-fresh`.** The record's harness analysis stops at the work branch — it correctly verifies (and I reproduced) that `arch-map`/`trajectory-map`/`status-map`/`open-items`/`ratify-fresh` all SKIP here with "generated freshness is the trunk lane's, §5.2" — but never states what happens once the branch is on the merge candidate, where nothing skips. Driven: clone at cb5d9cc on a non-work branch, `trunk_step --regen` (all six families **ok**), then `check.py --tier smoke` = **exit 1**, gated by `FAIL ratify-fresh`. Cause: `trace.py --ratify modified --check` short-circuits with "no `Modified` SR — the re-attest window is closed" while nothing is Modified (that is why trunk passes), and opening the window un-short-circuits it onto the committed historical brief, which reports `docs\ratify\2026-07-29d-phase5.md is STALE against the registry`. Reverting the 11 flips in a throwaway clears it (exit 0), so the flip is the trigger. **Root cause is NOT WI-280:** I probed trunk state 7cb7011 directly, flipped a single unrelated `SR-001` to `Modified`, and got the **identical** STALE message — the brief has been rotten since before this branch and trunk's green is an artifact of the closed window. WI-280 is simply the first WI to open one. Unrecorded either way, and it blocks this merge as surely as the lint red. -> Regenerate the stale brief in the trunk lane (`trace.py --ratify modified --out docs/ratify/2026-07-29d-phase5.md`) before/with the merge, and record in the fragment that opening a Modified window makes `ratify-fresh` gating on the composed tree — the record enumerates the G3-advisory demotions but not this `[G2,G3]` step. -> @owner
- [MINOR] docs/log.d/WI-280-bounded-core-decomposition.md (fragment, three figures) with tests/test_module_size_ratchet.py:267-307 -> Three signed numbers do not reproduce, in a record whose load-bearing claims all do. (1) **"941-line facade"** (repeated in the retired ratchet entry as "-970 (1911 -> 941)" and "941 is under THRESHOLD"): by the ratchet's own metric (`len(text.splitlines())`, tests/test_module_size_ratchet.py:40) gen_trajectory.py at the S6 commit d600969 is **942** (bytes 47338, 942 LF, 0 CRLF — measured three ways), so the delta is -969; at the tip it is **945**. Harmless (the entry is retired, nothing enforces it) but it is the permanent record. (2) **"both emit 1,776,473 bytes"** (slice-11 byte-golden paragraph): the identity claim is true and I reproduced it exactly, but the figure is not the tip's — both renderers emit **1,787,126** bytes with `.git` present, 1,787,086 without, and no commit on the branch yields 1,776,473 (83d6741 → 1,783,465, which is exactly the committed PROJECT_STATE.html; 5f11dc8/1288520/25b9ce6 → 1,786,110; 2158f08/234debc/7d38b1c → 1,785,756). (3) **"`ruff format --check`: 128 files already formatted"**: measured **131** repo-wide, **129** over the bar's declared scope (`project-trajectory/scripts tests`), 46 over scripts alone. One more nit in the same class: "The trunk step re-derives the identical bytes" for docs/gate is true of the gating first non-comment line and the `# basis:` line, but the as-of comment moves `83d6741` → `cb5d9cc` on regen. -> Re-stamp the three figures to the metric named beside them (942/945, 1,787,126, 129), and narrow the docs/gate sentence to the basis + gate lines. -> @owner
- [MINOR] project-trajectory/scripts/check_trajectory.py:2721-2769 (`_render_surface_paths`) -> The one slice-11 change to a *shipped* checker's behaviour lands with **no test and no owning spine row**. `grep -rn "_render_surface_paths\|_RENDER_SURFACE_GLOB" tests/` returns only the size-ratchet's comment; `critique_ratchet_findings` has no test either, and neither appears in low-level-requirements.csv or test-cases.csv (LLR-084 "Critique closure ratchet" owns the different `critique_staged_findings`). I drove both arms by hand and both are correct — in-repo it returns the facade + all six `traj_*.py` + the recipe, sorted; and with a synthetic root that does not contain the checker, both fallback homes (`project-trajectory/scripts`, `scripts`) return generator+siblings correctly. So this is not a defect, it is an unguarded change: the fragment's own justification is that a facade-only surface would leave the warn "running and always passing", which is precisely the failure mode a test would pin. Pre-existing gap extended, not created. -> Add one test asserting the surface contains the `traj_*` family (and one for the fallback arm), or record the absence in docs/enforcement-audit.md as a known Prose-only rule. -> @owner

RATIFICATION: **NOT ratified this round — and no human sitting is required.** The eleven rows earn ratification on their merits and I say so on evidence I produced: the SR edit is Status-only (field-compare of docs/requirements/system-requirements.csv at 7cb7011 vs HEAD — **135 rows both sides, identical header, exactly 11 changed cells, every one `Status` `Verified`→`Modified`, zero text changes**, independently confirming the builder's measurement); the LLR edit is **19 rows, `Module` cell only**, no `Status` and no `CodeSymbol` touched; the flipped SR set is **exactly** the set of SRs owning an edited LLR (set equality both directions — nothing missed, no unedited row flipped); `trace.py --ratify modified` renders **11 sections, each showing only the Module cell before/after**; and the amended pointers are true — the pre-split renderer (scripts at 7d38b1c) and the post-split renderer, run over identical inputs, emit **byte-identical** PROJECT_STATE.html (sha256 match, identical even before ASOF stripping). The builder's reasoning is correct and I reproduced the counterfactual it rejected: with the 19 LLRs flipped instead, `trace.py --ratify modified` reports "**No `Modified` SR — nothing owes a re-attest**" and the gate stays **G3** — the sitting would never see the change. Consequences verified as stated: `derive_gate.py --check` green at **G2**; `trace.py --strict` **exit 0**; `trace.py --strict --require-verified` **exit 1** with exactly 11 status findings, all on the flipped rows. What I withhold is only the *effect*: ratifying restores G3, and I have driven that the G3 bar is **red on `lint`**. Recording a G3 ratification over a red G3 bar is the silent-green defect this gate exists to prevent. Ratification is granted on the round-2 verdict, autonomously, once finding 1 is fixed and the round-2 bar is green — ordinary-class rows with mechanized evidence need no human.

VERDICT: CHANGES-REQUESTED findings=4

### REVIEWER — G3 — Round 2 — 2026-07-31
Verdict: APPROVE
Findings:
- none — all four round-1 findings close at tip 380e9c2, each re-driven from scratch on a real clone rather than read off the record. **(a) `ruff check` — fixed at the root, not suppressed.** `ruff check project-trajectory/scripts tests` = **All checks passed!** (exit 0). The two deletions are provably dead: `grep -rn "gt\.math\|gt\.subprocess\|gen_trajectory\.math\|gen_trajectory\.subprocess" tests/ project-trajectory/` is **empty**, and the only surviving textual hits in the facade are inside comments — nothing patched them, so removal was right and suppression would have been wrong. The suppressions are justified, and I checked the rationale rather than accepting it: deriving module-object reads mechanically gives exactly **`gt.traj_graph` (3 sites)** and **`gt.traj_parse` (3 sites)** — tests/test_gen_trajectory.py:5072,5087 patch `gt.traj_graph._detour_points` and :5329,5346 patch `gt.traj_parse.subprocess`, which is real and is the stated reason. **No live import was suppressed instead of removed.** Two precise observations, no remedy owed: `traj_status`'s marker is a no-op (it IS used — gen_trajectory.py:899 `traj_status.run_status`; ruff never flagged that line), and `traj_views`/`traj_render`/`traj_panels` have no current module-object consumer, so their markers buy a uniform declared surface ("Every sibling is ALSO bound as a bare module attribute") rather than silence a live diagnostic; RUF100 is not enabled, so neither can rot into a false green. **(b) The brief is the real instrument.** `docs/ratify/2026-07-31-wi280.md`: **11 sections / 143 lines**, the sections being exactly SR-050/052/053/054/055/056/071/088/089/090/135 — the same set I independently proved owns an edited LLR — each showing only the `Module` cell before/after, baseline `2eddc78c8` git-derived, no `--since` (correct, because amend+flip landed in one commit). `ratify-fresh` reports `is current (baseline git-derived)`, **exit 0**. It still bites: mutating `LLR-051`'s Module in a clone flips it to **STALE**, naming the new brief. The selection is documented and deliberate, not a dodge — `newest_ratify_brief` (trace.py:1319) picks newest **by NAME (the stamped date), explicitly "not by mtime"** — so `2026-07-29d-phase5.md` is a finished sitting's record that correctly stops being the gated file. **I withdraw my round-1 remedy for it:** regenerating that file would have rewritten what a past sitting was actually shown; a new window owing its own brief is the right fix and the builder chose it. The `docs/orphans-allow` entry is legitimate — the exact `docs/reviews/*` retained-evidence precedent, stated with a reason as that file's own rule demands, affecting orphan classification only and never link-breakage (check_docs still **0 broken**), and the brief stays gated by `ratify-fresh`, a stronger guard than navigability. **(c) Both composed-tree states reproduced on a real clone WITH history, not refuted.** (i) merge candidate, window open, non-work branch, after `trunk_step --regen` (all six families ok): gate **G2**, 19 steps, `ratify-fresh` **PASS**, `lint` **PASS**, **RESULT: PASS, exit 0** — the sole `FAIL` is the advisory `traceability --require-verified`, by construction on the eleven. (ii) post-ratification, eleven flipped back to `Verified` + regen: gate **G3**, **20/20 PASS, RESULT: PASS, exit 0** — the exact state I withheld over, previously `FAIL lint`. **(d) Metrics true by the metric named.** Byte-golden re-driven at this tip (pre-split scripts from 7d38b1c vs the shipped tree over identical inputs): both emit **1,787,126 bytes** / **1,776,534 characters**, **byte-identical even before the ASOF strip**; facade **949** splitlines at the round-2 tip (942 at S6, which I verified in round 1); `ruff format --check` **129** over the declared scope, 131 repo-wide; `check_docs` **332 docs, 934 links, 0 broken**. The "1,776,473 was a character count" explanation is honest and lands 61 short of this tip's character count — consistent with its own "taken at an earlier commit" qualifier, not a fresh overstatement. **(e) Confirmed:** on the regenerated clone the **IF-082/083/084/085 warns are gone** (only the pre-existing IF-055/080/081 remain, alongside the recorded traj_graph/traj_panels/traj_render residue and trunk's own drive/trunk_step warns), while the branch as committed shows **4** — the rule reads what `gen_arch_map` harvests into trunk-owned `docs/architecture.md`, exactly as claimed. **Plus MINOR 4, mutation-proven by me rather than on report:** stubbing the primary arm's sibling loop reds only `test_render_surface_covers_the_whole_generator_family`; stubbing the fallback arm's glob reds only `test_render_surface_fallback_arm_finds_both_scaffold_homes`; both green restored. Full suite at the tip: **1 failed, 1702 passed, 12 skipped** — the sole failure the standing `test_this_repo_is_not_a_work_branch`, expected on a claimed branch. -> no remedy owed -> @owner

RATIFICATION: **GRANTED. I ratify the eleven `Modified` SRs — SR-050, SR-052, SR-053, SR-054, SR-055, SR-056, SR-071, SR-088, SR-089, SR-090, SR-135 — back to `Verified` under `docs/gate-policy: autonomous`, restoring the derived gate to G3. No human sitting is required.** I am the independent fresh-context reviewer that policy names, and the blessing rests on evidence this record produced, not on the builder's report. The amendment is exactly what the brief shows and nothing more: 135 rows both sides, identical header, **11 changed cells, every one `Status` `Verified`→`Modified`, zero text changes**; 19 LLR `Module`-cell edits with no `Status` and no `CodeSymbol` touched; the flipped set equal in both directions to the set of SRs owning an edited LLR. The amended pointers are **true** — the pre-split and post-split renderers emit byte-identical dashboards over identical inputs — so the evidence that verified these rows still verifies them, which is the whole question a sitting answers. The condition I attached in round 1 is now discharged and I verified the discharge myself: the post-ratification G3 bar is **20/20 PASS, exit 0**, so this ratification no longer lands a G3 claim over a red G3 gate. Ordinary-class rows, mechanized evidence, brief rendered and gated fresh: flip the eleven to `Verified` and merge.

VERDICT: APPROVE findings=0
