# ADJUDICATE — WI-547 — amendment at fb0ed7c

Question judged, per row: did the amendment change the requirement's MEANING, or only its CLARITY?

Every amended cell is a **Rationale** cell. Every change strips (or, at SR-175, restructures in place) the `Hat-derived (hat.X):` provenance label — the roster-lens attribution naming which hat produced the row — while preserving the substantive charter clause (C-SEC-2, C-MNT-7, C-DPR-2, …) and the obligation the row imposes. A builder or test acting on the BEFORE text acts identically on the AFTER text; the capability, actor, scope, threshold and acceptance are untouched. The derivation that survives is the charter reference; only the roster-label wrapper and, in a few rows, its self-referential citation-home sentence are gone.

- [CLARITY] SR-024 Rationale -> generate dimensional coverage from declared inputs; negative tests are a separate obligation -> same -> only the `Hat-derived (hat.TEST-ENGINEER):` label dropped; "systematic expansion reduces the risk…" reads identically.
- [CLARITY] SR-033 Rationale -> generated checklist surfaces warn-tier budgets a human ticks; row is the project's ANSWER, not a charter-imposed obligation -> same -> only the `Hat-derived (hat.PERFORMANCE),` label dropped; the "no wider than what the charter asks" clause is unchanged.
- [CLARITY] SR-043 Rationale -> enumerate the irreversible actor-starting action, naming the human-held override; fail-open arm deliberate -> same -> only the `Hat-derived (hat.SECURITY):` label dropped; "C-SEC-2 asks…" and the fail-open clause survive verbatim.
- [CLARITY] SR-052 Rationale -> keyboard operability, accessible name, colour-not-sole-channel, contrast floor; ACCESSIBILITY charter `always` -> same -> only the `Hat-derived (hat.ACCESSIBILITY):` label dropped.
- [CLARITY] SR-053 Rationale -> one cross-view coherence property, child-chain acceptance; CONSISTENCY charter the sole deriving lens -> same -> only the `Hat-derived (hat.CONSISTENCY):` label dropped.
- [CLARITY] SR-054 Rationale -> task-level findability + legibility-as-robustness; both lenses `always`, reachable today -> same -> only the `Hat-derived (hat.UX-DESIGNER + hat.UX-ENGINEER):` label dropped.
- [CLARITY] SR-111 Rationale -> recorded-origin stamp lets a maintainer identify upstream version and compute a re-sync diff -> same -> the trailing `Hat-derived (hat.MAINTAINER): C-MNT-7…` sentences (charter answered + citation-home + "stands without the citation") removed; those were provenance meta, not the stamp obligation.
- [CLARITY] SR-112 Rationale -> generated per-agent skill copies, drift is a finding, generated-marking stops editing a disposable copy -> same -> the trailing `Hat-derived (hat.MAINTAINER):` sentences and "Same disposition as SR-111" citation removed; obligation untouched.
- [CLARITY] SR-129 Rationale -> capability is the converter as check on a representation change; cell-exact round-trip over the live registry -> same -> only the `Hat-derived (hat.TEST-ENGINEER):` label dropped; "A converter is the check…" reads identically.
- [CLARITY] SR-144 Rationale -> immutable per-close record; split is a field; dead-lane claim never released -> same -> only the `Hat-derived (hat.UNATTENDED-OPS):` label dropped; the C-UNA-3/C-UNA-5 clause survives.
- [CLARITY] SR-146 Rationale -> dispatch content carries a DECLARED inclusion rule; reviewable file with digest -> same -> only the `Hat-derived (hat.SECURITY):` label dropped; "C-SEC-5 requires…" survives.
- [CLARITY] SR-147 Rationale -> single structured carrier; converter proves it cell-for-cell BEFORE the authority flip -> same -> only the `Hat-derived (hat.TEST-ENGINEER):` label dropped; the converter-as-evidence clause survives.
- [CLARITY] SR-149 Rationale -> vocabulary conversion is a standing CONDITION with carve-outs (no history rewrite, no re-worded attestation) -> same -> only the `Hat-derived (hat.MAINTAINER):` label dropped; "C-MNT-3 gives every declared vocabulary value one home…" survives.
- [CLARITY] SR-167 Rationale -> perf verdict's both breach arms; names no artifact; comparator only in acceptance; LLR-014/TC-014 re-point still owed -> same -> only the `Hat-derived (hat.PERFORMANCE):` label dropped; "the charter's third clause…" survives.
- [CLARITY] SR-175 Rationale -> declared basis/exclusion for personal data crossing to external providers; pull-channel limit; DATA-PROTECTION lens with SECURITY+LEGAL converging -> same -> the front `Hat-derived (…C-SEC-5…C-LEG-3…):` wrapper and its docs/plans citation restructured into "derived from the data-protection lens, with the security and legal lenses converging…"; three-charters/one-crossing content preserved, obligation untouched.
- [CLARITY] SR-176 Rationale -> matched value never reaches durable storage; deliberate narrowing of C-DPR-2; PII redaction the build debt -> same -> only the front `Hat-derived (hat.DATA-PROTECTION, C-DPR-2 — clause text…):` wrapper dropped; "The finding record is…" survives.
- [CLARITY] SR-177 Rationale -> throughput claim OBSERVABLE not BUDGETED; lanes=1 today; aggregation surface the build gap -> same -> only the front `Hat-derived (hat.PERFORMANCE, C-PRF-1 — clause text…):` wrapper dropped; "The charter refuses a declared budget with no measurement…" survives.

VERDICT: CLARITY rows=17
