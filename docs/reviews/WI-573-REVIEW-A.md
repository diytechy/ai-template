# WI-573 — REVIEW-A rollup

Compiled by the supervising session (2026-09-02) from the round files under
`docs/reviews/wi-573-adjudicate-llr-136-llr-158/`, time-ordered, governing
line last. This is the first amendment adjudication run under the arm WI-572
shipped (the released-rung aftermath). No mechanized round exists for an
adjudication lane (the queued WI-559 defect); both rounds were drawn by the
supervisor through an independent Opus reviewer with a hostile brief.
`001-ADJUDICATE-07cbabb.md` is the adjudicator's own verdict (`VERDICT:
MEANING rows=2`; one sentence re-issued in place at the rework), not a round.

### Supervisor note — NOT a round, does not govern

The adjudication (Opus 5 at the medium tier — the minted row's BuildTier
pinned it below the phase's strong default) ruled both amended rows MEANING
and withheld the re-anchor for a real defect in LLR-158's amended text (a
false statement of the `SPINE_CSVS` / `OUTSIDE_THE_APPROVAL_ACT` partition),
drafting a corrective successor as the template's withhold exception allows.
The lane touched no registry, Status or snapshot path; the dispatcher closed
it mechanically.

### REVIEW-A — Round 2 — supervisor-drawn, independent Opus, hostile brief — tip 446e19e

Both MEANING rulings re-derived and confirmed (LLR-136 gained two binding
obligations; LLR-158 an entire second half); the withhold contract-sanctioned
and its defect reproduced at three lines of `acceptance_record.py`. One
MAJOR: the drafted successor was told to take `intake.py snapshot --approves`
on its own lane — a spine-class worker lane that `lane_approval_refusal`
would refuse at merge, and the authoring lane blessing its own text. Two
MINORs: no fragment or `Deferred open items:` line; one sentence called a
loop-side surface "the owner's brief".
(Full text: `002-REVIEW-A-446e19e-supervisor.md`.)

VERDICT: CHANGES-REQUESTED findings=3

### REVIEW-A — Round 3 — supervisor-drawn verification — tip f784b09

All three closed with corrections re-derived independently: the successor
corrects LLR-158's Detail and code_symbol cells only and takes no snapshot
act; the re-anchor is routed to the amendment adjudication minted at that
successor's merge (a mechanically real route: `staged_spine_amendments` on a
released rung); `parse_dispositions` returns one valid draft; the verdict
file ends in its single MEANING line with the corrected sentence accurate;
the fragment's file-level declaration parses; the diff since the refresh is
four record files only.
(Full text: `003-REVIEW-A-f784b09-supervisor.md`.)

VERDICT: APPROVE findings=0
