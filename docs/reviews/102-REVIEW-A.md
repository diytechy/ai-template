- [MINOR] docs/knowledge/prompt-image-token-efficiency.md:21 -> the pack states "Anthropic uses `ceil(width/28) × ceil(height/28)`" as the provider's method, but Anthropic documents image tokens = (width × height)/750; the 28-grid under-counts (~1,800 vs ~1,867 for 1000×1400) and presents an author approximation as the provider's own formula, in a pack whose whole value is trustworthy durable evidence (the grounding review claimed to have verified "the provider formulas" but let this through) -> label the 28-grid as the pack's own estimation method or use the documented (w×h)/750 formula -> @owner
- [MINOR] docs/knowledge/prompt-image-token-efficiency.md:1 -> this pack is linked to CMP-004 "Unattended loop & floor" (components.csv:5, README.md:14), but its subject (native-text-vs-rasterized-prompt encoding; conclusion "no image renderer justified for this kit") does not inform agent_loop/route/score/subagent_gate/check_privacy, and unlike agent-routing.md the pack body cites no component — so its CMP-004 membership is asserted only in the index/csv, a thin dogfooding link -> add a body line grounding the CMP-004 relationship or re-home the pack to a more apt component -> @owner
- [MINOR] docs/log.md:6864 -> WI-155 close evidence records the full suite as "769 passed, 34 skipped"; a clean run in this session reproduces 800 passed, 3 skipped (same 803 total) — the 31 extra skips are sh/git-gated script-e2e tests (onboard/devsetup, pre-commit-hook, check_privacy), so the loop's close run lacked a POSIX shell/git on PATH and did not exercise ~31 of the "every script end-to-end" tests; systemic (748–769 passed / 34 skipped across ~7 prior sessions) and honestly recorded, harmless for this docs-only WI, but the headline green overstates the close-bar coverage -> note the skipped script-e2e class in the log or run the full bar in an sh/git-equipped env at WI close -> @owner
VERDICT: APPROVE findings=3
