- [MAJOR] project-trajectory/scripts/gen_trajectory.py:1375 -> the new `cedge` glyph is emitted once for every descendable container, even when that container has multiple immediate children; it is a short arrow inside the parent block and terminates at no child, so it does not meet SR-056's and the WI-143 spec's requirement for one horizontal parent-to-child arrow per containment edge -> render a distinct horizontal arrow for every immediate parent-child containment relationship, with its endpoint at the corresponding child (and update the fixture to exercise a container with multiple children) -> @owner
VERDICT: CHANGES-REQUESTED findings=1
