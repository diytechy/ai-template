### REVIEWER — G3 — Round 1 — 2026-07-31
Verdict: CHANGES-REQUESTED
Findings:
- [MAJOR] project-trajectory/README.md:79 -> The `scripts/agent_common.py` kit-contents row still ends "+ the generated run-state write" — a live, present-tense promise of the retired `docs/run-state` mechanism in shipped, adopter-facing kit text, the same defect class as the WI's own defect 2 (a cited mechanism that no longer exists). Driven: `grep -n "run-state\|run_state" project-trajectory/scripts/agent_common.py` finds only the retirement comment at :63-65 ("Their old durable home, the dispatcher-generated `docs/run-state` file, retired with the dispatcher at concurrency-restructure Phase 5 — the stop banner and exit codes carry the outcome now") — no write function of any name remains in the module, and no script anywhere in `project-trajectory/scripts/` writes such a file. The line matches the sweep's own stated pattern (`run-state`) over its own stated domain (`project-trajectory/**/*.md`), so the slice's mandated search should have caught it. -> Delete the "+ the generated run-state write" clause (or re-point it at what agent_common actually provides now — the stop banner is already named earlier in the same row) and re-check the row against the module's real exports. -> @owner
- [MINOR] project-trajectory/skills/session-protocol/SKILL.md:96-99 -> "With the parallel-dispatch scheduler, WI ordering is derived from the registry (the DAG + `Priority` + gate class), not a hand-curated `docs/next-wi` ... set `BuildTier` deliberately ... — the dispatcher reads it from the WI row" is live, present-tense guidance naming the deleted dispatcher twice as the acting mechanism. The real readers today: ordering is `schedule.py`'s frontier (drive.py:245-246 re-derives it per cycle), and BuildTier is pinned by the WORKER from its assigned WI row (agent_loop.py:1768-1776), not by any dispatcher. `scope: this-repo` (SKILL.md frontmatter), so not adopter-shipped — hence MINOR — but it sits squarely inside the sweep's declared domain (all `project-trajectory/**/*.md`, `dispatcher` pattern) and misdirects this repo's own sessions to a module deleted at Phase 5. -> Reword to name the live mechanisms (`schedule.py` for ordering; "the worker pins its BUILD tier from the WI row") and re-sync the per-agent copies (`bootstrap.py --sync` / the skills-sync step). -> @owner
- [MAJOR] docs/log.d/WI-375-unattended-text-truth.md:32-37 (repeated at docs/work/archive/WI-375-unattended-operation-text-matches-ship.md:13) -> The signed sweep claim — "Remaining mentions are all historical retirement records ... none promises a retired mechanism as live" / "Sweep performed over all kit markdown: every remaining dispatcher/run-* mention is a historical retirement record, not a live promise" — is refuted by the two findings above: both hits match the fragment's OWN stated pattern (`run-state` at README.md:79, `dispatcher` at session-protocol/SKILL.md:98) over its OWN stated domain (`project-trajectory/**/*.md`), so the recorded sweep either was not run as described or its hits were not judged. This fails the spec's Done-when item 3 verbatim ("Any other kit text promising the retired dispatcher's behaviour is found and corrected — the search is part of the slice, not an assumption that these two sites are all of them") and is this repo's named recurring defect: a signed claim that passes every mechanical check. -> Fix the two sites, re-run the sweep, and re-state the sentence in both the fragment and the archived Deliverable to what a rerun actually shows. -> @owner
VERDICT: CHANGES-REQUESTED findings=3

Driven green (for the record, so the fix round can delta-scope): (a) entry point real — bootstrapped a fresh scaffold in a tmp git repo, plain `agent_loop.py --root .` with `AGENT_CMD` wired and an empty registry → "drive: queue drained - no ready work items; 0 WI(s) integrated this run." exit **0**; the no-role branch (agent_loop.py:2669-2670) really delegates to drive.py, which re-derives the frontier every cycle (drive.py:245-246), resumes parked claims (:73-91, :224-233), refuses stranded claims by name (:194-211), stops on every composed refusal (:277-280, :381-384), and has no push path. (b) Capability row (PROCESS_OPTIONS.md:29): `agent_loop.py`, `drive.py`, `integrate.py`, `agents.template.csv`, the three `agent-resume.template.*` launchers all exist; `ls docs/run-*` → No such file or directory. (c) ADOPTING recipe claims verified: `wi_convert.py` really ships `--verify` and `--to-specs` (wi_convert.py:677-690), `--interactive` exists (agent_loop.py:1237), the claim rung really refuses non-`ordinary` SafetyClass (integrate.py:245-249), `docs/parallel-ready` is read by no script (grep over `project-trajectory/scripts/` = 0 code hits), `stack.ini.template:104` carries the `[generated]` default set and integrate.py:804-822 reads it. (d) Byte budgets: measured PROCESS.md **64,319**, PROCESS_OPTIONS.md **163,157**, ADOPTING.md **55,509**, AGENTS.template.md 9,991 (<10,000) — all three logged deltas exact, both re-stamped baselines match reality. (e) Harness: `check.py --run-steps skills-sync` → OK, 12 copies match source; configured `check_docs.py --stale` → OK, 327 docs, 933 links, **0 broken**; `check_trajectory.py --strict` → clean (375 WIs, pre-existing WARNs only); archived spec's banner verified honest (WI-374 spec in `docs/work/archive/`, WI-374 in `docs/log.md`, no live citer of the old `docs/specs/unattended-entry-point.md` path).

### REVIEWER — G3 — Round 2 — 2026-07-31
Verdict: APPROVE
Findings:
- none — the fix commit 5a21f64 closes all three round-1 findings, delta-scoped and re-driven. (a) Both fixes accurate against the code: the README agent_common row now states the retirement as history ("(The generated run-state write retired with the dispatcher at concurrency-restructure Phase 5.)"), which matches the module's own comment (agent_common.py:63-65) and the round-1 grep showing no write function survives; the session-protocol bullet's two new attributions both verify — `schedule.py` really derives ordering from the DAG + Priority + gate class (schedule.py:21 sort key "(gate class, Priority desc, transitive downstream-dependent count desc, ...)", :76 lowest-gate-first rank, :371 Priority read) and the worker session really reads BuildTier from its WI row (agent_loop.py:1768-1776, verified round 1); per-agent copies re-synced — `check.py --run-steps skills-sync` → OK, 12 copies match source. (b) My sweep re-run with the widened pattern (`docs/run-|run-state|AGENT_JOBS|dispatcher|--jobs|parallel-ready|reservation`, all `project-trajectory/**/*.md`, case-insensitive): every remaining hit re-judged, and the restated result is now TRUE, including its four named categories — historical retirement records (PROCESS_OPTIONS:581/591/2321/2338, ADOPTING:569-588 the migration recipe, downstream-resync:76-96 the retired-surface deletion list) and the dated ADOPTING changelog bullets (ADOPTING:659-660, the WI-266 bullet carrying its own "(Superseded by WI-322 ...)" marker); the live and real `check.py --jobs 0` flag (check.py:1385 defines `--jobs`; session-protocol:74's sentence is true); generic English (EXTERNAL_SKILLS.md:21 "your gate or dispatcher" — the adopter's own); and the dual-plan prompt templates' "DISPATCHER NOTES (delete this block before sending the prompt)" headers (prompts/dual-plan-{planner,critic,arbiter}.template.md:2 — addressed to whoever dispatches the prompt, a role not the retired machinery). No live promise of a retired mechanism remains. (c) Configured `check_docs.py --stale` → OK, 328 doc(s), 933 links, **0 broken**. (d) The refutation record states what happened without softening: the fragment opens "**Round 1 refuted the first signed sweep**", names both surviving sites and their fixes, then restates only the re-run result; the archived Deliverable says "the first signed sweep was REFUTED by review round 1" with both sites named and points at this review file for the round-by-round record — the R-F/finding-lifecycle shape (a refuted claim recorded as refuted, not overwritten). Budgeted files untouched by the fix commit (PROCESS.md/PROCESS_OPTIONS.md not in its stat), so the round-1-verified baselines still hold exactly. -> no remedy owed -> @owner
VERDICT: APPROVE findings=0
