- [RETURN] LLR-207 -> a builder must implement one tree-bound verdict record — the record-path fold, the two peel shapes, the logged-session join, the phase span and count, and the trailer cross-check — that hands the merge gate and the loop's round scheduler the same answer at the same tree -> SR-156 does call for a fail-closed serial verdict rung and IF-175 states the two-shape peel in the same words, so the DESIGN is well-parented and correctly decomposed; TC-205 is its sole verifier -> not ready: two Detail clauses contradict the module they specify, re-driven here and not inherited — `governing_identity` is said to answer "for HEAD or an explicit revision" when the default is the BRANCH TIP and `verdict.py:50` records measuring at `HEAD` as the original two-reader disagreement (`format_branch_trailer`'s docstring at `:738` forbids the word by name), and `governing_rev` is said to walk "until it can peel a verified refresh" when a peel `continue`s (`:467`) and the walk in fact ends where a commit's identity differs from its first parent's (`:473`), so a builder implementing the sentence returns the tip on a no-refresh branch, which is the OI-76 defect the row exists to eliminate; and two obligations the cell states have no detector anywhere — mutating `logged_rounds`' `len(ph) == 1` to last-wins, and deleting `branch_trailers`' carrier check at `:802-803`, each left `134 passed` across `test_verdict_record.py test_integrate_admission.py test_integrate_station.py` (baseline also 134), with `branch_trailers`/`Review-Verdict` appearing in no other test module repo-wide. Its `Component = CMP-008` also falsifies an Approved cell: `CMP-006`'s note calls `kitlib/station.py` "the one package module NOT owned here", true while LLR-182 was the only kitlib module at CMP-008 and false now that this row puts `verdict.py` there too — caused by this row, so it re-points with it.
- [RETURN] TC-205 -> its Method must be a true account of what the suite drives, and its Evidence must reach every obligation LLR-207 states -> it traces to SR-156, LLR-207 and IF-175, and its named tests genuinely do cover the identity fold, the encoding boundary, the trailer grammar, both peel orders and the dial matrix -> not ready: the Method claims "a changed work blob and a changed `docs/work/` spec each fold DIFFERENT", but at `tests/test_verdict_record.py:81-90` `_listing` numbers blobs `{:040x}` so the spec entry's sha ends `0002` and its path holds no `00001` — the `b"00001"` -> `b"00009"` substitution moves only `src/widget.py`, and the second assertion DROPS the spec entry rather than changing it; and the Evidence reaches neither `work_tip` nor `refresh_attestation`'s refusal arms, both named in LLR-207's `CodeSymbol` and argued in its Rationale ("Separate read-only and reset peels protect both contracts") — `refresh_attestation` occurs in `test_verdict_record.py` only at `:608` as a positive assertion, while all 26 occurrences of the refusal and reset-peel arms sit in `tests/test_integrate_station.py`, a module this row does not cite. The two undetected guards above are this row's gap seen from the requirement side.
- [RETURN] LLR-208 -> a builder must make the per-review-scope rollup derived state: parser-defined scopes over every round file, a reserved stem for the flat pre-train layout, collision refused by name, --check answering absent/diverged/EXTRA, the write path pruning the extra, work-branch stand-down, and a file that states it is not the gate's input -> SR-170 asks exactly this (shared derived surfaces are the serial merge step's alone), the row claims no governing verdict and so does not re-create the artifact it retires, and its text reads as a closed obligation with an actor named at every clause -> not ready on ONE ground and it is downward, not textual: the cell's load-bearing claim that the rollup is "regenerated by the trunk step" is the clause that makes SR-170's exclusive-writer contract true here, and nothing verifies it — deleting the whole `("verdict-rollup", _has("docs/reviews"), _cmd("gen_verdict_rollup.py"), ...)` row from `trunk_step.REGEN_STEPS` (grep count 0, syntax checked) left all four of TC-206's cited evidence nodes plus the whole of `tests/test_trunk_step.py` green at `27 passed`. I record that the first adjudication found this row's TEXT ready and I do not disagree with that reading; the method I am given asks whether anything the row says goes unverified, and this does.
- [RETURN] TC-206 -> it must prove the rollup's generation, all three --check answers, the extra-arm's REMEDY clearing the failure, the flat-layout stem, the collision refusal, the honesty sentence, and the declaration/stand-down wiring -> SR-170 and LLR-208 both make the serial-trunk connection the point of the row, and its four cited nodes do cover generation, freshness declaration, commit-floor wiring and the lane skip -> not ready: its Expected asserts the rollup "is written by a regenerator the serial merge step runs", and that is precisely the sentence the mutation above falsifies while every cited node stays green — the Expected claims a verification the Evidence does not deliver.

OUTCOME: RETURN rows=4
