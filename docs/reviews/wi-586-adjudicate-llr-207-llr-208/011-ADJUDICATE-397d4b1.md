- [RETURN] LLR-207 -> a builder must provide one tree-bound verdict definition whose byte-preserving record fold, verified refresh/reset peel, read-only governing walk, logged-session phase join, declared phase span/count, and trailer cross-check give the scheduler and merge gate the same answer -> SR-156 requires the fail-closed serial verdict rung, its siblings leave that decision here, and TC-205 is the sole downward verifier -> not ready: `governing_identity` incorrectly says its default is HEAD instead of the named branch tip, `governing_rev` incorrectly makes a verified refresh the walk terminus instead of a peel followed by continued identity-preserving traversal, and no cited test detects either the ambiguous-two-session-log refusal or rejection of a trailer carried by a different work identity; assigning this second CMP-008 kitlib module also makes CMP-006's Approved "one package module NOT owned here" note false.
- [RETURN] TC-205 -> the test must verify every LLR-207 verdict-record clause and accurately state the exercised fixtures -> its SR-156/LLR-207/IF-175 chain calls for the identity, evidence-provenance, peel, phase, and cross-check cases it largely enumerates -> not ready: its identity Method says the docs/work blob is changed when the cited test drops that entry, its Evidence omits the module that exercises `work_tip` and `refresh_attestation` refusal behavior, and it contains no detector for LLR-207's ambiguous-log or mismatched-carrier guards.
- [RETURN] LLR-208 -> a builder must generate one factual rollup per parser-defined review scope, refuse the reserved-name collision, make check/write agree on absent, divergent, and extra files, prune retired scopes, stand down on work branches, and regenerate the artifact only at the serial trunk step -> SR-170 directly requires trunk-only ownership and the sibling LLRs leave this rollup mechanism distinct, while TC-206 is its sole verifier -> not ready: the load-bearing claim that the trunk step regenerates the rollup is not covered; removing the `verdict-rollup` member from `trunk_step.REGEN_STEPS` leaves every cited TC-206 node and the whole trunk-step module green (20 tests in the re-driven evidence set).
- [RETURN] TC-206 -> the test must prove rollup rendering, every freshness answer and remedy, flat-scope/collision behavior, the non-authority notice, work-branch stand-down, and execution through the serial trunk regenerator -> SR-170 and LLR-208 make that serial-trunk connection the reason this derived artifact is permitted -> not ready: the Evidence proves the generator, freshness declaration/enforcer, commit-floor wiring, and branch skip, but never proves that the serial trunk regeneration path contains and executes the rollup step, so its Expected claims more than its tests establish.
OUTCOME: RETURN rows=4
