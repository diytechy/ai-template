- [RETURN] LLR-207 -> define the one tree-bound verdict record shared by the merge gate and review scheduler, including safe refresh peeling, logged-review provenance, phase accounting, and carrier verification -> SR-156 calls for a fail-closed serial verdict seam, IF-175 confirms the two distinct peel contracts, and TC-205 is this row's sole claimed verification -> not ready because its Detail wrongly permits `HEAD`, falsely says the identity-preserving walk ends when it can peel a refresh, and states multi-log ambiguity, carrier verification, and reset-peel behavior that TC-205's cited evidence does not fully test
- [RETURN] TC-205 -> demonstrate both answers of the verdict identity/evidence rules so the scheduler's owed phases and the merge gate's verdict agree on one governing tree -> it traces to SR-156, LLR-207, and IF-175 and its named tests exercise most identity, provenance, phase, trailer, and refresh cases -> not ready because no cited test covers the ambiguous two-session-log refusal or a trailer carried by a work-changing commit, its Evidence omits the station tests that exercise `work_tip` and refresh-attestation refusal arms, and its Method calls removal of a docs/work entry a changed spec blob
- [RETURN] LLR-208 -> define the review-scope rollup as trunk-owned derived state whose parser-defined scopes, collision refusal, stale/extra detection, pruning, work-branch stand-down, and non-authoritative wording stay coherent -> SR-170 requires derived shared surfaces to have the serial merge step as exclusive writer, while TC-206 is the only child claiming to verify this rollup -> not ready because the load-bearing claim that the trunk regeneration path runs the rollup remains unprotected by TC-206's cited tests
- [RETURN] TC-206 -> prove the rollup generator's output and all freshness outcomes, plus its declaration, commit-floor enforcement, serial-trunk regeneration, and work-branch stand-down -> SR-170 and LLR-208 make the trunk-step connection essential, and the cited tests cover generation, freshness wiring, and lane skipping -> not ready because removing `verdict-rollup` from `trunk_step.REGEN_STEPS` leaves every cited evidence node green, so its Expected claim that the serial merge step runs the regenerator is not verified
OUTCOME: RETURN rows=4
