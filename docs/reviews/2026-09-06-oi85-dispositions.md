# OI-85 continuation review dispositions

Subject: Fable's repair commits through `1e78ada3`, the accepted OI-85(a) need
act, and the independently justified P9R/H2/H3/H5 implementation. This records
review decisions; it does not approve Drafted artifacts or start the control.

| Review | Finding and disposition |
|---|---|
| [Runtime review](2026-09-06-oi85-runtime-structured-opus.md) | Backslashes were folded on POSIX by the composer but treated as filename bytes by R-E. The common carrier helper now uses native path semantics. Ordinary and repeated `./` spelling has explicit coverage. |
| [Runtime closure](2026-09-06-oi85-runtime-closure-structured-opus.md) | APPROVE with residual path/test observations. Root reproduced whitespace/lexical-component omissions as three failing cases and fixed them at the shared helper. Drive, worker and interactive main paths refuse unavailable launch identity before setup; malformed needs still fail closed. No extra wrapper or generic registry inference was retained. |
| Independent P9R source review by Sol | Restore `_stage_facts` as a stage-only read; restore root shared render inputs beside the package/facade in both critique-staleness layouts. Both fixed with focused regressions. The optional `process_panel` snapshot argument already preserves its three-argument caller. |
| [Record Critique](2026-09-06-oi85-record-critique-structured-opus.md) | F1: name actual requirement counterparts through IF-011→LLR-035→SR-168/SR-070, without inventing direct SR interface fields. F2: attended identity/return path added to the actual prompt. F3: stopping reason cites existing Inspection LLR-exemption and required TC rules. F4: new reviewer paragraph follows the existing unwrapped convention. |
| [Record closure](2026-09-06-oi85-record-closure-structured-opus.md) | APPROVE, zero findings, with explicit G1–G5 judgments against the SN/SR-derived rubric. The reviewer treats existing SR-161 machine output as unimplemented, not implicitly passed. This is Critique of guidance and recorded reasoning; the separate manual Inspection reads this actual record. |
| [P9R/selector review](2026-09-06-oi85-p9r-selector-structured-opus.md) | Confirmed: selected data/source changes owe secondary generated-output freshness too. Extend through existing freshness commands and verify stale-output refusal. Rejected subpoint: authored `components.toml` exists and is a real generator input; its derived counterpart belongs to generated companions. Closure is recorded after the repair. |

The complete HTML-family run also exposed two preservation gaps: the SVG
radius source scan had not followed the moved emitters, and the package
initializer/shared display reader were missing from component attribution.
The test now scans the actual package without weakening the radius invariant;
LLR-035 names both modules under its existing dashboard responsibility.
Both failed cases then passed. Formatting the moved panel module changes no
behavior. Full-suite and commit-bar results belong to the
[continuation log](../log.d/2026-09-06-oi85-plan-completion.md).

The correct continuation is the repaired current runner, not an assumed
replacement. The [control preflight](../ai-template-redesign-2026-09-05-codex/CONTROL-PREFLIGHT.md)
identifies missing route-complete spend reservations and the explicit reviewed
pause-deletion launch act. No review turns unknown costs into zero, changes the
owner's limits, absorbs unrelated snapshot drift, or retires supported APIs.

## Inspection findings and source binding

The [fresh document Inspection](2026-09-06-oi85-inspection-structured-opus.md)
reports TC-209 and TC-210 PASS, and TC-211 INCOMPLETE. The normal proportionality
sample lacks the applicable SR-161 perspective record; that existing
implementation gap stays visible in the procedure's result. It is not repaired
by weakening the procedure, manufacturing a record from Hat-Refs, or claiming
a prose review executed a missing machine producer.

The inspector's other observations have these dispositions:

- **Subject version:** the claimed mismatch is disproved by the captured
  Critique packet: it supplies DECOMPOSITION-AMENDMENTS.md at SHA256
  `dedff3e3672fa0178ec4a2c317beb02850a34327ecbc3de3d5212ee0a93e115d`, exactly
  the version supplied to the inspector. The paragraph about a prior closure
  is historical, not evidence of a later self-review. The outer subject digest
  is the assembled prompt digest; artifact digests occur inside that packet.
  This record preserves the important artifact binding explicitly.
- **Already-public status behavior:** the separate P9R source review reads the
  facade, current source diff and compatibility cases, and confirms the
  preserved command and 0/1 behavior. The document Critique explicitly deferred
  that source judgment to this separate review, which is now linked from the
  Inspection result. Root also verifies the two formerly failing HTML cases
  and the complete family in the unfiltered suite; the document inspector did
  not run code.
- **Provider session ID:** no provider ID was supplied to the critic's prompt;
  the CLI assigned one while running and the launcher recorded it afterward.
  Those are different information boundaries. The linked invocation carries
  `a480da05-19ab-4d20-b6e8-396467f01a3d`; the critic correctly did not invent it.
  The reviewer declaration and subsequent telemetry do not contradict each
  other.
- **Usability anchor:** the full SN-012 need and rationale were supplied to the
  Critique, together with SR-186's delivered-process obligation. The inspector
  did not receive that full need cell. Its packet limit does not establish an
  underived anchor. The Critique's derivation and zero-finding anchor judgment
  remain available in its record; no rubric clause is removed to obtain a pass.

[Selector closure](2026-09-06-oi85-p9r-selector-closure-structured-opus.md)
returned APPROVE. The final deletion observation was then closed by broadening
when a generated companion is absent, so deleting CURRENT cannot narrow past
its vacuous freshness check. The final selector tests pass; optional OKF is
currently disabled and absent, and enabling it requires revisiting this
meta-repo's explicit selection table. This is local assistance, not an adopter
profile engine.
