### REVIEWER — G3 — Round 1 — 2026-07-31
Verdict: APPROVE
Findings:
- none — every claim in the replacement bullet was driven against its source and held; the diff is exactly the WI's scope; both mechanical bars and the smoke tier were re-run in the worktree and match the log fragment's signed record. -> no remedy owed -> @owner
VERDICT: APPROVE findings=0

Driven green (the record):
- (a) **Bullet claims vs reality, one by one.** MULTI_REPO.md §3.3 really mandates repo-local id spaces + the `CIF-###` handle (MULTI_REPO.md:117-127: "Each repo owns its **own** `IF-###` space, so `IF-001` in two different module repos are *different* interfaces that merely share a string ... the `CIF-###` is the one stable, global handle"). trace.py really anchors the IF-ID column: `^IF-\d+$` is defined at trace.py:239-241 and enforced on `interfaces.csv` via `integrity_findings("IF", raw_ifs)` (trace.py:2167) whose `pattern.match` (:461-462) reports any qualified form (`repo:IF-007`) as malformed — the bullet's "rejects any qualified form there" is literally what the code does. The "no tool validates the far side" caveat is true: trace.py's `interface_findings` (:644-672) checks only the local SR-Refs back-link plus a warn-only *ThisProject* module join and never reads `Counterpart`; gen_arch_map.py reads `Counterpart` only to draw an edge when it resolves to a *known local module* and silently skips it otherwise (gen_arch_map.py:589-591); check_trajectory's component-coupling rule joins `(ThisProject, Counterpart)` against the local arch-map only (check_trajectory.py:1142-1159). No script resolves a counterpart repo, its cited IF id, or its version. The worked snippet really shows different local ids with the far id cited in the Contract cell (INTERFACES.template.md:79-80: `IF-001` Provides / `IF-002` Consumes, "depends on IF-001 v1 schema"), so "exactly as the snippet below shows" is accurate — the snippet was unchanged and needed no change. `REPO-###` is a real registry tier (trace.py:224-226).
- (b) **No new contradiction introduced.** Same file: "One contract, one home ... the consuming side links it by `IF-ID`" (:47-49) is the citation reading the new bullet formalizes, and the intro's "one stable id, one home" (:12) reads as the owner-side authoritative id — consistent. PROCESS.md §8 (:858-872, "the consuming side links the same `IF-###` and pins the version") is the same link-by-citation phrasing, not an identical-row-id mandate; EXAMPLE.md's multi-repo note states the repo-local rule outright (:366-373, "These `IF-###` ids are **owner-local** — each repo has its own `IF-001…`"), and its intra-repo snippet (:283-285) uses different ids in one shared registry. One sub-finding-level note, recorded not charged: the bullet paraphrases §3.3's "the moment the *coordinator* referenced both" as "the moment *anything* referenced both" — a generalization, and a true one (any single referencer of both ends hits the same collision).
- (c) **Scope exact.** `git diff HEAD~1 --stat` = 3 files: the one template bullet replaced (INTERFACES.template.md:54-67, -3/+14 lines, nothing else in the file touched), the new log fragment `docs/log.d/WI-376-cross-repo-if-id-rule.md`, and the spec's active→archive move gaining its `## Deliverable` and dropping `specref` — the same close shape as WI-374/WI-375's archived specs. No schema, script, registry, or budgeted-file change (PROCESS.md / PROCESS_OPTIONS.md / AGENTS.template.md untouched).
- (d) **Mechanical bars re-run in the worktree** (`c:\Projects\ai-template-wi376`, main repo's .venv): `check_docs.py --root . --ignore docs/test/report.md --ignore "docs/work/*" --stale` → OK, 328 doc(s), 933 intra-repo link(s), **0 broken** (4 pre-existing stale *hints* only). `check_trajectory.py --strict` → **clean** (375 work item(s), 358 done, graph acyclic; pre-existing WARNs only). The archived spec parses through the kit's own reader (`agent_common.read_spec_rows` over `docs/work`, zero on_error callbacks): WI-376 Status=`done`, Deliverable non-empty — R-A satisfied.
- (e) **Log fragment judged claim by claim** (docs/log.d/WI-376-cross-repo-if-id-rule.md): the contradiction it narrates is real (the removed bullet said "Both sides reference the same `IF-ID`. Use identical ids across repos" — HEAD~1 verbatim); the §3.3 / EXAMPLE.md attributions check out per (a)/(b); "Not changed" matches the diff stat exactly; the field motivation restates the WI title's own record. Its bar claim "smoke standing-red only + check_docs OK" was **re-driven, not trusted**: `python -m pytest -q -n auto -m smoke` in the worktree → **1 failed, 551 passed, 4 skipped**, the single red being `tests/test_check_lane.py::test_this_repo_is_not_a_work_branch` — the documented work-branch standing red (check.py:1095-1116's WI-357 history rule makes any claimed branch name read as a work lane forever; the test asserts the kit's checkout is not one, which is false in every WI worktree by construction and unrelated to this change). check_docs OK per (d). "No budgeted file touched" — true per (c).
VERDICT: APPROVE findings=0
