### REVIEWER — G3 — Round 1 — 2026-07-30
Verdict: APPROVE
Findings:
- [MINOR] tests/test_gen_trajectory.py:4757 -> The routed-level harness pin covers only a 2-wire port (and with 16.0 px slack against the spec's ~15), while the `_LEAD_RUNGS` collapse — where the recorded residue actually lives (the 5-wire `unphased` port: I measure its tightest pairs first 8 px clear at ~15.6/17.1/21.3 px, worst ~40.1) — is pinned only at the `_lead_rung` unit level, so a future change to `_harness_ends`' rung mirroring that re-fuses a wide fan's OUTER pairs would pass the whole suite. Non-blocking: the population-level claim is deliberately unpinned (no-crossing-count-proxy standing rule), and a fixed-input geometry pin is not that proxy. -> Add one routed 5-wire case beside it asserting every pair of the fan reaches `_FAN_PITCH` clear of its partner within 60 px (pure geometry on a constructed edge set, like the 2-wire case). -> @owner
- [MINOR] docs/work/archive/WI-366-spread-the-port-fans-the-critique-mea.md:41 -> The measured table's pair-clearance numbers are convention-sensitive and the convention is unstated: an independent stdlib reproduction on the emitted SVGs (first arclength at which a strand is 8 px clear of its partner polyline, required in BOTH directions) gives 119 -> 69 of the same 271 pairs against the recorded 118 -> 62 (one-direction: 59), and the `unphased` tightest adjacent pairs at 15.6/17.1 px against the recorded 16.9/18.9 — same conclusions everywhere, but the exact figures move a few percent with direction and sampling choices, and the periodic advisory critique is named as the judge of this residue. Non-blocking: every headline claim reproduced within tolerance, including the block-1 fused pair (0.25/0.14/0.57/2.11 before -> 5.98/8.05/9.53/7.48 after, my measurement). -> One sentence in the Deliverable stating the measure (along-which-strand arclength, one- or two-direction, sampling step) so the critique can reproduce the numbers exactly. -> @owner
VERDICT: APPROVE findings=2
