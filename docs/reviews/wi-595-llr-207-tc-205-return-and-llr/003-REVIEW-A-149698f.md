## 2026-09-04 — Independent review, WI-595

Evidence: `python3 project-trajectory/scripts/check.py --jobs 0` reported `RESULT: PASS`; `python3 project-trajectory/scripts/trace.py --strict-integrity` ended `Traceability: SN=27 SR=76 LLR=191 TC=190 orphans=0 integrity=0 ...`. A real empty, correctly titled mechanical-close commit returned `empty-close-attestation= None`. Targeted pytest execution was unavailable because `/usr/local/bin/python3: No module named pytest`.

- [MAJOR] docs/test/test-cases.toml:2083 -> LLR-207 newly makes a non-empty changed-path set an admission condition, but its only three mechanical-close evidence tests cover the positive, forged subject, and outside-`docs/work/` path; no regression would catch an empty correctly titled close becoming peelable, which would let it preserve an earlier approval -> add and cite a real-git empty-close refusal test that asserts the merge gate asks for review -> @owner; construction cannot make this bad Git-history shape unrepresentable because commits are untyped external input, and `mechanical_close_attestation` is already the single validation boundary.

VERDICT: CHANGES-REQUESTED findings=1
