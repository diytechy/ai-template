- [MAJOR] project-trajectory/hooks/commit-msg:47 -> `Blocked-WI: not-a-wi` is accepted as a sanctioned disposition, and `agent_dispatch._substantive_tip()` likewise excludes any nonempty `Blocked-WI:` value; a real train with an approved older head then integrated a newer unreviewed `Blocked-WI: not-a-wi` commit with no `reviewed-head-trailer-slip` event -> require each exempt `Blocked-WI:` value to be a parseable `WI-<n>` in both the hook and diagnostic (with one shared/identical grammar), and add an integration regression that invalid blocked evidence cannot hide a newer build commit -> @owner
- [MINOR] tests/test_commit_msg_wi_trailer.py:204 -> the required linked-worktree regression is skipped on this Windows checkout because `_sh_or_skip()` cannot find `sh`, even though Git for Windows successfully runs the hook through real `git commit`; the test itself only drives `git commit` and does not need a shell executable -> remove that pre-skip or locate Git's bundled shell so the shared-`core.hooksPath` worktree contract is actually exercised on Windows -> @owner
VERDICT: CHANGES-REQUESTED findings=2
