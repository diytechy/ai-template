- [MAJOR] project-trajectory/scripts/agent_dispatch.py:822 -> the backstop is fail-open when a train's first/only substantive build commit bypasses the hook: `reviewed_train_head()` returns `None`, `_substantive_tip()` returns that untrailered tip, and the `reviewed` truthiness guard emits no `reviewed-head-trailer-slip` event; this is precisely the pre-floor/`--no-verify` path the new diagnostic claims to surface -> journal the mismatch whenever a substantive tip differs from the resolved reviewed head, including an explicit missing-reviewed-head value, and add the no-valid-WI-trailer train regression -> @owner
- [MINOR] tests/test_commit_msg_wi_trailer.py:145 -> the required linked-worktree hook case is uncovered: the claimed end-to-end test invokes the hook script directly in one scaffold worktree and never configures `core.hooksPath`, creates a linked worktree, or runs `git commit`; the shipped hook happened to pass my real linked-worktree probe, but that contract has no regression guard -> add a real `git worktree add` fixture with the shared hooks path and assert malformed, valid, and sanctioned commits through `git commit` -> @owner
- [MINOR] docs/specs/WI-282.md:56 -> the diff marks the close criterion complete although the scoped registry row is still `queued` and still references this live, unarchived spec; the independently run strict trajectory check likewise warns that the amended SpecRef was not re-affirmed -> leave this Done-when item unchecked until the integrator's done-flip/archive commit, or perform that close ritual in the appropriate integration commit -> @owner
VERDICT: CHANGES-REQUESTED findings=3
