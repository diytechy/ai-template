### REVIEWER — G3 — Round 1 — 2026-08-01
Verdict: CHANGES-REQUESTED
Findings:
- [MAJOR] project-trajectory/scripts/integrate.py:422 -> The attestation binds NOTHING to the tree it names, so the one constraint's second half is satisfiable by a string. `bar_green_attestation` accepts any commit-message line starting with `Bar-Green:`, and the value it returns (`bar PASS (N steps, tier T) trunk=<sha10>`) carries no tree or commit identity, so `_merge_ready` (:934) proves only "some line on this tip says the word". Driven twice on constructed topologies. (a) FORGERY: an ordinary work commit `WI-401: a perfectly ordinary work commit` whose body carries `Bar-Green: bar PASS (2 steps, tier all) trunk=deadbeef00` makes `_merge_ready` return `(True, 'bar PASS (2 steps, tier all) trunk=deadbeef00')`; the real `integrate --tier smoke` then exits 0, merges, and lands `sneaky.txt` on trunk — with the recording stub harness never invoked at all (`harness-order.txt` absent, `_order(wt) == []`). (b) AMEND: after a genuine green refresh, `git commit -q --amend --no-edit` with an extra file staged keeps the message while the tree moves `6358baecea…` -> `cc45882f56…`; `_merge_ready` still says ready, `integrate` exits 0, and `amended-in.txt` reaches trunk with the bar not re-run (harness order unchanged). So the docstring's "the bar is attested to a TREE, not to a run … a property of X that git adjudicates" (:70-72) and the fragment's "a later commit on the branch revokes it" describe a binding that does not exist — git adjudicates only that the *message* is attached, and amend/rebase/cherry-pick all carry the message onto a different tree. This is the whole mechanical bar in the loop, and the composed-tree bar it replaced could not be satisfied this way. -> Bind the trailer to what it attests: emit `Bar-Green: <summary> tree=<git rev-parse HEAD^{tree}>` in the refresh commit and have `_merge_ready` compare it against `git rev-parse <branch>^{tree}`, refusing on mismatch. That is two lines, kills every accidental carry-over (amend, rebase, cherry-pick, a copied message) and makes the sentence in the docstring true. -> @owner
- [MAJOR] project-trajectory/scripts/integrate.py:437 -> `_work_tip` peels on the SAME unbound token, and `refresh` then `reset --hard`s to what it peeled — so a committed work commit can be silently destroyed. The loop peels while `bar_green_attestation(rev) is not None`, up to `_MAX_REFRESH_PEEL`, with no other test that the commit is a refresh. Driven: on a closed `wi-401`, a commit `WI-401: late fix` carrying `Bar-Green: I ran it locally, honest` is peeled (`_work_tip` returns the earlier honest tip `eb4f7573…`, not the real tip `4577ab28…`); the next `refresh` resets to it and the branch's tracked file list after the refresh is `['.gitignore','PROJECT_STATE.html','docs/review-policy','docs/stack.ini','docs/work/archive/WI-401-widget.md','regenerated.txt','scripts/check.py','scripts/trunk_step.py','seed.txt','wi-401.txt']` — `late.txt` is gone from the branch and never reaches trunk. The same peel also removes that commit from `_verdict_gate`'s `code_time` measurement, so the identical token both hides code from the review-freshness rule and admits it to the slot. (The gate itself is sound without the token: an ordinary post-review commit is still caught — driven, `docs/reviews/WI-401-REVIEW-A.md predates the branch's last code commit`.) -> Identify a refresh commit structurally, not by a substring: require the two-parent shape AND the `refresh: <branch> onto trunk` subject AND (with the fix above) a `tree=` value equal to that commit's own tree, before `_work_tip` will peel it or `_merge_ready` will honour it. -> @owner
- [MAJOR] project-trajectory/scripts/integrate.py:782 -> `_shed_residue` does not do its job in the flow it was added for, because git reports ignored DIRECTORIES as one collapsed line. Deviation 3 justifies the machinery by measurement ("the first green e2e run exited nonzero on it") and promises "the refresh deletes what its own bar added"; the baseline diff is line-equality against `git status --porcelain --ignored=matching`, so if the residue directory already exists the post-bar line is byte-identical to the baseline line and the whole directory is skipped. Driven: with `bar-cache/worker-run.txt` created in the lane worktree BEFORE the run, `BASELINE DIRT: ['!! bar-cache/']`; the real `integrate --tier smoke` then merges green and still exits **1** — `UNLOAD INCOMPLETE … DIRTY (1 uncommitted or ignored path(s)) … !! bar-cache/`, `STILL HELD - wi-401`, `INCOMPLETE - 1 merged branch(es) NOT unloaded` — and the refresh's own `bar-cache/run.txt` is still on disk. This is the normal case, not an exotic one: `drive._default_worker` and `refresh` share ONE lane worktree (`integrate.lane_worktree`), the worker runs the bar there before it closes, and the shipped `project-trajectory/gitignore.template` ignores `__pycache__/`, `.pytest_cache/` and `.ruff_cache/` as directories. The e2e that measured it green uses a lane worktree no worker ever built in, which is the one shape where the fix works. -> Compare the baseline as a PATH SET expanded past collapsed directory lines (`git status --porcelain --ignored=matching --untracked-files=all` / an explicit walk), so a directory that predates the refresh still has the files the refresh's own bar added removed from it — or, if that is judged too sharp a knife, drop `_shed_residue` and record honestly that the unload reports the lane's tool residue as dirt. Either way the current form is machinery that does not deliver its stated measurement. -> @owner
- [MINOR] project-trajectory/scripts/check.py:1057 -> The recorded justification for the one new flag states the wrong failure direction, in four places at once. "Skipping them there would let the only mechanical bar in the loop pass over the artifacts it had just written" (repeated at docs/log.d/WI-386-station-protocol.md:56, docs/work/archive/WI-386-station-protocol-refresh-before-merge.md:49, and tests/test_module_size_ratchet.py:598 as the reviewed-bump reason). Driven on the REAL scaffold fixture with `--trunk-lane` stripped from `_run_bar`'s argv: the refresh does not pass over anything — it goes RED, `refresh REFUSED … the bar is RED on the refreshed tree (bar reported SKIP - a skip is a refusal here: SKIP arch-map / status-map / derived-gate / open-items / trajectory-map / okf / ratify-fresh …)`, because `_run_bar` (:647) already treats any SKIP as a refusal. So the flag is genuinely load-bearing — without it the refresh can never succeed — but the hole it is said to close was already closed fail-closed, and the ratchet's own reviewed-bump reason for overriding a fail-closed rule rests on a mechanism that does not exist. The deviation's CONCLUSION (opt-in, a forgetful caller gets the stricter answer) is correct and re-driven: the flag is off by default, reaches the runner through the real CLI, and restores exactly `_TRUNK_FRESHNESS_STEPS` — the same seven the deleted candidate-tree bar ran, so the bar is equivalent, not weaker. -> Re-state the reason once and propagate: without `--trunk-lane` the refresh bar SKIPs the seven freshness steps and `_run_bar` reads any SKIP as a refusal, so the refresh could never go green — the flag makes the mechanical bar possible, it does not rescue it from a false pass. -> @owner
- [MINOR] tests/test_integrate.py:1263 -> `test_slot_acquisition_has_exactly_one_call_site` guards the literal `acquire_lock(` and not the requirement. It CAN fail — mutant `def _second_slot(root): return ac.acquire_lock(root / "out" / "other.lock")` reds it (`assert 2 == 1`, both lines named) — so it is not a guard that cannot fail. But the owner's requirement is that SLOT ACQUISITION has one call site, and a second acquisition through the existing helper slips past: mutant `_extra = _slot(root)` inserted at the top of `integrate_one` gives `1 passed in 0.08s` while making "restricting to pessimistic is one call moved" false. Requirement 2 itself re-drove clean and the builder's claim HOLDS: deleting exactly `_drain`'s five-line refresh loop in drive.py and changing nothing else leaves the queue working end to end against the real bar (`is not merge-ready … the pessimistic sequence` -> `refreshed … bar PASS (17 steps, tier smoke)` -> `merged` -> `unloaded` -> `audit clean`); the only casualty in the whole suite is `test_drive_end_to_end_claims_builds_merges_and_drains`, which asserts the speculative path was taken — correct and expected. -> Add one assertion to the same test: `_slot(` occurs exactly twice in the source (the definition and its single call), so a second acquisition by either route reds. -> @owner
- [MINOR] project-trajectory/scripts/integrate.py:871 -> `refresh` resolves trunk as `_head(root)`, which is not trunk in a configuration `lane_worktree` explicitly supports. Its docstring (:498-506) says a registered holder wins "and, in attended serial work, possibly the MAIN checkout" — but when the main checkout holds the branch, `wt is root` and `_head(root)` is the BRANCH tip. Driven: main checkout switched to `wi-401` with real trunk at `d0e1ce386f`, `integrate.py refresh` prints `integrate: refreshed wi-401 onto trunk 6430dad86c` (that sha IS the branch), writes a `Bar-Green: bar PASS (2 steps, tier smoke) trunk=6430dad86c` trailer, and `trunk-moved.txt` is absent from the branch — the trunk was never merged in. Contained (a later `integrate` from a checkout on trunk finds the ancestor relation false and re-refreshes in-slot, and `finished_branches` cannot see the branch from that checkout at all), so nothing reaches trunk wrongly; what ships is a false operator message and a false attestation from a supported path. -> Refuse in `refresh` when the resolved lane worktree is the primary checkout and its HEAD is the branch being refreshed — "switch the main checkout off <branch> first" — or resolve trunk from `ac.primary_worktree_root`'s recorded trunk rather than from whatever HEAD happens to be. -> @owner
- [MINOR] docs/log.d/WI-386-station-protocol.md:74 -> Deviation 6 understates what it hands to WI-390, and nothing mechanical will catch the gap. It records that "SR-132's description … is now false, and LLR-140/LLR-141's Notes are out of date". Re-read: LLR-140's **Detail** (docs/okf/low-level-requirements/LLR-140.md:12) is actively FALSE, not out of date — it specifies "integrate_one: --no-ff --no-commit merge, trunk_step.py folded into the merge commit …, check.py --jobs 0 at the declared tier on the composed tree read fail-closed …, ff-only trunk advance, branch deleted on green, candidate parked on red", every clause of which this WI deleted; and IF-080's **Contract** (docs/okf/interfaces/IF-080.md:12) is false in the same way and additionally omits the new `refresh` operation entirely, while §A9.1 item 2 scopes the interface registry only to the connectivity drift. `check_trajectory --root . --strict` exits 0 over all of it (re-driven: rc=0, 388 items, only the pre-existing IF-055/080/081 and drive/traj_* connectivity WARNs), so the false records are invisible to the harness until a human reads them. The owner's ruling (spine work waits, batches, runs alone) is not in question — the record of the debt is. -> Amend the fragment to name LLR-140's Detail and IF-080's Contract as FALSE (with the specific clauses) alongside SR-132, so WI-390 inherits the real scope rather than "Notes are out of date". -> @owner
- [MINOR] project-trajectory/scripts/drive.py:195 -> A genuine trunk-merge conflict has no forward path, and nothing records that it happened. Driven two ways. In-slot: `integrate --tier smoke` on a branch whose `contested.txt` collides with trunk exits 1 with `REFUSED - refresh REFUSED for wi-401 - merging trunk d494794fab in CONFLICTS; resolve it on the branch in <lane worktree> (git merge d494794fab), commit, then refresh again` — the branch is left at its work commit, `git status --porcelain` empty, `MERGE_HEAD` gone, so the failure-path claim HOLDS (also re-driven for the red-bar case). Two-branch drain: `wi-401` merges and unloads, then `wi-402 is not merge-ready … the pessimistic sequence` and the same conflict refusal exits 1 — the first merge stands, the second branch stays finished-and-claimed. This is a loud, self-describing handback rather than a silent hang, and §A2 is honest that the conflict moves to the lane. The residue is that `drive.run` returns nonzero and every relaunch re-hits the identical conflict with no automated progress and no owner surface naming it (no handback, no `docs/work/pause`, nothing in status) — which is exactly §A3/WI-387's scope arriving one row early. -> No code change owed here; record it in the fragment as a known terminal state that WI-387's handback outcome closes, so the next reader does not have to re-derive it from a refusal string. -> @owner
VERDICT: CHANGES-REQUESTED findings=8

### REVIEWER — G3 — Round 2 — 2026-08-01
Verdict: APPROVE
Findings:
- none against the eight round-1 findings — all eight re-driven at `55f95fac`, all eight fixed, none carried. THE THREE MAJORS, re-driven as my ORIGINAL exploits rather than as their tests. (1) Forgery: the same ordinary commit carrying `Bar-Green: bar PASS (2 steps, tier all) trunk=deadbeef00` now gives `_merge_ready` = `(False, 'its tip is not a verified refresh commit - no Bar-Green: trailer naming this exact tree and parent')`; `integrate` falls into the pessimistic arm and the stub harness really runs (`harness-order.txt` goes `[]` -> `['trunk_step','check','--jobs','0','--tier','smoke','--trunk-lane']`), so the file lands on trunk BARRED — the correct outcome, not the exploit. (2) Amend: `git commit --amend --no-edit` with a file added moves the tree `3d7386fc5a…` -> `baad1ec422…`, `refresh_attestation` returns None, the slot re-refreshes, and the harness order DOUBLES (two full runs recorded), so the amended content is barred before it merges. Copied-genuine-message driven separately: the verbatim message on a later commit -> attestation None. (3) Data loss: `_work_tip` on the forged-trailer branch now returns the real tip `5ba1bd084a…` rather than its parent, `refresh` succeeds, and `late.txt` SURVIVES on the branch. The binding itself checks out: `tree=` is knowable before the commit because `git write-tree` writes the same index `commit` will use, the post-commit self-check refuses the script's own unverifiable output, and `_work_tip` peels only at the sha a verified refresh recorded. THE OTHER FIVE. `--trunk-lane` direction: the old backwards sentence is gone from the repo (`grep "pass over the artifacts"` outside this file: 0 hits) and the corrected form sits in five homes — check.py:1059, the ratchet entry, the fragment, the archived spec, and tests/test_check_lane.py:302. Slot guard: BOTH mutants now red — `_extra = _slot(root)` in `integrate_one` gives `assert 3 == 2` (round 1's escape, closed) and a second `ac.acquire_lock(` gives `assert 2 == 1`. MAIN checkout: `refresh` refuses by name ("the MAIN checkout at … has wi-401 checked out, so this repo has no trunk checked out to merge IN"), returns no sha and writes no attestation — re-driven with the real trunk at `b2bbea0dcf` and the primary sitting on the branch. Spine debt: the fragment now names SR-132, LLR-140's **Detail** and IF-080's **Contract** clause by clause, records that IF-080 omits `refresh` entirely and that §A9.1 item 2 does not scope it, and separates LLR-141 as incomplete rather than false. Conflict terminal state: recorded as WI-387's to close. The previously-false claim is recorded as a CORRECTION, not silently rewritten — the Summary states round 1 found the second half "satisfiable by a string" and that "Both are recorded below, because what shipped is the second one", and deviation 2 says in its own text that the first version "had the failure direction backwards". Re-measured: `check.py` is 1547 lines and the ratchet reads 1547 (+26/-2 = +24 net, matching the entry's arithmetic); full unfiltered `pytest -q -n auto` = **2 failed, 1726 passed, 12 skipped in 523.38s**, matching the claim exactly; `ruff check .` All checks passed and `ruff format --check .` 146 files already formatted; `check_trajectory --root . --strict` rc=0. The two reds are still only `test_this_repo_is_not_a_work_branch` (`assert 'wi-386' is None`) and `test_meta_repo_has_zero_unexplained_orphans` (`docs/concurrency-v2.md:10` and `:979 broken link -> work/deferred/`, the only two check_docs failures across 337 docs) — `22e66e51` and `4ae2e838` are reachable in the object DB but not on this branch, so the orphans red is the behind-trunk condition and clears at merge. -> no remedy owed -> @owner
- [MINOR] project-trajectory/scripts/integrate.py:468 -> A fourth commit shape still verifies, and the stated bound overprices it. `refresh_attestation`'s contract says forging all three names "is no longer an accident that a copied message can cause; it is re-implementing the refresh". Driven — it is three git commands and no bar: in the lane worktree, `git add -A`, `T=$(git write-tree)`, `P=$(git rev-parse wi-401)`, then a commit whose subject is `refresh: wi-401 onto trunk deadbeef01` and whose body carries `Bar-Green: tree=$T work=$P bar PASS (99 steps, tier all)`. `refresh_attestation` returns `('4c9ab7d242…', 'bar PASS (99 steps, tier all)')`, `_merge_ready` is `(True, 'bar PASS (99 steps, tier all)')`, `integrate --tier smoke` exits 0 reporting `wi-401 merged (WI-401); bar PASS (99 steps, tier all)`, `never-barred.txt` reaches trunk, and `harness-order.txt` is `[]` — the bar never ran at any point. The FIX is not in question and this is not the round-1 finding restated: every accidental carrier (copy, amend, rebase, cherry-pick, a hand-quoted trailer) now refuses, which is exactly what I filed. What is left is the record — "re-implementing the refresh" reads as a cost nobody pays by hand, and the actual cost is three commands whose format is printed in every refresh commit on trunk, which matters here because the operator of this system is an agent that reads the source. The only structural closure is a bar the slot cannot skip, and Decision 3 deleted that by owner ruling, so nothing is owed but an honest number. -> Restate the bound in the same place: forging it means deliberately naming the tree and the parent (three git commands), so it is closed against every accidental carrier and open to anyone who means it — and note that closing it further would mean re-barring at the slot, which Decision 3 rules out. -> @owner
- [MINOR] project-trajectory/scripts/integrate.py:906 -> The new directory prune reaches one step past its own stated rule, onto directories that predate the refresh. The comment says "a directory that still holds a pre-existing file is the lane's, not ours" — true, but an EMPTY pre-existing directory is also the lane's and is removed. Driven: with an empty ignored `bar-cache/` created in the lane worktree BEFORE the refresh (and `out/run-logs/session.md` alongside as the control), the refresh's bar writes `bar-cache/run.txt`, `_shed_residue` deletes it, and the walk-up `rmdir` then removes `bar-cache/` itself — `pre-existing EMPTY dir survived: False`, while `out/run-logs/session.md survived: True` and the unload still reports `!! out/`. No file content can be lost, since the prune only ever calls `rmdir` and that fails on a non-empty directory — verified in the pre-existing-FILE case, where `bar-cache/worker-run.txt` is kept while `bar-cache/run.txt` is shed. It is worth closing anyway because this repo has a live case of an empty untracked directory being load-bearing: `docs/work/deferred/`, whose on-disk emptiness is the only reason `check_docs` resolves that link on trunk — named in this WI's own fragment as the finding-not-fixed-here. -> Record the pre-existing directory set alongside the ignored-file baseline and prune only directories absent from it, so the rule the comment states is the rule the code follows. -> @owner
- [MINOR] docs/log.d/WI-386-station-protocol.md:141 -> The "Reviewed baseline bump" paragraph is the one part of the record round 2 did not bring up to date, in two ways. (a) "Complexity ratchet untouched (`main` stayed at 16 — the flag is an assignment, not a branch)" is true of the baseline file and true of `check.py:main`, but it leaves a reader concluding nothing crossed — when the round-2 guards pushed `refresh` over C901 and the only reason the ratchet is untouched is that it was decomposed rather than baselined. Driven, and the decomposition is REAL rather than cosmetic: inlining `_refresh_preflight` back into `refresh` gives `C901 refresh is too complex (13 > 10)`, while as shipped `refresh` measures 9 and `_refresh_preflight` 6, both under `MAX_COMPLEXITY = 10`, and `integrate.py` carries no baseline entry at all so a bump would have needed a new one. It moved no behaviour either: the preflight runs `_declared_bar_or_refusal` -> the primary-checkout refusal -> `lane_worktree` -> dirty -> `_work_tip` -> `reset --hard` in that order, which is round 1's sequence with exactly the one new guard inserted. (b) "eleven of the 24 lines are the argparse help and the comment" undercounts: the explanatory comment is 10 added lines and the `help=` string 3, i.e. 13 of the 26 added (24 net), with 4 more in the module usage docstring. The figure errs against the builder's own argument, so the substance stands and only the number is wrong — in the surface that compiles into `log.md` as the permanent record of what shipped. -> Add one sentence recording that `refresh` crossed C901 (13 > 10) with the round-2 guards and was decomposed into `_refresh_preflight` per the ratchet's decompose-don't-bump instruction, and re-stamp the eleven to 13. -> @owner
VERDICT: APPROVE findings=3

### REVIEWER — G3 — Round 3 — 2026-08-01
Verdict: APPROVE
Findings:
- none against round-2 finding 2 (the prune) — the fix is right and, more to the point, the builder's premise check is right and I reproduced it before judging the fix. On a synthetic repo ignoring `cache/`: an EMPTY ignored directory is reported `!! cache/` by `git status --porcelain --ignored=matching` at `-u` default AND at `-uall`, whether it never held content or was just emptied, while `git ls-files -o -i --exclude-standard` reports nothing for it. So deleting the prune would have made every merge exit nonzero over a directory the refresh itself emptied, and no git listing can separate "pre-existing empty" from "the bar made it" — a snapshot is the only instrument, which is what `existing_directories` is. BOTH halves drive. Pre-existing empty survives: `bar-cache/` created empty before the refresh, `ignored_files(wt) == set()` and `_worktree_dirt(wt) == ['!! bar-cache/']` (the two readings disagreeing exactly as the docstrings say), the snapshot contains it, the refresh sheds `bar-cache/run.txt` and the DIRECTORY SURVIVES. Not vacuous: with nothing pre-existing, the same run removes `bar-cache/` entirely, `_worktree_dirt(wt) == []` and the real `integrate --tier smoke` exits **0** with the unload completed. Nesting stops where it should — I made the stub bar write `bar-cache/x/y/run.txt` under a PRE-EXISTING empty `bar-cache/`: `x/` is pruned, `bar-cache/` survives. Mutation-driven both ways, and each mutant reds only its own test: deleting the `if directory in baseline_dirs: break` guard reds `test_an_empty_directory_that_predates_the_refresh_is_not_pruned` alone (3 passed), and replacing it with an unconditional `break` reds `test_a_directory_the_bar_itself_created_is_pruned` alone (3 passed) — so it is a rule with two outcomes, not a guard that cannot fail. SNAPSHOT MOMENT is right: instrumented, it is taken with `HEAD` equal to the work tip and no `MERGE_HEAD` — i.e. after `_refresh_preflight`'s `reset --hard` and before the trunk merge, so it captures the lane's own state and nothing the refresh goes on to create; a directory the trunk merge or `trunk_step` introduces is correctly outside the baseline, and one holding tracked files cannot be pruned anyway because `rmdir` fails. COST measured rather than asserted: 116 directories in 0.036 s on this worktree, 433 in 0.274 s on the kit checkout that has a `.venv`, and a rate probe over `.venv` alone of ~4,200 dirs/s — so a `node_modules`-scale lane (~50k directories) would pay ~12 s against an eleven-minute bar, which is what "cheap relative to what it guards" claims. `_shed_residue` itself went C901 8 -> 9 for the guard, still inside the limit. -> no remedy owed -> @owner
- none against round-2 finding 1 (the bound) — it is recorded, not closed, which is the right disposition, and the four wordings are consistent and accurate. I re-drove the forgery against the SHIPPED recipe verbatim rather than against their test: `git add -A`, `T=$(git write-tree)`, `P=$(git rev-parse wi-401)`, then a commit with subject `refresh: wi-401 onto trunk deadbeef01` and body `Bar-Green: tree=$T work=$P bar PASS (99 steps, tier all)`. `refresh_attestation` returns `('62511499e4…', 'bar PASS (99 steps, tier all)')`, `_merge_ready` is `(True, …)`, `integrate --tier smoke` exits 0, `never-barred.txt` reaches trunk, and `harness-order.txt` is `[]` before and after — no bar, ever. So the price named is the price paid. The four texts (`refresh_attestation`'s contract, the module docstring, the fragment, the archived Deliverable) all say the same thing in the same shape — "defeats ACCIDENT, not INTENT", the three preparatory commands enumerated with the commit named separately, Decision 3 as the reason it is accepted, and "a slot-side bar and a revisited DECISION 3, not a longer trailer" as the only reopening — and none of them hides the commit, so the honest total is four git invocations. That "three" is my own round-2 phrasing which they adopted; if it is ever restated, four is the number, and the discrepancy changes no judgement. ON THE TEST, since my opinion was asked for the record: `test_a_deliberately_forged_attestation_is_a_STATED_limit_not_a_defence` is the RIGHT shape, not an ossified defect, for three reasons. (1) It pins an owner-ruled acceptance rather than an accident — the limit exists because Decision 3 deleted the merge bar, so the test records a consequence of a ruling, not a bug someone tolerated. (2) Its failure mode is the useful one: the only way to red it is to make the design stronger, and when that happens the reader is routed by name and comment to §A2.0 and the contract before "fixing" it — which converts a silent scope change into a noticed one, the same constraints-over-checks instinct §0 asks for. (3) It cannot go vacuous, because it asserts BOTH that the shape verifies and that `_order(wt) == []`; either half changing reds it. The one thing that would make it ossifying — being read out of context as "unbarred content should merge" — is answered by placement as much as by wording: it sits in §5b.5 immediately after the four accident regressions (`…forged_bar_green_trailer_does_not_make_a_branch_merge_ready`, `…queue_refuses_to_land_a_forged_attestation_unbarred`, `…amending_a_refresh_commit_revokes_its_attestation`, `…work_commit_that_quotes_the_trailer_is_never_peeled_away`), so nobody meets the limit without first meeting the four defences that bound it. A limit test without that pairing WOULD be ossification; with it, it is documentation the harness enforces. -> no remedy owed -> @owner
- none against round-2 finding 3 (the record) — every figure re-measured independently and every one reproduces. `check.py` is 1547 lines and the ratchet entry reads 1547; `git diff --numstat c89cf63a..HEAD` gives `26 2` for it, so 24 net, and the added lines break down as 10 comment + 3 `help=` string = **13 of 26**, plus 4 in the module usage docstring — exactly what the paragraph now says. The C901 account is now in the record and it is true: at the shipped tip `refresh` measures **9** and `_refresh_preflight` **6** against `MAX_COMPLEXITY = 10` (`ruff check --select C901 --config "lint.mccabe.max-complexity = 10"` on `integrate.py` passes clean), `integrate.py` carries no `tests/test_complexity_ratchet.py` baseline entry at all so a bump would have meant minting one for a function invented this session, and the round-2 claim that inlining it back gives 13 > 10 still reproduces. "No behaviour moved" also re-checks against the source: the preflight runs `_declared_bar_or_refusal` -> the primary-checkout refusal -> `lane_worktree` -> the dirty check -> `_work_tip` -> `reset --hard`, which is round 1's order with the one new guard inserted. -> no remedy owed -> @owner
- none — bars re-run at `1329bd4e` and all match the claim. NOTE ON METHOD: my first full-suite run overlapped the mutation probes above, which edit `integrate.py` in place, so I discarded it and re-ran on a tree verified byte-identical to `HEAD` (`git diff --exit-code` clean); the numbers below are from that clean run. `pytest -q -n auto` = **2 failed, 1729 passed, 12 skipped in 422.40s** (their 394.13s is wall-clock variance on the same counts). `ruff check .` All checks passed; `ruff format --check .` 146 files already formatted; `check_trajectory --root . --strict` rc=0. The two reds are still only `test_this_repo_is_not_a_work_branch` (`assert 'wi-386' is None`) and `test_meta_repo_has_zero_unexplained_orphans` (`docs/concurrency-v2.md:10` and `:979 broken link -> work/deferred/`, the only two check_docs failures across 337 docs). SCOPE OF THIS VERDICT, stated plainly: `git merge-base --is-ancestor 4ae2e838 HEAD` is FALSE — the branch has not merged trunk, deliberately, pending `wi-384`. So this APPROVE covers the branch as it stands, not the composed result; the orphans red is the behind-trunk condition and is expected to clear when `22e66e51` arrives, but I have not verified the composed tree and this round makes no claim about it. NOT RE-EXAMINED this round, deliberately: everything outside my three round-2 findings — the constraint and its two reads, the disposable-commit rule, the failure paths, the two owner requirements, the deletion ledger, the spine-debt record and the conflict terminal state were all verified at rounds 1 and 2, and the only production file touched since is `integrate.py`'s residue/attestation-record surface, which is what this round drove. -> no remedy owed -> @owner
VERDICT: APPROVE findings=0
