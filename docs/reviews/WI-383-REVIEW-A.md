### REVIEWER — G3 — Round 1 — 2026-08-01

Verdict: CHANGES-REQUESTED

Scope: the branch `wi-383` at `507106e1`, diffed against its claim commit `b7b9fbaa`
(three commits: `d8e732a4`, `b8c7cc21`, `507106e1`). `git merge-base --is-ancestor
0b4774f0 HEAD` is **FALSE** — the branch has **not** merged trunk (trunk has moved to
`0b4774f0` since the claim), so every judgement below is about the branch as it
stands and makes no claim about the composed tree.

Findings:
- [MAJOR] project-trajectory/scripts/schedule.py:595 -> The headline claim that the two axes are separated STRUCTURALLY on the ordering side is false, and the property is unpinned at the only place it can be broken. Three surfaces say it in the same words — the Deliverable ("`order_key` now takes the RANK, not the classification, so no concurrency value can reach the sort at all — the independence is structural on that side rather than merely tested"), docs/log.d/WI-383-two-axes-classifier.md:28, and `order_key`'s own docstring (:599-601, "the ordering axis is all this function is entitled to see"). `order_key`'s second parameter is an untyped positional; nothing in the function inspects it, and the ONLY thing that makes the sentence true is the single call site at :634. Driven as a mutation: `"_key": order_key(w, rank, …)` -> `order_key(w, concurrency, …)` and nothing else changed. The full unfiltered suite then reports **`1 failed, 1755 passed, 8 skipped in 478.85s`** — byte-for-byte the same counts as the clean tree, with the same single standing red (`test_this_repo_is_not_a_work_branch`). Not a behaviour-preserving mutant either: on a five-WI registry (`gate`, `protected`, `high-risk`, `spine`, `ordinary`) the shipped frontier is `['WI-004','WI-001','WI-002','WI-003','WI-009']` (spine first, rank 0) and the mutant's is `['WI-001','WI-002','WI-003','WI-004','WI-009']` — every exclusive kind ties on the string `"exclusive"`, rank ordering inside the group is destroyed, and the spine WI falls from first to fourth. It survives because the one named test, `test_order_key_can_only_see_the_rank_axis` (tests/test_schedule.py:330), calls `order_key` with literal ints and so cannot observe what `evaluate` hands it, and because the only frontier-ordering test that crosses a concurrency boundary (`test_lowest_rank_sorts_first`, spine vs ordinary) is preserved by the alphabetical accident `"exclusive" < "parallel"`. This is the WI's central deliverable asserted as a guarantee it does not have. -> Make it true or stop calling it structural: either have `order_key` refuse a non-rank (`if not isinstance(rank, int): raise TypeError`, which reds the mutant instantly), or add one `evaluate`-level test that orders four EXCLUSIVE kinds against each other (`spine` 0 / `gate` 2 / `protected` 3 / `high-risk` 4) so the ordering assertion is bound to the call site rather than to a hand-passed integer. -> @owner
- [MINOR] tests/test_schedule.py:307 -> `test_concurrency_does_not_determine_rank` is a tautology over the test file's own literal and can never fail for a code change. Its body reads `RULED_TABLE` — the module-level dict declared at tests/test_schedule.py:255 — and never touches `sched`, so it asserts that the test's copy of §A1 is self-consistent, not that `_KIND_CONCURRENCY`/`_KIND_RANK` are. Driven: `_KIND_RANK` mutated so concurrency FULLY determines rank (every exclusive kind -> 0, `critique`/`ordinary` -> 1), i.e. the named property made maximally false in the code. Running the pin alone gives `1 passed in 0.19s`; running the file gives `6 failed, 33 passed`, and the six are `test_classify_every_declared_value_is_deterministic`, `test_rank_1_is_reserved_and_unoccupied`, `test_critique_is_parallel_and_ranks_ahead_of_ordinary`, `test_planmode_dual_derives_the_high_risk_kind_from_the_signal`, `test_planmode_dual_contradicting_safetyclass_quarantines`, `test_planmode_absent_or_other_changes_nothing` — none of them the pin. So the PROPERTY is genuinely covered (by the table test binding `RULED_TABLE` to `classify()`), and no coverage is lost; what is wrong is the record. The Deliverable and the fragment (:50) both cite "the five exclusive kinds hold four distinct ranks" as one of four independent pins, and that one pin drives nothing. -> Bind it to the module: build the two sets from `sched._KIND_CONCURRENCY` / `sched._KIND_RANK` instead of from `RULED_TABLE`, so the sentence in the fragment is the sentence the test checks. -> @owner
- [MINOR] docs/requirements/system-requirements.csv:95 -> The `checkpoint` deviation leaves two false spine cells that the WI-390 handoff does not name, one of them in a row this branch edited. `SR-094`'s AcceptanceCriteria reads *"Spine, critique, **checkpoint**, and dual-plan structure override dishonest declarations"* and `TC-091`'s Description/Method read *"Classify missing, unknown, structurally contradictory, critique, **checkpoint**, and dual-plan WIs"* — both now name a classifier input that this branch deleted. SR-094 is not incidental: it is the parent SR of the rewritten function, cited in `schedule.py`'s own section header (:433 `SR-093/SR-094`) and in the module docstring. TC-091 is sharper still — commit `b8c7cc21` edits that exact row to repoint its `Evidence`, and leaves the same row's Description naming the deleted input. The fragment's "No spine amendment" paragraph (:77-82) lists SR-093, SR-124, LLR-059, LLR-089, LLR-095, LLR-131, LLR-058 and LLR-123; `grep "SR-094" docs/log.d/WI-383-two-axes-classifier.md` returns nothing, and TC-091 appears only as the Evidence repoint and the separate node-id finding. Not a code change and not this row's to amend (`Method` and `AcceptanceCriteria` are RATIFIED cells — `spine_cell_class` returns `"ratified"` for both, driven) — but WI-390 inherits what the fragment writes down, and this is the same shape REVIEW-A round 1 filed against WI-386's Deviation 6. -> Add SR-094's AcceptanceCriteria and TC-091's Description/Method to the "No spine amendment" list, naming `checkpoint` as the false clause, so WI-390 gets the real scope. -> @owner
- [MINOR] project-trajectory/scripts/schedule.py:83 -> The new concurrency vocabulary collides head-on with the pre-existing `Exclusive` mutex-key column, inside the one module whose stated diagnosis is that an overloaded word caused the defect. `CONCURRENCY_EXCLUSIVE = "exclusive"` now sits beside `w["exclusive"]` (the `Exclusive` registry column, a list of mutex keys), `_exclusive_conflicts()` (:643) and `record["exclusive"]` — two unrelated ideas, both spelled *exclusive*, both about not-running-together, with no note anywhere disambiguating them. It reaches the wire: a WI declaring a mutex key emits `{"concurrency": "parallel", …, "exclusive": ["registry-lock"], "rank": 6}` from `evaluate` (driven verbatim), which reads as a self-contradiction to anyone who has not read the source. `simulate`'s revised docstring (:750-752) then says it models "neither build time … nor the `exclusive` concurrency axis" while it DOES apply `_exclusive_conflicts` through `frontier`, so the one sentence that uses both senses is the ambiguous one. Nothing is functionally wrong and no test is owed. -> One comment at `_exclusive_conflicts` and one clause in the module docstring stating that the `Exclusive` COLUMN is a mutex key set and has nothing to do with the `exclusive` CONCURRENCY value; consider renaming the emitted record key to `exclusive_keys`, which is a one-line change with no live consumer (`grep sched_class|exclusive` across scripts/HTML finds none outside `schedule.py` and the generated arch-map). -> @owner
- none against the §A1 table, the fail-closed quarantine, the reserved rank-1 gap, the deletions, the `checkpoint` no-producer claim, the ratchets, or the TC-091 disposition — every one re-driven, and every builder claim in this list HOLDS. **§A1 VERBATIM**, read off the design text (docs/concurrency-v2.md:174-183) rather than the summary: `_KIND_RANK` is `{spine 0, attestation 2, gate 2, protected 3, high-risk 4, critique 5, ordinary 6}` with `RANK_UNCLASSIFIED = 7`, and `_KIND_CONCURRENCY` is exclusive for spine/attestation/gate/protected/high-risk, **parallel for critique**, parallel for ordinary — seven rows, seven matches, and `PlanMode=dual` resolves to the `high-risk` kind (`classify({'planmode':'dual'})` -> `('exclusive', 4, ['exclusive:dual-plan'])`). **THE RESERVED GAP IS THE RIGHT CALL and its test is real**: §"Revised breakdown" assigns the `adjudication` kind to WI-388, §A1 has already ruled its rank at 1, so writing the ladder whole costs one comment and saves renumbering a ruled table — and `test_rank_1_is_reserved_and_unoccupied` is NOT decorative, it reds under the rank mutation above. **FAIL-CLOSED ON BOTH AXES**: driven for a missing kind (`classify({})` -> `('unclassified', 7, ['unclassified:missing'])`), an unknown value, a structural contradiction (`{'safetyclass':'ordinary'}, structural='spine'`) and a dual/declared contradiction — all four through the one `_unclassified()` helper, all four returning both axes refused. Mutating `_unclassified` to return `_KIND_RANK["ordinary"]` (fail OPEN on rank only) reds three tests, so this one is genuinely pinned. **THE DELETIONS** are all gone (`grep` for `SCHED_SPINE_SERIAL|SCHED_PROTECTED|SCHED_SINGLE_WI|SCHED_ORDINARY|_GATE_RANK|is_schedulable_class|EXIT_TRAIN_END|ASSIGNMENT-END` across live `.py` returns only `schedule.py`'s explanatory comment and `agent_common.py`'s retired-number note), and **nothing went vacuous, because nothing covered them**: the same grep run against the BASE commit's `tests/` returns hits in `test_schedule.py` only — no test anywhere named `EXIT_TRAIN_END`, `ASSIGNMENT-END` or `worker["sched"]`, so the §7 continuation guard's refusing arm had zero coverage before deletion and no assertion was dropped. `agent_loop --wi 'WI-201;WI-204'` **survives and still works**: `parse_wi_list` is untouched, `test_build_worker_assignment_good_base_parses_wi_list` and the real end-to-end `test_worker_multi_wi_assignment_builds_in_order` (two sessions, two train logs, both WI trailers) pass in the clean full run — and that end-to-end is precisely the path the deleted guard ran on. **THE `checkpoint` NO-PRODUCER CLAIM IS TRUE, exhaustively**: an unfiltered repo-wide `grep -rl checkpoint` (excluding `.git` and `docs/archive/`) returns 17 files and **not one is a script** — they are the design/log/registry surfaces plus an unrelated knowledge note; no `Checkpoint` column exists in `WI_COLUMNS` in any of the three F5-duplicated readers (`schedule.py`, `check_trajectory.py`, `agent_common.py`) or in `wi_convert.py`'s writer, so the key had no producer and `classify({'safetyclass':'ordinary','checkpoint':True})` now correctly answers `('parallel', 6, ['parallel:ordinary'])`. The fragment is also honest that the identical no-producer argument applies to `critique`, and that §A1 naming it is the only thing that keeps it. **THE RATCHETS re-measure exactly and sit in the commit that earned them**: `wc -l agent_loop.py` = **2973** against **3026** at the base, and `ruff --select C901` puts `run_iteration` at **20** on the branch (`max-complexity=19` reports `20 > 19`; `max-complexity=20` reports nothing) against **23** at the base — both stamps land in `d8e732a4`, the commit that deletes the code, not in the close commit. **TC-091's Evidence repoint is classified correctly**: `spine_cell_class('docs/test/test-cases.csv','Evidence')` returns `"traced"` (`Evidence` is in `SPINE_TRACED_CELLS`, the §A5.1/WI-380 split), while `Method` returns `"ratified"` — so the repoint arms no re-attest window and is not an amendment. And the closed row genuinely HAD to clear its `SpecRef`: restoring `specref = "docs/concurrency-v2.md"` on the `complete/` spec makes `check_trajectory --root . --strict` emit `ERROR - R-F WI-383: status=done but SpecRef … is still set` and `1 error(s) in docs/work`. -> no remedy owed -> @owner
- none against their own finding (the unresolvable Evidence node id) — **both halves confirmed, and it warrants a WI.** Half one: `tests/test_agent_loop_dispatch.py` is absent from the tree and was deleted on **2026-07-29** by `31ad569d` ("Phase 5 item 1: delete the parallel dispatcher … and its test modules"), yet TC-091's Evidence still names `tests/test_agent_loop_dispatch.py::test_unclassified_wi_fails_closed_without_stopping_others`. Half two, driven rather than argued: I replaced that entry with `tests/this_file_has_never_existed.py::test_entirely_invented` — a path with no file and no history — and ran every gate that could plausibly see it. `trace.py --strict --no-placeholders --require-verified --strict-schema` **rc=0** (`orphans=0 integrity=0 status-findings=0 schema-findings=0`, TC-091 still `Verified`); `check_trajectory --root . --strict` **rc=0** (`clean (389 work item(s), 366 done (94%), 16 cancelled, graph acyclic)`); `check_doc_refs --root . --strict` **rc=0** (`no dangling path or sym: references`); `pytest tests/test_trace.py tests/test_registry_checks.py` **70 passed**. Nothing anywhere convicts it. That is a Verified evidence cell citing a file that has never existed, with every gate green — the exact failure class this repo keeps paying for. Their disposition (its own WI: a resolver for `Evidence` node ids, or an honest ruling that the cell is prose) is right, and the resolver is the cheaper half: the FILE path is checkable with `Path.exists()` today, and only the `::node` suffix needs pytest to adjudicate. -> file it; no remedy owed on this branch -> @owner
- none — bars re-run on a tree verified byte-identical to `HEAD` (`git status --porcelain` empty, `git diff --exit-code` clean) BEFORE any mutation probe was applied, and the tree was restored and re-verified clean after each probe. `pytest -q -n auto` = **`1 failed, 1755 passed, 8 skipped in 388.69s`**, matching the builder's claim exactly, the sole red the standing `test_this_repo_is_not_a_work_branch` (`assert 'wi-383' is None`). `check_docs.py --root . --ignore docs/test/report.md --ignore "docs/work/*" --stale` -> **`OK - 339 doc(s), 969 intra-repo link(s), 0 broken`** rc=0 (plus the pre-existing status.md line-budget WARN and the pre-existing stale-link hints). `check_trajectory.py --root . --strict` -> **rc=0, `clean (389 work item(s), 366 done (94%), 16 cancelled, graph acyclic)`**, WARNs only the pre-existing connectivity/IF-055/IF-080/IF-081 set plus the SpecRef-post-dates advisories on WI-381/387/388/389/390. `check_doc_refs.py --root . --strict` -> **rc=0**. `ruff check .` -> `All checks passed!`; `ruff format --check .` -> `146 files already formatted`. ON THE STALE ARCH-MAP, which I chased before reading the fragment's last paragraph: `gen_arch_map.py --check --strict-parse` is **RED** on this branch (`code map STALE in docs\architecture.md`) and was **green at the base commit** (verified by extracting `b7b9fbaa` with `git archive` and re-running there), and `check.py --run-step arch-map --trunk-lane` reports `FAIL arch-map exit 1`. It is **not a finding**: `arch-map` is in `_TRUNK_FRESHNESS_STEPS`, so it SKIPs on a claimed work branch by design (concurrency-restructure §5.2), the station refresh regenerates via `trunk_step.py` before it bars, and the fragment records the staleness explicitly. I confirmed regeneration produces exactly the right map — the four hunks are the dropped `agent_loop --> schedule` edge, the imports line, and the three renamed signatures (`classify` -> `(concurrency, rank, …)`, `is_schedulable(concurrency)`, `order_key(wi, rank, …)`). Same for `docs/status.md` still listing WI-383 in its ready frontier: that block is `<!-- BEGIN GENERATED STATUS -->`, and `status-map` is a trunk-lane step. NOT COVERED, deliberately: the composed tree (the branch has not merged trunk, so I ran no composed bar and make no claim about one); `IF-055`'s Contract, which names the now-deleted `SCHED_*` constants — I checked and it was ALREADY false at the base commit (`grep SCHED_ integrate.py` at `b7b9fbaa` is empty), so it is inherited drift already named in §A9.1 item 2, not this row's; and the dispatcher-side question of what now enforces a spine batch's homogeneity, which is WI-381's scope arriving after this one. -> no remedy owed -> @owner
VERDICT: CHANGES-REQUESTED findings=4
### REVIEWER — G3 — Round 2 — 2026-08-01

Verdict: APPROVE

Scope: the branch at `9f72f25b` (`b19dcc8a` + `9f72f25b` over my round-1 review).
`git merge-base --is-ancestor 0b4774f0 HEAD` is still **FALSE** — the branch has
**not** merged trunk — so this APPROVE covers the branch as it stands and makes no
claim about the composed tree.

Findings:
- none against my four round-1 findings — all four re-driven at `9f72f25b`, all four fixed, and the MAJOR was taken the right way round. **THE MAJOR.** The retraction is complete and honest: the old sentence survives in exactly ONE place in the repo, inside the retraction block that quotes it in order to call it false (`docs/log.d/WI-383-two-axes-classifier.md:74`, *"That was false"*), and a sweep for "structural" across the Deliverable, the fragment, `schedule.py` and `test_schedule.py` returns nothing else but the unrelated `structural=` cross-check parameter. What replaced it is what is actually true — a convention held by one call site, plus a test that convicts the edit — stated identically in all three homes. **THE NEW PIN GENUINELY CONVICTS, re-driven as MY OWN mutation rather than as their test**: `order_key(w, rank, …)` -> `order_key(w, concurrency, …)`, nothing else changed, gives `tests/test_schedule.py` = **`1 failed, 39 passed in 1.14s`** with the single red `test_the_frontier_orders_one_concurrency_group_by_rank` and the diff `At index 0 diff: 'WI-001' != 'WI-004'` — the exact figure they reported, and the pin is the ONLY red, so it is targeted rather than a shotgun. It convicts a CLASS, not one token: I also drove `order_key(w, 0, …)` (the ordering axis dropped entirely rather than swapped) and it reds the same pin plus `test_lowest_rank_sorts_first` (`2 failed, 38 passed`). And **no literal path back into the property remains** — the only two `order_key(…)` calls with hand-passed integers are in `test_rank_outranks_every_tiebreaker_in_the_order_key`, which pins "rank is the leading term against a maxed Priority, downstream count and hard path", a property that test CAN see; its name no longer claims the call-site property, and my mutation leaves it green, correctly. **ON THE DECLINED REMEDY, since my opinion was asked: I agree, and would not now propose it.** The `isinstance(rank, int)` guard was my weaker option and their reasoning defeats it on the merits, not on taste. It is a *check* bolted into a pure library function to detect a caller's mistake after the fact — the exact inversion §0 names — and it is not even a good check: it converts "the caller passes the rank" into "the caller passes an int", which the concurrency string happens to fail and every wrong integer passes (`order_key(w, downstream, …)` or `order_key(w, w["priority"], …)` would sail through it while destroying the order). The call-site test is strictly stronger because it asserts the OUTCOME the property exists for, and it is the only form that could have caught the mutation I actually drove. Recording it as considered-and-declined with the reason is the right disposition; a successor re-proposing it now meets the argument. **FINDING 2**, the tautological pin: `test_concurrency_does_not_determine_rank` now builds its two sets from `sched._KIND_CONCURRENCY`/`sched._KIND_RANK`. Re-driven with my `_KIND_RANK` collapse (every exclusive kind -> 0, `critique`/`ordinary` -> 1): the pin **reds alone** — `assert {'exclusive': {0}, 'parallel': {1}} == {'exclusive': {0,2,3,4}, 'parallel': {5,6}}`, `1 failed in 0.21s` — where round 1 gave `1 passed in 0.19s`. **FINDING 3**, the WI-390 handoff: the "No spine amendment" list now names `SR-094`'s AcceptanceCriteria and `TC-091`'s Description/Method as separate bullets, quotes **checkpoint** as the false clause in both, states that SR-094 is the parent SR of the rewritten function, and says in its own text that round 1 caught the omission "including one in a row this branch edited" — a correction, not a silent rewrite. **FINDING 4**, the collision: resolved on the wire and verified there rather than in the diff — `evaluate` now emits `{"concurrency": "parallel", …, "exclusive_keys": [], "rank": 6}` and the record's key set is `['concurrency','disposition','downstream','exclusive_keys','hard_path','id','priority','rank','reasons','status']` with no `exclusive` key at all; `test_shared_exclusive_key_serializes` asserts both halves (`d["exclusive_keys"] == ["db"]` and `"exclusive" not in d`). Leaving the internal `w["exclusive"]` alone is right and I checked why: the three remaining uses are all the WI dict's own field mirroring the `Exclusive` COLUMN name, copied verbatim across the three F5-duplicated readers, so renaming it there would be drift. `simulate`'s docstring — the one sentence that used both senses — now says plainly that it DOES apply the mutex keys through `frontier` and does NOT apply the concurrency axis, which is the true reading. -> no remedy owed -> @owner
- none against WI-394's central measurement — **the control reproduces exactly, and it is what makes the row's boundary a claim rather than a hunch.** Driven independently, each mutation applied to a clean tree and the tree restored and re-verified after each. CONTROL: `TC-091`'s `Verifies` -> `SR-999;LLR-999` gives `trace.py --strict --no-placeholders --require-verified --strict-schema` **rc=1**, `FINDING (orphan): TC TC-091 references unknown SR-999` *and* `… unknown LLR-999`, `orphans=5`. The three unchecked cells all give rc=0 on BOTH tools: `LLR-059`'s `Module` -> `project-trajectory/scripts/this_module_has_never_existed.py` with `CodeSymbol` -> `entirely_invented_symbol` (trace rc=0, `orphans=0 integrity=0`, zero FINDING lines; `check_trajectory --strict` rc=0 clean), and `TestRefs` -> `(see TC-999)` likewise. Their **113 passed** figure across `test_trace.py`, `test_registry_checks.py`, `test_check_flows.py`, `test_check_stubs.py`, `test_modules_registry.py` and `test_components_registry.py` under the `Module`/`CodeSymbol` mutation re-measures to **113 passed in 24.74s** exactly. So the row's boundary sentence is right and is NOT "traced cells are unchecked": a pointer into another registry is joined and validated, a pointer out into the code or test tree is not. I also confirmed the supporting claim that `Module` is genuinely *read* — `trace.py:1938` builds `module_ids` from LLR `Module` cells solely as the join target for the IF `ThisProject` endpoint check (`:665-668`), which is warn-only, so a `Module` no file backs joins to nothing exactly as stated. **THE ROW ITSELF checks out on every point asked.** Id 394 is genuinely unclaimed: enumerating `refs/heads/*` and `refs/remotes/*` with `git ls-tree -r` over `docs/work` + `docs/specs` shows `WI-392` on `wi-378` only, `WI-393` on `wi-391` only, and `WI-394` on `wi-383` only — invisible from this tree, which is precisely the §B3 id-reservation hazard the fragment names. `specref = docs/specs/WI-394.md` resolves and `check_doc_refs --strict` is rc=0 over it (its two invented paths carry the sanctioned `path-ok` idiom — a documented mechanism at `check_doc_refs.py:13-14`/`:296`, not an invented escape hatch). It schedules `ready`, `concurrency=parallel`, `rank=6`, driven through the real CLI. Both options carry a stated cost — (a) "a new check is what §0 warns about", (b) "`Automated=Yes` + `Status=Verified` beside an unchecked evidence pointer is a strong implication to leave standing on purpose" — the third file-half-only shape is named separately so it is not mistaken for (a), and `Done-when` requires the OWNER's ruling in `log.md`'s Decisions before anything is built. The `checkpoint` rows are correctly pushed to WI-390 rather than absorbed, in both the spec's Scope notes and the row title. The `561 -> 22` precedent it cites also checks out against WI-062's own record and WI-308's spec. -> no remedy owed -> @owner
- [MINOR] docs/specs/WI-394.md:65 -> The incidental `check_doc_refs` datum reproduces in BEHAVIOUR but the stated MECHANISM is wrong, and the true one changes the option-(a) argument it is offered to support. The behaviour is exactly as claimed — I built a probe doc carrying both forms: the bare `tests/this_file_has_never_existed.py` gives `WARN - … does not exist in the repo` and `1 dangling reference(s)`, while the same path as `…::test_entirely_invented` gives `OK - no dangling path or sym: references`, rc=0. But the reason is NOT "its PATH tier requires the token to *end* in a known source extension, and a `::node` suffix does not" (repeated at docs/log.d/WI-383-two-axes-classifier.md:196 and, as "one tokenising reason", at :198 and docs/specs/WI-394.md:69). The extension rule is at `check_doc_refs.py:155` and is **never reached**: `is_path_shaped` short-circuits ten lines earlier at **`:145`**, `if "::" in token or ";" in token or "," in token: return False` — and its comment (`:141-144`) names the cell by name: *"`::` is a pytest node id (tests/x.py::test_name — **the kit's sanctioned Evidence form**, a real file plus a selector) … none is a single filesystem path, so they are out of the path tier's scope (false-positive control is the point)"*. The cited rule would not have produced the behaviour anyway, because it is a DISJUNCTION and `tests/` is itself in `PATH_PREFIXES` (`:91`), so the `startswith` half would have rescued the token. Driven: `is_path_shaped('tests/nope.py')` -> `True`; `is_path_shaped('tests/nope.py::test_x')` -> `False`; `is_path_shaped('project-trajectory/scripts/nope.py::sym')` -> `False`. This is not pedantry about a line number — it inverts the character of the gap in both directions. Option (a) is HARDER than "one tokenising reason" suggests, because it must overturn a deliberate, commented exclusion and re-argue the false-positive rationale that justified it, not repair an oversight; and the file-half-only shape is CHEAPER than the spec implies, because the change is splitting on `::` at that guard rather than teaching a tokeniser a new shape, and `tests/` is already a recognised prefix. An owner weighing (a) against (b) is entitled to know the exclusion was made on purpose, with a reason, by this kit. (Unaffected and correct: the second half — that the tool never reads the CSVs anyway, its scan surface being root `*.md` + `docs/**/*.md`.) -> Restate the mechanism in both homes: `check_doc_refs.py:145` deliberately excludes any `::` token, naming the Evidence form and citing false-positive control, so the gap is a recorded decision rather than an accident — and note that the file-half fix therefore lives at that guard. -> @owner
- [MINOR] docs/log.d/WI-383-two-axes-classifier.md:233 -> The re-measured bars block carries one figure that is not reproducible at the commit it sits in. The block is headed *"Bars (real output, this branch, **after the round-1 fixes**)"* and three of its five figures were correctly re-measured this round (`1756` passed, `572` smoke, `871` untraced — all three re-driven by me, all three exact). The `check_trajectory` line still reads **`clean (389 work item(s), …)`**, but `9f72f25b` — the very commit that wrote this block — also files `WI-394`, so the real output at that commit is **`clean (390 work item(s), 366 done (94%), 16 cancelled, graph acyclic)`**, which is what I get running it now. A one-digit slip, but it sits in the surface that compiles into `log.md` as the permanent record of what shipped, in a block that advertises itself as re-measured, and unreproducible figures are the specific defect class this round exists to catch. -> Re-stamp the line to 390. -> @owner
- none — bars re-run on a tree verified byte-identical to `HEAD` (`git status --porcelain` empty, `git diff --exit-code` clean) AFTER every mutation probe above had been applied and reverted; no probe overlapped a suite run. `pytest -q -n auto` = **`1 failed, 1756 passed, 8 skipped in 540.09s`**, matching their claim exactly (540 s vs their 400 s is wall-clock variance on identical counts), the sole red the standing `test_this_repo_is_not_a_work_branch`. `pytest -q -n auto -m smoke` = **`1 failed, 572 passed in 12.96s`**, matching their claim including the disappearance of the four skips. `check_docs --root . --ignore docs/test/report.md --ignore "docs/work/*" --stale` -> **`OK - 341 doc(s), 970 intra-repo link(s), 0 broken`** rc=0 (plus the pre-existing status.md line-budget WARN). `check_trajectory --root . --strict` -> rc=0, **`clean (390 work item(s), 366 done (94%), 16 cancelled, graph acyclic)`**. `check_doc_refs --root . --strict` -> rc=0, **871 untraced**. `ruff check .` -> `All checks passed!`; `ruff format --check .` -> `146 files already formatted`. NOT RE-EXAMINED this round, deliberately: everything outside my four findings and WI-394 — the §A1 table, the fail-closed quarantine, the reserved rank-1 gap, the deletion ledger, the `checkpoint` no-producer sweep, the `--wi 'WI-201;WI-204'` survival, the ratchets and the R-F close were all driven at round 1, and the only production file touched since is `schedule.py`'s ordering/naming surface, which is what this round drove; and the composed tree, which I still have not seen. The stale `docs/architecture.md` is unchanged in disposition — a trunk-lane artifact by §5.2, regenerated by the station refresh, correctly recorded. -> no remedy owed -> @owner
VERDICT: APPROVE findings=2
### REVIEWER — G3 — Round 3 — 2026-08-01

Verdict: APPROVE

Scope: the branch at `94e4a4d8`, one commit past my round-2 review, restricted to
my two round-2 MINORs. `git merge-base --is-ancestor 0b4774f0 HEAD` is still
**FALSE** — the branch has **not** merged trunk — so this APPROVE covers the
branch as it stands and makes no claim about the composed tree.

Findings:
- none against the DIRECTION of the re-pricing, which is right, and I say so as the one who established the mechanism. The wrong reason is quoted and named as wrong in both homes rather than swapped out, which is the house form. Their control is the one I should have stated and did not: `is_path_shaped('tests/no_extension_at_all')` -> **True**, re-driven, and it settles the question outright — the final rule is a disjunction and `tests/` is in `PATH_PREFIXES`, so no extension is required and the extension rule cannot have caused the pass. **(a) IS HARDER, and by more than they wrote.** The deliberate decision is not only commented, it is GUARDED: `tests/test_check_doc_refs.py:86 test_node_ids_and_joined_lists_are_not_path_flagged` exists precisely to hold the `::` exclusion (*"a pytest node id (the sanctioned Evidence form) … the path tier must not flag them"*). So a resolver overturns a decision that has both a stated rationale and a named regression test defending it. Their added question — what a citation to a test *renamed but still present* should cost a reader — is the right one to have surfaced, and it is exactly the case the guard's rationale was buying. **(c) DESERVES ITS PROMOTION.** Driven end to end rather than judged from the prose: with the split applied at both sites the tool behaves correctly — `check_doc_refs --strict` goes from `OK` to **6 dangling**, and all six are TRUE (WI-394's own quoted invented citations, which would then need the `path-ok` marker it already uses for the bare-path form, plus the real `tests/test_agent_loop_dispatch.py::…` entries this row was found by), with **zero** false positives against the four existing-file citations elsewhere in the repo. The core change measures **+4/-2, 2 net lines**. So "cheap" is right and the promotion is right: 2 net lines plus one amended guard test plus two `path-ok` markers, against (a)'s new finding class, CSV reader, symbol oracle and warn-first triage of the existing census. Does promoting it change what a ruler would choose? I think yes, and legitimately — as a trailing paragraph the file-half shape read as a footnote to (a); as a first-class option beside a re-priced (a) it is visibly the one that closes the whole observed failure mode (every dead citation in this repo died because its FILE was deleted) at a fraction of the cost, and a ruler can now pick it without feeling they picked a diminished (a). -> no remedy owed -> @owner
- [MINOR] docs/specs/WI-394.md:157 -> Option (c)'s stated mechanism is not implementable where it says, and applied literally it produces FALSE POSITIVES — which matters because that sentence is the whole of (c)'s price. Both homes say the change is *"splitting on `::` at that guard rather than returning `False`: take the part before it and let the existing path tier judge it"*, and docs/log.d/WI-383-two-axes-classifier.md:223 goes further — *"is the whole change inside the tool"*. It is not. `is_path_shaped` is a PREDICATE returning a bool; it cannot hand a rewritten token back to its caller, and `path_findings` (`check_doc_refs.py:264`) re-derives `clean = token.strip().rstrip("/")` from the ORIGINAL token and stats the whole `path::node` string. Driven — I applied exactly the edit as worded and nothing else: `check_doc_refs --strict` reports **10 dangling references**, and four of the seven distinct tokens have file halves that EXIST — `tests/test_check_harness.py::test_step_gate_honours_an_explicit_gate`, `tests/test_dogfood_sync.py::test_scaffold_omissions_list_is_current`, `tests/test_generated_newlines.py::test_write_text_accepts_newline_on_the_declared_floor`, `tests/test_pre_commit_hook.py::test_run_steps_gate_promotes_the_warn_first_floor` (all four files verified present). Adding the second site — `clean = token.split("::", 1)[0].strip().rstrip("/")` — fixes it completely: **6 dangling, all true, zero false positives**. Two related over-statements ride on the same sentence. (i) The claim that (c) *"concedes the exact case `check_doc_refs`'s `::` guard was protecting"* is not right: (c) does not concede the renamed-test case, it RE-SCOPES the guard to the half it can adjudicate — and it reds the same named guard test `test_node_ids_and_joined_lists_are_not_path_flagged` that (a) would (driven: `1 failed, 15 passed` on `tests/test_check_doc_refs.py`). So the asymmetry the spec draws — (a) "must overturn a deliberate decision", (c) "the guard agreeing with itself" — is overdrawn; both overturn it, and only the SIZE differs. (ii) "The registry side still needs a reader … that is the bulk of the remaining work" is correct and unaffected. None of this changes the ranking — (c) remains far cheaper than (a) and I would still promote it — but a ruler pricing (c) off this paragraph would budget a one-site edit and get a false-positive regression plus a guard test to amend. -> Restate (c)'s mechanism in both homes: split on `::` at the guard AND at `path_findings`' `clean =` (2 net lines, driven: 6 true findings, 0 false), note that it reds `test_node_ids_and_joined_lists_are_not_path_flagged` and so amends the same guarded decision (a) would, and drop "concedes the exact case the guard was protecting" — the renamed-test case is ruled prose, not conceded. -> @owner
- [MINOR] docs/work/queued/WI-394-a-verified-spine-row-can-cite-evidenc.md:3 -> The row `title` still announces **"TWO HONEST OPTIONS, AND A BUILDER MUST NOT PICK"** and then enumerates three — `(a)`, `(b)` and `(c) THE FILE HALF ONLY`. The spec's own heading was correctly changed in the same commit (`## Two honest options` -> `## The options`); the row title was not. This is not cosmetic in this kit: the `Title` cell is the surface a ruler meets FIRST — it is what `docs/status.md`'s generated ready-frontier renders and what `PROJECT_STATE.html` shows — so the one-line summary of a row whose entire purpose is "the owner picks" mis-states how many things there are to pick from, and the option this round promoted is the one the count omits. -> Change "TWO HONEST OPTIONS" to "THREE OPTIONS" in the row title, matching the spec heading. -> @owner
- none — bars re-measured independently at `94e4a4d8` on a tree verified byte-identical to `HEAD` (`git status --porcelain` empty, `git diff --exit-code` clean), AFTER the option-(c) probes above had been applied and reverted; no probe overlapped a suite run. Every figure in the re-measured block reproduces exactly: `pytest -q -n auto` = **`1 failed, 1756 passed, 8 skipped`** (426.42s here against their 567.93s — wall-clock only, identical counts, which is precisely the distinction their new paragraph draws); `pytest -q -n auto -m smoke` = **`1 failed, 572 passed`**; `check_docs … --stale` -> **`OK - 341 doc(s), 970 intra-repo link(s), 0 broken`**; `check_trajectory --root . --strict` -> **`clean (390 work item(s), 366 done (94%), 16 cancelled, graph acyclic)`**, the corrected count; `check_doc_refs --root . --strict` -> **`OK … 871 untraced`**; `ruff check .` -> `All checks passed!`; `ruff format --check .` -> `146 files already formatted`. The cross-round series is right and I corroborate it from my OWN runs rather than theirs: **1755** is what I measured at round 1, **1756** at round 2, **1756** now — and the "round 2 changed no code" premise is mechanically checkable, `git diff --stat b19dcc8a..HEAD -- '*.py'` is EMPTY, so the +1 is the round-1 call-site pin and nothing since has moved the suite. `check_docs` is now present so every bar the protocol names appears in the block. NOT RE-EXAMINED this round, deliberately: everything outside my two round-2 MINORs — the two axes and their four pins, the §A1 table, the fail-closed quarantine, the deletions, the ratchets, the R-F close, WI-394's control and its wider-class measurements were all driven at rounds 1 and 2, and no `.py` has changed since; and the composed tree, which I have not seen in any round. The `isinstance` decline now carries round 2's sharper form as its primary argument, recorded so a successor meets the argument rather than the taste. -> no remedy owed -> @owner
VERDICT: APPROVE findings=2
