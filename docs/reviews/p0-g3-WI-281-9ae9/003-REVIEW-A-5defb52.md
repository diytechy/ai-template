- [MAJOR] tests/conftest.py:57 -> the re-tier leaves `test_session_stdin` in the smoke bar; on this supported Windows checkout the real new command `python -m pytest -q -n auto -m smoke` fails (`365 passed, 1 failed`), so the required commit bar is not green -> make the Windows smoke bar green (land the WI-275 shim-fixture correction or re-tier the failing module with a documented boundary) before completing WI-281 -> @owner
- [MAJOR] .github/workflows/test.yml:80 -> the only CI wall-clock measurement invokes `--mode warn`, whose over-budget path exits 0; a 61 s probe printed `WARN` and passed, contradicting WI-281's required CI enforcement and allowing budget regressions to ship -> run this CI lane with `--mode enforce` on a stable runner (or obtain and record an approved spec change before weakening the requirement) -> @owner
- [MAJOR] docs/status.md:38 -> the authoritative per-commit bar still says `~3.3 min`, while this diff declares the bar `<= 60 s`; this directly misses WI-281 Done-when's declared-surface update and misstates the current contract -> update the status bar prose to the declared <=60 s smoke budget in the same completion change -> @owner
- [MAJOR] docs/requirements/work-items.csv:280 -> WI-281 remains `queued` with an empty Deliverable and live SpecRef, although its spec requires the completion row to be `done` and the spec archived; this leaves the work item lifecycle and dashboard state inconsistent with the claimed close -> after the blockers are fixed, fill the Deliverable, set the row done, clear SpecRef, and archive the spec with its close date -> @owner
VERDICT: CHANGES-REQUESTED findings=4
