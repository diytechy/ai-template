# WI-281 review A — 45637d2 (independent)

Reviewed commit 45637d2 ("align smoke-budget enforcement documentation") atop the
WI-281 train. It resolves the prior review's finding 3 (the test_smoke_budget.py
docstring called the CI check "warns-first per lane" while the workflow runs
`--mode enforce`). Verified by driving the real shipped paths, not by reading:

- **Finding 3 FIXED & verified.** The new docstring — local default report-only
  `--mode warn`, CI `--mode enforce` to fail a breach — matches
  `check_smoke_budget.py` (argparse default=warn) and `.github/workflows/test.yml:84`
  (`--mode enforce`). Drove all four exit paths: warn/over→exit 0 + `WARN`,
  enforce/over→exit 1 + `FAIL`, default/over→exit 0, enforce/within→exit 0.
  warn/enforce language is now consistent across stack.ini, both test files, the
  script, and the workflow.
- **Membership ratchet bites (real collection).** Temporarily set
  `[smoke-budget] max-tests=300`; the live assertion failed on the real 349-test
  `-m smoke` collection with the "re-tier or re-stamp" message, then reverted
  clean. CI enforce lane run: `7.9s vs 60s budget -> within`, exit 0.
- **Partition intact.** smoke=349, slow=1035, neither=0, both=0 (349+1035=1384);
  no test deleted (WI-281 added 6 in-process budget tests). Windows smoke bar
  green: `349 passed in 7.33s` (review-A finding-1 Windows red resolved —
  test_session_stdin re-tiered whole). trace.py clean (orphans=0 integrity=0);
  the 3 session-protocol SKILL.md copies are byte-identical.

The two remaining Done-when gaps are NOT defects in this diff — they are
root-coordination-truth edits SR-060 and the worker prompt (agent_loop.py:348-352)
bar a worker branch from making, correctly `-> @owner`. A CHANGES-REQUESTED here
routes to `rework` (agent_dispatch.py:920-924) and would loop the worker on items
it cannot touch, deadlocking the integration that is the only thing able to
complete them. Recorded for the owner/integrator, not as blockers:

- [MINOR] docs/status.md:38 -> the hand-authored "Bar (per commit)" prose still says `~3.3 min`, contradicting the declared `[smoke-budget] seconds=60` and the measured ~7 s bar; it sits below `<!-- END GENERATED STATUS -->`, so neither gen_trajectory --status nor the integrator's generate_status (emits no bar line) rewrites it -> owner hand-edits the bar text to the <= 60 s / ~7 s re-tier at integration -> @owner
- [MINOR] docs/requirements/work-items.csv:280 -> WI-281 is still `queued` with an empty Deliverable and a live SpecRef; the worker contract bars flipping Status/Deliverable cells on a train branch -> the integrator's _rewrite_wi_rows + synth_deliverable set the row `done` with its Deliverable and archive the spec at integration -> @owner

VERDICT: APPROVE findings=2
