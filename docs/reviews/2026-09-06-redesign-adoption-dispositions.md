# Adoption and purpose review dispositions

The [Opus 5/high review](2026-09-06-redesign-adoption-promises-opus.md) assessed
the exact guidance/charter diff and eight supplied adopter cases. It requested
changes; the following is the builder's disposition, not an independent
approval or an executed migration.

| Finding | Disposition |
|---|---|
| F1: lost audit instructions | Fixed: name the roster path and the audit's matrix, zero-reach needs and per-hat reach counts in the canonical method. The actual brief remains the behavior to inspect. |
| F2: template/instance agreement omitted; neighboring comments stale | Fixed: restore the template/instance axis in CONSISTENCY; explain its wider semantic scope and PERFORMANCE's operating-resource scope beside the historical comments. |
| F3: overloaded adopter question / implied reversal guarantee | Simplified to one adoption/upgrade question about completing the change while preserving content, policy meaning, evidence and unfinished work. Removed implied reversal guarantee. Retain the stable hat name: the governing Fable disposition explicitly chose extending this hat over adding another. |
| F3: conditional predicate may miss policy-only changes | Confirmed limit, now explicit beside the seed predicate. Adoption/resync revalidation applies at its own entry point, and checks real applicability before relying on the roster. Existing project predicates are not silently broadened. |
| F4: anchors have no inbound use | Fixed: the existing 27-need purpose worksheet links the live README objective anchors. No generic adopter skill links to this kit's project-specific objectives. |
| F4: objectives paraphrase the vision | No new requirement tier is claimed. The user requested objective anchors inside the vision; the governing plan chose these prose clauses and deferred reference-carrier work. They unpack purpose; SN/SR cells retain acceptance. |
| Cases 5/6: changed hats and no-impact record home | Fixed: review inbound references when changing or removing a hat; record no semantic impact and its reason in the resync commit. |
| Optional extra authority cross-reference | Existing opening prohibition on worker approval/snapshot writes remains explicit. No new instruction grants that authority; avoid another redundant procedural link. |

The review accepted the method's distinctions between a missing stakeholder
outcome, a missing SR constraint and an implementation defect. Its cases
exercise the guidance through independent judgment; they do not substitute for
the [populated upgrade oracle](../ai-template-redesign-2026-09-05-codex/P0-ADOPTER-EVIDENCE.md)
or claim that a real adopter has approved the workflow.

The [independent closure review](2026-09-06-redesign-adoption-closure-opus.md)
returned **APPROVE**. Its remaining checks are concrete: the root README has an
explicit `vision` anchor, a Project vision heading now parents O1–O6, and the
canonical skill is materialized into both agent directories. Documentation-link
and dogfood tests verify those integration details; no visual-usability claim
is made from source inspection.
