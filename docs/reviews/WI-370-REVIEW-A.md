### REVIEWER — G3 — Round 1 — 2026-07-30
Verdict: CHANGES-REQUESTED
Findings:
- [MAJOR] project-trajectory/scripts/integrate.py:204 -> The rung under-refuses relative to the R-E bar it fronts: `_specref_refusal` accepts two ref shapes that red R-E `--strict` on every composed tree — a bare-fragment ref (`"#anchor"`: `ref.split("#", 1)[0]` is `""`, and `root / ""` is root, which exists) and a ref whose path part names a directory (`"docs"`: `.exists()` is true where R-E requires `is_file()`). Reproduced live against this branch: `_specref_refusal(root, {"specref": "#improvement-plan"}, "WI-X")` and `..., {"specref": "docs"}, ...` both return None while `check_trajectory.specref_findings` returns "has no path — a SpecRef names a document, not a bare fragment" and "names a directory, not a document" for the same inputs. Neither shape is anchor resolution (the Deliverable's legitimate carve-out) — both are R-E's PATH half, both were added to R-E by 131-REVIEW-A driving the rule, and both recreate the exact defect WI-370 exists to close: the spec claims cleanly, then reds `--strict` from a file the closing branch cannot amend (the open-wants-ref/terminal-wants-cleared R-F trap), on a day the drain measured two degenerate agent-filed refs already. The empty-ref and nonexistent-path shapes ARE closed (verified non-vacuous: at the claim sha, both new refusal tests fail because the claim SUCCEEDS end-to-end — exit 0, branch cut — not merely refused later). -> Mirror R-E's path half: `pathpart = ref.partition("#")[0].strip()`; refuse an empty pathpart ("names no document") and require `(root / pathpart).is_file()` rather than `.exists()`; add the two regression tests (bare fragment, directory) beside the existing rung tests, and while there give test_claim_refuses_a_specref_that_does_not_resolve the queued-spec-intact assertion its two siblings carry. -> @owner
- [MINOR] PROJECT_STATE.html:1 -> The close commit (`04123a5`) moved the WI-370 spec from `docs/work/active/` to `docs/work/archive/` — a registry edit — without regenerating the dashboard: `gen_trajectory.py --check` exits 1 at HEAD ("project-state dashboard STALE"). This is the identical finding WI-369-REVIEW-A filed one close earlier, now recurred at the very next close, and the asymmetry is in this WI's own history: the claim commit (`9b563c0`) folded the regeneration in, the close did not. Non-blocking on its own (mechanically visible; the train convention has the integrator re-regenerate on the composed tree), but a second consecutive occurrence argues the close ritual needs the regen step, not just the claim. -> Run `python project-trajectory/scripts/gen_trajectory.py` and commit the regen with the MAJOR fix above (or record the integrator-owed regen at the drain sitting, and consider adding the regen to the close checklist so this stops recurring). -> @owner
VERDICT: CHANGES-REQUESTED findings=2
