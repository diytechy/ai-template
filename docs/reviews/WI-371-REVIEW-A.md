### REVIEWER — G3 — Round 2 — 2026-07-30
Verdict: APPROVE
Round-2 delta: commit `17d7046` verified as exactly the claimed one-sentence rephrase — the archived spec's Deliverable tail (4 insertions / 3 deletions, sole file `docs/work/archive/WI-371-shoot-mjs-deletes-session-baselines.md`) drops the backticked gitignored path in favor of prose, and the prose accurately records both the builder's probe and this reviewer's repeated probe (marker in a session subdirectory survived; planted stale top-level PNG deleted). `check_doc_refs.py --root . --strict` now exits 0 on the branch ("OK - no dangling path or sym: references"). Nothing new appeared; both Round-1 findings carry forward.
Findings:
- [MINOR] PROJECT_STATE.html:1 -> The close commit (`a83c2ed`) moved the WI-371 spec from `docs/work/active/` to `docs/work/archive/` — a registry edit — without regenerating the dashboard, and the staleness persists at Round-2 HEAD: `gen_trajectory.py --check` still exits 1 after `17d7046`, which itself edited spec body text that renders into the dashboard's details blob. The committed HTML still lists WI-371 under "Next work". This is the exact finding class WI-369's Round-1 review filed one sitting earlier, and this branch's own claim commit (`e9765d9`) folded the regen in, demonstrating the convention. Non-blocking per that precedent: the miss is mechanically visible (the freshness gate reds; `check_trajectory.py --strict` itself stays exit 0), and the train convention has the integrator re-regenerate on the composed tree — but a twice-in-two-WIs repeat suggests paying it at close, not at drain. -> Run `python project-trajectory/scripts/gen_trajectory.py` and commit the regen (or record the integrator-owed regen explicitly at the drain sitting). -> @owner
- [MINOR] scripts/dashboard-shots/shoot.mjs:86 -> The scoped clean calls `rmSync(join(OUT, f))` without `recursive` on every top-level name ending in `.png`, so a *subdirectory* whose name ends in `.png` (e.g. a baseline dir carelessly named `before-1280px-light-arch-full.png/`) crashes the run before any shot with a raw `ERR_FS_EISDIR` — verified directly (node 24.18.0, `rmSync` on a directory named `probe.png` throws `ERR_FS_EISDIR`). Contrived input and the error names the offending path, so non-blocking; but the guard is one clause and keeps the "subdirectories are someone else's work product" contract literal instead of name-shaped. -> `if (f.endsWith(".png") && statSync(join(OUT, f)).isFile()) rmSync(join(OUT, f));` (import `statSync`). -> @owner
VERDICT: APPROVE findings=2
