### REVIEWER — G3 — Round 1 — 2026-07-30
Verdict: CHANGES-REQUESTED
Findings:
- [MAJOR] docs/specs/WI-373.md:63 -> The branch stops at the build commit: docs/log.d/ holds only .gitkeep (no WI-373 fragment), the spec is unarchived, the Deliverable unfilled — while house precedent on this trunk is close-before-review (WI-370's `04123a5 "WI-370 close: archive the spec, drop the log fragment"` and WI-371's `a83c2ed` both preceded their review rounds). On APPROVE the integrator queue-drain merges, trunk_step's §5.1 fragment compile finds nothing, and WI-373 lands with no `docs/log.md` entry — breaking the history-lives-in-log.md invariant — and the flagged +1,207 growth on watched PROCESS_OPTIONS.md (161,117 → 162,324, verified against the blobs) never gets the "WI log's byte-delta report" that the byte-budget-guard skill's own enforcement column names as the baseline's enforcer; the in-build spec-checkbox edit also leaves a standing `check_trajectory` WARN ("WI-373: its SpecRef docs/specs/WI-373.md changed after the WI row was last touched") on every strict run until the row is re-touched. -> Add the close commit per session-protocol §4 (fragment with summary, deviations from spec, byte deltas, pytest totals; archive the spec with Deliverable filled; clear SpecRef) so round 2 can verify it landed. -> @owner
- [MINOR] OWNER_SCRATCHPAD.md:1 -> The build commit silently includes a +9/−1 hunk to the owner-only scratchpad — a fourth surface outside the spec's "documentation-only; three homes" scope, unmentioned anywhere in the commit message, and unauditable by any reviewer (CLAUDE.md bars reading the file), so the WI ships a change nobody but the owner can vouch for; the working agreement's own line is "the silent extra is what destroys trust". -> Owner confirms the hunk is their own note (recorded in the round record) or splits it out of the WI branch. -> @owner
- [MINOR] project-trajectory/AGENTS.template.md:168 -> The working agreement gained a third clause ("a review finding is a claim — confirm (reproduce) or refute it before acting on it") beyond the spec's and registry row's "two missing judgment rules" (reversal-evidence + sunk-cost-keeping only), restating the PROCESS_OPTIONS finding lifecycle in a second home with no pointer — against the spec's own "three homes, each fact stated once": a future edit to the lifecycle's terms (what counts as reproduction, where refutations are recorded) updates process-options.md and nothing ties this clause to it, so downstream repos inherit diverging rules. -> Amend the spec/round record to sanction the clause as the deliberate always-loaded echo (and point it at the lifecycle the way sibling bullets point at process.md §6), or drop it — which also relieves the 17-byte headroom. -> @owner
- [MINOR] docs/specs/WI-373.md:59 -> Done-when box 2 is checked, but the promised "sunk-cost-keeping escalation shape" — the written case's contents (what was found, why the shape can't reach the requirement, cost of both paths) — shipped nowhere: AGENTS.template.md:174 says only "escalated as a written case to its owner" and the audit row repeats that phrase, while the sole enumeration lives in this spec, which archives at close and never ships downstream (grep "cost of both paths" hits only docs/specs/WI-373.md), so a downstream agent told to escalate has no stated case contents anywhere in the kit. -> Land the three components where budget allows (the enforcement-audit row note or PROCESS_OPTIONS' lifecycle paragraph), or amend the Done-when wording to what actually shipped before checking the box. -> @owner
- [MINOR] docs/enforcement-audit.md:58 -> The new row names "`score_reviews.py`'s confirmed-finding rate is the advisory backstop" while the scoreboard that rate feeds has been dark since 2026-07-15 (docs/reviews/scoreboard.txt last touched in `403acf5`, 2026-07-15; the WI's own spec pins the dormancy and defers feed-or-delete to the owner), so the audit — whose bar is honest tiers — points a reader at a tally nothing currently produces: refutations "counted against the reviewer's confirmed-finding rate" land nowhere today. -> Append the dormancy note to the row ("scoreboard dark since 2026-07-15; feed-or-delete is audit rec #8, an owner call"). -> @owner
VERDICT: CHANGES-REQUESTED findings=5
