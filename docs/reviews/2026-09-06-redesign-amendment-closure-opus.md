# redesign-amendment-closure — adversarial review

Requested route: Opus 5, high; declared CLI stream-json pattern.

Subject SHA256: `97b6f8304b9acc4e4316517e138687c7a2c33ae4441a0520efe9481bb368b968`

Tracked invocation: [session log](../iteration/call_be2b2e4f769840c8a8aa166329579bba-20260906-101602.log).

Review uses supplied source with tools disabled. It is an independent
assessment, not a requirement approval or a test execution.

# Intermediate closure review — DECOMPOSITION-AMENDMENTS packet

Scope: closure of B1–B7 only, against the supplied packet and PROCESS clauses. Tools disabled; all line-range citations and registry contents are taken as supplied and remain intake checks.

**APPROVE**

## Closure findings

| # | Status | Evidence in current packet |
| --- | --- | --- |
| B1 | Closed | H4's acceptance cell reads "Smoke tier at the per-commit commit bar, the Full suite at phase close"; "work-item/slice or" is gone. Matches the declared moment-to-tier table and the 2026-09-05 close-bar ruling. |
| B2 | Closed | Neither the failing-first clause nor the cadence-exception marker appears in the proposed cell. The note points at `PROCESS.md:491-496` as the existing home and explicitly declines to restate the ordering rule. The supplied DevStg-Impl clause confirms test-first is standing doctrine, so H4 adds no obligation. |
| B3 | Closed | The false "does not change selectors" claim is replaced by an explicit statement that the SN × conditional-hat matrix changes and both lenses reach SN-026; the SN-036/SR-161 applicability decision is recorded (existing SR-175 home, no further SR owed), SR-175's substantive work is preserved as open, and the rejected option (b) is named as a feed-back applicability step, not a re-litigation. |
| B4 | Closed | The `shall` requires the record to identify a fresh non-author session for **attended and unattended** Critique acceptance, and a missing reviewer is an Inspection finding — the promise cannot be discharged without the reviewer. SR-154 stays the home of scheduling/family/degradation/escalation; LLR-048's two-parent link is named as owed at authoring rather than duplicated. |
| B5 | Closed | One method: `verification = "Inspection"`; "or attested" removed from the AC; the reviewer's semantic decision is the recorded input, not a second method; "regression" replaced by "inspection case". |
| B6 | Closed | H5 reads the existing scoped decomposition/review record and SR-161's applicability/no-finding fields; TC evidence anchors into that record; no new schema, form, ratchet, or count sensor. The `PROCESS_OPTIONS.md` path is corrected. |
| B7 | Closed on substance | The supplied PROCESS text confirms both load-bearing clauses: SR → ≥1 LLR *or* Analysis/Inspection/Attest, and every SR → ≥1 TC regardless of method. The three Inspection rows' direct TCs are permitted. |

## Remaining blockers

None. Everything left is implementation debt or future allocation, explicitly scoped as such by the packet: SR-175's declared-set/planted-credential and consent/inclusion work; explicit rubric derivation and per-finding anchors across Critique paths; the changed-side/counterpart record; SR/TC ID allocation at intake.

## Non-blocking intake checks

1. The second PROCESS block is rendered as a blockquote but condenses two separate sentences. Quote verbatim or mark it a summary — B7's fix was "quote the clause".
2. H2: state that the direct TC is *in addition to* the re-pointed LLR-048/TC-048 evidence, not instead of it, so the exemption reads as a floor.
3. Verify the `legal` / `personal-data` tag tokens against LEGAL and DATA-PROTECTION `listens_for`.
4. `aspect = "process"` appears on H5 only; confirm whether H2/H3 owe it.
