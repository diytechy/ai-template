# Dispositions — the 2026-09-06 Fable review of the redesign implementation

Companion to [the review](2026-09-06-redesign-fable-review.md); numbering
matches it. Owner instruction 2026-09-06: make the fixes, lock the
recommendation, keep execution moving. Each fix carries a regression test
that failed before the change.

| # | Finding | Disposition |
|---|---|---|
| 1 | Durable base pollutes evidence after a trunk merge | **FIXED.** `default_base` keeps `merge-base(trunk, HEAD)` whenever `trunk_name` names a ref distinct from the current branch, and uses the claim commit only in the single checkout (where merge-base collapses to HEAD). The existing single-checkout test now merges a trunk commit carrying `WI: WI-777` and asserts the linked lane's base is the trunk tip and WI-777 is not in its evidence. Known limit, unchanged: a single checkout that also merges trunk in still reads trunk's commits (claim..HEAD); documented in the docstring. |
| 2 | Fingerprint scan crashes at import | **FIXED.** Non-regular entries are skipped; a failed launch capture warns once to stderr and records `None`, which `running_scripts_moved` reads as "unavailable" (False), never a restart on every poll. A read failure of a real file still raises at comparison time and requests the typed restart. Tests: a dangling symlink plus a directory named `dir.py` leave the digest unchanged; a raising capture yields `None` and no restart. |
| 3 | Every integration ends the dispatcher | **RULED, IMPLEMENTED.** Fresh-process design kept; the launchers relaunch on exit 11, bounded at 50, live copies and shipped templates alike. The dispatcher still owns the boundary (stop admission, drain, preserve, exit). Test: an engine stub that exits 11 twice is run three times by `agent-resume.sh` (live + template; the `.cmd` twin has the same test under the Windows-only mark). The hash still covers every `*.py` under the scripts dir; narrowing it to imported modules was not cleanly expressible and is not needed once the relaunch exists. |
| 4 | Ctrl-C persists a half-filled record | **FIXED.** `invoke_session` catches `BaseException` so wall-secs, ended-at and `usage-status: unavailable` are filled on the way out; `invoke_and_persist` records `outcome: INTERRUPTED` and re-raises. The telemetry commit on the sitting's branch is retained by design (the record is the evidence of the interrupted call). |
| 5 | Restart drain swallows real outcomes | **RETAINED BY DESIGN.** Routing a NEEDS-HUMAN handback or a green refresh through the merge slot during a drain would run the dispatcher's own stale code for exactly the work the drain exists to avoid. The fresh process resumes the preserved lane: a NEEDS-HUMAN worker re-exits 7 and is handled then; a green refresh is re-run. Cost is one redone session or refresh per drain; correctness holds. An attempted narrower fix was reverted. |
| 6 | `diff-tree` without `-z` | **RECORDED.** Rare (a quoted non-ASCII spec name); degrades to the merge-base fallback in the linked-lane case, which is now the primary rule anyway. |
| 7 | One non-load-bearing assertion | **RECORDED.** The load-bearing check is in `test_dual_plan_routing`. |
| 8 | H1 vs R-E SpecRef grammar | **FIXED.** R-E now judges a `#fragment` on a TOML registry target as a row id: `#SN-1` is clean, `#need.SN-1` reports "did you mean #SN-1?", an unknown id reports "names no such row". The reader and hint live in `kitlib.spine` (`registry_ids`, `registry_row_hint`). The composer normalises a leading `./`. Strict trajectory check on this repo: no live SpecRef newly flagged. `check_trajectory.py` takes a reviewed +5 SLOC bump for the call site (baseline comment carries the reason). |
| 9 | P2a in-array comment loss; parse-error skip | **RECORDED** in P2A-EXECUTION.md: in-array comments are not preserved; a dependent whose frontmatter does not parse is skipped by both passes, the integrity check is the net. No writer change. |
| 10 | Old-kit resync test skip scope | **FIXED.** Only the preserved application's `npm test` is conditional on Node; the kit assertions always run and the skip is reported after them. The `--force` leg carries a comment saying what it does and does not prove. |
| 11 | Need-tier re-attestation debt has no consumer | **CARRIED.** OI-85 minted: re-attest SN-007 and SN-026 as amended, rule the SN-024 question, and decide whether to fund the parked SN drift detector. Typed brief with options and a recommendation (a). No need cell changed. |
| 12 | SR-184 contradicts SN-024 | **FIXED at the child.** The disclaiming sentence is removed from SR-184's rationale; the question is OI-85's. |
| 13 | SN-007 acceptance states no full-suite observable | **CARRIED** into OI-85's re-attestation of SN-007 (owner wording). |
| 14 | Inspection carriers keyed to H-labels | **FIXED.** Headings and anchors renamed to their subjects; TC-209..211 method/evidence cells and the amendment record follow; link check clean. |
| 15 | Design-replacement rule in four homes | **RECORDED**, not consolidated in this sitting: PROCESS §5 is the short home and the skill the fuller statement; folding the skill's extra rules into PROCESS costs watched bytes and is its own edit. |
| 16 | SR-184 acceptance names its method; hats.toml outside the snapshot | **PARTLY FIXED.** SR-184's acceptance now states the observable without naming the method; the advisory still fires on the word "rubric", which is the artifact the row inspects, so it is inherent and retained. The charter-drift gap is recorded, not fixed. |
