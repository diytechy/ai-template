### REVIEWER — G3 — Round 1 — 2026-07-31
Verdict: APPROVE
Findings:
- none against the central risk — **no test coverage was lost or silently changed**, driven mechanically rather than read. (1) *Nothing lost:* I reproduced the AST claim independently and made it stricter than the builder's. `ast.get_source_segment` on a `FunctionDef` **omits the decorator list**, so a naive form of this check cannot see a mutated `@pytest.mark.parametrize` — the exact way a split could drop cases while keeping function names; I re-cut every function from the first `@` line through `end_lineno`, and made the comparison duplicate-aware (a name copied into four modules must match a trunk copy in *each* module, not just once). Result over trunk `5cc5ab6`'s three parents (`test_trajectory.py` 187 fns / 151 tests, `test_trace.py` 96 / 81, `test_agent_loop.py` 122 / 111 = **405 top-level functions**) against the eleven resulting modules: **405/405 present, zero missing names, and exactly ONE differs** — `test_parse_map_rejects_entry_without_equals` (the documented comment repoint). All **12** decorated functions' `@parametrize` blocks are byte-identical. `_vendor_core` is not a changed body but an *added* copy — trunk's original survives byte-identical at `tests/test_agent_loop.py:568`. Test counts redistribute exactly: trajectory 56+25+45+25=**151**, trace 44+21+16=**81**, agent_loop 56+21+23+27=**127** (=111+16). As *collected* (parametrized expanded), agent_loop 121+23 = 58+34+29+23 = **144**. I also checked the two vectors a body comparison cannot see: **within-module relative order is preserved in all eleven modules** (the sole inversion is the hoisted `_vendor_core` helper copy, not a test), and **module-local fixture shadowing** — trunk's only module-local fixture is `loop_repo` (`test_agent_loop.py`), it stayed in the parent, every `loop_repo` test stayed with it, and the string's one occurrence outside the parent (`test_agent_loop_policy.py:9`) is docstring prose, not a fixture request; `conftest.py`'s `scaffold` is unshadowed. (2) *Tier membership:* trunk **1713 total / 556 smoke / 1157 slow**; branch **1714 / 557 / 1157** — the `+1` on both sides is the new guard test and the **slow count is held exactly**, which is the number a forgotten stem or a dropped test would have moved. All seven minted stems are in `conftest.SLOW_MODULES`. The guard is not decorative: I proved it bites without editing the file, monkeypatching `SLOW_MODULES` in a scratch interpreter — dropping `test_trace_rules`, `test_trajectory_arch` or `test_agent_loop_policy` each makes `test_wi277_split_modules_stay_slow` fail naming the missing stem, and it passes again on restore. (3) *Fixtures/helpers:* **no new shared module** — the whole diff is 11 test modules + `conftest.SLOW_MODULES` + the guard + the fragment; grep for `from test_` / `import test_` across the seven new files returns **nothing**, and each copied helper carries the `test_integrate.py::git_repo` "copied rather than imported" wording. (4) *Green:* full unfiltered `pytest -q -n auto` = **1 failed, 1701 passed, 12 skipped in 715.00s**, reproducing the fragment's figures exactly; 1701+1+12 = **1714** = the collected total, all of it executed. The sole red is `test_check_lane.py::test_this_repo_is_not_a_work_branch` (`_work_branch` answers `wi-277-test-monolith-split`), red by design on a claimed branch — not chased. Smoke = **1 failed, 552 passed, 4 skipped** (=557), also as logged. `check_docs --ignore docs/test/report.md --ignore "docs/work/*" --stale` → `OK - 330 doc(s), 933 intra-repo link(s), 0 broken`; `check_trajectory --strict` → `clean (375 work item(s), 359 done (96%), 14 retired, graph acyclic)` — both matching the fragment verbatim; `ruff check tests/` clean, which is what backs the "ruff-verified, not eyeballed" import-pruning claim (`os`/`subprocess`/`skip_without_env_gates` out of the trajectory parent, `skip_without_env_gates`/`SCRIPTS` out of the trace parent). (5) *The flagged S8 deviation — I ACCEPT all three repointings.* `test_agent_loop_policy.py:283` ("the preflight above" → "the model-map preflight in tests/test_agent_loop.py"): the referent is real and stayed put (`test_agent_loop.py:1129` Slice A golden net, `:1143` `test_model_placeholder_without_model_fails_preflight`), and a verbatim move would have left "above" pointing at a file the test no longer lives in. `test_agent_loop_routing.py:22` ("the golden-net suites above" → named file): trunk's wording sat at `test_agent_loop.py:1622` and meant the loop_repo end-to-end suites, all of which stayed in the parent — same dangling-reference argument, correctly resolved. `test_agent_loop_routing.py:320-325` is an *addition* rather than a repoint, and a necessary one: the Slice D header claims `classify_outcome` **and** `worker_endstate`/`worker_exit_banner` were extracted, and half those tests left, so verbatim would have left the header lying about tests that are not there. All three are minimal, inline-flagged `WI-277`, and none touches an assertion. (6) *The deferral is stated, not a defect:* the fragment opens (:3-6) and closes (:195-201) with `tests/test_gen_trajectory.py` (S1–S5) deliberately untouched — confirmed untouched in the diff — and the rationale holds independently: `WI-280` really is live at `docs/work/active/wi-280-bounded-core-decomposition/`, and the WI's own spec carries `needs = ["~WI-280"]` (soft), so proceeding on S6–S8 while deferring the anchor is exactly what that edge licenses. **The anchor split is out of scope for this round.** -> no remedy owed -> @owner
- [MINOR] tests/test_agent_loop_worker.py:45 (`pytestmark = env_gate_skipif("git")`) against the block header at :511-517 -> The one place the split is **not** behavior-preserving, and the inline comment asserts the opposite. Three of the eleven functions moved into the worker module are pure — `test_worker_exit_banner_returns_code_and_prints` (:620), `test_build_worker_assignment_is_none_without_wi_and_train` (:641), `test_parse_args_defaults` (:672) — they touch no git, and in trunk they lived in `tests/test_agent_loop.py`, which carries **no module-level `pytestmark`**, and they carried no per-test gate either. The worker module's pre-existing module-wide git skipif now applies to them. Driven, both sides, with git removed from PATH (`PATH=/c/Windows/System32:/c/Windows`): trunk `pytest tests/test_agent_loop.py -k "<the three>"` → **3 passed, 118 deselected**; branch `pytest tests/test_agent_loop_worker.py -k "<the three>"` → **3 skipped, 31 deselected**. So on an ungated machine three pure seams stop running where they used to pass. The header comment at :515-517 states "the module-level `pytestmark = env_gate_skipif("git")` already covers the per-test gates the moved tests carry" — these three carry none, and the mark *adds* a gate rather than covering one; the fragment's "That is what makes the split behavior-preserving" (:35-37) and "No test was renamed, re-tiered..." (:9) have this one exception. Impact is genuinely narrow: git is present on all three CI legs and on the bar machine (hence the identical 1714/557/1157 and the reproduced 1701/1/12), and the harness loudly banners "A pass total measured here is NOT comparable" when a gate is unsatisfied — but this suite's own `WI-333` note, which S7 was careful to move verbatim, is precisely about a safety net that silently stopped catching and nobody noticed because every machine happened to satisfy the gate. -> Either move the three pure seams to the ungated pure-parse home (`tests/test_agent_loop_policy.py`) — at the cost of splitting the Slice D/E block the builder deliberately kept whole — or keep the placement and fix the record: state in the comment that the mark **adds** a git gate to three previously ungated pure tests, and record the exception against the behavior-preservation claim in the fragment. -> @owner
- [NIT] docs/log.d/WI-277-test-monolith-split.md:14-17 -> The summary sentence added by the self-correction (34c7827) reaches the right conclusion through a description of the evidence that does not hold up, and this is the fragment's one signed mechanical claim. "the only **three** whose text differs are **those documented repairs** plus the `_vendor_core` copy note" names four items for three slots, and mis-states what a top-level-function comparison can see: `_vendor_core` is not a changed body but an added copy (trunk's original is byte-identical at `test_agent_loop.py:568`), and two of the "three comment repairs" (`test_agent_loop_routing.py:22` and `:320-325`) are **module-level section dividers outside any function**, which such a comparison cannot see at all. My stricter reproduction gives the clean number: of the 405, **exactly one** function differs (`test_parse_map_rejects_entry_without_equals`, the only repair that sits inside a body). The likely third slot in a naive name-keyed script is `_git`, which is not a difference: trunk already carried two distinct `_git` helpers (`test_agent_loop.py:154`, `test_agent_loop_worker.py:78`) that I diffed and found **semantically identical — the local variable is `proc` in one and `p` in the other, nothing else** — both survive unchanged, and last-wins keying collapses them into a phantom diff. (This also confirms the module's "its own equivalent `_git`" claim at :138 as literally true, so the moved tests are unaffected.) -> Restate to what the check actually shows: "all 405 survive; exactly one function body differs (the `parse_map` comment repoint); the other two repairs are module-level dividers; `_vendor_core` is a copy carrying a copy note and the trunk original is unchanged" — and, if `_git` was the third slot, name it as a comparison artifact rather than a difference. -> @owner
- [NIT] tests/test_trajectory.py:1-10 (against tests/test_trace.py:3-9 and tests/test_agent_loop.py:7-13) -> S6 is the only one of the three splits that left its parent without a forwarding address. S7 and S8 both added a "WI-277 split this module by behavior boundary... moved to X / Y" paragraph to the parent's module docstring; S6 did not — `tests/test_trajectory.py`'s docstring is byte-identical to trunk's, so a reader landing on the parent that lost **95 of its 151 tests** gets no pointer to `test_trajectory_staged` / `_arch` / `_specs`, while the other two parents hand it over. Same navigability argument the fragment itself makes twice ("a comment that outlives the code it documents is how the next reader gets misled", :113-114 and :163-164) — applied to the S7/S8 parents but not the S6 one. Relatedly, the fragment's enumeration of edits (:10-13: "the new module docstrings, the per-module import lines, the helper copies, and three comment repairs in S8") omits these two **parent** docstring edits, which are not new-module docstrings. -> Add the same one-paragraph pointer to `tests/test_trajectory.py`'s docstring, and add the parent docstrings to the fragment's edit list. -> @owner
VERDICT: APPROVE findings=3

### REVIEWER — G3 — Round 2 — 2026-07-31
Verdict: APPROVE
Findings:
- none against the round-1 findings — all three close at tip `b469e58`, delta-scoped and re-driven, and the MINOR one was fixed at the seam rather than papered over with a comment. **(a) The gate regression is really gone, and I drove it in both directions.** With git off `PATH` (`PATH=/c/Windows/System32:/c/Windows`), the three pure tests in their new home give **3 passed, 23 deselected** — exactly the builder's figure, against **3 skipped** on the round-1 tree; the whole `tests/test_agent_loop_policy.py` module is **26 passed** ungated, so nothing in it is gated by anything. I also checked the converse claim, which nobody asked me to take on trust: are the tests that *stayed* in `tests/test_agent_loop_worker.py` genuinely git-dependent, or is the mark now over-broad in the other direction? Ungated, the module reports **31 skipped**, which proves only that the mark fires — so I forced the harness's own override (`KIT_ENV_GATES_SATISFIED=1`) *while git was genuinely off PATH*, making every test run without git. Result: the Slice D/E block that stayed is **8 failed, 0 passed** (`worker_endstate` ×6 plus the two `build_worker_assignment` cases that resolve a real base) — every one of them truly needs git, so the gate is earned, not inherited. The same probe on the policy module is **26 passed**, confirming it needs no gate. The boundary is now drawn where behavior actually divides. **(b) The AST evidence survives the moves.** Re-ran my strict form at this tip — decorator-inclusive (a `FunctionDef` source segment omits decorators, so the naive form is blind to a mutated `@parametrize`) and duplicate-aware: trunk `5cc5ab6`'s three parents hold **405** top-level functions, **zero missing names**, and **exactly one differing body** — still `test_parse_map_rejects_entry_without_equals`, now at `tests/test_agent_loop_policy.py:283`; all **12** decorated functions' `@parametrize` blocks remain byte-identical, and the three relocated tests are byte-identical to trunk in their new home (they appear exactly once each, in the policy module). So the second move preserved text as exactly as the first. **(c) Tiers unmoved:** **1714 total / 557 smoke / 1157 slow**, all three equal to the round-1 tip; per module `test_agent_loop` 58, `_policy` 26, `_routing` 29, `_worker` 31 = **144** = the pre-split `121 + 23`. Both destination stems are already in `SLOW_MODULES`, so the relocation crossed no tier line — which is why "no test was re-tiered" survives the fix. **(d) The new comments are true, not decorative:** `tests/test_agent_loop_policy.py` carries **no module-level `pytestmark`** (verified by AST scan, and empirically by the 26-passed ungated run), and `tests/test_agent_loop_worker.py:45` really does carry `pytestmark = env_gate_skipif("git")`, so the worker block header's "would ADD a gate to any pure test placed under it" and the policy module's converse header are both accurate; the false "already covers the per-test gates" sentence is gone. The S8 table's redistribution verifies exactly by AST — of the parent's 111 test functions, **56 + 21 + 26 + 8 = 111** land in parent/routing/policy/worker — and all three parent docstrings now carry the forwarding pointer, closing the round-1 NIT. The round-1 evidence NIT is closed too: the fragment's restated paragraph (:15-30) now matches my measurements clause for clause, including `_vendor_core`'s original at `test_agent_loop.py:568` and the `_git` phantom at `:154`/`:78` differing only by a `proc`/`p` local. **(e) Bars at this tip:** full unfiltered `pytest -q -n auto` = **1 failed, 1701 passed, 12 skipped in 743.24s** (1701+1+12 = 1714, the collected total), sole red the standing `test_check_lane.py::test_this_repo_is_not_a_work_branch`; smoke **1 failed, 552 passed, 4 skipped**; `check_docs` configured step `OK - 331 doc(s), 933 intra-repo link(s), 0 broken`; `check_trajectory --strict` `clean (375 work item(s), 359 done (96%), 14 retired, graph acyclic)`; `ruff check tests/` clean including `--select F401`, so no import was orphaned by moving the three tests out. The fragment's "The behavior-preservation claim holds only after this fix; before it, this slice had exactly one exception" (:165-166) is the honest sentence — it records the defect rather than quietly erasing it, and the `331 (330 before the round-1 review doc landed)` attribution is likewise correct and correctly explained. **The anchor split (`tests/test_gen_trajectory.py`, S1–S5) remains out of scope for this round**, still deferred behind the live `WI-280`. -> no remedy owed -> @owner
- [NIT] docs/log.d/WI-277-test-monolith-split.md:228-229 -> The one claim in the new post-fix subsection that does not name what it measured. "The affected modules together: `297 passed, 1 skipped`" is **true** — I reproduced it exactly — but only for one particular set, and the obvious readings all miss: the eleven modules this WI split collect 376; the four `agent_loop`-family modules give `143 passed, 1 skipped`; the three modules the fix commit actually touched give `113 passed`; `agent_loop` + `trajectory` families give `294 passed, 1 skipped`. The figure reproduces only for **`agent_loop` family (4) + `trajectory` family (4) + `test_smoke_tier.py`** = 144 + 151 + 3 = 298 → `297 passed, 1 skipped`, and the lone skip is `tests/test_agent_loop.py:1296` ("advisory-lock degrade is POSIX-only"), pre-existing and environmental rather than anything this WI did. It took four separate invocations to find the set, which is the friction this repo's named recurring defect is about — a signed number a later reader cannot re-derive is indistinguishable from one that was never run. Everything else in that subsection names its command or its scope precisely. -> Name the set inline — e.g. "the `agent_loop` and `trajectory` families plus `test_smoke_tier`: `297 passed, 1 skipped` (the skip is the POSIX-only advisory-lock case)" — or drop the line, since the full-suite total two paragraphs up already covers it. -> @owner
VERDICT: APPROVE findings=1

### REVIEWER — G3 — Round 3 — 2026-07-31
Verdict: APPROVE
Findings:
- none against the anchor split (S1–S5), the trunk merge or the close — this is the round where the WI's central risk was largest (a 5,359-line monolith into eight modules plus the one shared module the WI sanctions) and it comes back clean on evidence I produced. **(a) Nothing lost.** Re-ran my stricter method against the pre-split anchor at `e1269cf`: decorator-inclusive (a `FunctionDef` source segment omits decorators, so the naive form is blind to a mutated `@parametrize`) and duplicate-aware, over all nine files. **Zero missing, zero extra, zero duplicated, ZERO bodies changed, 163 test functions before and after.** I reconciled the builder's headline number rather than taking it: counting every *bound* name including tuple-unpacking targets gives **282 → 282** exactly as claimed; counting only defs plus simple assignments gives **278 → 278**. Both agree, and the structural census is identical on each side — `FunctionDef` 226=226, `Assign` 53=53, `ClassDef` 1=1, symmetric set difference empty. Per module: facade 14, parse 5, graph 25, views 37, panels 31, render 32, sweeps 12, status 7 = **163**, the anchor's exact count. I also re-ran the S6–S8 comparison at this tip to confirm the trunk merge did not disturb the earlier slices: still zero missing names, and the trajectory family is 153 tests = the original 151 + WI-280's 2. **(b) The shared module is disciplined, not a dumping ground.** I measured every one of the 34 names rather than the six I was pointed at. All six extras are genuinely multi-module and the table's per-test counts verify: `_wcag` panels(1)/render(4)/sweeps(2) — **three** modules, so the plan's "used by exactly ONE module" really is refuted; `_css_var` 2, `_style_surfaces` 2, `_palette_vocabularies` 2, `SMALL_WIS` 2, `_layer_with` 2. Of the other 28, 12 are directly multi-module and **16 are imported by one module or none — but every one of them traces to a multi-module builder inside the file**, which is the only reason they can live there: `BLOCKREF`→`write_wis`→`make_repo` (8 modules), and `CONT_ARCH`/`CONT_LLRS`/`CONT_CMPS`/`CONT_IFS`→`containerize`→`_every_emitter_document` (3 modules). Nothing in the file is an orphan. The converse discipline holds too: I checked all fourteen names the fragment lists as having moved with their single module (`make_status_repo`, `status_text`, `block_of`, `_know_section`, `_loops_div`, `_hero_of`, `_landing_dashboard`, `_label_boxes`, `_spine_with_sns`, `sw_section`, `_sample_path_d`, `_viewbox_of`, `_scrambled_spine`, `_SubprocessShim`) — **every one is absent from `traj_fixtures.py` and used by exactly one module**. `_every_emitter_document` is byte-identical with its owner-ruling docstring intact (it falls out of the zero-bodies-changed result), and **no test module imports another** — `traj_fixtures.py` has no `test_` prefix so it is never collected, which keeps the S6–S8 idiom intact rather than breaking it. **(c) Tier membership.** 1716 total / 557 smoke / 1159 slow at the tip **and identical at the pre-anchor baseline `e1269cf`**, which I measured in a throwaway detached worktree — and flat at **all five slice commits** (S1 1716/557/1159, S2, S3, S4, S5 the same), with the facade walking 163→125→88→44→14 underneath. Every one of the seven stems joined `SLOW_MODULES` **in the commit that created the module it names** (S2 status+panels, S3 views, S4 render+sweeps, S5 graph+parse), and S1 really is the zero-risk proof-of-mechanics commit it claims: all 163 tests still in the parent. The derived guard bites three distinct ways, proven by monkeypatching in a scratch interpreter with no file edited: a brand-new `tests/test_traj_brandnew.py` absent from `SLOW_MODULES` fails it by name, deleting all traj modules trips the explicit vacuity assert, and dropping `test_traj_render_sweeps` from `SLOW_MODULES` still fails the literal half. **(d) The three deviations — I accept all three.** The T7 scale-to-fit pair to `test_traj_views` keeps `_spine_with_sns` from becoming a third shared fixture, and the section banner is one behavior; the `u3_sw_drill` test to views keeps `sw_section` single-homed, which I verified it is. The `import sys` drop is the only edit that is not a pure move, and it holds up: **no production file changed at all** (`git diff --name-only e1269cf HEAD` touches nothing outside `tests/` and `docs/`), so the facade itself is untouched by this WI. I reproduced the diagnostic by re-inserting the module-level import into a scratch copy — ruff emits **both `F401` ("sys imported but unused") and `F811` ("Redefinition of unused sys from line 21")**, so the fragment's "F811" is accurate. The local import is the right one to keep: `sys.` appears nowhere else in the module, `test_gen_trajectory_self_heals_sibling_import` is byte-identical to trunk, and it genuinely exercises the guarded path — it strips `SCRIPTS` from `sys.path`, clears both modules from `sys.modules`, loads `gen_trajectory.py` by spec and asserts `mod.ct is not None`, i.e. the except-branch re-added its own directory. **(e) The merge resolution is correct.** All three `_render_surface_paths`-family tests are **byte-identical to trunk's `0fc58fb`** version, live in `test_trajectory_staged.py` (the critique-staleness pair sits inside the WI-243 perceptual-re-fire section whose watched file set they pin, with `ROOT` imported), and **none was left behind in the parent**. `test_render_surface_covers_the_whole_generator_family` still calls `ct._render_surface_paths(ROOT)` and asserts coverage of the whole generator family — post-WI-280 that is precisely the test that catches the decomposition drifting out of the staleness watch list, so it pins what WI-280 intended. **(f) Green.** Full unfiltered suite **1 failed, 1707 passed, 8 skipped in 349.37s** (1707+1+8 = **1716**, the collected total), sole red the standing `test_check_lane.py::test_this_repo_is_not_a_work_branch`; `check_docs` configured step **OK - 333 doc(s), 934 intra-repo link(s), 0 broken**; `check_trajectory --strict` **clean (376 work item(s), 361 done (96%), 14 retired, graph acyclic)**; `ruff check .` all passed and `ruff format --check tests/` 98 files already formatted; the split family whole **174 passed**, which is the anchor's 163 plus the 11 in the untouched `test_gen_trajectory_pending.py` exactly as the fragment says. The archived Deliverable verifies claim by claim — the redistribution arithmetic (151→56+25+45+25; 81→44+21+16; 111→56+21+26+8), the 34-name fixture module, the "measured, not assumed" membership and the `_wcag` refutation, both AST results, the fourteen stems, and its honest paragraph crediting the round-1 MINOR and the converse drive — with the three record slips below. My probe worktree is removed and the tree is clean; the only file I touched is this review. -> no remedy owed -> @owner
- [NIT] docs/work/archive/WI-277-split-the-test-monoliths-by-stable-behav.md ("a 286-line parent") and docs/log.d/WI-277-test-monolith-split.md:267 + :269 (inventory table, `286` and `769`) -> Three signed line counts went stale one commit before they were written, and one of them is in the permanent archived Deliverable. Measured at this tip: `tests/test_gen_trajectory.py` is **295** lines, not 286, and `tests/test_traj_graph.py` is **770**, not 769. The cause is visible in the history: both figures were true at S5 (`61f56e7` — I measured 286 and 769 there), then `2a084ec` added the forwarding-pointer docstring (+9) and the corrected patch-site note (+1). Both are docstring-only and both are *good* changes, but they landed **before** the log fragment (`10e0563`) and the close (`053e1df`) were authored, so the record was written against a tree that no longer existed. The irony is that the neighbouring commit `48146a3` exists precisely to "state the anchor's real line count" — it fixed the `5,3xx` placeholder in `traj_fixtures.py` (correctly: 5,359 verifies) while the parent's own count silently drifted. Every other number in both documents re-derives exactly, which is what makes these three stand out. -> Re-stamp to 295 and 770 in the inventory table and to 295 in the archived Deliverable, or state the metric and the commit the counts were taken at. -> @owner
- [NIT] docs/log.d/WI-277-test-monolith-split.md:318 (the six-name table, `_layer_with` row) -> One cell in the table that carries this WI's central fixture-membership argument is off by one. Claimed "views (3), render (3)"; measured by counting the test functions that reference the name, **views 3, render 2**. The other five rows verify exactly (`_wcag` panels 1 / render 4 / sweeps 2; `_css_var` 1/2; `_style_surfaces` 2/2; `_palette_vocabularies` 1/1; `SMALL_WIS` 2/2), and the row's conclusion is unaffected — `_layer_with` is still used by two modules, so it still belongs in the shared module. It is only the count that is wrong, in the one table a future reader will re-run when deciding whether a name has earned its place there (the docstring's standing instruction tells them to). -> Correct the cell to "views (3), render (2)". -> @owner
- [NIT] docs/log.d/WI-277-test-monolith-split.md:430-436 -> The close bar is signed as "the full unfiltered suite ... run over the final tree", but it was measured one commit before the final tree and does not reproduce at the tip. Recorded: `1 failed, 1703 passed, 12 skipped`. Driven at `053e1df` in a fully gated shell: **`1 failed, 1707 passed, 8 skipped`**. Both reconcile to 1716 and both carry only the standing work-branch red, so nothing is lost — but the 4-test difference is worth recording rather than leaving as an unexplained discrepancy, because the cause is this WI's own ceremony. I isolated it: `tests/test_wi_convert.py` gives **4 skipped at `e1269cf` and 0 at the tip**, the skip reason naming `active/wi-277-test-monolith-split` — "an in-flight claim ... conversion is a drained-stop operation". The close commit archives WI-277's own spec, which drains the claim and **un-skips four tests that this WI had been suppressing while it was open**. So the recorded 12 is the honest number for a pre-close tree and the tip's 8 is the honest number for the merged one. (For completeness, the split is shell-sensitive in the ordinary way too: run from PowerShell, where `sh` is not on PATH and the `posix-shell` gate is unsatisfied, the same tip gives `2 failed, 1672 passed, 42 skipped` — the extra red being `test_prereq_toolchain::test_every_required_environment_gate_is_satisfied`, exactly the gate reporting itself.) -> Re-stamp to the tip's `1 failed, 1707 passed, 8 skipped`, with the one-line reason the count moved (the close drains this WI's claim, un-skipping four `test_wi_convert` drained-stop cases) — it is a genuinely interesting property of the close ceremony and belongs in the record. -> @owner
VERDICT: APPROVE findings=3
