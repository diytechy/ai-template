# oi85-p9r-selector-closure-structured — adversarial review

Requested route: Opus 5, high; declared CLI stream-json pattern.

Subject SHA256: `c3a38670df25682c5561a7cc5a4dbe6e0775ecd0c53b5c7633f7e46416619d0a`

Tracked invocation: [session log](../iteration/call_59b436d2d4e84d96913b44b8de089740-20260906-161311.log).

Review uses supplied source with tools disabled. It is an independent
assessment, not a requirement approval or a test execution.

{
  "verdict": "APPROVE",
  "findings": [
    "CLOSED (was MAJOR) — stale-generated-output loophole. `GENERATED` now carries `docs/open-items.html` and `docs/requirements/components.derived.toml` as whole-owned companions, and `commands()` appends the existing `gen_open_items.py --root . --check` and `gen_components.py --root . --check`. Scenario A (LLR `module` value edit, str->str, `data_only` True) and Scenario B (open-items OneLine/Recommendation edit) now regenerate-and-compare the exact surfaces they stale, so the inverted incentive is gone: regenerating those two files no longer lands an unclassified path, and leaving them stale no longer yields a green narrow bar. `test_narrow_commands_keep_every_affected_generated_surface_fresh` pins all eight freshness fragments by string, and `test_whole_generated_companions_can_follow_a_classified_data_edit` pins the companion-only semantics.",
    "CLOSED (extension accepted) — the same root cause reached two further current-tree surfaces and both are covered. `docs/ratify/CURRENT.md` is a whole-owned companion with `trace.py --root . --approve modified --check` (check.py `approval-fresh`, which compares against the baseline the brief itself declares); `docs/cli-reference.md` and `docs/interface-reference.md` are marker-region companions with the two `gen_arch_map.py --check` invocations. The `--src project-trajectory/scripts` literal matches this repo's `[paths] src` and all four CLIs were observed exiting 0 on the current tree. The single marker-region helper (`generated_region_prose_unchanged`, exactly one pair + byte-identical head/foot) is reused for all three region files rather than duplicated per surface.",
    "WITHDRAWN (was MINOR) — `docs/requirements/components.toml` is not dead membership. `trunk_step._components` resolves it via `spine_carrier.resolve` as the authored component carrier, separate from the generated `components.derived.toml`; the two are correctly split across `REGISTRY_DATA` and `GENERATED`, so a hand-edit of the derived view still cannot narrow. `test_authored_component_carrier_is_real_registry_data` is non-vacuous — removing the entry flips the case to `full`.",
    "OBSERVATION (non-blocking, no change requested) — deletion asymmetry on whole-owned companions. The `GENERATED` branch of `unknown` has no `is_file()` guard (unlike the `CORE_ONLY` branch, and unlike `REGISTRY_DATA`/marker-region files, which broaden because `read_text` raises). So deleting a whole-owned output alongside a classified primary edit still narrows. Four of the five fail closed anyway (the matching `--check` errors on the missing file while its registry is present); the one that does not is `docs/ratify/CURRENT.md`, since `approval-fresh` is documented as silent with no brief. Consequence is bounded: the deletion can never authorize narrowing alone (`primary` excludes `GENERATED`), CI and the phase-close Full bar are untouched, and deleting the live brief is not a routine edit. If the owner ever wants it aligned with the stated deletion rule it is one token — `and (root / path).is_file()` on that branch — and it does not disturb any existing case.",
    "OBSERVATION (non-blocking, currently unreachable) — OKF is correctly excluded today: `[checks] okf_export = false` and no tracked `docs/okf/`, and both the policy file (`docs/stack.ini`) and any `docs/okf/` path are unclassified, so flipping the policy is itself a Full change. The residual is only the change *after* such a flip: a same-shape registry edit would narrow while a forgotten OKF regeneration goes unchecked. Noting it as the membership owner's future item, not asking for it now — adding speculative coverage for a disabled export is exactly the defensive code this review excludes."
  ],
  "basis": "Reviewed with tools disabled against the supplied final `scripts/check_changed.py`, its complete 44-case test module, the P9R section-2 contract and work package, the check.py declared freshness steps, trunk_step's REGEN_STEPS ownership table, and the stack.ini `[generated]` ownership rows. Closure of the prior MAJOR verified by re-running its own two scenarios through the final `select`/`commands`: both now emit the previously missing `--check` invocations. Completeness of the extension verified by walking every row of the declared generated-ownership table against the narrowable input sets (REGISTRY_DATA, WI frontmatter, CORE_ONLY) and confirming each derived surface either has a narrow freshness command, is self-enforced by a retained core test (the module-size ratchet), or derives only from inputs that broaden to Full (skills INDEX, prompt CATALOG, verdict rollup, dated ratify briefs, OKF). The withdrawn MINOR was re-checked against trunk_step's `_components` carrier resolution. Independently re-derived the previously sound properties rather than restating them: `and`/`or` precedence in the `unknown` comprehension groups as three terms; `--no-renames` turns renames into delete+add and the delete broadens; non-ancestor, absent, unreadable and non-UTF-8 bases return None -> Full; generated-only proposals hit the `primary` empty branch -> Full; `shape` broadens on new rows, new fields, type changes and malformed TOML; exactly one representative HTML boundary case with the seven-module HTML census omitted and the four shared core modules retained; failure propagation stops at the first nonzero. Fresh evidence accepted as reported (44 passed in 12.46 s, ruff check/format and git diff --check clean, all five freshness CLIs exit 0, failure-first collection error before implementation); no test execution was performed in this review. No new validator, wrapper, policy or impact engine was introduced, and nothing wires the selector into CI or the phase-close bar. The one deletion nuance and the one conditional OKF gap are recorded as non-blocking observations because neither is a defect in delivered behavior and neither meets the concrete-defect bar for this closure review."
}
