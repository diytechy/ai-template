Task exercise: project state — 1 tab switch / 1 click (`Process`, where `Current gate: G2` and lifecycle state are named); next work — 0 tab switches / 0 clicks (`NEXT WORK` on the landing view); how the parts connect — 1 tab switch / 1 click (`How (SW architecture)`).

- [MAJOR] T2 -> the shipped landing `What` view in `1280px-light-arch-full.png` opens the 25-SN / 110-SR / 112-LLR / 115-TC icicle fully expanded into a multi-screen wall even though every tier exceeds the `>3` threshold -> give the What decomposition the same threshold-driven, start-collapsed tiering used by When and Knowledge, retaining the flat icicle only for small registries -> @owner
- [MAJOR] T4 -> the initial 390px How render clips the right-hand `CMP-002 — Generators` node and its label at the diagram edge (`390px-light-sw-full.png`), so the label is not readable at default zoom -> use a 390px topology that keeps every node and its full label inside the visible diagram while preserving the default label size -> @owner
- [MAJOR] T5 -> the When diagram's top-right containment/descend arrow (`.cedge`) is only 1.06:1 in light (`#4f46e5` on `#0369a1`) and 1.99:1 in dark (`#818cf8` on `#0369a1`), below the 3:1 graphical/UI-boundary floor in both shipped themes -> derive the arrow and marker stroke from each node fill's contrast-safe control token (such as its computed `--ring`) so every expand/descend affordance reaches at least 3:1 in both themes -> @owner
- [MAJOR] T7 -> the 390px What, When, How, and Knowledge initial views explicitly require “Scroll sideways to see the full view”; their screenshots cut off right-side lanes or nodes rather than reflowing (`390px-*-{arch,dag,sw,know}-full.png`) -> replace the narrow internal horizontal canvases with stacked or compact responsive layouts whose complete initial view fits the 390px viewport without sideways scrolling -> @owner
VERDICT: CHANGES-REQUESTED findings=4
