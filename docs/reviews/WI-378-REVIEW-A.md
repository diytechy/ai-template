### REVIEWER — G3 — Round 1 — 2026-08-01
Verdict: CHANGES-REQUESTED
Findings:
- [MAJOR] docs/work/complete/WI-378-ratify-verdict-freshness-loop.md:28 -> **The census counts 4 of the 20 merged branches the predicate has governed, and the four were the ones the session brief handed over — not a population the row derived.** The freshness comparison is not new: `code_time`/`_last_commit_time`/the `vtime < code_time` refusal all landed WITH `integrate.py` in `e1cf5743` ("Phase 4a: the local integrator lands"), driven — `git log --reverse -S"code_time" -- project-trajectory/scripts/integrate.py` returns exactly two commits, `e1cf5743` and `37dfa9ee` (WI-386's `_work_tip` peel, which only makes the gate *more* permissive and touches no branch here). `docs/review-policy` has read `1` since `274c64be`, long before any of these merges. So every one of the 20 `integrate: merge` commits on `ConcurrencyTrainRewrite` merged under this exact predicate, and `merge-base --is-ancestor e1cf5743 <merge>` is true for all 20. I re-ran the row's own method — walk the branch's first-parent commits from `merge-base(^1,^2)`, treat each commit touching `docs/reviews/WI-*-REVIEW-*.md` as a round, read the last `VERDICT:` in the file at that commit, count a round only when an APPROVE was followed (before the next round) by a commit touching something outside `docs/reviews/` + `docs/log.d/` — over all 20. **Within the four censused branches I reproduce the row exactly: WI-280 4 rounds / 2 staled, WI-380 3 / 1, WI-384 5 / 3, WI-386 5 / 3 = 9, of which 2 are `docs/work/`-only, and every named staling commit matches.** But three uncensused branches contribute four more, all verified by hand: **WI-371** (`4073a6de`) — round-1 verdict `VERDICT: APPROVE findings=2` at `2cad638a`, staled by `17d70468` whose entire diff is `docs/work/archive/WI-371-shoot-mjs-deletes-session-baselines.md | 7 +++---`, i.e. **a `docs/work/`-only record edit that followed its own verdict — a third member of the class the row reports as exactly two**; **WI-374** (`8ffc6f89`) — two stalings, `5ab57a7e`'s `APPROVE findings=3` staled by `959a7d91` (`project-trajectory/scripts/drive.py`) and `7b07d699`'s `APPROVE findings=0` staled by `ad2acd23` (`docs/requirements/low-level-requirements.csv`, `docs/test/test-cases.csv`), both "the gate working"; **WI-277** (`8bde0a60`) — `eb645db5`'s `APPROVE findings=1` staled by nine commits (`e1269cf5` trunk merge + the S1–S5 test splits + `053e1df0` close), "the gate working". **Corrected totals: 13 staled APPROVEs — 9 the gate working, 1 trunk moving, 3 a record edit following its own verdict; the `docs/work/` exclusion would suppress 3, i.e. 23.1 %, not "2 of 9".** Two consequences. (a) The sentence *"Those two are the **only** ones an exclusion of `docs/work/` would have suppressed"* (:47) is a falsified universal — `17d70468` is a third. (b) The wrong figures are already stamped into five surfaces, two of which **ship downstream**: `project-trajectory/PROCESS_OPTIONS.md:410` ("nine APPROVEs staled — six … one … two"), `project-trajectory/scripts/integrate.py:549` (the `_verdict_gate` docstring, "nine staled APPROVEs … only two"), `docs/log.d/WI-378-verdict-freshness-ordering.md:23`, this Deliverable, and `docs/specs/WI-392.md:39` ("only **2 of 9** … ~22 %"). This is precisely the failure mode the row itself filed WI-392 against — a driven figure whose *population* is not stated cannot be told apart from a stale one — and the row inherited its population from a brief without re-deriving it, having correctly refused to inherit two other figures from the same brief. -> Re-drive the census over all 20 integrator merges (the walk is mechanical; my script is `scratchpad/rev378_all.py`) and re-state **13 / 9-1-3 / three-not-two / 23 %** in all five surfaces — or, if a narrower population is genuinely intended, say in the record which branches were censused and on what stated principle, so the figure is re-derivable. Note the correction *strengthens* the decision, it does not weaken it: the new record-only case is a **`Deliverable` prose correction**, the exact field the row argues a reviewer must re-read. -> @builder
- [MINOR] project-trajectory/skills/byte-budget-guard/SKILL.md:26 -> **The inherited +846 is attributed to three rows; it belongs entirely to one.** The stamp reads "**+846 was inherited unstamped** — WI-380/384/386 all landed 2026-08-01 without re-stamping". Driven, `git show <merge>:project-trajectory/PROCESS_OPTIONS.md | wc -c` at each merge: `0fc58fb6` (WI-280) **163157**, `8c4d5f78` (WI-380) **163157**, `979d8e09` (WI-384) **164003**, `c2a9af17` (WI-386) **164003**, branch base `02b2901e` **164003**, HEAD **165557**. So WI-380 contributed **0** bytes and WI-386 contributed **0**; the whole +846 is WI-384's, in `e869770d` + `eb1b25f5` (`git log 8c4d5f78..979d8e09 -- project-trajectory/PROCESS_OPTIONS.md`). The three-way phrasing is true about the *failure to re-stamp* and false about the *bytes*, and a successor reading the stamp will carry the wrong attribution forward — which is the same class as F1, in the record whose whole job is provenance. The arithmetic itself is right: 165557−164003 = **+1,554** this row's, 164003−163157 = **+846** inherited, 165557−163157 = **+2,400** total; `AGENTS.template.md` 9,991 (9 bytes of headroom) and `PROCESS.md` 64,319 are both byte-identical to base, verified. All three materialized copies (`project-trajectory/skills/`, `.agents/skills/`, `.claude/skills/`) are md5-identical for both edited skills. -> Say "+846 inherited, all of it WI-384's (`e869770d`, `eb1b25f5`); WI-380 and WI-386 changed the file by 0 bytes but likewise did not re-stamp." -> @builder
- [MINOR] docs/work/complete/WI-378-ratify-verdict-freshness-loop.md:103 -> **"Evidence that ordering is the right remedy" over-reads an n=3 sample, and the uncensused population contains its counterexample.** The literal claim holds — I verified `merge-base --is-ancestor` and timestamps for all three: WI-380 close `8c023497` (1785561882) precedes round 1 `b2093871` (1785562863); WI-384 `9fcb8702` (1785564159) precedes `184760c2` (1785565667); WI-386 `07652d37` (1785563619) precedes `9556c667` (1785565042); and no window on any of the three is staled *solely* by a close ceremony. But the zero is partly masking, not elimination: **WI-384's `dba18f2a` ("re-close the record against the merged tree", `docs/log.d/` + `docs/work/complete/` only) and WI-386's `1329bd4e` (`docs/log.d/` + `docs/work/archive/` only) are both post-APPROVE record edits inside a staling window** — the very pattern rule 1 forbids — and cost nothing only because a real code change (`277cb7b9` ADOPTING.md; `5cbaa063` integrate.py) shared their window. And WI-371, which the census omits, is the direct counterexample: it **closed before its verdict** (`a83c2ed1` close → `2cad638a` APPROVE) and *still* bought a `docs/work/`-only round, because a composed-bar doc-refs red forced a `Deliverable` prose fix after the APPROVE (`17d70468`'s own message says so). So the honest reading is: the ordering retires the *close-ceremony* sub-class, which is real and worth writing down; it does not retire the record-edit class, and three branches whose windows all happened to contain code changes cannot show that it does. -> Scope the claim to what the sample supports — "no window was staled solely by a close ceremony across the three; two of the three nonetheless re-touched their close record after an APPROVE and were masked by a concurrent code change" — and add WI-371 as the case showing the ordering is necessary but not sufficient. -> @builder
- [NIT] project-trajectory/PROCESS_OPTIONS.md:404 -> **The canonical home's dichotomy misclassifies one of its own nine.** "every commit after an APPROVE buys another round — correct when it changed what ships, avoidable bookkeeping when it did not. Two ordering rules retire the avoidable half" splits the census into ships-changed (unavoidable) and didn't (avoidable). WI-380's `c42e370b` changed nothing that ships (its whole diff is `docs/log.d/` + `docs/work/archive/`) yet is not avoidable by either ordering rule — it is a correction the round-2 verdict itself demanded, which is exactly why the row declines option (b) three paragraphs of the Deliverable later. Both the `session-protocol` SKILL.md bullet and the `_verdict_gate` docstring carry the missing qualifier ("A correction the verdict itself demanded still costs a round: that is the gate working, not a defect"); the shipped canonical text does not, so a downstream reader gets the version whose own census contradicts it. -> Carry the SKILL.md sentence into the PROCESS_OPTIONS paragraph (one clause), or drop "the avoidable half" for "the two avoidable cases". -> @builder
- none — WHAT HELD, driven rather than taken. **`_verdict_gate` is behaviourally unchanged, byte-level:** the file's only diff against `02b2901e` is `16 0` (16 additions, 0 deletions), every added line inside the `_verdict_gate` docstring; and parsing both revisions, stripping every module/class/function docstring and dumping the AST gives an identical tree (`sha256` prefix `e25093db8721566f` both sides). **Correction 2a holds and refutes the spec's premise:** `8311c75c` touches exactly `docs/gate`, `docs/requirements/system-requirements.csv` and `docs/reviews/WI-280-REVIEW-A.md` in one commit, so `git log -1 --format=%ct 8311c75c -- . ":(exclude)docs/reviews" ":(exclude)docs/log.d"` and `… -- docs/reviews/WI-280-REVIEW-A.md` both return **1785508039** — equal, and the predicate is a strict `<`, so the ratifying flip could not stale itself; the file at that commit ends `VERDICT: APPROVE findings=0`; `99a05967` (whose entire diff is the two `docs/work/` spec paths) is what refused. **Correction 2b holds in both directions:** WI-384's extra rounds 4 and 5 were staled by `03cd6353` (`docs/declared-absences`) and `10e59e2d` (`check_doc_refs.py`), WI-386's round 5 by `97059fe9` (`tests/test_wi_convert.py`) — all touch `docs/work/` too, and excluding it suppresses none, so the brief's four collapse to one; and no *additional* `docs/work/`-only case hides inside those four branches (my independent classifier finds exactly the two the row names). **The quoted WI-380 counterfactual reproduces to the digit:** `code_time` with `docs/work` also excluded = **1785563826** vs `verdict_time` **1785564586** — it would have passed. **The 6/1/2 sub-taxonomy is right within the censused four** (WI-384 ×3 and WI-386 ×3 each hold a real code or declared-doc change; WI-280's `ad2541d3` is the one window whose only non-`docs/work` content is a hand trunk merge). **WI-392 is well-formed and lands where it says:** it parses, `check_trajectory --root . --strict` is clean at 390 items / graph acyclic, `schedule.py ready` lists `WI-392 ordinary P0 down=0 path=0`, `docs/specs/WI-392.md` exists and the queued row's `specref` resolves, no id collision in `docs/work/`; its scope is the *durable half* of WI-386's reviewer's proposal (provenance, rung 1 presence-only, rung 2 named-not-assumed with the three real obstacles) and it correctly records the round cap as measured-and-declined — with the caveat that its "2 of 9 … ~22 %" inherits F1 and should read 3 of 13 / 23 %, which does not change its argument. **Bars, all run in this worktree:** `pytest -q -n auto` → **`1 failed, 1744 passed, 12 skipped in 423.60s`** (the one red is the standing `test_check_lane.py::test_this_repo_is_not_a_work_branch`, `assert 'wi-378' is None` — the checkout *is* a work branch; counts identical to the fragment's, wall differs with sibling worktrees running); `pytest -q -n auto -m smoke` → `1 failed, 560 passed, 4 skipped in 19.79s` (exactly as recorded); `check_docs.py --root . --ignore docs/test/report.md --ignore "docs/work/*" --stale` → `OK - 340 doc(s), 974 intra-repo link(s), 0 broken` exit 0; `check_trajectory.py --root . --strict` → `clean (390 work item(s), 366 done (94%), 16 cancelled, graph acyclic)` exit 0 (the SpecRef-staleness WARNs are WI-381/383/387/388/389/390's, not this row's); `check_doc_refs.py --root . --strict` → `OK - no dangling path or sym: references` exit 0; `ruff check .` → `All checks passed!`; `ruff format --check .` → `146 files already formatted`. Every figure the fragment states for these bars matches what I measured. -> no remedy owed -> @owner
- none — WHAT THIS ROUND DID NOT COVER, stated plainly. I did **not** re-drive WI-392's three false-figure table (WI-380's `2 failed, 7 passed`, WI-391's `109 links`, WI-384's "two false positives") — they are other lanes' records and this row only cites them. I did **not** evaluate the merge: trunk is one commit ahead at `0b4774f0` (a WI-391 claim bookkeeping commit), this branch has not merged it, and three sibling lanes are live; whether the composed tree bars is the integrator's problem, not this round's. I did **not** re-review the WI-280/380/384/386 work itself, only the git shape of their branches. I did **not** audit `PROCESS_OPTIONS.md` outside the new paragraph, the rest of `integrate.py` outside `_verdict_gate`'s docstring and the AST equality proof, or any test module. I did **not** run the G3-only steps (dupes census, coverage, arch-map) beyond the bar named in the brief. My own census is a replay of the predicate over history, not an execution of `integrate.py` at each historical tip — it shares the row's method and therefore its blind spots, save the population, which is the one thing I varied. -> no remedy owed -> @owner
VERDICT: CHANGES-REQUESTED findings=4
