# oi85-runtime-structured — adversarial review

Requested route: Opus 5, high; declared CLI stream-json pattern.

Subject SHA256: `1a0a7f836a65394be3c4f417545db96e28604a11361ed8c40bc33a4be809fd64`

Tracked invocation: [session log](../iteration/call_4a1411572bee4af285929224ae26a5e4-20260906-153424.log).

Review uses supplied source with tools disabled. It is an independent
assessment, not a requirement approval or a test execution.

{
  "verdict": "CHANGES_REQUESTED",
  "findings": [
    "BLOCKING — New H1/R-E divergence introduced by unnecessary normalization. spine_carrier.names_carrier (spine_carrier.py:482) applies str(rel_path).replace('\\\\','/') unconditionally. On POSIX a backslash is a legal filename byte, so plan_briefs._canonical_need_ref('docs\\\\requirements\\\\stakeholder-needs.md#SN-1') returns SN-1 (the new tests/test_hats.py case asserts this), while check_trajectory.specref_findings rejects the same string at check_trajectory.py:2861 ('does not resolve to an in-repo file') because root / pathpart does not exist on POSIX — it never reaches names_carrier. Same string, same box, two answers; before this diff both readers rejected it. The helper's own docstring states 'Registry references use POSIX paths on disk', which makes the folding unnecessary by contract. Fix by REMOVAL: drop the .replace('\\\\','/') from both lines and delete the backslash assertion, keeping only the PurePosixPath './'-collapsing the repair actually needs.",
    "BLOCKING — The changed hats test drops coverage of the regression it documents. tests/test_hats.py retains the comment about the composer refusing the root-relative spelling 'before 2026-09-06 while R-E resolved it clean', but the assertion moved from './docs/requirements/stakeholder-needs.toml#SN-1' to '././docs/...'. The single './' form is the actual documented failure mode; '././' is not a spelling anyone writes. Assert both rather than substituting.",
    "specref_findings can now abort the whole check run on a malformed needs registry. load_needs raises SystemExit when the TOML does not parse (spine_carrier.py:958) and resolve refuses a dual .md/.toml carrier; a findings function that raises suppresses every other R-E finding in the run. The deleted registry_ids docstring stated the contract being replaced: 'None is \"unknown, do not judge\" (the same contract as a markdown target's unreadable anchor set)'. Confirm check_trajectory already loads needs fatally upstream (likely); if not, guard the call or emit a finding. Either way the malformed case is untested — only empty is covered by the new test.",
    "Confirm the refusal covers every coordinator entry. agent_common.py:138 says the capture happens 'before either coordinator loop starts' — 'either' names two loops, but the guard sits only in agent_loop.main (agent_loop.py:4502). Drive mode is covered because _drive_entry is reached at agent_loop.py:4522, after the guard. The second coordinator loop must be identified and confirmed to route through agent_loop.main; otherwise the new docstring claim 'agent_loop refuses that unknown identity before it enters a coordinator role' is false for that path.",
    "The 'capture failed' sentinel now has two homes. agent_loop.py:4502 re-derives the meaning of LAUNCHED_SCRIPTS_FINGERPRINT is None, which agent_common already owns in running_scripts_moved. A one-line agent_common.drift_detection_available() keeps the contract in the defining module and reduces the finding-4 audit to a grep for one name — the kit's own single-home rule applied to the repair.",
    "Verify no surviving references to kitlib.spine.registry_ids, which is removed from both __all__ and the module body: any other importer (test, dual-plan composer, docs/registry-machinery-reference.md) breaks at import or goes stale. Also confirm the pre-existing test_specref_table_path_fragment_on_a_registry_is_a_finding targets the NEEDS carrier — if it uses any other registry it now falls to the anchor path and its 'names no such row' assertion cannot hold.",
    "The narrowing test asserts less than the change does. test_specref_does_not_treat_nested_config_tables_as_registry_rows covers pyproject.toml, but the change makes a fragment on ANY non-need TOML unjudged, including interfaces.toml#IF-0068. That is the accepted trade, but pin it with an assertion on a real spine registry plus a comment marking it deliberate, or a later reader will assume ID validation still applies to the other registries."
  ],
  "basis": "Source-only adversarial read of the supplied diff and quoted source (agent_common.py:90-160, agent_loop.py:4480-4540, check_trajectory.py:2825-2905, plan_briefs.py:274-339, spine_carrier.py:470-499 and 900-963) plus the four supplied test files. No commands executed and no test results relied upon. Scope limited to the two Fable follow-up repairs; the check_trajectory rendering-package hunk was not reviewed. Both repairs are structurally correct and each closes a real hole: the coordinator guard bounds the previously unbounded EXIT_RESTART/relaunch chain at one typed preflight exit that launchers do not re-arm, while leaving generic importers degrading rather than crashing; and need_ids(load_needs(...)) fixes a latent bug where registry_ids' 'frozenset(found) or None' made an EMPTY need registry read as unjudgeable so every SN fragment passed. The scope narrowing is not challenged — the deleted registry_ids docstring itself scopes the two-reader disagreement to the canonical need carrier, so narrowing to that carrier discharges the stated obligation. CHANGES_REQUESTED rests on finding 1: the diff introduces a fresh, concrete H1/R-E divergence on POSIX via a normalization the repair does not need and the helper's own docstring argues against, fixable by deletion rather than by adding a compensating check; finding 2 is a same-file coverage regression on the exact behavior the repair claims to restore."
}
