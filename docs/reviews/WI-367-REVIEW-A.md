### REVIEWER — G3 — Round 1 — 2026-07-30
Verdict: APPROVE
Findings:
- [MINOR] docs/work/archive/WI-367-viewbox-padding-for-wraparound-lanes.md:74 -> The paragraph headlined "No port-fan regression" itself prints the number that misses WI-366's letter: WI-366's clause is stated in CSS px ("strokes leaving a shared port should reach >= 8 CSS px separation within ~15 CSS px of the port"), its block-1 named site was recorded at exactly 8.00 with zero margin, and after the 0.9877 fit-scale that pair renders 7.90 CSS px at 1680px — so by the same measure WI-366 used, the letter at its flagship site is no longer met, yet the miss sits under a "no regression" heading and is absent from "Residue, stated" (which lists only the How-SW 2.23%). The render-scale-artifact classing is otherwise defensible: the geometry is byte-identical (independently verified — 413 `d` attributes identical in order between parent-script and HEAD-script regenerations of the same repo state), WI-366 recorded the same class for the How-SW 8-vs-10 finding, the separation measure is convention-sensitive at the few-percent level (WI-366-REVIEW-A finding 2), and WI-366's own pixel corroboration at +15 was 9.8/10.5 px gaps, which scale to ~9.7/10.4 — still clear. But a rendered regression of a recorded Done-when number is residue, and residue is where it should live. -> Copy the block-1 7.90 px line into the "Residue, stated" section as a recorded WI-366 letter-miss for the periodic advisory critique to judge. -> @owner
- [MINOR] docs/work/archive/WI-367-viewbox-padding-for-wraparound-lanes.md:31 -> Two record figures do not reproduce: (a) the sw-0 table row's "wire ink 0.0 .. 923.0" — no path in that layer reaches x=0.0 (class="wire" paths start at x=190.0, cedge arrows at 175.0; measured independently on both the shipped and the regenerated document); (b) "4 of 49 drill layers" holds only under an unstated counting that excludes the 25 archl-sn-* sub-layer SVGs — the emitted document carries 74 data-layer SVGs, of which 4 overflow. Every load-bearing figure reproduces exactly (when-0 −17.5/+19.0 over 0..692; when-1 −6.5/+11.0 over 0..585; when-22 −6.5 over 0..996; sw-0 +19.0 over 0..904; AFTER boxes -20 0 733 354 / -9 0 607 286 / -9 0 1005 490 / 0 0 925 150; zero outboard ink AFTER, x and y, over all 74 layers); these are record-hygiene errors only. -> Correct the sw-0 minimum and state the layer-count convention. -> @owner
- [MINOR] tests/test_gen_trajectory.py:4965 -> `_path_xs` reads `Q` precisely because "the loops diagram writes it, and it would otherwise be the first thing a successor moved under this wrapper" (gen_trajectory.py comment) — but no test pins Q: the new vocabulary test covers M/h, C, and the A-bail only, and the whole-document sweep's `_sample_path_d` drops Q paths (its M/L/C tokenizer leaves them under 2 points), so a regression that broke Q parsing in `_path_xs` would pass the entire suite and silently un-pad the first Q emitter moved under `_svg_frame`. -> Add one line to the vocabulary test: `assert gt._path_xs("M1.0,2.0 Q3.0,4.0 5.0,6.0") == [1.0, 3.0, 5.0]`. -> @owner
VERDICT: APPROVE findings=3
