### REVIEWER — G3 — Round 1 — 2026-07-30
Verdict: APPROVE
Findings:
- [MINOR] docs/log.d/WI-368-composed-tree-harness.md:34 -> The fragment states "Filed as its own item (WI-369) rather than papered over here", past tense, but no WI-369 spec exists anywhere at review time: not in docs/work/queued/, deferred/, active/, or archive/ on this branch, the trunk tip (b648341), wi-366-port-fans, or integrate/candidate — a tree-wide `git grep WI-369` hits only this sentence. The observation itself is accurate and reproduced (the omissions test reds with "SCAFFOLD_OMISSIONS entries now materialized: docs/work/active" while the wi-366 claim is outstanding), but the filing has not happened, and once this fragment compiles into the permanent log the record will assert a filing that may never occur — exactly the claim-only-what-happened bar the repo holds records to. -> Create docs/work/queued/WI-369-<slug>.md (the standing intake shape, cf. commit 59b5b4a) before or at this branch's merge, or amend the fragment to "filed at merge as WI-369". -> @owner
- [MINOR] project-trajectory/scripts/integrate.py:470 -> The Deliverable and docstring say discovery "mirrors the shipped pre-commit hook's scripts-dir probe", but the known-layout tuple probes only lowercase `scripts` and `project-trajectory/scripts`, while the hook (project-trajectory/hooks/pre-commit:84-98) additionally honors KIT_SCRIPTS_DIR and accepts `Scripts/` explicitly for case-sensitive filesystems. Consequence is narrow and fail-safe: only an out-of-root invoker driving a `Scripts/`-layout candidate on a case-sensitive FS silently falls back to the invoker's copy (the pre-fix behavior; the in-root shape still resolves via the root-relative join, which carries the real casing). Not a correctness defect, but the "one convention, two enforcement points" claim is broader than the code. -> Either add `Path("Scripts")` to the tuple or narrow the docstring/Deliverable to "mirrors the hook's default-layout probe (KIT_SCRIPTS_DIR and Scripts-casing excepted)". -> @owner
VERDICT: APPROVE findings=2
