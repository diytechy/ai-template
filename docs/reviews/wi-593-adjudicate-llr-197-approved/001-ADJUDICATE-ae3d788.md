# ADJUDICATE — WI-593 — amendment at ae3d788

Question judged, per row, and the only one: did the amendment change the requirement's MEANING, or only its CLARITY?

Scope verified independently against the anchor (`docs/archive/last_approved/docs/requirements/system-requirements.toml`, copied 2026-09-04 at a1d80c6f) rather than taken on trust: for all 17 rows the ONLY differing key is `rationale` — `title`, `statement`, `acceptance`, `sn_refs`, `hat_refs`, `boundary_refs` and `status` are byte-identical, and every row is still `Approved` on both sides. So no cell that states an obligation, a threshold, an actor or an acceptance condition moved at all; the amendment lives entirely in the explanatory tier.

Within `rationale`, every edit does one thing: it strips the prose `Hat-derived (hat.X):` roster-lens attribution (at SR-111/SR-112 also the two-sentence citation-home note that ended "the sentence above stands without the citation"; at SR-175 it restructures the same content in place). What survives in every row is the substantive charter clause the row answers — C-SEC-2, C-MNT-7, C-MNT-3, C-DPR-2, C-PRF-1, C-UNA-3/5, C-SEC-5, C-UXD-1/C-UXE-2 — and the whole obligation account. The deriving lens itself is not lost either: it is carried structurally in each row's `hat_refs` array (SR-024 `["TEST-ENGINEER"]`, SR-111/112 `["MAINTAINER"]`, SR-053 `["CONSISTENCY"]`, SR-175 `["DATA-PROTECTION","LEGAL","SECURITY"]`, …), which the amendment did not touch. The prose label was a paraphrase of a field; removing a paraphrase changes no obligation.

A builder acting correctly on the BEFORE text of any of these rows acts correctly on the AFTER text; no correct implementation of the old text could fail the new one, and none could pass the new one while doing something the old forbade.

- [CLARITY] SR-024 Rationale -> dimensional coverage is GENERATED from declared inputs, not hand-listed; negative/planted-failure tests remain a separate obligation -> same -> only the `Hat-derived (hat.TEST-ENGINEER):` label dropped; "Systematic expansion reduces the risk…" and the both-halves carve-out read identically.
- [CLARITY] SR-033 Rationale -> release gate carries a generated checklist surfacing warn-tier budgets for human tick-off; the row is the project's ANSWER to the charter question, not a charter-imposed obligation -> same -> only the `Hat-derived (hat.PERFORMANCE),` label dropped; the "no wider than what the charter actually asks" limit and the must-not-be-read-as-prescribed warning survive verbatim.
- [CLARITY] SR-043 Rationale -> enumerate the one irreversible actor-starting action with the dial/human authority that permits it; fail-open arm deliberate, human-held override never relaxed -> same -> only the `Hat-derived (hat.SECURITY):` label dropped; "C-SEC-2 asks…" and the entire WHY-THE-FAIL-OPEN-ARM clause are unchanged.
- [CLARITY] SR-052 Rationale -> keyboard operability, accessible name per element, colour never the sole channel, readable contrast floor; method mechanical, acceptance is the child chain -> same -> only the `Hat-derived (hat.ACCESSIBILITY):` label dropped; the charter-ruled-`always` and closed-dependency reasoning survives (and the lens it now refers to unnamed is still `hat_refs = ["ACCESSIBILITY"]`).
- [CLARITY] SR-053 Rationale -> one cross-view coherence property, clauses pinned separately, acceptance the child chain; fan-out is the anchor census, not a decision count -> same -> only the `Hat-derived (hat.CONSISTENCY):` label dropped; the sole-deriving-lens argument and the re-open-if-the-charter-is-cut note survive.
- [CLARITY] SR-054 Rationale -> task-level findability + legibility-as-robustness written from stakeholder intent; one unpinnable first-time-reader clause named as a limit at the child -> same -> only the `Hat-derived (hat.UX-DESIGNER + hat.UX-ENGINEER):` label dropped; C-UXD-1/C-UXE-2 and the both-`always` reachability claim survive.
- [CLARITY] SR-111 Rationale -> a recorded-origin stamp lets a maintainer identify the upstream version a scaffold came from and compute a re-sync diff; hash-derived versioning rejected -> same -> the trailing `Hat-derived (hat.MAINTAINER): C-MNT-7 …` block removed, including its own note that the obligation "stands without the citation"; that block asserted where a clause is DEFINED, imposing nothing on a builder.
- [CLARITY] SR-112 Rationale -> generated per-agent skill copies, forced fan-out, drift is a finding, generated-marking stops a maintainer editing a disposable copy -> same -> the trailing `Hat-derived (hat.MAINTAINER):` block and the "same disposition as SR-111" citation removed; the "identify itself as generated" substance is already carried by the surviving generated-marking sentence.
- [CLARITY] SR-129 Rationale -> the CAPABILITY is the converter as the check on a representation change; layout/claim-detection belong to LLR-136; the 140-cell lesson -> same -> only the `Hat-derived (hat.TEST-ENGINEER):` label dropped; "A converter is the check on a representation change…" and the cell-exact round-trip survive.
- [CLARITY] SR-144 Rationale -> immutable per-close record reconstructed from no mutable proxy; the split is a field because silence is unactionable -> same -> only the `Hat-derived (hat.UNATTENDED-OPS):` label dropped; the C-UNA-3 deadlocked-until-morning and C-UNA-5 pages-nobody clauses survive.
- [CLARITY] SR-146 Rationale -> steering prose ships as a reviewable file with a per-session digest; transport decision stays at LLR-163 -> same -> only the `Hat-derived (hat.SECURITY):` label dropped; "C-SEC-5 requires that content composed for dispatch … carry a DECLARED inclusion rule" survives word for word.
- [CLARITY] SR-147 Rationale -> ONE obligation: a single structured carrier, proved cell-for-cell by the converter BEFORE the authority flip; the measurement and the one-row argument -> same -> only the `Hat-derived (hat.TEST-ENGINEER):` label dropped; the enforcer-must-be-shown-to-bite clause survives.
- [CLARITY] SR-149 Rationale -> the vocabulary conversion is a standing CONDITION, with carve-outs that history is not rewritten and an attestation never re-worded -> same -> only the `Hat-derived (hat.MAINTAINER):` label dropped; "C-MNT-3 gives every declared vocabulary value exactly one normative definition in one home…" survives intact.
- [CLARITY] SR-167 Rationale -> the perf verdict itself, BOTH breach arms in one exit contract, naming no artifact; vacuous here but shipped; the LLR-014/TC-014 re-point deliberately still owed -> same -> only the `Hat-derived (hat.PERFORMANCE):` label dropped; the charter's-third-clause paragraph and the still-owed act survive unchanged.
- [CLARITY] SR-175 Rationale -> a declared inclusion/exclusion rule for repository content briefed to external providers, content beyond SN-026's demand; the pull-channel limit and what is NOT yet mechanized -> same -> the front `Hat-derived (hat.DATA-PROTECTION, with hat.SECURITY C-SEC-5 and hat.LEGAL C-LEG-3 …)` wrapper restructured into "derived from the data-protection lens, with the security and legal lenses converging on the same boundary control (three charters, three reasons, one crossing)"; the three-charters/one-crossing content, the amend-SN-026 rejection, the discipline-not-declaration account and the pull-channel limit are all preserved. The dropped `C-SEC-5`/`C-LEG-3` clause ids and the plan path were pointers to where a lens is written down, not conditions on this row.
- [CLARITY] SR-176 Rationale -> the matched value never reaches durable storage (the deliberate narrowing of C-DPR-2's retention/access ask); credential shapes redacted today, PII classes the stated build debt -> same -> only the front `Hat-derived (hat.DATA-PROTECTION, C-DPR-2 — clause text …):` wrapper dropped; C-DPR-2 is still named where the narrowing is recorded, and the measurement, the mechanized half and the not-yet-mechanized half are untouched.
- [CLARITY] SR-177 Rationale -> the throughput claim made OBSERVABLE rather than BUDGETED, the numeric-target refusal and its reason; lanes=1 here; NOT DECOMPOSED with the aggregation surface as the whole build gap -> same -> only the front `Hat-derived (hat.PERFORMANCE, C-PRF-1 — clause text …):` wrapper dropped; C-PRF-1 is still named where the deliberate-less-than narrowing is recorded.

VERDICT: CLARITY rows=17

## Observation (no action taken here — a CLARITY verdict owes nothing further)

This exact amendment was adjudicated once before, at WI-547
(`docs/reviews/wi-547-adjudicate-sr-024-sr-033-sr/001-ADJUDICATE-fb0ed7c.md`,
same 17 rows, same verdict). It is judged again here because a CLARITY verdict
by design does NOT re-anchor `docs/archive/last_approved`, so the anchor-vs-live
drift that raises the row is still there afterwards and will raise it again. That
is a detector/aftermath seam, not a defect in either verdict; recorded here so
the third sitting is not spent re-deriving it. No registry cell was edited.
