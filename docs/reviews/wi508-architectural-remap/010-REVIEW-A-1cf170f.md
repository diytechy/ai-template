- [MAJOR] docs/ratify/CURRENT.md:62 -> The proposed `Verifies` rewrite removes the last direct TC routes to the Approved, `Verification = "Test"` SR-163; `trace.py --strict` now reports `SR SR-163 has no test (TC)`, while the brief itself says the two TCs do not cover its complete join/universe acceptance -> retain or add a TC that verifies and exercises every SR-163 acceptance arm before regenerating the re-attestation brief -> @owner
- [MAJOR] docs/ratify/CURRENT.md:52 -> TC-199 and TC-200 are both `Drafted — never approved` in the live registry and approved snapshot, but the rendered brief simultaneously labels each `_approved — re-attestation owed_`; this can send a first approval through the wrong approval semantics -> render Drafted rows as first-approval owed (and include their complete current cells) unless their own Status is actually Approved -> @owner
- [MAJOR] docs/ratify/CURRENT.md:58 -> The changed TC-199 Method is cut off as `… [64 more chars — read the registry row]`, hiding the changed scope allocation (`SR-166` / TC-176) that an approver must re-read; PROCESS.md requires re-attestation to re-read changed cells -> emit the complete before/after Method cell (with its word-level diff) in the approval brief rather than truncating it -> @owner
VERDICT: CHANGES-REQUESTED findings=3
