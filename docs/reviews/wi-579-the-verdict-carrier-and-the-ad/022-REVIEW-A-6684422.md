### REVIEW-A — Independent diff review — Round 022 — 2026-09-03
Verdict: CHANGES-REQUESTED
Findings:
- [MAJOR] project-trajectory/scripts/integrate.py:1327 -> `review_rounds = 2` is reduced to a boolean, so one logged `REVIEW-A` APPROVE clears `_verdict_gate` (driven: the real gate returned `None` with only that one round) even though the declared policy requires two independent verdicts -> retain the parsed required count and refuse unless the governing evidence contains the required REVIEW-A/REVIEW-B cycle(s), with a regression that proves one approval is refused at 2 -> @owner; this is validation at the merge trust boundary, where construction cannot guarantee a partially recorded external review cycle (antidote: the smallest change that makes this fix unnecessary).
- [MINOR] docs/requirements/low-level-requirements.toml:2187 -> for clarity, new LLR-207 says `fold_listing` takes an "already-decoded" sequence, contradicting the shipped byte-preserving boundary (`git_bytes`, raw NUL records, and no decode) that its new regression proves -> say it takes raw byte entries and never decodes them -> @owner

VERDICT: CHANGES-REQUESTED findings=2
