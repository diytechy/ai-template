- [MAJOR] project-trajectory/scripts/agent_loop.py:3362 -> `review_owed_by_evidence` accepts any on-disk filename whose cited commit has the current identity, without the branch-scoped committed-path walk or the logged-REVIEW session join; a committed BUILD-session file reproduced `review_owed=False` while `_verdict_gate` refused the same lane as unlogged, so the loop can skip the round the gate later demands -> route this reader through `kitlib.verdict.branch_paths`/`logged_rounds`/`round_entries` (using the worker base and branch) and add the BUILD-session regression -> @owner
- [MAJOR] project-trajectory/scripts/integrate.py:1504 -> the `Review-Verdict` cross-check inspects only its word, so a verified `APPROVE rounds=0` trailer alongside one governing round silently clears the gate (reproduced: `gate=None`); this violates the declared `rounds=<N>` evidence carrier and leaves contradictory machine evidence undetected -> define the count's scope and refuse a mismatched stamped count, with a regression test; construction is unavailable because Git commit trailers are untyped, externally authored metadata rather than a stricter in-memory type or a single owning boundary -> @owner
- [MINOR] docs/requirements/low-level-requirements.toml:2188 -> new LLR-207's living rationale cites OI-76 and its historical ruling, contrary to PROCESS.md §3's no-provenance rule for spine cells -> remove the provenance frame while retaining the standing technical reason -> @owner
VERDICT: CHANGES-REQUESTED findings=3
