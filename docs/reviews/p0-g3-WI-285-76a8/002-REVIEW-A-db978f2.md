- [MAJOR] project-trajectory/scripts/agent_dispatch.py:1173 -> a declared `[product] test` whose executable is missing raises `FileNotFoundError` out of `subprocess.run`; the real dispatcher then exits 1 after the worker is ready, instead of treating the bar as red and parking/reworking the train, violating the WI's fail-closed/park-on-failure requirement and SR-008's missing-binary rule -> catch `OSError` around the bar launch and return `(False, diagnostic)` so `integrate_train` takes its existing rework path; add an end-to-end `[product]` missing-executable regression asserting no integration advance and a parked/rework outcome -> @owner
VERDICT: CHANGES-REQUESTED findings=1
