### REVIEWER — G3 — Round 1 — 2026-08-01
Verdict: APPROVE
Findings:
- none against the handback DECISION — the refutation holds and the row should not be built. Every claim re-driven independently at `0b4774f0` before judging it, and the two that carry the argument reproduce to the digit. THE COST, re-measured my own way rather than from their numbers: resolving every inline markdown link by path relative to the file holding it (the WI-288 rule) gives **124 inbound links across 25 files, 61 unique targets, and all 61 resolve today** — their figure exactly, and 23 more than any `archive/specs/` substring grep can see, because forms like `](specs/WI-109.2026-07-20.md)` from `docs/archive/README.md` and bare `](WI-110.2026-07-20.md)` between siblings contain no such substring. THE MIRROR HALF likewise: **91 outbound relative links across 43 of the 111 archived specs** — their figure exactly — of which **75 across 35 files carry a `/` and break on any depth change**, 16 across 12 are bare sibling names that break only on a SPLIT, and **0 are broken today**, so every one of the 91 is a link the move would newly break. Honest total 215 rewrite targets, which is what they claim. `docs/archive/specs/` holds **111** files, confirmed. THE STRUCTURAL ARGUMENT is sound and I could not break it — see the two findings below that sharpen its evidence rather than contest it. THE CONSUMER SURVEY re-run from scratch (`grep -rn "archive/specs\|ARCHIVE_SPECS" --include=*.py --include=*.html --include=*.yml --include=*.cmd --include=*.sh`): `check_trajectory.py` is the only production reader, at `ARCHIVE_SPECS_DIR` (:161) with two call sites in `_own_spec` (:1794, :1798), and its required change is to WIDEN `archive.glob("WI-###.*.md")` so it finds the file in EITHER subfolder — i.e. the only code that reads the directory must be taught to disregard the distinction the row would create, which is their point and it survives. `gen_trajectory.py` contains the string `archive` **zero** times; `PROJECT_STATE.html`'s five hits are all inside rendered WI TITLE text (WI-391's own intake title and WI-387's), not path references; `integrate.py` names no spec path at all. `docs/orphans-allow:50` is `docs/archive/specs/*` and its fnmatch `*` spans separators, so the census survives the split — checked, not assumed. -> no remedy owed -> @owner
- [MINOR] docs/work/active/wi-391/WI-391-mirror-specs-of-record-onto-the-termi.md:3 -> "Confirmed the spec's figures exactly" is not reproducible, and the handback should not have leaned on it. Four of the intake's five figures fail to reproduce under every counting convention I tried, and the one that does is the one nobody disputes. Measured at HEAD over `git ls-files` `*.md/*.py/*.csv/*.html`: `archive/specs/<name>.md` occurs **175 times across 37 files** (intake: 154/30); as markdown link targets, **104 across 13 files** (intake: 101); `docs/log.md` alone holds **124 occurrences on 106 lines** (intake: 119/101) with **92 markdown link targets** (intake: 92 — the one exact match) over **55 unique targets by basename, 57 counting fragments** (intake: 61; 61 is the count of unique targets across ALL 25 linking files, so the intake appears to have attributed a repo-wide figure to log.md). Re-run at `277cb7b9`, the commit that wrote the intake: 174/38 and log.md 123/105 — so drift since the measurement explains at most one occurrence, not twenty-one. A narrower pattern requiring the `<stem>.<date>.md` shape gives 152/31, the closest I got to 154/30, which suggests the convention was that and went unrecorded. Every variant I ran is at or ABOVE the intake's number, so the error runs in the direction that strengthens the handback's "these undercount" conclusion — which is exactly why asserting an exact confirmation was unnecessary. -> Replace "confirms the spec's figures exactly" with the figures actually re-derived and the pattern used to derive them (path-resolved inbound: 124/25/61; outbound: 91/43; files: 111), and record that the intake's substring counts are convention-dependent and unreproducible rather than wrong. -> @owner
- [MINOR] docs/work/active/wi-391/WI-391-mirror-specs-of-record-onto-the-termi.md:3 -> The classification split is 92/3/16, not 93/3/15, and the file that moves is worth naming because it is a third failure shape. Deriving each archived spec's terminal state from its filename and the `docs/work/` folders: **92** map to a `complete/` row, **3** map to a `cancelled/` row (`WI-108.2026-07-29.md`, `WI-123.2026-07-27.md`, `WI-271.2026-07-29.md`), and **16** have no derivable state. The extra one is `WI-300-sr052-binding.2026-07-26.md`: WI-300 is in `complete/`, so counting it as derivable gets 93/15, but the filename does not match `_own_spec`'s own glob (`WI-300.*.md` does not match `WI-300-sr052-binding.…`), so the checker cannot attribute it either and the strict count is the honest one. That makes THREE ways the mirror function fails to be total — a shared effort doc with no id, an id-suffixed spec the glob cannot see, and a doc with citers in both terminals — not one. -> State 92/3/16 with the strict rule (attributable only if the name matches the checker's own glob), and name `WI-300-sr052-binding.2026-07-26.md` as the second shape. -> @owner
- none against follow-up (c) — it is a LIVE regression, I reproduced both defect classes today in the current close path, and it deserves its own row whatever happens to WI-391. `agent_dispatch.py` was deleted whole at `31ad569d` ("Phase 5 item 1", 4,042 lines) and `_relink_archived_specs` / `_rebase_moved_spec_links` / `_archive_closed_specs` went with it; a repo-wide grep for those symbols in `*.py` returns **zero** live hits, and nothing in `integrate.py`, `drive.py`, `trunk_step.py` or `agent_common.py` moves a spec at close — so archival today is an unassisted `git mv` and R-F (`check_trajectory.py:1712-1718`) tells a human to perform it by hand. DRIVEN, then reverted: with the tree clean at a baseline of 4 pre-existing broken links, I added `docs/specs/WI-999.md` carrying one link at each depth (`../log.md`, `../../project-trajectory/PROCESS.md`, sibling `README.md`) and one inbound link to it from `docs/status.md`, confirmed `check_docs` still reported exactly 4, then performed the archival exactly as it happens now — a bare move to `docs/archive/specs/WI-999.2026-08-01.md`. `check_docs` went to **8** broken links: three are the moved file's OWN links resolving one directory short (WI-353's defect, verbatim), and one is `docs/status.md:354 broken link -> specs/WI-999.md` (WI-288's defect, verbatim). Both rows' Deliverables record what this cost before: WI-288 was found at the "live 2026-07-24 hand-integration of WI-281 … WI-274 identical", WI-353 at "archiving WI-328's spec BY HAND: six broken links, check_docs went from OK to FAIL immediately". `docs/archive/` is exempt from the ORPHAN check only, not the broken-link check, so the archive does not absorb it. The probe was removed and the tree verified clean (`git status --porcelain` empty, back to 4). -> File it as its own row: rehome the indivisible move+relink+rebase ritual (the WI-288/WI-353 contract) onto whatever owns spec archival now. -> @owner
- none against follow-up (b), and my own answer to it: RESTATE, do not strike. Striking `docs/concurrency-v2.md:883-885` loses the reason, and the reason is sound — a reader should be able to tell shipped from cancelled without opening a spec. That goal is already MET for the 93 attributable specs by the registry half WI-384 shipped (the id is in the filename; the terminal folder is the answer), and it is UNMEETABLE for the 16 that have no single terminal state, which no folder layout can fix. The honest restatement carries both halves and the reason the mirror was refused: the fact is answered once, in `docs/work/`, and mirroring it into the spec home would create a second encoding that can disagree with the first — the exact defect §B2's first half deleted. That preserves §0's worked example instead of quietly dropping a ruled sentence, and it leaves the owner a record of what was decided rather than a gap. -> Restate §B2's second sentence as satisfied-by-the-registry-half plus a named exception for shared effort docs; do not delete it. -> @owner
- [MINOR] docs/concurrency-v2.md:883 -> The structural argument is right but leads with its weakest evidence. As handed back, the load-bearing claim is an ANALOGY — "a spec folder beside the WI folder is the same shape as an attribute beside a folder" — and analogies are the part of an argument a reviewer is supposed to attack. I attacked it and it holds (two independent encodings of one fact with no single source of truth is the same defect whether the second encoding is a key or a directory; today `docs/archive/specs/` encodes terminal state ZERO times, so divergence is currently unrepresentable and the mirror is what makes it representable, with nothing cross-checking it and no §0-legal way to add one). But the argument does not NEED the analogy, because the counter it is defending against — "a mirror derived mechanically at move time is no worse than the derived artifacts this repo already gates" — is refuted by measurement, not by reasoning: **a derived artifact needs a total function from state to location, and this one is not total for 16 of 111 files (14%)**, it has no regenerator that a freshness gate could hang off (nothing writes that directory; `gen_trajectory.py` never touches it), and in at least one case it is not merely undefined but CONTRADICTORY. I confirmed the contradiction end to end: `research-knowledge.2026-07-29.md` is cited by WI-138 and WI-145 in `complete/` and by WI-158 in `cancelled/`, and WI-158's own Deliverable (`docs/work/cancelled/WI-158-okf-pack-export-deferred-export-know.md:12`) says the record "stays in the archived shared spec (docs/archive/specs/research-knowledge.2026-07-29.md, **archived with this last open citer**)" — so the doc reached the archive BY the cancelled citer while two of its citers shipped, and either destination is a false answer for real work. `docs/specs/README.md:16-19` makes that shape first-class ("**or** a shared **effort doc** with `#anchor`s"), and :49-50 makes the archival rule explicitly last-citer-wins, so this is the documented design colliding with the proposal, not an accident. -> Lead the structural section with the non-total mirror function and the WI-158 contradiction, and keep the disposition analogy as the second argument. -> @owner
- [MINOR] docs/concurrency-v2.md:883 -> The benefit side is refuted only qualitatively, and the numbers kill it outright. §B2's sentence promises that "a spec's location answers shipped-or-cancelled without opening it". Measured: the split would put **3 files in `cancelled/` and 92 in `complete/`** — the answer is "shipped" for 97% of the population — and for the 93 WI-named specs the question is ALREADY answerable without opening anything, because the filename carries the id and `docs/work/complete/` vs `docs/work/cancelled/` is one `ls` away; that is precisely what WI-384 delivered. So the row buys a second, divergence-capable encoding of a fact the registry half already answers in one step, for the 97%-of-the-time answer, while leaving the 16 files where the question is genuinely hard (the effort docs) exactly as unanswerable as they are today — the mirror cannot file them at all. That, not the cost, is why the row should not be built at any price. -> Add the 3/92/16 split and the one-`ls` alternative to the handback so the benefit is refuted on its own terms, independently of the 215-target migration cost. -> @owner
- [MINOR] docs/work/active/wi-391/WI-391-mirror-specs-of-record-onto-the-termi.md:3 -> Claim 5's "enforcement layer maintaining enforcement layer" is the same objection that would sink follow-up (c), and as written the handback argues both sides. Claim 5 disparages rebuilding `_relink_archived_specs` + `_rebase_moved_spec_links` + their tests as enforcement maintaining enforcement, to justify a one-time script for WI-391; follow-up (c) then asks for exactly that machinery back. Sized from the deleted blob (`31ad569d^:project-trajectory/scripts/agent_dispatch.py`, 4,043 lines): `_resolvable_link` 16 + `_redirected_link_target` 18 + `_rewrite_md_links` 33 + `_rebase_moved_spec_links` 30 + `_relink_archived_specs` 35 = **132 lines of link machinery**, plus the 45-line `_archive_closed_specs` that made the move indivisible, plus 7 named guards I can still find by name in the deleted modules (5 in `test_agent_dispatch_decisions.py`, 2 in `test_agent_loop_integrate.py`; the Deliverables say 6+2=8, so the eighth is named without a matchable token). Against that, ~70 lines for a one-time relinker over a known move map is credible arithmetic — half the machinery, none of the CRLF/no-op/fragment cases that have to keep working forever. ~2 hours is optimistic but the right order for the code; what it does not price is that 92 of the 124 targets sit in `docs/log.md`, so the row also owes a record-surface exception the intake itself says is unbounded. The two claims are reconcilable and the handback should say how: WI-391's need was a ONE-TIME bulk migration, (c)'s is a RECURRING per-close operation, and (c)'s correct form under §0 is not a check at all — it is the indivisible move-and-relink ritual WI-288/WI-353 already proved out, i.e. a constraint that makes the half-done move unrepresentable, which is the shape §0 asks for rather than the shape it warns about. -> Add one sentence distinguishing the one-time migration from the recurring close path, and state that (c) is constraint-shaped (an indivisible ritual), so claim 5's objection does not reach it. -> @owner
- [MINOR] docs/declared-absences:29 -> Follow-up (a) is confirmed in substance but its remedy is wrong: the two lines want RESTATING, not deleting. Both entries are real and both say what the handback reports — ":29 `docs/archive/specs/complete/` — a destination WI-391 will CREATE… Remove this line when WI-391 lands", ":30 the pair — and the file header at :15-17 does say a path that is simply ROT "belongs fixed", so a cancelled row leaves two entries promising directories nothing will ever create. But deleting them is not free, and I drove it: with both lines removed, `check_doc_refs.py --root . --strict` goes from 2 dangling references to **4**, the two new ones being `docs/archive/specs/complete/` and `docs/archive/specs/cancelled/` named in the WI-391 spec's own title text — which survives cancellation, since a cancelled row keeps its spec as the record. (`docs/log.md:20938` names both paths too and is already classified as a record surface, so it does not dangle either way.) So the entries are still doing the job the file exists for; what goes false is only the REASON, and with it the materialize-guard's instruction. -> Restate both reasons — the paths are the destinations of a PROPOSAL that was refuted (cite this review and the concurrency-v2 §B2 disposition), they must never materialize, and there is no longer a landing row to remove them on — rather than deleting the lines and re-opening two dangling references. -> @owner
VERDICT: APPROVE findings=6
