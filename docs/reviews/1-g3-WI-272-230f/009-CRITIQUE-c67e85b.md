- [MAJOR] U5 -> The When tab's status legend and execution meter use green `#047857` for `done`, while the What tab's TC column/legend and the Knowledge tab's Test Case node/legend use that same green for the Test Case type; the When phase palette also reuses the What/Knowledge type hues (for example phase 1 teal versus System Requirement teal and phase 3 indigo versus Stakeholder Need indigo), so adjacent status, phase, and type vocabularies assign the same hues to different concepts -> declare disjoint palette tokens for status, phase, and type/tier, move the colliding Test Case/SR/SN or phase accents to non-overlapping hue families, and regenerate every affected node and legend from those tokens -> @owner
VERDICT: CHANGES-REQUESTED findings=1
