- [MAJOR] project-trajectory/scripts/agent_dispatch.py:466 -> `_harness_floor_failures` accepts a missing root `.venv` whenever ambient Python is >=3.11, and `harness_python` then silently selects ambient `sys.executable`; this violates WI-286's required shared, pinned root toolchain and lets unpinned/missing dev tools produce a green worker run -> fail preflight when the root `.venv` interpreter is absent (and when its layout is incomplete), rather than falling back to ambient Python; cover the no-`.venv` >=3.11 dispatcher flow -> @owner
VERDICT: CHANGES-REQUESTED findings=1
