The split does not satisfy “one row, one direction, one kind.” I reproduced seven major defects and four minor defects. The reviewed worktree remained unchanged and clean.

1. [MAJOR] — [docs/work/README.md:34](C:/Projects/ai-template-s4review/docs/work/README.md:34), [wi_convert.py:553](C:/Projects/ai-template-s4review/project-trajectory/scripts/wi_convert.py:553) — IF-159 is still read/write, and its body falsely promises failed output is not left on disk. The row is `requestors = ["scripts/wi_convert"]`, but `_self_verify` reopens the file at line 575. I forced `parse_spec` to return a mismatch and called the real `write_spec_file`; it raised `ConvertError` while reporting `left_on_disk=True`. Change the implementation to validate the rendered text in memory before writing, making the row genuinely write-only and making invalid output structurally unable to reach disk.

2. [MAJOR] — [interfaces.toml:730](C:/Projects/ai-template-s4review/docs/requirements/interfaces.toml:730), [score_reviews.py:426](C:/Projects/ai-template-s4review/project-trajectory/scripts/score_reviews.py:426) — IF-160 names `score_reviews` only as a requestor/writer, but `record_round` first reads the scoreboard and then rewrites it at lines 510–515. A temp scoreboard reproduction preserved an existing provider and round: `old_preserved_from_read=(2.0, 3)`, `rounds=2`, then rewrote the old provider. The body also says “the escalation policy reads it,” but `rg "read_scoreboard|scoreboard" agent_route.py agent_loop.py` found no policy-side file reader; only `score_reviews.record_round` reads it. Split the scoreboard read and write directions into separate rows and remove the unsupported escalation-policy claim.

3. [MAJOR] — [interfaces.toml:1036](C:/Projects/ai-template-s4review/docs/requirements/interfaces.toml:1036), [trunk_step.py:362](C:/Projects/ai-template-s4review/project-trajectory/scripts/trunk_step.py:362) — IF-156 calls `trunk_step` a consumer, but that consumer deletes every fragment after reading it. The existing test `test_a_merge_staged_fragment_compiles_from_merge_head_history` passed and explicitly verifies the file no longer exists after compilation. This is a consumer that writes/mutates the owned medium. Keep IF-156 as the read contract and mint a requestor/mutation row for the deletion lifecycle.

4. [MAJOR] — [docs/log.d/README.md:5](C:/Projects/ai-template-s4review/docs/log.d/README.md:5) — the live medium already violates newly declared IF-156. Running `trunk_step.py --root . --dry-run` exited 1 because three tracked fragments open with `#` or `###`, not `##`:

   - `2026-08-18-shipped-docs-prose.md`
   - `2026-08-25-oi63-legibility-round.md`
   - `2026-08-25-owner-rulings-oi63-oi64.md`

   Correct their opening headings or migrate them before declaring the drop-box conforming; then prove the real dry-run returns 0.

5. [MAJOR] — [interfaces.toml:618](C:/Projects/ai-template-s4review/docs/requirements/interfaces.toml:618), [interfaces.toml:626](C:/Projects/ai-template-s4review/docs/requirements/interfaces.toml:626) — minted IF-164 is not disjoint from IF-163. IF-163 owns the entire blackboard and explicitly includes the generated block; IF-164 owns that same generated block. Both name `agent_common`, whose [current_state_excerpt](C:/Projects/ai-template-s4review/project-trajectory/scripts/agent_common.py:2082) reads the whole `## Current State` section, including the markers at `docs/status.md:363–373`. Thus the generated bytes have two rows/owners. Narrow IF-163 to the hand-authored bytes outside the markers; leave the generated region exclusively to IF-164.

6. [MAJOR] — [subagent_gate.py:59](C:/Projects/ai-template-s4review/project-trajectory/scripts/subagent_gate.py:59) — IF-020 remains bundled after IF-151 was split out. Its `channel = "exit-code"` body also promises the JSON decision on stdout and an appended log file: three output kinds under one row. Mint separate stdout and file rows, leaving IF-020 solely the exit alphabet.

7. [MAJOR] — [spine_carrier.py:55](C:/Projects/ai-template-s4review/project-trajectory/scripts/spine_carrier.py:55), [interfaces.toml:1402](C:/Projects/ai-template-s4review/docs/requirements/interfaces.toml:1402) — IF-102 still promises `rows_seq_from_text`, despite the worker explicitly removing it from IF-102’s data because only `agent_route` uses it. That exact call is already IF-119. `rg rows_seq_from_text` found the sole product caller at `agent_route.py:297`. Remove the `rows_seq_from_text` clause from IF-102; IF-119 already owns it.

8. [MAJOR] — [check_trajectory.py:1136](C:/Projects/ai-template-s4review/project-trajectory/scripts/check_trajectory.py:1136) — strict enforcement allows invalid ownership/body states:

   - IF-145 owner changed to `scripts/check_perf`: warning emitted, exit 0.
   - IF-164 owner changed to `scripts/gen_trajectory`: warning emitted, exit 0.
   - IF-145 body deleted while its marker remained: no targeted diagnostic, exit 0.

   Controls behaved correctly: a row naming both directions made `trace --strict` exit 1; planted uncited IF-199 made `check_trajectory --strict` exit 1; swapping seeded IF-001 for IF-145 failed the exact-set pytest pin. Promote owner-exact mismatches and missing bodies to strict findings, as queued WI-533 already anticipates.

9. [MINOR] — [test-cases.toml:1601](C:/Projects/ai-template-s4review/docs/test/test-cases.toml:1601) — TC-161’s live `method` still cites retired IF-127. `verifies` was re-pointed, but the explanatory test contract was not. `PROJECT_STATE.html` correctly regenerates the same rot from that live cell. Change the approved method through the required approval workflow to IF-075 and regenerate projections. All other IF-116/IF-127 hits were historical logs, plans, ruled open-item decisions, reviews, archives, comments, or the slice record.

10. [MINOR] — [trunk_step.py:167](C:/Projects/ai-template-s4review/project-trajectory/scripts/trunk_step.py:167) — README exemption is case-sensitive on Windows. A temp reproduction returned:

   - `README.md -> []`
   - `readme.md -> ['readme.md']`
   - `README.MD -> ['README.MD']`
   - `WI-1-README.md -> ['WI-1-README.md']`

   Use `p.name.casefold() != "readme.md"` so the same filesystem object receives consistent treatment while legitimate fragment names remain included.

11. [MINOR] — [WI-531 log:10](C:/Projects/ai-template-s4review/docs/log.d/2026-08-29-wi531-if-row-split.md:10) — “eight far sides” does not reproduce. Diffing `673d9f3c` against `816090cd` found 14 far-list edits: seven `requestors` and seven `consumers`. Removing two collapse merges and three generated-document re-points leaves nine ordinary corrections: IF-010, IF-012, IF-037, IF-046, IF-050, IF-053, IF-081, IF-090 and IF-102. Change eight to nine or explicitly define a different counting rule.

The two collapses themselves are valid: both former IF-075/127 requestors call `reattest_model`, and both former IF-101/116 requestors call `read_watermark` followed by `bump_watermark`.

Other reproduced controls:

- `trace.py --strict`: exit 1 on the three acknowledged standing spine/provenance findings; `interfaces=154 interface-findings=0`.
- `check_trajectory.py --strict`: exit 0 with warnings.
- Counts reproduced: 136 → 154, 20 additions, 2 deletions, watermark IF=164, reference `73 / 150 / 150`, allow seed 118 and 139 total entries—21 past seed, comprising IF-145–164 plus inherited IF-144.
- CLI probes: `check_perf` 0/SKIP; `check_privacy --repo` 0; `gen_okf --check` 0; `gen_arch_map --contracts-doc ... --check` 0; missing coverage report 0/SKIP; malformed plan coverage 2; malformed subagent JSON 0/fail-open.
- My first `run_menu` probes incorrectly supplied unsupported `--root` and returned argparse 2; corrected `--list --file docs/stack.ini` returned 0, while an unknown name against the repo’s empty `[run]` configuration returned the documented “nothing declared” code 1.
- Final `git status --short` and `git diff --stat` were empty.

VERDICT: CHANGES-REQUESTED findings=11