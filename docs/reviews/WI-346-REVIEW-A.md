### REVIEWER — G3 — Round 1 — 2026-07-29
Verdict: APPROVE
Findings:
- [MINOR] docs/work/archive/WI-346-give-gen-trajectory-py-one-local-spine-l.md:17 -> the Deliverable states "Byte-determinism verified by hand from the worktree (`--check` and `--status --check` both up to date)", but neither is reproducible at the branch tip: the same commit archives the spec, so `gen_trajectory.py --check` and `--status --check` both report STALE there. The substance is correct — I isolated it by dropping the refactored `gen_trajectory.py` onto an untouched `8e07b72` tree, where both checks return "up to date", exit 0 — but the durable record as written cannot be re-run by the next reader -> qualify the sentence: verified pre-close against the parent tree; the tip reads stale by the registry flip alone, whose regeneration is the trunk's per §5.2 (the contradiction the follow-up log fragment already records) -> @owner
- [MINOR] project-trajectory/scripts/gen_trajectory.py:275 -> the new single site that now names all three spine paths mixes forms: `ct.SR_CSV` for the SR arm, string literals for LLR and TC, though `ct.LLR_CSV` and `ct.TC_CSV` exist with byte-identical values. Carried over verbatim from the copies, and gen_trajectory.py hardcodes those two paths at its other sites too, so this is pre-existing house inconsistency rather than a regression — but a WI whose thesis is "state the rule once" is the moment it is cheapest to close -> optional, zero-risk: use `ct.LLR_CSV` / `ct.TC_CSV` in `_spine`'s return tuple (or leave, if the file's literal convention is deliberate) -> @owner
VERDICT: APPROVE findings=2
