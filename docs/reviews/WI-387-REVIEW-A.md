### REVIEWER — G3 — Round 1 — 2026-08-01
Verdict: CHANGES-REQUESTED
Findings:
- [MAJOR] project-trajectory/scripts/handback.py:226 -> The ruled red arm silently mis-reverts any lane that RENAMED a file, because `git diff --name-status -z` emits THREE fields for a rename and the parse assumes two. `diff.renames` has defaulted to true since Git 2.9, so `R100\0<old>\0<new>\0` is ordinary output, not an exotic case; the pairing `for i in range(0, len(fields) - 1, 2)` desynchronises at the first rename and every subsequent field is read in the wrong slot. Driven twice on constructed lanes. (a) Rename late in the sort order: a lane that renames `src/old_name.py -> src/new_name.py` and adds `broken.py` yields `['A','broken.py','D','docs/work/active/…','A','docs/work/queued/…','R100','src/old_name.py','src/new_name.py']`; `src/new_name.py` falls off the end of the `len(fields)-1` bound, so the quarantined branch ends holding BOTH `src/old_name.py` (restored) and `src/new_name.py` (never reverted) — duplicated content in trunk — and the `.patch` does not mention `new_name` at all. (b) Rename FIRST in the sort order, which is the damaging alignment: `['R100','Aold.py','Anew.py','D','docs/work/active/…','A','docs/work/queued/…','M','z_broken.py']` pairs as `('R100','Aold.py')`, `('Anew.py','D')`, `('docs/work/active/…','A')`, `('docs/work/queued/…','M')` — so paths are read as statuses, the bookkeeping filter no longer matches (it tests `fields[i+1]`, which is now a status letter), and `z_broken.py` — THE FAILING FILE — is dropped past the loop bound entirely. Result, verbatim from the run: `handback: quarantined wi-401 -> docs/work/handback/wi-401.patch (4 path(s) reverted)`, refusal `None`, and `git show wi-401:z_broken.py` still `VALUE = (`. Three of the four git operations were `git rm -q -f -- D`, `git checkout <base> -- A`, `git checkout <base> -- M` — every one a no-match failure whose return code is DISCARDED at :244/:246 — and the `.patch` is 474 bytes containing only the `Aold.py` deletion, so the failing diff is lost from the record too. `_refresh_or_quarantine` then re-refreshes, still reds, and returns the refusal: the run stops on a branch whose product tree has been half-reverted behind the operator's back. This is the exact shape the module's own docstring claims it handles ("the product paths go back to what trunk had, and nothing live can red anything"), and `test_quarantine_reverts_the_product_and_keeps_the_failing_diff` drives A/M/D and only A/M/D, so no test can fail on it. -> Parse the `-z` stream as records, not pairs: consume one status field, then two paths when it starts with `R`/`C` and one otherwise (revert the new path as an add and restore the old one), and CHECK the return code of every `git rm`/`git checkout` in the revert loop, refusing rather than printing a count that did not happen. Add a rename case to the three shapes the test already drives. -> @owner
- [MAJOR] project-trajectory/scripts/integrate.py:317 -> `_abandoned_claim` is a suffix match, not a claim-subject match, so `claim()` will `git branch -D` a real one-commit work branch — an over-broad match on a deletion path. The docstring says the tip's subject "is a claim for THIS branch" and `_claim_subject` (:311) exists expressly so "the writer and the abandoned-claim reader below must agree on it exactly", but the reader never calls it: it tests `subject.endswith("-> active/{} (bookkeeping)".format(branch))`, discarding the whole `claim: <wi_id> ` prefix — and `_abandoned_claim` is not even passed the `wi_id` its caller `_claim_refusal(root, wi_id, branch)` holds. The third fact is also mis-stated: "the tip's PARENT is [an ancestor of trunk], so no work was ever built on it" does not follow — a parent on trunk proves only that the branch is ONE commit ahead, and that one commit can carry anything. Driven on constructed topologies. (a) A branch `wi-401` whose single commit is `wip: nearly done -> active/wi-401 (bookkeeping)` and adds `real-work.txt`: `_abandoned_claim` -> `True`, `claim(root, "WI-401", "wi-401")` prints `integrate: deleted the abandoned claim branch wi-401`, returns 0, and `real-work.txt` is gone from the re-cut branch. (b) A genuine claim subject for a DIFFERENT id on the same branch name — `claim: WI-999 -> active/wi-401 (bookkeeping)`, carrying `junk.txt` — is likewise deleted and re-cut. The suite is green through both: `test_a_branch_carrying_work_is_a_collision_the_claim_still_refuses` builds TWO commits so the tip is a work commit, and `test_an_unrelated_branch_of_the_same_name_is_never_deleted` builds a branch with no claim-ish subject at all, so neither negative can fail if the matcher loosens — a stated invariant with no test that can fail. The deletion prints no sha, so the operator is not even handed the reflog handle. -> Take `wi_id` and compare the tip subject to `_claim_subject(wi_id, branch)` exactly, so the one home is genuinely one home; add the one-commit-carrying-work negative (subject ending in the suffix but not equal to the claim subject) as the test that can fail. -> @owner
- [MINOR] project-trajectory/scripts/integrate.py:559 -> A branch CAN present two outcomes for one id, and `branch_outcomes` resolves the contradiction silently instead of reporting it — and resolves it toward the answer that skips the verdict gate. `landed` is a dict keyed on the spec's BASENAME, so a branch that leaves the same spec in two declared folders overwrites the first with whatever `ls-tree` emits last, i.e. plain alphabetical precedence: `queued` > `draft` > `deferred` > `complete` > `cancelled`. Driven on three constructed pairs: `('complete','queued')` -> `{'WI-401': 'handback'}`, `unresolved=[]`; `('cancelled','queued')` -> `handback`; `('complete','cancelled')` -> `merged`. So the id whose spec sits in `complete/` — trunk's record that the work is DONE — resolves to `handback` and `_verdict_gate` `continue`s past it with no APPROVE. The `unresolved` fail-closed arm covers only "landed nowhere", never "landed twice". Fail-closure does hold in the composed system, and I drove that too rather than assert it: the resulting tree reds `check_trajectory` with `ERROR - duplicate work-item id WI-401`, which the §A2 refresh runs before anything merges — and because the outcome reads `handback`, `_refresh_or_quarantine` quarantines (docs/work/ is exempt, so the duplicate survives), re-refreshes, stays red and stops the run. So nothing smuggles through; but the invariant the module states at :121-127 ("no fourth answer and no state file that could hold one … one fact, read twice") is enforced by an unrelated rung in a different script, not where it is claimed, and no test can fail if this read starts guessing. -> Collect per-basename outcomes into a set and route a spec that landed in more than one declared folder to `unresolved` (the refusal text already reads correctly for it: the branch "names none of the three"). -> @owner
- [MINOR] project-trajectory/scripts/integrate.py:405 -> Two load-bearing sentences in the claim-inversion rationale are false as written, in the surface that is now the design's record. (a) "Crash before the branch and there is nothing but an unreferenced object git will collect." The window before `commit-tree` is not empty: the spec has already been `git mv`d out of `queued/` into `active/<branch>/` (:429) and `add -A` has staged it and the regen output (:457). Driven — reproduce that state and `git status --porcelain` reads `R  docs/work/queued/WI-401-widget.md -> docs/work/active/wi-401/WI-401-widget.md` with NO branch ref, the next `integrate claim` refuses `the trunk working tree is dirty - a claim is a clean serial commit`, and `drive.run`'s cycle-top dirty check turns that into `EXIT_PREFLIGHT`, i.e. a stopped walk-away run needing hand repair. It is not a regression (the old order had the same pre-commit window, and the dirty check already fronted `_stranded_claims` for it), so the deletion of `_stranded_claims` stands — but "the claim does two writes" and "exactly one crash window" are the premises the deletion is argued from, and there are three interesting points, not two. (b) "the hook's live rung there was the generated-artifact freshness floor, and the regeneration below satisfies it by construction". The shipped `hooks/pre-commit` runs four groups, and `trunk_step.py --regen` regenerates six artifacts (`arch-map, okf, derived-gate, trajectory, status, open-items`) against the hook's ten `--run-steps` names: `registry-integrity`, `trajectory` (the SSOT check, a different thing from the `trajectory` GENERATOR step), `skills-sync` and `ratify-fresh` are not covered — and outside `--run-steps` the commit also skips `check_privacy.py --author` and the ALWAYS-ON secrets floor (`docs/privacy-check` is `false` here, but the secrets scan is on in every repo by design), plus the `format` step and the `commit-msg` hook. Most of those are vacuous for a pure bookkeeping commit given the previous commit passed, but two are not: `ratify-fresh` compares the re-attestation brief against the registry the claim MUTATES and this repo carries seven briefs under `docs/ratify/`, and the secrets floor is content-scanning regenerated artifacts that now reach trunk unscanned. Trunk advances (`reset --hard`, :488) to a commit no hook and no bar has inspected; the next thing to bar it is a later lane's refresh. -> Correct both sentences: name the pre-`commit-tree` dirty-trunk window and say plainly that it is handled by the existing dirty check rather than by the inversion; and replace "the hook's live rung there was the freshness floor" with the measured list of what is bypassed, keeping the argument (it is a bookkeeping commit) but not the claim of exhaustiveness. -> @owner
- [MINOR] project-trajectory/scripts/drive.py:347 -> A hole in "every lane ends in a merge" that is neither filed nor tested: a DECIDED exit from a worker that has ALREADY moved its specs out of `active/` cannot hand back, and stops the run. Driven — a lane that closes `WI-401` into `complete/`, commits, and then exits non-zero gives `hand_back(root, "wi-401", "worker exit 6")` -> `(None, 'cannot read the claimed spec WI-401-widget.md: [Errno 2] No such file or directory: …/active/wi-401/WI-401-widget.md')`, because `_claimed_specs` reads TRUNK (where the spec is still claimed) while the read at :177 goes to the LANE tree (where it is not). `_lane_close` then returns `EXIT_PREFLIGHT` and `run()` exits before `_drain`, so a branch that is FINISHED and would have merged cleanly is left unmerged with a misleading `cannot hand back` message. This is not exotic: the row's own argument is that "a review escalation is the most common handback cause", and a review escalation happens at the END of a lane, i.e. exactly when the close may already be written; `EXIT_BUDGET`/`EXIT_STALL` after a completed build have the same shape. Their filed finding 1 covers the mirror case (exit 0 WITHOUT closing) and this one — non-zero WITH closing — is the other diagonal, unrecorded. -> Either short-circuit `_lane_close` when `integrate.finished_branches` already contains the branch (let the drain merge it on its declared outcome, which is what the tree says), or have `hand_back` detect the already-closed lane and refuse by name instead of by `OSError` — and record whichever, since it is a boundary of the invariant. -> @owner
- [MINOR] project-trajectory/scripts/drive.py:320 -> `_WORKER_OUTCOMES` converts three RESUMABLE conditions into human-blocked ones, and the cost is not recorded anywhere. `EXIT_BUDGET` (a worker that hit its iteration ceiling), `EXIT_STALL` and `EXIT_PAUSED` are all "decided" exits, so the lane hands back — and `hand_back` sets `blockref` to the spec's own path, which `schedule._disposition` reads as `blocked`. Driven end to end in probe E: after a handback the frontier is `[]`, and it only returns to `['WI-401']` after the `blockref` line is deleted by hand. Before this row, `EXIT_BUDGET` stopped the run with the claim parked and a relaunch RESUMED the same lane, so a WI needing more than one worker budget finished across relaunches with no human in the loop. Now it returns blocked and an unattended run can never pick it up again. That may well be the right trade — but the spec's justification ("the dominant handback shape is green-but-not-approved or cannot-proceed-for-config-reasons") is about `EXIT_NEEDS_HUMAN`, and the budget/stall case is neither. Two smaller notes in the same area, no remedy owed: a second handback of the same WI accumulates a second `## Handback` section (driven — parses fine, `Deliverable=''`, `BlockRef` intact, just accretes); and `quarantine` uses `git add -A` in the lane worktree (:247), which is the sweep-everything shape this house avoids elsewhere. -> Record the budget/stall consequence in the fragment's deviations (or narrow `_WORKER_OUTCOMES`/omit the `blockref` for `EXIT_BUDGET`/`EXIT_STALL` so a ceiling stays resumable), so the walk-away property is not quietly traded in a set literal. -> @owner
- none against the outcome-keyed verdict gate, the anti-livelock blockref, the F5 widening, the scaffold surface, the crash path or the bars — all re-driven, all hold. THE GATE, both directions: `merged` still cannot pass without a fresh APPROVE, and WI-386's whole freshness apparatus survives intact under the signature change (absent verdict, CHANGES-REQUESTED, an APPROVE predating the last code commit, an APPROVE after it, and the log-fragment exclusion are all still driven, now with `{"WI-401": "merged"}`); `cancelled`/`handback` owe none, shown at the helper AND through the whole slot (`test_a_cancelled_branch_merges_through_the_slot_owing_no_verdict` at policy 1 with no verdict artifact anywhere). My smuggle attempt is finding 3 above and it does not reach trunk. THE BLOCKREF is genuinely load-bearing and I drove the loop's absence rather than the cell: post-handback `schedule.frontier` is `[]` and `evaluate` reads `status=queued, disposition=blocked`; delete the `blockref` line and the same WI is back on the frontier and re-claimable — so the claim/return/re-claim livelock (which the stall guard could not catch, since each handback merges and moves trunk) is really closed. THE .PATCH IS BAR-INERT, driven on a REAL bootstrapped scaffold rather than in-repo: `agent_common.spec_files` does not see it (`rglob("WI-*.md")`), and on a scaffold carrying `docs/work/handback/wi-401.patch` at `HEAD`, `check_trajectory` rc=0 `clean (1 work item)`, `check_docs` rc=0 `18 doc(s), 67 link(s), 0 broken`, `check_stubs` OK, `check_privacy` clean. THE SCAFFOLD SURFACE is complete — I bootstrapped one (`bootstrap.py --dest … --agents claude --stack python`, 124 files) and drove the machinery on it: `scripts/handback.py` present, `integrate.py claim --wi WI-401 --branch wi-401` -> `integrate: claimed WI-401 on wi-401 (branch cut + trunk advance)` with `main` and `wi-401` both at the claim commit (the inversion, on a fresh scaffold), then `hand_back` -> `(['WI-401'], None)`, `finished_branches` `['wi-401']`, `branch_outcomes` `({'WI-401': 'handback'}, [])`. No MAPPING omission this time. THE BODY GRAMMAR: the three F5 copies are byte-identical across the whole `SPEC_HANDBACK`-to-`parse_spec_row` region (compared programmatically, not by eye) and `wi_convert`'s variant partitions on the same literal; the previously-SILENT failure is genuinely closed — `read_spec_rows` skips a malformed spec with no sink at all (`agent_common.py:800-818`), so before the widening a returned spec would have vanished from `schedule`'s registry without a word while `check_trajectory` reported R-A, and both paths now parse. An open row carrying `## Handback` does not trip R-A because the partition leaves `Deliverable=''` (driven through all four readers over one real returned file). CRASH IS STILL A CRASH: `_lane_close` returns `None` for any code outside `_WORKER_OUTCOMES`, the branch stays in `active/`, the next cycle's `_parked_branches` re-assigns it, and the trunk-unmoved counter bounds a worker that keeps dying — and `_parked_branches` is byte-identical to its pre-branch text (compared blob to blob, `6b22f169` vs `HEAD`), as claimed. THEIR TWO FILED FINDINGS are both real and both correctly left as design questions: exit 0 with specs still in `active/` does park and lean on the stall guard, and `_refresh_or_quarantine` does return the refusal when `"merged" in outcomes.values()`. DEVIATIONS: the new module is justified (`integrate.py` 1588 with the largest separable unit already extracted; the WI-374 precedent is apt) and the baseline entry's "what remains is irreducibly its own" survives inspection — claim + `_abandoned_claim`, the outcome read the slot gates on, and the verdict gate are three halves of decisions that live here; `EXIT_TRAIN_END`'s exclusion falls to the crash path, which parks rather than deciding, so it is safe under either sibling outcome; the absent `handback` CLI and the absent `Contracts:` line are both argued from §A9.1 and I agree the IF-080 drift belongs to WI-390 rather than to a sibling that would paper it over. THE DUPES RE-STAMP IS A MOVE, NOT NEW DUPLICATION: two fingerprints change, the third pair (`check_trajectory == schedule`, `1dbf7e455ac3`) does not, which is only possible if the added lines land inside the already-matched block — the count of allowed pairs is unchanged. THE SLOW_MODULES ADDITION IS MEASURED, not assumed: `pytest -q tests/test_handback.py --durations=0` = **10 passed in 10.01s** serial, ~1/6 of the stated 60 s commit-bar budget, in the same class as its two siblings. BARS, run in the foreground on a tree verified byte-identical to `HEAD` and with NO probe overlapping (every probe below constructs its own temp repos and imports the scripts read-only; `git status --short` empty before and after): `pytest -q -n auto` = **1 failed, 1762 passed, 12 skipped in 437.39s**, matching the claim exactly, sole red the standing `test_this_repo_is_not_a_work_branch` (`assert 'wi-387' is None`); `check_docs --stale` **OK - 339 doc(s), 971 intra-repo link(s), 0 broken**; `check_trajectory --root . --strict` rc=0 **clean (389 work item(s), 366 done (94%), 16 cancelled, graph acyclic)** with pre-existing WARNs only (IF-055/080/081 undeclared, four connectivity WARNs, five SpecRef-post-dates advisories); `check_doc_refs --root . --strict` rc=0; `ruff check .` All checks passed; `ruff format --check .` 148 files already formatted. -> no remedy owed -> @owner
- none — SCOPE AND WHAT I DID NOT COVER, stated plainly. `git merge-base --is-ancestor <trunk> HEAD` is false: the branch has not merged trunk, so this verdict is about the branch as it stands and makes no claim about the composed result. NOT COVERED: the real `agent_loop` worker path (every drive test I re-drove uses the injected worker seam or a stub bar, so the mapping from a live session's exit code to `_WORKER_OUTCOMES` is read, not driven); concurrent lanes (`wi-383`, `wi-378`, `wi-391` are live and I touched none of them); the interaction of a handback with a MULTI-id branch (all my topologies claim one WI); `quarantine` under a symlink, submodule or mode-only change (I drove add/modify/delete/rename); and the spine debt (`LLR-143`, `TC-137`, `PROCESS_OPTIONS.md`) which the fragment correctly defers to WI-390 and which I read but did not re-verify against the registries. The two MAJORs are both in code that DELETES or REVERTS on a branch, which is why they are MAJOR rather than MINOR: neither is reachable by an honest lane doing ordinary things TODAY, but both are silent when they fire and both are one plausible lane away (a rename in a build; a hand-cut branch name collision), and in each case the suite is green while the behaviour is wrong. -> no remedy owed -> @owner
VERDICT: CHANGES-REQUESTED findings=6

### REVIEWER — G3 — Round 2 — 2026-08-01
Verdict: APPROVE
Findings:
- none against MAJOR 1 (the rename parse) — fixed, and BOTH round-1 exploits re-driven from scratch on new topologies rather than against their tests. (a) The damaging alignment, which is the one that lost the failing file: raw fields `['R100','Aold.py','Anew.py','D','docs/work/active/…','A','docs/work/queued/…','M','z_broken.py']` now parse as four records, the quarantine reports `3 path(s) reverted`, and `git show wi-401:z_broken.py` is back to the base content `ok = 1` — i.e. THE FAILING FILE IS ACTUALLY REVERTED, where round 1 left it `VALUE = (` while printing a confident count. `Aold.py` is restored, `Anew.py` is gone, and the `.patch` carries `z_broken`, `Aold` AND `Anew`. (b) The late-rename shape: `broken.py` gone, `src/old_name.py` restored, `src/new_name.py` gone, the patch carries all three and `git apply --check` accepts it — so lossless as well as inert. TRUNCATION is right in both directions and I drove the boundary rather than the happy case: `['M']`, `['R100','a']` and `['R100','a','b','M']` all return `None` (refusal), while `[]` returns `[]` — an empty diff is not a short read, which is the distinction that matters since `changed == []` has its own legitimate refusal. THE RETURN CODES are genuinely checked: fault-injecting a record naming a path absent at the base gives `the quarantine revert of wi-401 FAILED on no-such-file.py (checkout M) - the lane is reset to its tip and nothing was quarantined: error: pathspec 'no-such-file.py' did not match any file(s) known to git`, with the lane back at its tip, clean, and the lane's own work still present — the opposite of round 1, where four no-match calls were discarded in silence. Their own test fault-injects on the LAST step, which is the better placement (earlier steps have already run, so it proves the reset undoes real work) and is stronger than my probe. ON `C` (copy), asked and answered precisely: `diff_records` consumes three fields for it and `_revert_ops("C100", [orig, copy])` returns `[('rm', copy), ('checkout', orig)]`, which is correct — but I could not get git to EMIT a `C` from the call `quarantine` actually makes. With `diff.renames=copies` set in both the trunk and the lane, a genuine copy still came back as `A src/copy.py`, because plain `--name-status` needs `--find-copies-harder` to report a copy whose source is unmodified. So the `C` arm is defensive rather than exercised by shipped behaviour, and the copy is handled correctly anyway through the `A` path (driven: copy removed, original untouched, broken file reverted). That is the right shape — the parse must not desynchronise if a repo ever configures its way into a `C` — and I record it so nobody later reads the arm as dead and deletes it. `git add -A` is gone; the artefact is staged by name. -> no remedy owed -> @owner
- none against the mutation isolation — verified independently, and it is exactly as claimed. I applied each mutant to the shipped `integrate.py` myself and ran `tests/test_integrate.py` serially. SUFFIX-MATCH MUTANT (restore `endswith("-> active/<branch> (bookkeeping)")` in place of the `_claim_subject` equality): **2 failed, 93 passed**, and the two are precisely `test_a_subject_that_merely_ENDS_like_a_claim_is_not_a_claim` and `test_a_genuine_claim_subject_for_a_DIFFERENT_id_is_not_this_claim` — the subject fact and its `wi_id` half. CONTENT-CHECK MUTANT (`return True` before the diff read): **1 failed, 94 passed**, and the one is `test_a_one_commit_branch_carrying_work_is_never_read_as_abandoned` — the content fact. Nothing else moves in either direction, so the two facts are each pinned rather than jointly pinned by neither, which is the distinction that was absent in round 1. THAT THEY REPORTED THE FIRST ATTEMPT FAILING is the right call and worth recording: negatives that all carried product content would have let a suffix matcher pass while looking like three guards, and the fix (making two of them bookkeeping-only so only the subject can reject them) is what converts resemblance into conviction. Tree restored and verified byte-identical (`git diff --exit-code` clean) before any bar was run. -> no remedy owed -> @owner
- none against MINOR 3, 4 or 5 — all three re-driven. OUTCOMES: all THREE folder pairs, including `('complete','cancelled')` which their test does not cover, now give `({}, ['WI-401-widget.md'])` and `integrate_one` refuses with `left claimed spec(s) without exactly ONE declared state directory`, trunk unmoved — so fail-closure is where the outcome is read, not borrowed from another script's duplicate-id rung. (The uncovered pair runs the identical code path, so the test's two cases are sufficient; I mention it only because I drove the third.) THE HOOK LIST is accurate against my own independent measurement: `trunk_step.REGEN_STEPS` is `arch-map, okf, derived-gate, trajectory, status, open-items`, which maps onto the hook's `arch-map, okf, derived-gate, trajectory-map, status-map, open-items` — 6 of the 10 `--run-steps` — leaving `registry-integrity`, the `trajectory` SSOT check, `skills-sync` and `ratify-fresh` uncovered, and the hook's other three groups (`check_privacy --author`, the always-on secrets floor, `format`) plus `commit-msg` outside `--run-steps` entirely. That is the docstring's list, item for item, including the two it correctly flags as non-vacuous. The three-window account of the claim is also right and the retraction is unhedged. `_lane_close`'s already-closed arm resolves my finding 5 in the direction I would have chosen: the tree has named an outcome, so the drain merges it on that rather than the driver overruling the tree with an exit code. MINOR 6 is recorded in all three places asked for. -> no remedy owed -> @owner
- [MINOR] project-trajectory/scripts/integrate.py:467 -> The re-claim prints that it deleted a branch it did NOT delete, because `git branch -D`'s return code is unchecked — the same discarded-code class this round just fixed in `_revert`, still present eight lines from it. Driven: crash a real claim (`claim`, then `reset --hard HEAD~1`) while a LANE WORKTREE holds the branch, which is the ordinary state after a worker has started. `_abandoned_claim` correctly returns `True`; `ac.git(root, "branch", "-D", branch)` then fails (git refuses to delete a branch checked out in a worktree) and the failure is thrown away, so the console says `integrate: deleted the abandoned claim branch wi-401 (was 06f820dee5; recoverable via git reflog / git branch wi-401 06f820dee5)` over a branch that is still there. The claim then fails at the next step with `integrate: REFUSED - branch cut failed (trunk restored): fatal: a branch named 'wi-401' already exists` — which is fail-closed and correct in outcome (I verified rc=1, trunk clean, the spec back in `queued/`, the branch intact), but the operator is handed two false statements in a row: that the branch was deleted, and that the problem is a branch cut, when the actual diagnosis is "the lane worktree at <path> holds it". `_worktree_holding` already exists in this module and would name the holder. Not blocking — nothing is lost and nothing merges — but a deletion path that reports success on failure is precisely the shape that made round 1's quarantine defect invisible. -> Check the `branch -D` code: on failure, refuse naming the holder (`_worktree_holding`) instead of printing a deletion that did not happen; print the sha only on the success path. -> @owner
- [MINOR] project-trajectory/scripts/integrate.py:360 -> The fourth fact convicts on "touches only bookkeeping-or-generated surfaces", which is broader than "is what a crashed claim leaves", so a branch whose content IS bookkeeping-shaped can still be destroyed — the case I was asked to probe, and it is real. Driven, all with the tip subject EXACTLY `_claim_subject("WI-401","wi-401")`, one commit past a trunk ancestor: a commit adding only `docs/log.d/WI-401-hours.md` is convicted (`True`), the re-claim deletes the branch and the fragment is gone from the re-cut branch; a commit rewriting only `docs/log.md` is convicted; a commit hand-writing only the declared `PROJECT_STATE.html` is convicted. A log fragment is somebody's work. THE HONEST BOUND, which is why this is MINOR and not a repeat of round 1: the loss is confined to bookkeeping surfaces (not arbitrary product code), the sha and a `git branch <b> <sha>` restore line are now printed so it is auditable, and reachability is very low — the full ladder must pass first, so the WI has to be in `queued/` AND on the ready frontier while a branch named `wi-<id>` carries the exact claim subject, which only `claim()` writes. The realistic residual is an AMENDED claim commit (an agent folding its fragment into the claim rather than committing beside it), and even that needs the WI to have been returned to `queued/` first. A tighter conviction is available at no cost, since the shape is known exactly: the tip's diff must be the spec move (`queued/<spec>` deleted, `active/<branch>/<spec>` added) plus declared generated paths — nothing else. SECOND, SMALLER: the comment at :355 says "A repo that has not declared its generated artifacts therefore fails CLOSED here", which is true only of commits that TOUCH a generated path. Driven both ways — with no `stack.ini` at all, a bookkeeping-only commit under the claim subject still returns `True`, while a REAL crashed claim (whose regen wrote `PROJECT_STATE.html`) returns `False` and the re-claim refuses with rc=1. The mechanism works for the case that matters and the fixture change is genuinely REQUIRED rather than convenient — I mutated `declare_generated` out of `claim_repo` and `test_a_crashed_claim_leaves_an_orphan_branch_the_next_claim_re_cuts` and `test_the_re_claim_names_the_sha_it_deleted` both red (`branch wi-401 already exists`) — but the sentence claims more than the code does. -> Narrow the content fact to the claim's own shape (the spec move plus declared generated paths), and restate :355 as "a repo that has not declared its generated artifacts cannot have its own claim commits recognised, so a crashed claim there refuses rather than re-cuts". -> @owner
- [MINOR] docs/log.d/WI-387-terminal-outcomes-no-hanging-branch.md:165 -> The deviation bullet states the shipped baseline as `1418 → 1638`; the shipped stamp is **1643**, which the same fragment gets right 59 lines later ("NEW 1588, re-stamped to 1643 across round 2"). `1638` was the intermediate the ratchet's own two-part entry passes through (+50, then +5 for the fail-closed comment), so the bullet is one increment stale rather than invented — but this is the surface that compiles into `docs/log.md` as the permanent record of what shipped, the fragment contradicts itself, and I re-measured independently: `integrate.py` is **1643** lines at `73f85377`, `handback.py` 353 and `drive.py` 495, the latter two well under `THRESHOLD = 1500` and correctly carrying no baseline entry. -> Change `1638` to `1643` at :165. -> @owner
- none — BARS, re-run in the foreground on a tree verified byte-identical to `HEAD` (`git diff --exit-code` clean after both mutants were reverted, and `git status --short` empty immediately before and after the suite; no probe ran concurrently — every topology above is built in its own temp repo and imports the scripts read-only). `pytest -q -n auto` = **1 failed, 1771 passed, 12 skipped in 486.90s**, matching the claim exactly, and the +9 reconciles against the diff (test_handback 10 → 13, test_drive +1, test_integrate +5). Sole red the standing `test_this_repo_is_not_a_work_branch` (`assert 'wi-387' is None`), which fails by construction on a work branch. `check_docs --stale` **OK - 340 doc(s), 971 intra-repo link(s), 0 broken**; `check_trajectory --root . --strict` rc=0 **clean (389 work item(s), 366 done (94%), 16 cancelled, graph acyclic)**, pre-existing WARNs only; `check_doc_refs --root . --strict` rc=0; `ruff check .` All checks passed; `ruff format --check .` 148 files already formatted. THE RED THEY CAUSED AND RECORDED: `test_integrate_refuses_and_holds_the_trunk_when_the_bar_is_undeclared` still asserts what its name says — it asserts `"docs/stack.ini is absent" in out`, the ABSENT-file refusal, not the weaker "no [product] key" one the fixture's new `stack.ini` would otherwise have produced. The file is unlinked after the claim that needs it, with the reason at the site. That is the right resolution and I checked the assertion text rather than taking the description. SCOPE: `git merge-base --is-ancestor <trunk> HEAD` is still false — the branch has not merged trunk, so this APPROVE covers the branch as it stands and makes no claim about the composed result. NOT COVERED this round, deliberately: everything I listed as uncovered in round 1 (the real `agent_loop` worker path, concurrent lanes, multi-id branches, the spine debt owed to WI-390) is still uncovered, plus `quarantine` under symlinks, submodules and mode-only changes — I drove add/modify/delete/rename/copy and stopped there. THE VERDICT PLAINLY: this is an APPROVE. Both MAJORs are fixed at the mechanism rather than at the symptom, both original exploits refuse or revert correctly when re-driven from scratch, the isolation claim is true under my own mutants, and the three remaining findings are all MINOR — two are narrowings on a path that now fails closed and prints its own undo command, and one is a stale number in the record. None of them blocks the queue. -> no remedy owed -> @owner
VERDICT: APPROVE findings=3
