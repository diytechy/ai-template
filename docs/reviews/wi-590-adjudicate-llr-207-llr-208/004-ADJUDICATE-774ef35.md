# WI-590 ADJUDICATE round 004 — 774ef35

Mixed batch. `LLR-208`/`TC-206` are APPROVED: the single ground WI-586 returned
them on is now genuinely discharged, re-driven here by mutation rather than
inherited. `LLR-207`/`TC-205` are RETURNED on a finding that is NEW to this
round — not one of WI-586's, all of which I confirmed fixed — because the module
`LLR-207` owns grew a second disposable-commit class after the row text was
written, and neither the requirement nor its test followed it.

Provenance of the return, established before ruling: the rows are NOT the
byte-identical text of the prior rereads. `WI-587` (`64692ddf`) and `WI-588`
(`95686911`, `6f274193`) landed the repairs, and `f4ca1bd5` ("four batch-lane
defects", merged at `c590637d`) landed the peel change that this round faults —
after the last edit to `LLR-207.detail`. The row is stale against its own
module, not wrong as authored.

Instruments run here: `check.py --jobs 0` → `RESULT: PASS` (`derived-stage`,
`approval-fresh`, `verdict-rollup` SKIP as trunk-owned on a work branch);
`trace.py --strict-integrity` → `orphans=0 integrity=0 interface-findings=0`.
The three cited test modules → `139 passed in 83.22s`. Working tree clean after
every mutation arm (`git status --porcelain` empty).

- [RETURN] LLR-207 -> a builder must implement one tree-bound verdict record in `kitlib/verdict.py`: a byte-preserving record fold, a verified reset peel and a read-only governing walk, a logged-session evidence join, a declared phase span and count, and a trailer cross-check that cannot create an approval -> SR-156 requires the fail-closed verdict rung at the serial seam, its siblings (LLR-138/140/145/150/151) leave that decision here, TC-205 is its sole downward verifier, and CMP-006's note — repaired by WI-587 — now correctly names this module as CMP-008 with IF-175 as its seam -> NOT ready: the `governing_rev` clause says the walk peels "any verified refresh it meets", but the walk peels through `_peel_target` (`verdict.py:431-442`), whose own docstring reads "TWO commits are disposable and this is their one home" — the station refresh AND `mechanical_close_attestation` (`:376-428`), the machinery's own adjudication close, verified by exact subject, single parent and all-paths-under-`docs/work/`. That second class is a public `__all__` export that moves the governing identity on real lanes, it is absent from this `detail` and from `code_symbol`, and it is described by NO row in any requirements registry (grep: zero hits) — so the row is not merely incomplete, it is the mechanism's only possible home. A builder implementing this cell literally builds a one-class peel and reproduces exactly the defect `f4ca1bd5` repaired: a mechanical close staling the round that had just judged the lane.
- [RETURN] TC-205 -> the test must verify every clause LLR-207 states and accurately describe the fixtures it drives -> its SR-156/LLR-207/IF-175 chain calls for the identity, provenance, peel, phase and cross-check arms it enumerates at length, and every one of its 46 citations resolves to a test that exists in the file it names and passes (verified node by node — the prior round's Evidence and fixture-misstatement findings are discharged) -> NOT ready for one reason and one only: its `Method` section headed "THE PEEL" enumerates the refresh class alone, and neither `Method` nor `Evidence` contains the string "mechanical". The second peel class HAS three shipped, passing tests — `test_the_mechanical_close_does_not_stale_the_round_it_follows` (`tests/test_verdict_record.py:1600`), `test_only_the_machinerys_own_close_subject_peels` (`:1628`), `test_a_close_that_reached_outside_docs_work_does_not_peel` (`:1640`) — and they are cited by NO test case in the registry at all. So the coverage exists and the record does not claim it: a behavior of this row's own module goes unverified by the row that exists to verify it, while the tests that would verify it sit orphaned. Secondary, non-blocking on its own: `Tier = "Smoke"` overstates the cheap gate — 8 of the 46 citations live in `test_integrate_admission`/`test_integrate_station`, both in `conftest.SLOW_MODULES` and so excluded from `-m smoke` (measured: 56 of 139 collect as smoke).
- [APPROVE] LLR-208 -> a builder must generate one factual rollup per parser-defined review scope, refuse the reserved-stem name collision on both arms, make check and write agree on absent/diverged/extra, prune retired scopes, stand down on a work branch, and regenerate ONLY at the serial trunk step -> SR-170 requires exactly this trunk-only exclusive-writer contract at a shared authority surface, the sibling LLRs (060/124/137/141) hold the other surfaces without overlapping this mechanism, and TC-206 is its sole verifier -> READY. Every detail clause verified TRUE against `gen_verdict_rollup.py`: the scope set is the name parser's `train` over `rglob` (`scopes()`, `:73-94`), not directory enumeration; the flat layout takes the reserved `FLAT_SCOPE = "_flat"` stem (`:55`, `:102-108`); the collision is refused by name BEFORE the check/write branch so both arms are covered by construction (`:165-175`, `:239-246`); `--check` reports absent, diverged-on-normalized-line-endings and extra (`:177-189`); the write path prunes (`:208-224`); and the rendered `HEADER` (`:62`) carries "**The merge gate does not read this file.**", which is what keeps the artifact honest about its own standing. The load-bearing clause — that membership in the trunk regen set is part of THIS contract — is now true and enforced, which is the sole ground of the prior return and is discharged below.
- [APPROVE] TC-206 -> the test must prove rendering, every freshness answer AND its remedy, flat-scope and collision behavior, the non-authority notice, the work-branch stand-down, and execution through the serial trunk regenerator -> SR-170 and LLR-208 make that trunk connection the reason this derived artifact is permitted at all, and all five cited nodes exist in the files they name -> READY. The prior round returned this pair because deleting the `verdict-rollup` member of `trunk_step.REGEN_STEPS` left every cited node green. I re-drove that mutation myself rather than trusting the fix: deleting lines 588-593 (`ast.parse` clean, `verdict-rollup` occurrences → 0) takes `tests/test_trunk_step.py` from `17 passed` to `1 failed, 16 passed`, failing `test_regen_really_writes_the_verdict_rollup` on `assert 'regen — verdict-rollup ok' in ...` — a message that names the step that stopped running. Restored → `17 passed`. The arm calls `ts.regen(tmp_path)`, the trunk step's own entry point, and never invokes the generator directly, which is precisely the distinction the finding turned on. My first mutation attempt was a regex that broke the module's syntax and proved nothing; the result above is from the corrected, valid arm.

OBSERVATION FOR THE COORDINATOR: the returned half cannot be repaired by another
reread — an adjudicator may flip a `Status` and take a snapshot, never edit the
cells its findings name. The useful next act is this row's merge, which mints
the disposition below. The approved half is flipped and anchored in the commit
following this one; `LLR-207` and `TC-205` stay `Drafted` inside the same
snapshotted registries, which is what they are.

OUTCOME: RETURN rows=4
