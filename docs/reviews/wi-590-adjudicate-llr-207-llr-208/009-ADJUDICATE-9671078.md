# WI-590 ADJUDICATE round 009 — 9671078

Read at `fa4683c3`; the two commits since `9671078` are `docs/log.d/` telemetry
and `docs/reviews/` records, both `RECORD_PREFIXES` paths, so the governing
identity is unmoved and this is the tree round 008 reviewed.

Scope is TWO rows. `LLR-208` and `TC-206` were approved by round 004 and
anchored at `a1d80c6f`; they are not in this batch and I touched neither.

PROVENANCE FIRST, because it decides what this act can usefully do. The
registries have not been edited since `a1d80c6f`, whose only spine content was
those two `Status` flips. `LLR-207` and `TC-205` are therefore BYTE-IDENTICAL to
the text rounds 004 and 006 read and returned. This is the third consecutive
return on one unchanged pair, which is a property of the machinery and not of
the rows: an adjudicator may flip a `Status` and take a snapshot, and may never
edit a cell its findings name. No further reread can change this answer.

I did not inherit the finding. Every claim below is re-derived on this tree from
the source, not from the round-004 or round-006 text.

Instruments, once each: `check.py --jobs 0` → `RESULT: PASS` (`derived-stage`,
`approval-fresh`, `verdict-rollup` SKIP as trunk-owned on a work branch);
`trace.py --strict-integrity` → `SN=27 SR=76 LLR=191 TC=190 orphans=0
integrity=0 interface-findings=0 paraphrase-advisories=3`.

Driven here, not read:

- `governing_rev` peels through `_peel_target` — `verdict.py:431` defines it,
  `:542` is the single call site — and `_peel_target` returns
  `refresh_attestation(...)` OR `mechanical_close_attestation(root, rev)`
  (`:376`). Two classes, one walk.
- `mechanical_close_attestation` is a public export: `__all__:141`.
- `grep` for it across `docs/requirements/` and `docs/test/` returns ZERO rows.
  It is described by no requirement in the repo, so `LLR-207` is not one of
  several candidate homes — it is the only one.
- The three tests for the second class exist at `tests/test_verdict_record.py`
  `:1600`, `:1628`, `:1640` and pass: `3 passed, 53 deselected in 2.62s`. No
  test case in the registry cites any of them (`grep -c mechanical_close
  docs/test/test-cases.toml` → `0`).
- `TC-205` has exactly 46 citations, ALL of which resolve to a `def` in the file
  they name (checked node by node, `UNRESOLVED: []`); 38 are
  `test_verdict_record`, 3 `test_integrate_admission`, 5
  `test_integrate_station`. The latter two modules are both in
  `tests/conftest.py` `SLOW_MODULES` (`:95`, `:96`), so 8 of 46 citations are
  excluded from `-m smoke` while the row declares `Tier = "Smoke"`.
- Sampled the clauses the findings do NOT touch, to be sure the return is
  narrow: `declared_phases` really clamps (`:762`, `REVIEW_PHASES[: max(0,
  required)]`) and `round_count` really takes the per-phase max (`:807`). Those
  clauses are true as written. The row is accurate everywhere except the two
  cells below.

- [RETURN] LLR-207 -> a builder must implement one tree-bound verdict record in `kitlib/verdict.py`: a byte-preserving record fold, a verified tip-only reset peel, a read-only governing walk that admits every verified machine-authored commit class moving the tree without changing what the lane claims, a logged-session evidence join, a declared phase span and count, and a trailer cross-check that cannot create an approval -> reading UP, SR-156 requires the serial seam's fail-closed verdict rung and this is where that rung is decided; SIDEWAYS, the siblings hold other surfaces without reaching this one — LLR-138 the pause, LLR-140 the integrator, LLR-145 the spec move, LLR-150 lane mechanics, LLR-151 the claim authority — so no other row can carry it; DOWN, TC-205 is the sole verifier -> NOT ready. The `governing_rev` clause says the walk peels "any verified refresh it meets" — one class — while the walk it describes delegates to `_peel_target`, whose own docstring says "TWO commits are disposable and this is their one home". The second, `mechanical_close_attestation`, is verified against git by exact subject, single parent and all-paths-under-`docs/work/`, and it exists because an adjudication lane CANNOT close before its final round: the close lands after the APPROVE and staled the round that had just judged the lane (measured on WI-586). A builder implementing this cell literally builds the one-class peel and reproduces exactly that defect. The omission is also in `code_symbol`, which lists 21 symbols and not this one. Because no registry row anywhere describes the function, the gap is not a thin cell — it is a shipped, `__all__`-exported behavior of this row's own module that the requirement record does not contain at all.
- [RETURN] TC-205 -> the test must verify every clause LLR-207 states and describe the fixtures it actually drives -> its SR-156 / LLR-207 / IF-175 chain calls for the identity, encoding, trailer, evidence-join, phase, cross-check and peel arms it enumerates at length, and it discharges that faithfully for all of them but one: all 46 citations resolve and the row's fixture descriptions check out -> NOT ready for a single reason. Its `Method` section headed "THE PEEL" enumerates the refresh class alone, and neither `Method` nor `Evidence` contains the string "mechanical". The second peel class has three shipped, passing tests — the positive (`test_the_mechanical_close_does_not_stale_the_round_it_follows`) and both refusals (`test_only_the_machinerys_own_close_subject_peels`, `test_a_close_that_reached_outside_docs_work_does_not_peel`) — cited by no test case in the registry. So the coverage exists and the record denies it: the row that exists to verify LLR-207 leaves a behavior of LLR-207's module unverified on paper while the tests that would verify it sit orphaned. Approving this text would enter a standing claim that the peel was reviewed as a one-class rule. Secondary and rulable either way, so not the ground of this return: `Tier = "Smoke"` while 8 of 46 citations sit in `SLOW_MODULES` and never run under `-m smoke` — sibling TC-132 cites the same station module at `Full`. Both readings are currently available and one of them should be closed.

DISPOSITION: the follow-up is ALREADY DRAFTED in this spec's `## Dispositions`
and I am not duplicating it — a second block would mint a second work item for
one gap. I re-verified it verbatim rather than assuming it: it parses to exactly
one draft (`intake.parse_dispositions` → `refusal=None`, 1 draft, `kind='spine'`,
`bar='DevStg-Tests'`), and all four of its items are still true at this tree,
line numbers included (`_peel_target` at `:431`, `mechanical_close_attestation`
at `:376`, the three tests at `:1600`/`:1628`/`:1640`, both station modules in
`SLOW_MODULES`). It needs no amendment.

NO REGISTRY CELL AND NO SNAPSHOT WERE TOUCHED. Both rows returned, so every
`--approves` token is dropped: naming a registry this act blessed nothing in
would re-anchor its live text. That also means round 008's MAJOR finding — the
whole-file anchoring copy silently absorbing 14 drifted Approved rows — cannot
recur through this act, because this act takes no copy. The finding stands
unchanged against the kit and remains routed `@owner`.

FOR THE COORDINATOR: do not schedule a fourth read of this pair. The rows cannot
change until the drafted disposition is minted, and it is minted at THIS row's
MERGE. The merge is the exit.

OUTCOME: RETURN rows=2
