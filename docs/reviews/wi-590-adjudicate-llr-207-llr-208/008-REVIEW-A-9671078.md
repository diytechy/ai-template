# WI-590 REVIEW-A — round 008, reviewed sha 9671078

Scope: `git diff contract_split...HEAD` minus records/generated. Six files: two
`Status` flips (LLR-208, TC-206), the anchoring snapshot copy of both
registries, the archive stamp line, and a `## Dispositions` draft returning
LLR-207/TC-205.

Failure classes this change admits, hunted in this order: (1) **silent wrong
content in the approval record** — a whole-file copy that blesses text no act
read; (2) **a wrongly-approved spine row**, which the record then carries
forward as a standing claim; (3) **a ruled follow-up silently dropped** by a
draft that will not parse at the mint.

Instruments, run once each on this tree:

- `python3 project-trajectory/scripts/check.py --jobs 0` → `RESULT: PASS`
  (registry-integrity, vocabulary, need-form, privacy, doc-navigability,
  skills-index, prompt-catalog, staged-divergence, approval-immutable PASS;
  derived-stage / approval-fresh / verdict-rollup SKIP on a work branch).
- `python3 project-trajectory/scripts/trace.py --strict-integrity` →
  `Traceability: SN=27 SR=76 LLR=191 TC=190 orphans=0 integrity=0 ...
  interface-findings=0 paraphrase-advisories=3.`

Driven, not read (class 2 and 3):

- LLR-208's approval ground re-derived by mutation, not inherited: deleting the
  `verdict-rollup` row from `trunk_step.REGEN_STEPS` reds
  `test_regen_really_writes_the_verdict_rollup` naming the step —
  `assert 'regen — verdict-rollup ok' in ...` / `1 failed in 0.06s`; tree
  restored, `git status --porcelain` empty. The detector is real.
- TC-206's five cited evidence tests: `5 passed in 1.86s`. Its `Method` claims
  (collision refused ahead of BOTH arms, extra-file pruned by the write pass,
  normalized line endings, reserved flat stem) check out against
  `gen_verdict_rollup.py:165-248`. None of its four cited modules is in
  `tests/conftest.py` `SLOW_MODULES`, so its `Tier = "Smoke"` is honest — the
  tier defect the disposition raises for TC-205 does **not** also sit on the row
  that was approved.
- The disposition draft parses: `intake.parse_dispositions` returns one draft,
  refusal `None`, `kind='spine'`, `bar='DevStg-Tests'`, scope prose attached.
- Every measurable claim in the disposition re-derived independently and holds:
  LLR-207 `code_symbol` omits `mechanical_close_attestation`; its `Detail` says
  "peeling any verified refresh"; TC-205 has exactly 46 citations (38
  `test_verdict_record`, 3 `test_integrate_admission`, 5
  `test_integrate_station`), `Tier = "Smoke"`, and neither cell contains
  "mechanical"; the three named tests exist at :1600/:1628/:1640 and give
  `3 passed, 53 deselected`; TC-132 is `Full` and cites the station module;
  `docs/registry-machinery-reference.md:836 §12.2` exists; `work_tip`'s
  docstring does still claim "governing_identity measures code-time here" while
  calling `refresh_attestation` directly. The return is well-founded.

## Findings

- [MAJOR] docs/archive/last_approved/README.md:17 -> the anchoring copy re-blessed FOURTEEN Approved rows outside this act's scope, silently. Driven with the kit's own predicate: at the merge base and still on `contract_split` today, `baseline_snapshot.is_drifted` fires on LLR-045, LLR-058, LLR-136, LLR-140, LLR-144, LLR-158, LLR-197, LLR-198, LLR-203, LLR-204 (Detail/Rationale/Title) and TC-082, TC-138, TC-147, TC-194 (Method) — 14 rows owing re-attestation; at HEAD the count is 0 in both registries. Nothing this lane read, and no line of the round-004 or round-006 verdict, names one of them: `refresh_ledger`'s own docstring calls this set "the approved text a copy would silently re-bless", and `refresh_refusal` waves it through because LLR-208's flip is a `Status` move "in that same registry" — an authority rule whose stated premise is a human sitting that "rules a registry's rows together", which is false for a machine adjudication lane whose `Adjudicates` cell names four rows. After merge the record asserts an owner blessed 14 amendments no one read, and the trunk-side re-attest debt disappears with no trace -> in THIS lane, enumerate the absorbed rows (run `refresh_ledger`) and record them in the round file and in a `## Dispositions` draft so the debt survives the copy, or hold the flip until an act names them; upstream, `project-trajectory/prompts/adjudicate-first-approval.template.md:92` step 2 tells the adjudicator only that returned rows "stay Drafted inside it" and is silent on pre-existing drift, so the brief must show the absorb ledger the same way it shows `approves_rows` -> this cannot be made unrepresentable inside this diff: `unanchored_findings` is decidable only because the snapshot is a WHOLE FILE ("row extraction would have deleted the very evidence this reads"), so at the file granularity the record is stored at there is no representation that distinguishes blessed text from absorbed drift; making it unreachable means per-row anchoring, a kit redesign well outside a spine adjudication lane -> @owner
- [MINOR] docs/archive/last_approved/README.md:17 -> for clarity: the stamp reads `ref: WI-590` for both registries, while this file's own header at :6 promises "a human's citation of the act that authorised a refresh" and every prior line quotes the owner's words and names a `docs/log.d/` record. A bare work-item id cites no act and no human, and the one artifact that does record the judgement — `docs/reviews/wi-590-adjudicate-llr-207-llr-208/004-ADJUDICATE-774ef35.md`, whose `- [APPROVE] LLR-208` / `- [APPROVE] TC-206` lines are the actual authority — is named nowhere in the record the header says carries it. The lane did not choose this: `adjudicate_brief.py:780` hard-codes the ref as `wi_id`. Sharper wording resolves it -> either have the generator emit the verdict path it already knows as the ref, or amend the README header to say what the machine-written lines actually are (a pointer to the adjudication row, not a human's citation) -> @owner

VERDICT: CHANGES-REQUESTED findings=2
