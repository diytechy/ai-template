### REVIEWER — G3 — Round 1 — 2026-07-31
Verdict: APPROVE
Findings:
- [MINOR] docs/work/archive/WI-374-unattended-entry-point-claimer.md:12 (repeated at docs/log.d/WI-374-unattended-entry-point.md:8) -> The signed size figure "`scripts/drive.py` (269 lines, stdlib only)" matches no measure of the shipped file: the repo's own ratchet metric (`len(text.splitlines())`, tests/test_module_size_ratchet.py:40) gives **310**, non-blank gives 274, non-blank-non-comment gives 261 — and `git diff HEAD~1 --numstat` is `310 0 project-trajectory/scripts/drive.py` in the single build commit, so no intermediate state explains 269. The neighboring figures are honest (agent_loop "+20 lines net" = 41 added − 21 removed exactly; ratchet re-stamps 3006→3026 and 2078→2085 both equal the tip's `wc -l`; "~280 net new production code" is defensible as non-blank-non-comment 261 + 20 = 281), which makes the one wrong number stand out in the Done-when's own box ("the WI's own record states the line count against the 4,042 that were deleted"); the archived Deliverable's "a 15-line delegation" is similarly loose (delegation + `_coordinator_lock` extraction = 21 new lines with docstrings). -> Re-stamp to the ratchet metric (310) or name the metric being counted, in both the fragment and the archived Deliverable. -> @owner
- [MINOR] docs/log.d/WI-374-unattended-entry-point.md:68 -> "**Bars:** ... smoke + `check_docs --stale` green at each commit" signs a green a fresh checkout of this branch cannot reproduce, in both halves. Driven at the tip: `pytest -q -n auto -m smoke` = **1 failed, 555 passed in 11.68s** — the sole failure is tests/test_check_lane.py::test_this_repo_is_not_a_work_branch, the standing WI-357 work-branch conditional that reds BY DESIGN on any claimed-branch checkout (`check._work_branch('.')` answers `wi-374-unattended-entry-point`; trunk answers None; the WI-372 review record reports these identical totals WITH that qualifier, and the merge gate is unaffected because the candidate worktree runs on CANDIDATE_BRANCH, for which the detector answers None). And bare `check_docs.py --stale` exits **1** on this branch AND on trunk alike (the same 4 pre-existing broken links, all in docs/work/archive/); only the bar's configured step (`--ignore docs/test/report.md --ignore "docs/work/*" --stale`) is green — driven: OK, 326 doc(s), 932 intra-repo link(s), 0 broken. The full unfiltered suite at the tip — the totals the fragment defers to this record — is **1 failed, 1700 passed, 8 skipped in 599.68s**, the sole failure the same standing work-branch conditional. Nothing behavioral: both reds are pre-existing/environmental, neither is this WI's code, and `tests/test_drive.py` itself is **10 passed in 28.49s** with smoke-tier collection **0** (10 deselected — slow-tier membership real, smoke budget untouched). -> Restate per the WI-372 record convention: "smoke 1 failed (the standing work-branch conditional), rest green; the bar's doc-navigability step green" — the compiled log is the permanent record and should carry the reproducible sentence. -> @owner
- [MINOR] project-trajectory/scripts/drive.py:191-196 with :240-243 -> The drained banner's integrated count omits residue merges — including the exact case the code's own comment names ("a finished branch may still be waiting (e.g. built by hand between runs) - drain before declaring the queue empty"). Driven on a bootstrapped scaffold: claim WI-401, close it by hand on its branch (a finished residue branch), leave the frontier empty, then `drive.run(...)` — integrate REALLY merges it (`integrate: wi-401-widget merged (WI-401); bar PASS (34 steps, tier smoke)`, branch deleted, rc 0) yet the banner prints "queue drained - no ready work items; **0 WI(s) integrated this run.**" `merged` is incremented only by `_cycle_progress` when the cycle's OWN branch ref disappears, so any branch the serial queue drains beyond that one (residue at the final drain, or extra finished branches during a cycle's drain) is merged but uncounted in the walk-away console's summary — the one line a returning human reads first. Control flow, refusals, and exit codes are all correct; only the informational count is wrong. -> Count drain merges (e.g. diff `integrate.finished_branches`/branch refs across the drain) or narrow the banner's words to what the counter measures ("N WI(s) claimed-and-merged by this run's cycles"). -> @owner
VERDICT: APPROVE findings=3

### REVIEWER — G3 — Round 2 — 2026-07-31
Verdict: APPROVE
Findings:
- none against the round-1 findings and the four codex-derived fixes as SHIPPED BEHAVIOR — all re-driven at tip 5ab57a7 (repo `.venv`), the two record residues below are new. (1) Line count: "397 lines by the size-ratchet's splitlines method" verifies by that method exactly (397). (2) Bars wording: the fragment now names the standing WI-357 red and the configured check_docs step — re-measured true: smoke **1 failed, 555 passed in 11.78s**, full unfiltered **1 failed, 1703 passed, 8 skipped in 632.75s** (sole failure `test_this_repo_is_not_a_work_branch` both times), configured `check_docs` OK (327 docs, 932 links, 0 broken), `check_trajectory --strict` clean; `tests/test_drive.py` **13 passed** and with both ratchets 15 passed in 29.27s (`_resume_or_claim` stays under C901, `agent_loop.py:main` stays 27, drive.py at 397 < 1500 needs no ratchet entry). (3) Residue counting: my round-1 scenario re-driven — hand-closed residue + empty frontier now banners "1 WI(s) integrated this run" (was 0). Codex fixes, each at the REAL seam (plain `agent_loop.py` launch, no injection): (a) stranded claim (active spec, branch ref deleted) → exit **2** "claimed spec(s) with NO branch ref - refusing to treat this queue as drainable: wi-401-widget", spec untouched — and a normal parked branch still resumes ("cycle 1 - resuming parked branch wi-401-widget"); (b) lazy config preflight both directions — unwired + EMPTY queue → drained banner exit **0**; unwired + queued WI → exit **2** BEFORE any claim (queued spec intact, no branch, no claim commit); (c) dirty trunk before resume — parked repo + untracked file → exit **2** "the trunk working tree is dirty ..." before any worker session; (d) dial forwarding — every forwarded flag exists on agent_loop.py's parser with matching types (`--stall-limit` int, always forwarded since `str(int)` is always truthy; `--wait-on-limit`/`--limit-retry-fallback` ints forwarded as a pair only when wait_on_limit is set, matching their coupled semantics; `--live-status` store_true forwarded bare; `--model`/`--model-map` forwarded post-`resolve_coordinator_dials` so CLI > env > stack.ini resolution rides into the worker; the five maps as strings, empties skipped). The not-taken disposition (no duplicated launchability/identity preflight ahead of the claim) is recorded in the fragment and holds — the honest residual (a wired-but-BROKEN AGENT_CMD still parks one claim before the worker's own preflight refuses) is covered by the parked-resume path, and duplicating agent_loop's preflight in the driver is the dupes-census drift the disposition names. -> no remedy owed -> @owner
- [MINOR] docs/work/archive/WI-374-unattended-entry-point-claimer.md:12 (and docs/log.d/WI-374-unattended-entry-point.md:5) -> The round-1 line-count fix left the SAME Deliverable cell internally contradictory: it now opens "397 lines by the size-ratchet's splitlines method" (verified by that method: 397 exactly) but still closes "10 new tests in tests/test_drive.py" (actual: 13 — `grep -c "^def test_"` = 13, and the log fragment's own Deliverables paragraph says "Thirteen") and "Net new production code ~280 lines against the 4,042" — a figure no metric reaches at this tip (splitlines 397 + 20 = 417; non-blank-non-comment 331 + ~15 = ~346; the ~280 reconciled against the ROUND-1 file, 261 + 20, and went stale when the fix commit grew drive.py by 87 lines). The log's one-liner ":5 "as ~280 lines of composition"" carries the same stale figure. -> Re-stamp the two figures ("13 new tests", "~350" or the metric-named number) in the archived Deliverable and the fragment's one-liner — the cell is the permanent record and currently disagrees with itself two sentences apart. -> @owner
- [MINOR] project-trajectory/scripts/drive.py:246-258 -> The round-1 banner undercount is fixed only on the path it was driven on; the same count is still wrong one step away, and the fragment's "all three fixed" (docs/log.d/WI-374-unattended-entry-point.md, round-1→round-2 paragraph) overstates. Driven, pure residue (my round-1 scenario re-run at this tip): hand-closed residue branch + empty frontier → final drain merges it and the banner now says "1 WI(s) integrated this run" — fixed. Driven, mixed: hand-closed residue wi-402-gadget + queued WI-401, injected closing worker → cycle 1's NORMAL end-of-cycle drain merges BOTH through the real bar (`integrate: wi-401-widget merged (WI-401); bar PASS (34 steps, tier smoke)` then `integrate: wi-402-gadget merged (WI-402); bar PASS (34 steps, tier smoke)`), `_cycle_progress` counts only the cycle's own ref (merged=1), and by the final drain `residue = len(finished_branches)` is already 0 — banner: "queue drained - no ready work items; **1 WI(s) integrated this run.**" for a run that merged 2. The `residue` term only catches residue that SURVIVES until the empty-frontier cycle. Informational only: control flow, refusals and exit codes all correct. -> Count per-drain (branch refs or `finished_branches` before vs after EVERY `integrate.integrate` call, not just the last), or take round 1's other remedy and narrow the words to "claimed-and-merged by this run's cycles"; soften "all three fixed" to match. -> @owner
VERDICT: APPROVE findings=2

### REVIEWER — G3 — Round 3 — 2026-07-31
Verdict: APPROVE
Findings:
- none — the round-2 findings both close at tip 959a7d9, delta-scoped and re-driven. (1) Counting: my mixed residue+claim scenario re-run against the shipped code (hand-closed residue wi-402-gadget + queued WI-401, injected closing worker, REAL bar) now banners "queue drained - no ready work items; **2 WI(s) integrated this run.**" after `integrate: wi-401-widget merged ... bar PASS (34 steps, tier smoke)` and `integrate: wi-402-gadget merged ... bar PASS (34 steps, tier smoke)` — exact; and the pure-residue scenario re-run still banners exactly **1** (no double count between the per-cycle `merged += len(finished_branches)` banking and the drain path's own `merged + residue` sum — the run that ends in the empty-frontier drain never reaches the cycle banking, and by the next cycle `finished_branches` is empty). The exactness argument in the new comment holds by code-read: the count is banked only after `integrate.integrate` returns 0, a held branch makes it return 1 via `_held_summary` before banking, and a red drain returns before any banner; the stall path is preserved (`_cycle_stall` keeps the trunk-unmoved counter; a worker that reports DONE without finishing banks 0 because its branch is not in `finished_branches` — the parked-stall test still passes). (2) Records: `drive.py` is **400** by the stated ratchet splitlines method (measured 400 exactly), "13 tests" matches `grep -c "^def test_"` = 13, and "~427 net new ... (drive.py 400 + the agent_loop delegation +20 + the bootstrap MAPPING rows +7)" is itemized, metric-named, and arithmetically exact against the numstats already verified in rounds 1-2; the fragment's "Merged" design bullet now describes the shipped counting, and the round narrative honestly records round 2's catch instead of "all three fixed". Bars at this tip: `tests/test_drive.py` + both ratchets **15 passed in 19.68s** (`_resume_or_claim` under C901, `agent_loop.py:main` stays 27, drive.py at 400 < 1500 needs no size-ratchet entry). -> no remedy owed -> @owner
VERDICT: APPROVE findings=0
