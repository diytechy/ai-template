# WI-566 — ADJUDICATE amended-cell meaning/clarity — commit 05fb6a3

One line per amended row. Question judged: did the amendment change the requirement's MEANING, or only its CLARITY?

- [CLARITY] SR-024 Rationale -> systematic expansion covers the charter's coverage half, negative tests a separate obligation -> same, minus the "Hat-derived (hat.TEST-ENGINEER):" attribution -> only the provenance label was dropped; the coverage/negative-test split a builder must honour is word-for-word identical.
- [CLARITY] SR-033 Rationale -> generated checklist surfaces warn-tier budgets, no wider than the charter asks -> same, minus the "Hat-derived (hat.PERFORMANCE)," attribution -> label removed; the answer-not-obligation framing and every clause are unchanged.
- [CLARITY] SR-043 Rationale -> row enumerates the fan-out irreversible action per C-SEC-2, fail-open deliberate, human-held override never relaxed -> same, minus the "Hat-derived (hat.SECURITY):" attribution -> label removed; the C-SEC-2 mapping and the fail-open/override reasoning are byte-preserved.
- [CLARITY] SR-052 Rationale -> a11y bar (keyboard, name, colour, contrast) stated, method mechanical via child chain -> same, minus the "Hat-derived (hat.ACCESSIBILITY):" attribution -> label removed; the four clauses and the CLOSED-dependency argument are identical.
- [CLARITY] SR-053 Rationale -> one cross-view coherence property, child chain is acceptance, lens-not-need parentage -> same, minus the "Hat-derived (hat.CONSISTENCY):" attribution -> label removed; the coherence obligation and fan-out census are unchanged.
- [CLARITY] SR-054 Rationale -> task-level usability property, findability+legibility, one unpinnable first-reader clause as recorded residue -> same, minus the "Hat-derived (hat.UX-DESIGNER + hat.UX-ENGINEER):" attribution -> label removed; the perceptual obligation and residue limit are identical.
- [CLARITY] SR-111 Rationale -> version stamp lets a maintainer identify upstream version and compute a re-sync diff; hash-derivation rejected -> same obligation; the trailing hat sentence and the C-MNT-7 citation-home clause were dropped -> what must be built (the recorded-origin stamp) and why is unchanged; only the derivation provenance/citation prose was removed.
- [CLARITY] SR-112 Rationale -> generated per-agent skill copies, drift is a finding, generated-marking stops editing a disposable copy -> same obligation; the trailing hat sentence and C-MNT-7 citation clause dropped -> the forced-fan-out/generated-marking obligation is intact; only provenance prose removed.
- [CLARITY] SR-129 Rationale -> converter as the check on a representation change, cell-exact round-trip over the live registry -> same, minus the "Hat-derived (hat.TEST-ENGINEER):" attribution -> label removed; the converter-as-check obligation and 140-cell lesson are unchanged.
- [CLARITY] SR-144 Rationale -> immutable per-close record, refusals make immutability a property, split is a field -> same, minus the "Hat-derived (hat.UNATTENDED-OPS):" attribution -> label removed; the C-UNA-3/C-UNA-5 mapping and record obligations are identical.
- [MEANING] LLR-144 Detail -> anti-livelock mechanism: "schedule reads queued+blockref as blocked, so the driver cannot claim and return it forever" -> "the terminal partial/ move alone is the anti-livelock property — the spec never returns to queued/" -> the mechanism changed: old design requires a queued+blockref marker read as blocked; new design forbids the return-to-queued path entirely and rests on the terminal move. An implementation of the old text (writing a blockref marker) is not what the new text describes.
- [MEANING] TC-138 Method -> asserts "an already-declared blockref ... survive[s]" -> asserts "a close writes NO blockref because the partial/ folder is itself terminal (the anti-livelock property is the move, not a queued-plus-blockref marker)" -> opposite acceptance condition: old test checks a blockref persists; new test checks no blockref is written. A close that keeps a blockref passes the old test and fails the new one.
- [CLARITY] SR-146 Rationale -> prompt shipped as a reviewable file with a digest per C-SEC-5's declared inclusion rule -> same, minus the "Hat-derived (hat.SECURITY):" attribution -> label removed; the declared-inclusion/digest obligation is unchanged.
- [CLARITY] SR-147 Rationale -> single-carrier claim proven cell-for-cell by the converter before the authority flip -> same, minus the "Hat-derived (hat.TEST-ENGINEER):" attribution -> label removed; the one-row/converter-proof obligation is intact.
- [MEANING] LLR-058 Detail -> excludes "blocked/deferred/..." WIs; "a queued WI carrying a blockref reads as blocked" -> excludes "terminally-closed/deferred/..." WIs; "a lane stopped early leaves the frontier through its terminal partial/ move, its spec no longer being queued/" -> the exclusion criterion changed (blocked -> terminally-closed) and the mechanism by which a stopped lane leaves the ready set changed from a blockref read to a terminal partial/ move. The ready-frontier derivation behaves differently; an implementation of the old text mis-computes the new frontier.
- [CLARITY] SR-149 Rationale -> vocabulary-conversion condition, history/attestations carved out, per C-MNT-3 one-definition-one-home -> same, minus the "Hat-derived (hat.MAINTAINER):" attribution -> label removed; the check/carve-out obligations are unchanged.
- [CLARITY] SR-167 Rationale -> perf verdict home for both breach arms, names no artifact, LLR-014/TC-014 re-point deliberately owed -> same, minus the "Hat-derived (hat.PERFORMANCE):" attribution -> label removed; every clause including the deferred re-point is intact.
- [MEANING] LLR-198 Detail -> "three ... sources are blocked_pending ... spine_pending ... pause_pending"; "into two private names"; "re-exports all four ... former names" -> "two ... sources are spine_pending ... pause_pending"; "into a private name"; "re-exports the three surviving names (spine_pending, pause_pending, pending_block)" -> the blocked_pending source is deleted from the derivation. A builder implementing the old text builds a blocked_pending source and a fourth re-export; the new text forbids both. Distinct code, distinct surface.
- [MEANING] TC-194 Method -> drives "blocked rows, Drafted or drifted SRs, ..."; asserts "a blocked row projects its pointer", "resolves all four former names", and the IF-138 blocked-row-arms clause -> drives only "Drafted or drifted SRs, tracked/malformed pauses"; asserts "resolves the three surviving former names"; blocked-row assertions and the IF-138 arm removed -> the test no longer exercises blocked-row behaviour and drops the fourth-name and IF-138 checks. Acceptance conditions differ; a build passing the new test could omit blocked-row handling the old test required.
- [MEANING] TC-147 Method -> "an approved SR amendment mints ONE adjudication row (id max+1, blockref empty, the before/after listing in ## Context)" -> same, minus "blockref empty" -> the test drops the "blockref empty" assertion. An adjudication row minted with a non-empty blockref fails the old test but passes the new one, so the acceptance condition changed.
- [CLARITY] SR-175 Rationale -> boundary control derived from data-protection with security (C-SEC-5) and legal (C-LEG-3) converging; SN-026 governs which model not what shown; amend-SN-026 alternative not taken -> same substance, restructured from the "Hat-derived (hat.DATA-PROTECTION, with ...)" parenthetical into prose ("derived from the data-protection lens, with the security and legal lenses converging") -> the three-charters/one-crossing derivation, discipline-not-declaration and not-yet-mechanized clauses are all preserved; only the attribution's phrasing moved.
- [CLARITY] SR-176 Rationale -> matched value never reaches durable storage; C-DPR-2 narrowed; transcript redacts credential shapes; PII classes not yet in redaction set -> same, minus the "Hat-derived (hat.DATA-PROTECTION, C-DPR-2 — ...):" attribution -> label removed; every narrowing and build-debt clause is intact.
- [CLARITY] SR-177 Rationale -> throughput made observable not budgeted per C-PRF-1; numeric target deliberately declined; lanes=1 argument; undecomposed with aggregation surface as build gap -> same, minus the "Hat-derived (hat.PERFORMANCE, C-PRF-1 — ...):" attribution -> label removed; the observable-not-budgeted obligation and undecomposed status are unchanged.

VERDICT: MEANING rows=23
