- [MAJOR] project-trajectory/scripts/check_dupes.py:202 -> `_contiguous_runs` treats line-adjacent (including same-line) hits as one overlapping block, but `_merged_signature` then appends only each hit's final token; two separately duplicated 30-token expressions on one line separated by differing code therefore get one census fingerprint, and after changing an interior token of the second expression in both files the actual CLI still exits 0 with the stale census -> carry token-window offsets, split only genuinely step-1-overlapping runs, and add this census-then-interior-edit regression -> @owner
VERDICT: CHANGES-REQUESTED findings=1
