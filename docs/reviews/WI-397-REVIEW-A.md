### REVIEWER — G3 — Round 1 — 2026-08-01
Verdict: APPROVE
Findings:
- none against the rung itself, and I drove it on THIS REPOSITORY rather than only through its fixtures — a rung tested only by the tests that shipped with it is a rung tested by its author's assumptions. I cut a probe branch off the row's own tip (`wi-397-probe` @ `ce251ece`), committed exactly what a lane filing a follow-up would commit (`docs/work/queued/WI-999-a-lane-side-follow-up.md`), and ran `integrate._minted_id_refusal` from the primary checkout with the claimed set the slot itself derives (`_claimed_wi_ids(root, "wi-397")` → `['WI-397']`). Verbatim: `wi-397-probe ADDS work-item spec(s) carrying an id outside its claimed set (WI-397) - a WORK BRANCH NEVER MINTS A WORK-ITEM ID (owner ruling R1, 2026-08-01 …)` followed by `  WI-999 minted at docs/work/queued/WI-999-a-lane-side-follow-up.md`. THE DISCRIMINATION IS THE POINT AND IT HOLDS ON THE REAL TREE: that same branch also ADDS `docs/work/complete/WI-397-a-work-branch-never-mints-an-id.md` — this row's own terminal-outcome move — and the refusal names WI-999 alone. Against the unmutated branch the rung returns `None`. The probe branch is deleted (`git branch -D wi-397-probe`), the lane is clean at `2a6232de`, and `git status --short` is empty. -> no remedy owed -> @owner
- none against the claimed-set derivation, which is the half a wrong answer would be silent about. It is `_claimed_wi_ids` — TRUNK's `active/<branch>/` read, the reader that already existed — not the branch's own tree, which is what makes the question decidable: a branch cannot widen its own licence by writing into its claim directory, because the set is read from the side it does not control. I checked the degenerate direction too, since a rung whose set can be empty is a rung that refuses everything: with `claimed=[]` the same probe refuses with `… outside its claimed set (empty)`, and that is UNREACHABLE through the slot — `_merge_refusal` returns `trunk holds no claimed specs for <branch>` before the rung is called, which I confirmed by reading the ladder rather than assuming the ordering. -> no remedy owed -> @owner
- none against `--no-renames`, the one implementation choice here that could have been cargo, and the row does not assert it — it MEASURES it. `test_the_mint_is_seen_even_when_git_would_report_it_as_a_rename` builds the alignment where the trap is unambiguous (the minted spec is a byte copy of the claimed one; the closed copy is edited) and asserts the trap BEFORE asserting the rung sees through it: under `-M`, `WI-777` appears in exactly one record and that record is an `R`, while the only `A` left is the branch's own legitimate close. I re-ran it and read the failure I caused on the way in — my first draft of that assertion claimed there would be no adds at all, and the real output (`A docs/work/complete/WI-401-widget.md` beside `R100 …active/… …queued/WI-777-copy.md`) is sharper than the claim I had written, so the test now states what git actually does. A flag with a driven reason on file is not the same object as a flag someone copied from the function next door. -> no remedy owed -> @owner
- none against the "trunk-side minting stays free" half, which is the clause an over-eager rung would have broken and which the DONE-WHEN names. It is free BY CONSTRUCTION rather than by an exemption list: the delta is `merge-base(trunk, branch)..branch`, so anything trunk did sits in the BASE and is not in the diff at all. Driven on a topology where trunk really does mint mid-flight (`test_a_trunk_side_mint_after_the_claim_is_not_the_branchs`: trunk files `WI-500` after the claim, the branch closes, the rung admits — and the test asserts the file really is present on trunk first, so it cannot pass vacuously). One ordering property I checked rather than assumed, because the rung sits in front of the pessimistic in-slot refresh: refreshing can only move content OUT of the branch's delta and into the base, never in, so a refresh can never turn an admitted branch into a minter; the reverse (refused → admitted) needs trunk to have added the identical path, which is the two sides holding the same row, i.e. not a collision. `trunk_step` writes no spec files, so the un-re-run rung after an in-slot refresh is not a gap. -> no remedy owed -> @owner
- none against the three deduplications, and I read them against the scope guard ("keep it ONE rung — do not grow a general `docs/work/` diff-policy engine") rather than waving them through as tidy-up. None of them is engine and none was elective: `check_dupes` convicted the `--name-status` walk copied out of `_abandoned_claim` (fingerprint `687ac09cb5b1`, `integrate.py == integrate.py`) and `_name_status` DELETES the copy instead of adding a census line — the answer this repo prefers and rarely takes; the complexity ratchet put `integrate_one` at C901 11 and `_merge_refusal` is the extraction the ratchet's own text asks for over a bigger number, the `_drop_abandoned` precedent from WI-387; `_spec_id` is one filename→id read shared with `_claimed_specs`, which matters because the rung's whole question is "is this id in that set" and two parsers that disagreed would answer it wrongly in one direction. I checked the ladder for a subtler regression while I was there: `_merge_refusal` evaluates its rungs SEQUENTIALLY, so cheapest-first is preserved — a tuple of calls would have evaluated `_verdict_gate`'s git reads even when the mint rung already refused, and the code says so in a comment. The one behaviour change I looked for is in `_claimed_specs`: a glob hit that carries no id is now skipped rather than yielding a non-id like `WI-notes.md`. Unreachable from `claim`, which only ever writes `WI-<n>-<slug>.md`, and strictly better where it is reachable by hand — a non-id in the merge message and in every refusal is worse than a loud `trunk holds no claimed specs`. -> no remedy owed -> @owner
- none against the record, checked figure by figure rather than read. `integrate.py` measured with the census's own metric (`len(text.splitlines())`) = **1890**, and the baseline entry reads **1890** — agreement `True`; `handback.py` 362, unchanged and correctly not restated as a live claim anywhere in this row's prose. The fragment says "six tests" and there are six (`-k "mint or handbacks_return or terminal_outcome_move"` → **6 passed, 98 deselected**). THE ONE PLACE THIS ROW COULD HAVE CHEATED IS ITS OWN DONE-WHEN #4 — "the R1 ruling is recorded in log.md's Decisions" — and it does not: `## Decisions log` is in `trunk_step.RESERVED_HEADINGS`, so a fragment claiming it is REFUSED and fragments append to the log's end rather than splicing into its structural sections. I confirmed the convention rather than taking the deviation's word for it: `git log -S"Spine work waits, batches, and runs alone" -- docs/log.md` gives `72c5b756`, a single-parent TRUNK commit, which is how all four 2026-07-31 rulings arrived. The Deliverable and the fragment both say where the entry goes and why, which is the honest form of "not done here" — and the post-merge trunk commit is inside RULING-6's bookkeeping surfaces (`docs/log.md` is in `BOOKKEEPING_PREFIXES`), so it is not a smuggled product change either. -> no remedy owed -> @owner
- none, and I confirmed the row's FIRST prose finding by reproducing it, because a finding recorded in place of a WI id is exactly the discipline this row exists to enforce and a false one would discredit it. The claim: claiming a row that a plan doc links to reds trunk's `doc-navigability`. At the branch's base — trunk's own claim commit — `check_docs.py --root . --ignore docs/test/report.md --ignore "docs/work/*" --stale` FAILS with `docs/backlog-plan-2026-08-01.md:56 broken link -> work/queued/WI-397-a-work-branch-never-mints-an-id.md (target not found)`, because the claim moved the spec to `active/wi-397/`. That is trunk red, with no branch to blame, and it will recur on each of the nine remaining rows in that table. The branch repairs it to the terminal home (`work/complete/…`) and `check_docs` now reads **OK - 348 doc(s), 1018 intra-repo link(s), 0 broken**. Filing it as prose against WI-393 rather than minting an id is the correct call under R1 and I would have made the same one. -> no remedy owed -> @owner
- none — BARS at `2a6232de`, every one foreground and read, `git status --short` empty before and after (including after the probe branch above). Full `pytest -q -n auto` **1793 passed, 8 skipped in 378.96s**; `tests/test_integrate.py` alone **104 passed in 146.50s** (102 `def test_` at module scope, the rest parametrised); `pytest -q -n auto -m smoke` **573 passed in 28.63s**; `check_trajectory --root . --strict` rc=0 **clean (395 work item(s), 369 done (93%), 17 cancelled, graph acyclic)**; `check_docs` (bar flags) **0 broken**; `check_doc_refs --root . --strict` rc=0 **no dangling path or sym: references · 927 untraced**; `ruff format --check` 146 files already formatted and `ruff check` All checks passed. SCOPE: this covers the BRANCH, not the composed tree — trunk is not yet an ancestor, so the station refresh's own bar is what will speak for the composition, and the two findings §6 of the handoff records mean a red there must be reproduced by hand rather than read off the refusal. NOT COVERED, stated rather than implied: the rung is never exercised through a real `integrate` drain in these tests (the fixtures stop at `integrate_one`, which is where every other gate in this module is driven too); it reads spec FILES, so an id-shaped claim recorded in some other file is out of its reach BY DESIGN and the row says so; and multi-id branches are only covered in the direction that matters here (the claimed set is a set, and the refusal prints it). **THIS IS AN APPROVE:** the constraint is the one the ruling asked for, it is refused at the right point with an actionable message, both halves — branch-refused and trunk-free — are driven rather than argued, the flag that makes it correct has a measured reason on file, the three deduplications are the ratchets' own prescribed answers rather than scope creep, and the record states its one deviation with the mechanical reason it exists. -> no remedy owed -> @owner
VERDICT: APPROVE findings=0
