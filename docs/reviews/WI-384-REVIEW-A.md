### REVIEWER — G3 — Round 1 — 2026-08-01
Verdict: CHANGES-REQUESTED
Findings:
- [MAJOR] project-trajectory/scripts/agent_common.py:656 -> The RULED deciding reason for declaring `draft/` — "invisible to `max(id) + 1` … the next mint would reissue an id a draft already holds" (`concurrency-v2.md` §B3, transcribed to check_trajectory.py:358, schedule.py:176, bootstrap.py:1415, ADOPTING.md:758, PROCESS_OPTIONS.md:1576, README.md:37, tests/test_wi_folder_loaders.py:360, tests/test_module_size_ratchet.py:688 and docs/log.d/WI-384-…:35) is FALSE against the shipped mint. The mint is `plan_artifacts.file_selected_wis` -> `_existing_wi_nums` (plan_artifacts.py:146-166), which reads ids from `wi_convert.spec_paths` (wi_convert.py:446-453) — an UNFILTERED `work_dir.rglob("WI-*.md")` that never consults `SPEC_STATUS_DIRS` and only regex-matches the FILENAME, never parsing frontmatter or status. Driven on two temp trees built through `wc.work_dir_for(csv)`: a spec at `docs/work/draft/WI-042-held.md` gives `_existing_wi_nums=[42]` -> next mint `WI-043`, and the SAME spec at the undeclared `docs/work/thinking/WI-042-held.md` gives `_existing_wi_nums=[42]` -> next mint `WI-043`. Identical. `spec_paths(docs/work)` returns BOTH files. So an undeclared folder already reserves the id, and the one clause called "the strongest argument for `draft/`" is the one clause that is not true. The other two halves ARE real and I confirmed them (`read_spec_rows` filters, so the duplicate-id guard and the dashboard do go blind) — and they are exactly what the branch's own `test_a_drafted_id_is_visible_to_the_registry_and_so_reserved` actually asserts (`read_spec_rows` + the `ctraj.load_wis` duplicate finding), so the code is right, the test is non-vacuous, and only its docstring over-claims. The conclusion (declare `draft/`) stands unchanged; the record does not, and it now ships downstream in ADOPTING.md's migration recipe and PROCESS_OPTIONS.md. -> Correct the clause at all ten sites to name what is actually blind — the duplicate-id guard and the dashboard, both driven — and drop `max(id) + 1` from the list, or say plainly that the mint sees an undeclared folder today and the declaration is what stops that from being an accident. Do not "fix" `spec_paths` to filter: that would make the sentence true by making id reservation WORSE. -> @owner
- [MINOR] docs/dupes-allow:654 -> The recorded reason for the third re-stamped fingerprint is false: "the `plan_briefs == schedule` pair below (6b98b4c1e7c5 -> a986f553a391) … the edit was to `schedule.py`'s module docstring, not to the shared block." Driven by isolating the change on an otherwise-BASE tree: patching only `_RETIRED = "retired"` -> `_NEVER_READY = ("deferred", "draft")` and nothing else reproduces `a986f553a391` exactly (`~38 tokens`, `plan_briefs.py:87 == schedule.py:96`). That constant assignment IS the block's opening statement (schedule.py:102 at HEAD, :96 at base, immediately above `_utf8_console`), so the edit is inside the matched block, not the docstring — and `check_dupes.fingerprint` (check_dupes.py:72-99) drops `tokenize.COMMENT` from the signature entirely, so a comment or docstring rewrap at the top of the file could not have moved this fingerprint at all. The SANCTION itself is legitimate and I verified it as registration, not greening: 164 duplicate blocks at base and 164 at HEAD, 164 census rows at base and 164 at HEAD, the same three pairs and no new one (HEAD code + BASE allowlist reports exactly those three findings and nothing else), and the two agent_common blocks SHRANK — `== check_trajectory` 968 -> 918 tokens, `== schedule` 970 -> 920 — so "the class did not grow, the block shrank by the deleted validator" holds. -> Re-state the third entry's reason as the `_RETIRED` -> `_NEVER_READY` constant that opens the block; keep the fingerprint. -> @owner
- [MINOR] docs/log.d/WI-384-work-item-state-model.md:77 -> "154 inbound links, 109 of them inside `docs/log.md`" — the 154 reproduces exactly (154 occurrences of `archive/specs/<name>.md` repo-wide over `*.md`/`*.py`/`*.csv`/`*.html`; 150 of them under `docs/**.md`), but no counting method yields 109 for log.md. Measured every way I could construct it: 119 occurrences, 101 matching lines, 92 markdown links parsed with `re.findall(r'\]\(([^)\s]+)\)')`, 90 by a literal `](…archive/specs/….md)` grep, 78 restricted to `archive/specs/WI-`, 61 unique targets, 127 bare `archive/specs/` occurrences. At BASE the log.md occurrence count is the same 119, so the branch's own four-link edit is not the cause. The figure is in the surface that becomes the permanent record of why the §B2 half was split off, and a follow-up row will be sized against it. -> Re-stamp to the measured figure (119 occurrences / 92 markdown links, whichever the remainder row will actually rewrite), stating which is counted. -> @owner
- [MINOR] docs/log.d/WI-384-work-item-state-model.md:3 -> "The work-item status vocabulary becomes `draft | queued | active | deferred | cancelled | complete`" (and identically the Deliverable at docs/work/complete/WI-384-…:11-13) lists a DIRECTORY as a status and omits a real one. `complete` is never a Status value: `SPEC_STATUS_DIRS["complete"] == "done"` in all three F5 copies, and `check_trajectory.KNOWN_STATUSES` (check_trajectory.py:195-203) is `draft, queued, active, done, deferred, blocked, cancelled` — seven words, six directories. Driven: a spec whose row reads `Status=complete` is not expressible through the folder at all, and the six-word list is the one an adopter reads first. The branch's own shipped docs get this right — PROCESS_OPTIONS.md:1572 states `Status ∈ {draft,queued,active,done,deferred,blocked,cancelled}` and README.md:36 says "seven-state lifecycle" — so the imprecision is confined to the two record surfaces, one of which compiles into log.md. -> In the fragment and the Deliverable, separate the two vocabularies: six DIRECTORIES `draft/queued/active/deferred/cancelled/complete`, seven STATUSES with `complete/` -> `done` and `blocked` derived from `queued/` + a blockref. -> @owner
- [MINOR] project-trajectory/ADOPTING.md:754 -> The downstream migration recipe names the wrong flag for the trap it is warning about: "the backlog-staleness clock follows a spec with `git log --follow`, and a rewritten history re-dates every row." The shipped reader is `--follow --diff-filter=AM` (check_trajectory.py:2152), and that module's own docstring at 2128-2136 states that `--follow` ALONE "changes nothing about the answer". Driven on this very migration: for `docs/work/complete/WI-374-…`, `--follow --diff-filter=AM` answers `2026-07-31 02:17:36` (its true pre-migration date) while `--follow` alone answers `2026-08-01 00:56:34` — today, the rename commit — and the same split holds for WI-369 (07-30 vs 08-01), WI-001 (07-29 vs 08-01) and WI-280 (07-31 vs 08-01). The recipe's ADVICE (`git mv`, not delete+add) is correct and I verified the migration honours it; only the cited mechanism is the one that does not work. -> Cite `git log --follow --diff-filter=AM`, the pair the reader actually uses. -> @owner
- none — the two standing-rule deviations and the scope reduction, driven and JUDGED SOUND] (1) `docs/log.md` on a work branch: the diff is exactly four link TARGETS, `work/archive/…` -> `work/complete/…` at lines 18659/18693/18936/18960, with link text, prose and every record line byte-unchanged (`git diff 73e60161 HEAD -- docs/log.md` is 4 changed lines and nothing else; three prose mentions of `docs/work/archive/` at 18459/18537/19166 were correctly LEFT). Necessity driven: restoring the base log.md into the merged tree reds `check_docs` with exactly those four `broken link -> work/archive/…` failures and no others; with the edit, `0 broken`. Conflict risk refuted by construction, not argument: `plan_artifacts.append_log_summary` (plan_artifacts.py:304-320) opens log.md in `"a"` mode and writes at EOF, ~1,480 lines below the lowest edit, so `trunk_step --compile-log` cannot collide with it; and `git merge-tree --write-tree ConcurrencyTrainRewrite wi-384` succeeds cleanly (tree `5be0e16e`), on which `check_docs` reads `336 doc(s), 956 link(s), 0 broken`. No legitimate alternative existed: a `docs/log.d/` fragment can only APPEND, so it cannot retarget an existing line, and the WI-288 relinker really is gone. The two `deferred/`-orphan fixes do not fight — trunk `22e66e51` retargeted `concurrency-v2.md`'s links to `work/queued/` and touched nothing else, the branch added `.gitkeep`s, and the composed tree is green. (2) The linecount ratchet: all four bumps are registration. Measured base -> HEAD as total lines / comment lines / significant tokens — `check_trajectory` 3098->3116 (+18), comments +13, CODE TOKENS **-38**; `agent_common` 1720->1728 (+8), comments +11, tokens **-43**; `bootstrap` 2232->2241 (+9), comments +6, tokens +4 (the three `GITKEEP_DIRS` rows); `check.py` 1523->1524 (+1), comments +0, tokens +0 (a docstring rewrap). Two of the four modules got smaller in code mass while their baselines rose; nothing was greened. (3) The §B2 scope reduction is a legitimate "finish the rest, file the remainder" split, NOT the load-bearing half. The row's thesis is §0's principle test — delete the attribute, the cross-check and both raise paths — and that lives entirely in the work-item registry and is fully built. `docs/archive/specs/` carries NO state attribute at all (its files are `<stem>.<date>.md`), so mirroring it deletes nothing, makes nothing unrepresentable and closes no raise path; §B2 itself frames it as "a spec's location answers shipped-or-cancelled *without opening it*", which is navigation. Leaving it whole also introduces no new inconsistency: R-F still routes every terminal spec to the one home it always had. A remainder row IS owed and none is filed (`docs/work/queued/` holds WI-378/381/383/387/388/389/390 and nothing for this). -> no remedy owed -> @owner
- none — what else held, re-driven] MIGRATION RECORD, clean: 378 specs in `docs/work/archive/` at base; exactly 16 carry `^disposition = "retired"`; those exact 16 filenames are the 16 now in `cancelled/`; all 362 others are in `complete/` with IDENTICAL BLOB HASHES (`git ls-tree -r` comparison: the only `complete/` blob without a base-archive twin is WI-384's own new spec), and each of the 16 diffs against its base file by exactly one removed line, the `disposition` line — byte-compared, not eyeballed, zero other differences. No row landed in the wrong terminal folder. STALENESS CLOCK: swept all 363 `complete/` specs through `git log -1 --follow --diff-filter=AM` — 0 unresolvable, and exactly 1 dates 2026-08-01 (WI-384's own, legitimately); the rename+edit 16 also answer their pre-migration dates (WI-382/WI-385 07-31 23:13:09, WI-060 07-29), the `R<similarity>`/`AM` behaviour as claimed. DELETIONS: `parse_spec_status(relpath)` takes no frontmatter in all three F5 copies (agent_common:715, check_trajectory:417, schedule:235); no `disposition` cross-check or raise path survives anywhere; `wi_convert`'s `RETIRED`/`ARCHIVE` constants and disposition arm are gone, `STATUS_DIRS`/`DIR_STATUSES` is a bijection; the four tests are absent by name. The surviving refusal is the undeclared-directory one only. SCAFFOLD PROBE re-run from scratch, not taken on report: `bootstrap.py --dest` created all six status dirs; `schedule.py ready --explain` read `WI-001 ready` / `WI-002 draft … excluded:draft`; `integrate.py claim` moved the spec to `active/wi-001-probe/`; closing into `complete/` read `check_trajectory: clean (2 work item(s), 1 done (50%), graph acyclic)`; a stray `archive/` spec gave `ERROR - archive/WI-003-late-arrival.md: 'archive' is not a status directory (the spec form knows only active, cancelled, complete, deferred, draft, queued)` at **exit 1**. DEVIATION 3 is true and bounded: removing the `if label == "shipped": continue` reds with `in the shipped render, the emitted statusColor map disagrees … {'retired': '#78716c'}` vs `{'cancelled': '#78716c'}` — the same hex, a stale WORD in an older renderer's committed markup, exactly WI-372's truth-times case, with the `if lb != "shipped"` precedent already in the fixture docstring; the shipped copy contributes no map shape a fresh document lacks (4-key `statusColor` either way), and `seen >= 2` still passed for every remaining label, so it is not vacuous. BARS, all re-run here and all matching the fragment: full `1 failed, 1712 passed, 12 skipped in 378.74s (-n auto)` with the sole failure `test_this_repo_is_not_a_work_branch` (the standing work-branch red); `check_docs --stale` exit 0, `336 doc(s), 957 link(s), 0 broken`; `check_trajectory --strict` exit 0, `clean (388 work item(s), 363 done (94%), 16 cancelled, graph acyclic)`; `ruff check` clean, `ruff format --check` `146 files already formatted`. BYTE DELTAS exact: `AGENTS.template.md` 9,991 -> 9,991, `PROCESS_OPTIONS.md` 163,157 -> 163,834 (+677), `PROCESS.md` unchanged. Spine untouched (`git diff` over `docs/requirements`, `docs/test/test-cases.csv`, `docs/architecture.md` is empty); SR=135 LLR=126 TC=123. NOT COVERED: the smoke tier (the full unfiltered suite subsumes it), the rendered dashboard (`PROJECT_STATE.html` is stale by the trunk-lane rule and this branch does not regenerate it), and downstream re-adoption of the ADOPTING.md recipe against a real migrated repo. -> no remedy owed -> @owner
VERDICT: CHANGES-REQUESTED findings=5

### REVIEWER — G3 — Round 2 — 2026-08-01
Verdict: APPROVE
Findings:
- none against the round-1 MAJOR — it is resolved, and resolved the right way. The builder reproduced the drive rather than taking it on report, and I re-drove the replacement. (a) The stale clause is GONE from every site: a grep for the old "invisible to `max(id) + 1`" wording returns 0 at all eleven named files (the three F5 copies, `bootstrap.py`, `ADOPTING.md`, `PROCESS_OPTIONS.md`, `README.md`, `work/WI-000.template.md`, its dogfooded `docs/work/queued/WI-000-example.md`, and both test modules) and every one now names the duplicate-id guard instead. The "all ten transcription sites" count holds — the template and its scaffolded copy are ONE site in two files, byte-identical at blob `9269b786` and pinned by the dogfood-sync guard; I checked that rather than assume it. Two of those sites (the shipped `WI-000` exemplar and its dogfooded copy) I did not list in round 1 and the builder found unprompted — that is the exemplar an adopter reads first, so it mattered. (b) The replacement clause is TRUE, and it is not vaguer than what it replaced; it is more specific, because it names consumers a test can bind. Re-driven end to end on a FRESH scaffold: with the spec at the declared `docs/work/draft/WI-042-held.md` the dashboard renders it (2 occurrences of `WI-042` in the emitted `PROJECT_STATE.html`); with the identical spec at the undeclared `docs/work/thinking/`, `gen_trajectory` emits NO document at all — `no work items (placeholder-only or absent registry) — nothing to render; vacuously clean`, exit 0 — and `schedule ready` prints `no ready work items`. So "the duplicate-id guard and the dashboard go blind" is not a paraphrase of the old sentence, it is what the code does, and it is stronger than the wording claims: the whole registry reads empty. `check_trajectory --strict` is the one consumer that does NOT go blind there (`ERROR - thinking/WI-042-held.md: 'thinking' is not a status directory`, exit 1). (c) The mint half re-confirmed: `wi_convert.spec_paths` is still the unfiltered walk, and it was deliberately NOT filtered — recorded as a non-action in the Deliverable with the reason (filtering would build the hazard the clause imagined), which is the correct call and the one I asked for. (d) Leaving `concurrency-v2.md` to the owner with the correction carried in the fragment is right: it is the ruled design doc, the fragment compiles into `log.md`, and WI-390's sweep inherits from there. -> no remedy owed -> @owner
- none against round-1 MINOR 1, 2, 3 and 4 — all four re-driven at `eb1b25f5` and all four hold. (1) `docs/dupes-allow:655-660` now records the isolated cause and retracts the module-docstring account in place; the reason it now gives — the ~38-token block opening at the module constant, with the `_RETIRED` -> `_NEVER_READY` rename alone reproducing `a986f553a391` — is exactly what my round-1 patch-one-line-on-a-base-tree experiment showed. (2) EVERY corrected figure reproduces EXACTLY, re-measured with a fresh parse at this tip: 154 occurrences repo-wide of an archived-spec reference across **30 distinct files**, of which **101 are markdown link targets**; `docs/log.md` alone **119 occurrences on 101 lines, 92 link targets, 61 unique targets**; **111 files** in the archived-specs directory. The claim now also uses the RIGHT quantity for what it asserts — "101 are markdown link TARGETS, the quantity a relocation actually has to rewrite" — where the superseded figure conflated occurrence counts with rewrite work. (3) The two vocabularies are separated in both record surfaces and stated as I asked: six directories, seven statuses, `complete/` -> `done`, `blocked` derived from `queued/` plus a blockref, and the explicit "`complete` is never a Status value" — which matches `check_trajectory.KNOWN_STATUSES` exactly. (4) The flag evidence is correct INCLUDING the part worth doubting: I drove all four forms on two migrated specs, and both single flags really do give the same wrong answer — `WI-374` reads `2026-07-31 02:17:36` under `--follow --diff-filter=AM`, and `2026-08-01 00:56:34` (the rename commit) under `--follow` alone, under `--diff-filter=AM` alone, AND under no flags at all; `WI-369` reads `2026-07-30 10:07:12` versus `2026-08-01 00:56:34` three ways. So the recipe's "it needs BOTH" is exact. -> no remedy owed -> @owner
- none against the ratchet re-stamp — re-measured by the round-1 method, which is what made round 1's judgement mean anything. Base `184760c2` -> tip, as total lines / comment lines / significant tokens: `check_trajectory` 3116->3119, comments +3, **code tokens +0**; `bootstrap` 2241->2243, comments +2, **+0**; `agent_common` 1728->1731, comments +3, **+0**; `schedule` +3 lines, comments +3, **+0** (correctly NOT re-stamped — it sits under the census `THRESHOLD = 1500`); `check.py` unchanged in all three. "Pure comment lines, zero code tokens" is exact. The F5 comment really was tightened first — the correction cost 3 lines in each of the three copies rather than the larger block a looser wording would have charged 3x. Independently: an AST comparison with every docstring stripped is IDENTICAL base -> tip for all four scripts and for `tests/test_wi_folder_loaders.py`, so this round changed no behaviour anywhere; the sole non-identical module is `tests/test_module_size_ratchet.py`, which is the three baseline numbers themselves, and it passes. -> no remedy owed -> @owner
- [MINOR] docs/work/queued/WI-391-mirror-specs-of-record-onto-the-termi.md:3 -> WI-391 names a symbol that does not exist. Its "ALSO IN SCOPE" clause tells the future builder to touch "`check_trajectory`'s `ARCHIVE_SPECS_DIR` + `_spec_of_record`'s archived glob". `ARCHIVE_SPECS_DIR` is real (check_trajectory.py:156, used at :1789 and :1793), but `_spec_of_record` does not exist — a recursive grep over `project-trajectory/` and `tests/` returns nothing. The function that actually holds the archived glob is `_own_spec` (check_trajectory.py:1770, `matches = sorted(archive.glob(...))` at :1791, two call sites). This row is the ONLY record that the remainder is owed, and this is one of the two code sites it points at, so the pointer should resolve. Everything else in the row verifies: it parses identically through all three F5 readers (`Status=queued`, `SpecRef=docs/concurrency-v2.md` resolving, empty Deliverable per R-A); its id really did come from the mint (driven at the round-1 tip — `_existing_wi_nums` sees 389 ids, max 390, next mint `WI-391`); it enters the ready frontier without disturbing it (before: `WI-386, WI-383, WI-380`; after: the same three in the same order with `WI-391` appended at `down=0 path=0`); the WI-288 relinker it names as probable predecessor is real and died where claimed (`_relink_archived_specs` added at `8d95d538 WI-288`, deleted at `31ad569d` Phase 5); `docs/specs/README.md` really does name the flat home; and its scope matches what was left unbuilt — §B2's "Specs mirror it" sentence and nothing else, correctly framed as navigation-not-a-constraint and declared all-or-nothing. -> Replace `_spec_of_record` with `_own_spec` (check_trajectory.py:1770). -> @owner
- [MINOR] project-trajectory/ADOPTING.md:759 -> The corrected clause is right for the case the F5 comments scope it to, and over-general in the one adopter-facing copy. The scripts say "an undeclared directory's specs are skipped BELOW", i.e. an undeclared directory under `docs/work/` — true, and I drove it. `ADOPTING.md` generalises to "a draft parked in an improvised folder … The id MINT is safe either way (it reads filenames through an unfiltered walk)", and an adopter will read a sibling directory as an improvised folder. Driven on the same scaffold with the spec moved to `docs/drafts/WI-042-held.md`: `wi_convert.spec_paths` returns only the `WI-000` example, `_existing_wi_nums` returns `[0]`, so the next mint is `WI-001` and WOULD reissue a held id — and `check_trajectory --strict` reads `clean (no work items — placeholder-only or absent registry; vacuously clean)` with no error whatsoever. So outside `docs/work/` the mint is NOT safe, nothing reds, and §B3's original sentence was right for exactly that reading ("drafts live nowhere, or in an unscanned folder"). This does not weaken the ruling — it is the strongest form of it, and it is one clause. -> In `ADOPTING.md` scope the parenthetical the way the scripts do ("an undeclared directory under `docs/work/`"), or add the stronger case: a folder OUTSIDE `docs/work/` is invisible to the mint too, and nothing reds. -> @owner
- none — bars re-run at `eb1b25f5`, all green and all matching. Full suite `1 failed, 1712 passed, 12 skipped in 745.01s (-n auto)`, sole failure the standing `tests/test_check_lane.py::test_this_repo_is_not_a_work_branch`. `check_docs --stale` exit 0, `337 doc(s), 959 intra-repo link(s), 0 broken`. `check_trajectory --strict` exit 0, `clean (389 work item(s), 363 done (93%), 16 cancelled, graph acyclic)` — 389 and 93% are WI-391 entering the graph, arithmetic consistent. `ruff check` clean; `ruff format --check` `146 files already formatted`. Composed-tree check against the NEW trunk, since trunk moved: `git merge-tree --write-tree 4ae2e838 eb1b25f5` succeeds cleanly (tree `2514125d`), and on that tree `check_docs` reads `337 doc(s), 958 link(s), 0 broken` and `check_trajectory --strict` reads `clean (389 work item(s), 363 done (93%), 16 cancelled, graph acyclic)` exit 0 — so the round-1 `log.md` retarget and the two trunk-side fixes still do not fight. NOT RE-EXAMINED this round, deliberately: the 378-spec migration record, the staleness-clock sweep over all 363, the deletion set, the scaffold claim/close probe and deviation 3 — all verified at round 1, and the AST-with-docstrings-stripped comparison above proves this round touched no code that could have disturbed them. Also not examined: the trunk-side SpecRef-staleness warnings the coordinator names (they appear neither on this branch nor on the composed tree, so I have no reproduction to judge), and `concurrency-v2.md` §B3 itself, deliberately left to the owner. -> no remedy owed -> @owner
VERDICT: APPROVE findings=2

### REVIEWER — G3 — Round 3 — 2026-08-01
Verdict: APPROVE
Findings:
- none — the merge is provably not an evil merge, which is the strongest thing that can be said about it and is what I went after first. I reconstructed what git ALONE produces from the merge base (`73e60161`) — `git merge-tree --write-tree 277cb7b9 8c4d5f78` — and it reports exactly TWO conflicts and no others: `CONFLICT (file location)` on WI-380's spec, and `CONFLICT (content)` in `tests/test_module_size_ratchet.py`. `docs/log.md` is listed as `Auto-merging` with no conflict, which is my round-1 structural argument confirmed in the event rather than in theory. I then diffed that mechanical tree (`996451e8`) against the ACTUAL merge tree (`4381a461`): **exactly one path differs, `tests/test_module_size_ratchet.py`.** Every other path in the composed tree — all 426 files that moved — is byte-identical to what git produced without a human. The file-location conflict was resolved to git's own suggestion with no hand edit at all (the mechanical tree's stage-3 blob and the merged tree's blob are the same object, `29045ad5`). And the one hand-resolved file is bounded: the set of lines in the merged `test_module_size_ratchet.py` present in NEITHER parent is exactly **10 lines** — the 9-line resolution comment and `"check_trajectory.py": 3251,`. Nothing was smuggled through the merge, because there is nowhere it could have been. No conflict markers survive anywhere (`git grep -E "^(<{7}|>{7})"` is empty repo-wide; both formerly-conflicted files return 0). -> no remedy owed -> @owner
- none against the file-location resolution — WI-380's spec is intact, correctly placed, and its record survived. Byte identity checked two independent ways: the git blob is `29045ad5` at both `8c4d5f78:docs/work/archive/…` and `HEAD:docs/work/complete/…`, and a sha256 over each stream gives `3cbe6dcbc0622b20` on both sides. It lands in `complete/`, NOT `cancelled/` — and I checked that as a REGISTRY fact, not a path fact: all three F5 readers return `Status=done` for WI-380, with `SpecRef` correctly cleared (R-F) and a 6,634-character Deliverable, so the record moved with the file rather than the file moving without it. `docs/work/archive/` is gone rather than emptied — absent from the tree (`git ls-tree -r HEAD`), absent from the index (`git ls-files`), absent from disk; `docs/work/` holds exactly the six declared directories. Their confirming evidence (the undeclared-directory error staying silent) is sound but weaker than the direct check, so I made the direct check. Both `.gitkeep`s (`deferred/`, `draft/`) survive the merge. One thing I looked for that the merge could plausibly have broken and did not: WI-380's own log entry arriving from trunk could have linked its spec at `work/archive/…` and gone dangling the moment this branch deleted that directory — it does not, and `](work/archive/` now occurs **0** times in `docs/log.md`, so no fifth log edit was needed and none was made. -> no remedy owed -> @owner
- none against the ratchet resolution — I re-measured it independently rather than checking their arithmetic against itself. Using the census's own metric, `len(text.splitlines())`, read straight from git at each revision: merge-base `73e60161` = **3098**; trunk `8c4d5f78` (WI-380) = **3230**, so +132; branch `277cb7b9` (WI-384) = **3119**, so +21; the MERGED file measures **3251**. `3098 + 132 + 21 = 3251` — equal, so the two WIs' edits are disjoint and the stamped number is the file's true size rather than a side picked or a difference split. Both reason chains survive VERBATIM, checked as exact substring containment rather than by eye: WI-380's block (`+93 (3098 -> 3191)` and `+39 (3191 -> 3230)`, 1,621 characters lifted from trunk's copy) and WI-384's (`+18 (3098 -> 3116)` and `+3 (3116 -> 3119)`, 1,118 characters lifted from the branch's copy) both appear unmodified in the merged entry, with the new `+21 net (3230 -> 3251)` note sitting after them and explicitly recording that neither record was dropped to make the number fit. WI-380's two OTHER re-stamped ratchet files came through untouched — `tests/test_complexity_ratchet.py` and `tests/test_dashboard_size_budget.py` are byte-identical to trunk at HEAD. -> no remedy owed -> @owner
- none against the generated-artifact claim — verified as an empty diff against trunk, and I widened the set beyond the four they named because "a work branch smuggling a generated artifact through a merge" is the failure mode. `git diff 8c4d5f78 HEAD` is EMPTY for all six of `PROJECT_STATE.html`, `docs/gate`, `docs/status.md`, `docs/architecture.md`, `docs/test/report.md` and `docs/open-items.html`. `docs/log.md` differs from trunk by `4  4` in `--numstat` and the four changed lines are precisely the four link targets accepted at round 1 (`work/archive/` -> `work/complete/` for WI-368/369/370/371) — not four lines that happen to number four, the same four. -> no remedy owed -> @owner
- none against the two round-2 MINORs — both fixed, and (b) fixed with the stronger form. (a) WI-391 now carries resolving pointers with file:line: `ARCHIVE_SPECS_DIR` at `check_trajectory.py:156` used at `:1789`/`:1793`, and `_own_spec`'s archived glob at `:1770` with the glob at `:1791` — all four line numbers check out against the file, and `_spec_of_record` is gone from the row. (b) I re-drove the inversion on a FRESH scaffold bootstrapped from the merged tree, one spec moved through three placements, and it is exactly as they state — and the inversion really is the whole point: `docs/work/draft/` (declared) gives `_existing_wi_nums=[0, 42]` -> next mint `WI-043`, validator **exit 0** reading `clean (1 work item(s), 0 done (0%))` — the row is IN the registry; `docs/work/thinking/` (undeclared, inside) gives the same `[0, 42]` -> `WI-043`, but validator **exit 1** with a named error — the id is held by ACCIDENT and the harness is LOUD; `docs/drafts/` (outside) gives `[0]` -> next mint **`WI-001`**, which really would reissue the held id, and the validator reads `clean (no work items — placeholder-only or absent registry; vacuously clean)` at **exit 0** — the collision is REAL and the harness is SILENT. ADOPTING.md now states both cases in that order with that conclusion, so the adopter-facing copy is no longer the over-general one. -> no remedy owed -> @owner
- none — the composed tree holds together, and the deltas are attributed rather than merely totalled. Full suite on the branch as it now stands: **`1 failed, 1718 passed, 12 skipped in 467.37s`** (`-n auto`), sole failure the standing `tests/test_check_lane.py::test_this_repo_is_not_a_work_branch`. `1718 + 12 + 1 = 1731`, which is the collected count I measured independently (`--collect-only -q` -> `1731 tests collected`), and round 2 ran `1712 + 12 + 1 = 1725`, so the delta is **+6** — attributed, not assumed: `tests/test_trajectory_staged.py` (pre-existing at the merge base, 766 lines / 27 test functions) grew to 1,013 lines / **33** test functions, `+6`, and the other three test files WI-380 touched (`test_complexity_ratchet`, `test_dashboard_size_budget`, `test_module_size_ratchet`) each added **zero** test functions. So all six new tests are WI-380's staged-amendment coverage and none came from the merge itself. `check_trajectory --strict` exit 0: `clean (389 work item(s), 364 done (94%), 16 cancelled, graph acyclic)` — and the counts reconcile from the filesystem: 364 `complete/` + 16 `cancelled/` + 9 `queued/` + 1 `active/` + 0 `draft/` + 0 `deferred/` = 390 spec files, minus the inert `WI-000` example = **389**. The item count is unchanged from round 2 (WI-391 took it 388 -> 389 there) and `done` moved 363 -> 364, which is WI-380 closing — exactly the attribution claimed. `check_docs --stale` exit 0, `338 doc(s), 964 intra-repo link(s), 0 broken`. `ruff check` clean; `ruff format --check` `146 files already formatted`. I now also have a reproduction of the SpecRef-staleness warning the coordinator flagged, which I could not reproduce at round 2: `WARN - WI-390: its SpecRef docs/concurrency-v2.md changed after the WI row was last touched` — warn-only, `--strict` still exits 0, correctly NOT silenced by mass-touching the rows, and it belongs to the trunk lane rather than to this row. -> no remedy owed -> @owner
- none — scope of this round, stated plainly. RE-EXAMINED: the whole merge (mechanical-vs-actual tree comparison, both conflict resolutions, conflict-marker sweep), WI-380's spec identity/placement/record, the ratchet arithmetic and both reason chains, the generated-artifact set and `docs/log.md` against trunk, both round-2 MINOR fixes with the three-placement drive re-run on a scaffold built from the merged tree, and every bar on the branch as it now stands. NOT re-examined, deliberately, and safe not to be: the 378-spec migration record, the staleness-clock sweep over the migrated specs, the `disposition` deletion set, the claim/close scaffold probe and deviation 3 — all verified at round 1, all on paths the mechanical-vs-actual comparison proves the merge reproduced byte-for-byte from a parent. NOT examined at all: WI-380's own substance, which is `docs/reviews/WI-380-REVIEW-A.md`'s business and arrived through trunk already reviewed; and `concurrency-v2.md` §B3, still deliberately left to the owner with the correction carried in this row's log fragment for WI-390's sweep to inherit. This branch is clear to advance trunk. -> no remedy owed -> @owner
VERDICT: APPROVE findings=0
