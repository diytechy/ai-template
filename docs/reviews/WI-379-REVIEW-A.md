### REVIEWER — G3 — Round 1 — 2026-07-31

Verdict: APPROVE

Findings:

- [MINOR] docs/work/archive/WI-379-bootstrap-mapping-omits-schedule.md:14 (repeated at docs/log.d/WI-379-schedule-mapping.md:31-35) -> The audit sentence is arithmetically false in **both** readings: "43 mapped, 46 on disk; the remainder are bootstrap itself and the deliberately kit-only gen_skills_index" — 43 + 2 = 45, not 46. Driven by parsing the MAPPING list literal with `ast` (exact, not regex) at both commits: **pre-fix (f5207fb) 43 `.py` rows** with **three** unmapped modules on disk (`bootstrap`, `gen_skills_index`, `schedule`); **post-fix (2d77293) 44 `.py` rows** with exactly the two named remainders — and the shipped scaffold I bootstrapped carries `ls scripts/*.py | wc -l` = **44**, matching. The cell pairs a PRE-fix count with a POST-fix remainder list. The substantive claim it supports is nonetheless TRUE and I confirmed it independently: the same audit reports `missing == {}` at the tip and exactly `{'schedule': ['agent_loop.py','drive.py','integrate.py','traj_parse.py']}` pre-fix, so schedule really was the only omission and this really is a complete fix. Only the number is wrong. -> Re-stamp to "44 mapped, 46 on disk" (post-fix state, matching the two named remainders), in both the archived Deliverable and the log fragment. -> @owner

- [MINOR] tests/test_bootstrap.py:894 -> The new guard's MAPPING extractor is a regex over the **whole** of `bootstrap.py`, not over the MAPPING literal: `re.findall(r'"(?:scripts/)?([A-Za-z_]\w*)\.py"', mapping_src)`. Any double-quoted `"<name>.py"` literal anywhere in that 2,232-line file silently enters `mapped` — and `mapped` is what decides both "is this import shipped?" and "do we even walk this script?", so an over-match is a **false negative on the exact module being discussed**. Not hypothetical that it scans beyond the list: driven, the MAPPING literal spans lines 1124-1401 and the regex takes **3 hits outside it** (bootstrap.py:1555 `"scripts/gen_arch_map.py"`, :1576 `["scripts/trace.py"]`, :1582 `["scripts/gen_open_items.py"]`). Today all three name already-mapped modules so the guard is intact (I verified `set(regex) == set(ast-parsed MAPPING src stems)` exactly, 44 == 44, no extras and no misses), but the failure mode is precisely the one this WI exists to prevent: the next time someone writes a refusal message or docstring mentioning a sibling `.py` by name, the guard stops guarding that sibling and nothing reds. The remedy costs four lines and needs no new import — the test file already does `import ast`, and walking the `MAPPING` `ast.Assign` for `elts[0].value` is what I used to audit it. -> Extract MAPPING via `ast` (locate the `MAPPING` assignment, read each tuple's first element) instead of regexing the file text. -> @owner

- [NIT] tests/test_bootstrap.py:874-918 -> The class guard covers `import`/`from ... import` only, so a mapped script that reaches a sibling by **path** rather than by import is outside it — the same "works in the kit, ImportError downstream" shape one step over. Driven: exactly one such reference exists today, `project-trajectory/scripts/check.py:502` `skills_gen = _SCRIPTS / "gen_skills_index.py"`, and it is genuinely safe — guarded by `.exists()` with a documented vacuous `python -c pass` fallback (check.py:495-506) and pinned by `tests/test_pre_commit_hook.py::test_hook_skills_sync_step`. Nothing to fix now; noting the boundary so the next reader does not read the guard as broader than it is. The docstring and the records do not overclaim (they say "imports"), so this is scope, not inaccuracy. -> Optionally extend to `SCRIPTS / "<name>.py"`-shaped literals, or state the import-only boundary in the docstring. -> @owner

Driven end to end, both directions — the before/after scaffold probes REPRODUCED:

- **AFTER (this branch, tip 2d77293).** `bootstrap.py --dest <tmp>/after --stack python --agents claude` → `scripts/schedule.py` present; `git init`, one queued spec (`WI-001`, `specref = docs/specs/WI-001.md`, `safety_class = ordinary`), `schedule.py ready --explain` → `WI-001 ready ordinary P1 down=0 path=0 ordinary:optimistic-packing-eligible;ready`; then `integrate.py --root . claim --wi WI-001 --branch wi-001-probe` → **`integrate: claimed WI-001 on wi-001-probe (trunk commit + branch cut)`, exit 0**, bookkeeping commit `claim: WI-001 -> active/wi-001-probe`, branch cut, spec in `docs/work/active/wi-001-probe/`. `drive.py --root . --help` imports and exits 0.
- **BEFORE (pre-fix bootstrap.py from git history, `git show f5207fb:...`, 2,224 lines, `"scripts/schedule.py" in MAPPING == False`, dropped into an otherwise-identical copy of this branch's `project-trajectory/`).** Scaffold built into `<tmp>/before` → **no `scripts/schedule.py`**. The identical probe: `integrate.py claim` → **`File ".../scripts/integrate.py", line 257, in _claim_refusal / import schedule / ModuleNotFoundError: No module named 'schedule'`, exit 1**. And `drive.py --root .` → **`File ".../scripts/drive.py", line 47, in <module> / import schedule / ModuleNotFoundError`, exit 1**. Both headline capabilities dead on arrival, exactly as filed — the diagnosis is not misattributed and the fix is the fix.
- **The guard bites, mutation-proven independently.** I re-derived the test's logic in a standalone script and ran it against the pre-fix `bootstrap.py` taken from git (not a hand-edit): it reports exactly `{'schedule': ['agent_loop.py', 'drive.py', 'integrate.py', 'traj_parse.py']}` — the four importers, no more, no fewer — and `{}` at the tip. **Function-level imports are caught**: `integrate.py`'s own `import schedule` lives at line 257 *inside* `_claim_refusal` (deliberately deferred), and `ast.walk` reaches it — integrate.py appears in that dict, so the deepest-buried of the four is the one the guard proves it can see. `agent_loop.py:150/158` and `traj_parse.py:35/39` are likewise `try:`-guarded function/module-level forms and are also caught. Skips are correct: `bootstrap` is skipped explicitly (and is not in `mapped` anyway — belt and braces), and kit-only tools are skipped by `path.stem not in mapped`, which is why `gen_skills_index.py` is not walked. No false positives are reachable today (regex ⊇ MAPPING exactly; an *under*-match would be the false-positive direction and there is none).
- **`gen_skills_index` is genuinely kit-only, and should stay unshipped.** It regenerates `skills/INDEX.csv` and runs `--check-agents` byte-identity from the **neutral `skills/` source**, which only the kit repo hosts — the scaffold receives materialized per-agent copies (`.claude/skills/…/SKILL.md`, confirmed in my scaffold) and **no** `skills/` source and **no** `INDEX.csv`, so downstream there is nothing to generate and nothing to drift from. This is designed, not accidental: `check.py:495-506` branches on `skills_gen.exists()` to a vacuous no-op so `--run-step skills-sync` still resolves downstream, and `tests/test_pre_commit_hook.py::test_hook_skills_sync_step` pins that. Shipping it would hand adopters a script that can only fail or no-op. Correct call.
- **Nothing rode along.** `git diff f5207fb --stat` = 7 files, +137/-10, every one in scope: the MAPPING row (+8 with its why-comment), the `tests/test_bootstrap.py` scaffold-list entry + the new guard, the README kit-contents row, the ratchet re-stamp, the archived spec (moved out of `active/`, Deliverable filled), the log fragment. No production behavior touched beyond the MAPPING list. The **ratchet re-stamp carries its written reason** at the entry (tests/test_module_size_ratchet.py:669-673) and the number is exact: pre-fix `bootstrap.py` measures **2224** and the tip **2232** by the ratchet's own `len(text.splitlines())`, matching `2224 -> 2232 (+8)`. **Control bytes: clean** — the 2d77293 repair removed a literal `\x08` that had eaten the backtick in "`bootstrap.py`" (verified in the commit via `cat -A`); scanning both record files now yields zero control characters, zero C1 bytes, no CRLF, and the only non-ASCII is U+2014 (em dash) ×5. The README row's facts check out (IF-053 is schedule's Provides row to check_trajectory; the dashboard reads it via IF-071/IF-085), and "WI-280 alone added six sibling modules" is exactly the six named in the README (traj_graph/parse/render/views/panels/status). `docs/work/active/wi-379-schedule-mapping/` is empty — the branch reads as FINISHED to the integrator.
- **Bars, all driven at 2d77293 with the repo `.venv` (3.11.9).** Full unfiltered suite `pytest -q -n auto`: **1 failed, 1708 passed, 8 skipped in 251.69s** — the sole failure is `tests/test_check_lane.py::test_this_repo_is_not_a_work_branch` (`assert 'wi-379-schedule-mapping' is None`), the standing WI-357 work-branch conditional that reds by design on any claimed-branch checkout; not chased. `ruff check .` → **All checks passed!**; `ruff format --check .` → **146 files already formatted**. `check_docs.py --root . --ignore docs/test/report.md --ignore "docs/work/*" --stale` → **OK, 333 doc(s), 934 intra-repo link(s), 0 broken** (exit 0; four pre-existing staleness *hints* only). `check_trajectory.py --root . --strict` → **clean (377 work items, 362 done (96%), 14 retired, graph acyclic)**, exit 0, WARNs all pre-existing (IF-055/080/081, the traj_* connectivity rows). Targeted: the new guard + the scaffold file-list test + the whole size-ratchet module, **3 passed**. The generated STATUS block in `docs/status.md` still lists WI-379 on the ready frontier — correct on a branch (trunk-owned, regenerated by the integrator's trunk step), and `--strict` confirms no R-D/forward-only violation.

VERDICT: APPROVE findings=3

### REVIEWER — G3 — Round 2 — 2026-07-31

Verdict: APPROVE

Findings:

- none against the two round-1 MINORs — both close at tip 4cc12c1, delta-scoped and re-driven, and I agree with scoping the NIT out. **(1) The extractor is correct and cannot silently fail open.** It now takes the module-level `MAPPING` assignment out of `bootstrap_tree.body` and reads the string constants inside that literal — and I verified the result is EXACT, not merely narrower: at the tip the extractor's set is **identical** to the ast-parsed set of MAPPING `.py` source stems (44 == 44, symmetric difference empty in both directions), and at pre-fix `f5207fb` **43 == 43**, likewise empty both ways. So no over-count and no under-count. *Taking both tuple halves is provably harmless here, not lucky*: I enumerated every `.py` string constant reachable inside the literal and got **88 at the tip / 86 pre-fix**, an exact multiset match with `44×2 / 43×2` row halves — i.e. there is no `.py` constant inside the literal that is not a row half — and `src == dest` for **every** `.py` row at both commits (zero renames), so src-only, dest-only and the union are the same 44 names. The literal's shape backs this: all 105 elements (104 pre-fix) are plain 2-tuples of string constants, zero malformed, zero multi-dot `.py` names that `rsplit("/")[-1][:-3]` could mangle. **(2) The `assert mapping_node is not None` genuinely fires** — driven two ways by mutating the tip source in memory and re-running the shipped expression: rename the binding (`MAPPING = [` → `MAPPING_RENAMED = [`) → the generator yields nothing and `next(..., None)` returns **None**, so the assert trips with "bootstrap.py no longer defines MAPPING"; move the binding into a function so it is no longer a module-level statement → also **None**, assert trips. Scanning `tree.body` rather than `ast.walk(tree)` is the safer of the two choices: a MAPPING shadowed or relocated into a function fails LOUD instead of quietly reading the wrong list. **(3) The guard still bites** — I ran the SHIPPED test body verbatim (new extractor included, not my round-1 re-derivation) against the pre-fix `bootstrap.py` from git: exactly `{'schedule': ['agent_loop.py', 'drive.py', 'integrate.py', 'traj_parse.py']}`, and `{}` against the tip; `tests/test_bootstrap.py` as a module is **49 passed in 71.00s**. **(4) The arithmetic is right and now internally consistent**, verified by my own independent count rather than by reading: **46** kit scripts on disk (`SCRIPTS.glob("*.py")`), **43** mapped `.py` rows at `f5207fb`, **44** at the tip, the two unmapped at the tip being exactly `bootstrap` and `gen_skills_index` — `44 + 2 = 46` checks. Both records now state it that way (fragment "43 mapped before this fix, 44 after, against 46 … 44 + 2 = 46"; archived Deliverable the same), and the fragment additionally records the gen_skills_index rationale accurately — I re-confirmed the scaffold receives materialized `.claude/skills/…/SKILL.md` copies but no neutral `skills/` source and no `INDEX.csv`, and `check.py:495-506` branches to the vacuous no-op, pinned by `tests/test_pre_commit_hook.py::test_hook_skills_sync_step`. **(5) On the un-actioned NIT: I agree it is correctly out of scope.** The guard's contract is import-time failure, the sole path-reference instance (`check.py:502`) is `.exists()`-guarded with a tested fallback, and widening the guard to string literals would re-introduce the very prose-matching false-negative surface round 1 asked to remove. -> no remedy owed -> @owner

- [NIT] docs/work/archive/WI-379-bootstrap-mapping-omits-schedule.md:14 -> The corrected sentence closes its parenthetical em-dash clause with a dash immediately followed by a comma — "…the deliberately kit-only gen_skills_index (44 + 2 = 46) **-,** so this is a complete fix…" — which is a punctuation error in what is now the permanent archived record. Content is right; only the mark is wrong. (I also checked the fragment's new class-fix clause, which lands as one 223-char unwrapped line at docs/log.d/WI-379-schedule-mapping.md:44 in a file otherwise wrapped near 72 — but the round-1 ratchet paragraph at :57 is already 222 chars, so the file has no single convention to violate and I am not asking for a rewrap.) -> Drop the comma (or the closing dash) in the archived Deliverable. -> @owner

Bars re-driven at 4cc12c1 with the repo `.venv` (3.11.9): full unfiltered `pytest -q -n auto` → **1 failed, 1708 passed, 8 skipped in 295.68s**, the sole failure `tests/test_check_lane.py::test_this_repo_is_not_a_work_branch` (`assert 'wi-379-schedule-mapping' is None`) — the standing WI-357 work-branch conditional, red by design on a claimed branch, not chased; identical totals to round 1, so the delta added no test and broke none. `ruff check .` → **All checks passed!**; `ruff format --check .` → **146 files already formatted** (the `re` import in the test module is still used elsewhere, so removing the regex left no F401). `check_docs.py --root . --ignore docs/test/report.md --ignore "docs/work/*" --stale` → **OK, 334 doc(s), 934 intra-repo link(s), 0 broken**, exit 0 (334 vs round 1's 333 = this review file, now committed). `check_trajectory.py --root . --strict` → **clean (377 work items, 362 done (96%), 14 retired, graph acyclic)**, exit 0, WARNs all pre-existing. **No control bytes crept back**: scanning `docs/log.d/WI-379-schedule-mapping.md`, `docs/work/archive/WI-379-bootstrap-mapping-omits-schedule.md`, `tests/test_bootstrap.py` and this review file — zero C0 control characters (beyond `\n`/`\t`), zero C1 bytes, zero CR, and the only non-ASCII is ordinary typography (U+2014/U+2013/U+00A7/U+2192/U+2026/U+00D7). Delta since 2d77293 is 4 files (+52/-7) and nothing rode along: the extractor, the two record corrections, and the round-1 review file.

VERDICT: APPROVE findings=1
