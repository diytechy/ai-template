# WI-574 — REVIEW-A rollup

Compiled by the supervising session (2026-09-02) from the round files under
`docs/reviews/wi-574-spot-check-the-clean-close-of/`, time-ordered, governing
line last. This is the sample spot-check (`complete_review = "sample"`) of
the WI-572 clean close. No mechanized round exists for an adjudication lane
(the queued WI-559 defect); the round was drawn by the supervisor through an
independent Opus reviewer with a hostile brief. The spot-check's own record
(two ADJUDICATE sessions at the medium tier: the close "stands with findings,
two successors drafted", then a correction of one overstated Bar claim) is
the adjudicator's verdict, not a round.

### Supervisor note — NOT a round, does not govern

The lane touched record files only; the dispatcher closed it mechanically
and the station refreshed it green. Its two drafted successors (one kit row;
one owner-routed through an `open_item`) mint at this merge.

### REVIEW-A — Round 3 — supervisor-drawn, independent Opus, hostile brief — tip 1f94aec

Verified by driving rather than reading: on a scaffold lane the merge refusal
names the four spine registries (SN included) and the snapshot directory on
a flip and admits the same lane without it; `_first_approval_drafts` mints
exactly one row on a released rung and none on a held one; the amendment
template's stale line survives only as quoted history. The record's ~20
citations resolve; both findings are real (`test_derive_stage.py` lacks the
work-branch exemption `check.py` carries; `trace.py` never calls
`human_approves_spine`) and unfiled elsewhere; `parse_dispositions` returns
two executable, non-duplicative drafts, neither instructing a flip or a
snapshot write; the corrected Bar claim is true (four `test_wi_convert`
guards had been skipped by the in-flight claim; 21 pass at the tip); smoke
1463 / 4 skipped in 21.18 s, budget 21.3 s. Three MINORs: one line pointer
off (`:606` vs `:573`), plan arms 4–6 unevidenced in the record though they
hold, and one trimmed `check_docs` quote.
(Full text: `003-REVIEW-A-1f94aec-supervisor.md`.)

VERDICT: APPROVE findings=3
