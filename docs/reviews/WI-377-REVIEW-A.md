### REVIEWER — G3 — Round 1 — 2026-07-31
Verdict: APPROVE
Findings:
- none — the double-print premise was confirmed both structurally and by driving real check.py runs; the helper's edges (spaces, retry, FAIL/SKIP, malformed lines) were probed against the actual step-name rules; the regression test's shapes match real output and the equality property holds on real outputs; the deviation is sound and honestly recorded; the diff is exactly the WI's scope. -> no remedy owed -> @owner
VERDICT: APPROVE findings=0

Driven green (the record):
- (a) **The double-print premise, confirmed structurally and empirically.** Under `--jobs>1`, run_plan's lane runner prints `"  {:5} {:16} {}".format(status, name, detail)` as each step finishes (check.py:1299), and main's summary block prints the identical shape for every result (check.py:1510-1513) — twice per step. Serially (`jobs <= 1`), run_step (check.py:1199-1215) prints only the `=== name : cmd ===` banner and streams child output; the status line appears once, in the summary. `_run_bar` invokes `--jobs 0` (integrate.py:544 — one worker per step), so the doubling hits exactly the integrator's path. **Driven, not just read:** a full G1 plan run in the worktree (`check.py --gate G1 --jobs 0` vs `--jobs 1`, both exit 0, 4 steps of which 3 PASS + the standing work-branch derived-gate SKIP) produced 6 `PASS` lines at jobs 0 vs 3 at jobs 1 — the old line-count would have reported "6 steps" vs "3 steps". Both REAL outputs fed through `integ._passed_steps` (via conftest's `load_script("integrate")`): jobs 0 → `{doc-navigability, privacy, registry-integrity}`, jobs 1 → the identical set, `equal: True`. (The cheaper `--run-steps` path prints no summary block at all — check.py:1450-1466 exits straight off run_plan's return — so the doubling and the fix were validated on the full-plan path _run_bar actually uses.)
- (b) **Edge behavior probed.** *Spaces in step names:* every built-in name is space-free (BUILTIN_STEP_NAMES, check.py:185ff) and this repo declares only `doc-refs`/`dupes`/`module-coverage` (docs/stack.ini:124/141/153); `extra_steps` validates only empty-name and built-in shadowing (check.py:322-329), so a hypothetical `[step:my step]` WOULD be accepted and `split()[1]` would truncate it to its first token, collapsing two such names — recorded as a note, not charged: the count is display-only (green/red is decided earlier by exit code plus the SKIP scan), the `{:16}` column format already makes a spaced name unparseable for any column reader, and the worst case is a slight undercount in a cosmetic figure where the old code double-counted everything. *FAIL-then-PASS retry:* impossible in one output — run_plan executes each plan step exactly once (no retry machinery, check.py:1264-1307), duplicate `[step:x]` sections raise configparser's DuplicateSectionError at profile read, shadowing exits loudly; and the count is computed only on the `code == 0` branch, where no FAIL exists. *FAIL/SKIP never count:* the regex requires the literal `PASS` token after leading whitespace — `RESULT: PASS` (starts "RESULT:") and pytest's `PASSED` (no whitespace after PASS) don't match; driven in the test's `mixed` case. *SKIP refusal untouched:* the `\s*SKIP\s` refusal scan (integrate.py:545-553) is byte-identical in the diff — only the passes count changed.
- (c) **The regression test, judged against real shapes and run.** The test's line shape (`"  PASS  format           0.1s"` — two-space indent, width-5 status, width-16 name) matches the real output verbatim (`"  PASS  perf-budgets     0.1s"` from the driven run), and its banner is exactly check.py:1505's `"=" * 56`. It pins the property the row asked for — the doubled `--jobs` shape and the serial shape of the same plan report the SAME count — plus the malformed name-less `PASS` guard and FAIL/SKIP exclusion. `pytest -q tests/test_integrate.py` in the worktree: **60 passed in 56.83s** (includes the new test). One cosmetic nit, not charged: the test comment's doubled quotes (`""bar PASS (40 steps)""`).
- (d) **The deviation is sound and honestly recorded.** The row offered banner-split or a check.py machine-total; the fragment (docs/log.d/WI-377-honest-bar-step-count.md) chose distinct-name and says why. The argument holds: a banner-split couples the integrator to the summary banner's exact width and character — any change there makes the split miss and **silently fall back to double-counting** (fail-open, the worst failure mode for a measurement fix); a machine-total line means changing check.py, a shipped take-wholesale file every adopter re-syncs (larger blast radius for a cosmetic figure). Distinct-name needs no check.py change and is provably jobs-invariant — demonstrated on real output in (a). The fragment labels it "Deviation from the filed remedy options" explicitly, and the archived spec's Deliverable restates both the choice and the reason. Its stance that existing "40 steps" log entries stand as history of what the tool printed is the right call — evidence records what was observed, and rewriting it would be the actual falsification.
- (e) **Scope exact; bars re-run in the worktree** (`c:\Projects\ai-template-wi377`, main repo's .venv). `git diff HEAD~1 --stat` = 4 files: integrate.py (+`_passed_steps` helper, `_run_bar` reporting `len()` of it — +25/-2), tests/test_integrate.py (+1 test), the new log fragment, and the spec's active→archive move gaining `## Deliverable` and dropping `specref` (the same close shape as WI-374/375/376). No check.py, schema, registry, or budgeted-file change. `check_trajectory.py --strict` → **clean** (375 work item(s), 358 done, graph acyclic; the two IF-080/081 WARNs are pre-existing WI-376 registry rows, exit 0). `check_docs.py --ignore docs/test/report.md --ignore "docs/work/*" --stale` → **OK — 328 doc(s), 933 intra-repo link(s), 0 broken** (2 pre-existing stale hints). The archived spec parses through the kit's own reader (`agent_common.read_spec_rows` over docs/work): WI-377 Status=`done`, Deliverable non-empty — R-A satisfied. The fragment's bar claims (targeted integrator tests green, check_docs OK, no budgeted file touched) were re-driven, not trusted, and match; its "smoke green except the standing work-branch red" is consistent with the documented `test_this_repo_is_not_a_work_branch` standing red every WI worktree shows by construction.
VERDICT: APPROVE findings=0
