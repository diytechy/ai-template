- [MAJOR] A1 -> In the When roadmap and How software-architecture SVGs, focused nodes such as `phase 1+2` and `CMP-002` update neither their detail panel nor drill state when Space is pressed, although Enter does; keyboard activation is therefore incomplete -> handle Space on every SVG node with the same activation path as click/Enter (and prevent its default page-scroll behavior) -> @owner
- [MAJOR] A4 -> In the How software-architecture graph, component rectangles depend on an outline of `rgb(226,232,240)` against `rgb(255,255,255)` in light mode (1.23:1), with the dark pair likewise only 1.22:1, both below the 3:1 graphical/UI-boundary floor -> raise the component boundary contrast to at least 3:1 in both themes and remeasure the rendered pairs -> @owner
- [MAJOR] T1 -> From the landing view, project state takes one Process-tab click and connections take one How-tab click, but no surface names the next action/WI: one When-tab click plus two hierarchy descents only exposes a queued `WI-228` without identifying it as next, while the Process resume-loop panel is generic method guidance -> add an obvious labelled next-action/next-WI status surface reachable with the single tab switch, including an explicit empty/blocked state when no next WI exists -> @owner
- [MAJOR] T2 -> The Knowledge tab opens with all 311 focusable concept nodes in one full graph instead of applying the declared `>3` start-collapsed rule, producing the large-project wall of nodes/edges the anchor forbids -> introduce a collapsed top-level knowledge view and breadcrumb-preserving drill-down, leaving flat rendering only for containers of three or fewer nodes -> @owner
VERDICT: CHANGES-REQUESTED findings=4
