# WI-575 — REVIEW-A rollup

Compiled by the supervising session (2026-09-02) from the round files under
`docs/reviews/wi-569-wi-508-spine-reseal-one-clean/`, time-ordered, governing
line last. WI-575 rode the same exclusive spine lane as WI-569, so the three
supervisor-drawn rounds cover both rows and are summarised here for the part
that judges this one. No mechanized round exists for this row: the loop's only
session on the lane never took it.

### Supervisor note — NOT a round, does not govern

WI-575 was claimed onto the lane with WI-569 and named in the same worker
assignment, but the single build session delivered WI-569 alone and the
weekday blackout opened before a second session could run. On the owner's
direction the supervisor stopped the loop and performed this row by hand: the
correction of LLR-158's `detail` and `code_symbol` cells that the WI-573
amendment adjudication withheld its re-anchor for.

The row's own constraint held and was re-driven at every tip: no `status`
cell moved, `intake.py snapshot` was never run, nothing under
`docs/archive/last_approved/` was written. The re-anchor of LLR-158 is owed
by the amendment adjudication this merge mints, not by this lane.

### REVIEW-A — Round 2 — supervisor-drawn, independent Opus, hostile brief — tip 4566ca2

On this row the round re-derived the partition by importing the modules and
confirmed the correction exactly: three spine registries, four in the
approval-act set once the need tier joins it, three outside it, seven
snapshotted, the union identity true and disjoint, and all twelve names in
`code_symbol` real attributes. No citation frame was smuggled into the cell.
The round's five findings were against the lane as a whole and none of them
was against this row's correction; its record defects are answered in the
WI-569 rollup.
(Full text: `002-REVIEW-A-4566ca2-supervisor.md`.)

VERDICT: CHANGES-REQUESTED findings=5

### REVIEW-A — Round 3 — supervisor-drawn verification — tip 9f8cab1

The lane's four reworked findings verified; this row's closed spec confirmed
well formed, with `## Deliverable` before `## Context` and `specref` cleared,
and the approval brief byte-identical to a fresh regeneration. One finding,
against the WI-569 fragment's count of its own rework.
(Full text: `003-REVIEW-A-9f8cab1-supervisor.md`.)

VERDICT: CHANGES-REQUESTED findings=1

### REVIEW-A — Round 4 — supervisor-drawn verification — tip 4e8bbb0

The count corrected, no contradiction surviving, the diff since the previous
tip confined to the record files, the lane constraint re-driven None at the
tip, and the open-items check down to the trunk-owned advisory alone.
(Full text: `004-REVIEW-A-4e8bbb0-supervisor.md`.)

VERDICT: APPROVE findings=0
