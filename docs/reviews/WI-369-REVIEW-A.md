### REVIEWER — G3 — Round 1 — 2026-07-30
Verdict: APPROVE
Findings:
- [MINOR] PROJECT_STATE.html:1 -> The close commit (`f90641f`) moved the WI-369 spec from `docs/work/active/` to `docs/work/archive/` — a registry edit — without regenerating the dashboard, so `gen_trajectory.py --check` exits 1 at HEAD ("project-state dashboard STALE") and the committed HTML still renders WI-369 among live work. The repo's own claim commit (`14acfb8`, "with its regeneration folded in") demonstrates the convention for the mirror-image move, and CLAUDE.md instructs regenerating after editing the registry or the spine. Non-blocking: the miss is mechanically visible (the freshness gate reds, nothing is silently wrong), the log fragment claims no green it didn't earn on this surface, and the train convention has the integrator re-regenerate on the composed tree — but the debt should be either paid or recorded. -> Run `python project-trajectory/scripts/gen_trajectory.py` and commit the regen (or record the integrator-owed regen explicitly at the drain sitting). -> @owner
- [MINOR] docs/declared-absences:14 -> Two header sentences still state the pre-WI-369 rule as an absolute — line 8 "an entry whose path MATERIALIZES must be removed (a test enforces both)" and line 14 "a materialized path fails the honesty test" — which is now false for the two `LIFECYCLE:` rows; the exception paragraph five lines below contradicts rather than qualifies them, leaving the file's own header stating the guard twice unmarked-blind. Non-blocking: the exception paragraph is adjacent, explicit, and correct ("Everything unmarked keeps the exact guard"), so a whole-header reader gets the true rule. -> One-word qualifiers: "an UNMARKED entry whose path materializes must be removed" / "an unmarked materialized path fails the honesty test". -> @owner
VERDICT: APPROVE findings=2
