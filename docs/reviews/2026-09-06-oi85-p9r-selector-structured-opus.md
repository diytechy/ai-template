# oi85-p9r-selector-structured — adversarial review

Requested route: Opus 5, high; declared CLI stream-json pattern.

Subject SHA256: `ede4288bf7237d31c08e68c5c6a00ddc5866dc0ec0cbb9b2aa685e878def4894`

Tracked invocation: [session log](../iteration/call_2d1f01b4fdea429fafbc72f5d1172014-20260906-160259.log).

Review uses supplied source with tools disabled. It is an independent
assessment, not a requirement approval or a test execution.

{
  "verdict": "CHANGES_REQUESTED",
  "findings": [
    "MAJOR — Stale-generated-output loophole: the narrow command set omits two declared freshness enforcers for artifacts derived from paths the selector marks narrowable. Seam: `scripts/check_changed.py` `commands()` (the four post-pytest commands: `derive_stage --check`, `trace --strict --no-placeholders --strict-schema`, `gen_trajectory --check`, `gen_trajectory --status --check`) together with `REGISTRY_DATA`/`GENERATED` membership. Scenario A (self-demonstrating on this very slice): the only edit is `module` values in `docs/requirements/low-level-requirements.toml` (`traj_render.py` -> `rendering/traj_render.py`). That path is in `REGISTRY_DATA`, the shape is str->str, so `data_only` is True and `select` returns `core+boundary`. The P9R execution record states this exact edit required regenerating the derived component inventory (\"`gen_components.py --root . --check` passes after regenerating the derived component list\"), yet no narrow command runs `gen_components.py --check`, so a committed-stale `docs/requirements/components.derived.toml` yields a green local bar. Scenario B: edit a `OneLine`/`Recommendation` value in `docs/requirements/open-items.toml` (also in `REGISTRY_DATA`, shape unchanged) -> `core+boundary`; `docs/open-items.html` — a committed generated owner surface with its own `open-items` harness step, named as such in `traj_status.status_block` — is never regenerated or checked. `--status --check` only byte-compares the status block, not that second rendered surface. This is precisely the contract's \"Keep required generated-output freshness when source data changes\" and \"Classify other generated HTML, including open-items views, deliberately rather than leaving an unnoticed second rendering path\". Inverted incentive that makes it worse: neither `docs/open-items.html` nor `docs/requirements/components.derived.toml` is in `GENERATED`, so an author who *does* regenerate them lands an unclassified path -> Full, while leaving them stale is the only route to a narrow result. Smallest root-cause repair: add those two paths to `GENERATED` (companion-only, so they still cannot authorize narrowing alone, exactly like `PROJECT_STATE.html`) and append their existing `--check` invocations to the narrow list in `commands()`. No new mechanism, no impact engine — two list entries and two commands.",
    "SUB-POINT of the above (same repair site, MINOR): `REGISTRY_DATA` lists `docs/requirements/components.toml`, which matches no path in this repository — the component carrier here is `components.derived.toml`. As written the entry is dead membership; and the derived carrier must not simply be moved into `REGISTRY_DATA` (that would let a hand-edit of a generated file narrow). Classify it as a generated companion per the repair above, and drop or correct the dead `components.toml` entry so the whitelist states what it actually covers."
  ],
  "basis": "Read the LLR-AND-RENDERING section 2 contract, the P9R work package, the complete execution record, the full `check_changed.py` source and its 31-case test module, `traj_display.py`, `traj_status.py`, the facade header/import gate/re-exports/`build_html`/`main`, the `process_panel` seam, the critique render-surface source, and the tracked product/test diffs. Verified each of the five previously-repaired defects independently rather than restating them: the `process_panel` default fourth argument is safe because `load_stage_record` returns `{}` not `None`; `_stage_facts` is stage-only and its test is non-vacuous (a deliberately malformed SR registry does not break it); `status_block`/`build_html` each take exactly one snapshot, so there is no duplicate or contradictory read; the render surface covers facade + root `traj_*.py` + `rendering/*.py` + recipe in both scaffold layouts, pinned by two tests; the absence proof copies the tree without `rendering/`, runs the real parser/status/pending modules and the real `--status` CLI, and the recorded `import rendering` mutation (9 failed/23 passed) shows it is not vacuous. Also checked the selector for the specific failure classes requested: `unknown`-list operator precedence groups correctly as three `and`-terms under `or`; deletes broaden through `is_file()`/`git show` failure; renames broaden as delete+add (tested); non-ancestor, absent and unreadable bases and non-UTF-8 path bytes all return `None` -> Full; generated-only -> Full; authored status prefix/suffix compared byte-for-byte with a marker-count guard; core/shared tests (`test_traj_status`, `test_traj_parse`, `test_gen_trajectory_pending`, `test_trajectory_arch`) remain selected; `HTML_TESTS` matches the 7-module census; exactly one representative HTML boundary case; failure codes propagate on the first nonzero command; nothing wires the selector into CI or the phase-close bar. Facade compatibility is preserved: `from rendering import traj_context, traj_graph, traj_panels, traj_render, traj_views` still binds the module objects as `gen_trajectory.<name>` attributes for the patch-the-module consumers, and all value re-exports are unchanged; direct top-level `import traj_render` was never the declared seam (`import gen_trajectory` is), and the affected tests were migrated. The `specref_findings` hunk is from an earlier commit in the range against base `83f2c7aa`, not this slice, and its ruling is recorded in commit 1e78ada3 — not re-litigated. The single blocking finding is a membership/command omission with a two-line repair, reproducible from the slice's own data edit; everything else in scope is sound."
}
