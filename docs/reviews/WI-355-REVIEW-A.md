### REVIEWER — G3 — Round 1 — 2026-07-29
Verdict: APPROVE
Findings:
- [MINOR] project-trajectory/scripts/check.py:66 -> The `--list` help still reads "Print the step plan for the gate and exit" with no caveat, but a residual divergence survives the fix BY DESIGN: with no `--gate` on a G3 repo, `--list` prints `check_trajectory.py --strict` while `--run-steps trajectory` runs it without `--strict` (both measured on a bootstrapped scaffold). That is the hook contract and must not be closed — but it is the same confusion class the row was opened about, and today it is recorded only in the archived spec's Deliverable body, not at the CLI. Non-blocking: the row's measured defect (explicit `--gate G3` on both surfaces) is fixed and pinned, and the `--run-step` help does now say "An explicit --gate builds the step AT that gate (else "all")", from which a careful reader can derive it. -> Add one line under `--list`, e.g. "With no --gate this reflects docs/gate; --run-step/--run-steps default to "all" (see --run-step)." -> @owner
- [MINOR] tests/test_check_harness.py:300 -> The comment claims "Name lookup stays unfiltered at any gate" and the Deliverable asserts "a gate-scoped step like `format` is still findable at any gate", but the only assertion is at gate `"all"`. I verified the claim is true today (`--gate G1 --run-step format` resolves and PASSes), yet the explicit-low-gate path is pinned by nothing: a future change making `steps()` filter its returned table by gate would break `--gate G1 --run-step format` with the whole suite green. Non-blocking: current behaviour is correct and the two halves the row demanded are both genuinely pinned. -> Add one line beside it, `assert [s for s in check.steps(80, "full", "G1") if s[0] == "format"]`. -> @owner
VERDICT: APPROVE findings=2
