# Handoff 2026-09-06 — redesign continuation

The independent runtime repairs, renderer separation and review/adoption
guidance are committed. **The whole redesign plan is not complete:** the
SR-161 perspective-record gap still prevents a complete TC-211 sample,
artifact adjudications remain separate, and the control window has not begun.
Replacing the runner remains conditional on evidence.

This is a resume map, not another plan or approval act. Start with
[`CLAUDE.md`](../CLAUDE.md), the `session-protocol` and `antidote` skills, then
the [current execution record](ai-template-redesign-2026-09-05-codex/EXECUTION-RECORD.md).
The [plan](ai-template-redesign-2026-09-05-codex/README.md) and its
[implementation packages](ai-template-redesign-2026-09-05-codex/IMPLEMENTATION.md)
remain the scope of record. Earlier handoffs and the
[September 5 sitting answers](decisions-for-review-2026-09-05.md) are history;
their old restart prompts and pending lists are not current instructions.

## Committed baseline

Branch: **`contract_split`**. The working tree was clean at the start of this
documentation sitting. Inspect Git again when resuming; this document will
itself be in a later documentation commit.

| Commit | Result |
|---|---|
| `cde260dd0aea68b92469c5b45fdde14c3dd78d59` | OI-85 applied; Fable repairs reviewed and corrected; independent P9R and guidance implementation finished |
| `ae610215` | Source-anchored adopter resync recipe and execution evidence linked to the implementation |

No push or control launch occurred. The tracked [`docs/work/pause`](work/pause)
remains present. Policy authority is in [`process.toml`](process.toml); the
current stage and dependency-ready frontier are generated in
[`status.md`](status.md#current-state). An additional
`redesign-codex-crossref-2026-09-05` worktree existed at this sitting; inspect
its ownership before treating it as disposable.

## What is finished

- **OI-85(a):** the exact SN-007/SN-026/SN-024 need-tier act and named-registry
  snapshot are recorded in the [owner ruling](log.d/2026-09-06-oi85-owner-ruling.md).
  It settles attended versus unattended Critique family selection while
  retaining fresh, non-author independence. The general need-drift detector
  was separately deferred. No other registry snapshot was approved by this act.
- **Current runner repairs and accounting:** the existing verified claim and
  linked-lane merge-base serve their respective evidence scopes; unavailable
  launch identity refuses execution; script drift drains and restarts through
  bounded launchers; interruption preserves an invocation record. Each
  invocation has route attribution and reported usage where available in
  [`iteration/`](iteration/), indexed by [`iteration_index.md`](iteration_index.md).
  Missing usage is explicit, and ambiguous or cumulative counters are not
  summed. This is useful future metrics input, not a complete billing meter.
- **P9R:** HTML emitters live under `scripts/rendering/`, sharing an in-memory
  display snapshot. Core status/parser tests exercise a renderer-absent copy;
  the supported `gen_trajectory` facade remains. The conservative local
  [`check_changed.py`](../scripts/check_changed.py) selector previews against
  an explicit proposal base and falls back to Full for unknown/shared impact.
  CI and phase-close Full remain unchanged. An ordinary-change narrow run has
  not yet supplied an operational speedup measurement. See [P9R evidence](ai-template-redesign-2026-09-05-codex/P9R-EXECUTION.md).
- **H1/H2/H3/H5 and adoption:** parent-need context, Critique provenance,
  semantic counterpart review and proportionality guidance are delivered.
  TC-209 and TC-210 Inspections pass; TC-211 is explicitly incomplete in
  [the procedures and results](test/inspection-procedures.md). The canonical
  [spine-authoring skill](../project-trajectory/skills/spine-authoring/SKILL.md)
  tells adopters to recheck hats against their own vision, derive new hats or
  needs where warranted, and rederive affected SRs through their own authority.
  [RESYNC_PACK.md](../project-trajectory/RESYNC_PACK.md) gives the coherent
  source/package migration from `cde260dd`; preserve adopter-owned records.

## Next work, in order of dependency

1. **Close the existing SR-161 implementation gap proportionately.** Read
   SR-161 in [system requirements](requirements/system-requirements.toml) and
   LLR-183's explicit `NOT DISCHARGED` clause in
   [low-level requirements](requirements/low-level-requirements.toml), then
   [the H5 record](ai-template-redesign-2026-09-05-codex/DECOMPOSITION-AMENDMENTS.md).
   Row-level `Hat-Refs` already resolve names and derive the effective set;
   they do not record each decomposition's applicability decisions or explicit
   no-finding outcomes. Recheck intake and the queue for an existing owner
   before minting work; WI-545 is broader code-decomposition debt and must not
   be assumed to own SR-161. Implement and verify the missing producer/consumer
   obligation through the existing method, then repeat TC-211's normal sample
   and counterexample. Do not weaken the Inspection or substitute prose for
   the required machine-readable record.
2. **Finish scoped artifact adjudications under the live authority.** The
   [amendment record](ai-template-redesign-2026-09-05-codex/DECOMPOSITION-AMENDMENTS.md)
   distinguishes delivery, Inspection results and first approval. SR-184–186
   and TC-209–211 remain Drafted. Existing queued adjudications retain their
   own specs and dependencies; completing code does not close them. Inspect
   [`ratify/CURRENT.md`](ratify/CURRENT.md) and the live snapshot diff before
   any approval. A whole-file SR snapshot can absorb unrelated older rationale
   amendments; the OI-85 need-only copy is not permission for that reset.
3. **Prepare the Short control launch.** The owner already selected the
   [operating envelope](ai-template-redesign-2026-09-05-codex/CONTROL-DECISION.md#owner-ruling-2026-09-06):
   first eight completed WIs or two active days, capped at twelve active
   coordinator hours and US$100 aggregate provider spend, including drain
   reservations. The unresolved issue is a defensible spending arrangement
   for every enabled route. The [read-only preflight](ai-template-redesign-2026-09-05-codex/CONTROL-PREFLIGHT.md)
   found telemetry but no aggregate admission cap or verified account bounds.
   Revalidate it at the proposed frozen launch commit; unknown spending cannot
   count as zero. After concrete launch preparation, the ruling requires the
   owner's reviewed deletion of the pause file. Keep queue, sampling,
   consolidation, authority and provider-consent settings as ruled. The
   perspective-record work and need-tier act are independent of this launch.
4. **Use the control evidence to decide the conditional packages.** P1/P1A,
   P3–P8 replacement and integration experiments, capability retirement and
   replacement P9/P10 cutover remain conditional. A weak sample means
   insufficient evidence and targeted repair, with no automatic second window.
   Worktrunk remains an optional operator workflow; no dependency or wholesale
   replacement decision follows from the independent repairs. Preserve the
   public renderer facade until a separate supported-version decision permits
   retiring it.

The queue remains live scope, not permission to implement every old proposal.
Re-read complete eligible specs and reconcile their obligations before claiming
or changing them. The current runner uses fresh builder invocations for review
rework; the WI, branch, prior evidence and feedback persist, not the previous
builder's hidden session context. Routing may keep or change the model under
the declared policy. See [execution details](ai-template-redesign-2026-09-05-codex/EXECUTION-DETAILS.md).

## Verification and review to carry forward

The implementation's final unfiltered suite passed: **3661 passed, 22 skipped
in 744.34s**. [The continuation log](log.d/2026-09-06-oi85-plan-completion.md)
retains failed-first checks, fixes, focused regressions and the final result.
This documentation sitting does not claim a new full run.
<!-- fig: cmd=".venv/bin/python -m pytest -q -n auto --junitxml=out/run-logs/oi85-full-final.xml" rev=1e78ada3+OI85-final; implementation committed as cde260dd -->

The resync follow-up's smoke tier passed with **1680 passed, 4 skipped**;
the separate enforcer measured **61.7s against the unchanged 60s ceiling** and
exited 1 on timing alone. The owner's standing permission to commit despite
this machine's flaky timing was used. No budget or existing test-tier ceiling
was weakened. See [that log](log.d/2026-09-06-oi85-resync.md); this documentation
sitting's checks are recorded in [its own log](log.d/2026-09-06-resume-surfaces.md).
<!-- fig: cmd=".venv/bin/python scripts/check_smoke_budget.py --mode enforce" rev=cde260dd+resync; out/run-logs/oi85-resync-smoke-budget.txt -->

Sol, Terra and Luna assisted implementation and review; intermediate Claude
CLI reviews used Opus 5 at high effort. The
[OI-85 review dispositions](reviews/2026-09-06-oi85-dispositions.md) link findings,
closure reviews and the incomplete Inspection. Invocation logs retain actual
usage availability. Local macOS results are not claims of a new Linux/Windows
CI run. Ignored `out/run-logs/` captures supplement the tracked records and
may be absent in another checkout.

For continued implementation, the owner's review preference is **Opus 5/high
for reviews during execution**, or **Fable 5.1/high if doing only one large
review at the end**. Use Claude CLI; consult the call patterns in the
[shipped agent guide](../project-trajectory/AGENTS.template.md) and the enabled
route definitions rather than assuming another chat's tools are available.

## Fresh-session kickoff

> Continue the redesign on `contract_split`. Read `CLAUDE.md`, the session and
> antidote skills, `docs/handoff-2026-09-06.md`, and the current section of the
> redesign execution record. Inspect Git and the live queue before editing.
> Resolve SR-161/LLR-183's missing per-decomposition perspective record with the
> smallest complete change and finish TC-211's sample; preserve the separate
> artifact-approval route. Use Sol/Terra/Luna for bounded independent work where
> useful and Claude CLI adversarial review under the model instructions above.
> Keep the control ruling and pause in force while preparing its spending and
> launch evidence. Commit completed work with hooks enabled; do not push.
