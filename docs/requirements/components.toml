[component.CMP-006]
name = "W1 Registry & conformance"
category = "software"
knowledge = "registry-hygiene"
status = "Drafted"
notes = """The spine and everything that decides whether it holds: spine_carrier, trace, trace_text, derive_gate, check_trajectory, plan_coverage, migrate_carrier, wi_convert, gen_arch_map. The shared helper package kitlib (LLR-181, WI-448) carries a tag here AND in CMP-007/008/009 because every component imports it; which unit OWNS a shared kernel is unruled and open as OI-48, so the four-way tag is a statement of usage, not of ownership. gen_arch_map also carries a CMP-009 tag through LLR-080, the one LLR row whose modules straddle this cut - its single import edge stays POLICED (IF-117 covers it), so no seam is suppressed."""

[component.CMP-007]
name = "W2 Gatekeeper"
category = "software"
status = "Drafted"
notes = """Every verdict a hook, CI job or gate run consumes: check, check_privacy, subagent_gate, the 8 check_* lints, and the shipped pre-commit/pre-push hooks."""

[component.CMP-008]
name = "W3 Autonomy"
category = "software"
knowledge = """docs/knowledge/agent-routing;docs/knowledge/effort-tiering;docs/knowledge/prompt-image-token-efficiency"""
status = "Drafted"
notes = """The unattended coordinator end to end: the agent_* family plus adjudicate_brief, the 5 plan_* round modules, dispatch, handback, intake, integrate, lane, prompts, schedule, score_reviews, spec_move, trunk_step."""

[component.CMP-009]
name = "W4 Human & adopter surfaces"
category = "software"
knowledge = "downstream-resync"
status = "Drafted"
notes = """Everything a person or an adopting repo reads or runs: bootstrap, run_menu, the 7 remaining gen_*, the 6 traj_* dashboard modules, and the onboard/dev-setup templates."""

