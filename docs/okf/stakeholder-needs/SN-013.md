---
type: "Stakeholder Need"
title: "No Python 3 interpreter on PATH (or the Windows Store alias that resolves but ex"
description: "No Python 3 interpreter on PATH (or the Windows Store alias that resolves but exits nonzero)"
tags: []
resource: "docs/requirements/stakeholder-needs.toml (SN-013)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/stakeholder-needs.toml (SN-013) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# SN-013 — No Python 3 interpreter on PATH (or the Windows Store alias that resolves but ex

**Need.** No Python 3 interpreter on PATH (or the Windows Store alias that resolves but exits nonzero)

**Why it matters.** Provision

**Acceptance intent.** The git hooks / coordinator probe by running a candidate and skip-or-report clearly; they never crash cryptically.
- Realized by: [SR-021](../system-requirements/SR-021.md)
