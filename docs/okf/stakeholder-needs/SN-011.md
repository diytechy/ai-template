---
type: "Stakeholder Need"
title: "The kit's scripts run on a clean Python 3.11+ with no pip installs, on Windows a"
description: "The kit's scripts run on a clean Python 3.11+ with no pip installs, on Windows and POSIX (and macOS)."
tags: []
resource: "docs/requirements/stakeholder-needs.md (SN-011)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/stakeholder-needs.md (SN-011) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# SN-011 — The kit's scripts run on a clean Python 3.11+ with no pip installs, on Windows a

**Need.** The kit's scripts run on a clean Python 3.11+ with no pip installs, on Windows and POSIX (and macOS).

**Why it matters.** Portability — the kit must not impose its own dependency stack on a downstream repo to run its checks.

**Acceptance intent.** Every kit script is stdlib-only; the CI matrix is green on Linux + Windows + macOS.
- Realized by: [SR-034](../system-requirements/SR-034.md) · [SR-035](../system-requirements/SR-035.md) · [SR-114](../system-requirements/SR-114.md)
