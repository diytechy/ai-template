---
type: "Stakeholder Need"
title: "Several LLM families are configurable \u2014 selected per *job* and per capability le"
description: "Several LLM families are configurable \u2014 selected per *job* and per capability level \u2014 and work that benefits from an independent second opinion is automatically routed to a *different* family wherever that is configured; every prompt a job launches is reviewable prose and every launched session is attributable."
tags: []
resource: "docs/requirements/stakeholder-needs.md (SN-026)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/stakeholder-needs.md (SN-026) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# SN-026 — Several LLM families are configurable — selected per *job* and per capability le

**Need.** Several LLM families are configurable — selected per *job* and per capability level — and work that benefits from an independent second opinion is automatically routed to a *different* family wherever that is configured; every prompt a job launches is reviewable prose and every launched session is attributable.

**Why it matters.** One family is a single point of both failure and bias: an outage, a retired model or an expired auth stalls the whole loop, and a reviewer drawn from the implementer's own family shares its blind spots — so a same-family review reads like corroboration while adding little. Declaring the families and levels in a registry, rather than hard-coding one vendor, is also what keeps the kit vendor-neutral as models turn over. And a routing decision is only half the story: if the *prose* a role is launched with lives inside a script, the people who most need to review the redaction and adversarial clauses cannot see them, and a recorded verdict cannot be reproduced or challenged because nothing says which prose produced it.

**Acceptance intent.** Every declared job — planner, critic, arbiter, implementer, reviewer, adjudicator — resolves through a declared weighted pool with a capability requirement and same-or-stronger fallback; the draw prefers a cross-family route for the judging roles, degrades to the *documented* same-family mode when only one family is routable rather than silently skipping the second opinion, and logs every selection before launch — never a silent model swap. Each job's prompt is a versioned Markdown template with a declared slot contract, declared prohibited source classes and a declared output schema; one strict renderer refuses an unknown, missing or unrendered slot before launch; and every session record carries the template id/hash, the rendered-prompt hash, the route id and the model.
- Realized by: [SR-079](../system-requirements/SR-079.md) · [SR-080](../system-requirements/SR-080.md) · [SR-083](../system-requirements/SR-083.md) · [SR-084](../system-requirements/SR-084.md) · [SR-154](../system-requirements/SR-154.md) · [SR-155](../system-requirements/SR-155.md) · [SR-156](../system-requirements/SR-156.md) · [SR-157](../system-requirements/SR-157.md)
