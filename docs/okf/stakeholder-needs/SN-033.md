---
type: "Stakeholder Need"
title: "Scope: template (adopters + this repo). A stakeholder can read each stakeholder "
description: "Scope: template (adopters + this repo). A stakeholder can read each stakeholder need and recognize the outcome they asked for without knowing how the repository is built."
tags: []
resource: "docs/requirements/stakeholder-needs.toml (SN-033)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/stakeholder-needs.toml (SN-033) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# SN-033 — Scope: template (adopters + this repo). A stakeholder can read each stakeholder 

**Need.** Scope: template (adopters + this repo). A stakeholder can read each stakeholder need and recognize the outcome they asked for without knowing how the repository is built.

**Why it matters.** A need written in internal engineering vocabulary can remain technically current while becoming unreadable to the person whose intent it is supposed to preserve. The resulting failure is an approved requirement spine that no stakeholder can independently validate.

**Acceptance intent.** The rule applies specifically to each SN need cell, not to engineering requirements or acceptance evidence. A declared check reports the row and phrase when a need cell contains an internal path, implementation-only identifier or process citation; a reviewed exception list distinguishes names that are themselves user-facing interfaces. The DevStg-Reqs review evidence includes the resulting findings.
- Realized by: [SR-150](../system-requirements/SR-150.md)
