---
type: "Stakeholder Need"
title: "Scope: this repo (adopters advised). A person working on this repository can ope"
description: "Scope: this repo (adopters advised). A person working on this repository can open one launcher that lists the repository's available actions and runs the one they choose, on each platform this repository supports; adopters are told the pattern exists."
tags: []
resource: "docs/requirements/stakeholder-needs.toml (SN-035)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/stakeholder-needs.toml (SN-035) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# SN-035 — Scope: this repo (adopters advised). A person working on this repository can ope

**Need.** Scope: this repo (adopters advised). A person working on this repository can open one launcher that lists the repository's available actions and runs the one they choose, on each platform this repository supports; adopters are told the pattern exists.

**Why it matters.** This repository has a dozen legitimate actions whose invocations are buried in documentation; a menu makes them reachable without remembered commands. This deliberately proposes amending the recorded self-application boundary ("no product launchers" — this repo has no product) to "no product launch": a menu of repository actions is not a product. The boundary prose in the contributor guide and status scope amends only if this need is attested.

**Acceptance intent.** A root menu launcher exists for this repository's supported platforms, lists actions from one declared action inventory (so completeness is checkable against a single source), and runs the chosen action; the recorded non-goal is amended in the same change that lands the launcher, not before. (Sitting note: the adversarial review found the existing shipped requirement already specifies the menu launcher for adopters and recommended executing this as a one-requirement self-application amendment rather than a new need — kept at need tier for the sitting to settle.)
