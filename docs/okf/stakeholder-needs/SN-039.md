---
type: "Stakeholder Need"
title: "Scope: template (adopters + this repo). An adopter can tell whether each stakeho"
description: "Scope: template (adopters + this repo). An adopter can tell whether each stakeholder need applies to the reusable template, this repository alone, or both, without inferring scope from surrounding prose."
tags: []
resource: "docs/requirements/stakeholder-needs.toml (SN-039)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/stakeholder-needs.toml (SN-039) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# SN-039 — Scope: template (adopters + this repo). An adopter can tell whether each stakeho

**Need.** Scope: template (adopters + this repo). An adopter can tell whether each stakeholder need applies to the reusable template, this repository alone, or both, without inferring scope from surrounding prose.

**Why it matters.** A need whose audience is ambiguous gets enforced against the wrong repositories: an obligation meant only for the template's own upkeep leaks into every adopter's process, or a template promise is quietly treated as one repository's habit. The scope must be a declared value a check can read, not a reading-between-the-lines exercise.

**Acceptance intent.** Every non-example stakeholder-need row carries a required scope value from a closed vocabulary (template / this repository / both); a checker reports a missing or invalid value; the scope field enters the registry schema in the scheduled schema batch rather than as a one-off column.
