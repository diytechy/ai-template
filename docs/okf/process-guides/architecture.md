---
type: "Process Guide"
title: "Architecture \u2014 the kit meta-repo (self-adoption)"
description: "One page over the kit's **own** product: the reusable process kit under `project-trajectory/` \u2014 its runnable scripts (`project-trajectory/scripts/`), git hooks (`project-trajectory/hooks/`), and the artifact templates it ships \u2014 verified b\u2026"
tags: []
resource: "docs/architecture.md"
---

# architecture — Architecture — the kit meta-repo (self-adoption)

**Summary.** One page over the kit's **own** product: the reusable process kit under `project-trajectory/` — its runnable scripts (`project-trajectory/scripts/`), git hooks (`project-trajectory/hooks/`), and the artifact templates it ships — verified b…

**Source (unmodified).** [docs/architecture.md](../../../docs/architecture.md).
