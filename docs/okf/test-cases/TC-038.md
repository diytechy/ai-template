---
type: "Test Case"
title: "verifies SR-038;LLR-035"
description: "Run the dashboard suite; the generated root HTML is one offline file (no external hosts/CDN) carrying the completeness tiles, the spine icicle, the roadmap DAG, the module-map view when an inventory exists, the OKF Knowledge tab when a committed docs/okf bundle exists (typed nodes, spine edges, embedded descriptions, docs/okf link-outs that resolve; the GENERATED banner never rendered; a malformed concept skipped with a warn; omitted and byte-identical without a bundle), the git-derived as-of stamp (absent outside git; a stamp-only diff passes --check) and the responsive mobile-shell markers; generation is byte-deterministic and --check trips stale/missing output."
tags: ["Full", "Integration", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.csv (TC-038)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-038) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-038 — verifies SR-038;LLR-035

**Method.** Run the dashboard suite; the generated root HTML is one offline file (no external hosts/CDN) carrying the completeness tiles, the spine icicle, the roadmap DAG, the module-map view when an inventory exists, the OKF Knowledge tab when a committed docs/okf bundle exists (typed nodes, spine edges, embedded descriptions, docs/okf link-outs that resolve; the GENERATED banner never rendered; a malformed concept skipped with a warn; omitted and byte-identical without a bundle), the git-derived as-of stamp (absent outside git; a stamp-only diff passes --check) and the responsive mobile-shell markers; generation is byte-deterministic and --check trips stale/missing output.

**Expected.** Satisfies SR-038 AcceptanceCriteria

**Evidence.** `tests/test_gen_trajectory.py`
- Verifies (SR): [SR-038](../system-requirements/SR-038.md)
- Verifies (LLR): [LLR-035](../low-level-requirements/LLR-035.md)
