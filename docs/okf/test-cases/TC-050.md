---
type: "Test Case"
title: "verifies SR-049;LLR-050"
description: "derive the gate from fixture states across every per-artifact rule and guard the cache; --print reports the derived current phase"
tags: ["Full", "Unit", "Automated=Yes", "Approved"]
resource: "docs/test/test-cases.toml (TC-050)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-050) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-050 — verifies SR-049;LLR-050

**Method.** derive the gate from fixture states across every per-artifact rule and guard the cache; --print reports the derived current phase

**Expected.** derive_gate computes the correct gate per artifact state (draft drop, undecomposed DevStg-Reqs, decomposed-unverified DevStg-Tests - including a Modified SR, whose phase reads DevStg-Tests with children counted-but-never-capping - and verified DevStg-Impl) and --check catches drift; the compared basis line carries modified=N beside drafts=N; --print shows the derived current phase (the meta reads phase=4)

**Evidence.** `tests/test_derive_gate.py`
- Verifies (SR): [SR-049](../system-requirements/SR-049.md)
- Verifies (LLR): [LLR-050](../low-level-requirements/LLR-050.md)
