---
type: "Test Case"
title: "verifies SR-061;LLR-073"
description: "Drive plan_coverage_step end-to-end with the real plan_round machine and the real plan_coverage.py subprocess over goal/plan fixtures: a clean run advances the round past coverage1; a findings run bounces to a repair offered for exactly the implicated plan and a repaired file re-runs clean and advances; a findings run repeating after its one repair PAGEs; an exit-2 malformed input is its own outcome (malformed=True, findings=False) yet bounces both plans; implicated parsing names only the faulting plan; and the report payload is captured (never printed)."
tags: ["Smoke", "Integration", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.csv (TC-073)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-073) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-073 — verifies SR-061;LLR-073

**Method.** Drive plan_coverage_step end-to-end with the real plan_round machine and the real plan_coverage.py subprocess over goal/plan fixtures: a clean run advances the round past coverage1; a findings run bounces to a repair offered for exactly the implicated plan and a repaired file re-runs clean and advances; a findings run repeating after its one repair PAGEs; an exit-2 malformed input is its own outcome (malformed=True, findings=False) yet bounces both plans; implicated parsing names only the faulting plan; and the report payload is captured (never printed).

**Expected.** Satisfies SR-061 AcceptanceCriteria for the dual-plan coverage step adapter (provisional home pending the dual-plan structuring WI)

**Evidence.** `tests/test_plan_coverage_step.py`
- Verifies (SR): [SR-061](../system-requirements/SR-061.md)
- Verifies (LLR): [LLR-073](../low-level-requirements/LLR-073.md)
