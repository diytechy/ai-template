---
type: "Test Case"
title: "verifies SR-053;LLR-104"
description: "Generate the dashboard with the Process tab enabled (G2 gate) and assert --nhead is declared, .stgt/.stgn resolve to var(--nlabel)/var(--nsub), .hooplab/.hubname resolve to var(--nhead), and none of the four selectors' rule bodies contain a raw px value."
tags: ["Full", "Unit", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.toml (TC-107)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-107) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-107 — verifies SR-053;LLR-104

**Method.** Generate the dashboard with the Process tab enabled (G2 gate) and assert --nhead is declared, .stgt/.stgn resolve to var(--nlabel)/var(--nsub), .hooplab/.hubname resolve to var(--nhead), and none of the four selectors' rule bodies contain a raw px value.

**Expected.** No ad-hoc px font-size remains on .stgt/.stgn/.hooplab/.hubname; all four resolve to a declared CSS custom property.

**Evidence.** `tests/test_traj_render.py::test_u1_process_tab_type_scale_matches_the_shared_tokens`
- Verifies (SR): [SR-053](../system-requirements/SR-053.md)
- Verifies (LLR): [LLR-104](../low-level-requirements/LLR-104.md)
