---
type: "Test Case"
title: "verifies SR-052;LLR-101"
description: "Generate the dashboard from a tiered fixture registry and walk EVERY emitted <svg> subtree: an svg declaring role=img must contain no tabindex descendant and must carry its own accessible name (aria-label or <title>); every other emitted container declares role=group. Guarded against a vacuous pass by asserting the fixture emits focusable nodes at all. Regression-probed: forcing the role back to a constant img fails the tabindex assertion."
tags: ["Full", "Unit", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.csv (TC-104)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-104) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-104 — verifies SR-052;LLR-101

**Method.** Generate the dashboard from a tiered fixture registry and walk EVERY emitted <svg> subtree: an svg declaring role=img must contain no tabindex descendant and must carry its own accessible name (aria-label or <title>); every other emitted container declares role=group. Guarded against a vacuous pass by asserting the fixture emits focusable nodes at all. Regression-probed: forcing the role back to a constant img fails the tabindex assertion.

**Expected.** No focusable element inside a children-presentational subtree (was 1,146 of 1,150 before WI-297; now 0), 43 containers role=group, the one remaining role=img being the non-interactive loops diagram with its aria-label.

**Evidence.** `tests/test_gen_trajectory.py::test_a2_no_focusable_node_sits_inside_a_children_presentational_svg ::test_svg_nodes_carry_escaped_title_tooltips`
- Verifies (SR): [SR-052](../system-requirements/SR-052.md)
- Verifies (LLR): [LLR-101](../low-level-requirements/LLR-101.md)
