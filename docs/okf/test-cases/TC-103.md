---
type: "Test Case"
title: "verifies SR-054;LLR-100"
description: "Generate the dashboard from a tiered fixture registry and assert the drill emits <nav class=\"crumbs\" with a declared data-root-crumb root label, and that the crumb-click handler truncates the trail to the clicked ancestor (trail=trail.slice(0,i+1)) so the parent view is restored."
tags: ["Full", "Unit", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.toml (TC-103)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-103) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-103 — verifies SR-054;LLR-100

**Method.** Generate the dashboard from a tiered fixture registry and assert the drill emits <nav class="crumbs" with a declared data-root-crumb root label, and that the crumb-click handler truncates the trail to the clicked ancestor (trail=trail.slice(0,i+1)) so the parent view is restored.

**Expected.** Breadcrumb nav present with a root crumb; crumb click restores the ancestor view.

**Evidence.** `tests/test_traj_views.py::test_when_view_breadcrumb_restores_parent`
- Verifies (SR): [SR-054](../system-requirements/SR-054.md)
- Verifies (LLR): [LLR-100](../low-level-requirements/LLR-100.md)
