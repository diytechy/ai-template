---
type: "Test Case"
title: "verifies SR-055;LLR-056"
description: "Run the dashboard suite; the Process tab renders both loop panels (intake loop A + human-decision loop B) with the shared LLM_Agent entry node appearing exactly once, every stage's link resolving to its canonical doc, the gate-ratification stage present in loop B, a data-less repo rendering byte-identically, and generation byte-deterministic with --check tripping stale output."
tags: ["Full", "Integration", "Automated=Yes", "Planned"]
resource: "docs/test/test-cases.csv (TC-056)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-056) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-056 — verifies SR-055;LLR-056

**Method.** Run the dashboard suite; the Process tab renders both loop panels (intake loop A + human-decision loop B) with the shared LLM_Agent entry node appearing exactly once, every stage's link resolving to its canonical doc, the gate-ratification stage present in loop B, a data-less repo rendering byte-identically, and generation byte-deterministic with --check tripping stale output.

**Expected.** Both loop panels present; LLM_Agent entry node rendered once and shared; loop A stages = [intake -> triage-to-WI -> resume-loop -> build/review -> merge]; loop B stages = [open-items population incl. gate-ratification -> human review/ruling -> log Decisions -> merge], each stage 1:1 to a panel node; all stage links resolve; data-less render byte-identical; deterministic; --check trips.

**Evidence.** `tests/test_gen_trajectory.py::test_process_tab_renders_intake_and_decision_loops ::test_process_loops_share_one_llm_agent_entry ::test_process_loop_stage_links_resolve ::test_process_loops_byte_identical_without_data`
- Verifies (SR): [SR-055](../system-requirements/SR-055.md)
- Verifies (LLR): [LLR-056](../low-level-requirements/LLR-056.md)
