---
type: "Test Case"
title: "verifies SR-053;LLR-109"
description: "Enumerate every declared colour vocabulary and compute pairwise CIE76 deltaE over all cross pairs, skipping the declared tier<->okf mirror (and asserting that mirrored pair is byte-equal). Assert >= 15 within a vocabulary and >= 12 across. Pure constant arithmetic - no fixture render needed."
tags: ["Full", "Unit", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.toml (TC-114)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-114) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-114 — verifies SR-053;LLR-109

**Method.** Enumerate every declared colour vocabulary and compute pairwise CIE76 deltaE over all cross pairs, skipping the declared tier<->okf mirror (and asserting that mirrored pair is byte-equal). Assert >= 15 within a vocabulary and >= 12 across. Pure constant arithmetic - no fixture render needed.

**Expected.** No pair below its floor; closest surviving pair 12.5 (Process Guide vs phase[2]).

**Evidence.** `tests/test_traj_render.py::test_u5_pairwise_deltae_holds_within_and_across_every_vocabulary`
- Verifies (SR): [SR-053](../system-requirements/SR-053.md)
- Verifies (LLR): [LLR-109](../low-level-requirements/LLR-109.md)
