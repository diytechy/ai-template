---
type: "Test Case"
title: "verifies SR-140;LLR-158"
description: "Run the attestation-ledger suite: it DRIVES the premise (the amendment seam reports an amend+flip as empty) before asserting the ledger catches it, pins the digest to the ratified cells only, and drives the ghost anchor, the `superseded` retirement, and the ledger's deletion, rename and creation against real git plumbing."
tags: ["Smoke", "Integration", "Automated=Yes", "Draft"]
resource: "docs/test/test-cases.csv (TC-153)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-153) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-153 — verifies SR-140;LLR-158

**Method.** Run the attestation-ledger suite: it DRIVES the premise (the amendment seam reports an amend+flip as empty) before asserting the ledger catches it, pins the digest to the ratified cells only, and drives the ghost anchor, the `superseded` retirement, and the ledger's deletion, rename and creation against real git plumbing.

**Expected.** Satisfies SR-140 AcceptanceCriteria

**Evidence.** `tests/test_attestation_ledger.py`
- Verifies (SR): [SR-140](../system-requirements/SR-140.md)
- Verifies (LLR): [LLR-158](../low-level-requirements/LLR-158.md)
