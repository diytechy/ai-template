---
type: "Test Case"
title: "verifies SR-053;LLR-102"
description: "Assert the U5 palette-decollision invariants directly against the declared Python constants: (1) TIER_FILL[sn/sr/llr/tc] equals OKF_TYPE_FILL[Stakeholder Need/System Requirement/Low-Level Requirement/Test Case] pairwise (the intentional mirror, checked as a bijection not a set match), (2) no hex is shared across the status / tier+type / sw / phase vocabulary groups outside that mirror, (3) no PHASE_ACCENTS entry equals the light or dark --accent token, (4) pairwise (all C(8,2) pairs) CIE76 deltaE across PHASE_ACCENTS is >= 15."
tags: ["Full", "Unit", "Automated=Yes", "Approved"]
resource: "docs/test/test-cases.toml (TC-105)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-105) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-105 — verifies SR-053;LLR-102

**Method.** Assert the U5 palette-decollision invariants directly against the declared Python constants: (1) TIER_FILL[sn/sr/llr/tc] equals OKF_TYPE_FILL[Stakeholder Need/System Requirement/Low-Level Requirement/Test Case] pairwise (the intentional mirror, checked as a bijection not a set match), (2) no hex is shared across the status / tier+type / sw / phase vocabulary groups outside that mirror, (3) no PHASE_ACCENTS entry equals the light or dark --accent token, (4) pairwise (all C(8,2) pairs) CIE76 deltaE across PHASE_ACCENTS is >= 15.

**Expected.** No cross-vocabulary hex collision found; no phase equals --accent; minimum pairwise deltaE >= 15 (achieved 18.1).

**Evidence.** `tests/test_traj_render.py::test_u5_no_hex_reused_across_unrelated_colour_vocabularies tests/test_traj_render.py::test_u5_phase_accents_clear_a_pairwise_deltae_floor`
- Verifies (SR): [SR-053](../system-requirements/SR-053.md)
- Verifies (LLR): [LLR-102](../low-level-requirements/LLR-102.md)
