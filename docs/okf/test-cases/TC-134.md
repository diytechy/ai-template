---
type: "Test Case"
title: "verifies SR-006;LLR-141"
description: "Run the check-harness lane suite: a claimed work branch skips the declared freshness steps with the \u00a75.2 notice and untouched exit code while non-freshness steps still run; the closing commit and its successors keep the skip (history-derived signal); the trunk lane never skips."
tags: ["Full", "Integration", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.toml (TC-134)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-134) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-134 — verifies SR-006;LLR-141

**Method.** Run the check-harness lane suite: a claimed work branch skips the declared freshness steps with the §5.2 notice and untouched exit code while non-freshness steps still run; the closing commit and its successors keep the skip (history-derived signal); the trunk lane never skips.

**Expected.** Satisfies the work-branch lane-skip acceptance of SR-006 via the LLR-141 detail contract

**Evidence.** `tests/test_check_lane.py`
- Verifies (SR): [SR-006](../system-requirements/SR-006.md)
- Verifies (LLR): [LLR-141](../low-level-requirements/LLR-141.md)
