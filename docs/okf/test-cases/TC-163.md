---
type: "Test Case"
title: "verifies SR-149;LLR-169"
description: "Plant a retired tag in a live authored file and one in each declared carve-out class (archive, the project log, an attestation brief, a closed work-item spec, a generated surface, the owner scratchpad), plus one at depth under a carved-out directory; run check_vocab.py at both severities; plant a line marker and a file marker; plant the word 'gate' in its surviving sense; and run the checker over the real meta-repo tree."
tags: ["Smoke", "Unit", "Automated=Yes", "Draft"]
resource: "docs/test/test-cases.toml (TC-163)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-163) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-163 — verifies SR-149;LLR-169

**Method.** Plant a retired tag in a live authored file and one in each declared carve-out class (archive, the project log, an attestation brief, a closed work-item spec, a generated surface, the owner scratchpad), plus one at depth under a carved-out directory; run check_vocab.py at both severities; plant a line marker and a file marker; plant the word 'gate' in its surviving sense; and run the checker over the real meta-repo tree.

**Expected.** The live file is reported with its path and line; the default severity exits 0 and --strict exits 1 on the same finding; no carve-out is ever reported, at any depth; a marked line is skipped while its unmarked neighbour is not, and a marked file is skipped whole; the surviving sense of 'gate' is clean; and the meta-repo itself is clean at the ERROR severity, so the assertions cannot pass against fixtures while the conversion rots.

**Evidence.** `tests/test_stage_ladder.py`
- Verifies (SR): [SR-149](../system-requirements/SR-149.md)
- Verifies (LLR): [LLR-169](../low-level-requirements/LLR-169.md)
