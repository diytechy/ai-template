---
type: "Test Case"
title: "verifies SR-157;LLR-003"
description: "Run the registry-checks suite; leftover -000 rows, empty/out-of-vocab fields, and (once the spine is phased) a ratified row whose Phase is not a full-cell bare integer (blank, prefixed like P1/v2, or unparseable) are flagged."
tags: ["Full", "Integration", "Automated=Yes", "Approved"]
resource: "docs/test/test-cases.toml (TC-003)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-003) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-003 — verifies SR-157;LLR-003

**Method.** Run the registry-checks suite; leftover -000 rows, empty/out-of-vocab fields, and (once the spine is phased) a ratified row whose Phase is not a full-cell bare integer (blank, prefixed like P1/v2, or unparseable) are flagged.

**Expected.** Satisfies the acceptance folded into LLR-003 (parent SR-157)

**Evidence.** `tests/test_registry_checks.py`
- Verifies (SR): [SR-157](../system-requirements/SR-157.md)
- Verifies (LLR): [LLR-003](../low-level-requirements/LLR-003.md)
