---
type: "Test Case"
title: "verifies SR-070;LLR-139"
description: "Run the pending-surface pause suite: the block renders since/reason verbatim (no clock), omits the stamp when since is empty, matches the reader's fail-closed placeholder on malformation, clears on deletion, and is deterministic."
tags: ["Full", "Unit", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.toml (TC-136)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-136) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-136 — verifies SR-070;LLR-139

**Method.** Run the pending-surface pause suite: the block renders since/reason verbatim (no clock), omits the stamp when since is empty, matches the reader's fail-closed placeholder on malformation, clears on deletion, and is deterministic.

**Expected.** The pending block renders 'Paused since <date> - <reason>' with the declared date verbatim (no clock), omits the stamp when since is empty, renders the pinned fail-closed placeholder on malformation, and clears when the pause file is deleted.

**Evidence.** `tests/test_gen_trajectory_pending.py::test_tracked_pause_projects_reason_and_declared_since; tests/test_gen_trajectory_pending.py::test_pause_malformed_text_matches_the_coordinator_reader`
- Verifies (SR): [SR-070](../system-requirements/SR-070.md)
- Verifies (LLR): [LLR-139](../low-level-requirements/LLR-139.md)
