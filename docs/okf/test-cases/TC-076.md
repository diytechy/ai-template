---
type: "Test Case"
title: "verifies SR-066;LLR-076"
description: "Run the real agent_loop --dual-plan over a fixture repo (PlanMode=dual WI, C#-claused goal brief, rubric, registries) with a fake agent CLI: the full round completes unattended (goal/plans/coverage/critiques/verdict-run1+2/verdict artifacts in docs/plans/DP-001-*/, both position-swapped runs agreeing via a content-keyed arbiter), the selected plan's rows land as queued WIs hanging off the parent and the real check_trajectory passes; the worker --wi path refuses a dual row fail-closed naming --dual-plan; the flag on a non-dual row is refused (the trigger is declared at filing, never by flag); a label-keyed (position-biased) arbiter disagrees across the swap and PAGEs position-unstable with the attended NEEDS-HUMAN run-state written; a missing plan rubric PAGEs honestly."
tags: ["Full", "Integration", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.csv (TC-076)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-076) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-076 — verifies SR-066;LLR-076

**Method.** Run the real agent_loop --dual-plan over a fixture repo (PlanMode=dual WI, C#-claused goal brief, rubric, registries) with a fake agent CLI: the full round completes unattended (goal/plans/coverage/critiques/verdict-run1+2/verdict artifacts in docs/plans/DP-001-*/, both position-swapped runs agreeing via a content-keyed arbiter), the selected plan's rows land as queued WIs hanging off the parent and the real check_trajectory passes; the worker --wi path refuses a dual row fail-closed naming --dual-plan; the flag on a non-dual row is refused (the trigger is declared at filing, never by flag); a label-keyed (position-biased) arbiter disagrees across the swap and PAGEs position-unstable with the attended NEEDS-HUMAN run-state written; a missing plan rubric PAGEs honestly.

**Expected.** Satisfies SR-066 AcceptanceCriteria for the dual-plan coordinator dispatch

**Evidence.** `tests/test_agent_loop_dualplan.py`
- Verifies (SR): [SR-066](../system-requirements/SR-066.md)
- Verifies (LLR): [LLR-076](../low-level-requirements/LLR-076.md)
