---
type: "Test Case"
title: "verifies SR-040;LLR-037"
description: "Run the agent-loop suite; a REVIEW-B-mapped phase invokes the second fake CLI and not the default, a broken map entry exits preflight naming the phase, and the banner carries review-policy.\nCORRECTED 2026-08-18: this method claimed a fourth clause \u2014 \"a bloated status.md warns without blocking\" \u2014 that NO test in the evidence file performs, and the row was `Approved` while claiming it. The file-granular evidence cell is what hid it: naming a 63-test file rather than the functions leaves a stated clause and an absent test indistinguishable, so the evidence below is now function-granular and the clause is dropped rather than left as a green nothing checks. The tripwire it referred to has no carrier at all (SR-040's acceptance and LLR-037 record the retirement and the refused substitute); when the sitting rules whether to rebuild it, its test is minted with it."
tags: ["Full", "Integration", "Automated=Yes", "Modified"]
resource: "docs/test/test-cases.toml (TC-040)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-040) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-040 — verifies SR-040;LLR-037

**Method.** Run the agent-loop suite; a REVIEW-B-mapped phase invokes the second fake CLI and not the default, a broken map entry exits preflight naming the phase, and the banner carries review-policy.
CORRECTED 2026-08-18: this method claimed a fourth clause — "a bloated status.md warns without blocking" — that NO test in the evidence file performs, and the row was `Approved` while claiming it. The file-granular evidence cell is what hid it: naming a 63-test file rather than the functions leaves a stated clause and an absent test indistinguishable, so the evidence below is now function-granular and the clause is dropped rather than left as a green nothing checks. The tripwire it referred to has no carrier at all (SR-040's acceptance and LLR-037 record the retirement and the refused substitute); when the sitting rules whether to rebuild it, its test is minted with it.

**Expected.** Satisfies SR-040's routing and dial criteria; the resume-surface tripwire clause is unverified BY CONSTRUCTION because it has no carrier

**Evidence.** `tests/test_agent_loop.py::test_cmd_map_broken_entry_fails_preflight; tests/test_agent_loop.py::test_review_policy_surfaced_in_banner; tests/test_agent_loop.py::test_model_map_entry_without_equals_fails_preflight`
- Verifies (SR): [SR-040](../system-requirements/SR-040.md)
- Verifies (LLR): [LLR-037](../low-level-requirements/LLR-037.md)
