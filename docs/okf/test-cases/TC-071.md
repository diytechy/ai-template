---
type: "Test Case"
title: "verifies SR-061;LLR-071"
description: "Assemble each hat brief over a fixture repo whose status.md/log.md carry a sentinel and registries carry known rows; assert every brief contains registry material and never the sentinel; assert strict-fill errors (unknown key, unfilled placeholder), dispatcher-block stripping, override-wins-over-kit template loading, and hat-key vocabulary stability."
tags: ["Smoke", "Unit", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.csv (TC-071)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-071) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-071 — verifies SR-061;LLR-071

**Method.** Assemble each hat brief over a fixture repo whose status.md/log.md carry a sentinel and registries carry known rows; assert every brief contains registry material and never the sentinel; assert strict-fill errors (unknown key, unfilled placeholder), dispatcher-block stripping, override-wins-over-kit template loading, and hat-key vocabulary stability.

**Expected.** Satisfies SR-061 AcceptanceCriteria - briefs redacted by construction (allowlist-only), strict slot fill (provisional home pending the structuring WI)

**Evidence.** `tests/test_plan_briefs.py`
- Verifies (SR): [SR-061](../system-requirements/SR-061.md)
- Verifies (LLR): [LLR-071](../low-level-requirements/LLR-071.md)
