---
type: "Test Case"
title: "verifies SR-138;LLR-156"
description: "Run the migration cases in the process-config suite: a full conversion deletes every legacy file and lands all three ratification dials; a second pass is a no-op; a hand-trimmed process.toml leaves the legacy file in place with its value intact; the F5 translation tables are pinned equal and the retired enum key is absent from the shipped template."
tags: ["Full", "Integration", "Automated=Yes", "Drafted"]
resource: "docs/test/test-cases.toml (TC-152)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-152) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-152 — verifies SR-138;LLR-156

**Method.** Run the migration cases in the process-config suite: a full conversion deletes every legacy file and lands all three ratification dials; a second pass is a no-op; a hand-trimmed process.toml leaves the legacy file in place with its value intact; the F5 translation tables are pinned equal and the retired enum key is absent from the shipped template.

**Expected.** Satisfies SR-138 AcceptanceCriteria

**Evidence.** `tests/test_process_config.py;tests/test_rule_sync.py`
- Verifies (SR): [SR-138](../system-requirements/SR-138.md)
- Verifies (LLR): [LLR-156](../low-level-requirements/LLR-156.md)
