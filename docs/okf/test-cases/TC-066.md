---
type: "Test Case"
title: "verifies SR-065;LLR-066"
description: "Run a parallel session + a downstream-migration fixture; assert reason-coded telemetry aggregated per (train, WI, session) with no id collision, the banner content, the --jobs 1 hold until both audits pass + the recorded two-worker flip, legacy active/tracks reconciliation, and a non-adopting repo left unaffected."
tags: ["Full", "Integration", "Automated=No", "Draft"]
resource: "docs/test/test-cases.csv (TC-066)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-066) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-066 — verifies SR-065;LLR-066

**Method.** Run a parallel session + a downstream-migration fixture; assert reason-coded telemetry aggregated per (train, WI, session) with no id collision, the banner content, the --jobs 1 hold until both audits pass + the recorded two-worker flip, legacy active/tracks reconciliation, and a non-adopting repo left unaffected.

**Expected.** Satisfies SR-065 AcceptanceCriteria
- Verifies (SR): [SR-065](../system-requirements/SR-065.md)
- Verifies (LLR): [LLR-066](../low-level-requirements/LLR-066.md)
