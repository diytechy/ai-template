---
type: "Test Case"
title: "verifies SR-048;LLR-049"
description: "Run the top-view suite: an inventory over 10 modules with no containing CMPs warns plain and fails --strict; right-sized CMP rows that contain the modules clear it; a nested CMP counts only at its root; docs/components-check off silences; a <=10-module and an arch-map-less inventory are vacuous; gen_trajectory renders the containerized <details> top view with expandable members and one deduped cross-component edge per crossing pair, stays flat (byte-identical) without CMP containment, and is byte-stable through --check."
tags: ["Full", "Integration", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.csv (TC-049)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-049) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-049 — verifies SR-048;LLR-049

**Method.** Run the top-view suite: an inventory over 10 modules with no containing CMPs warns plain and fails --strict; right-sized CMP rows that contain the modules clear it; a nested CMP counts only at its root; docs/components-check off silences; a <=10-module and an arch-map-less inventory are vacuous; gen_trajectory renders the containerized <details> top view with expandable members and one deduped cross-component edge per crossing pair, stays flat (byte-identical) without CMP containment, and is byte-stable through --check.

**Expected.** Satisfies SR-048 AcceptanceCriteria

**Evidence.** `tests/test_trajectory.py; tests/test_gen_trajectory.py`
- Verifies (SR): [SR-048](../system-requirements/SR-048.md)
- Verifies (LLR): [LLR-049](../low-level-requirements/LLR-049.md)
