---
type: "Test Case"
title: "verifies SR-066;LLR-074"
description: "Build a fixture repo (work-items.csv with the round's parent WI, a selected plan carrying a fan-in Plan-WI row) under tmp_path: assert DP-NNN allocation sequence (empty->DP-001, existing->DP-002, max+1), stage writes are named UTF-8 files, filing allocates sequential ids from max+1, maps plan-local predecessors incl. the fan-in row and appends the parent WI, honors the tier map (default medium), files queued rows with an empty Deliverable (R-A) and the given SpecRef/Workstream, preserves the CSV's LF, no-ops on a table-less brief, and that the real check_trajectory.py exits 0 (plain and --strict) on the result; log append adds the block and preserves prior content."
tags: ["Smoke", "Integration", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.csv (TC-074)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-074) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-074 — verifies SR-066;LLR-074

**Method.** Build a fixture repo (work-items.csv with the round's parent WI, a selected plan carrying a fan-in Plan-WI row) under tmp_path: assert DP-NNN allocation sequence (empty->DP-001, existing->DP-002, max+1), stage writes are named UTF-8 files, filing allocates sequential ids from max+1, maps plan-local predecessors incl. the fan-in row and appends the parent WI, honors the tier map (default medium), files queued rows with an empty Deliverable (R-A) and the given SpecRef/Workstream, preserves the CSV's LF, no-ops on a table-less brief, and that the real check_trajectory.py exits 0 (plain and --strict) on the result; log append adds the block and preserves prior content.

**Expected.** Satisfies SR-066 AcceptanceCriteria for the dual-plan artifact filer

**Evidence.** `tests/test_plan_artifacts.py`
- Verifies (SR): [SR-066](../system-requirements/SR-066.md)
- Verifies (LLR): [LLR-074](../low-level-requirements/LLR-074.md)
