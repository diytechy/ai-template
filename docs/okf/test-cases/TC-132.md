---
type: "Test Case"
title: "verifies SR-156;LLR-140"
description: "Run the integrator suite: each claim refusal fires by name and the green claim moves the spec and cuts the branch; finished-branch detection keys on the tree; the verdict gate refuses absent, CHANGES-REQUESTED and stale-APPROVE verdicts and passes a fresh APPROVE; a missing or empty bar declaration refuses; the window audit flags a non-merge product commit and passes a bookkeeping-only window; one end-to-end claim->work->close->integrate run fast-forwards the trunk to a --no-ff merge commit."
tags: ["Full", "Unit", "Automated=Yes", "Approved"]
resource: "docs/test/test-cases.toml (TC-132)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-132) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-132 — verifies SR-156;LLR-140

**Method.** Run the integrator suite: each claim refusal fires by name and the green claim moves the spec and cuts the branch; finished-branch detection keys on the tree; the verdict gate refuses absent, CHANGES-REQUESTED and stale-APPROVE verdicts and passes a fresh APPROVE; a missing or empty bar declaration refuses; the window audit flags a non-merge product commit and passes a bookkeeping-only window; one end-to-end claim->work->close->integrate run fast-forwards the trunk to a --no-ff merge commit.

**Expected.** Satisfies SR-156 AcceptanceCriteria via the LLR-140 detail contract

**Evidence.** `tests/test_integrate.py`
- Verifies (SR): [SR-156](../system-requirements/SR-156.md)
- Verifies (LLR): [LLR-140](../low-level-requirements/LLR-140.md)
