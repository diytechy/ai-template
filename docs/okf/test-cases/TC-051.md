---
type: "Test Case"
title: "verifies SR-050;LLR-051"
description: "Run the dashboard suite; the Process tab renders its three panels from the live registries + docs/gate (the current-gate highlight matches docs/gate and the tier counts match the spine), process-doc link-outs resolve, a bundle-/data-less repo renders byte-identically (no Process data joins), and generation stays byte-deterministic with --check tripping stale output."
tags: ["Full", "Integration", "Automated=Yes", "Planned"]
resource: "docs/test/test-cases.csv (TC-051)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-051) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-051 — verifies SR-050;LLR-051

**Method.** Run the dashboard suite; the Process tab renders its three panels from the live registries + docs/gate (the current-gate highlight matches docs/gate and the tier counts match the spine), process-doc link-outs resolve, a bundle-/data-less repo renders byte-identically (no Process data joins), and generation stays byte-deterministic with --check tripping stale output.

**Expected.** Process tab present with data-derived joins correct; links resolve; data-less render byte-identical; deterministic; --check trips.

**Evidence.** `tests/test_gen_trajectory.py (planned; node paths pinned at WI-085 implementation)`
- Verifies (SR): [SR-050](../system-requirements/SR-050.md)
- Verifies (LLR): [LLR-051](../low-level-requirements/LLR-051.md)
