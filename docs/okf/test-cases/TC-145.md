---
type: "Test Case"
title: "verifies SR-156;LLR-151"
description: "Run the integrator claim suite: a spine claim succeeds on an idle station (the deleted safety-class arm, red-then-green); a hand claim while another holder has the dispatch lock refuses naming the lock and leaves the queue untouched, while dispatch_lock_held=True proceeds; a spine batch claims as ONE bookkeeping commit whose subject joins the ids and whose specs all land in active/<branch>/; every pre-existing single-WI claim contract (abandoned-claim re-cut, relink oracle, minted-id rung, RULING-6 audit) holds unchanged."
tags: ["Full", "Unit", "Automated=Yes", "Approved"]
resource: "docs/test/test-cases.toml (TC-145)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-145) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-145 — verifies SR-156;LLR-151

**Method.** Run the integrator claim suite: a spine claim succeeds on an idle station (the deleted safety-class arm, red-then-green); a hand claim while another holder has the dispatch lock refuses naming the lock and leaves the queue untouched, while dispatch_lock_held=True proceeds; a spine batch claims as ONE bookkeeping commit whose subject joins the ids and whose specs all land in active/<branch>/; every pre-existing single-WI claim contract (abandoned-claim re-cut, relink oracle, minted-id rung, RULING-6 audit) holds unchanged.

**Expected.** Satisfies SR-156 AcceptanceCriteria via the LLR-151 detail contract

**Evidence.** `tests/test_integrate.py`
- Verifies (SR): [SR-156](../system-requirements/SR-156.md)
- Verifies (LLR): [LLR-151](../low-level-requirements/LLR-151.md)
