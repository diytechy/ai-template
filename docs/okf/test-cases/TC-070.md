---
type: "Test Case"
title: "verifies SR-061;LLR-070"
description: "Drive plan_round with injected fake step results: the happy path selects with the 8-session spend; ready_steps offers parallel pairs; APPROVE-only critiques skip revision and coverage2; coverage findings bounce to the implicated plans once and page on repeat; a second critique / an unearned or second revision / a third arbiter run / recording into a closed round raise RoundCapError; arbiter disagreement pages position-unstable; budget exhaustion pages; page_action maps the three gate policies and fails safe; state survives a JSON round-trip; the walk CLI prints the happy path."
tags: ["Smoke", "Unit", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.csv (TC-070)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-070) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-070 — verifies SR-061;LLR-070

**Method.** Drive plan_round with injected fake step results: the happy path selects with the 8-session spend; ready_steps offers parallel pairs; APPROVE-only critiques skip revision and coverage2; coverage findings bounce to the implicated plans once and page on repeat; a second critique / an unearned or second revision / a third arbiter run / recording into a closed round raise RoundCapError; arbiter disagreement pages position-unstable; budget exhaustion pages; page_action maps the three gate policies and fails safe; state survives a JSON round-trip; the walk CLI prints the happy path.

**Expected.** Satisfies SR-061 AcceptanceCriteria for the dual-plan round sublibrary (provisional home pending the structuring WI)

**Evidence.** `tests/test_plan_round.py`
- Verifies (SR): [SR-061](../system-requirements/SR-061.md)
- Verifies (LLR): [LLR-070](../low-level-requirements/LLR-070.md)
