---
type: "Test Case"
title: "verifies SR-137;LLR-155"
description: "Run the process-config suite: it sources the SHIPPED hook's own sh functions and drives them against `tomllib` over a table of adversarial file shapes (trailing comments, dotted keys, inline tables, uppercase TRUE, BOM, CRLF, wrong section), asserts the two agree on every shape and that an unparseable file reads ON in both, and drives the mixed-config refusal and the wrong-typed-dial refusal."
tags: ["Full", "Integration", "Automated=Yes", "Draft"]
resource: "docs/test/test-cases.toml (TC-150)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-150) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-150 — verifies SR-137;LLR-155

**Method.** Run the process-config suite: it sources the SHIPPED hook's own sh functions and drives them against `tomllib` over a table of adversarial file shapes (trailing comments, dotted keys, inline tables, uppercase TRUE, BOM, CRLF, wrong section), asserts the two agree on every shape and that an unparseable file reads ON in both, and drives the mixed-config refusal and the wrong-typed-dial refusal.

**Expected.** Satisfies SR-137 AcceptanceCriteria

**Evidence.** `tests/test_process_config.py`
- Verifies (SR): [SR-137](../system-requirements/SR-137.md)
- Verifies (LLR): [LLR-155](../low-level-requirements/LLR-155.md)
