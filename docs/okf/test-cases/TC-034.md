---
type: "Test Case"
title: "verifies SR-034"
description: "Run the dependency-ledger suite; an AST scan asserts every kit script's top-level imports resolve to stdlib, a local sibling, or a Kind=python row in docs/dependencies.md, and the scanner is driven over a fixture tree with a genuinely undeclared import to prove it catches one."
tags: ["Full", "Analysis", "Automated=Yes", "Modified"]
resource: "docs/test/test-cases.csv (TC-034)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-034) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-034 — verifies SR-034

**Method.** Run the dependency-ledger suite; an AST scan asserts every kit script's top-level imports resolve to stdlib, a local sibling, or a Kind=python row in docs/dependencies.md, and the scanner is driven over a fixture tree with a genuinely undeclared import to prove it catches one.

**Expected.** Satisfies SR-034 AcceptanceCriteria

**Evidence.** `tests/test_dependency_ledger.py`
- Verifies (SR): [SR-034](../system-requirements/SR-034.md)
