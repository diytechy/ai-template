---
type: "Test Case"
title: "verifies SR-147;LLR-165"
description: "Run the carrier-converter suite: it converts the live spine, re-parses the emitted TOML with tomllib and asserts the round-trip is cell-for-cell identical to the source for every row of every tier, then DRIVES the loss detector against corruption classes (a dropped row, a dropped field, a truncated value, a re-typed value, a mangled ref list) and asserts each is REFUSED with a finding naming the row and field, so the clean result is evidence rather than a check that cannot fail."
tags: ["Smoke", "Integration", "Automated=Yes", "Approved"]
resource: "docs/test/test-cases.toml (TC-159)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-159) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-159 — verifies SR-147;LLR-165

**Method.** Run the carrier-converter suite: it converts the live spine, re-parses the emitted TOML with tomllib and asserts the round-trip is cell-for-cell identical to the source for every row of every tier, then DRIVES the loss detector against corruption classes (a dropped row, a dropped field, a truncated value, a re-typed value, a mangled ref list) and asserts each is REFUSED with a finding naming the row and field, so the clean result is evidence rather than a check that cannot fail.

**Expected.** Satisfies SR-147 AcceptanceCriteria

**Evidence.** `tests/test_migrate_carrier.py`
- Verifies (SR): [SR-147](../system-requirements/SR-147.md)
- Verifies (LLR): [LLR-165](../low-level-requirements/LLR-165.md)
