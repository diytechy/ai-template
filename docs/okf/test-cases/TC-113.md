---
type: "Test Case"
title: "verifies SR-052;LLR-108"
description: "Over the shipped dashboard plus seven fixture renders, collect every focusable element (tabindex=0, button) and role=img graphic with its accessible name (aria-label, else the first child <title>, else text content) and assert: none is empty, none is a bare registry id. Separately assert navigation landmarks in one document carry distinct names."
tags: ["Full", "Unit", "Automated=Yes", "Approved"]
resource: "docs/test/test-cases.toml (TC-113)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-113) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-113 — verifies SR-052;LLR-108

**Method.** Over the shipped dashboard plus seven fixture renders, collect every focusable element (tabindex=0, button) and role=img graphic with its accessible name (aria-label, else the first child <title>, else text content) and assert: none is empty, none is a bare registry id. Separately assert navigation landmarks in one document carry distinct names.

**Expected.** Every control is named, no name is a bare id, and no two navigation landmarks share a name.

**Evidence.** `tests/test_traj_render.py::test_a2_every_control_name_is_present_and_not_a_bare_id tests/test_traj_render.py::test_a2_landmark_names_are_distinct`
- Verifies (SR): [SR-052](../system-requirements/SR-052.md)
- Verifies (LLR): [LLR-108](../low-level-requirements/LLR-108.md)
