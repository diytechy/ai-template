---
type: "Test Case"
title: "verifies SR-145;LLR-168"
description: "Run the planner twice over one snapshot, then over snapshots satisfying exactly one precedence rung each."
tags: ["Full", "Unit", "Automated=Yes", "Planned"]
resource: "docs/test/test-cases.csv (TC-162)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-162) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-162 — verifies SR-145;LLR-168

**Method.** Run the planner twice over one snapshot, then over snapshots satisfying exactly one precedence rung each.

**Expected.** Two runs return one decision; each single-rung snapshot selects its own rung; no lower rung is selected while a higher rung has work.

**Evidence.** `tests/test_resume_plan.py`
- Verifies (SR): [SR-145](../system-requirements/SR-145.md)
- Verifies (LLR): [LLR-168](../low-level-requirements/LLR-168.md)
