---
type: "Test Case"
title: "verifies SR-171;LLR-174"
description: "Run the agent-loop rate-limit cases against the fake-agent CLI: a throttled session exits WAITING (5) with the banner naming the parsed reset time and the WAITING outcome in the iteration log - never a stall abort, even at stall-limit 1; with --wait-on-limit set and an unparseable reset wording, the loop sleeps the declared --limit-retry-fallback (capped at the ceiling), prints the wait, and retries so the run continues and finishes; a healthy transcript that merely mentions limits reads COMMITTED, never WAITING; an error result that is no rate limit reads ERROR rather than being retried as a throttle; the reset-clock parser is unit-covered on both am/pm and 24-hour wordings."
tags: ["Full", "Integration", "Automated=Yes", "Drafted"]
resource: "docs/test/test-cases.toml (TC-168)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-168) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-168 — verifies SR-171;LLR-174

**Method.** Run the agent-loop rate-limit cases against the fake-agent CLI: a throttled session exits WAITING (5) with the banner naming the parsed reset time and the WAITING outcome in the iteration log - never a stall abort, even at stall-limit 1; with --wait-on-limit set and an unparseable reset wording, the loop sleeps the declared --limit-retry-fallback (capped at the ceiling), prints the wait, and retries so the run continues and finishes; a healthy transcript that merely mentions limits reads COMMITTED, never WAITING; an error result that is no rate limit reads ERROR rather than being retried as a throttle; the reset-clock parser is unit-covered on both am/pm and 24-hour wordings.

**Expected.** Satisfies SR-171 AcceptanceCriteria via the LLR-174 detail contract

**Evidence.** `tests/test_agent_loop.py::test_limit_hit_backs_off_without_counting_stall; tests/test_agent_loop.py::test_unparseable_reset_falls_back_and_retries; tests/test_agent_loop.py::test_healthy_transcript_mentioning_limits_is_not_a_throttle; tests/test_agent_loop.py::test_error_session_reads_error_not_no_commit; tests/test_agent_loop_policy.py::test_seconds_until_reset_parses_both_clock_formats`
- Verifies (SR): [SR-171](../system-requirements/SR-171.md)
- Verifies (LLR): [LLR-174](../low-level-requirements/LLR-174.md)
