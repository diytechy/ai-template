---
type: "Test Case"
title: "verifies SR-112;LLR-043"
description: "Run the skills-sync suite: a hand-edited per-agent copy fails gen_skills_index --check-agents, bootstrap --sync restores byte-identity, a repo with no per-agent dir (and a scaffold with no neutral source) is vacuous, bootstrap --agents codex populates .agents/skills from source, and --sync never touches a non-skills file (a sentinel survives)."
tags: ["Full", "Integration", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.csv (TC-045)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-045) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-045 — verifies SR-112;LLR-043

**Method.** Run the skills-sync suite: a hand-edited per-agent copy fails gen_skills_index --check-agents, bootstrap --sync restores byte-identity, a repo with no per-agent dir (and a scaffold with no neutral source) is vacuous, bootstrap --agents codex populates .agents/skills from source, and --sync never touches a non-skills file (a sentinel survives).

**Expected.** Satisfies SR-112 AcceptanceCriteria (checked per-agent fan-out)

**Evidence.** `tests/test_skills_sync.py::test_drifted_copy_fails_and_sync_restores`
- Verifies (SR): [SR-112](../system-requirements/SR-112.md)
- Verifies (LLR): [LLR-043](../low-level-requirements/LLR-043.md)
