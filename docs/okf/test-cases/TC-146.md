---
type: "Test Case"
title: "verifies SR-093;LLR-152"
description: "Run the schedule + integrate suites' adjudication cases: the kind classifies exclusive at rank 1 (the reserved slot, pinned by name); an adjudication-only lane's refresh runs NO bar yet still commits a verified Bar-Green attestation whose summary says no-bar, while a mixed batch bars as usual (fail toward the bar); bar = G2 reaches check.py as --gate G2 (asserted off the recording stub's own argv), an undeclared bar passes no --gate, and a malformed value refuses the refresh naming it."
tags: ["Full", "Unit", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.toml (TC-146)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-146) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-146 — verifies SR-093;LLR-152

**Method.** Run the schedule + integrate suites' adjudication cases: the kind classifies exclusive at rank 1 (the reserved slot, pinned by name); an adjudication-only lane's refresh runs NO bar yet still commits a verified Bar-Green attestation whose summary says no-bar, while a mixed batch bars as usual (fail toward the bar); bar = G2 reaches check.py as --gate G2 (asserted off the recording stub's own argv), an undeclared bar passes no --gate, and a malformed value refuses the refresh naming it.

**Expected.** Satisfies SR-093 AcceptanceCriteria

**Evidence.** `tests/test_schedule.py; tests/test_integrate.py`
- Verifies (SR): [SR-093](../system-requirements/SR-093.md)
- Verifies (LLR): [LLR-152](../low-level-requirements/LLR-152.md)
