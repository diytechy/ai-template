---
type: "Test Case"
title: "verifies SR-044;LLR-041;LLR-042"
description: "Run the interface-connectivity suite: trace.py flags a malformed/duplicate IF id and an empty or unresolvable IF SR-Ref under --strict while a clean seam passes and the endpoint advisory is warn-only; a multi-module arch-map with an empty or absent registry warns connectivity-undeclared and docs/interfaces-check off silences it; a single-module inventory is vacuous; a source/sink Notes marker suppresses the missing-direction warn; an Active seam uncited by any TC warns; a Contracts docstring lands in the arch-map and a dangling one warns; gen_trajectory renders the IF graph deterministically; a legacy interfaces.csv without the new columns still parses; an IF row's Component tag resolving to a phantom CMP fails --strict while a resolving one passes (WI-064)."
tags: ["Full", "Integration", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.csv (TC-044)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-044) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-044 — verifies SR-044;LLR-041;LLR-042

**Method.** Run the interface-connectivity suite: trace.py flags a malformed/duplicate IF id and an empty or unresolvable IF SR-Ref under --strict while a clean seam passes and the endpoint advisory is warn-only; a multi-module arch-map with an empty or absent registry warns connectivity-undeclared and docs/interfaces-check off silences it; a single-module inventory is vacuous; a source/sink Notes marker suppresses the missing-direction warn; an Active seam uncited by any TC warns; a Contracts docstring lands in the arch-map and a dangling one warns; gen_trajectory renders the IF graph deterministically; a legacy interfaces.csv without the new columns still parses; an IF row's Component tag resolving to a phantom CMP fails --strict while a resolving one passes (WI-064).

**Expected.** Satisfies SR-044 AcceptanceCriteria

**Evidence.** `tests/test_trace.py::test_if_tier_integrity; tests/test_trajectory.py::test_interface_coverage_warns; tests/test_gen_trajectory.py::test_how_sw_graph_renders_seams; tests/test_gen_arch_map.py::test_contracts_and_if_edges; tests/test_components_registry.py::test_if_component_tag_must_resolve`
- Verifies (SR): [SR-044](../system-requirements/SR-044.md)
- Verifies (LLR): [LLR-041](../low-level-requirements/LLR-041.md) · [LLR-042](../low-level-requirements/LLR-042.md)
