---
type: "Test Case"
title: "verifies SR-156;LLR-144"
description: "Run the handback suite: a partial close moves each claimed spec to the terminal partial/ and writes its immutable per-close report under docs/handbacks/, leaves the branch finished, is CLEAN under the real trajectory check (R-A exempts partial \u2014 the spec's definition is deliberately byte-identical), refuses a second close without residue, and is LOUD when a terminal spec has no report; an already-declared blockref and an existing Deliverable survive; a branch trunk holds no claim for is refused. The quarantine arm reverts the product paths and keeps the failing diff as a bar-inert .patch, reverts a RENAME correctly (the --name-status -z stream read as records, not pairs - mutation-proven), leaves the handback bookkeeping alone, refuses when the red is not the lane's own code, and restores rather than reports when a revert step fails."
tags: ["Full", "Unit", "Automated=Yes", "Modified"]
resource: "docs/test/test-cases.toml (TC-138)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-138) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-138 — verifies SR-156;LLR-144

**Method.** Run the handback suite: a partial close moves each claimed spec to the terminal partial/ and writes its immutable per-close report under docs/handbacks/, leaves the branch finished, is CLEAN under the real trajectory check (R-A exempts partial — the spec's definition is deliberately byte-identical), refuses a second close without residue, and is LOUD when a terminal spec has no report; an already-declared blockref and an existing Deliverable survive; a branch trunk holds no claim for is refused. The quarantine arm reverts the product paths and keeps the failing diff as a bar-inert .patch, reverts a RENAME correctly (the --name-status -z stream read as records, not pairs - mutation-proven), leaves the handback bookkeeping alone, refuses when the red is not the lane's own code, and restores rather than reports when a revert step fails.

**Expected.** Satisfies SR-156 AcceptanceCriteria via the LLR-144 detail contract

**Evidence.** `tests/test_handback.py`
- Verifies (SR): [SR-156](../system-requirements/SR-156.md)
- Verifies (LLR): [LLR-144](../low-level-requirements/LLR-144.md)
