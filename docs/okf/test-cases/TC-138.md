---
type: "Test Case"
title: "verifies SR-132;LLR-144"
description: "Run the handback suite: a handback returns each claimed spec to queued/ with a ## Handback section, its commit range and a blockref to its own path, leaves the branch finished and drops the row off the ready frontier while all three F5 readers still parse it; an already-declared blockref and an existing Deliverable survive; a branch trunk holds no claim for is refused. The quarantine arm reverts the product paths and keeps the failing diff as a bar-inert .patch, reverts a RENAME correctly (the --name-status -z stream read as records, not pairs - mutation-proven), leaves the handback bookkeeping alone, refuses when the red is not the lane's own code, and restores rather than reports when a revert step fails."
tags: ["Full", "Unit", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.csv (TC-138)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-138) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-138 — verifies SR-132;LLR-144

**Method.** Run the handback suite: a handback returns each claimed spec to queued/ with a ## Handback section, its commit range and a blockref to its own path, leaves the branch finished and drops the row off the ready frontier while all three F5 readers still parse it; an already-declared blockref and an existing Deliverable survive; a branch trunk holds no claim for is refused. The quarantine arm reverts the product paths and keeps the failing diff as a bar-inert .patch, reverts a RENAME correctly (the --name-status -z stream read as records, not pairs - mutation-proven), leaves the handback bookkeeping alone, refuses when the red is not the lane's own code, and restores rather than reports when a revert step fails.

**Expected.** Satisfies SR-132 AcceptanceCriteria

**Evidence.** `tests/test_handback.py`
- Verifies (SR): [SR-132](../system-requirements/SR-132.md)
- Verifies (LLR): [LLR-144](../low-level-requirements/LLR-144.md)
