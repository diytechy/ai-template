---
type: "Test Case"
title: "verifies SR-044;LLR-068"
description: "Run the spec-interface suite: an unarmed spec (no ## Interfaces section) is vacuous; a section citing a resolvable IF passes; an unresolvable IF-### warns plain (exit 0) and errors under --strict (exit 1); a Proposed citation with no rationale warns while a nearest-IF rationale clears it; an armed section citing no IF and no intra-module escape warns; the intra-module escape passes; the specs/ README and the -000 example are not armed."
tags: ["Smoke", "Integration", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.csv (TC-068)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-068) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-068 — verifies SR-044;LLR-068

**Method.** Run the spec-interface suite: an unarmed spec (no ## Interfaces section) is vacuous; a section citing a resolvable IF passes; an unresolvable IF-### warns plain (exit 0) and errors under --strict (exit 1); a Proposed citation with no rationale warns while a nearest-IF rationale clears it; an armed section citing no IF and no intra-module escape warns; the intra-module escape passes; the specs/ README and the -000 example are not armed.

**Expected.** Satisfies SR-044 AcceptanceCriteria

**Evidence.** `tests/test_trajectory.py::test_spec_interfaces_unarmed_is_vacuous; tests/test_trajectory.py::test_spec_interfaces_resolvable_passes; tests/test_trajectory.py::test_spec_interfaces_unresolvable_warns_then_errors_under_strict; tests/test_trajectory.py::test_spec_interfaces_proposed_needs_rationale; tests/test_trajectory.py::test_spec_interfaces_empty_section_warns; tests/test_trajectory.py::test_spec_interfaces_intra_module_escape_passes; tests/test_trajectory.py::test_spec_interfaces_readme_and_example_not_armed`
- Verifies (SR): [SR-044](../system-requirements/SR-044.md)
- Verifies (LLR): [LLR-068](../low-level-requirements/LLR-068.md)
