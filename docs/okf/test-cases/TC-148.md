---
type: "Test Case"
title: "verifies SR-170;LLR-154;IF-090"
description: "Run the integrator + dispatcher suites' intake-arm cases: a merged branch that amended a ratified SR cell yields the adjudication row as its own mint: commit immediately after the merge commit on trunk (in-slot, serial); the dispatcher's empty-frontier rung 1 mints gap rows and KEEPS DRIVING them in the same run, then exits honestly when every gap already carries a row; a census-mint refusal stops the run loudly; the handback loop tests drive the disposition row the same run mints."
tags: ["Full", "Integration", "Automated=Yes", "Modified"]
resource: "docs/test/test-cases.toml (TC-148)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-148) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-148 — verifies SR-170;LLR-154;IF-090

**Method.** Run the integrator + dispatcher suites' intake-arm cases: a merged branch that amended a ratified SR cell yields the adjudication row as its own mint: commit immediately after the merge commit on trunk (in-slot, serial); the dispatcher's empty-frontier rung 1 mints gap rows and KEEPS DRIVING them in the same run, then exits honestly when every gap already carries a row; a census-mint refusal stops the run loudly; the handback loop tests drive the disposition row the same run mints.

**Expected.** Satisfies SR-156 AcceptanceCriteria via the LLR-154 detail contract

**Evidence.** `tests/test_integrate.py; tests/test_dispatch.py`
- Verifies (SR): [SR-170](../system-requirements/SR-170.md)
- Verifies (LLR): [LLR-154](../low-level-requirements/LLR-154.md)
