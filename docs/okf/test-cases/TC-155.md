---
type: "Test Case"
title: "verifies SR-143;LLR-160"
description: "Run the queue-conflict cases: each of the three signals fires with both ids in deterministic order, a closed row never conflicts, unrelated open rows and a shared stopword are silent, and the findings are wired on the warn-only line rather than into the exit code."
tags: ["Smoke", "Unit", "Automated=Yes", "Draft"]
resource: "docs/test/test-cases.toml (TC-155)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-155) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-155 — verifies SR-143;LLR-160

**Method.** Run the queue-conflict cases: each of the three signals fires with both ids in deterministic order, a closed row never conflicts, unrelated open rows and a shared stopword are silent, and the findings are wired on the warn-only line rather than into the exit code.

**Expected.** Satisfies SR-143 AcceptanceCriteria

**Evidence.** `tests/test_loop_order.py`
- Verifies (SR): [SR-143](../system-requirements/SR-143.md)
- Verifies (LLR): [LLR-160](../low-level-requirements/LLR-160.md)
