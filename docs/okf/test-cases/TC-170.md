---
type: "Test Case"
title: "verifies SR-173;LLR-142"
description: "Run the trunk-step regen order/commit cases: the executed step sequence follows the declared dependency table - arch-map before okf, derived-gate before its two consumers (trajectory, status), open-items last - asserted on the printed per-step lines of a real run rather than on the table constant; a regen run creates NO git commit, a green step's output staying uncommitted in the working tree for the caller's one serial commit; and the failure path is executed, not inferred - a run whose later step fails after green steps have dirtied the tree exits nonzero with HEAD unmoved and the green output still uncommitted, so a later step's failure can never leave a partially regenerated set in history."
tags: ["Smoke", "Unit", "Automated=Yes", "Drafted"]
resource: "docs/test/test-cases.toml (TC-170)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-170) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-170 — verifies SR-173;LLR-142

**Method.** Run the trunk-step regen order/commit cases: the executed step sequence follows the declared dependency table - arch-map before okf, derived-gate before its two consumers (trajectory, status), open-items last - asserted on the printed per-step lines of a real run rather than on the table constant; a regen run creates NO git commit, a green step's output staying uncommitted in the working tree for the caller's one serial commit; and the failure path is executed, not inferred - a run whose later step fails after green steps have dirtied the tree exits nonzero with HEAD unmoved and the green output still uncommitted, so a later step's failure can never leave a partially regenerated set in history.

**Expected.** Satisfies SR-173 AcceptanceCriteria via the LLR-142 detail contract

**Evidence.** `tests/test_trunk_step.py::test_regen_runs_in_declared_dependency_order; tests/test_trunk_step.py::test_regen_never_commits_the_caller_owns_the_commit; tests/test_trunk_step.py::test_regen_failure_after_green_steps_commits_nothing`
- Verifies (SR): [SR-173](../system-requirements/SR-173.md)
- Verifies (LLR): [LLR-142](../low-level-requirements/LLR-142.md)
