---
type: "Test Case"
title: "verifies SR-051;LLR-052"
description: "Run the dashboard suite; with fixture registries exceeding the tier thresholds the When/How-SW views render seams wired to endpoint block input/output ports (a cross-container seam aggregated to the container boundary), a double-click (or its keyboard alternative) on a container descends one layer and the breadcrumb restores the prior view, tiering still fires only above the >3 thresholds and composes with TOP_VIEW_MAX, each aggregated parent edge equals the deduped union of its children crossing edges, a data-less repo renders byte-identically, and rendering is byte-deterministic with --check tripping stale output."
tags: ["Full", "Integration", "Automated=Yes", "Verified"]
resource: "docs/test/test-cases.csv (TC-052)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-052) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-052 — verifies SR-051;LLR-052

**Method.** Run the dashboard suite; with fixture registries exceeding the tier thresholds the When/How-SW views render seams wired to endpoint block input/output ports (a cross-container seam aggregated to the container boundary), a double-click (or its keyboard alternative) on a container descends one layer and the breadcrumb restores the prior view, tiering still fires only above the >3 thresholds and composes with TOP_VIEW_MAX, each aggregated parent edge equals the deduped union of its children crossing edges, a data-less repo renders byte-identically, and rendering is byte-deterministic with --check tripping stale output.

**Expected.** Seams attach to block ports at the visible tier and aggregate at container boundaries; descend-a-layer + breadcrumb navigation works with a keyboard alternative; tiering + TOP_VIEW_MAX unchanged; aggregated edges equal the child-edge union; data-less render byte-identical; deterministic; --check trips.

**Evidence.** `tests/test_gen_trajectory.py::test_when_view_seams_wire_to_block_ports ::test_when_view_double_click_descends_a_layer ::test_when_view_breadcrumb_restores_parent (+ the WI-087 tiering nodes re-verified at the WI-141 dev slice)`
- Verifies (SR): [SR-051](../system-requirements/SR-051.md)
- Verifies (LLR): [LLR-052](../low-level-requirements/LLR-052.md)
