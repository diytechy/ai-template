---
type: "Test Case"
title: "verifies SR-051"
description: "Run the dashboard suite; with fixture registries exceeding 3 phases / workstreams / components the When view starts at phase blocks exploding to workstream then work-item tiers and the How-SW view starts at component blocks (a flat render at or below each threshold), each aggregated parent edge equals the deduped union of its children crossing edges, the phase encoding is present, and rendering is byte-deterministic with --check tripping stale output."
tags: ["Full", "Integration", "Automated=Yes", "Planned"]
resource: "docs/test/test-cases.csv (TC-052)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.csv (TC-052) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# TC-052 — verifies SR-051

**Method.** Run the dashboard suite; with fixture registries exceeding 3 phases / workstreams / components the When view starts at phase blocks exploding to workstream then work-item tiers and the How-SW view starts at component blocks (a flat render at or below each threshold), each aggregated parent edge equals the deduped union of its children crossing edges, the phase encoding is present, and rendering is byte-deterministic with --check tripping stale output.

**Expected.** Tiering fires only above the > 3 thresholds and composes with TOP_VIEW_MAX; aggregated edges equal the child-edge union; deterministic; --check trips.
- Verifies (SR): [SR-051](../system-requirements/SR-051.md)
