---
type: "Test Case"
title: "verifies SR-049;LLR-147"
description: "Fixture-drive the SN-coverage rung: an uncovered ratified SN caps raw at DevBar-Below (the runnable value floors to DevBar-Reqs) with uncovered=1 on the basis line and drafts=0; citing the need from an SR's SN-Refs restores the prior level (uncovered=0, DevBar-Release); a Draft SN keeps riding the draft rung with uncovered=0 (the double-counting seam - one fact, one rung); -000 rows on both sides of the join are ignored, so an example SR row cannot fake coverage of a real need; the ex-draft counterfactual rebuilds the cited set over the non-draft subset, so a need answered only by a Draft SR reads DevBar-Below there too; and the sn_cited_ids parse is pinned equal between trace.py and derive_gate.py across the separator/empty/absent-cell battery (tests/test_rule_sync.py)."
tags: ["Full", "Unit", "Automated=Yes", "Approved"]
resource: "docs/test/test-cases.toml (TC-141)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/test/test-cases.toml (TC-141) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# TC-141 — verifies SR-049;LLR-147

**Method.** Fixture-drive the SN-coverage rung: an uncovered ratified SN caps raw at DevBar-Below (the runnable value floors to DevBar-Reqs) with uncovered=1 on the basis line and drafts=0; citing the need from an SR's SN-Refs restores the prior level (uncovered=0, DevBar-Release); a Draft SN keeps riding the draft rung with uncovered=0 (the double-counting seam - one fact, one rung); -000 rows on both sides of the join are ignored, so an example SR row cannot fake coverage of a real need; the ex-draft counterfactual rebuilds the cited set over the non-draft subset, so a need answered only by a Draft SR reads DevBar-Below there too; and the sn_cited_ids parse is pinned equal between trace.py and derive_gate.py across the separator/empty/absent-cell battery (tests/test_rule_sync.py).

**Expected.** Satisfies SR-049 AcceptanceCriteria

**Evidence.** `tests/test_derive_gate.py`
- Verifies (SR): [SR-049](../system-requirements/SR-049.md)
- Verifies (LLR): [LLR-147](../low-level-requirements/LLR-147.md)
