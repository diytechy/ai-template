---
type: "System Requirement"
title: "Performance-budget breaches red their hard-gated rows"
description: "check_perf.py shall exit nonzero exactly when an in-tier Gate=fail performance-budget row breaches \u2014 its measured metric worse than its absolute Budget, or regressed beyond its declared Tolerance band around the committed baseline \u2014 while Gate=warn rows and out-of-tier rows only warn, and a metric with no measurement this run is a reported skip, never a silent pass."
tags: ["S", "Test", "Drafted", "perf"]
resource: "docs/requirements/system-requirements.toml (SR-167)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/system-requirements.toml (SR-167) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# SR-167 — Performance-budget breaches red their hard-gated rows

**Requirement.** check_perf.py shall exit nonzero exactly when an in-tier Gate=fail performance-budget row breaches — its measured metric worse than its absolute Budget, or regressed beyond its declared Tolerance band around the committed baseline — while Gate=warn rows and out-of-tier rows only warn, and a metric with no measurement this run is a reported skip, never a silent pass.

**Acceptance criteria.** An in-tier Gate=fail PB row whose measured metric (docs/test/perf-metrics.json) breaches its absolute Budget, or falls outside its declared Tolerance band around the committed baseline (docs/test/perf-baseline.json), makes check_perf.py --tier <t> exit nonzero naming the row; the same breach on a Gate=warn row, or on a row outside the run tier, exits zero and warns; a Gate=fail row with no measurement this run is a per-row reported skip, never silent; a row with no baseline or no tolerance cannot fail on the regression arm while its absolute arm still applies; --update-baseline is the reviewed acceptance path for an intended regression. Evidence, virtualized against a bootstrapped scaffold: tests/test_check_perf.py (both breach arms, the warn arms, tier scoping, malformed values, and the harness wiring at DevBar-Release via test_harness_runs_perf_at_g3).

**Rationale.** Realizes SN-008 (a reader can believe a green — a breach on a row the project chose to hard-gate must red the run, not ride as a tracked number) and SN-012 (the perf layer is opt-in; a repo declaring no PB rows pays nothing, while one that does gets the same honest-gate discipline as every other check). Minted at the owner's 2026-08-15 L2 ruling at the sitting sweep: the second read's L2 and adversarial F6 both found the tolerance verdict fully built (check_perf.py evaluate, the exit contract stated in the module docstring), fully decomposed (LLR-014 "Budget comparator", whose detail states it verbatim) and fully proven virtualized (TC-014 -> tests/test_check_perf.py: test_regression_beyond_tolerance_fails_when_gate_fail end-to-end against a bootstrapped scaffold, test_regression_uses_direction_and_tolerance at unit level, test_within_tolerance_regression_passes the passing arm, test_absolute_breach_fails_when_gate_fail the absolute arm) — yet stated by no SR: LLR-014 rode SR-006 (the step plan and missing-tool refusal), IF-004/IF-031 rode SR-006 + SR-015 (PB back-link resolvability), and SR-033 states only the warn-tier, never-fails side, so the verdict the interfaces' contracts describe had no requirement home (F6: "their contract has no home to point at"). One row for BOTH breach arms deliberately — absolute and regression are one delivered exit contract, the SR-157 one-verdict shape, and the second read's regression framing was the symptom, not the boundary. Vacuous in THIS repository — no performance-budgets.csv exists here and §9's perf layer is declared absent — but the template ships the layer, and the owner's ruling is that a shipped layer owes a stated, tested obligation even where locally unused. IF-004/IF-031 req_refs gain this row in the same act (off-spine, approval drafted); LLR-014.sr_refs and TC-014.verifies are deliberately NOT re-pointed here — both rows are Approved, so amending their cells overrides attestation, which is the sitting's act: when taken, LLR-014 adds this row beside SR-006 and TC-014 verifies it beside LLR-014.
- Realizes: [SN-008](../stakeholder-needs/SN-008.md) · [SN-012](../stakeholder-needs/SN-012.md)
