---
type: "System Requirement"
title: "Crash safety and git-as-authority recovery"
description: "out/dispatch/ shall be a rebuildable journal/cache, never the sole record: every startup shall acquire the dispatcher lock and reconstruct one ownership/state record per WI and train from Git alone - the authoritative integrated disposition from refs/heads/llm/integration (the development branch is its published projection, not the recovery authority), plus llm/train/*, llm/integrate/*, refs/llm/reservations/*, worktrees, reservation commits, and exact-head review records - quarantining ambiguity/duplicate reservations and failing closed on unprovable ownership before deriving new work; recovery shall handle a crash at every lifecycle boundary (including the reservation transaction, both CAS points, and the worktree sync) without double-assignment, lost commits, false completion, or half-integrated root, and shall reconstruct even with all of out/dispatch/ deleted."
tags: ["M", "Test", "Draft", "Parallel dispatch"]
resource: "docs/requirements/system-requirements.csv (SR-064)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/system-requirements.csv (SR-064) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# SR-064 — Crash safety and git-as-authority recovery

**Requirement.** out/dispatch/ shall be a rebuildable journal/cache, never the sole record: every startup shall acquire the dispatcher lock and reconstruct one ownership/state record per WI and train from Git alone - the authoritative integrated disposition from refs/heads/llm/integration (the development branch is its published projection, not the recovery authority), plus llm/train/*, llm/integrate/*, refs/llm/reservations/*, worktrees, reservation commits, and exact-head review records - quarantining ambiguity/duplicate reservations and failing closed on unprovable ownership before deriving new work; recovery shall handle a crash at every lifecycle boundary (including the reservation transaction, both CAS points, and the worktree sync) without double-assignment, lost commits, false completion, or half-integrated root, and shall reconstruct even with all of out/dispatch/ deleted.

**Acceptance criteria.** Injecting termination at each lifecycle boundary reconstructs exactly one owner, double-runs/falsely-completes nothing, deletes no unintegrated commit/dirty tree, and leaves root entirely before or after integration; every constituent reservation reconstructs from refs/llm/reservations/* including before its first commit; deleting all of out/dispatch/ still reconstructs from Git; stale PIDs and released OS locks never block recovery.

**Rationale.** Realizes SN-025 and SN-017 - a walk-away parallel run must survive process/host death with the disk intact, reconstructing every reservation and disposition from durable Git refs so nothing is double-run or falsely done (docs/specs/parallel-wi-dispatch.md 11).
- Realizes: [SN-017](../stakeholder-needs/SN-017.md) · [SN-025](../stakeholder-needs/SN-025.md)
- Decomposed by: [LLR-065](../low-level-requirements/LLR-065.md)
- Verified by: [TC-065](../test-cases/TC-065.md)
