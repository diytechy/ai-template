---
type: "System Requirement"
title: "Pre-commit hook floor"
description: "The pre-commit hook shall run the integrity + secrets floor on every commit, agent-neutrally \u2014 a LOCAL floor, bypassable by the session (git commit --no-verify), so it discharges \"no unchecked write enters governed state\" only as a PAIR with the hosted re-run of the same bar (SR-151/SR-152), never alone."
tags: ["M", "Test", "Modified"]
resource: "docs/requirements/system-requirements.toml (SR-019)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/system-requirements.toml (SR-019) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# SR-019 — Pre-commit hook floor

**Requirement.** The pre-commit hook shall run the integrity + secrets floor on every commit, agent-neutrally — a LOCAL floor, bypassable by the session (git commit --no-verify), so it discharges "no unchecked write enters governed state" only as a PAIR with the hosted re-run of the same bar (SR-151/SR-152), never alone.

**Acceptance criteria.** The hook runs trace --strict-integrity and the secrets scan on staged content and blocks a commit that fails either; a --no-verify commit bypasses it and is caught instead by the hosted re-run, which is what makes the pair — not this row — the honest unit of the claim.

**Rationale.** Realizes SN-005 and SN-009 — a floor only CI enforces is one the local tree crosses freely, and a secret caught after the commit is already in history. Trusting the agent to run the checks was rejected: a floor exists for the times the agent forgets, so it is git-enforced and agent-neutral.
- Realizes: [SN-005](../stakeholder-needs/SN-005.md) · [SN-009](../stakeholder-needs/SN-009.md)
- Decomposed by: [LLR-019](../low-level-requirements/LLR-019.md)
- Verified by: [TC-019](../test-cases/TC-019.md)
