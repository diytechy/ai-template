---
type: "System Requirement"
title: "Doc reference validation"
description: "check_doc_refs.py shall flag a documentation reference to a repository path that does not exist and an asserted sym:<module>.<name> reference missing from the generated module-map inventory, warn-first (exit 0) with --strict gating; absent inventory skips the symbol tier cleanly."
tags: ["S", "Test", "Verified", "Doc currency"]
resource: "docs/requirements/system-requirements.toml (SR-041)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/system-requirements.toml (SR-041) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# SR-041 — Doc reference validation

**Requirement.** check_doc_refs.py shall flag a documentation reference to a repository path that does not exist and an asserted sym:<module>.<name> reference missing from the generated module-map inventory, warn-first (exit 0) with --strict gating; absent inventory skips the symbol tier cleanly.

**Acceptance criteria.** A backticked repo path that does not exist warns and gates only under --strict; URLs, globs, placeholders, generated blocks and path-ok lines are ignored; a dangling sym: reference is flagged against the module map; with no inventory the symbol tier skips and the path tier still runs.

**Rationale.** Realizes SN-004 and SN-008 — link resolution sees a broken link but not prose naming a file that was renamed away, nor an asserted code symbol that no longer exists; both read as correct forever. Validating every backticked word was rejected as unaffordable in false positives, so only path-shaped tokens and an explicit sym: convention are checked, generated marker blocks are exempt, and a path-ok line is the escape hatch for prose that names a path deliberately absent from the tree.
- Realizes: [SN-008](../stakeholder-needs/SN-008.md) · [SN-010](../stakeholder-needs/SN-010.md)
- Decomposed by: [LLR-038](../low-level-requirements/LLR-038.md)
- Verified by: [TC-041](../test-cases/TC-041.md)
