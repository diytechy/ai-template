---
type: "System Requirement"
title: "Prompts are reviewable files with a per-session audit trail"
description: "Every prompt the delivered loop launches shall be a shipped, reviewable file with strictly filled slots \u2014 listed by digest in a freshness-gated generated catalogue, each session recording which template it used and the fingerprint of what it rendered to."
tags: ["S", "Test", "Approved", "process"]
resource: "docs/requirements/system-requirements.toml (SR-146)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/system-requirements.toml (SR-146) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# SR-146 — Prompts are reviewable files with a per-session audit trail

**Requirement.** Every prompt the delivered loop launches shall be a shipped, reviewable file with strictly filled slots — listed by digest in a freshness-gated generated catalogue, each session recording which template it used and the fingerprint of what it rendered to.

**Acceptance criteria.** An unknown or unfilled slot is a refusal; a missing shipped template is a named preflight refusal rather than a first-session crash; the catalogue lists every shipped prompt with its digest and `--check` fails when stale; a line-ending difference is not a digest change; an operator override is named in the session record rather than only in the launch flags.

**Rationale.** Prose steers the sessions this loop launches and had been reviewable only by reading Python source, which makes the process trusted rather than inspectable. The audit trail is the part that makes the move useful rather than cosmetic: without a per-session digest, "which instruction did this session see" stays an inference. (The argv-vs-shell-string transport decision is design, and lives one tier down in LLR-163.)
- Realizes: [SN-005](../stakeholder-needs/SN-005.md)
- Decomposed by: [LLR-162](../low-level-requirements/LLR-162.md) · [LLR-163](../low-level-requirements/LLR-163.md) · [LLR-164](../low-level-requirements/LLR-164.md) · [LLR-167](../low-level-requirements/LLR-167.md)
- Verified by: [TC-157](../test-cases/TC-157.md)
