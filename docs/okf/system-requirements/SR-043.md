---
type: "System Requirement"
title: "Subagent spawn gate"
description: "A PreToolUse subagent-spawn gate shall, during an unattended run, refuse or defer subagent spawns per the declared subagent-gate dial in the single policy declaration home (off|ask|deny; absent=off; the legacy one-word file is still read through the migration window), honor a launcher-set SUBAGENT_GATE=allow override the model cannot set, and fail open on any error so a broken gate never wedges the tools."
tags: ["S", "Test", "Modified"]
resource: "docs/requirements/system-requirements.toml (SR-043)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/system-requirements.toml (SR-043) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# SR-043 — Subagent spawn gate

**Requirement.** A PreToolUse subagent-spawn gate shall, during an unattended run, refuse or defer subagent spawns per the declared subagent-gate dial in the single policy declaration home (off|ask|deny; absent=off; the legacy one-word file is still read through the migration window), honor a launcher-set SUBAGENT_GATE=allow override the model cannot set, and fail open on any error so a broken gate never wedges the tools.

**Acceptance criteria.** With [checks] subagent_gate = "deny": a Task/Agent spawn is refused (permissionDecision=deny, exit 2); ask defers to approval; off/absent and SUBAGENT_GATE=allow allow it; a non-spawn tool is not gated; a malformed payload fails open (allow) and is logged. Read off the current carrier, as the current set: docs/process.toml [checks] subagent_gate, with the legacy docs/subagent-gate as the migration-window fallback.

**Rationale.** Realizes SN-006 (a walk-away run stays safe — bounded, supervised fan-out with the override held by the human, not the model) and SN-012 (opt-in: absent/off is a vacuous allow, so a non-adopter pays nothing). Supervision, not security — a model that can edit files can remove the hook (adapted from stop-subagent-fanout, MIT; spec docs/archive/INTEGRATION_PLAN.md Phase 4). Hat-derived (hat.SECURITY): C-SEC-2 asks that the irreversible actions an unattended run can take be enumerated, each naming the dial or human authority that permits it — which is this row exactly, for the one action that starts further actors. A direct hit, and the reason this row is not accretion: its parentage was real and lived only in SN-006's `why` cell until the safety half moved into that need's normative text at this same pass. OPEN QUESTION FOR THE SITTING, recorded rather than resolved here (2026-08-16 adversarial round, finding F1): with the bound now normative in SN-006, this row's fail-open-on-any-error clause is in tension with that bound — a gate error drops the ceiling the need promises — and the two candidate resolutions (fail closed, or keep failing open under a narrower ceiling that stays enforced when the hook errors) are both behavior changes an owner must rule; the fail-open behavior is UNCHANGED pending that ruling, because a gate that wedges every tool on its own bug is the failure this clause was written against. (Derived-requirement label, added 2026-08-16 — PROVISIONAL, unsigned.)
- Realizes: [SN-006](../stakeholder-needs/SN-006.md) · [SN-012](../stakeholder-needs/SN-012.md)
- Decomposed by: [LLR-040](../low-level-requirements/LLR-040.md)
- Verified by: [TC-043](../test-cases/TC-043.md)
