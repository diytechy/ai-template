---
type: "System Requirement"
title: "Subagent spawn gate"
description: "A PreToolUse subagent-spawn gate shall, during an unattended run, refuse or defer subagent spawns per docs/subagent-gate (off|ask|deny; absent=off), honor a launcher-set SUBAGENT_GATE=allow override the model cannot set, and fail open on any error so a broken gate never wedges the tools."
tags: ["S", "Test", "Verified", "Unattended coordinator"]
resource: "docs/requirements/system-requirements.csv (SR-043)"
---

# SR-043 — Subagent spawn gate

**Requirement.** A PreToolUse subagent-spawn gate shall, during an unattended run, refuse or defer subagent spawns per docs/subagent-gate (off|ask|deny; absent=off), honor a launcher-set SUBAGENT_GATE=allow override the model cannot set, and fail open on any error so a broken gate never wedges the tools.

**Acceptance criteria.** With docs/subagent-gate: deny a Task/Agent spawn is refused (permissionDecision=deny, exit 2); ask defers to approval; off/absent and SUBAGENT_GATE=allow allow it; a non-spawn tool is not gated; a malformed payload fails open (allow) and is logged.

**Rationale.** Realizes SN-006 (a walk-away run stays safe — bounded, supervised fan-out with the override held by the human, not the model) and SN-012 (opt-in: absent/off is a vacuous allow, so a non-adopter pays nothing). Supervision, not security — a model that can edit files can remove the hook (adapted from stop-subagent-fanout, MIT; ClaudeGuardChecks INTEGRATION_PLAN.md Phase 4).
- Realizes: [SN-006](../stakeholder-needs/SN-006.md) · [SN-012](../stakeholder-needs/SN-012.md)
- Decomposed by: [LLR-040](../low-level-requirements/LLR-040.md)
- Verified by: [TC-043](../test-cases/TC-043.md)
