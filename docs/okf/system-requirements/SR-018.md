---
type: "System Requirement"
title: "Privacy gate two-axis"
description: "When the declared privacy_check dial is on, the system shall additionally refuse, at the same hook floor, a commit or push carrying a PII/identity class \u2014 exempting configured author identities."
tags: ["M", "Test", "Modified", "Secrets + privacy lint"]
resource: "docs/requirements/system-requirements.toml (SR-018)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/system-requirements.toml (SR-018) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# SR-018 — Privacy gate two-axis

**Requirement.** When the declared privacy_check dial is on, the system shall additionally refuse, at the same hook floor, a commit or push carrying a PII/identity class — exempting configured author identities.

**Acceptance criteria.** With privacy-check on, an identity/PII class is flagged unless the author is in EXEMPT_EMAILS; with it off, only the secrets floor runs.

**Rationale.** Realizes SN-009 — identity and PII classes are a policy choice a repo makes about its own contributors, unlike a leaked credential, which is a defect in every repo. Making them always-on was rejected because the exemption list they need is project-specific: a repo that has not declared its author identities would have the layer firing on its own commit metadata, and the fix would be to silence the scanner rather than to configure it.
- Realizes: [SN-009](../stakeholder-needs/SN-009.md)
- Decomposed by: [LLR-018](../low-level-requirements/LLR-018.md)
- Verified by: [TC-018](../test-cases/TC-018.md)
