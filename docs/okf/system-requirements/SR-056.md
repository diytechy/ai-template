---
type: "System Requirement"
title: "Decomposition render polish"
description: "The tiered decomposition views shall render right-sized columns (a tier column no wider than its content requires within a declared bound); one horizontal parent-to-child arrow per containment edge, making containment explicit rather than implied by nesting alone; and a hover/focus highlight that persists on the last-hovered item until another takes it (no flash-on-exit); self-contained, byte-deterministic, --check freshness unchanged."
tags: ["C", "Test", "Verified", "Trajectory"]
resource: "docs/requirements/system-requirements.csv (SR-056)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/system-requirements.csv (SR-056) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# SR-056 — Decomposition render polish

**Requirement.** The tiered decomposition views shall render right-sized columns (a tier column no wider than its content requires within a declared bound); one horizontal parent-to-child arrow per containment edge, making containment explicit rather than implied by nesting alone; and a hover/focus highlight that persists on the last-hovered item until another takes it (no flash-on-exit); self-contained, byte-deterministic, --check freshness unchanged.

**Acceptance criteria.** Each containment edge renders exactly one parent-to-child arrow; column widths honor the declared bound; the persistent-highlight contract is present and keyed to the last-hovered node; a data-less repo renders byte-identically; deterministic; --check trips on stale output.

**Rationale.** Realizes SN-023/SN-010 (owner intake 2026-07-13 item 1B): the shipped tiering (SR-051) made large projects readable top-down, but the render is column-wasteful, containment is implicit, and the hover highlight vanishes on exit - polish with mechanically assertable outcomes, so Verification stays Test (the Critique rows judge the residual look-and-feel).
- Realizes: [SN-010](../stakeholder-needs/SN-010.md) · [SN-023](../stakeholder-needs/SN-023.md)
- Decomposed by: [LLR-057](../low-level-requirements/LLR-057.md)
- Verified by: [TC-057](../test-cases/TC-057.md)
