---
type: "System Requirement"
title: "Always-on secrets floor"
description: "check_privacy.py shall scan every commit's staged diff, message, and outgoing range for secret patterns regardless of the privacy toggle, unless docs/secrets-scan is explicitly set to off."
tags: ["M", "Test", "Modified", "Secrets + privacy lint"]
resource: "docs/requirements/system-requirements.csv (SR-017)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/system-requirements.csv (SR-017) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# SR-017 — Always-on secrets floor

**Requirement.** check_privacy.py shall scan every commit's staged diff, message, and outgoing range for secret patterns regardless of the privacy toggle, unless docs/secrets-scan is explicitly set to off.

**Acceptance criteria.** A staged PEM header, token, or key pattern is flagged and blocks; a clean diff passes; the floor runs with privacy-check off, and is disabled only by an explicit docs/secrets-scan: off.

**Rationale.** Realizes SN-009 — a committed secret cannot be un-published, so the scan runs everywhere by default rather than where someone remembered to enable it. Folding it into the privacy toggle was rejected: that makes the highest-severity check opt-in alongside a preference. The one opt-out is an explicit declaration, so turning it off is a visible act.
- Realizes: [SN-009](../stakeholder-needs/SN-009.md)
- Decomposed by: [LLR-017](../low-level-requirements/LLR-017.md)
- Verified by: [TC-017](../test-cases/TC-017.md)
