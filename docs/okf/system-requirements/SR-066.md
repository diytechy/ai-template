---
type: "System Requirement"
title: "Dual-plan decomposition round"
description: "A queued WI whose registry row declares PlanMode=dual shall be decomposed by a dual-plan round rather than a direct BUILD: two independent fresh planner sessions (routed to different model families where the enabled pool allows, else a recorded same-family degraded pair), a mechanical coverage pre-pass gating each plan, one cross-critique in which each plan is judged by the other hat's route, one bounded revision each against CHANGES-REQUESTED, and a position-swapped arbiter pair whose two runs must agree on the TRUE plan; the selected plan's rows are filed as queued WIs hanging off the round's parent. Hard caps bound the spend (one critique round, one revision each, one coverage repair per plan per stage, a round-budget session ledger); every cap breach, arbiter disagreement, runtime-nonresponse, or budget exhaustion shall PAGE onto docs/gate-policy's failure semantics rather than proceed. Briefs shall be assembled allowlist-only from system-requirements.csv + interfaces.csv so status.md/log.md/self-assessments are unreachable by construction; sessions run in the repo cwd on that redaction floor, with empty-cwd isolation an optional hardening. The worker assignment path shall refuse a PlanMode=dual row fail-closed (naming the --dual-plan entry); and under the --jobs dispatcher a PlanMode=dual row shall classify as a single-WI traincar - the SR-058 high-risk/critique class derived from the PlanMode signal itself, never a second hand-set SafetyClass cell (a contradicting declared SafetyClass quarantines unclassified) and never packed with other WIs - and shall auto-dispatch as the round: one serialized docs-only disposition onto the integration ref (SELECT closes the parent row done with the filed children hanging off it; PAGE commits the evidence and maps onto docs/gate-policy via plan_round.page_action without pausing disjoint work under autonomous); the serial resume driver shall page the --dual-plan ask when only dual rows are dependency-actionable - a dual row never parks silently on either path."
tags: ["S", "Test", "Verified", "Dual-plan decomposition"]
resource: "docs/requirements/system-requirements.csv (SR-066)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/system-requirements.csv (SR-066) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# SR-066 — Dual-plan decomposition round

**Requirement.** A queued WI whose registry row declares PlanMode=dual shall be decomposed by a dual-plan round rather than a direct BUILD: two independent fresh planner sessions (routed to different model families where the enabled pool allows, else a recorded same-family degraded pair), a mechanical coverage pre-pass gating each plan, one cross-critique in which each plan is judged by the other hat's route, one bounded revision each against CHANGES-REQUESTED, and a position-swapped arbiter pair whose two runs must agree on the TRUE plan; the selected plan's rows are filed as queued WIs hanging off the round's parent. Hard caps bound the spend (one critique round, one revision each, one coverage repair per plan per stage, a round-budget session ledger); every cap breach, arbiter disagreement, runtime-nonresponse, or budget exhaustion shall PAGE onto docs/gate-policy's failure semantics rather than proceed. Briefs shall be assembled allowlist-only from system-requirements.csv + interfaces.csv so status.md/log.md/self-assessments are unreachable by construction; sessions run in the repo cwd on that redaction floor, with empty-cwd isolation an optional hardening. The worker assignment path shall refuse a PlanMode=dual row fail-closed (naming the --dual-plan entry); and under the --jobs dispatcher a PlanMode=dual row shall classify as a single-WI traincar - the SR-058 high-risk/critique class derived from the PlanMode signal itself, never a second hand-set SafetyClass cell (a contradicting declared SafetyClass quarantines unclassified) and never packed with other WIs - and shall auto-dispatch as the round: one serialized docs-only disposition onto the integration ref (SELECT closes the parent row done with the filed children hanging off it; PAGE commits the evidence and maps onto docs/gate-policy via plan_round.page_action without pausing disjoint work under autonomous); the serial resume driver shall page the --dual-plan ask when only dual rows are dependency-actionable - a dual row never parks silently on either path.

**Acceptance criteria.** agent_loop --dual-plan over a fixture PlanMode=dual WI runs the full round unattended (plans, coverage, cross-critiques, both position-swapped arbiter runs agreeing) and files the selected rows as queued WIs the real check_trajectory accepts; the worker --wi path refuses a dual row naming --dual-plan and the flag refuses a non-dual row; a position-biased arbiter disagreeing across the swap PAGEs position-unstable with the attended NEEDS-HUMAN run-state; each hat brief carries registry material and never a status.md/log.md sentinel (redacted by construction); a cap breach, budget exhaustion, or arbiter disagreement PAGEs per docs/gate-policy; a fixture PlanMode=dual row on the frontier under --jobs auto-runs the full round unattended (never joining a multi-WI traincar), files the selected children on the integration ref and closes the parent row done, and a swap-disagreement PAGE honors docs/gate-policy (attended parks NEEDS-HUMAN; autonomous continues without pausing disjoint work); the serial driver writes the NEEDS-HUMAN run-state ask naming --dual-plan when only dual rows are dependency-actionable.

**Rationale.** Realizes SN-024/SN-006: rival WI decompositions are a subjective artifact comparison with no crisp measurable interface, so acceptance is adjudicated by independent critical eyes (two planners plus the position-swapped arbiter) against docs/rubrics/plan-decomposition.md - never a single self-judging session - and the whole round runs unattended and PAGEs clearly. Gives the WI-194..WI-199 round set (built provisionally on the SR-061 dispatcher home, WI-190/DP-001) its own requirement of record; the auto-dispatch SafetyClass and session-isolation calls are the WI-201 ruling, wired by WI-209 (schedule.classify derives the class from the PlanMode signal; the fail-closed worker refusal stays as the backstop).
- Realizes: [SN-006](../stakeholder-needs/SN-006.md) · [SN-024](../stakeholder-needs/SN-024.md)
- Decomposed by: [LLR-070](../low-level-requirements/LLR-070.md) · [LLR-071](../low-level-requirements/LLR-071.md) · [LLR-073](../low-level-requirements/LLR-073.md) · [LLR-074](../low-level-requirements/LLR-074.md) · [LLR-076](../low-level-requirements/LLR-076.md)
- Verified by: [TC-070](../test-cases/TC-070.md) · [TC-071](../test-cases/TC-071.md) · [TC-073](../test-cases/TC-073.md) · [TC-074](../test-cases/TC-074.md) · [TC-076](../test-cases/TC-076.md)
