---
type: "System Requirement"
title: "Stakeholder-need scope is a declared, checked value"
description: "Every non-example stakeholder-need row shall carry a scope value from the declared closed vocabulary (template / this repository / both), with a missing or out-of-vocabulary value reported by the harness naming the row."
tags: ["S", "Test", "Draft", "Traceability"]
resource: "docs/requirements/system-requirements.toml (SR-164)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/system-requirements.toml (SR-164) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# SR-164 — Stakeholder-need scope is a declared, checked value

**Requirement.** Every non-example stakeholder-need row shall carry a scope value from the declared closed vocabulary (template / this repository / both), with a missing or out-of-vocabulary value reported by the harness naming the row.

**Acceptance criteria.** The SN carrier schema declares the scope field; the checker reports a missing or invalid value naming the row; example -000 rows are exempt; the live registry's rows each carry a valid value when the field lands.

**Rationale.** Realizes SN-039 — a need whose audience is ambiguous gets enforced against the wrong repositories, and the scope must be a value a check can read, not inferred from prose. SN-039 had no citing SR. The field enters the SN carrier schema in the scheduled schema batch, per the need's own acceptance, rather than as a one-off column.
- Realizes: [SN-039](../stakeholder-needs/SN-039.md)
