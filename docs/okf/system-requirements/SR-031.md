---
type: "System Requirement"
title: "Declared-policy readers agree"
description: "Every delivered enforcer of a declared policy dial shall read the same value for that dial as every other enforcer \u2014 one parse, one answer, across the hooks, the loop and the checkers alike."
tags: ["M", "Test", "Modified", "Declared-policy readers"]
resource: "docs/requirements/system-requirements.toml (SR-031)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/system-requirements.toml (SR-031) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# SR-031 — Declared-policy readers agree

**Requirement.** Every delivered enforcer of a declared policy dial shall read the same value for that dial as every other enforcer — one parse, one answer, across the hooks, the loop and the checkers alike.

**Acceptance criteria.** Every enforcer reads the same value for the same dial; the two delivered grammars (tomllib and the hooks' sh) return the same answer over the adversarial file-shape table, including the trailing-comment decoy that once made the privacy gate fail OPEN.

**Rationale.** Realizes SN-004 (policies drive the gate) and SN-005 (one source, every enforcer reads it the same way). Partition with SR-137 (census): the one-home surface and its checked shape are SR-137's obligations at B-01/B-02; this row is the package-wide coherence property those mechanics serve. The shared-parse mechanism and the legacy migration-window read decompose to this row's LLRs.
- Realizes: [SN-004](../stakeholder-needs/SN-004.md) · [SN-005](../stakeholder-needs/SN-005.md)
- Decomposed by: [LLR-031](../low-level-requirements/LLR-031.md)
- Verified by: [TC-031](../test-cases/TC-031.md)
