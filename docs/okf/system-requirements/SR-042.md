---
type: "System Requirement"
title: "OKF knowledge-bundle export"
description: "gen_okf.py shall export the spine registries AND the key process docs as a generated Open Knowledge Format bundle under docs/okf \u2014 one typed markdown concept per real row (graph as resolvable markdown links, per-tier indexes) plus a Process Guide concept per present process doc (a summary derived from that doc and a resource pointer to the unmodified source) \u2014 deterministic (no clocks), with --check failing on any stale, missing or extra bundle file; docs/okf-export off silences and placeholder-only registries are vacuous."
tags: ["M", "Test", "Verified", "Knowledge export"]
resource: "docs/requirements/system-requirements.csv (SR-042)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/system-requirements.csv (SR-042) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# SR-042 — OKF knowledge-bundle export

**Requirement.** gen_okf.py shall export the spine registries AND the key process docs as a generated Open Knowledge Format bundle under docs/okf — one typed markdown concept per real row (graph as resolvable markdown links, per-tier indexes) plus a Process Guide concept per present process doc (a summary derived from that doc and a resource pointer to the unmodified source) — deterministic (no clocks), with --check failing on any stale, missing or extra bundle file; docs/okf-export off silences and placeholder-only registries are vacuous.

**Acceptance criteria.** One frontmatter-typed file per real SN/SR/LLR/TC row whose graph links resolve; a Process Guide concept for each present process doc carrying a summary derived from the doc (not hand-authored) with the source left unmodified; regeneration is byte-identical; --check trips on a registry OR doc edit, a missing bundle and a hand-added file; the write mode prunes extras; off and placeholder-only registries pass silently, and process guides emit only alongside a real spine.

**Rationale.** Realizes SN-010 and SN-012 — the traceability knowledge AND the process knowledge become portable into any OKF consumer as one bundle, with no new source of truth and no runtime dependency (Thread 48 Layer A + B2, owner-ruled on-by-default/opt-out + commit-the-bundle); emit-only stdlib (JSON string scalars are valid YAML), never a parser; B2 leaves the source docs untouched (the intrusive B1 stays deferred); the dashboard's Knowledge tab (SR-038, WI-070) is the bundle's first real consumer, resolving the 2026-07-11 OKF audit's no-consumer finding.
- Realizes: [SN-010](../stakeholder-needs/SN-010.md) · [SN-012](../stakeholder-needs/SN-012.md)
- Decomposed by: [LLR-039](../low-level-requirements/LLR-039.md)
- Verified by: [TC-042](../test-cases/TC-042.md)
