---
type: "Low-Level Requirement"
title: "Process tab intake + human-decision loop panels"
description: "Extends SR-050's process_panel with the project's two circular working loops as linked flow panels: (A) the intake loop - owner/agent intake -> triage into work items with spec detail -> the resume loop (docs/next-wi) -> build/review -> merge; (B) the human-decision loop - open-items population (incl. the gate-ratification table) -> human review and ruling -> the log's Decisions record -> merge - with the LLM_Agent entry node rendered once and shared by both loops. Each stage links to its canonical home (status.md, work-items.csv, open-items.md, log.md); data-derived where a canonical source exists; a data-less repo renders byte-identically; --check unchanged (the trajectory-map step)."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-056)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-056) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-056 — Process tab intake + human-decision loop panels

**Detail.** Extends SR-050's process_panel with the project's two circular working loops as linked flow panels: (A) the intake loop - owner/agent intake -> triage into work items with spec detail -> the resume loop (docs/next-wi) -> build/review -> merge; (B) the human-decision loop - open-items population (incl. the gate-ratification table) -> human review and ruling -> the log's Decisions record -> merge - with the LLM_Agent entry node rendered once and shared by both loops. Each stage links to its canonical home (status.md, work-items.csv, open-items.md, log.md); data-derived where a canonical source exists; a data-less repo renders byte-identically; --check unchanged (the trajectory-map step).

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `process_panel/_loop_panel`
- Decomposes: [SR-055](../system-requirements/SR-055.md)
- Verified by: [TC-056](../test-cases/TC-056.md)
