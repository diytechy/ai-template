---
type: "Low-Level Requirement"
title: "Dual-plan coverage step adapter"
description: "Runs plan_coverage.py headless (WI-197, DP-001 selected plan P4) after generation and after revision and maps its exit/report contract to plan_round's STEP_COVERAGE outcome: run_coverage() invokes the checker as a subprocess (sys.executable, sibling script, --out), returns the typed result {exit, findings, malformed, implicated true plan keys parsed from the 'FAIL - <planfile>:' lines via a caller-supplied plan_key_of, report}, and captures the per-plan + pairwise report as the payload the critic/arbiter briefs embed (returned, never printed); exit 1 = findings, exit 2 = malformed (its own flag) - both bounce, empty implicated => both plans re-run (fail-closed). to_record_kwargs() projects the result onto record(STEP_COVERAGE, findings=bool, plans=[...]) kwargs; the adapter is side-effect-free (never touches round state), so the coordinator records the bounce-once-then-page disposition."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-073)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-073) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-073 — Dual-plan coverage step adapter

**Detail.** Runs plan_coverage.py headless (WI-197, DP-001 selected plan P4) after generation and after revision and maps its exit/report contract to plan_round's STEP_COVERAGE outcome: run_coverage() invokes the checker as a subprocess (sys.executable, sibling script, --out), returns the typed result {exit, findings, malformed, implicated true plan keys parsed from the 'FAIL - <planfile>:' lines via a caller-supplied plan_key_of, report}, and captures the per-plan + pairwise report as the payload the critic/arbiter briefs embed (returned, never printed); exit 1 = findings, exit 2 = malformed (its own flag) - both bounce, empty implicated => both plans re-run (fail-closed). to_record_kwargs() projects the result onto record(STEP_COVERAGE, findings=bool, plans=[...]) kwargs; the adapter is side-effect-free (never touches round state), so the coordinator records the bounce-once-then-page disposition.

**Code.** `project-trajectory/scripts/plan_coverage_step.py` — `run_coverage/to_record_kwargs/_implicated_plans`
- Decomposes: [SR-066](../system-requirements/SR-066.md)
- Verified by: [TC-073](../test-cases/TC-073.md)
