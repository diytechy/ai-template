---
type: "Low-Level Requirement"
title: "Coverage-step result adapter"
description: "Converts coverage CLI results and report payload into clean, implicated-repair, repeated-failure, or malformed round records. Exit 0 advances the round; a first finding implicates and repairs once; a repeated finding pages; malformed is distinct from findings and fails closed."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-073)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-073) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-073 — Coverage-step result adapter

**Detail.** Converts coverage CLI results and report payload into clean, implicated-repair, repeated-failure, or malformed round records. Exit 0 advances the round; a first finding implicates and repairs once; a repeated finding pages; malformed is distinct from findings and fails closed.

**Code.** `project-trajectory/scripts/plan_coverage_step.py` — `run_coverage/to_record_kwargs/_implicated_plans`
- Decomposes: [SR-155](../system-requirements/SR-155.md)
- Verified by: [TC-073](../test-cases/TC-073.md)
