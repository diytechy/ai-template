---
type: "Low-Level Requirement"
title: "Hats roster reader + planner-brief injection"
description: "Reads the declared expert-perspective roster (docs/requirements/hats.toml; SN-036 via OI-19, ruled 2026-08-13): validates the three required keys and the closed applies_when grammar at load (malformed refuses loudly and PAGES the round; absence is opt-out), selects the applicable hats for a decomposition context (an undeclared field satisfies no clause), and renders the one-line-per-hat markdown block the dual-plan planner brief embeds via its {{HAT_QUESTIONS}} slot. Whitespace collapses at load so roster text cannot mint a markdown heading inside the composed brief."
tags: ["Draft"]
resource: "docs/requirements/low-level-requirements.toml (LLR-168)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-168) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-168 — Hats roster reader + planner-brief injection

**Detail.** Reads the declared expert-perspective roster (docs/requirements/hats.toml; SN-036 via OI-19, ruled 2026-08-13): validates the three required keys and the closed applies_when grammar at load (malformed refuses loudly and PAGES the round; absence is opt-out), selects the applicable hats for a decomposition context (an undeclared field satisfies no clause), and renders the one-line-per-hat markdown block the dual-plan planner brief embeds via its {{HAT_QUESTIONS}} slot. Whitespace collapses at load so roster text cannot mint a markdown heading inside the composed brief.

**Code.** `project-trajectory/scripts/hats.py` — `load/applicable/brief_block/parse_condition`
- Decomposes: [SR-155](../system-requirements/SR-155.md)
- Verified by: [TC-162](../test-cases/TC-162.md)
