---
type: "Low-Level Requirement"
title: "Model routing + fixed escalation policy"
description: "load_registry parses docs/agents.csv (id charset [A-Z0-9][A-Z0-9.-]*, tier vocab, dup detection; id is a join key never parsed) and load_enabled reads the ordered docs/agents-enabled pool; select(pool,tier) picks by enable-list preference order honoring per-model cooldown (cool/available generalize the rate-limit backoff), the reviewer heterogeneity preference (prefer a different provider, degrade to same-provider when none), and walks a tier UP never down; escalate(rounds) is the fixed win-stay/lose-shift decision (margin>=2 primary, swap after 2 failed gates, tier-up only after the swap fails, page on shared-failure/contradiction/tripwire) with per-repo-overridable constants; failure_action keys the page-the-human semantics to docs/gate-policy."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-044)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-044) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-044 — Model routing + fixed escalation policy

**Detail.** load_registry parses docs/agents.csv (id charset [A-Z0-9][A-Z0-9.-]*, tier vocab, dup detection; id is a join key never parsed) and load_enabled reads the ordered docs/agents-enabled pool; select(pool,tier) picks by enable-list preference order honoring per-model cooldown (cool/available generalize the rate-limit backoff), the reviewer heterogeneity preference (prefer a different provider, degrade to same-provider when none), and walks a tier UP never down; escalate(rounds) is the fixed win-stay/lose-shift decision (margin>=2 primary, swap after 2 failed gates, tier-up only after the swap fails, page on shared-failure/contradiction/tripwire) with per-repo-overridable constants; failure_action keys the page-the-human semantics to docs/gate-policy.

**Code.** `project-trajectory/scripts/agent_route.py` — `load_registry/select/escalate/failure_action`
- Decomposes: [SR-045](../system-requirements/SR-045.md)
- Verified by: [TC-046](../test-cases/TC-046.md)
