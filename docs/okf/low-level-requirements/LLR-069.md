---
type: "Low-Level Requirement"
title: "Dual-plan coverage pre-pass"
description: "Makes rival WI decompositions mechanically comparable (WI-190, process-options.md 'Dual-plan decomposition' step 3): parses the goal brief's numbered C# clauses and each plan's Plan-WI table, resolves Covers (C#/SR-###) and Interfaces (IF-### / Proposed: + rationale / intra-module escape) cites against system-requirements.csv + interfaces.csv, flags duplicate row ids, unknown predecessors and cycles, and emits per-plan coverage + the pairwise coverage diff. Coverage gaps are the report's payload (never findings); reference/structure findings exit 1; a clause-less goal or table-less plan exits 2; an absent registry degrades to an honest note. The rationale check is PRESENCE-only - honesty stays the rubric's seam-duplication anchor (docs/rubrics/plan-decomposition.md B1)."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-069)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-069) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-069 — Dual-plan coverage pre-pass

**Detail.** Makes rival WI decompositions mechanically comparable (WI-190, process-options.md 'Dual-plan decomposition' step 3): parses the goal brief's numbered C# clauses and each plan's Plan-WI table, resolves Covers (C#/SR-###) and Interfaces (IF-### / Proposed: + rationale / intra-module escape) cites against system-requirements.csv + interfaces.csv, flags duplicate row ids, unknown predecessors and cycles, and emits per-plan coverage + the pairwise coverage diff. Coverage gaps are the report's payload (never findings); reference/structure findings exit 1; a clause-less goal or table-less plan exits 2; an absent registry degrades to an honest note. The rationale check is PRESENCE-only - honesty stays the rubric's seam-duplication anchor (docs/rubrics/plan-decomposition.md B1).

**Code.** `project-trajectory/scripts/plan_coverage.py` — `parse_goal/parse_plan/check_plan/find_cycle/format_report`
- Decomposes: [SR-044](../system-requirements/SR-044.md)
- Verified by: [TC-069](../test-cases/TC-069.md)
