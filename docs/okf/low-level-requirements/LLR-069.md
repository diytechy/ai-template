---
type: "Low-Level Requirement"
title: "Rival-plan coverage comparison"
description: "Parses rival plan tables; resolves clause/SR/IF coverage; validates dependencies; emits pairwise differences and distinct findings/malformed/absent-registry outcomes. Commensurable plans emit per-plan and pairwise coverage; findings (bad refs or plan graphs) exit 1, malformed inputs exit 2, and an absent registry notes without failing."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-069)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-069) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-069 — Rival-plan coverage comparison

**Detail.** Parses rival plan tables; resolves clause/SR/IF coverage; validates dependencies; emits pairwise differences and distinct findings/malformed/absent-registry outcomes. Commensurable plans emit per-plan and pairwise coverage; findings (bad refs or plan graphs) exit 1, malformed inputs exit 2, and an absent registry notes without failing.

**Code.** `project-trajectory/scripts/plan_coverage.py` — `parse_goal/parse_plan/check_plan/find_cycle/format_report`
- Decomposes: [SR-155](../system-requirements/SR-155.md)
- Verified by: [TC-069](../test-cases/TC-069.md)
