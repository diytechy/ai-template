---
type: "Low-Level Requirement"
title: "Lock mutual exclusion"
description: "Refuses a second coordinator holding the same per-worktree lock, preventing a two-writer race; the refusal is the preflight's typed nonzero exit, not a warning, so only one writer runs per checkout."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-030)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-030) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-030 — Lock mutual exclusion

**Detail.** Refuses a second coordinator holding the same per-worktree lock, preventing a two-writer race; the refusal is the preflight's typed nonzero exit, not a warning, so only one writer runs per checkout.

**Code.** `project-trajectory/scripts/agent_common.py` — `acquire_lock`
- Decomposes: [SR-027](../system-requirements/SR-027.md)
- Verified by: [TC-030](../test-cases/TC-030.md)
