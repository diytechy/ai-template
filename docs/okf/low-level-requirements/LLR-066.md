---
type: "Low-Level Requirement"
title: "Parallel telemetry + downstream migration/audits"
description: "Reason-coded events aggregated by (train, WI, session) + a lanes/frontier/queue/ceiling banner; downstream migration holds --jobs 1 until the soft-edge audit AND the SafetyClass audit pass, then records the two-worker flip; reconciles legacy active rows + docs/tracks/* within one compatibility window."
tags: ["Planned"]
resource: "docs/requirements/low-level-requirements.csv (LLR-066)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-066) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-066 — Parallel telemetry + downstream migration/audits

**Detail.** Reason-coded events aggregated by (train, WI, session) + a lanes/frontier/queue/ceiling banner; downstream migration holds --jobs 1 until the soft-edge audit AND the SafetyClass audit pass, then records the two-worker flip; reconciles legacy active rows + docs/tracks/* within one compatibility window.

**Code.** `project-trajectory/scripts/agent_loop.py` — `telemetry/migrate/audit_flip`
- Decomposes: [SR-065](../system-requirements/SR-065.md)
- Verified by: [TC-066](../test-cases/TC-066.md)
