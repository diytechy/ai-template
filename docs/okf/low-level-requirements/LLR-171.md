---
type: "Low-Level Requirement"
title: "Language-neutral registry templates + ID scheme"
description: "The shipped registry templates and the ID scheme (SN/SR/LLR/TC and the off-spine spaces) carry no language-specific token: a scaffold produced with a non-Python profile passes trace.py --strict-integrity --strict-schema unmodified, and a token scan over the shipped templates and the ID vocabulary finds no language-specific token."
tags: ["Draft"]
resource: "docs/requirements/low-level-requirements.toml (LLR-171)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-171) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-171 — Language-neutral registry templates + ID scheme

**Detail.** The shipped registry templates and the ID scheme (SN/SR/LLR/TC and the off-spine spaces) carry no language-specific token: a scaffold produced with a non-Python profile passes trace.py --strict-integrity --strict-schema unmodified, and a token scan over the shipped templates and the ID vocabulary finds no language-specific token.

**Rationale.** The observable minted for SR-035 by owner ruling (13u rider): stack-agnosticism was a real delivered property with no observable, and this is the check that makes it falsifiable.

**Code.** `project-trajectory/registries` — `registry templates / ID vocabulary`
- Decomposes: [SR-035](../system-requirements/SR-035.md)
- Verified by: [TC-165](../test-cases/TC-165.md)
