---
type: "Low-Level Requirement"
title: "Default-density start-collapse (T2 core)"
description: "The MECHANIZED core of usability anchor T2 (docs/rubrics/dashboard-usability.md), split out of LLR-055 under the (f) decomposition ruling: a view whose scale EARNS tiering - a bundle spanning > 3 OKF types, a module set above the component threshold (the SR-089 '>3' rule) - renders START-COLLAPSED as the shared type-tiered drill, one block per type/container, rather than a flat wall of nodes; at or below the threshold it stays flat, so the tiering is earned by scale and never imposed on a small project. SCOPE WIDENED 2026-07-26: the Knowledge, How-SW AND the landing What views. The What icicle was the one view that never earned its density - it rendered the whole spine at LEAF scale (one unit per TC), the 119-CRITIQUE T2 MAJOR - and now start-collapses to one block per SN with descend-on-click, reusing _drill_layer_svg/_render_drill so the interaction idiom stays single- sourced. Capping DEPTH was rejected and the emitter says why: height is leaf-proportional, so stopping at the SR lane still stacks one unit per SR. T2's remaining 'reads legibly at default density' judgement stays perceptual under LLR-055/TC-055."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-099)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-099) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-099 — Default-density start-collapse (T2 core)

**Detail.** The MECHANIZED core of usability anchor T2 (docs/rubrics/dashboard-usability.md), split out of LLR-055 under the (f) decomposition ruling: a view whose scale EARNS tiering - a bundle spanning > 3 OKF types, a module set above the component threshold (the SR-089 '>3' rule) - renders START-COLLAPSED as the shared type-tiered drill, one block per type/container, rather than a flat wall of nodes; at or below the threshold it stays flat, so the tiering is earned by scale and never imposed on a small project. SCOPE WIDENED 2026-07-26: the Knowledge, How-SW AND the landing What views. The What icicle was the one view that never earned its density - it rendered the whole spine at LEAF scale (one unit per TC), the 119-CRITIQUE T2 MAJOR - and now start-collapses to one block per SN with descend-on-click, reusing _drill_layer_svg/_render_drill so the interaction idiom stays single- sourced. Capping DEPTH was rejected and the emitter says why: height is leaf-proportional, so stopping at the SR lane still stacks one unit per SR. T2's remaining 'reads legibly at default density' judgement stays perceptual under LLR-055/TC-055.

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `know_view/sw_view/_render_drill`
- Decomposes: [SR-054](../system-requirements/SR-054.md)
- Verified by: [TC-102](../test-cases/TC-102.md)
