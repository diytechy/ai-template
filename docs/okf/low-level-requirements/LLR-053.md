---
type: "Low-Level Requirement"
title: "Dashboard accessibility rendering"
description: "Renders PROJECT_STATE.html operable and readable without a pointer or full colour perception: every interactive element (tab buttons, expandable <details> blocks, SVG nodes with detail panels) is keyboard-reachable (native focusable elements or tabindex+key handler) and carries an accessible name (a <title>/aria-label or visible text); every status/phase/type encoding pairs its colour with a text or shape cue (never colour alone); text keeps a contrast ratio against its fill at or above the threshold declared in docs/rubrics/dashboard-accessibility.md (WCAG 2.1 AA). The design is adjudicated against that rubric by the SR-084/SR-085 critique loop - this LLR names the code surface the critique judges (Critique is non-LLR-exempt per SR-086). Self-contained, byte-deterministic; --check unchanged."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-053)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-053) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-053 — Dashboard accessibility rendering

**Detail.** Renders PROJECT_STATE.html operable and readable without a pointer or full colour perception: every interactive element (tab buttons, expandable <details> blocks, SVG nodes with detail panels) is keyboard-reachable (native focusable elements or tabindex+key handler) and carries an accessible name (a <title>/aria-label or visible text); every status/phase/type encoding pairs its colour with a text or shape cue (never colour alone); text keeps a contrast ratio against its fill at or above the threshold declared in docs/rubrics/dashboard-accessibility.md (WCAG 2.1 AA). The design is adjudicated against that rubric by the SR-084/SR-085 critique loop - this LLR names the code surface the critique judges (Critique is non-LLR-exempt per SR-086). Self-contained, byte-deterministic; --check unchanged.

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `build_html/_svg_node/_nav`
- Decomposes: [SR-052](../system-requirements/SR-052.md)
- Verified by: [TC-053](../test-cases/TC-053.md)
