---
type: "Low-Level Requirement"
title: "Dispatcher + worktree pool + atomic reservations"
description: "agent-resume dispatcher/integrator with --jobs (default 2)/auto/AGENT_JOBS; reconcile->gate->build-out launch; packs the frontier into traincars and dispatches dependency-clear ones to free workers; reserves constituents atomically via refs/llm/reservations/WI-### (commit-tree metadata + update-ref --stdin zero-old-value); leases worktrees; rescans for dynamic refill; honors pause/blackout/capacity."
tags: ["Planned"]
resource: "docs/requirements/low-level-requirements.csv (LLR-062)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-062) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-062 — Dispatcher + worktree pool + atomic reservations

**Detail.** agent-resume dispatcher/integrator with --jobs (default 2)/auto/AGENT_JOBS; reconcile->gate->build-out launch; packs the frontier into traincars and dispatches dependency-clear ones to free workers; reserves constituents atomically via refs/llm/reservations/WI-### (commit-tree metadata + update-ref --stdin zero-old-value); leases worktrees; rescans for dynamic refill; honors pause/blackout/capacity.

**Code.** `project-trajectory/scripts/agent_loop.py` — `dispatch/reserve_traincar/worktree_pool`
- Decomposes: [SR-061](../system-requirements/SR-061.md)
- Verified by: [TC-062](../test-cases/TC-062.md)
