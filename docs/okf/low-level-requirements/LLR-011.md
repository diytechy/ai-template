---
type: "Low-Level Requirement"
title: "Idempotent write + kit-version stamp"
description: "Skips an existing file unless --force; write_kit_version stamps docs/kit-version (SHA + date, -dirty on an uncommitted kit tree) so a re-sync is diffable."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-011)"
---

# LLR-011 — Idempotent write + kit-version stamp

**Detail.** Skips an existing file unless --force; write_kit_version stamps docs/kit-version (SHA + date, -dirty on an uncommitted kit tree) so a re-sync is diffable.

**Code.** `project-trajectory/scripts/bootstrap.py` — `write/--force + write_kit_version`
- Decomposes: [SR-011](../system-requirements/SR-011.md)
- Verified by: [TC-011](../test-cases/TC-011.md)
