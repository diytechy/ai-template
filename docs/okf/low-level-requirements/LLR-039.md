---
type: "Low-Level Requirement"
title: "OKF bundle emitter"
description: "emit() builds {relpath: content} for every real row (sorted, deterministic, no timestamps): frontmatter via JSON-quoted YAML scalars, bodies carrying the row prose plus up/down markdown links from locally built reverse maps, per-tier and root indexes, the UPSTREAM.md spec pin; on_disk() snapshots docs/okf for the byte-compare; write mode writes-if-changed and prunes; small loaders duplicated per the F5 rule (no new sibling import)."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-039)"
---

# LLR-039 — OKF bundle emitter

**Detail.** emit() builds {relpath: content} for every real row (sorted, deterministic, no timestamps): frontmatter via JSON-quoted YAML scalars, bodies carrying the row prose plus up/down markdown links from locally built reverse maps, per-tier and root indexes, the UPSTREAM.md spec pin; on_disk() snapshots docs/okf for the byte-compare; write mode writes-if-changed and prunes; small loaders duplicated per the F5 rule (no new sibling import).

**Code.** `project-trajectory/scripts/gen_okf.py` — `emit/concept/fm/on_disk`
- Decomposes: [SR-042](../system-requirements/SR-042.md)
- Verified by: [TC-042](../test-cases/TC-042.md)
