---
type: "Low-Level Requirement"
title: "Per-module coverage-floor comparator"
description: "Reads the docs/coverage-floors census (module + min percent; blank and # lines ignored) and coverage.py's JSON report (per-file percent_covered, path-normalized), fails on any module below its floor or absent from the report, SKIPs when the report is absent, is a no-op with an empty census, and fails loudly on a malformed floor line."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-098)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-098) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-098 — Per-module coverage-floor comparator

**Detail.** Reads the docs/coverage-floors census (module + min percent; blank and # lines ignored) and coverage.py's JSON report (per-file percent_covered, path-normalized), fails on any module below its floor or absent from the report, SKIPs when the report is absent, is a no-op with an empty census, and fails loudly on a malformed floor line.

**Code.** `project-trajectory/scripts/check_coverage.py` — `load_floors/load_report/evaluate`
- Decomposes: [SR-110](../system-requirements/SR-110.md)
- Verified by: [TC-101](../test-cases/TC-101.md)
