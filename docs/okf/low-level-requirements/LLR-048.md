---
type: "Low-Level Requirement"
title: "Independent critique dispatch"
description: "Detects Critique-scoped builds and assembles a fresh family-heterogeneous rubric/intent/artifact brief with no self-assessment. Only a Critique-scoped build schedules the session \u2014 any other build does not; the brief carries rubric, intent and artifact recipe and excludes the build transcript as well as self-assessment."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-048)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-048) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-048 — Independent critique dispatch

**Detail.** Detects Critique-scoped builds and assembles a fresh family-heterogeneous rubric/intent/artifact brief with no self-assessment. Only a Critique-scoped build schedules the session — any other build does not; the brief carries rubric, intent and artifact recipe and excludes the build transcript as well as self-assessment.

**Code.** `project-trajectory/scripts/agent_loop.py` — `load_critique_srs/build_scope_srs/critique_brief/critique_prompt`
- Decomposes: [SR-154](../system-requirements/SR-154.md)
- Verified by: [TC-048](../test-cases/TC-048.md)
