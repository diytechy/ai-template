---
type: "Low-Level Requirement"
title: "WI-registry validation"
description: "Loads work-items.csv and validates it: well-formed WI ids, resolvable predecessor refs, an acyclic dependency DAG (the cycle is named on failure) and unknown-SR warnings; plus the status.md-vs-registry SSOT rules (ssot_findings): R-A Deliverable-iff-done as a hard error, R-B..R-E status coherence + SpecRef resolution as warn / --strict-error; staged_findings adds the warn-first no-validation-delta check. Vacuous on a placeholder-only or absent registry; docs/trajectory-check: off silences the gate step."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-034)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-034) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-034 — WI-registry validation

**Detail.** Loads work-items.csv and validates it: well-formed WI ids, resolvable predecessor refs, an acyclic dependency DAG (the cycle is named on failure) and unknown-SR warnings; plus the status.md-vs-registry SSOT rules (ssot_findings): R-A Deliverable-iff-done as a hard error, R-B..R-E status coherence + SpecRef resolution as warn / --strict-error; staged_findings adds the warn-first no-validation-delta check. Vacuous on a placeholder-only or absent registry; docs/trajectory-check: off silences the gate step.

**Code.** `project-trajectory/scripts/check_trajectory.py` — `load_wis/validate/ssot_findings`
- Decomposes: [SR-037](../system-requirements/SR-037.md)
- Verified by: [TC-037](../test-cases/TC-037.md)
