---
type: "Low-Level Requirement"
title: "WI dependency-DAG validation"
description: "Loads the WI registry - the docs/work/ spec folder, the ONE home since the CSV home retired at concurrency-restructure Phase 5 (a stray work-items.csv is itself an integrity finding, never a second home) -; validates WI ids, predecessor resolution, and hard-edge acyclicity; respects the absent/placeholder/opt-out vacuity rules."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-034)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-034) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-034 — WI dependency-DAG validation

**Detail.** Loads the WI registry - the docs/work/ spec folder, the ONE home since the CSV home retired at concurrency-restructure Phase 5 (a stray work-items.csv is itself an integrity finding, never a second home) -; validates WI ids, predecessor resolution, and hard-edge acyclicity; respects the absent/placeholder/opt-out vacuity rules.

**Code.** `project-trajectory/scripts/check_trajectory.py` — `load_wis/validate`
- Decomposes: [SR-067](../system-requirements/SR-067.md)
- Verified by: [TC-037](../test-cases/TC-037.md)
