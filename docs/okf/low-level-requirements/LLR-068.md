---
type: "Low-Level Requirement"
title: "Spec-interface citation check"
description: "Specs act on declared interface boundaries (WI-191): a docs/specs/*.md file carrying an `## Interfaces` section (the specs/ README + the -000 example are skipped) must cite only IF-### seams that resolve in interfaces.csv, and a cited Status=Proposed seam must carry a non-empty rationale on its citation line - a PRESENCE check (honesty is the reviewer gap, enforcement-audit.md finding 4 + docs/rubrics/spec-interface-hygiene.md B1). WARN plain / ERROR under --strict (G2+); vacuous-until-armed (a spec with no such section is skipped), and an armed section citing no IF-### and stating no intra-module escape (PROCESS.md \u00a78) is itself a finding."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-068)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-068) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-068 — Spec-interface citation check

**Detail.** Specs act on declared interface boundaries (WI-191): a docs/specs/*.md file carrying an `## Interfaces` section (the specs/ README + the -000 example are skipped) must cite only IF-### seams that resolve in interfaces.csv, and a cited Status=Proposed seam must carry a non-empty rationale on its citation line - a PRESENCE check (honesty is the reviewer gap, enforcement-audit.md finding 4 + docs/rubrics/spec-interface-hygiene.md B1). WARN plain / ERROR under --strict (G2+); vacuous-until-armed (a spec with no such section is skipped), and an armed section citing no IF-### and stating no intra-module escape (PROCESS.md §8) is itself a finding.

**Code.** `project-trajectory/scripts/check_trajectory.py` — `spec_interface_findings/_spec_interfaces_section/_proposed_rationale_present`
- Decomposes: [SR-044](../system-requirements/SR-044.md)
- Verified by: [TC-068](../test-cases/TC-068.md)
