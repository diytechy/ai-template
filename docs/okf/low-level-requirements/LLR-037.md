---
type: "Low-Level Requirement"
title: "Per-phase cmd map + size tripwire"
description: "cmd_map parsed by parse_map (same KEY=value syntax; templates may not contain , or ; \u2014 the dispatcher wrapper covers those); session_template(phase) falls back to AGENT_CMD; every map entry preflighted (parse + executable); review-policy read via read_declared and printed in the banner; status_size_warning is pure and warn-only (limit<=0 disables)."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-037)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-037) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-037 — Per-phase cmd map + size tripwire

**Detail.** cmd_map parsed by parse_map (same KEY=value syntax; templates may not contain , or ; — the dispatcher wrapper covers those); session_template(phase) falls back to AGENT_CMD; every map entry preflighted (parse + executable); review-policy read via read_declared and printed in the banner; status_size_warning is pure and warn-only (limit<=0 disables).

**Code.** `project-trajectory/scripts/agent_loop.py` — `session_template/status_size_warning`
- Decomposes: [SR-040](../system-requirements/SR-040.md)
- Verified by: [TC-040](../test-cases/TC-040.md)
