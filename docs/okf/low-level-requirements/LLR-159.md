---
type: "Low-Level Requirement"
title: "Hook call sites read policy through the query entry point"
description: "Replace the first-non-comment-line grep of the privacy and review policy files with a call to the query entry point, treating a non-zero exit as a block."
tags: ["Planned"]
resource: "docs/requirements/low-level-requirements.csv (LLR-159)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-159) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-159 — Hook call sites read policy through the query entry point

**Detail.** Replace the first-non-comment-line grep of the privacy and review policy files with a call to the query entry point, treating a non-zero exit as a block.

**Rationale.** The hook keeps its fail-closed posture while losing its private parse convention.

**Code.** `project-trajectory/hooks/pre-commit` — `config_query`
- Decomposes: [SR-139](../system-requirements/SR-139.md)
- Verified by: [TC-153](../test-cases/TC-153.md)
