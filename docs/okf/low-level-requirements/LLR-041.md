---
type: "Low-Level Requirement"
title: "IF-tier integrity in trace.py"
description: "Reads docs/requirements/interfaces.csv and filters real (non -000) rows; integrity_findings(IF, ...) flags a malformed/duplicate IF id (the always-on floor); interface_findings(ifs, sr_ids, module_ids) makes an empty or unresolvable SR-Refs a --strict finding (the PB back-link idiom, so every seam links the spine and stays transitively TC-covered) and a ThisProject endpoint resolving to no normalized LLR Module a warn-only advisory (the LLR Module set is a partial, differently-named inventory, so check_trajectory owns the full arch-map coverage). Absent interfaces.csv is vacuous."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-041)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-041) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-041 — IF-tier integrity in trace.py

**Detail.** Reads docs/requirements/interfaces.csv and filters real (non -000) rows; integrity_findings(IF, ...) flags a malformed/duplicate IF id (the always-on floor); interface_findings(ifs, sr_ids, module_ids) makes an empty or unresolvable SR-Refs a --strict finding (the PB back-link idiom, so every seam links the spine and stays transitively TC-covered) and a ThisProject endpoint resolving to no normalized LLR Module a warn-only advisory (the LLR Module set is a partial, differently-named inventory, so check_trajectory owns the full arch-map coverage). Absent interfaces.csv is vacuous.

**Code.** `project-trajectory/scripts/trace.py` — `interface_findings/integrity_findings`
- Decomposes: [SR-044](../system-requirements/SR-044.md)
- Verified by: [TC-044](../test-cases/TC-044.md)
