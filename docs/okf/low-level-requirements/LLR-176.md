---
type: "Low-Level Requirement"
title: "The push-channel inclusion conventions, pinned as they exist"
description: "The brief composers' inclusion rule as it exists today, pinned so the convention is falsifiable while SR-175's declared-set surface is unbuilt: plan_briefs.build_surface reads ONLY the SR and IF registries (the two-file read IS the redaction boundary - status.md, log.md and every self-assessment are unreachable because the function never names them); adjudicate_brief.compose embeds oneline/name-status logs only, never commit bodies, and excludes docs/log.md by name, as do plan_briefs and intake.context_block; agent_loop.worker_prompt clips predecessor Deliverables, an --oneline --no-decorate log and a --name-status diff at declared caps; agent_loop.critique_brief embeds SR intent, TC Parameters and the rubric files. Across the whole composing path no authorship field is formatted into any prompt: a source sweep of the composing modules and the shipped prompt templates for the authorship tokens (%an, %ae, %ad, --author, git blame, git show) finds zero hits, and that sweep is a standing test rather than a one-time measurement. NOT BUILT YET, the same debt-stating pattern as LLR-172: the declared-set surface SR-175 requires (a per-row inclusion/exclusion field on docs/agents.toml, or a process.toml dial) and the planted-credential dispatch block do not exist - this row pins the conventions that declaration will state."
tags: ["Drafted"]
resource: "docs/requirements/low-level-requirements.toml (LLR-176)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-176) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-176 — The push-channel inclusion conventions, pinned as they exist

**Detail.** The brief composers' inclusion rule as it exists today, pinned so the convention is falsifiable while SR-175's declared-set surface is unbuilt: plan_briefs.build_surface reads ONLY the SR and IF registries (the two-file read IS the redaction boundary - status.md, log.md and every self-assessment are unreachable because the function never names them); adjudicate_brief.compose embeds oneline/name-status logs only, never commit bodies, and excludes docs/log.md by name, as do plan_briefs and intake.context_block; agent_loop.worker_prompt clips predecessor Deliverables, an --oneline --no-decorate log and a --name-status diff at declared caps; agent_loop.critique_brief embeds SR intent, TC Parameters and the rubric files. Across the whole composing path no authorship field is formatted into any prompt: a source sweep of the composing modules and the shipped prompt templates for the authorship tokens (%an, %ae, %ad, --author, git blame, git show) finds zero hits, and that sweep is a standing test rather than a one-time measurement. NOT BUILT YET, the same debt-stating pattern as LLR-172: the declared-set surface SR-175 requires (a per-row inclusion/exclusion field on docs/agents.toml, or a process.toml dial) and the planted-credential dispatch block do not exist - this row pins the conventions that declaration will state.

**Rationale.** Pinning the conventions BEFORE the declaration surface exists is what stops the discipline regressing while the row is Drafted: the 2026-08-17 intake measurement (docs/plans/2026-08-17-wi468-obligation-intake-options.md §1) found the rule real but held in place by nothing, the same shape as the no-stub detector's history - a real property nobody could falsify. The sweep asserts the absence class (no authorship egress) rather than any particular formatting, so a new composer joins the bar by default.

**Code.** `project-trajectory/scripts/plan_briefs.py` — `build_surface + agent_loop.worker_prompt/critique_brief + adjudicate_brief.compose + intake.context_block`
- Decomposes: [SR-175](../system-requirements/SR-175.md)
- Verified by: [TC-171](../test-cases/TC-171.md)
