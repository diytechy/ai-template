---
type: "Low-Level Requirement"
title: "Dual-plan round artifact filer"
description: "The coordinator write-side of a dual-plan round (WI-198, DP-001 plan P5): allocates the next docs/plans/DP-NNN-<slug>/ (max existing +1, deterministic), writes each stage artifact (briefs/plans/revisions/critiques/coverage/verdict) as a stable-named UTF-8 file, appends the verdict summary to docs/log.md, and files the selected plan's Plan-WI rows as queued WIs in work-items.csv - fresh sequential ids, plan-local Predecessors mapped to the new ids plus the round's parent WI on every row, empty Deliverable (R-A), SpecRef=the plan-of-record, BuildTier from a tier map (default medium) - such that check_trajectory.py passes (well-formed, acyclic). Stdlib-only; the Plan-WI parser is duplicated from plan_coverage per F5, never imported. SR home rides SR-061 pending the dual-plan structuring WI."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-074)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-074) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-074 — Dual-plan round artifact filer

**Detail.** The coordinator write-side of a dual-plan round (WI-198, DP-001 plan P5): allocates the next docs/plans/DP-NNN-<slug>/ (max existing +1, deterministic), writes each stage artifact (briefs/plans/revisions/critiques/coverage/verdict) as a stable-named UTF-8 file, appends the verdict summary to docs/log.md, and files the selected plan's Plan-WI rows as queued WIs in work-items.csv - fresh sequential ids, plan-local Predecessors mapped to the new ids plus the round's parent WI on every row, empty Deliverable (R-A), SpecRef=the plan-of-record, BuildTier from a tier map (default medium) - such that check_trajectory.py passes (well-formed, acyclic). Stdlib-only; the Plan-WI parser is duplicated from plan_coverage per F5, never imported. SR home rides SR-061 pending the dual-plan structuring WI.

**Code.** `project-trajectory/scripts/plan_artifacts.py` — `allocate_round_dir/write_stage/file_selected_wis/append_log_summary/parse_plan_wis`
- Decomposes: [SR-061](../system-requirements/SR-061.md)
- Verified by: [TC-074](../test-cases/TC-074.md)
