---
type: "Low-Level Requirement"
title: "Dashboard usability rendering"
description: "Supports the core reading tasks with low friction: a first-time reviewer reaches the project state, the next work, and how the parts connect each within one tab switch (the landing view surfaces or one-click links to each - the concrete task list lives in docs/rubrics/dashboard-usability.md); views default to a legible density (start-collapsed per the SR-051 >3 rule) and reveal detail without losing context (breadcrumb/inline panels); labels stay readable at default zoom with no clipped or overlapping text. Adjudicated against the usability rubric by the SR-047 critique loop. Byte-deterministic; --check unchanged."
tags: ["Planned"]
resource: "docs/requirements/low-level-requirements.csv (LLR-055)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-055) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-055 — Dashboard usability rendering

**Detail.** Supports the core reading tasks with low friction: a first-time reviewer reaches the project state, the next work, and how the parts connect each within one tab switch (the landing view surfaces or one-click links to each - the concrete task list lives in docs/rubrics/dashboard-usability.md); views default to a legible density (start-collapsed per the SR-051 >3 rule) and reveal detail without losing context (breadcrumb/inline panels); labels stay readable at default zoom with no clipped or overlapping text. Adjudicated against the usability rubric by the SR-047 critique loop. Byte-deterministic; --check unchanged.

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `build_html/_nav/when_view`
- Decomposes: [SR-054](../system-requirements/SR-054.md)
- Verified by: [TC-055](../test-cases/TC-055.md)
