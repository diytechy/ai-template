---
type: "Low-Level Requirement"
title: "Dashboard usability rendering"
description: "Supports the core reading tasks with low friction: a first-time reviewer reaches the project state, the next work, and how the parts connect each within one tab switch (the landing view surfaces or one-click links to each - the concrete task list lives in docs/rubrics/dashboard-usability.md); labels stay readable at default zoom with no clipped or overlapping text. Adjudicated against the usability rubric by the SR-084/SR-085 critique loop. Byte-deterministic; --check unchanged. NARROWED by the WI-300 (f) ruling (2026-07-25): the MECHANIZED cores of anchors T2 and T3 have left this LLR for child rows that name the tests owning them - T2 start-collapse -> LLR-099/TC-102, T3 breadcrumb-restores-parent -> LLR-100/TC-103. NARROWED AGAIN by the 2026-07-26 T1/T3 ruling: T3 leaves ENTIRELY (LLR-100 now owns the whole anchor, no residue here - the critique cannot verify the return path from a static shot set while TC-103 can), and T1 also leaves ENTIRELY (LLR-115/TC-120 now own the whole anchor, no residue here) - WI-315 bound it per the (f) pattern once WI-305 fixed the open T1 defect: the 2026-07-26 reword replaced the perceptual 'the entry point is obvious' with an operational bar (each of the three core reading tasks reachable in <= 1 tab switch from the landing view, its entry point a labelled nav control or a named landing surface, judged against the RENDERED artifact carrying real registry data, never the nav skeleton), which is mechanizable, so nothing perceptual is left here for T1. This LLR keeps only what a TC cannot pin, so the critique no longer re-judges what a test enforces. Do NOT restate a child's claim here: an anchor lives in exactly one row."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-055)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-055) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-055 — Dashboard usability rendering

**Detail.** Supports the core reading tasks with low friction: a first-time reviewer reaches the project state, the next work, and how the parts connect each within one tab switch (the landing view surfaces or one-click links to each - the concrete task list lives in docs/rubrics/dashboard-usability.md); labels stay readable at default zoom with no clipped or overlapping text. Adjudicated against the usability rubric by the SR-084/SR-085 critique loop. Byte-deterministic; --check unchanged. NARROWED by the WI-300 (f) ruling (2026-07-25): the MECHANIZED cores of anchors T2 and T3 have left this LLR for child rows that name the tests owning them - T2 start-collapse -> LLR-099/TC-102, T3 breadcrumb-restores-parent -> LLR-100/TC-103. NARROWED AGAIN by the 2026-07-26 T1/T3 ruling: T3 leaves ENTIRELY (LLR-100 now owns the whole anchor, no residue here - the critique cannot verify the return path from a static shot set while TC-103 can), and T1 also leaves ENTIRELY (LLR-115/TC-120 now own the whole anchor, no residue here) - WI-315 bound it per the (f) pattern once WI-305 fixed the open T1 defect: the 2026-07-26 reword replaced the perceptual 'the entry point is obvious' with an operational bar (each of the three core reading tasks reachable in <= 1 tab switch from the landing view, its entry point a labelled nav control or a named landing surface, judged against the RENDERED artifact carrying real registry data, never the nav skeleton), which is mechanizable, so nothing perceptual is left here for T1. This LLR keeps only what a TC cannot pin, so the critique no longer re-judges what a test enforces. Do NOT restate a child's claim here: an anchor lives in exactly one row.

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `build_html/_nav/when_view`
- Decomposes: [SR-054](../system-requirements/SR-054.md)
- Verified by: [TC-055](../test-cases/TC-055.md)
