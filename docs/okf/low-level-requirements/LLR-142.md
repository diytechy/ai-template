---
type: "Low-Level Requirement"
title: "Trunk-step artifact regen (dependency order)"
description: "Regenerates arch-map, okf, derived-gate, dashboard, status block and open-items as subprocesses in dependency order (derived-gate before its two consumers), skipping absent artifact families with a notice and stopping loudly at the first failure. Never commits; the caller owns the commit."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-142)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-142) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-142 — Trunk-step artifact regen (dependency order)

**Detail.** Regenerates arch-map, okf, derived-gate, dashboard, status block and open-items as subprocesses in dependency order (derived-gate before its two consumers), skipping absent artifact families with a notice and stopping loudly at the first failure. Never commits; the caller owns the commit.

**Rationale.** derive_gate runs before the dashboard/status regens because docs/gate is their input, not their output.

**Code.** `project-trajectory/scripts/trunk_step.py` — `regen`
- Decomposes: [SR-156](../system-requirements/SR-156.md)
- Verified by: [TC-135](../test-cases/TC-135.md)
