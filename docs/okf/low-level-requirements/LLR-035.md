---
type: "Low-Level Requirement"
title: "Offline project-state dashboard"
description: "Renders the root PROJECT_STATE.html as one self-contained offline page: completeness tiles (spine_stats), the SN->SR->LLR->TC icicle (arch_icicle), the layered roadmap DAG (dag_svg), the module-map view parsed from architecture.md's generated block (sw_modules; omitted without a symbol inventory), the optional CMP table (cmp_rows), the git-derived as-of stamp (_asof - last source-touching commit, never now(); ASOF_RE excludes the line from the compare), and the responsive mobile shell; deterministic bytes; --check regenerates and byte-compares (the trajectory-map gate step)."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-035)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-035) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-035 — Offline project-state dashboard

**Detail.** Renders the root PROJECT_STATE.html as one self-contained offline page: completeness tiles (spine_stats), the SN->SR->LLR->TC icicle (arch_icicle), the layered roadmap DAG (dag_svg), the module-map view parsed from architecture.md's generated block (sw_modules; omitted without a symbol inventory), the optional CMP table (cmp_rows), the git-derived as-of stamp (_asof - last source-touching commit, never now(); ASOF_RE excludes the line from the compare), and the responsive mobile shell; deterministic bytes; --check regenerates and byte-compares (the trajectory-map gate step).

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `build_html/sw_modules/_asof/arch_icicle/dag_svg`
- Decomposes: [SR-038](../system-requirements/SR-038.md)
- Verified by: [TC-038](../test-cases/TC-038.md)
