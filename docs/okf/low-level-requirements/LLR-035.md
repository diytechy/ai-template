---
type: "Low-Level Requirement"
title: "Offline project-state dashboard"
description: "Renders docs/trajectory.html as one self-contained offline page: completeness tiles (spine_stats), the SN->SR->LLR->TC icicle (arch_icicle), the layered roadmap DAG (dag_svg), and the responsive mobile shell; deterministic bytes; --check regenerates and byte-compares (the trajectory-map gate step)."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-035)"
---

# LLR-035 — Offline project-state dashboard

**Detail.** Renders docs/trajectory.html as one self-contained offline page: completeness tiles (spine_stats), the SN->SR->LLR->TC icicle (arch_icicle), the layered roadmap DAG (dag_svg), and the responsive mobile shell; deterministic bytes; --check regenerates and byte-compares (the trajectory-map gate step).

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `build_html/spine_stats/arch_icicle/dag_svg`
- Decomposes: [SR-038](../system-requirements/SR-038.md)
- Verified by: [TC-038](../test-cases/TC-038.md)
