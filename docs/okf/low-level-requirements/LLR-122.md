---
type: "Low-Level Requirement"
title: "dev-setup wires core.hooksPath"
description: "dev-setup wires the pre-commit floor (core.hooksPath) from its baseline, so the floor is active from the setup the contributor actually runs."
tags: ["Approved"]
resource: "docs/requirements/low-level-requirements.toml (LLR-122)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-122) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-122 — dev-setup wires core.hooksPath

**Detail.** dev-setup wires the pre-commit floor (core.hooksPath) from its baseline, so the floor is active from the setup the contributor actually runs.

**Rationale.** Split from the template row: reaching a green setup and being protected by the process floor fail independently, and the second is the one a contributor never notices is missing.

**Code.** `project-trajectory/scripts/dev-setup.template.sh` — `dev-setup`
- Decomposes: [SR-113](../system-requirements/SR-113.md)
- Verified by: [TC-032](../test-cases/TC-032.md)
