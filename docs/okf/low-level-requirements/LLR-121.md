---
type: "Low-Level Requirement"
title: "Kit-version stamp"
description: "write_kit_version stamps docs/kit-version (SHA + date, -dirty on an uncommitted kit tree) so a re-sync is diffable."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-121)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-121) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-121 — Kit-version stamp

**Detail.** write_kit_version stamps docs/kit-version (SHA + date, -dirty on an uncommitted kit tree) so a re-sync is diffable.

**Rationale.** Split from the idempotent-write row: the two obligations share a module but not a failure mode — a scaffold can write correctly and still leave the adopter unable to say what they scaffolded from.

**Code.** `project-trajectory/scripts/bootstrap.py` — `write_kit_version`
- Decomposes: [SR-111](../system-requirements/SR-111.md)
- Verified by: [TC-011](../test-cases/TC-011.md)
