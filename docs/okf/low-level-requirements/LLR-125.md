---
type: "Low-Level Requirement"
title: "Atomic reservations + worktree leasing"
description: "Reserves constituents atomically via refs/llm/reservations/WI-### (commit-tree metadata + update-ref --stdin zero-old-value); leases worktrees; rescans for dynamic refill; honors pause/blackout/capacity."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-125)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-125) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-125 — Atomic reservations + worktree leasing

**Detail.** Reserves constituents atomically via refs/llm/reservations/WI-### (commit-tree metadata + update-ref --stdin zero-old-value); leases worktrees; rescans for dynamic refill; honors pause/blackout/capacity.

**Rationale.** Split from the launch row: the launch sequence can be correct while the claim is not atomic, and that failure is two workers building the same WI rather than a lane that never starts.

**Code.** `project-trajectory/scripts/agent_dispatch.py` — `reserve_traincar/pack_traincars`
- Decomposes: [SR-117](../system-requirements/SR-117.md)
- Verified by: [TC-062](../test-cases/TC-062.md)
