---
type: "Low-Level Requirement"
title: "Uniform node/edge/legend/highlight styling (U3 core)"
description: "The MECHANIZED core of uniformity anchor U3 (docs/rubrics/dashboard-uniformity.md), split out of LLR-054 under the WI-300 (f) ruling and delivered by WI-294 (117-CRITIQUE, 119-CRITIQUE): the hover/focus highlight ring resolves to ONE shared per-fill `--ring` custom property across every SVG emitter (drill/icicle/flat-DAG/knowledge), not a different hue per emitter (was var(--accent) in the drill views, a hardcoded amber elsewhere); and the When tab's phase-accent key renders through the SAME `.legend`/`<i>` component every other legend in the document uses, not a bespoke smaller inline-chip idiom. SCOPE: highlight-token equality and legend-component equality, asserted structurally against the emitted CSS/markup. Whether a reader perceives the RESULT as uniform (spacing, exact visual weight) remains perceptual under LLR-054/TC-054."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-103)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-103) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-103 — Uniform node/edge/legend/highlight styling (U3 core)

**Detail.** The MECHANIZED core of uniformity anchor U3 (docs/rubrics/dashboard-uniformity.md), split out of LLR-054 under the WI-300 (f) ruling and delivered by WI-294 (117-CRITIQUE, 119-CRITIQUE): the hover/focus highlight ring resolves to ONE shared per-fill `--ring` custom property across every SVG emitter (drill/icicle/flat-DAG/knowledge), not a different hue per emitter (was var(--accent) in the drill views, a hardcoded amber elsewhere); and the When tab's phase-accent key renders through the SAME `.legend`/`<i>` component every other legend in the document uses, not a bespoke smaller inline-chip idiom. SCOPE: highlight-token equality and legend-component equality, asserted structurally against the emitted CSS/markup. Whether a reader perceives the RESULT as uniform (spacing, exact visual weight) remains perceptual under LLR-054/TC-054.

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `_ring_ink/_ring_style/when_view legend`
- Decomposes: [SR-053](../system-requirements/SR-053.md)
- Verified by: [TC-106](../test-cases/TC-106.md)
