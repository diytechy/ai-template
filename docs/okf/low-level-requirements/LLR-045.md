---
type: "Low-Level Requirement"
title: "Managed review dispatch"
description: "Schedules review-policy sessions in managed mode, constructs redacted prompt-map briefs, parses verdicts, logs selections, and preserves unmanaged behavior. A declared review policy of N schedules N reviewer sessions (0/1/2 -> zero/one/two); a broken prompt map fails preflight before any session launches; an absent enable-list keeps the legacy unmanaged behavior."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-045)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-045) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-045 — Managed review dispatch

**Detail.** Schedules review-policy sessions in managed mode, constructs redacted prompt-map briefs, parses verdicts, logs selections, and preserves unmanaged behavior. A declared review policy of N schedules N reviewer sessions (0/1/2 -> zero/one/two); a broken prompt map fails preflight before any session launches; an absent enable-list keeps the legacy unmanaged behavior.

**Code.** `project-trajectory/scripts/agent_loop.py` — `main/reviewer_prompt/phase_tier`
- Decomposes: [SR-154](../system-requirements/SR-154.md)
- Verified by: [TC-082](../test-cases/TC-082.md)
