---
type: "Low-Level Requirement"
title: "Hook python probe"
description: "Probes candidate interpreters by RUNNING one \u2014 never by testing for the name on PATH, which a Windows Store alias satisfies while exiting nonzero \u2014 and skip-or-reports clearly when none works, so neither hook crashes cryptically on a box without a resolvable python3. Shared by pre-commit and pre-push (the module named here is the representative home; both hooks read the same probe). The equivalent obligation on the ROOT LAUNCHERS is stated on SR-160, not here: retired SR-021 spanned both audiences at once, and the launcher half is an observable of the front door rather than machinery of the hook floor."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-021)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-021) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-021 — Hook python probe

**Detail.** Probes candidate interpreters by RUNNING one — never by testing for the name on PATH, which a Windows Store alias satisfies while exiting nonzero — and skip-or-reports clearly when none works, so neither hook crashes cryptically on a box without a resolvable python3. Shared by pre-commit and pre-push (the module named here is the representative home; both hooks read the same probe). The equivalent obligation on the ROOT LAUNCHERS is stated on SR-160, not here: retired SR-021 spanned both audiences at once, and the launcher half is an observable of the front door rather than machinery of the hook floor.

**Code.** `project-trajectory/hooks/pre-push` — `python3 probe`
- Decomposes: [SR-019](../system-requirements/SR-019.md) · [SR-020](../system-requirements/SR-020.md)
- Verified by: [TC-021](../test-cases/TC-021.md)
