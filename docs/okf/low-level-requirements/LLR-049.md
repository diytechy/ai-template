---
type: "Low-Level Requirement"
title: "How-SW top-view containment rule + render"
description: "component_top_view is the one home for the module->CMP join (arch_inventory modules x load_cmps CMP rows x module_components LLR Component tags; _cmp_roots resolves PartOf to top-level roots, cycle-guarded); component_findings bounds the top view at TOP_VIEW_MAX=10 (top-level roots containing a module + uncontained modules), warn plain and error under --strict, opt-out via docs/components-check and vacuous for a <=10-module or arch-map-less inventory; gen_trajectory.sw_containment imports the same derivation to render the containerized <details> top view (expandable members + nested children + boundary-aggregated cross-component seams; flat fallback when no CMP contains a module), and build_html routes software CMPs to How-SW and non-software CMPs to the How-physical table."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-049)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-049) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-049 — How-SW top-view containment rule + render

**Detail.** component_top_view is the one home for the module->CMP join (arch_inventory modules x load_cmps CMP rows x module_components LLR Component tags; _cmp_roots resolves PartOf to top-level roots, cycle-guarded); component_findings bounds the top view at TOP_VIEW_MAX=10 (top-level roots containing a module + uncontained modules), warn plain and error under --strict, opt-out via docs/components-check and vacuous for a <=10-module or arch-map-less inventory; gen_trajectory.sw_containment imports the same derivation to render the containerized <details> top view (expandable members + nested children + boundary-aggregated cross-component seams; flat fallback when no CMP contains a module), and build_html routes software CMPs to How-SW and non-software CMPs to the How-physical table.

**Code.** `project-trajectory/scripts/check_trajectory.py` — `component_top_view/component_findings/module_components/load_cmps/_cmp_roots`
- Decomposes: [SR-048](../system-requirements/SR-048.md)
- Verified by: [TC-049](../test-cases/TC-049.md)
