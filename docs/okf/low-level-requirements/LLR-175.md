---
type: "Low-Level Requirement"
title: "Change-group classification for a partial attempt"
description: "Group a branch's changes by the parent directory of each changed path, grouping a rename by its new path and exempting the bookkeeping prefixes whole, then require a keep, discard or quarantine label per group and return the unlabelled groups as refusals."
tags: ["Planned"]
resource: "docs/requirements/low-level-requirements.csv (LLR-175)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-175) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-175 — Change-group classification for a partial attempt

**Detail.** Group a branch's changes by the parent directory of each changed path, grouping a rename by its new path and exempting the bookkeeping prefixes whole, then require a keep, discard or quarantine label per group and return the unlabelled groups as refusals.

**Rationale.** Returning the unlabelled set names what the adjudicator still owes.

**Code.** `project-trajectory/scripts/outcome.py` — `classify_groups`
- Decomposes: [SR-150](../system-requirements/SR-150.md)
- Verified by: [TC-169](../test-cases/TC-169.md)
