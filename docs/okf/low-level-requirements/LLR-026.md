---
type: "Low-Level Requirement"
title: "Headless session launch"
description: "Runs the agent CLI headless: build_argv substitutes {model}/{prompt} into argv, or - when a CmdTemplate carries no {prompt} placeholder - routes the prompt to the child's STDIN (run_session writes it from a daemon thread then closes), keeping the command line short enough to survive the Windows codex.CMD 8191-char cmd.exe cap while the no-wedge invariant still holds (a fixed prompt + EOF, never an interactive read; WI-216). stdin is otherwise closed so no prompt wedges the loop (WI-210: the scope is the dispatcher + worker sessions; the status.md-resume read is retired)."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-026)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-026) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-026 — Headless session launch

**Detail.** Runs the agent CLI headless: build_argv substitutes {model}/{prompt} into argv, or - when a CmdTemplate carries no {prompt} placeholder - routes the prompt to the child's STDIN (run_session writes it from a daemon thread then closes), keeping the command line short enough to survive the Windows codex.CMD 8191-char cmd.exe cap while the no-wedge invariant still holds (a fixed prompt + EOF, never an interactive read; WI-216). stdin is otherwise closed so no prompt wedges the loop (WI-210: the scope is the dispatcher + worker sessions; the status.md-resume read is retired).

**Code.** `project-trajectory/scripts/agent_loop.py` — `read_declared/main/build_argv/run_session`
- Decomposes: [SR-026](../system-requirements/SR-026.md)
- Verified by: [TC-026](../test-cases/TC-026.md)
