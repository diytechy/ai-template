---
type: "Low-Level Requirement"
title: "Link + vision-tag checker"
description: "Resolves intra-repo links, requires a PROJECT-VISION tag, and checks generated-doc freshness under --stale. A dead intra-repo link or an absent PROJECT-VISION tag fails; a clean doc set passes. Wired as check.py's built-in doc-navigability step, running from DevStg-Reqs on; orphans warn unless --strict-orphans, with docs/orphans-allow as the reviewed carve-out, and --stale is warn-only."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-012)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-012) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-012 — Link + vision-tag checker

**Detail.** Resolves intra-repo links, requires a PROJECT-VISION tag, and checks generated-doc freshness under --stale. A dead intra-repo link or an absent PROJECT-VISION tag fails; a clean doc set passes. Wired as check.py's built-in doc-navigability step, running from DevStg-Reqs on; orphans warn unless --strict-orphans, with docs/orphans-allow as the reviewed carve-out, and --stale is warn-only.

**Code.** `project-trajectory/scripts/check_docs.py` — `main`
- Decomposes: [SR-158](../system-requirements/SR-158.md)
- Verified by: [TC-012](../test-cases/TC-012.md)
