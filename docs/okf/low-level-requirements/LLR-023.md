---
type: "Low-Level Requirement"
title: "Arch-map splice + drift check"
description: "Regenerates the symbol map inside the BEGIN/END marker block and fails --check when the committed map has drifted."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-023)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-023) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-023 — Arch-map splice + drift check

**Detail.** Regenerates the symbol map inside the BEGIN/END marker block and fails --check when the committed map has drifted.

**Code.** `project-trajectory/scripts/gen_arch_map.py` — `build_map/splice_region`
- Decomposes: [SR-023](../system-requirements/SR-023.md)
- Verified by: [TC-023](../test-cases/TC-023.md)
