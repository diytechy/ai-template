---
type: "Low-Level Requirement"
title: "Tiered When/How-SW drill-down"
description: "Generalizes the shipped containment idioms into count-thresholded tiers: the When view groups phase -> workstream -> work item (each tier rendered as a collapsed native-details block when its member count exceeds 3, flat at or below; campaign containers kept at the bottom tier), carries the phase encoding (grouping-primary + per-phase color accent on lower-tier nodes), and draws one deduped parent-to-parent edge per crossing pair aggregated from the union of child edges; the How-SW view starts at component blocks when top-level components exceed 3 and explodes into modules (the TOP_VIEW_MAX right-sizing bound unchanged); in-place expand only, no zoom navigation; deterministic bytes; --check unchanged."
tags: ["Implemented"]
resource: "docs/requirements/low-level-requirements.csv (LLR-052)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-052) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-052 — Tiered When/How-SW drill-down

**Detail.** Generalizes the shipped containment idioms into count-thresholded tiers: the When view groups phase -> workstream -> work item (each tier rendered as a collapsed native-details block when its member count exceeds 3, flat at or below; campaign containers kept at the bottom tier), carries the phase encoding (grouping-primary + per-phase color accent on lower-tier nodes), and draws one deduped parent-to-parent edge per crossing pair aggregated from the union of child edges; the How-SW view starts at component blocks when top-level components exceed 3 and explodes into modules (the TOP_VIEW_MAX right-sizing bound unchanged); in-place expand only, no zoom navigation; deterministic bytes; --check unchanged.

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `when_view/_wi_phases/sw_containment`
- Decomposes: [SR-051](../system-requirements/SR-051.md)
- Verified by: [TC-052](../test-cases/TC-052.md)
