---
type: "Low-Level Requirement"
title: "Git-as-authority crash recovery"
description: "Treats out/dispatch/ as cache; on startup acquires the lock and reconstructs one owner/state per WI+train from Git (integrated disposition from llm/integration authoritative; llm/train/*, llm/integrate/*, refs/llm/reservations/*, worktrees, reservation commits, exact-head reviews), quarantines ambiguity/dupes, fails closed on unprovable ownership; recovers a crash at every boundary and with out/dispatch/ deleted."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-065)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-065) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-065 — Git-as-authority crash recovery

**Detail.** Treats out/dispatch/ as cache; on startup acquires the lock and reconstructs one owner/state per WI+train from Git (integrated disposition from llm/integration authoritative; llm/train/*, llm/integrate/*, refs/llm/reservations/*, worktrees, reservation commits, exact-head reviews), quarantines ambiguity/dupes, fails closed on unprovable ownership; recovers a crash at every boundary and with out/dispatch/ deleted.

**Code.** `project-trajectory/scripts/agent_loop.py` — `recover/reconstruct_state`
- Decomposes: [SR-064](../system-requirements/SR-064.md)
- Verified by: [TC-065](../test-cases/TC-065.md)
