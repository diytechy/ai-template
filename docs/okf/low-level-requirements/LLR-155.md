---
type: "Low-Level Requirement"
title: "Typed configuration schema and loader"
description: "Parse docs/config.toml with tomllib into a Config dataclass; walk the declared schema table to type-check every key; collect refusals as (key, reason) pairs rather than raising on the first one."
tags: ["Planned"]
resource: "docs/requirements/low-level-requirements.csv (LLR-155)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-155) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-155 — Typed configuration schema and loader

**Detail.** Parse docs/config.toml with tomllib into a Config dataclass; walk the declared schema table to type-check every key; collect refusals as (key, reason) pairs rather than raising on the first one.

**Rationale.** One walk over a declared schema keeps the validator and the documentation from drifting apart.

**Code.** `project-trajectory/scripts/config.py` — `load_config`
- Decomposes: [SR-137](../system-requirements/SR-137.md)
- Verified by: [TC-150](../test-cases/TC-150.md)
