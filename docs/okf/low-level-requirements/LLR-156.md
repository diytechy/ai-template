---
type: "Low-Level Requirement"
title: "Declared defaults for an absent configuration"
description: "Hold every dial's default in one table beside its type and its documentation string, so an absent file and an absent key resolve identically."
tags: ["Planned"]
resource: "docs/requirements/low-level-requirements.csv (LLR-156)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-156) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-156 — Declared defaults for an absent configuration

**Detail.** Hold every dial's default in one table beside its type and its documentation string, so an absent file and an absent key resolve identically.

**Rationale.** A default that lives in the reader rather than the schema is invisible to the adopter.

**Code.** `project-trajectory/scripts/config.py` — `DEFAULTS`
- Decomposes: [SR-137](../system-requirements/SR-137.md)
- Verified by: [TC-150](../test-cases/TC-150.md)
