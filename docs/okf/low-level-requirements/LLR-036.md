---
type: "Low-Level Requirement"
title: "Duplicate-block detector"
description: "Tokenizes each source file (stdlib tokenize; comments/NL/indent excluded, NEWLINE kept so windows cannot stitch unrelated statements), slides a --min-tokens window (default 30), merges overlapping hits by line offset into one finding per block, POSIX-normalizes the file pair, and suppresses pairs recorded in docs/dupes-allow."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-036)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-036) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-036 — Duplicate-block detector

**Detail.** Tokenizes each source file (stdlib tokenize; comments/NL/indent excluded, NEWLINE kept so windows cannot stitch unrelated statements), slides a --min-tokens window (default 30), merges overlapping hits by line offset into one finding per block, POSIX-normalizes the file pair, and suppresses pairs recorded in docs/dupes-allow.

**Code.** `project-trajectory/scripts/check_dupes.py` — `find_duplicates/significant_tokens`
- Decomposes: [SR-039](../system-requirements/SR-039.md)
- Verified by: [TC-039](../test-cases/TC-039.md)
