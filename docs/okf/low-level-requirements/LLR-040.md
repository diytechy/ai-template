---
type: "Low-Level Requirement"
title: "Subagent spawn gate"
description: "Pure decide(tool_name, policy, override) -> allow|ask|deny|defer over SPAWN_TOOLS={Task,Agent}; main reads the PreToolUse JSON payload, reads docs/process.toml [checks] subagent_gate (falling back to the legacy one-word docs/subagent-gate through the migration window), applies the SUBAGENT_GATE=allow override, emits permissionDecision (deny -> exit 2), and appends each decision to out/subagent-gate.log; any exception fails OPEN (allow) and logs, so a broken gate never wedges the agent's tools."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-040)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-040) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-040 — Subagent spawn gate

**Detail.** Pure decide(tool_name, policy, override) -> allow|ask|deny|defer over SPAWN_TOOLS={Task,Agent}; main reads the PreToolUse JSON payload, reads docs/process.toml [checks] subagent_gate (falling back to the legacy one-word docs/subagent-gate through the migration window), applies the SUBAGENT_GATE=allow override, emits permissionDecision (deny -> exit 2), and appends each decision to out/subagent-gate.log; any exception fails OPEN (allow) and logs, so a broken gate never wedges the agent's tools.

**Code.** `project-trajectory/scripts/subagent_gate.py` — `decide/main`
- Decomposes: [SR-043](../system-requirements/SR-043.md)
- Verified by: [TC-043](../test-cases/TC-043.md)
