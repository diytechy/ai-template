---
type: "Low-Level Requirement"
title: "Off-spine integrity + back-links"
description: "Integrity-checks the PB/REPO/PART/ASSET ids plus legacy MOD compatibility and resolves REPO/MOD DelegatedSRs against the SR set; -000 example rows ignored. Resolves each PB row's Refs to a real SR/LLR/Module id \u2014 an unresolvable or empty Refs is a finding; this is the CHECKER half of the deliberate pair with SR-015's delivered-data invariant, an on-purpose split, not an echo. An unused optional registry never blocks: absent registries are vacuous."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-005)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-005) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-005 — Off-spine integrity + back-links

**Detail.** Integrity-checks the PB/REPO/PART/ASSET ids plus legacy MOD compatibility and resolves REPO/MOD DelegatedSRs against the SR set; -000 example rows ignored. Resolves each PB row's Refs to a real SR/LLR/Module id — an unresolvable or empty Refs is a finding; this is the CHECKER half of the deliberate pair with SR-015's delivered-data invariant, an on-purpose split, not an echo. An unused optional registry never blocks: absent registries are vacuous.

**Code.** `project-trajectory/scripts/trace.py` — `integrity_findings/module_findings`
- Decomposes: [SR-157](../system-requirements/SR-157.md)
- Verified by: [TC-005](../test-cases/TC-005.md)
