---
type: "Low-Level Requirement"
title: "Explicit WI/train/worktree worker assignment"
description: "Replaces internal --track assumptions with explicit --wi/--train/worktree assignment; assembles the worker prompt from AGENTS.md + WI row + SpecRef + predecessor context + train diff + rework finding (never status.md/next-wi); collision-safe per-assignment logs/review evidence naming the exact reviewed commit; legacy --track warns deprecated for one window."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-061)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-061) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-061 — Explicit WI/train/worktree worker assignment

**Detail.** Replaces internal --track assumptions with explicit --wi/--train/worktree assignment; assembles the worker prompt from AGENTS.md + WI row + SpecRef + predecessor context + train diff + rework finding (never status.md/next-wi); collision-safe per-assignment logs/review evidence naming the exact reviewed commit; legacy --track warns deprecated for one window.

**Code.** `project-trajectory/scripts/agent_loop.py` — `assign_worker/worker_prompt`
- Decomposes: [SR-060](../system-requirements/SR-060.md)
- Verified by: [TC-061](../test-cases/TC-061.md)
