---
type: "Low-Level Requirement"
title: "Kernel advisory lock"
description: "Takes an OS advisory lock the kernel releases on process death, so no stale-pid file wedges the next run. The lock scope is per-worktree \u2014 the dispatcher, each worker and an interactive sitting take the same lock for their checkout."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-029)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-029) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-029 — Kernel advisory lock

**Detail.** Takes an OS advisory lock the kernel releases on process death, so no stale-pid file wedges the next run. The lock scope is per-worktree — the dispatcher, each worker and an interactive sitting take the same lock for their checkout.

**Code.** `project-trajectory/scripts/agent_common.py` — `acquire_lock/_take_os_lock`
- Decomposes: [SR-030](../system-requirements/SR-030.md)
- Verified by: [TC-029](../test-cases/TC-029.md)
