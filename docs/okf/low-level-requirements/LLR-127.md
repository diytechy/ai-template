---
type: "Low-Level Requirement"
title: "Release unstarted reservations on early end"
description: "Releases unstarted constituent reservations transactionally and recomputes the traincar DAG on any early end."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-127)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-127) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-127 — Release unstarted reservations on early end

**Detail.** Releases unstarted constituent reservations transactionally and recomputes the traincar DAG on any early end.

**Rationale.** Split from the continuation row: this is the cleanup path, and a cleanup that rides on the happy path's row gets tested only when the happy path is.

**Code.** `project-trajectory/scripts/agent_loop.py` — `continue_train`
- Decomposes: [SR-119](../system-requirements/SR-119.md)
- Verified by: [TC-063](../test-cases/TC-063.md)
