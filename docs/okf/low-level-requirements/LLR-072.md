---
type: "Low-Level Requirement"
title: "Dual-plan planner pair and fallback"
description: "Selects two fresh planner sessions across families where possible and applies the bounded nonresponse fallback with reason codes. A one-family or single-row pool still yields two fresh sessions with reason codes; a runtime nonresponse excludes the dead family; a no-responder outcome is explicit, never silent."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-072)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-072) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-072 — Dual-plan planner pair and fallback

**Detail.** Selects two fresh planner sessions across families where possible and applies the bounded nonresponse fallback with reason codes. A one-family or single-row pool still yields two fresh sessions with reason codes; a runtime nonresponse excludes the dead family; a no-responder outcome is explicit, never silent.

**Code.** `project-trajectory/scripts/agent_route.py` — `planner_pair/planner_fallback`
- Decomposes: [SR-155](../system-requirements/SR-155.md)
- Verified by: [TC-072](../test-cases/TC-072.md)
