---
type: "Low-Level Requirement"
title: "Pairwise deltaE floor across every vocabulary (U5 residue)"
description: "Mechanizes what LLR-102 left perceptual (WI-311). The repo already computed pairwise deltaE but only WITHIN PHASE_ACCENTS, which is the only reason 'a collision the identity check misses' looked like a judgement call: it is the same formula on a set nobody had widened. Two floors, each a recorded judgement - 15 WITHIN a vocabulary (every member can sit beside every other in one legend) and 12 ACROSS (two vocabularies share no legend, but 120-CRITIQUE reported a reader conflating exactly such a pair, so the floor is lower, not absent). The declared tier<->okf mirror is exempt BY DESIGN and is asserted BYTE-EQUAL, so 'exempt' cannot drift into 'nearly equal'. Found and fixed three live conflations: sw external/component 8.6, SR/phase[3] 9.5, Interface/phase[7] 10.5. SCOPE: perceptual SEPARATION of the declared palettes. Whether a given hue suits its concept is not asserted."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-109)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-109) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-109 — Pairwise deltaE floor across every vocabulary (U5 residue)

**Detail.** Mechanizes what LLR-102 left perceptual (WI-311). The repo already computed pairwise deltaE but only WITHIN PHASE_ACCENTS, which is the only reason 'a collision the identity check misses' looked like a judgement call: it is the same formula on a set nobody had widened. Two floors, each a recorded judgement - 15 WITHIN a vocabulary (every member can sit beside every other in one legend) and 12 ACROSS (two vocabularies share no legend, but 120-CRITIQUE reported a reader conflating exactly such a pair, so the floor is lower, not absent). The declared tier<->okf mirror is exempt BY DESIGN and is asserted BYTE-EQUAL, so 'exempt' cannot drift into 'nearly equal'. Found and fixed three live conflations: sw external/component 8.6, SR/phase[3] 9.5, Interface/phase[7] 10.5. SCOPE: perceptual SEPARATION of the declared palettes. Whether a given hue suits its concept is not asserted.

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `STATUS_FILL/TIER_FILL/OKF_TYPE_FILL/SW_NODE_FILL/PHASE_ACCENTS`
- Decomposes: [SR-053](../system-requirements/SR-053.md)
- Verified by: [TC-114](../test-cases/TC-114.md)
