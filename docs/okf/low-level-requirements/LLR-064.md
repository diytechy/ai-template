---
type: "Low-Level Requirement"
title: "Atomic integrator + CAS + blocked-disposition + publish"
description: "Single-writer integration on refs/heads/llm/integration (dispatcher worktree only): compose from HEAD, verify reservation/scope/review, resolve overlap (re-review material conflict), regenerate registry/log/status plus any present OKF then trajectory artifacts, run the combined/gate bar, and CAS-advance; blocked and dual-plan dispositions use the same freshness order before commit, and error resets first best-effort salvage DP-* evidence differing from the reset target (uncommitted or already committed past it). A dual-plan SELECT whose present generator rejects the filed rows fails closed before commit/CAS, surfaces the validator tail, and salvages the round instead of making a known-invalid registry authoritative. Publication to the clean dev worktree uses a second CAS + ff/reset sync only when the integration target descends from the current dev head; a diverged launch records both heads and pages NEEDS-HUMAN before work or publication."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-064)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-064) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-064 — Atomic integrator + CAS + blocked-disposition + publish

**Detail.** Single-writer integration on refs/heads/llm/integration (dispatcher worktree only): compose from HEAD, verify reservation/scope/review, resolve overlap (re-review material conflict), regenerate registry/log/status plus any present OKF then trajectory artifacts, run the combined/gate bar, and CAS-advance; blocked and dual-plan dispositions use the same freshness order before commit, and error resets first best-effort salvage DP-* evidence differing from the reset target (uncommitted or already committed past it). A dual-plan SELECT whose present generator rejects the filed rows fails closed before commit/CAS, surfaces the validator tail, and salvages the round instead of making a known-invalid registry authoritative. Publication to the clean dev worktree uses a second CAS + ff/reset sync only when the integration target descends from the current dev head; a diverged launch records both heads and pages NEEDS-HUMAN before work or publication.

**Code.** `project-trajectory/scripts/agent_dispatch.py` — `integrate_train/cas_ref/publish_integration`
- Decomposes: [SR-063](../system-requirements/SR-063.md)
- Verified by: [TC-064](../test-cases/TC-064.md)
