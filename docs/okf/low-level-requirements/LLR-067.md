---
type: "Low-Level Requirement"
title: "Cross-CMP-edge-without-IF finding"
description: "The AXES enforceability rule mechanized (WI-064): arch_inventory harvests the committed MODULE MAP block's `Imports (internal):` lines as the edge source; an import edge whose endpoints resolve (unique stem) to modules in DIFFERENT CMP-### components with no interfaces.csv row covering the pair (normalized ThisProject/Counterpart, either direction) is a finding, wired as component_findings' third rule \u2014 WARN plain / ERROR under --strict (G2+), sharing the docs/components-check opt-out. Vacuous by construction when any input is absent: no Imports lines, no real CMP rows, an endpoint with no Component-tag membership (the containment rule's job), or an unknown/ambiguous stem."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-067)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-067) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-067 — Cross-CMP-edge-without-IF finding

**Detail.** The AXES enforceability rule mechanized (WI-064): arch_inventory harvests the committed MODULE MAP block's `Imports (internal):` lines as the edge source; an import edge whose endpoints resolve (unique stem) to modules in DIFFERENT CMP-### components with no interfaces.csv row covering the pair (normalized ThisProject/Counterpart, either direction) is a finding, wired as component_findings' third rule — WARN plain / ERROR under --strict (G2+), sharing the docs/components-check opt-out. Vacuous by construction when any input is absent: no Imports lines, no real CMP rows, an endpoint with no Component-tag membership (the containment rule's job), or an unknown/ambiguous stem.

**Code.** `project-trajectory/scripts/check_trajectory.py` — `cross_component_findings/arch_inventory`
- Decomposes: [SR-044](../system-requirements/SR-044.md)
- Verified by: [TC-067](../test-cases/TC-067.md)
