---
type: "Low-Level Requirement"
title: "Cross-component import coverage"
description: "Joins imports, component membership, and declared seams; reports uncovered cross-component edges with strict/plain and vacuity rules. Uncovered mapped edges warn plain and fail --strict; a covering seam (in either direction), an intra-component or unmapped edge, absent imports, and the opt-out each stay silent. The module inventory joined against is the generated block gen_arch_map.py maintains in docs/architecture.md."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-067)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-067) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-067 — Cross-component import coverage

**Detail.** Joins imports, component membership, and declared seams; reports uncovered cross-component edges with strict/plain and vacuity rules. Uncovered mapped edges warn plain and fail --strict; a covering seam (in either direction), an intra-component or unmapped edge, absent imports, and the opt-out each stay silent. The module inventory joined against is the generated block gen_arch_map.py maintains in docs/architecture.md.

**Code.** `project-trajectory/scripts/check_trajectory.py` — `cross_component_findings/arch_inventory`
- Decomposes: [SR-159](../system-requirements/SR-159.md)
- Verified by: [TC-067](../test-cases/TC-067.md)
