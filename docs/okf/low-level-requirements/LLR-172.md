---
type: "Low-Level Requirement"
title: "Scope digest over the frozen spec region"
description: "Hash the declared scope-bearing frontmatter keys and the acceptance body of a spec, ignoring the fields a terminal move legitimately changes."
tags: ["Planned"]
resource: "docs/requirements/low-level-requirements.csv (LLR-172)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-172) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-172 — Scope digest over the frozen spec region

**Detail.** Hash the declared scope-bearing frontmatter keys and the acceptance body of a spec, ignoring the fields a terminal move legitimately changes.

**Rationale.** Hashing only the frozen region lets the legitimate close edits proceed.

**Code.** `project-trajectory/scripts/outcome.py` — `scope_digest`
- Decomposes: [SR-148](../system-requirements/SR-148.md)
- Verified by: [TC-166](../test-cases/TC-166.md)
