---
type: "Low-Level Requirement"
title: "Hierarchy seam ports"
description: "Attaches IF seams to visible input/output ports and aggregates cross-container seams at boundaries. At every visible tier seams terminate at the correct ports, and boundary aggregation adds no duplicates."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-087)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-087) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-087 — Hierarchy seam ports

**Detail.** Attaches IF seams to visible input/output ports and aggregates cross-container seams at boundaries. At every visible tier seams terminate at the correct ports, and boundary aggregation adds no duplicates.

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `_drill_svg/_drill_edges`
- Decomposes: [SR-169](../system-requirements/SR-169.md)
- Verified by: [TC-089](../test-cases/TC-089.md)
