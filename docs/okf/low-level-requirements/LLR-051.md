---
type: "Low-Level Requirement"
title: "Process reference tab"
description: "Renders the Process tab as a fully self-contained conditional panel registered in build_html (the Knowledge-tab pattern: nav button + section id=process): (1) the artifact-lifecycle x gates panel derived from the live registries + docs/gate (current-gate highlight; per-tier counts from the spine), (2) the resume-loop panel (the managed agent_loop flow with its escalation edges), (3) the slices -> campaigns -> gates panel derived from work-items.csv campaign bins; links out to the process masters wherever a canonical home exists (bounded in-view restatement only where no single doc states the relationship); sorted inputs, no clocks; a data-less repo renders byte-identically; --check unchanged (the trajectory-map step)."
tags: ["Implemented"]
resource: "docs/requirements/low-level-requirements.csv (LLR-051)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-051) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-051 — Process reference tab

**Detail.** Renders the Process tab as a fully self-contained conditional panel registered in build_html (the Knowledge-tab pattern: nav button + section id=process): (1) the artifact-lifecycle x gates panel derived from the live registries + docs/gate (current-gate highlight; per-tier counts from the spine), (2) the resume-loop panel (the managed agent_loop flow with its escalation edges), (3) the slices -> campaigns -> gates panel derived from work-items.csv campaign bins; links out to the process masters wherever a canonical home exists (bounded in-view restatement only where no single doc states the relationship); sorted inputs, no clocks; a data-less repo renders byte-identically; --check unchanged (the trajectory-map step).

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `process_panel/build_html`
- Decomposes: [SR-050](../system-requirements/SR-050.md)
- Verified by: [TC-051](../test-cases/TC-051.md)
