---
type: "Low-Level Requirement"
title: "Serial trunk step (log compile + regen)"
description: "Compiles docs/log.d/*.md into docs/log.md via plan_artifacts.append_log_summary in (git add-time, filename) order - all fragments validated first (name shape, ## heading, reserved section headings refused, an adding commit required - on HEAD, or on MERGE_HEAD during the integrator's merge composition), relative links rebased from docs/log.d/ to docs/, fragments deleted after append, all-or-nothing on any validation error. Never commits; the caller owns the commit. The artifact-regen half is LLR-142 (SR-134)."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-137)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-137) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-137 — Serial trunk step (log compile + regen)

**Detail.** Compiles docs/log.d/*.md into docs/log.md via plan_artifacts.append_log_summary in (git add-time, filename) order - all fragments validated first (name shape, ## heading, reserved section headings refused, an adding commit required - on HEAD, or on MERGE_HEAD during the integrator's merge composition), relative links rebased from docs/log.d/ to docs/, fragments deleted after append, all-or-nothing on any validation error. Never commits; the caller owns the commit. The artifact-regen half is LLR-142 (SR-134).

**Rationale.** One serial actor per §5.5.

**Code.** `project-trajectory/scripts/trunk_step.py` — `compile_log/ordered_fragments/rebase_links`
- Decomposes: [SR-130](../system-requirements/SR-130.md)
- Verified by: [TC-130](../test-cases/TC-130.md)
