---
type: "Low-Level Requirement"
title: "The mechanical queue-overlap pre-filter"
description: "Three signals over OPEN rows only \u2014 Jaccard over title token sets at a deliberately high bar, shared `SR-Refs`, and a shared anchor-stripped `SpecRef` \u2014 each reported once per pair per signal with both ids, in sorted order. Printed on the warn-only line beside backlog staleness; never appended to `errors`. A shared stopword alone is never a signal, and the findings stay out of the exit code even under --strict."
tags: ["Drafted"]
resource: "docs/requirements/low-level-requirements.toml (LLR-160)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-160) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-160 — The mechanical queue-overlap pre-filter

**Detail.** Three signals over OPEN rows only — Jaccard over title token sets at a deliberately high bar, shared `SR-Refs`, and a shared anchor-stripped `SpecRef` — each reported once per pair per signal with both ids, in sorted order. Printed on the warn-only line beside backlog staleness; never appended to `errors`. A shared stopword alone is never a signal, and the findings stay out of the exit code even under --strict.

**Rationale.** A low similarity bar turns a useful warn into noise that gets ignored, which is worse than not having the warn: this rung catches the same job minted twice, not two rows in the same area.

**Code.** `project-trajectory/scripts/check_trajectory.py` — `queue_conflict_findings/_title_tokens`
- Decomposes: [SR-157](../system-requirements/SR-157.md)
- Verified by: [TC-155](../test-cases/TC-155.md)
