---
type: "Low-Level Requirement"
title: "Dual-plan round state machine"
description: "Pure side-effect-free round-lifecycle library the coordinator drives (WI-194, DP-001 selected plan P1; process-options 'Dual-plan decomposition'): typed steps PLAN/COVERAGE/REPAIR/CRITIQUE/REVISE/ARBITER offered parallel-friendly by ready_steps; record() enforces the hard caps as RoundCapError (one critique round ever, one revision each and only against CHANGES-REQUESTED, one coverage repair per plan per stage with a repeated-findings PAGE), a 14-session default round-budget ledger sized for the 8-session happy path plus two legal repairs and fallback/relaunch headroom (explicit override retained; exhaustion PAGEs), the arbiter position-swap x2 agreement rule (disagreement PAGEs position-unstable), and page_action() mapping PAGE to the declared docs/gate-policy failure semantics (unknown policy fails safe to attended). State is one JSON-able dict (crash-resumable)."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-070)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-070) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-070 — Dual-plan round state machine

**Detail.** Pure side-effect-free round-lifecycle library the coordinator drives (WI-194, DP-001 selected plan P1; process-options 'Dual-plan decomposition'): typed steps PLAN/COVERAGE/REPAIR/CRITIQUE/REVISE/ARBITER offered parallel-friendly by ready_steps; record() enforces the hard caps as RoundCapError (one critique round ever, one revision each and only against CHANGES-REQUESTED, one coverage repair per plan per stage with a repeated-findings PAGE), a 14-session default round-budget ledger sized for the 8-session happy path plus two legal repairs and fallback/relaunch headroom (explicit override retained; exhaustion PAGEs), the arbiter position-swap x2 agreement rule (disagreement PAGEs position-unstable), and page_action() mapping PAGE to the declared docs/gate-policy failure semantics (unknown policy fails safe to attended). State is one JSON-able dict (crash-resumable).

**Code.** `project-trajectory/scripts/plan_round.py` — `new_round/ready_steps/record/disposition/page_action`
- Decomposes: [SR-066](../system-requirements/SR-066.md)
- Verified by: [TC-070](../test-cases/TC-070.md)
