---
type: "Low-Level Requirement"
title: "Budget comparator"
description: "Compares measured metrics (docs/test/perf-metrics.json) to declared budgets and the committed baseline (docs/test/perf-baseline.json) and fails beyond tolerance at the row's block/warn level; --update-baseline is the reviewed acceptance path for an intended regression. The step is wired into the harness at DevBar-Release, per its declared gates in docs/stack.ini."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-014)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-014) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-014 — Budget comparator

**Detail.** Compares measured metrics (docs/test/perf-metrics.json) to declared budgets and the committed baseline (docs/test/perf-baseline.json) and fails beyond tolerance at the row's block/warn level; --update-baseline is the reviewed acceptance path for an intended regression. The step is wired into the harness at DevBar-Release, per its declared gates in docs/stack.ini.

**Code.** `project-trajectory/scripts/check_perf.py` — `evaluate`
- Decomposes: [SR-006](../system-requirements/SR-006.md) · [SR-167](../system-requirements/SR-167.md)
- Verified by: [TC-014](../test-cases/TC-014.md)
