---
type: "Low-Level Requirement"
title: "Gate step plan + missing-tool guard"
description: "Builds the required-step plan per gate/tier and reports SKIP(missing) with a nonzero exit when a required tool is absent. On a claimed work branch (_work_branch: current git branch with a docs/work/active/<branch>/ directory; memoized, fail-closed to full checks off-git or on '..' names) the _TRUNK_FRESHNESS_STEPS set SKIPs with a \u00a75.2 notice, exit code untouched; the plan listing still names every step."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-006)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-006) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-006 — Gate step plan + missing-tool guard

**Detail.** Builds the required-step plan per gate/tier and reports SKIP(missing) with a nonzero exit when a required tool is absent. On a claimed work branch (_work_branch: current git branch with a docs/work/active/<branch>/ directory; memoized, fail-closed to full checks off-git or on '..' names) the _TRUNK_FRESHNESS_STEPS set SKIPs with a §5.2 notice, exit code untouched; the plan listing still names every step.

**Code.** `project-trajectory/scripts/check.py` — `steps/run_step/_work_branch`
- Decomposes: [SR-006](../system-requirements/SR-006.md)
- Verified by: [TC-006](../test-cases/TC-006.md)
