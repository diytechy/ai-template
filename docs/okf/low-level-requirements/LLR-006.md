---
type: "Low-Level Requirement"
title: "Gate step plan + missing-tool guard"
description: "Builds the required-step plan per gate/tier and reports SKIP(missing) with a nonzero exit when a required tool is absent; the plan listing still names every step. The work-branch lane skip is LLR-141, which decomposes from this same row."
tags: ["Approved"]
resource: "docs/requirements/low-level-requirements.toml (LLR-006)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-006) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-006 — Gate step plan + missing-tool guard

**Detail.** Builds the required-step plan per gate/tier and reports SKIP(missing) with a nonzero exit when a required tool is absent; the plan listing still names every step. The work-branch lane skip is LLR-141, which decomposes from this same row.

**Code.** `project-trajectory/scripts/check.py` — `steps/run_step`
- Decomposes: [SR-006](../system-requirements/SR-006.md)
- Verified by: [TC-006](../test-cases/TC-006.md)
