---
type: "Low-Level Requirement"
title: "SN-coverage gate rung + uncovered basis count"
description: "sn_gate reads the cited set sn_cited_ids builds from SR SN-Refs (an F5 duplicate of trace.py's parse, pinned equal by test_rule_sync like phase_num and LLR_EXEMPT): a RATIFIED SN cited by zero SRs caps the raw level at G0 - a ratified-but-unanswered need has not earned G1 - while a Draft SN stays exempt from the coverage rung and rides the existing draft rung (one fact, one finding; the itemized 'SN has no SR' listing remains trace.py's orphan rule at G2 strictness, reading the SAME cited set, so the gate input and the listing cannot contradict on one registry state). _raw_level rebuilds the cited set from the rows in scope, so the ex-draft counterfactual drops a Draft SR's citation with its row and never fabricates coverage. The compared basis line gains uncovered=N (ratified-and-uncited count) beside drafts=N/modified=N - additive for readers, but --check compares the line whole, so it is a cache-format change downstream repos regenerate through."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-147)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-147) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-147 — SN-coverage gate rung + uncovered basis count

**Detail.** sn_gate reads the cited set sn_cited_ids builds from SR SN-Refs (an F5 duplicate of trace.py's parse, pinned equal by test_rule_sync like phase_num and LLR_EXEMPT): a RATIFIED SN cited by zero SRs caps the raw level at G0 - a ratified-but-unanswered need has not earned G1 - while a Draft SN stays exempt from the coverage rung and rides the existing draft rung (one fact, one finding; the itemized 'SN has no SR' listing remains trace.py's orphan rule at G2 strictness, reading the SAME cited set, so the gate input and the listing cannot contradict on one registry state). _raw_level rebuilds the cited set from the rows in scope, so the ex-draft counterfactual drops a Draft SR's citation with its row and never fabricates coverage. The compared basis line gains uncovered=N (ratified-and-uncited count) beside drafts=N/modified=N - additive for readers, but --check compares the line whole, so it is a cache-format change downstream repos regenerate through.

**Rationale.** Before this rung SN->SR linkage was not a gate input at all: an unanswered ratified need left the gate untouched, orphaning only at G2 strictness (owner ruling 2026-08-01, dispatcher-flow review Q2). Measured safe before landing: the census on this repo read 25/25 SNs covered and zero ghost SN-Refs, so the rung lands green here and the uncovered case is constructed by fixture, not found.

**Code.** `project-trajectory/scripts/derive_gate.py` — `sn_gate/sn_cited_ids`
- Decomposes: [SR-049](../system-requirements/SR-049.md)
- Verified by: [TC-141](../test-cases/TC-141.md)
