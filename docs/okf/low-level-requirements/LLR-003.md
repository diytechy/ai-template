---
type: "Low-Level Requirement"
title: "Placeholder + schema checks"
description: "-000 example-row scan and required-field + closed-vocabulary validation over the real (non-placeholder) rows, plus the ratified-phase completeness rule: once any spine row is phased, a non-Draft SR/LLR/TC whose Phase does not digit-parse to a number is a --strict-schema finding (vacuous until armed; a downstream vN parses and passes)."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-003)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-003) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-003 — Placeholder + schema checks

**Detail.** -000 example-row scan and required-field + closed-vocabulary validation over the real (non-placeholder) rows, plus the ratified-phase completeness rule: once any spine row is phased, a non-Draft SR/LLR/TC whose Phase does not digit-parse to a number is a --strict-schema finding (vacuous until armed; a downstream vN parses and passes).

**Code.** `project-trajectory/scripts/trace.py` — `placeholder_findings/schema_findings/phase_ratified_findings`
- Decomposes: [SR-003](../system-requirements/SR-003.md)
- Verified by: [TC-003](../test-cases/TC-003.md)
