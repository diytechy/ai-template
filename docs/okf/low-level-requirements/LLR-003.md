---
type: "Low-Level Requirement"
title: "Placeholder + schema checks"
description: "-000 example-row scan and required-field + closed-vocabulary validation over the real (non-placeholder) rows, plus the ratified-phase NUMERIC-ONLY rule (owner ruling 2026-08-01): once any spine row is phased (digit-parse arming), a non-Draft SR/LLR/TC whose Phase is not a full-cell bare integer is a --strict-schema finding (vacuous until armed; a prefixed v2/P1 now fails - the --phase/--ratify filters and the phase-drop join match the cell literally, so a prefix disarms them silently; phase_num stays digit-extract for grandfathering). The -000 example-row scan spans SR/LLR/TC and the SN doc and rides --no-placeholders; --strict-schema is the gate for the required-field and Verification/Tier closed-vocabulary rules."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-003)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-003) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-003 — Placeholder + schema checks

**Detail.** -000 example-row scan and required-field + closed-vocabulary validation over the real (non-placeholder) rows, plus the ratified-phase NUMERIC-ONLY rule (owner ruling 2026-08-01): once any spine row is phased (digit-parse arming), a non-Draft SR/LLR/TC whose Phase is not a full-cell bare integer is a --strict-schema finding (vacuous until armed; a prefixed v2/P1 now fails - the --phase/--ratify filters and the phase-drop join match the cell literally, so a prefix disarms them silently; phase_num stays digit-extract for grandfathering). The -000 example-row scan spans SR/LLR/TC and the SN doc and rides --no-placeholders; --strict-schema is the gate for the required-field and Verification/Tier closed-vocabulary rules.

**Code.** `project-trajectory/scripts/trace.py` — `placeholder_findings/schema_findings/phase_ratified_findings`
- Decomposes: [SR-157](../system-requirements/SR-157.md)
- Verified by: [TC-003](../test-cases/TC-003.md)
