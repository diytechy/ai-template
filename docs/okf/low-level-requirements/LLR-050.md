---
type: "Low-Level Requirement"
title: "Derived-gate computation + hybrid cache"
description: "Per-artifact gate rules (sr_gate/maturity_gate/sn_gate) + min-aggregation over in-scope artifacts + the cached docs/gate (basis line + --check rot guard, legacy value-only); the derived current phase (max over ratified rows via phase_num) rides the basis line and --print, the phase analogue of the derived gate."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-050)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-050) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-050 — Derived-gate computation + hybrid cache

**Detail.** Per-artifact gate rules (sr_gate/maturity_gate/sn_gate) + min-aggregation over in-scope artifacts + the cached docs/gate (basis line + --check rot guard, legacy value-only); the derived current phase (max over ratified rows via phase_num) rides the basis line and --print, the phase analogue of the derived gate.

**Code.** `project-trajectory/scripts/derive_gate.py` — `compute`
- Decomposes: [SR-049](../system-requirements/SR-049.md)
- Verified by: [TC-050](../test-cases/TC-050.md)
