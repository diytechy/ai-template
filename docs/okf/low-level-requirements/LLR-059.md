---
type: "Low-Level Requirement"
title: "Pure safety classifier"
description: "Maps declared safety and policy inputs to one scheduling class and deterministic reason codes without side effects. A missing, unknown or contradictory declaration fails closed with its reason code for the affected WI only, and every consumer of the classification reads the same class."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-059)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-059) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-059 — Pure safety classifier

**Detail.** Maps declared safety and policy inputs to one scheduling class and deterministic reason codes without side effects. A missing, unknown or contradictory declaration fails closed with its reason code for the affected WI only, and every consumer of the classification reads the same class.

**Code.** `project-trajectory/scripts/schedule.py` — `classify/safety_class`
- Decomposes: [SR-153](../system-requirements/SR-153.md)
- Verified by: [TC-059](../test-cases/TC-059.md)
