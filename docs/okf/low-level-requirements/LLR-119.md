---
type: "Low-Level Requirement"
title: "T4 label legibility, mechanized: ink inside its box, truncation with a reveal"
description: "The MECHANIZED floor of usability anchor T4 (docs/rubrics/dashboard-usability.md), bound by WI-318 under the WI-300 (f) pattern and WIDENING the anchor's existing owner rather than adding a rubric anchor. The clause made objective is the one a reader cannot argue with: a label is illegible AS a label the moment its ink leaves the box it names, so no character a block renders may fall outside that block's own rect, horizontally or vertically. 121-CRITIQUE's MAJOR was the whole of a stakeholder need drawn as one unwrapped sub-label line, starting outside the left edge of its node and ending past the right one at every captured width. Column right-sizing (LLR-057, SR-056) cannot absorb that: it clamps at MAX_TIER_COL, so a 400-character need asks for a 2000px column and gets 172. The emitter therefore FITS the sub-label to the column it will actually be drawn in - a second line when one will not hold it, then an ellipsis - with three text lines the ceiling, the grid the id/name wrap idiom already proved fits row_h; a sub-label that already fits one line renders byte-identically to before. SCOPE - PROVEN: for every block of every drill layer in the emitted document, each label line's ink box lies inside its own node rect, measured from the emitted geometry with the emitter's declared per-character widths and the document's declared font sizes, entities counted as the single glyph they render. DELIBERATELY NOT: whether an ellipsis a reader meets is ACTIONABLE - truncated-WITHOUT-AFFORDANCE stays T4's perceptual residue under LLR-055/TC-055 (the full string keeps its homes in the block's title, aria-label and data-summary), so T4 remains a live critique anchor. WIDENED by WI-319 (121-CRITIQUE MINOR) to the anchor's OTHER measurable half. The landing next-work card spent a FIXED 60-character budget whatever width it had, cutting a title mid-word at 1680px with the card half empty and nothing visible to act on. HTML already fits text to the space available, so the card emits the whole clause and lets its own width do the fitting; the declared bound _NEXT_WORK_TITLE now caps ONE item's height rather than its text (measured over 320 registry rows: median 44, p90 126, max 609 characters of leading clause), and where it bites the remainder discloses through a NATIVE <details> cut at a word, so the head and the remainder rejoin continuously when opened. Native is the point: a summary is operable by pointer AND keyboard with no script, in a file opened from disk, which is what makes the affordance assertable from the emitted markup instead of needing a browser. SCOPE - PROVEN, second half: this emitter does not truncate the next-work title by a fixed budget, and any residue it does cut carries a visible, operable, script-free reveal that loses no text. DELIBERATELY NOT: a document-wide claim that no text anywhere is truncated without an affordance - that reader-experience clause stays T4's critique residue under LLR-055/TC-055, which is why T4 remains a live anchor."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-119)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-119) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-119 — T4 label legibility, mechanized: ink inside its box, truncation with a reveal

**Detail.** The MECHANIZED floor of usability anchor T4 (docs/rubrics/dashboard-usability.md), bound by WI-318 under the WI-300 (f) pattern and WIDENING the anchor's existing owner rather than adding a rubric anchor. The clause made objective is the one a reader cannot argue with: a label is illegible AS a label the moment its ink leaves the box it names, so no character a block renders may fall outside that block's own rect, horizontally or vertically. 121-CRITIQUE's MAJOR was the whole of a stakeholder need drawn as one unwrapped sub-label line, starting outside the left edge of its node and ending past the right one at every captured width. Column right-sizing (LLR-057, SR-056) cannot absorb that: it clamps at MAX_TIER_COL, so a 400-character need asks for a 2000px column and gets 172. The emitter therefore FITS the sub-label to the column it will actually be drawn in - a second line when one will not hold it, then an ellipsis - with three text lines the ceiling, the grid the id/name wrap idiom already proved fits row_h; a sub-label that already fits one line renders byte-identically to before. SCOPE - PROVEN: for every block of every drill layer in the emitted document, each label line's ink box lies inside its own node rect, measured from the emitted geometry with the emitter's declared per-character widths and the document's declared font sizes, entities counted as the single glyph they render. DELIBERATELY NOT: whether an ellipsis a reader meets is ACTIONABLE - truncated-WITHOUT-AFFORDANCE stays T4's perceptual residue under LLR-055/TC-055 (the full string keeps its homes in the block's title, aria-label and data-summary), so T4 remains a live critique anchor. WIDENED by WI-319 (121-CRITIQUE MINOR) to the anchor's OTHER measurable half. The landing next-work card spent a FIXED 60-character budget whatever width it had, cutting a title mid-word at 1680px with the card half empty and nothing visible to act on. HTML already fits text to the space available, so the card emits the whole clause and lets its own width do the fitting; the declared bound _NEXT_WORK_TITLE now caps ONE item's height rather than its text (measured over 320 registry rows: median 44, p90 126, max 609 characters of leading clause), and where it bites the remainder discloses through a NATIVE <details> cut at a word, so the head and the remainder rejoin continuously when opened. Native is the point: a summary is operable by pointer AND keyboard with no script, in a file opened from disk, which is what makes the affordance assertable from the emitted markup instead of needing a browser. SCOPE - PROVEN, second half: this emitter does not truncate the next-work title by a fixed budget, and any residue it does cut carries a visible, operable, script-free reveal that loses no text. DELIBERATELY NOT: a document-wide claim that no text anywhere is truncated without an affordance - that reader-experience clause stays T4's critique residue under LLR-055/TC-055, which is why T4 remains a live anchor.

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `_drill_block_label/_fit_lines/_next_work_title`
- Decomposes: [SR-054](../system-requirements/SR-054.md)
- Verified by: [TC-124](../test-cases/TC-124.md)
