---
type: "Low-Level Requirement"
title: "status.md forward-only done-id enforcement (mode-aware R-D restoration)"
description: "A `done` WI id token in a hand-edited docs/status.md is a finding (WARN plain / ERROR --strict; WI-200 restores the WI-180-retired R-D): only registry-done ids flag (queued/active/deferred/blocked and unknown ids never do); stands down when status.md carries the generated-block marker (an integrator snapshot - regenerate-and-byte-compare freshness is the specified-not-built successor); shares the docs/trajectory-check opt-out and the placeholder/absent-registry vacuity; a repo-state rule evaluated every run so the invariant holds at every integrator-published tree."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-075)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-075) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-075 — status.md forward-only done-id enforcement (mode-aware R-D restoration)

**Detail.** A `done` WI id token in a hand-edited docs/status.md is a finding (WARN plain / ERROR --strict; WI-200 restores the WI-180-retired R-D): only registry-done ids flag (queued/active/deferred/blocked and unknown ids never do); stands down when status.md carries the generated-block marker (an integrator snapshot - regenerate-and-byte-compare freshness is the specified-not-built successor); shares the docs/trajectory-check opt-out and the placeholder/absent-registry vacuity; a repo-state rule evaluated every run so the invariant holds at every integrator-published tree.

**Code.** `project-trajectory/scripts/check_trajectory.py` — `status_forward_only_findings`
- Decomposes: [SR-037](../system-requirements/SR-037.md)
- Verified by: [TC-075](../test-cases/TC-075.md)
