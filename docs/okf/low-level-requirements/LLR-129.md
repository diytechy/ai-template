---
type: "Low-Level Requirement"
title: "Gated downstream migration"
description: "Holds --jobs 1 until the soft-edge audit AND the SafetyClass audit pass, then records the two-worker flip; reconciles legacy active rows + docs/tracks/* within one compatibility window."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-129)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-129) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-129 — Gated downstream migration

**Detail.** Holds --jobs 1 until the soft-edge audit AND the SafetyClass audit pass, then records the two-worker flip; reconciles legacy active rows + docs/tracks/* within one compatibility window.

**Rationale.** Split from the telemetry row: measuring a parallel run and deciding a repo is ready to start one are different obligations, and only the second can make a repo unsafe.

**Code.** `project-trajectory/scripts/agent_dispatch.py` — `assess_migration/resolve_ceiling`
- Decomposes: [SR-121](../system-requirements/SR-121.md)
- Verified by: [TC-066](../test-cases/TC-066.md)
