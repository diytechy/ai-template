---
type: "Low-Level Requirement"
title: "Dashboard UI-uniformity rendering"
description: "One shared style system across every tab and SVG emitter: a single type scale + spacing rhythm and one status/phase/type colour vocabulary applied wherever the same concept renders; uniform node/edge/legend/detail-panel styling across the dag_svg/sw_graph/when_view emitters; the same interaction idiom (expand, hover, detail) wherever the same structure appears. Factored to shared style constants/helpers so the emitters cannot drift. Adjudicated against docs/rubrics/dashboard-uniformity.md by the SR-084/SR-085 critique loop. Byte-deterministic; --check unchanged."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-054)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-054) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-054 — Dashboard UI-uniformity rendering

**Detail.** One shared style system across every tab and SVG emitter: a single type scale + spacing rhythm and one status/phase/type colour vocabulary applied wherever the same concept renders; uniform node/edge/legend/detail-panel styling across the dag_svg/sw_graph/when_view emitters; the same interaction idiom (expand, hover, detail) wherever the same structure appears. Factored to shared style constants/helpers so the emitters cannot drift. Adjudicated against docs/rubrics/dashboard-uniformity.md by the SR-084/SR-085 critique loop. Byte-deterministic; --check unchanged.

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `build_html/_style/_svg_node`
- Decomposes: [SR-053](../system-requirements/SR-053.md)
- Verified by: [TC-054](../test-cases/TC-054.md)
