---
type: "Low-Level Requirement"
title: "Id + CSV-structure + citation integrity"
description: "Duplicate/malformed id detection per ID_PATTERNS, a per-row column-count check, and SR/LLR citation coherence (triangle_findings); the always-valid --strict-integrity floor."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-002)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-002) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-002 — Id + CSV-structure + citation integrity

**Detail.** Duplicate/malformed id detection per ID_PATTERNS, a per-row column-count check, and SR/LLR citation coherence (triangle_findings); the always-valid --strict-integrity floor.

**Rationale.** These three are wrong the moment they are written rather than at a gate, and each corrupts the join before the orphan rules could notice — so they run on every commit rather than waiting for a gate that would inherit an already-broken registry.

**Code.** `project-trajectory/scripts/trace.py` — `integrity_findings/structure_findings/triangle_findings`
- Decomposes: [SR-002](../system-requirements/SR-002.md)
- Verified by: [TC-002](../test-cases/TC-002.md)
