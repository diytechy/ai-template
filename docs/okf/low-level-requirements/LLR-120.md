---
type: "Low-Level Requirement"
title: "No edge through an unrelated node box (T8 objective floor)"
description: "The MECHANIZED floor of usability anchor T8 (docs/rubrics/dashboard-usability.md), bound under the (f) decomposition pattern. T8 is two clauses and only the first is measurable: NO EDGE PASSES THROUGH AN UNRELATED NODE BOX is geometry a reader cannot argue with, while 'crossings minimized, and where unavoidable in open space' is the reader-experience half. This row claims only the first. The routing itself shipped earlier (an obstacle-aware lane detour, with two follow-ups); what this row adds is the claim about the ARTIFACT, which is a DIFFERENT claim from the router unit tests and had only ever been hand-verified once: _detour_d returns a LEAST-BAD fallback when no candidate lane clears within _MAX_LANES, so 'the algorithm detours' does not imply 'nothing shipped through a box'. SCOPE - PROVEN: across the shipped dashboard plus one fixture per emitter, every emitted wire, sampled as the polyline a reader's eye follows, clears every node rect that is not its own source or target. DELIBERATELY NOT: crossing count, lane separation, or where a crossing falls - the residue 121-CRITIQUE actually reported (coincident horizontal corridors converging into dense port fans), which stays under LLR-055/TC-055 and is why T8 remains a live critique anchor. MEASURED at the binding, and worth the next slice's attention: 264 shipped wires, 0 through a box, but ONE wire rides within the router's own 3px clearance of a neighbouring node box's edge - the least-bad fallback firing once."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-120)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-120) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-120 — No edge through an unrelated node box (T8 objective floor)

**Detail.** The MECHANIZED floor of usability anchor T8 (docs/rubrics/dashboard-usability.md), bound under the (f) decomposition pattern. T8 is two clauses and only the first is measurable: NO EDGE PASSES THROUGH AN UNRELATED NODE BOX is geometry a reader cannot argue with, while 'crossings minimized, and where unavoidable in open space' is the reader-experience half. This row claims only the first. The routing itself shipped earlier (an obstacle-aware lane detour, with two follow-ups); what this row adds is the claim about the ARTIFACT, which is a DIFFERENT claim from the router unit tests and had only ever been hand-verified once: _detour_d returns a LEAST-BAD fallback when no candidate lane clears within _MAX_LANES, so 'the algorithm detours' does not imply 'nothing shipped through a box'. SCOPE - PROVEN: across the shipped dashboard plus one fixture per emitter, every emitted wire, sampled as the polyline a reader's eye follows, clears every node rect that is not its own source or target. DELIBERATELY NOT: crossing count, lane separation, or where a crossing falls - the residue 121-CRITIQUE actually reported (coincident horizontal corridors converging into dense port fans), which stays under LLR-055/TC-055 and is why T8 remains a live critique anchor. MEASURED at the binding, and worth the next slice's attention: 264 shipped wires, 0 through a box, but ONE wire rides within the router's own 3px clearance of a neighbouring node box's edge - the least-bad fallback firing once.

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `_route_edges/_detour_d/_polyline_hits`
- Decomposes: [SR-054](../system-requirements/SR-054.md)
- Verified by: [TC-125](../test-cases/TC-125.md)
