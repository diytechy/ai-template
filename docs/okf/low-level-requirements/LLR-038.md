---
type: "Low-Level Requirement"
title: "Path + sym: reference checker"
description: "Scans root *.md + docs/**/*.md; a backticked token is path-shaped when it carries a separator plus a known extension or top-level prefix (URLs/globs/placeholders excluded); GENERATED marker blocks and path-ok lines are skipped; sym:<module>.<name> is resolved against the module map parsed from architecture.md (module tail -> public symbols); warn-first, --strict exits 1."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-038)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-038) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-038 — Path + sym: reference checker

**Detail.** Scans root *.md + docs/**/*.md; a backticked token is path-shaped when it carries a separator plus a known extension or top-level prefix (URLs/globs/placeholders excluded); GENERATED marker blocks and path-ok lines are skipped; sym:<module>.<name> is resolved against the module map parsed from architecture.md (module tail -> public symbols); warn-first, --strict exits 1.

**Code.** `project-trajectory/scripts/check_doc_refs.py` — `findings_for/load_symbol_oracle/is_path_shaped`
- Decomposes: [SR-041](../system-requirements/SR-041.md)
- Verified by: [TC-041](../test-cases/TC-041.md)
