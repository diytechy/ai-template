---
type: "Low-Level Requirement"
title: "Path + sym: reference checker"
description: "Scans root *.md + docs/**/*.md; a backticked token is path-shaped when it carries a separator plus a known extension or top-level prefix (URLs/globs/placeholders excluded); GENERATED marker blocks and path-ok lines are skipped; sym:<module>.<name> is resolved against the module map parsed from architecture.md (module tail -> public symbols); warn-first, --strict exits 1. With no module-map inventory the symbol tier skips cleanly while the path tier still runs. Declared to the harness as docs/stack.ini's [step:doc-refs] row (gates = DevBar-Release, beside [step:figures] for the figure-provenance checker); docs/declared-absences is the reviewed absence carve-out."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-038)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-038) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-038 — Path + sym: reference checker

**Detail.** Scans root *.md + docs/**/*.md; a backticked token is path-shaped when it carries a separator plus a known extension or top-level prefix (URLs/globs/placeholders excluded); GENERATED marker blocks and path-ok lines are skipped; sym:<module>.<name> is resolved against the module map parsed from architecture.md (module tail -> public symbols); warn-first, --strict exits 1. With no module-map inventory the symbol tier skips cleanly while the path tier still runs. Declared to the harness as docs/stack.ini's [step:doc-refs] row (gates = DevBar-Release, beside [step:figures] for the figure-provenance checker); docs/declared-absences is the reviewed absence carve-out.

**Code.** `project-trajectory/scripts/check_doc_refs.py` — `findings_for/load_symbol_oracle/is_path_shaped`
- Decomposes: [SR-158](../system-requirements/SR-158.md)
- Verified by: [TC-041](../test-cases/TC-041.md)
