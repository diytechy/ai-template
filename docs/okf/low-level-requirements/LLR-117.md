---
type: "Low-Level Requirement"
title: "Theme-lock: one theme mechanism and no mixed-family surface/ink pair (T6 core)"
description: "The MECHANIZED core of usability anchor T6 (docs/rubrics/dashboard-usability.md), bound by WI-314 under the WI-300 (f) pattern - the residue table marked T6 mechanized with residue NONE, needing only a static token check. The measurable content is FAMILY PAIRING, not the absence of literals: a fixed status swatch carrying fixed white ink is theme-locked CORRECTLY, since neither side flips. A page inverts mid-view in exactly two ways, and this row forbids both. (1) A SECOND THEME MECHANISM below :root - the document declares exactly one prefers-color-scheme block, targeting :root, and one color-scheme, also at :root; a per-component override is precisely a seam a reader crosses while scrolling. (2) A MIXED PAIR - a theme-varying ink on a theme-invariant surface, or the reverse, which reads correctly in the theme it was authored in and inverts in the other. The two families are DERIVED from the emitted document (the dark block's override set IS the definition of theme-varying), never hand-listed, so a new token joins a family by existing; the dark block may not override a token :root never declares. Applied to CSS rules that declare both a background and an ink, to every emitted SVG node pairing an inline rect fill with an inline text fill, and to CSS-declared SVG text fills paired against the family their host nodes actually paint in the artifact. SCOPE and its deliberate narrowings: --ring is excluded because it is stamped FROM the host fill (invariant-with-invariant, varying-with-varying by construction; LLR-105 owns its floor); the JS-composed badge idiom pairs its background and ink at runtime from the invariant vocabulary, where LLR-114's arithmetic owns it; and the page CSS has exactly ONE rule declaring a classifiable background AND ink today (body), so that half of the check is prospective while the sweep's weight sits on the node and text-fill halves. Whether the chosen light and dark palettes are PLEASANT stays perceptual and is not claimed here."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-117)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-117) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-117 — Theme-lock: one theme mechanism and no mixed-family surface/ink pair (T6 core)

**Detail.** The MECHANIZED core of usability anchor T6 (docs/rubrics/dashboard-usability.md), bound by WI-314 under the WI-300 (f) pattern - the residue table marked T6 mechanized with residue NONE, needing only a static token check. The measurable content is FAMILY PAIRING, not the absence of literals: a fixed status swatch carrying fixed white ink is theme-locked CORRECTLY, since neither side flips. A page inverts mid-view in exactly two ways, and this row forbids both. (1) A SECOND THEME MECHANISM below :root - the document declares exactly one prefers-color-scheme block, targeting :root, and one color-scheme, also at :root; a per-component override is precisely a seam a reader crosses while scrolling. (2) A MIXED PAIR - a theme-varying ink on a theme-invariant surface, or the reverse, which reads correctly in the theme it was authored in and inverts in the other. The two families are DERIVED from the emitted document (the dark block's override set IS the definition of theme-varying), never hand-listed, so a new token joins a family by existing; the dark block may not override a token :root never declares. Applied to CSS rules that declare both a background and an ink, to every emitted SVG node pairing an inline rect fill with an inline text fill, and to CSS-declared SVG text fills paired against the family their host nodes actually paint in the artifact. SCOPE and its deliberate narrowings: --ring is excluded because it is stamped FROM the host fill (invariant-with-invariant, varying-with-varying by construction; LLR-105 owns its floor); the JS-composed badge idiom pairs its background and ink at runtime from the invariant vocabulary, where LLR-114's arithmetic owns it; and the page CSS has exactly ONE rule declaring a classifiable background AND ink today (body), so that half of the check is prospective while the sweep's weight sits on the node and text-fill halves. Whether the chosen light and dark palettes are PLEASANT stays perceptual and is not claimed here.

**Code.** `project-trajectory/scripts/gen_trajectory.py` — `HTML_TEMPLATE theme tokens + every emitter's paint`
- Decomposes: [SR-054](../system-requirements/SR-054.md)
- Verified by: [TC-122](../test-cases/TC-122.md)
