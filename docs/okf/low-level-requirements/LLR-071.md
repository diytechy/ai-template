---
type: "Low-Level Requirement"
title: "Redacted dual-plan brief assembler + hat prompt-map keys"
description: "Allowlist-only brief assembly (WI-195, DP-001 plan P2): build_surface reads ONLY system-requirements.csv + interfaces.csv (SR surface + IF excerpt, minimal columns so Rationale/Notes prose never rides along), so status.md/log.md/self-assessments are unreachable by construction; assemble does a strict {{NAME}} slot-fill (unknown key or unfilled placeholder raises, computed from the template's own placeholder set); HAT_KEYS registers DUALPLAN-PLANNER/CRITIC/ARBITER on --prompt-map/AGENT_PROMPT_MAP with a kit-template fallback located relative to the script."
tags: ["Verified"]
resource: "docs/requirements/low-level-requirements.csv (LLR-071)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-071) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-071 — Redacted dual-plan brief assembler + hat prompt-map keys

**Detail.** Allowlist-only brief assembly (WI-195, DP-001 plan P2): build_surface reads ONLY system-requirements.csv + interfaces.csv (SR surface + IF excerpt, minimal columns so Rationale/Notes prose never rides along), so status.md/log.md/self-assessments are unreachable by construction; assemble does a strict {{NAME}} slot-fill (unknown key or unfilled placeholder raises, computed from the template's own placeholder set); HAT_KEYS registers DUALPLAN-PLANNER/CRITIC/ARBITER on --prompt-map/AGENT_PROMPT_MAP with a kit-template fallback located relative to the script.

**Code.** `project-trajectory/scripts/plan_briefs.py` — `build_surface/assemble/load_template/strip_dispatcher_block/HAT_KEYS`
- Decomposes: [SR-066](../system-requirements/SR-066.md)
- Verified by: [TC-071](../test-cases/TC-071.md)
