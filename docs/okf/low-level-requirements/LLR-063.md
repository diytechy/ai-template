---
type: "Low-Level Requirement"
title: "Traincar continuation + fork/join + release-on-end"
description: "Continues a lane onto the reserved traincar's single hard successor while conditions hold (classifier permits, cap default 4); one Build pass + one review cycle with the policy-required verdict count, no constituent done until the train integrates; releases unstarted reservations + recomputes the DAG on any early end; a fork integrates the parent then splits, a join starts from the combined HEAD."
tags: ["Draft"]
resource: "docs/requirements/low-level-requirements.csv (LLR-063)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.csv (LLR-063) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# LLR-063 — Traincar continuation + fork/join + release-on-end

**Detail.** Continues a lane onto the reserved traincar's single hard successor while conditions hold (classifier permits, cap default 4); one Build pass + one review cycle with the policy-required verdict count, no constituent done until the train integrates; releases unstarted reservations + recomputes the DAG on any early end; a fork integrates the parent then splits, a join starts from the combined HEAD.

**Code.** `project-trajectory/scripts/agent_loop.py` — `continue_train/fork_join`
- Decomposes: [SR-062](../system-requirements/SR-062.md)
- Verified by: [TC-063](../test-cases/TC-063.md)
