---
type: "Low-Level Requirement"
title: "Serialized dual-plan disposition"
description: "Commits SELECT or PAGE as one docs-only disposition and regenerates validation. SELECT files the children as queued specs that pass the registry validator, validated atomically with their generated views; a PAGE preserves the round's evidence on disk and never surfaces silently."
tags: ["Modified"]
resource: "docs/requirements/low-level-requirements.toml (LLR-096)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/low-level-requirements.toml (LLR-096) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# LLR-096 — Serialized dual-plan disposition

**Detail.** Commits SELECT or PAGE as one docs-only disposition and regenerates validation. SELECT files the children as queued specs that pass the registry validator, validated atomically with their generated views; a PAGE preserves the round's evidence on disk and never surfaces silently.

**Code.** `project-trajectory/scripts/agent_loop.py` — `main`
- Decomposes: [SR-155](../system-requirements/SR-155.md)
- Verified by: [TC-098](../test-cases/TC-098.md)
