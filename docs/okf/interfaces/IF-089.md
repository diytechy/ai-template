---
type: "Interface"
title: "IF-089"
description: "the empty-frontier gap census (docs/concurrency-v2.md \u00a7A4 ladder rung 1 / \u00a7A5.2): gap_census calls trace.load_registries + trace.analyze (phase unfiltered, require_verified on) and reports findings.orphans + findings.status_findings + the draft SN ids \u2014 the registry gaps trace already names, reused not re-derived. The list is the intake mint helper's handoff (it mints concrete gap-closure rows from it; the dispatcher itself never mints \u2014 no silent planner mints)."
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-089)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-089) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-089 — Interface

**Contract.** the empty-frontier gap census (docs/concurrency-v2.md §A4 ladder rung 1 / §A5.2): gap_census calls trace.load_registries + trace.analyze (phase unfiltered, require_verified on) and reports findings.orphans + findings.status_findings + the draft SN ids — the registry gaps trace already names, reused not re-derived. The list is the intake mint helper's handoff (it mints concrete gap-closure rows from it; the dispatcher itself never mints — no silent planner mints).
