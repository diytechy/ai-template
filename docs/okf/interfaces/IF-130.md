---
type: "Interface"
title: "IF-130"
description: "consumes derive_gate.bar_label(name) -> str: appends the release-ceiling note when the value is the ceilinged bar, else returns the bar name unchanged, so the status snapshot's stage line renders the same wording for the derived gate as every other surface calling bar_label."
tags: ["Drafted"]
resource: "docs/requirements/interfaces.toml (IF-130)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-130) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-130 — Interface

**Contract.** consumes derive_gate.bar_label(name) -> str: appends the release-ceiling note when the value is the ceilinged bar, else returns the bar name unchanged, so the status snapshot's stage line renders the same wording for the derived gate as every other surface calling bar_label.
