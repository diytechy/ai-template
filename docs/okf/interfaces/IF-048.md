---
type: "Interface"
title: "IF-048"
description: "run_menu.py CLI: reads docs/stack.ini [run] and presents a numbered menu (no args) / direct launch by name with exit-code passthrough (run_menu.py <name>) / --list (name<TAB>desc, the agent surface); an absent or empty [run] section prints guidance and exits 1"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-048)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-048) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-048 — Interface

**Contract.** run_menu.py CLI: reads docs/stack.ini [run] and presents a numbered menu (no args) / direct launch by name with exit-code passthrough (run_menu.py <name>) / --list (name<TAB>desc, the agent surface); an absent or empty [run] section prints guidance and exits 1
