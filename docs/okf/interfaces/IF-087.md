---
type: "Interface"
title: "IF-087"
description: "reuses doc_files (the root *.md + docs/**/*.md walk with the linguist-generated prefix skip) and authored_lines (the GENERATED-marker-block skip) - so the two doc checks agree on the scanned surface and the authored-lines rule instead of each declaring its own"
tags: ["Drafted"]
resource: "docs/requirements/interfaces.toml (IF-087)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-087) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-087 — Interface

**Contract.** reuses doc_files (the root *.md + docs/**/*.md walk with the linguist-generated prefix skip) and authored_lines (the GENERATED-marker-block skip) - so the two doc checks agree on the scanned surface and the authored-lines rule instead of each declaring its own
