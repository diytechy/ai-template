---
type: "Interface"
title: "IF-091"
description: "reads the WI-380 amendment seam as-is - staged_spine_amendments(root, before, after) per-row {ratified, traced} cell dicts with before/after over a rev pair - plus SPINE_CSVS for the flip's registry walk; only the routed traced subset (SN-Refs, Verifies, LLR SR-Refs per the WI-388 ruling) mints, the rest stays silent by ruling"
tags: ["Active"]
resource: "docs/requirements/interfaces.csv (IF-091)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-091) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-091 — Interface

**Contract.** reads the WI-380 amendment seam as-is - staged_spine_amendments(root, before, after) per-row {ratified, traced} cell dicts with before/after over a rev pair - plus SPINE_CSVS for the flip's registry walk; only the routed traced subset (SN-Refs, Verifies, LLR SR-Refs per the WI-388 ruling) mints, the rest stays silent by ruling
