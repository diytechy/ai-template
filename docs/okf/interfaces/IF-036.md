---
type: "Interface"
title: "IF-036"
description: "reads the UPSTREAM manifest and fetches each declared upstream source over the network (network-gated), comparing bytes"
tags: ["draft"]
resource: "docs/requirements/interfaces.toml (IF-036)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-036) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-036 — Interface

**Contract.** reads the UPSTREAM manifest and fetches each declared upstream source over the network (network-gated), comparing bytes
