---
type: "Interface"
title: "IF-097"
description: "attest.ratification_projection(root): which spine tiers owe a human decision and which open review requests block, findings-carrying and never raising, so a refused dial renders its reason instead of an empty band"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-097)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-097) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-097 — Interface

**Contract.** attest.ratification_projection(root): which spine tiers owe a human decision and which open review requests block, findings-carrying and never raising, so a refused dial renders its reason instead of an empty band
