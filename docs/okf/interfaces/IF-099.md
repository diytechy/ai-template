---
type: "Interface"
title: "IF-099"
description: "admit.admission_verdict / the admissions ledger: the recorded conflict ruling and the scope/spine digests it was computed against, so the strict check can refuse a queued row whose verdict is absent or stale"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-099)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-099) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-099 — Interface

**Contract.** admit.admission_verdict / the admissions ledger: the recorded conflict ruling and the scope/spine digests it was computed against, so the strict check can refuse a queued row whose verdict is absent or stale
