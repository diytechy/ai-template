---
type: "Interface"
title: "IF-014"
description: "bootstrap.py CLI: --dest DIR [--stack/--domain/--agents/--force] writes the mapped kit files so the scaffold harness runs green; idempotent without --force; stamps docs/kit-version"
tags: ["Drafted"]
resource: "docs/requirements/interfaces.toml (IF-014)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-014) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-014 — Interface

**Contract.** bootstrap.py CLI: --dest DIR [--stack/--domain/--agents/--force] writes the mapped kit files so the scaffold harness runs green; idempotent without --force; stamps docs/kit-version
