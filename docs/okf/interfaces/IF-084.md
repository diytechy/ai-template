---
type: "Interface"
title: "IF-084"
description: "IF-056's derivation-loader seam, as HELD by the split sibling that now carries the import (WI-280): traj_status.py derives the status snapshot + the pending-owner-actions projection through the validator's loaders (read_rows/read_registry_rows/load_wis/load_ifs). Contract: IF-056's."
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-084)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-084) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-084 — Interface

**Contract.** IF-056's derivation-loader seam, as HELD by the split sibling that now carries the import (WI-280): traj_status.py derives the status snapshot + the pending-owner-actions projection through the validator's loaders (read_rows/read_registry_rows/load_wis/load_ifs). Contract: IF-056's.
