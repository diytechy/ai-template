---
type: "Interface"
title: "IF-102"
description: "derive_gate.spine_stage / verification_gate_for: which artifact tier is in process, so the planner can select current-stage spine work without inferring it from the harness gate"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-102)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-102) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-102 — Interface

**Contract.** derive_gate.spine_stage / verification_gate_for: which artifact tier is in process, so the planner can select current-stage spine work without inferring it from the harness gate
