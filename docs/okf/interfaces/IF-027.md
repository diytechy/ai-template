---
type: "Interface"
title: "IF-027"
description: "tokenizes each source file (stdlib tokenize) and slides the --min-tokens window across the set; reads docs/dupes-allow (the fingerprint+pair census)"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-027)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-027) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-027 — Interface

**Contract.** tokenizes each source file (stdlib tokenize) and slides the --min-tokens window across the set; reads docs/dupes-allow (the fingerprint+pair census)
