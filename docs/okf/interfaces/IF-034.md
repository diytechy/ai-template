---
type: "Interface"
title: "IF-034"
description: "reads the SN core-needs table and the PB budgets registry to build the release checklist"
tags: ["Stable"]
resource: "docs/requirements/interfaces.toml (IF-034)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-034) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-034 — Interface

**Contract.** reads the SN core-needs table and the PB budgets registry to build the release checklist
