---
type: "Interface"
title: "IF-015"
description: "agent_loop.py CLI: explicit session roles only - --wi runs one claimed worker assignment (--train is the optional session tag, defaulting to the branch name), --interactive launches one attached session, --dual-plan runs one decomposition round; a plain launch refuses with the map (claiming/merging live in integrate.py, Phase 5). Preflight refuses broken footing and the per-checkout lock refuses a second coordinator."
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-015)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-015) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-015 — Interface

**Contract.** agent_loop.py CLI: explicit session roles only - --wi runs one claimed worker assignment (--train is the optional session tag, defaulting to the branch name), --interactive launches one attached session, --dual-plan runs one decomposition round; a plain launch refuses with the map (claiming/merging live in integrate.py, Phase 5). Preflight refuses broken footing and the per-checkout lock refuses a second coordinator.
