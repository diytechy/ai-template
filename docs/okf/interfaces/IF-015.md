---
type: "Interface"
title: "IF-015"
description: "agent_loop.py CLI: --root . defaults to the WI-registry/Git-backed dispatcher; --jobs sets its ceiling, --wi/--train selects one explicit worker assignment, and --interactive launches one attached session. Preflight refuses broken footing and the per-checkout lock refuses a second coordinator."
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-015)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-015) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-015 — Interface

**Contract.** agent_loop.py CLI: --root . defaults to the WI-registry/Git-backed dispatcher; --jobs sets its ceiling, --wi/--train selects one explicit worker assignment, and --interactive launches one attached session. Preflight refuses broken footing and the per-checkout lock refuses a second coordinator.
