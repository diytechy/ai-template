---
type: "Interface"
title: "IF-020"
description: "subagent_gate.py PreToolUse contract: reads the tool-call JSON on stdin and emits a permissionDecision (deny -> exit 2) per docs/subagent-gate; fails open on any error"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-020)"
---

# IF-020 — Interface

**Contract.** subagent_gate.py PreToolUse contract: reads the tool-call JSON on stdin and emits a permissionDecision (deny -> exit 2) per docs/subagent-gate; fails open on any error
