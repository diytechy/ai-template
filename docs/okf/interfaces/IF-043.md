---
type: "Interface"
title: "IF-043"
description: "the pre-push hook runs check_privacy.py --range over the outgoing commit range and blocks a push that would publish a secret or a gated identity"
tags: ["Drafted"]
resource: "docs/requirements/interfaces.toml (IF-043)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-043) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-043 — Interface

**Contract.** the pre-push hook runs check_privacy.py --range over the outgoing commit range and blocks a push that would publish a secret or a gated identity
