---
type: "Interface"
title: "IF-071"
description: "gen_trajectory reads the scheduler's ready-frontier for the generated STATUS block + the Process-tab intake loop (schedule.load_rows/load_wis/frontier) so the dashboard shows the SAME dependency-ready ordering the dispatcher acts on - derived once in schedule.py (IF-053 already names it 'shared by ... the dashboard') and never recomputed in a second ranker. The frontier DECISION seam - distinct from the derivation-loader seam to check_trajectory (IF-056); degrades to an empty frontier block when schedule.py is absent (the WI-284 try/except)."
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-071)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-071) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-071 — Interface

**Contract.** gen_trajectory reads the scheduler's ready-frontier for the generated STATUS block + the Process-tab intake loop (schedule.load_rows/load_wis/frontier) so the dashboard shows the SAME dependency-ready ordering the dispatcher acts on - derived once in schedule.py (IF-053 already names it 'shared by ... the dashboard') and never recomputed in a second ranker. The frontier DECISION seam - distinct from the derivation-loader seam to check_trajectory (IF-056); degrades to an empty frontier block when schedule.py is absent (the WI-284 try/except).
