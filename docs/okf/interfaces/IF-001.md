---
type: "Interface"
title: "IF-001"
description: "trace.py CLI: --strict-integrity exits 1 on a duplicate/malformed id or mis-columned row; --strict adds orphan + IF/PB back-link findings; --require-verified adds the G3 status check; writes docs/test/report.md"
tags: ["Stable"]
resource: "docs/requirements/interfaces.toml (IF-001)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-001) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-001 — Interface

**Contract.** trace.py CLI: --strict-integrity exits 1 on a duplicate/malformed id or mis-columned row; --strict adds orphan + IF/PB back-link findings; --require-verified adds the G3 status check; writes docs/test/report.md
