---
type: "Interface"
title: "IF-013"
description: "check.py CLI: --gate DevBar-Reqs|DevBar-Tests|DevBar-Release|all with --tier/--coverage/--lenient runs the active gate's steps as subprocesses and exits nonzero on any required failure (never a silent pass)"
tags: ["Stable"]
resource: "docs/requirements/interfaces.toml (IF-013)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-013) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-013 — Interface

**Contract.** check.py CLI: --gate DevBar-Reqs|DevBar-Tests|DevBar-Release|all with --tier/--coverage/--lenient runs the active gate's steps as subprocesses and exits nonzero on any required failure (never a silent pass)
