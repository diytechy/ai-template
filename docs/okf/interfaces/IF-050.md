---
type: "Interface"
title: "IF-050"
description: "docs/gate derived marker: derive_gate writes the computed gate as the first non-comment line (with the # basis derivation above it); check.py resolve_gate reads it; --check is the freshness gate"
tags: []
resource: "docs/requirements/interfaces.toml (IF-050)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-050) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-050 — Interface

**Contract.** docs/gate derived marker: derive_gate writes the computed gate as the first non-comment line (with the # basis derivation above it); check.py resolve_gate reads it; --check is the freshness gate
