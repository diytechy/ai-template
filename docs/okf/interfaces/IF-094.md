---
type: "Interface"
title: "IF-094"
description: "sibling import for the station-cycle render's derived vocabulary (WI-389): _KIND_CONCURRENCY/_KIND_RANK/CONCURRENCY_EXCLUSIVE derive the exclusive-kind list the spine-barrier glyph and the escalation notes name, in rank order - the ruled A1/A8 tables read as constants; no call, no write"
tags: ["Stable"]
resource: "docs/requirements/interfaces.toml (IF-094)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-094) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-094 — Interface

**Contract.** sibling import for the station-cycle render's derived vocabulary (WI-389): _KIND_CONCURRENCY/_KIND_RANK/CONCURRENCY_EXCLUSIVE derive the exclusive-kind list the spine-barrier glyph and the escalation notes name, in rank order - the ruled A1/A8 tables read as constants; no call, no write
