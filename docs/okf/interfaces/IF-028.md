---
type: "Interface"
title: "IF-028"
description: "the sym: ORACLE seam (WI-455): consumes gen_arch_map.scan_inventory over the declared [paths] src root as the public-symbol inventory sym:<module>.<name> references are held to (the retired docs/architecture.md MODULE MAP parse's successor; sibling of IF-117, which consumes module_bindings for the LLR CodeSymbol anchor rule - two oracles for two questions, both now AST-side), and scans root + docs/*.md for dangling path / sym: references"
tags: ["Drafted"]
resource: "docs/requirements/interfaces.toml (IF-028)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-028) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-028 — Interface

**Contract.** the sym: ORACLE seam (WI-455): consumes gen_arch_map.scan_inventory over the declared [paths] src root as the public-symbol inventory sym:<module>.<name> references are held to (the retired docs/architecture.md MODULE MAP parse's successor; sibling of IF-117, which consumes module_bindings for the LLR CodeSymbol anchor rule - two oracles for two questions, both now AST-side), and scans root + docs/*.md for dangling path / sym: references
