---
type: "Interface"
title: "IF-051"
description: "reads SR/LLR/TC Status + the SN section-state to compute per-artifact maturity (also low-level-requirements, test-cases, stakeholder-needs)"
tags: ["drafted"]
resource: "docs/requirements/interfaces.toml (IF-051)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-051) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-051 — Interface

**Contract.** reads SR/LLR/TC Status + the SN section-state to compute per-artifact maturity (also low-level-requirements, test-cases, stakeholder-needs)
