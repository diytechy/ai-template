---
type: "Interface"
title: "IF-041"
description: "invokes the configured agent CLI headless (build_argv substitutes {model}/{prompt}, never through a shell) per run-phase and captures the typed session outcome"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-041)"
---

# IF-041 — Interface

**Contract.** invokes the configured agent CLI headless (build_argv substitutes {model}/{prompt}, never through a shell) per run-phase and captures the typed session outcome
