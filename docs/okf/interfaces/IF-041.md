---
type: "Interface"
title: "IF-041"
description: "invokes the configured agent CLI headless (build_argv substitutes {model}/{prompt}; on Windows it refuses prompt-in-argv through a .cmd/.bat shim because the OS may reparse it via cmd.exe even with shell=False) per the in-process phase and captures the typed session outcome"
tags: ["drafted"]
resource: "docs/requirements/interfaces.toml (IF-041)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-041) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-041 — Interface

**Contract.** invokes the configured agent CLI headless (build_argv substitutes {model}/{prompt}; on Windows it refuses prompt-in-argv through a .cmd/.bat shim because the OS may reparse it via cmd.exe even with shell=False) per the in-process phase and captures the typed session outcome
