---
type: "Interface"
title: "IF-041"
description: "invokes the configured agent CLI headless (build_argv substitutes {model}/{prompt}, never through a shell) per the in-process phase and captures the typed session outcome"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-041)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-041) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-041 — Interface

**Contract.** invokes the configured agent CLI headless (build_argv substitutes {model}/{prompt}, never through a shell) per the in-process phase and captures the typed session outcome
