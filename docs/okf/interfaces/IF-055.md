---
type: "Interface"
title: "IF-055"
description: "the dispatcher (--jobs) derives the ready frontier + traincar eligibility through the pure library (load_wis/evaluate + the SCHED_* classification constants) and packs only positively-classified work - unclassified fails closed per WI; the dispatcher itself owns reservations/worktrees/workers, the library stays side-effect-free."
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-055)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-055) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-055 — Interface

**Contract.** the dispatcher (--jobs) derives the ready frontier + traincar eligibility through the pure library (load_wis/evaluate + the SCHED_* classification constants) and packs only positively-classified work - unclassified fails closed per WI; the dispatcher itself owns reservations/worktrees/workers, the library stays side-effect-free.
