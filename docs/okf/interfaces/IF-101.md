---
type: "Interface"
title: "IF-101"
description: "attest.detect_candidates / ratification_projection: the changed-digest population and the boundary routing the planner's second rung orders, read into the frozen snapshot so the decision itself stays pure"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-101)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-101) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-101 — Interface

**Contract.** attest.detect_candidates / ratification_projection: the changed-digest population and the boundary routing the planner's second rung orders, read into the frozen snapshot so the decision itself stays pure
