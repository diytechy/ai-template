---
type: "Interface"
title: "IF-056"
description: "the kit's ONE sanctioned sibling import: the dashboard derives THROUGH the validator's loaders/joins (read_rows/load_wis/_split_refs/_norm_module/load_ifs/component_top_view/_cycles + the registry path constants) so ranking/derivation rules live in one home and the render can never disagree with validation; the two scripts always ship + re-sync together (bootstrap MAPPING). Declared at WI-064 when the cross-CMP rule surfaced the CMP-002 -> CMP-001 edge as the check's first live finding."
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-056)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-056) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-056 — Interface

**Contract.** the kit's ONE sanctioned sibling import: the dashboard derives THROUGH the validator's loaders/joins (read_rows/load_wis/_split_refs/_norm_module/load_ifs/component_top_view/_cycles + the registry path constants) so ranking/derivation rules live in one home and the render can never disagree with validation; the two scripts always ship + re-sync together (bootstrap MAPPING). Declared at WI-064 when the cross-CMP rule surfaced the CMP-002 -> CMP-001 edge as the check's first live finding.
