---
type: "Interface"
title: "IF-037"
description: "reads the declared policy surfaces through the shared declared-line parse - docs/process.toml (the one policy home), docs/gate (the generated stage cache) and the legacy one-word docs/*-policy files still honoured in the migration window - and, from docs/status.md, only the `## Current State` excerpt a stopping coordinator surfaces in its exit banner. No next-work or run-phase pointer is read; the dispatcher-era docs/run-state is retired."
tags: ["drafted"]
resource: "docs/requirements/interfaces.toml (IF-037)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-037) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-037 — Interface

**Contract.** reads the declared policy surfaces through the shared declared-line parse - docs/process.toml (the one policy home), docs/gate (the generated stage cache) and the legacy one-word docs/*-policy files still honoured in the migration window - and, from docs/status.md, only the `## Current State` excerpt a stopping coordinator surfaces in its exit banner. No next-work or run-phase pointer is read; the dispatcher-era docs/run-state is retired.
