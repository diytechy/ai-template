---
type: "Interface"
title: "IF-079"
description: "reads whichever registry home exists - the docs/work/ spec folder and/or a legacy docs/requirements/work-items.csv - to convert between them; --verify round-trips in a temp dir and never writes either live home."
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-079)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-079) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-079 — Interface

**Contract.** reads whichever registry home exists - the docs/work/ spec folder and/or a legacy docs/requirements/work-items.csv - to convert between them; --verify round-trips in a temp dir and never writes either live home.
