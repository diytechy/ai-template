---
type: "Interface"
title: "IF-083"
description: "IF-056's derivation-loader seam, as HELD by the split sibling that now carries the import (WI-280): traj_views.py renders the What/When/How-SW views through the validator's joins (component_top_view/load_ifs/_norm_module/_split_refs/read_rows), so the picture and the right-sizing rule can never disagree. Contract: IF-056's."
tags: ["drafted"]
resource: "docs/requirements/interfaces.toml (IF-083)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-083) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-083 — Interface

**Contract.** IF-056's derivation-loader seam, as HELD by the split sibling that now carries the import (WI-280): traj_views.py renders the What/When/How-SW views through the validator's joins (component_top_view/load_ifs/_norm_module/_split_refs/read_rows), so the picture and the right-sizing rule can never disagree. Contract: IF-056's.
