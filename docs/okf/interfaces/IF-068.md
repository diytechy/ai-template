---
type: "Interface"
title: "IF-068"
description: "PROPOSED (WI-274 part B): the coordinator dial read \u2014 agent_loop (or agent_common) reads a declared config section (jobs / fallback model + model-map / cmd templates) at boot with precedence CLI flag > AGENT_* env > declared file > built-in default, so the agent-resume.{cmd,sh,command} launchers shrink to platform plumbing instead of each declaring the same dial values (the 2026-07-22 dial edit touched the same two values in three files). Nearest existing: IF-049 (run_menu reads stack.ini [run]) \u2014 insufficient because that seam belongs to run_menu's capability menu, not the coordinator's dials; IF-015 (agent_loop CLI) carries --jobs per-invocation but declares no durable value. Exact home ([agent-loop] in stack.ini vs a sibling declared file) is a build-time design call."
tags: ["Proposed"]
resource: "docs/requirements/interfaces.csv (IF-068)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-068) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-068 — Interface

**Contract.** PROPOSED (WI-274 part B): the coordinator dial read — agent_loop (or agent_common) reads a declared config section (jobs / fallback model + model-map / cmd templates) at boot with precedence CLI flag > AGENT_* env > declared file > built-in default, so the agent-resume.{cmd,sh,command} launchers shrink to platform plumbing instead of each declaring the same dial values (the 2026-07-22 dial edit touched the same two values in three files). Nearest existing: IF-049 (run_menu reads stack.ini [run]) — insufficient because that seam belongs to run_menu's capability menu, not the coordinator's dials; IF-015 (agent_loop CLI) carries --jobs per-invocation but declares no durable value. Exact home ([agent-loop] in stack.ini vs a sibling declared file) is a build-time design call.
