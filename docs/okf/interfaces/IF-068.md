---
type: "Interface"
title: "IF-068"
description: "the session dial read (WI-274 part B): agent_loop reads the docs/stack.ini [agent-loop] section (fallback model / model-map) at boot via agent_common.read_agent_loop_config, resolving each dial with precedence CLI flag > AGENT_* env > declared file > built-in default, so the agent-resume launchers need no dial copies. (The dispatcher-era jobs dial retired at Phase 5; a declared value is ignored.)"
tags: ["Stable"]
resource: "docs/requirements/interfaces.toml (IF-068)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-068) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-068 — Interface

**Contract.** the session dial read (WI-274 part B): agent_loop reads the docs/stack.ini [agent-loop] section (fallback model / model-map) at boot via agent_common.read_agent_loop_config, resolving each dial with precedence CLI flag > AGENT_* env > declared file > built-in default, so the agent-resume launchers need no dial copies. (The dispatcher-era jobs dial retired at Phase 5; a declared value is ignored.)
