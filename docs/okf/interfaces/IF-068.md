---
type: "Interface"
title: "IF-068"
description: "the coordinator dial read (WI-274 part B): agent_loop reads the docs/stack.ini [agent-loop] section (jobs / fallback model / model-map) at boot via agent_common.read_agent_loop_config, resolving each dial with precedence CLI flag > AGENT_* env > declared file > built-in default, so the agent-resume.{cmd,sh,command} launchers no longer each declare the same dial VALUES (a one-dial owner change edits one file). Fail-soft: an absent section/key or an unreadable/malformed stack.ini yields the prior env/default behavior (never-breaking). Nearest existing: IF-049 (run_menu reads stack.ini [run]) \u2014 insufficient because that seam is run_menu's capability menu, not the coordinator's dials; IF-015 (agent_loop CLI) carries --jobs per-invocation but declares no durable value. Home chosen: [agent-loop] in the existing docs/stack.ini (one declared-config home, IF-049 precedent); the AGENT_CMD* templates stay launcher-side (genuinely per-platform strings). Ratified out of Proposed at WI-274 (implemented + consumed)."
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-068)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-068) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-068 — Interface

**Contract.** the coordinator dial read (WI-274 part B): agent_loop reads the docs/stack.ini [agent-loop] section (jobs / fallback model / model-map) at boot via agent_common.read_agent_loop_config, resolving each dial with precedence CLI flag > AGENT_* env > declared file > built-in default, so the agent-resume.{cmd,sh,command} launchers no longer each declare the same dial VALUES (a one-dial owner change edits one file). Fail-soft: an absent section/key or an unreadable/malformed stack.ini yields the prior env/default behavior (never-breaking). Nearest existing: IF-049 (run_menu reads stack.ini [run]) — insufficient because that seam is run_menu's capability menu, not the coordinator's dials; IF-015 (agent_loop CLI) carries --jobs per-invocation but declares no durable value. Home chosen: [agent-loop] in the existing docs/stack.ini (one declared-config home, IF-049 precedent); the AGENT_CMD* templates stay launcher-side (genuinely per-platform strings). Ratified out of Proposed at WI-274 (implemented + consumed).
