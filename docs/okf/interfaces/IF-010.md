---
type: "Interface"
title: "IF-010"
description: "gen_arch_map.py library + CLI (WI-455): scan_inventory(src_roots) exposes the per-module AST records the derived-architecture readers consume (IF-123/IF-124 and check_doc_refs' sym: oracle); the CLI splices the rendered map into opt-in --doc marker blocks (AGENTS.md/CLAUDE.md routing), and --check exits 1 when a routed block has drifted"
tags: ["Drafted"]
resource: "docs/requirements/interfaces.toml (IF-010)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-010) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-010 — Interface

**Contract.** gen_arch_map.py library + CLI (WI-455): scan_inventory(src_roots) exposes the per-module AST records the derived-architecture readers consume (IF-123/IF-124 and check_doc_refs' sym: oracle); the CLI splices the rendered map into opt-in --doc marker blocks (AGENTS.md/CLAUDE.md routing), and --check exits 1 when a routed block has drifted
