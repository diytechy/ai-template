---
type: "Interface"
title: "IF-002"
description: "check_docs.py CLI: --stale exits 1 on a broken intra-repo link, a missing PROJECT-VISION tag, or a generated doc newer than a doc that links it"
tags: ["Drafted"]
resource: "docs/requirements/interfaces.toml (IF-002)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-002) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-002 — Interface

**Contract.** check_docs.py CLI: --stale exits 1 on a broken intra-repo link, a missing PROJECT-VISION tag, or a generated doc newer than a doc that links it
