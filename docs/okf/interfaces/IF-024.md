---
type: "Interface"
title: "IF-024"
description: "reads the WI registry (the docs/work/ spec folder, the one home since Phase 5) for the roadmap DAG and the declared [paths] src tree's AST inventory for the How-SW module/graph view (WI-280: the registry read stays in the facade's main(); WI-455: the retired MODULE MAP parse became traj_parse.sw_modules over gen_arch_map.scan_inventory \u2014 the seam surface is unchanged)"
tags: ["Drafted"]
resource: "docs/requirements/interfaces.toml (IF-024)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-024) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-024 — Interface

**Contract.** reads the WI registry (the docs/work/ spec folder, the one home since Phase 5) for the roadmap DAG and the declared [paths] src tree's AST inventory for the How-SW module/graph view (WI-280: the registry read stays in the facade's main(); WI-455: the retired MODULE MAP parse became traj_parse.sw_modules over gen_arch_map.scan_inventory — the seam surface is unchanged)
