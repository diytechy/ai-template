---
type: "Interface"
title: "IF-023"
description: "reads the WI registry - docs/work/, or a legacy work-items.csv via dual-read - (ids/predecessors/status/Deliverable/SpecRef) and cross-reads docs/status.md, docs/architecture.md, docs/stack.ini and interfaces.csv for the SSOT + connectivity coverage; also reads the `## Interfaces` section of the docs/specs/ files its SpecRefs resolve, joining the cited IF-### against interfaces.csv (WI-191 - specs act on declared interface boundaries)"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-023)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-023) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-023 — Interface

**Contract.** reads the WI registry - docs/work/, or a legacy work-items.csv via dual-read - (ids/predecessors/status/Deliverable/SpecRef) and cross-reads docs/status.md, docs/architecture.md, docs/stack.ini and interfaces.csv for the SSOT + connectivity coverage; also reads the `## Interfaces` section of the docs/specs/ files its SpecRefs resolve, joining the cited IF-### against interfaces.csv (WI-191 - specs act on declared interface boundaries)
