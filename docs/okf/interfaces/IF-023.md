---
type: "Interface"
title: "IF-023"
description: "reads the WI registry - the docs/work/ spec folder, the one home since Phase 5 (a stray legacy work-items.csv is its own integrity finding) - (ids/predecessors/status/Deliverable/SpecRef) and cross-reads docs/status.md, docs/architecture.md, docs/stack.ini and interfaces.toml for the SSOT + connectivity coverage; also reads the `## Interfaces` section of the doc"
tags: ["Drafted"]
resource: "docs/requirements/interfaces.toml (IF-023)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-023) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-023 — Interface

**Contract.** reads the WI registry - the docs/work/ spec folder, the one home since Phase 5 (a stray legacy work-items.csv is its own integrity finding) - (ids/predecessors/status/Deliverable/SpecRef) and cross-reads docs/status.md, docs/architecture.md, docs/stack.ini and interfaces.toml for the SSOT + connectivity coverage; also reads the `## Interfaces` section of the doc
