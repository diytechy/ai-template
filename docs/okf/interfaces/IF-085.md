---
type: "Interface"
title: "IF-085"
description: "IF-071's frontier DECISION seam, as HELD by the split sibling that now carries the import (WI-280): traj_parse.py is the guarded `import schedule` block's ONE home in this module family (traj_status's Ready-frontier block and traj_panels' Next-work card both read it as traj_parse.schedule), so the dashboard and status.md project the SAME dependency-ready ordering. Contract: IF-071's; OPTIONAL by construction \u2014 a scaffold without schedule.py degrades to an empty block."
tags: ["Stable"]
resource: "docs/requirements/interfaces.toml (IF-085)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-085) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-085 — Interface

**Contract.** IF-071's frontier DECISION seam, as HELD by the split sibling that now carries the import (WI-280): traj_parse.py is the guarded `import schedule` block's ONE home in this module family (traj_status's Ready-frontier block and traj_panels' Next-work card both read it as traj_parse.schedule), so the dashboard and status.md project the SAME dependency-ready ordering. Contract: IF-071's; OPTIONAL by construction — a scaffold without schedule.py degrades to an empty block.
