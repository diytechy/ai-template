---
type: "Interface"
title: "IF-040"
description: "the pre-commit floor runs check.py --run-step arch-map/trajectory-map/okf to block a commit that staled a generated artifact (the freshness floor)"
tags: []
resource: "docs/requirements/interfaces.toml (IF-040)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-040) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-040 — Interface

**Contract.** the pre-commit floor runs check.py --run-step arch-map/trajectory-map/okf to block a commit that staled a generated artifact (the freshness floor)
