---
type: "Interface"
title: "IF-122"
description: "reads the stakeholder-needs registry through the CARRIER (IF-102): consumes spine_carrier.load_needs + spine_carrier.resolve, so the need tier answers whichever of TOML/legacy-markdown is live and an unreadable registry REFUSES loudly (SystemExit) rather than scanning as empty; a registry PRESENT but yielding zero scannable need cells reports VACUOUS, never clean, while absent stays the pre-scaffold clean skip - zero need cells reading as a clean tier on exactly the registry this check exists to guard is the false green SN-008 forbids."
tags: ["Drafted"]
resource: "docs/requirements/interfaces.toml (IF-122)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-122) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-122 — Interface

**Contract.** reads the stakeholder-needs registry through the CARRIER (IF-102): consumes spine_carrier.load_needs + spine_carrier.resolve, so the need tier answers whichever of TOML/legacy-markdown is live and an unreadable registry REFUSES loudly (SystemExit) rather than scanning as empty; a registry PRESENT but yielding zero scannable need cells reports VACUOUS, never clean, while absent stays the pre-scaffold clean skip - zero need cells reading as a clean tier on exactly the registry this check exists to guard is the false green SN-008 forbids.
