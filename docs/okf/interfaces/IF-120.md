---
type: "Interface"
title: "IF-120"
description: "asks the CARRIER (IF-102) one question - which carrier of a registry is live - for the open-items step's applicability predicate. Consumes spine_carrier.resolve. Naming a suffix inline instead would make the step SKIP silently on a migrated repo, and a skipped freshness step is the shape SN-008 forbids: the generated surface stops being regenerated and nothing says so."
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-120)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-120) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-120 — Interface

**Contract.** asks the CARRIER (IF-102) one question - which carrier of a registry is live - for the open-items step's applicability predicate. Consumes spine_carrier.resolve. Naming a suffix inline instead would make the step SKIP silently on a migrated repo, and a skipped freshness step is the shape SN-008 forbids: the generated surface stops being regenerated and nothing says so.
