---
type: "Interface"
title: "IF-025"
description: "walks the Python source tree (AST) for the symbol map + internal-import diagram and reads interfaces.csv to merge the IF-### seam edges"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-025)"
---

# IF-025 — Interface

**Contract.** walks the Python source tree (AST) for the symbol map + internal-import diagram and reads interfaces.csv to merge the IF-### seam edges
