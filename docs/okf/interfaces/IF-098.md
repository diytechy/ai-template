---
type: "Interface"
title: "IF-098"
description: "attest.detect_candidates(root, docs): rows whose current normative digest differs from their accepted anchor \u2014 the ledger-vs-current detector that replaces the commit-local staged diff"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-098)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-098) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-098 — Interface

**Contract.** attest.detect_candidates(root, docs): rows whose current normative digest differs from their accepted anchor — the ledger-vs-current detector that replaces the commit-local staged diff
