---
type: "Interface"
title: "IF-100"
description: "wi_convert.write_spec_file(work_dir, row): the ONE writer of the spec format, so a drafted successor or repair row is written and self-verified exactly as a minted row is"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-100)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-100) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-100 — Interface

**Contract.** wi_convert.write_spec_file(work_dir, row): the ONE writer of the spec format, so a drafted successor or repair row is written and self-verified exactly as a minted row is
