---
type: "Interface"
title: "IF-057"
description: "reads interfaces.toml (IF-ID column) and system-requirements.csv (SR-ID column) to resolve a proposed plan's per-WI Interfaces/Covers cites - the dual-plan coverage pre-pass (process-options.md \"Dual-plan decomposition\"); registries absent = refs unvalidated with an honest note, never findings"
tags: ["drafted"]
resource: "docs/requirements/interfaces.toml (IF-057)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-057) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-057 — Interface

**Contract.** reads interfaces.toml (IF-ID column) and system-requirements.csv (SR-ID column) to resolve a proposed plan's per-WI Interfaces/Covers cites - the dual-plan coverage pre-pass (process-options.md "Dual-plan decomposition"); registries absent = refs unvalidated with an honest note, never findings
