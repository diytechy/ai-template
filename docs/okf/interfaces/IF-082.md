---
type: "Interface"
title: "IF-082"
description: "IF-056's derivation-loader seam, as HELD by the split sibling that now carries the import (WI-280): traj_parse.py is gen_trajectory's registry/doc/git source layer and reads the validator's loaders + path constants (read_rows/_split_refs/_norm_module/load_ifs/SR_CSV) directly. The CONTRACT is IF-056's, unchanged and stated once there; this row exists because a seam is declared per importing module and the import moved."
tags: ["Stable"]
resource: "docs/requirements/interfaces.toml (IF-082)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-082) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-082 — Interface

**Contract.** IF-056's derivation-loader seam, as HELD by the split sibling that now carries the import (WI-280): traj_parse.py is gen_trajectory's registry/doc/git source layer and reads the validator's loaders + path constants (read_rows/_split_refs/_norm_module/load_ifs/SR_CSV) directly. The CONTRACT is IF-056's, unchanged and stated once there; this row exists because a seam is declared per importing module and the import moved.
