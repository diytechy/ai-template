---
type: "Interface"
title: "IF-096"
description: "config.SCHEMA / validate(): the converter renders the blank form from the same table the loader validates against, and proves its own output loads clean before writing it"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-096)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-096) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-096 — Interface

**Contract.** config.SCHEMA / validate(): the converter renders the blank form from the same table the loader validates against, and proves its own output loads clean before writing it
