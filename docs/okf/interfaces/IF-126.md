---
type: "Interface"
title: "IF-126"
description: "reads the approval baseline through IF-123 so the generated open-items surface can say which decision rows have moved after the last signing. Consumes the READ half only - load_all, rows_for and the SNAPSHOT_DIR constant - never copy_live. An absent snapshot is the clean pre-signing skip, never an empty baseline."
tags: ["draft"]
resource: "docs/requirements/interfaces.toml (IF-126)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-126) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-126 — Interface

**Contract.** reads the approval baseline through IF-123 so the generated open-items surface can say which decision rows have moved after the last signing. Consumes the READ half only - load_all, rows_for and the SNAPSHOT_DIR constant - never copy_live. An absent snapshot is the clean pre-signing skip, never an empty baseline.
