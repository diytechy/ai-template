---
type: "Interface"
title: "IF-052"
description: "reads the cached derived gate (first non-comment line, the derive_gate.py cache) as the Process tab's render condition and current-gate highlight; an absent docs/gate omits the tab so a gate-less repo renders byte-identically"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-052)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-052) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-052 — Interface

**Contract.** reads the cached derived gate (first non-comment line, the derive_gate.py cache) as the Process tab's render condition and current-gate highlight; an absent docs/gate omits the tab so a gate-less repo renders byte-identically
