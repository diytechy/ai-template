---
type: "Interface"
title: "IF-067"
description: "agent_dispatch.py module: the parallel dispatcher + serialized integrator split out of agent_loop (WI-218) - dispatch_run(args, root) is the whole engine (reconcile -> gate -> build-out, SR-061), re-launching the sibling agent_loop.py per worker assignment; the reservation/traincar/worktree family, the CAS-only integrator (integrate_train/blocked_disposition/dual_plan_disposition/publish_integration, SR-063), the SR-065 migration gate + telemetry, and the generated run-state all live here. agent_loop re-exports the historical names, so its public surface is unchanged."
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-067)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-067) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-067 — Interface

**Contract.** agent_dispatch.py module: the parallel dispatcher + serialized integrator split out of agent_loop (WI-218) - dispatch_run(args, root) is the whole engine (reconcile -> gate -> build-out, SR-061), re-launching the sibling agent_loop.py per worker assignment; the reservation/traincar/worktree family, the CAS-only integrator (integrate_train/blocked_disposition/dual_plan_disposition/publish_integration, SR-063), the SR-065 migration gate + telemetry, and the generated run-state all live here. agent_loop re-exports the historical names, so its public surface is unchanged.
