---
type: "Interface"
title: "IF-095"
description: "config.load_config(root) -> (Config, [Finding]): the attestation boundary and the final-review policy, typed; a non-empty finding list means the caller refuses rather than reading a default as a decision"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-095)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-095) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-095 — Interface

**Contract.** config.load_config(root) -> (Config, [Finding]): the attestation boundary and the final-review policy, typed; a non-empty finding list means the caller refuses rather than reading a default as a decision
