---
type: "Interface"
title: "IF-008"
description: "check_doc_refs.py CLI: --strict exits 1 on a dangling backticked repo path or a sym:<module>.<name> absent from the arch-map; warn-first (exit 0) otherwise"
tags: ["Drafted"]
resource: "docs/requirements/interfaces.toml (IF-008)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-008) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-008 — Interface

**Contract.** check_doc_refs.py CLI: --strict exits 1 on a dangling backticked repo path or a sym:<module>.<name> absent from the arch-map; warn-first (exit 0) otherwise
