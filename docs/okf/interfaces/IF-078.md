---
type: "Interface"
title: "IF-078"
description: "sibling import for the registry write side: the filing-home resolution and write_spec_file(row) when file_selected_wis files selected Plan-WIs as docs/work/queued/ spec files - the folder half of IF-061's write seam; CSV mode byte-unchanged."
tags: ["drafted"]
resource: "docs/requirements/interfaces.toml (IF-078)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-078) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-078 — Interface

**Contract.** sibling import for the registry write side: the filing-home resolution and write_spec_file(row) when file_selected_wis files selected Plan-WIs as docs/work/queued/ spec files - the folder half of IF-061's write seam; CSV mode byte-unchanged.
