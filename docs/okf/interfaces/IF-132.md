---
type: "Interface"
title: "IF-132"
description: "the dashboard's How-SW module source (WI-455, sitting-2 decision 8): sw_modules consumes gen_arch_map.scan_inventory(src_roots) over the declared [paths] src root and keeps only modules with >=1 public function symbol, replacing the retired parse of docs/architecture.md's committed MODULE MAP block - registries/source -> dashboard, no markdown way-station."
tags: ["Drafted"]
resource: "docs/requirements/interfaces.toml (IF-132)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.toml (IF-132) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/process.toml [checks] okf_export = false silences the layer).

# IF-132 — Interface

**Contract.** the dashboard's How-SW module source (WI-455, sitting-2 decision 8): sw_modules consumes gen_arch_map.scan_inventory(src_roots) over the declared [paths] src root and keeps only modules with >=1 public function symbol, replacing the retired parse of docs/architecture.md's committed MODULE MAP block - registries/source -> dashboard, no markdown way-station.
