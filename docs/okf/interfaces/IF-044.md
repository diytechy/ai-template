---
type: "Interface"
title: "IF-044"
description: "agent_route.py module/CLI: load_registry/load_enabled + resolve_enabled (a version-less enable-list token -> the newest pair in its Family-Model line) + parse_env (a pair row's Env cell) + select(pool,tier)->routed id honoring per-pair cooldown, the Family-keyed reviewer heterogeneity preference and tier-up-never-down; escalate()/failure_action() the fixed win-stay/lose-shift policy keyed to docs/gate-policy; --select prints the routed id + reason, --list the enabled pool"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-044)"
---
> **GENERATED — a reference copy, not the source of truth.** Derived from docs/requirements/interfaces.csv (IF-044) by scripts/gen_okf.py; edit the registry/doc, then rerun it (docs/okf-export: off silences the layer).

# IF-044 — Interface

**Contract.** agent_route.py module/CLI: load_registry/load_enabled + resolve_enabled (a version-less enable-list token -> the newest pair in its Family-Model line) + parse_env (a pair row's Env cell) + select(pool,tier)->routed id honoring per-pair cooldown, the Family-keyed reviewer heterogeneity preference and tier-up-never-down; escalate()/failure_action() the fixed win-stay/lose-shift policy keyed to docs/gate-policy; --select prints the routed id + reason, --list the enabled pool
