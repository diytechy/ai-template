---
type: "Interface"
title: "IF-044"
description: "agent_route.py module/CLI: load_registry/load_enabled + select(pool,tier)->routed id honoring per-model cooldown, the reviewer heterogeneity preference and tier-up-never-down; escalate()/failure_action() the fixed win-stay/lose-shift policy keyed to docs/gate-policy; --select prints the routed id + reason, --list the enabled pool"
tags: ["Stable"]
resource: "docs/requirements/interfaces.csv (IF-044)"
---

# IF-044 — Interface

**Contract.** agent_route.py module/CLI: load_registry/load_enabled + select(pool,tier)->routed id honoring per-model cooldown, the reviewer heterogeneity preference and tier-up-never-down; escalate()/failure_action() the fixed win-stay/lose-shift policy keyed to docs/gate-policy; --select prints the routed id + reason, --list the enabled pool
