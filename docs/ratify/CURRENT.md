# Re-attestation brief — spine rows owing a human act

_GENERATED by `trace.py --approve modified` (WI-316) — do not hand-edit; cite the spine registries, not this rendering. One section per SR that is `Drafted`, or whose chain has DRIFTED from the approved snapshot; each chain row shows only its CHANGED cells, before (the snapshot) vs after (the working tree), approved cells first. `Status` itself is never listed — the marker is not the amendment. Rule on each section: bless → set `Status` to `Approved` (process.md §7) — and from the first signing onward, run `intake.py snapshot` in the SAME commit, or the record of what was blessed does not move._

_Baseline: `docs/archive/last_approved` — copied 2026-08-23 (27a30842), the commit that last wrote this record._

_Approval provenance: the last commit to move a `Status` cell in a snapshotted registry is a296b4ff (2026-08-23) — the record's maturity cells have not moved since._


## SR-049 — Derived stage from artifact states


### LLR LLR-147
- **Detail**
  - before: Narrowed: this row named a standalone `sn_gate` function and a `docs/gate` basis-line field (`uncovered=N`); the ruled stage unification deleted `sn_gate`, `_raw_level` and the basis line itself with the bar axis, and no field carrying an uncovered COUNT survives. What the row actually served — an approved SN cited by zero SRs must not read as finished — is still enforced, folded directly into `spine_stage` (`spine_rules.py:711-715`): `spine_stage` reads the cited set `sn_cited_ids` builds from SR SN-Refs (an F5 duplicate of trace.py's parse, pinned equal by test_rule_sync) and returns `DevStg-Needs` for an APPROVED SN cited by zero SRs — an approved-but-unanswered need has not earned the next rung — while a Draft SN stays exempt from the coverage check and rides the existing draft rung (one fact, one finding; the itemized 'SN has no SR' listing remains trace.py's orphan rule at DevStg-Tests strictness, reading the SAME cited set, so the stage input and the listing cannot contradict on one registry state). The per-phase call passes the repo's whole settled SR set as `cited_srs` so a phase's own rows are never read as the coverage scope (spine_stage's own docstring, `:685-693`). The uncovered COUNT this row also claimed is not carried forward — nothing in `docs/stage` reports a bare uncovered=N — and is recorded here as dropped rather than silently implied.
  - after: Narrowed: this row named a standalone `sn_gate` function and a `docs/gate` basis-line field (`uncovered=N`); the ruled stage unification deleted `sn_gate`, `_raw_level` and the basis line itself with the bar axis, and no field carrying an uncovered COUNT survives. What the row actually served — an approved SN cited by zero SRs must not read as finished — is still enforced, folded directly into `spine_stage` (`spine_rules.py:711-715`): `spine_stage` reads the cited set `sn_cited_ids` builds from SR SN-Refs (ONE home — `kitlib/spine.py`, re-exported by both modules; it was a duplicate of trace.py's parse held equal by a test_rule_sync pin, and the pin retired with the copy, so the agreement is now unrepresentable-to-break rather than detected) and returns `DevStg-Needs` for an APPROVED SN cited by zero SRs — an approved-but-unanswered need has not earned the next rung — while a Draft SN stays exempt from the coverage check and rides the existing draft rung (one fact, one finding; the itemized 'SN has no SR' listing remains trace.py's orphan rule at DevStg-Tests strictness, reading the SAME cited set, so the stage input and the listing cannot contradict on one registry state). The per-phase call passes the repo's whole settled SR set as `cited_srs` so a phase's own rows are never read as the coverage scope (spine_stage's own docstring, `:685-693`). The uncovered COUNT this row also claimed is not carried forward — nothing in `docs/stage` reports a bare uncovered=N — and is recorded here as dropped rather than silently implied.

### LLR LLR-197 — ADDED since the snapshot
- **LLR-ID**: LLR-197
- **SR-Refs**: SR-157;SR-049
- **Title**: The spine ROW vocabulary as one home below both modules that judge a row
- **Module**: project-trajectory/scripts/kitlib/spine.py
- **CodeSymbol**: LLR_EXEMPT/is_drafted/is_approved/is_founded/llr_exempt/phase_num/sn_all_ids/sn_cited_ids/refs/is_example/load_csv
- **Detail**: What one registry row's cells MEAN, stated once below every reader: the three Status predicates of the closed {Drafted, Approved, Founded} vocabulary (each matching case-insensitively on the stripped cell, the one Status-casing rule), the LLR-exemption set and its predicate (matched on the STRIPPED cell, which is the direction review 017 caught the two copies disagreeing on), the digit extraction a free-form Phase cell parses through, the whole-text SN id scrape and the SN-Refs citation set the coverage rung and the orphan listing must read identically, plus the ref split, the -000 placeholder test and the registry CSV loader those rules are written on. The module imports only csv and re and no sibling of scripts/, which is the package rule that keeps the scaffolder's import graph small. sn_draft_ids is DELIBERATELY NOT HERE and the omission is part of the design: it was the tenth duplicate of the same pair, but its body is a delegation to spine_carrier, which this package may not import, so both former holders bind that sibling function directly under the same local name and the wrapper disappears rather than relocating. NOT CLAIMED: that these are the only shared row rules - is_example still has a third home in gen_release_checklist.py, pinned by test_rule_sync rather than consolidated.
- **Rationale**: trace.py ENFORCES the spine and spine_rules.py DERIVES its stage from the same rows, so every question either asked of a single cell was asked twice: nine duplicated function bodies held equal by nine tests/test_rule_sync.py equality pins. That is POLICY duplication, not plumbing, and a disagreement between those two modules is a false green or a false red AT A GATE - the exact failure class an adversarial review caught in the wild, the LLR-exemption copies differing on a whitespace-padded method. Under the retired F5 rule a pin was the most a test could do: it could CONTAIN the drift and never remove it. Owner ruling D-8 (OI-16) replaced F5, so the rules move to one home and the pins retire - drift made unrepresentable rather than detected, the declared-line precedent applied to the pair the census scored highest. The extraction also settled a divergence every one of those batteries was structurally blind to: LLR_EXEMPT was a tuple in one copy and a set in the other, and the pin compared set(a) == set(b), so the type never entered the assertion; the one home is a frozenset, because a mutable closed vocabulary handed out by a shared kernel can be moved by any caller for every other reader in the process. Its OWN module rather than kitlib/registry.py: that module is the docs/work/ spec-folder reader - a different registry, a different carrier, a different consumer set - and folding the two together would have grown the package's largest module into exactly the generic bucket the 2026-08-19 review's shape constraint (H-09) forbids this package. The component tag is single for LLR-184's reason: the readers straddle nothing today (every consumer is CMP-006), so no kernel declaration is warranted yet, and the multi-membership advisory keeps surfacing the module if that changes.
- **TestRefs**: TC-193
- **Status**: Drafted
- **Component**: CMP-006
- **Phase**: 5

### TC TC-193 — ADDED since the snapshot
- **TC-ID**: TC-193
- **Verifies**: SR-157;LLR-197
- **Level**: Unit
- **Method**: The consolidation is driven from both ends. IDENTITY, which is the deletion's warrant: test_the_spine_row_vocabulary_is_one_home asserts every re-exported name in trace.py, trace_text.py and spine_rules.py IS the kitlib.spine object (`is`, not `==` - a value battery over one shared function is the vacuous-agreement shape test_rule_sync exists to refuse), and that both sn_draft_ids names bind the carrier function each module itself imported. VALUES, which is the half of the retired batteries that was never about sameness: the three Status predicates are driven over the casing/whitespace/None battery WITH the four retired spellings (Draft, Verified, Planned, Modified) each pinned to False, the LLR-exemption is pinned over the closed vocabulary including review 017's whitespace-padded case and the frozenset TYPE, and the SN scrapes are pinned whole-text, -000-excluding, separator-splitting and filter-nothing. SHIPPING: test_bootstrap.py::test_the_common_package_ships_complete bootstraps a REAL scaffold and asserts the copied scripts/kitlib/ holds exactly the kit's module set, so a MAPPING row omitted for this module reds there rather than in a downstream repo's first check.
- **Tier**: Smoke
- **Expected**: Satisfies the acceptance folded into LLR-197 (parent SR-157)
- **Automated**: Yes
- **Evidence**: tests/test_rule_sync.py::test_the_spine_row_vocabulary_is_one_home; tests/test_rule_sync.py::test_the_status_predicates_answer_by_value; tests/test_rule_sync.py::test_the_llr_exemption_answers_by_value; tests/test_rule_sync.py::test_the_sn_scrapes_answer_by_value; tests/test_bootstrap.py::test_the_common_package_ships_complete
- **Status**: Drafted
- **Phase**: 5
