# Re-attestation brief — spine rows owing a human act

_GENERATED by `trace.py --approve modified` (WI-316) — do not hand-edit; cite the spine registries, not this rendering. One section per SR that is itself `Drafted`, has a `Drafted` LLR or TC anywhere in its chain, or whose chain has DRIFTED from the approved snapshot; each chain row shows only its CHANGED cells, before (the snapshot) vs after (the working tree), approved cells first — or, for a `Drafted` row with no cell diff, its full current content, since it owes only because it was never approved. `Status` itself is never listed — the marker is not the amendment. Rule on each section: bless → set `Status` to `Approved` (process.md §7) — and from the first signing onward, run `intake.py snapshot` in the SAME commit, or the record of what was blessed does not move._

_Baseline: `docs/archive/last_approved` — copied 2026-08-23 (2b7be11a), the commit that last wrote this record._

_Approval provenance: the last commit to move a `Status` cell in a snapshotted registry is 2b7be11a (2026-08-23) — the record's maturity cells have not moved since._


## SR-070 — Generated views are offline, deterministic and drift-checkable


### LLR LLR-199 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **LLR-ID**: LLR-199
- **SR-Refs**: SR-070
- **Hat-Refs**: MAINTAINER
- **Title**: The generated component view: the join, its three coverage edges and the approval it may never carry
- **Module**: project-trajectory/scripts/gen_components.py
- **CodeSymbol**: build/render/seam_placement/module_components
- **Detail**: The component view as a DERIVED registry rather than a hand-authored prose home. gen_components.py joins the four registries that already carry the facts - components.toml declares WHICH components exist, low-level-requirements.toml carries membership on its Component cell, system-requirements.toml is reached THROUGH that membership, interfaces.toml supplies the seams - and writes docs/requirements/components.derived.toml, declared in docs/stack.ini [generated] as kind components, freshness-gated by check.py's component-view step, regenerated on the trunk by trunk_step --regen, and stamped with the generated-but-live staleness header. Per component it emits the design rows that belong to it, the requirements those rows decompose, the perspectives that bear (trace.effective_hats over the members, so an SR-tier re-ruling propagates with no design cell edited), the modules, and the seams split internal from boundary by the components layer's own rule. THE THREE COVERAGE EDGES ARE ANSWERED IN THE OUTPUT, not in a comment: a requirement reaching no component is listed once in the unplaced table and counted, because nothing in any registry distinguishes a decomposition not yet written from a constraint over every component and a per-repo judgement has no place in shipped machinery; a requirement reaching several appears in EVERY one of them and additionally in that component's sr_shared_refs, so the view never implies exclusive ownership; a seam is placed by its Component tag UNIONED with the components its endpoints resolve into through trace_text.norm_module, and one that resolves nowhere is named in the unplaced table rather than dropped. NO APPROVAL CELL APPEARS, EVER: a generated file that flipped an approval would route around the very dial the human-approval policy governs, so the hand file declares the component and holds its maturity, this file describes it. Vacuous - exit 0, nothing written - for a repo with no real CMP row and no view on disk, because the component layer is optional and off-spine. NOT DISCHARGED, stated rather than left to be found: the view shows no INTERNALS by ruling, so a reader who needs mechanism reads the design rows it names; and freshness compares the artifact to its regeneration, never asking whether the SOURCE rows were right - that residue is the amend-without-flip guard this row does not build.
- **Rationale**: The column this replaces promised a prose home the kit never created and no script ever read, which is the weakest form a design record can take: its staleness was undetectable by construction, so nothing could ever report it wrong. A derived view inherits every check the spine already carries - the rows it joins have to be correct for the gate to pass at all - and it is stale-DETECTABLE by the same --check mechanism the other nine generated artifacts use. The cost is real and is accepted rather than hidden: a requirement list says what a component is obliged to do and nothing about how its parts fit, so the artifact that stays true is bought at the price of the artifact that showed mechanism. The coverage edges are answered in the emitted file rather than in the generator's head because a view that silently drops rows is indistinguishable from one with a bug, and the distinction between the two is the only thing that makes a derived artifact worth reading.
- **TestRefs**: TC-195
- **Status**: Drafted
- **Component**: CMP-009
- **Phase**: 5

### TC TC-195 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **TC-ID**: TC-195
- **Verifies**: SR-070;LLR-199;IF-139;IF-140;IF-141;IF-142;IF-143
- **Level**: Unit
- **Method**: The join, the three coverage edges, the standing constraint and the freshness contract, each driven against a two-component spine small enough to reason about by hand. THE JOIN: assert membership comes from the design tier's Component tag, that the requirement tier is reached through it, and that the modules follow. DERIVED NEVER COPIED: a design row carrying no hat cell still contributes its SR parent's perspectives, and re-ruling that one requirement moves the view with no design cell edited. EDGE 1: a requirement with no design child appears in the unplaced table, is counted there, and appears in no component section - visible rather than dropped. EDGE 2: a requirement reaching two components appears in both and is marked shared in both, while one reaching a single component is not. EDGE 3: seams enter the view, an interface with both endpoints inside a component is internal to it while one crossing two is a boundary of BOTH, an UNTAGGED interface is still placed through its endpoints, and one that resolves nowhere is reported as unplaced. NO APPROVAL: with one Approved and one Drafted component declared in the hand file, no maturity key and neither word appears in any emitted cell. FRESHNESS, DRIVEN BOTH WAYS: --check greens on a fresh view, reds after a registry row moves, and greens again once regenerated; a missing view reds; the written bytes carry no CRLF and two renders agree. VACUITY: a repo whose only CMP row is the inert -000 example writes nothing and exits zero on both paths.
- **Tier**: Smoke
- **Expected**: Satisfies the acceptance folded into LLR-199 (parent SR-070)
- **Automated**: Yes
- **Evidence**: tests/test_gen_components.py::test_membership_and_the_requirement_reach; tests/test_gen_components.py::test_the_hat_union_is_DERIVED_from_the_parent_not_copied; tests/test_gen_components.py::test_a_parent_re_ruling_moves_the_view_with_no_design_cell_edited; tests/test_gen_components.py::test_a_childless_requirement_is_listed_ONCE_and_never_dropped; tests/test_gen_components.py::test_a_shared_requirement_appears_in_EVERY_component_it_reaches; tests/test_gen_components.py::test_seams_enter_the_view_split_internal_from_boundary; tests/test_gen_components.py::test_an_untagged_seam_is_placed_by_its_ENDPOINTS; tests/test_gen_components.py::test_an_unplaceable_seam_is_REPORTED_not_silently_excluded; tests/test_gen_components.py::test_the_generated_view_carries_NO_approval_cell; tests/test_gen_components.py::test_check_reds_on_a_stale_view_and_greens_once_regenerated; tests/test_gen_components.py::test_check_reds_when_the_view_is_missing_entirely; tests/test_gen_components.py::test_the_written_file_is_LF_and_reproducible; tests/test_gen_components.py::test_a_repo_with_no_real_component_is_VACUOUS; tests/test_gen_components.py::test_the_cli_runs_out_of_process
- **Status**: Drafted
- **Phase**: 5

## SR-146 — Prompts are reviewable files with a per-session audit trail


### TC TC-192 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **TC-ID**: TC-192
- **Verifies**: SR-146;LLR-164
- **Level**: Unit
- **Method**: `gen_prompt_catalog.render` is exercised directly rather than read: `test_the_catalogue_lists_every_shipped_prompt_with_its_digest` asserts every `prompts.KIT_PROMPTS` key and every `prompts.catalog_rows()` digest appears in the rendered text (the PURE-render claim, since it reads only the shipped templates); `test_the_catalogue_on_disk_is_FRESH` runs `catalog.main(["--check"])` and asserts exit 0 against the committed file (the freshness gate LLR-164 names); `test_a_template_edit_moves_its_digest_and_so_the_catalogue` asserts a real text edit moves the digest while a bare line-ending difference does not (the CRLF-vs-stale-catalogue claim `--check` exists to avoid); `tests/test_generated_freshness_wiring.py::test_prompt_catalog_step_reds_when_a_template_changes` drives the wired `[step:]` enforcer end to end — edits a template, asserts the commit-floor step reds, then runs `gen_prompt_catalog.py` and asserts it goes green — over a bootstrapped scaffold, not the source tree alone.
- **Tier**: Smoke
- **Expected**: Satisfies the acceptance folded into LLR-164 (parent SR-146)
- **Automated**: Yes
- **Evidence**: tests/test_routing_and_prompts.py::test_the_catalogue_lists_every_shipped_prompt_with_its_digest; tests/test_routing_and_prompts.py::test_the_catalogue_on_disk_is_FRESH; tests/test_routing_and_prompts.py::test_a_template_edit_moves_its_digest_and_so_the_catalogue; tests/test_generated_freshness_wiring.py::test_prompt_catalog_step_reds_when_a_template_changes
- **Status**: Drafted
- **Phase**: 5

## SR-157 — Spine and work-registry rules red the harness verdict


### LLR LLR-201 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **LLR-ID**: LLR-201
- **SR-Refs**: SR-157
- **Title**: The checker's cross-row coherence rules, sited below the engine that composes them
- **Module**: project-trajectory/scripts/coherence.py
- **CodeSymbol**: spine_orphan_findings/tc_citation_findings/llr_module_ids/budget_backlink_findings/delegation_findings/component_membership_findings/knowledge_pack_advisories/PhaseScope/status_criterion_findings
- **Detail**: Every rule of the checker that needs MORE THAN ONE ROW to have an opinion, in one module below the engine that runs them. The four-tier orphan rules answer whether each row has the children its tier requires and whether each id it cites resolves - an SR its LLR, TC and SN, an LLR its SR parent and TC, a TC a real spine id (seam ids supplement, never replace, a spine citation), an SN at least one citing SR - and each tier now returns the same at-fault-id-and-finding pair, so the id set the rendered views flag is collected in ONE place rather than at eight append sites. The off-spine resolutions join the same way: a PB row's Refs against SR ids, LLR ids and LLR Module paths; a REPO row's DelegatedSRs against SR ids; a CMP row's PartOf and SupersededBy plus the Component tags on LLR, IF, PART and ASSET rows against the component registry; and a CMP Knowledge cell's docs/knowledge prefix against a real pack file, which is this module's single filesystem read and is named as such rather than left for a reader to find. PhaseScope resolves the delivery filter once - the requested labels plus the foundation phase every filter includes - and the require-verified status criterion is scoped by it. Emission ORDER is load-bearing and belongs to the composer: SR, then LLR, then TC, then SN, which is what the report and the console compare.
- **Rationale**: The engine that ran these was one 553-line function at five times the complexity limit, and the reason it could not be simplified in place is recorded in the ratchet it breached: a helper extracted INWARD is charged to its enclosing function by the same measure, so the only decomposition that pays is outward. Outward then raises the siting question, and the answer is a boundary that can be stated in one sentence rather than a line count: a rule that JOINS ACROSS ROWS lives here, and a rule that inspects one row's prose, or asks whether a FILE parses, stays with the engine and its carriers. That split is what makes the module honest about what it is - the checker's coherence half - instead of a bucket named after the function it was carved from, which is the generic-module failure this package was warned about by name. It is a plain sibling rather than a shipped-helper module for the reason the census recorded one slice earlier: the scaffolder may import the shared package and nothing else, and the checker's rules are no business of the scaffolder. The two attribute bags around the engine were typed in the same act because they are the same defect one level up - an empty class populated by assignment cannot say which fields exist, which is why two readers had to defend themselves with getattr and a default.
- **TestRefs**: TC-197
- **Status**: Drafted
- **Component**: CMP-006
- **Phase**: 5

### TC TC-197 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **TC-ID**: TC-197
- **Verifies**: SR-157;LLR-201
- **Level**: Unit
- **Method**: Two claims, because the row makes two. THE RULES, each driven directly rather than through the engine: the orphan pass emits SR then LLR then TC then SN and returns exactly the at-fault ids of the findings it emitted; a Drafted SR stands the child-completeness rules down while its SN linkage still applies; a PB Refs cell resolves against an LLR Module path as well as against ids, and an empty one is a finding; a Component tag naming no CMP row is a finding while an empty component registry disarms the membership half entirely; a knowledge ref resolves only under the docs/knowledge prefix and a traversal ref never escapes the pack root; the phase scope covers the foundation phase under every filter and a blank Phase always; and the status criterion defers an out-of-phase row rather than failing it. THE BOUNDARY AND THE BAGS: assert that this module imports no sibling of the scripts directory, that the engine still answers to every moved name and that each definition lives here; that the loaded registry record is frozen, total, and constructed at exactly one site; that the findings record's list defaults are per instance and that the two fields filled after the pure pass returns are DECLARED rather than conjured at a call site; that the flags record accepts either itself or a CLI namespace; that no caller imports argparse to forge the engine's config; and that the composer stays a composer - measured line span, and no nested def, which is how the number this split removed got to fifty.
- **Tier**: Smoke
- **Expected**: Satisfies the acceptance folded into LLR-201 (parent SR-157)
- **Automated**: Yes
- **Evidence**: tests/test_trace_coherence.py::test_the_orphan_rules_emit_sr_then_llr_then_tc_then_sn; tests/test_trace_coherence.py::test_a_drafted_row_stands_the_child_rules_down_but_not_its_sn_link; tests/test_trace_coherence.py::test_off_spine_backlinks_resolve_against_ids_and_llr_module_paths; tests/test_trace_coherence.py::test_component_membership_is_checked_from_the_primitive_side; tests/test_trace_coherence.py::test_the_knowledge_ref_resolves_only_the_pack_prefix; tests/test_trace_coherence.py::test_the_phase_scope_always_covers_the_foundation_phase; tests/test_trace_coherence.py::test_the_status_criterion_defers_out_of_phase_and_stands_down_for_drafted; tests/test_trace_coherence.py::test_registries_is_frozen_and_total; tests/test_trace_coherence.py::test_the_loader_is_the_only_construction_site_of_registries; tests/test_trace_coherence.py::test_findings_list_defaults_are_per_instance; tests/test_trace_coherence.py::test_the_two_post_analyze_fields_are_declared_not_conjured; tests/test_trace_coherence.py::test_analysis_flags_takes_a_record_or_a_namespace; tests/test_trace_coherence.py::test_no_caller_forges_an_argparse_namespace_for_the_engine; tests/test_trace_coherence.py::test_the_coherence_rules_import_no_sibling_of_scripts; tests/test_trace_coherence.py::test_trace_re_exports_every_moved_name; tests/test_trace_coherence.py::test_analyze_is_a_composer_and_stays_one
- **Status**: Drafted
- **Phase**: 5

## SR-159 — Declared-architecture connectivity gaps are reported


### LLR LLR-041
- **Detail**
  - before: Loads interfaces.csv; validates IF ids, SR back-links and Component tags, and emits the best-effort endpoint-module advisory. Malformed or duplicate ids and unresolved SR/Component links are findings; an unmatched ThisProject endpoint stays advisory-only, and an absent interface registry is vacuous.
  - after: Loads the interface registry; validates IF ids, SR back-links and Component tags, and emits the best-effort endpoint-module advisory. Malformed or duplicate ids and unresolved SR/Component links are findings; an unmatched module-shaped Provider endpoint stays advisory-only, and an absent interface registry is vacuous.

## SR-160 — Front-door launchers for the two universal contributor actions


### LLR LLR-193 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **LLR-ID**: LLR-193
- **SR-Refs**: SR-160
- **Title**: Root launcher interpreter selection: the loop-resume half's probe-then-floor, the front door's own robustness
- **Module**: agent-resume.sh
- **CodeSymbol**: pick_py
- **Detail**: pick_py probes each candidate interpreter twice, because 'found' and 'usable' are different questions: first that it RUNS at all (a Windows python3 on PATH is often the Microsoft-Store alias stub, which exists and executes nothing), then that its sys.version_info clears the 3.11 floor every kit script's tomllib import needs. Both .venv layouts are tried first (bin/ POSIX, Scripts/ a Windows-created venv), then python3, then python on PATH, so the pinned toolchain a contributor built wins over whatever ambient interpreter PATH happens to resolve. A refusal names every rejected candidate and why (PYWHY), never a bare 'not found', so the front door reports clearly instead of dying deep inside the engine at an obscure import. agent-resume.cmd's :pickpy label runs the identical two-stage probe for cmd.exe (.venv\Scripts\python.exe, .venv\bin\python, then python, then the py launcher), and agent-resume.command carries no probe of its own - its whole body delegates (exec ./agent-resume.sh) - so the policy reaches it by inheritance, not by a second copy. Together the three root files realize, on Windows/macOS/Linux alike, the loop-resume half of SN-034's two universal contributor actions as one action from the repository's front door. NOT DISCHARGED, stated rather than left to be discovered: SN-034 names TWO universal actions, and only this one - resuming the loop - has a root launcher today. The environment-preparation half's scripts (onboard.sh, dev-setup.sh) materialize under scripts/, not at the repository root SR-160's acceptance names (bootstrap.py's MAPPING; README.md's own 'Still owed' ledger states the gap in this repo's delivery). This row cites what exists rather than a module that does not, and the residual is recorded as a finding, not fabricated as a citation.
- **Rationale**: Realizes the acceptance clause 'does not crash cryptically' the parent states for a launcher with no working interpreter: an ambient below-floor python can boot silently past a working project .venv and die deep inside the engine at an obscure import, a failure mode invisible to any check that only reads launcher TEXT rather than running one. Two probes are the whole fix, because a launcher that only asks 'does this run' cannot distinguish a working-but-old interpreter from a working one; asking sys.version_info makes the floor an observable rather than an assumption. The residual is left unminted rather than described in the abstract, because SR-160's evidence discipline is that a cited module and symbol must exist to be read - inventing a plausible-sounding root onboarding launcher would be the exact defect the discipline exists to prevent, and the gap is real, load-bearing, and already named in this repo's own README.
- **TestRefs**: (see TC-188)
- **Status**: Drafted
- **Component**: CMP-009
- **Phase**: 5

### TC TC-188 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **TC-ID**: TC-188
- **Verifies**: SR-160;LLR-193
- **Level**: Integration
- **Method**: Eleven live-launcher cases, run against a REAL interpreter with sys.version_info spoofed rather than a rehearsal of the probe string, so the assertion is on the selection RULE and not on a fake that merely fails to run. THE PREFERENCE: a valid project .venv beside a below-floor ambient python is taken over the ambient one, on both agent-resume.sh and agent-resume.cmd. THE REFUSAL: with nothing above the floor anywhere, the launcher exits nonzero, never reaches the engine, and names the floor (3.11) and each rejected candidate with why - the diagnostic repo-review 2026-08-19 H-01 asked for, since a bare 'not found' would misdescribe an interpreter sitting right there. THE OTHER DIRECTION: a PATH python that clears the floor is taken and runs the engine, on both platforms, so the refusal case above cannot be a launcher that simply never selects anything. THE FALLBACK: a stale below-floor .venv loses to a good PATH python rather than wedging the launcher - the venv is PREFERRED, not exempt - covering both the sh fallback and the cmd case of a present-but-unusable venv, plus cmd's alias-stub-to-py-launcher walk. THE INHERITANCE: the .command wrapper reaches the same venv preference as agent-resume.sh, and a companion assertion pins that its whole body delegates rather than holding a second copy of the policy.
- **Tier**: Full
- **Expected**: Satisfies the acceptance folded into LLR-193 (parent SR-160)
- **Automated**: Yes
- **Evidence**: tests/test_launcher_interpreter.py::test_sh_prefers_the_project_venv_over_a_below_floor_ambient_python[live]; tests/test_launcher_interpreter.py::test_sh_refuses_a_below_floor_interpreter_and_names_what_it_rejected[live]; tests/test_launcher_interpreter.py::test_sh_takes_a_path_python_that_satisfies_the_floor[live]; tests/test_launcher_interpreter.py::test_sh_falls_back_when_the_venv_itself_is_below_the_floor[live]; tests/test_launcher_interpreter.py::test_the_command_wrapper_inherits_the_venv_preference[live]; tests/test_launcher_interpreter.py::test_the_command_wrapper_declares_the_inheritance_and_holds_no_copy[live]; tests/test_launcher_interpreter.py::test_cmd_prefers_the_project_venv_over_a_below_floor_ambient_python[live]; tests/test_launcher_interpreter.py::test_cmd_refuses_a_below_floor_interpreter_and_names_what_it_rejected[live]; tests/test_launcher_interpreter.py::test_cmd_takes_a_path_python_that_satisfies_the_floor[live]; tests/test_launcher_interpreter.py::test_cmd_falls_back_when_the_venv_is_present_but_unusable[live]; tests/test_launcher_interpreter.py::test_cmd_walks_past_an_alias_stub_to_the_py_launcher[live]
- **Status**: Drafted
- **Phase**: 5

## SR-161 — Decompositions carry a perspective record


### LLR LLR-202 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **LLR-ID**: LLR-202
- **SR-Refs**: SR-161
- **Hat-Refs**: MAINTAINER
- **Title**: The perspective record going stale behind an amendment: the Hat-Refs arm of the amendment guard
- **Module**: project-trajectory/scripts/check_trajectory.py
- **CodeSymbol**: staged_hat_refs_findings/traced_cells
- **Detail**: LLR-183 makes the perspective record RESOLVABLE and derives the effective set; nothing yet asks whether a still-resolvable record is still TRUE. staged_hat_refs_findings(root) is that question, and it is a second arm of the amend-without-flip guard rather than a new mechanism: it reads the one amendment set staged_spine_amendments already computes and reports each row whose approved half is non-empty while Hat-Refs is absent from its traced half - the substance moved, the perspective record did not. THE COMPARISON IS BY CELL CLASS, WHICH IS THE WHOLE DESIGN: the measured defect it is built against is a neighbouring check that blames the registry by LINE, where writing an informative cell re-dates the row and a blame time cannot tell an approved cell from a traced one, so this arm keys off split_changed_cells' §A5.1 split and is silent on a traced-only edit - the perspective backfill's own shape. Tier scope is STRUCTURAL, not an allowlist: traced_cells (extracted from spine_cell_class, which needed the set rather than one column's class) answers whether the registry declares the column at all, so the test-case tier is silent because it carries no such cell and a third registry that gains one is guarded with no edit here. THE BASELINE IS THE GUARD'S OWN - HEAD versus the index over rows reading the same approved-text Status on both sides - which leaves two honest vacuities inherited rather than re-litigated: a row minted in the same commit has nothing to compare, and a row below approval has blessed nothing to amend behind a human's back. Warn-first and never an exit code, printed only under --staged. NOT DISCHARGED, stated rather than left to be found: the warn fires once, at the commit that earns it, where the last_approved snapshot would make the finding STAND until answered; that baseline was declined on the timing argument this row exists to serve and on the ruling's own words, and promoting the arm to a drift-tier finding is the next rung if the warn is measured to be ignored.
- **Rationale**: The component and knowledge views are DERIVED, and a derived view is only as true as the rows it derives from: freshness compares an artifact to its regeneration and never asks whether the source was right, so a generated page can be perfectly fresh and confidently wrong. That is the one obligation generation cannot discharge, and the measured instance is a design row that published a deleted symbol for five weeks and was caught by a census rather than by a check. Building it as an arm of an existing guard rather than as its own rule is what keeps the two from disagreeing about what an amendment IS - the same amendment set, the same cell classes, one home. Warn-first because the judgement it asks for is a re-read, not a refusal: whether a moved obligation changes which lenses bear is exactly the call a checker cannot make, and a rule that guessed it would be silenced.
- **TestRefs**: TC-198
- **Status**: Drafted
- **Component**: CMP-006
- **Phase**: 5

### TC TC-198 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **TC-ID**: TC-198
- **Verifies**: SR-161;LLR-202
- **Level**: Unit
- **Method**: The arm driven over a real two-commit git repo whose HEAD holds an Approved SR-001 (and, for the tier case, an Approved LLR-001 + TC-001), with the change STAGED rather than committed - the hook's own question. FIRES: the requirement cell amended while Hat-Refs stands, one finding naming the row and the moved cells, with the amendment arm firing alongside it so the second arm is proven an addition rather than a re-spelling. SILENT ON TRACED-ONLY MOVEMENT, the regression case and a measured one: writing the Hat-Refs cell itself - literally the perspective backfill's edit - raises nothing, and neither does an SN-Refs re-point, because no approved cell moved. SILENT WHEN DISCHARGED: substance and Hat-Refs moving together raises nothing here while the amendment arm still fires. VACUOUS WHERE THERE IS NO BASELINE, both shapes: a row minted in the staged commit, and a row amended below approval on both sides. STRUCTURALLY SILENT ON A TIER WITH NO SUCH COLUMN: an LLR and a TC amended in the same commit yield exactly one finding, the LLR's, so the scoping cannot pass by the arm being silent everywhere.
- **Tier**: Smoke
- **Expected**: Satisfies the acceptance folded into LLR-202 (parent SR-161)
- **Automated**: Yes
- **Evidence**: tests/test_trajectory_staged.py::test_amending_approved_cells_without_moving_hat_refs_warns; tests/test_trajectory_staged.py::test_hat_refs_arm_is_silent_when_only_traced_cells_move; tests/test_trajectory_staged.py::test_hat_refs_arm_is_silent_when_the_cell_moves_with_the_amendment; tests/test_trajectory_staged.py::test_hat_refs_arm_is_vacuous_where_a_row_has_no_baseline; tests/test_trajectory_staged.py::test_hat_refs_arm_is_silent_on_a_tier_that_carries_no_such_column
- **Status**: Drafted
- **Phase**: 5

## SR-162 — Requirement boundary references resolve against the declared frame


### LLR LLR-187 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **LLR-ID**: LLR-187
- **SR-Refs**: SR-162
- **Title**: Boundary references as resolvable cells: the frame's own joins, the SR-to-crossing rule, and the severity split that makes both adoptable
- **Module**: project-trajectory/scripts/trace.py
- **CodeSymbol**: frame_findings/sr_boundary_findings/_frame_report_section
- **Detail**: The resolution half of the declared frame, sited in the trace pipe beside the spine's other reference rules. frame_findings resolves the frame registry's own three reference fields - a crossing's Entity and a relationship's From and To - against the declared entity ids, returning plain finding strings rather than the (at_fault_id, finding) pairs the older spine rules return, because no caller ever read the id. It is a FAILURE class, not an advisory: a tie-back to an entity that does not exist is a dangling reference exactly like a requirement citing a deleted need. Its one non-obvious rule is the vacuity guard: an empty entity set is vacuous ONLY when nothing references it, since crossings referencing entities that do not exist otherwise resolve to silence and the report then prints that every reference resolves - the false green this rule exists to prevent. sr_boundary_findings carries the requirement-side join and returns (findings, advisories) at TWO severities on purpose: an SR whose Boundary-Refs names an undeclared crossing is HARD and joins the --strict failure set, while the uncovered-requirement count, the crossings named by no requirement, and the realization gap (a declared crossing no interface row ties back to through InterfaceFromExternal/InterfaceToExternal) are each ONE summary advisory that never gates. Both rules are vacuous for a project declaring no frame, and _frame_report_section renders the resolved state or the findings into the report. THE INTERFACE-CELL HALF, carved out of the connectivity row by ruling and owned here: the endpoint-pair cells and the closed discrete/variable signal vocabulary are this parent's observables, DECLARED at the IF entries of the required-field and closed-vocabulary tables in this same module and executed by the generic checker that stays the schema row's - so a rule landing at those two cells belongs to this row while the checker that runs it does not. NOT DISCHARGED, stated rather than left to be discovered: the parent also asks that two joined seams whose declared signal types are incompatible be reported naming both rows, and no join exists to read - the carriage cell is the only seam-to-seam edge in the tier and it carries no signal vocabulary, so the clause has no executable form until one is authored. The parent's own named residual - that a reviewed change to one side of the requirement/interface relationship carries or justifies the change on the other - is a review obligation nothing here claims.
- **Rationale**: A boundary reference that resolves against nothing is the same defect as a requirement citing a deleted need, and it went unpoliced for as long as the frame was prose. Making the frame a registry with typed reference cells converts the whole class from unfalsifiable to mechanical at the cost of one existence check per cell. The severity split is what makes the rule shippable rather than merely correct: resolution can be armed hard the day it exists because it reds only rows that cite something untrue, while coverage - every requirement names a crossing - would red most of a spine on the day the column appears, for work that belongs to an authoring campaign, and a gate that is fully red on arrival is a gate somebody switches off. So the uncovered count ships as the one number the campaign has to move. The realization gap is reported at the same restraint and for a stronger reason: whether every declared crossing owes an interface row is a question the frame's own ruling deferred, so gating on it here would enforce a decision nobody made. Vacuity for a frameless project is not a convenience - it is what lets the rule ship to adopters who have not drawn a boundary yet without teaching them to ignore a permanent warning.
- **TestRefs**: (see TC-182)
- **Status**: Drafted
- **Component**: CMP-006
- **Phase**: 5

### TC TC-182 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **TC-ID**: TC-182
- **Verifies**: SR-162;LLR-187
- **Level**: Integration
- **Method**: Five claims, one per severity the row declares, driven over a scaffolded frame rather than over the live one so the negative cases can exist. RESOLUTION IS HARD: a crossing naming an undeclared entity, a relationship naming one, and an interface tie-back naming an undeclared crossing are each reported and each join the --strict failure set; a clean frame is silent. THE VACUITY GUARD IS NOT A PASS: a frame declaring crossings and relationships but NO entity is a finding in its own right, because the alternative reading returns silence and the report then claims every reference resolves - the false green the rule exists to prevent. VACUITY THAT IS REAL: a frame declaring nothing, and an absent frame registry, both report nothing at all, and the SR-side rule is vacuous for the same absence - a project that has drawn no boundary must not inherit a permanent warning. THE SR JOIN, BOTH SEVERITIES: a requirement naming an undeclared crossing is a HARD finding and a declared one is clean, while the uncovered-requirement count is asserted to be exactly one summary line that exits zero under --strict, and the realization gap is asserted reported and never gated. THE CARVED-OUT INTERFACE CELLS: a signal value outside the closed discrete/variable vocabulary is reported naming the row and the vocabulary, and stays warn-first - the observable is this parent's while the checker that runs it is the schema row's, so the assertion is on the message and the exit code together.
- **Tier**: Full
- **Expected**: Satisfies the acceptance folded into LLR-187 (parent SR-162)
- **Automated**: Yes
- **Evidence**: tests/test_external_frame.py::test_a_clean_frame_produces_no_finding; tests/test_external_frame.py::test_a_crossing_naming_an_undeclared_entity_is_a_FINDING; tests/test_external_frame.py::test_a_relationship_naming_an_undeclared_entity_is_a_FINDING; tests/test_external_frame.py::test_an_IF_tieback_naming_an_undeclared_crossing_is_a_FINDING; tests/test_external_frame.py::test_crossings_with_NO_entity_declared_is_a_FINDING_not_a_vacuous_pass; tests/test_external_frame.py::test_a_frame_declaring_NOTHING_stays_vacuous; tests/test_external_frame.py::test_the_frame_rules_are_VACUOUS_without_the_registry; tests/test_external_frame.py::test_an_SR_naming_an_undeclared_crossing_is_a_HARD_finding; tests/test_external_frame.py::test_an_SR_naming_a_DECLARED_crossing_is_clean; tests/test_external_frame.py::test_SR_boundary_COVERAGE_is_a_summary_advisory_and_never_an_error; tests/test_external_frame.py::test_the_realization_gap_is_REPORTED_and_never_gated; tests/test_external_frame.py::test_the_SR_boundary_rule_is_vacuous_without_a_frame; tests/test_trace.py::test_signal_refuses_an_unknown_value_as_a_warn
- **Status**: Drafted
- **Phase**: 5

## SR-164 — Stakeholder-need scope is a declared, checked value


### LLR LLR-194 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **LLR-ID**: LLR-194
- **SR-Refs**: SR-164
- **Title**: The scope obligation's seam: the schema tier's required-field and closed-vocabulary check, not yet extended to SN
- **Module**: project-trajectory/scripts/trace.py
- **CodeSymbol**: schema_findings/REQUIRED_FIELDS/ENUM_FIELDS
- **Detail**: schema_findings already walks the real (non-placeholder) rows of one labelled table and reports the exact two failure classes SR-164 asks of scope: a column listed in REQUIRED_FIELDS[label] left empty names the row and the column, and a value present but outside ENUM_FIELDS[label]'s closed set names the row, the offending value and the allowed set. This is the mechanized shape SN-039's own acceptance text designates as scope's future home - 'the scope field enters the registry schema in the scheduled schema batch' - rather than a bespoke one-off column check. NOT DISCHARGED, stated rather than left to be discovered: SN carries no entry in REQUIRED_FIELDS or ENUM_FIELDS today, no dict, checker branch, or test anywhere in the shipped scripts names 'scope', and the whole-registry schema tier does not run over the needs registry at all. This row records the seam the scheduled batch is to ride, not machinery that discharges the obligation; a module or symbol implementing scope-checking does not exist to cite.
- **Rationale**: Citing the general required-field/closed-vocabulary mechanism rather than a fabricated scope-specific one is not a paraphrase of the parent - it identifies WHERE the scheduled schema-batch work lands and WHY it is cheap: the exact checking shape SR-164 wants already exists, generically, proven over six other tables (SR, LLR, TC, IF, CMP and the depth-0 frame's three), so adding scope is one REQUIRED_FIELDS/ENUM_FIELDS entry and one registry column, not a new checker. Recording that now is what keeps this SR from reading need-less on the day the column finally lands, and naming the seam honestly - rather than inventing a scope_findings function that does not exist - is what SR-164's own evidence discipline demands.
- **TestRefs**: (see TC-189)
- **Status**: Drafted
- **Component**: CMP-006
- **Phase**: 5

### TC TC-189 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **TC-ID**: TC-189
- **Verifies**: SR-164;LLR-194
- **Level**: Unit
- **Method**: The seam's own two behaviours, driven over an existing REQUIRED_FIELDS/ENUM_FIELDS table (IF today, since no SN entry exists yet) because the shape being verified is the mechanism scope is scheduled to join, not a scope-specific checker: a column absent from a row raises a warn naming the row and the exact column, gated only under --strict and never by the bare read; and a value present but outside a closed vocabulary is reported as a schema finding naming the row, the offending value and the full allowed set, staying silent at --strict-schema alone and gating only when --strict joins it. Both are the identical shape SR-164's acceptance asks of scope - missing or out-of-vocabulary, naming the row - over the mechanism the field is scheduled to extend rather than a fabricated one.
- **Tier**: Smoke
- **Expected**: Satisfies the acceptance folded into LLR-194 (parent SR-164)
- **Automated**: Yes
- **Evidence**: tests/test_trace.py::test_missing_required_if_field_is_a_warn; tests/test_trace.py::test_out_of_vocabulary_aspect_is_a_schema_finding
- **Status**: Drafted
- **Phase**: 5

## SR-168 — The state view shows current progress and next work


### LLR LLR-198 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **LLR-ID**: LLR-198
- **SR-Refs**: SR-168
- **Title**: The pending-owner-action read model, sited below every module that reads it
- **Module**: project-trajectory/scripts/pending.py;project-trajectory/scripts/traj_status.py
- **CodeSymbol**: PendingItem/pending_items/owner_cards/blocked_pending/spine_pending/pending_block
- **Detail**: The state view's what-does-the-owner-still-owe derivation, in one module that renders no page and decides nothing about lanes. Three sources, all committed-tree-pure and clock-free: blocked_pending names every queued work item carrying a blockref together with the attestation page it waits on; spine_pending names every Drafted SR (a first approval is owed) and every SR whose text has drifted from its copy under docs/archive/last_approved (a re-attest is owed), one pointer line each naming the on-demand brief that carries the depth; pause_pending names the tracked docs/work/pause, fail-closed on a file it cannot read. pending_items joins the three into the TYPED model - one PendingItem per action carrying its kind and its rendered bullet - and the two shapes above it are declared once here rather than re-assembled per caller: pending_block is the model rendered, the byte-compared content of the generated owner surface's pending region, and owner_cards is the model minus the pause, which is what a drained-queue banner counts. THE SITING IS THE ROW: three modules read this derivation - the dashboard that renders the status snapshot, the generated owner surface, and the dispatcher's exit banner - and only the first is inside the render family, so a derivation defined there forced the other two to import the gen_trajectory facade for a state query and the dispatcher to reach in for two private names. traj_status.py re-exports all four under their former names, so no caller moved to gain the layer and the generated surfaces are byte-identical - which is why the split sibling is named in this row's Module cell beside the read model: the shim is part of what this row delivers, not a leftover.
- **Rationale**: A derivation every layer reads must sit below all of them, and this one did not: it lived inside a render module of the traj_* family, reachable only through a re-export facade of about a thousand lines. The repository review recorded the consequence twice - a large facade imported for a state query, and private names used as a cross-module API - and the second was DOCUMENTED as an interface row rather than removed, which is the failure mode this kit's own doctrine names: a seam registry records crossings, it does not license them. Extracting the derivation cut both edges at their cause and left the facade with no importers at all, which is now asserted rather than described. The typed item is what makes the banner's exclusion of the pause a declared filter over a kind field instead of a second opinion about what is pending: the ruled amendment requires that the banner and the owner surface can never disagree, and one read model with one filter is that requirement held by construction rather than by three callers agreeing to be careful. A generic shared module was refused for the same reason it is refused everywhere here, and the package of stdlib-only shared helpers by a hard rule rather than by taste - every module there must stay import-clean of the rest of the scripts directory so the scaffolder can import it, while this module reads the work registry through the validator's loaders and the spine through its sibling parser.
- **TestRefs**: TC-194
- **Status**: Drafted
- **Component**: CMP-009
- **Phase**: 5

### LLR LLR-200 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **LLR-ID**: LLR-200
- **SR-Refs**: SR-168
- **Title**: The depth-0 frame as a generated context view in the state view
- **Module**: project-trajectory/scripts/traj_context.py
- **CodeSymbol**: context_block/_context_svg/_ctx_layout
- **Detail**: Renders the declared external frame - the entities outside the system, the boundary crossings, and the external-to-external relationships the system is not a party to - as a self-contained block spliced into the state view's architecture tab ABOVE the derived structure, from the read model traj_parse.frame_context. One lane per crossing so a party holding several keeps one card spanning them; the system opposite; each crossing a directed wire whose heading is read from the system's own point of view; the relationships bowed clear of the system card entirely, carrying no interface vocabulary. The realizing IF-### rows are JOINED from the interface registry rather than restated on the frame row, so a crossing nothing realizes renders dashed and says so, and an interface row whose endpoint is outside the tree but which ties back to no crossing is listed with the reason its own row records. Three tables carry every cell the diagram truncates. Vacuous when the repository declares no frame or carries only the blank form's -000 example rows: the block is omitted and the artifact is byte-identical to one generated before the view existed. Fixed geometry, id-sorted inputs, no clocks, so the artifact's freshness byte-compare stays stable.
- **Rationale**: The boundary record is satisfied by a DERIVED view rather than by a picture kept beside an architecture document, which is what lets no such document exist. That is the reason the parent's "without consulting a second surface" holds for the boundary too: without this view, a reviewer who wanted to know what crosses this system's boundary had to open the registry. The realization join is derived rather than declared on the frame row because the frame is LOCKED and a realization is the other side's claim - the same split that lets a requirement state the crossing while a design row states which piece provides it. An unrealized crossing is drawn rather than hidden for the reason the empty-graph clause exists one row over: a frame silently shorter than the registry is indistinguishable from a complete one. Its OWN module rather than a seventh section of the views module: the view lands at ~440 lines and would have pushed that module past the decomposition threshold, whose rule is decompose, never bump.
- **TestRefs**: (see TC-196)
- **Status**: Drafted
- **Component**: CMP-009
- **Phase**: 5

### TC TC-194 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **TC-ID**: TC-194
- **Verifies**: SR-168;LLR-198;IF-138
- **Level**: Integration
- **Method**: Two claims, because the row makes two. THE BEHAVIOUR, unchanged by the move and still driven through the facade that used to own it: a blocked row projects its pointer, a Drafted or drifted SR projects the brief that carries the depth, a tracked pause projects one bullet and a malformed one still projects fail-closed, and the whole block is deterministic over two reads. THE MODEL AND THE SITING: assert every item carries a kind field, that owner_cards is exactly pending_items minus the pause, that every item's line appears in the rendered block, that the dispatcher's card read and the generated owner surface's block text each equal the read model's own answer, and that the facade still resolves all four former names to the read model's functions; then build the import graph of the shipped scripts INCLUDING imports written inside function bodies and assert that nothing imports the dashboard facade at all. IF-138, the loader seam this module carries, rides the blocked-row arms: they drive the derivation end to end through the validator's work-registry loaders over real temp repositories, so a loader that stopped answering would red here rather than silently projecting an empty owner queue.
- **Tier**: Smoke
- **Expected**: Satisfies the acceptance folded into LLR-198 (parent SR-168)
- **Automated**: Yes
- **Evidence**: tests/test_gen_trajectory_pending.py::test_the_typed_model_kinds_every_pending_item; tests/test_gen_trajectory_pending.py::test_owner_cards_is_pending_items_minus_the_pause; tests/test_gen_trajectory_pending.py::test_the_block_renders_exactly_the_model; tests/test_gen_trajectory_pending.py::test_the_dashboard_still_answers_to_the_former_private_names; tests/test_gen_trajectory_pending.py::test_the_dispatcher_reads_the_model_not_the_render_facade; tests/test_gen_trajectory_pending.py::test_the_owner_surface_reads_the_model_not_the_render_facade; tests/test_gen_trajectory_pending.py::test_pause_projection_is_deterministic; tests/test_gen_trajectory_pending.py::test_malformed_pause_still_projects_fail_closed; tests/test_import_layers.py::test_a_facade_is_an_entry_point_and_nothing_imports_it; tests/test_import_layers.py::test_a_view_never_imports_a_lifecycle_service
- **Status**: Drafted
- **Phase**: 5

### TC TC-196 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **TC-ID**: TC-196
- **Verifies**: SR-168;LLR-200
- **Level**: Unit
- **Method**: The derivation and the vacuity, both driven, over a hand-written frame small enough to reason about: two parties (one holding two crossings, one holding none), two crossings (one realized by an interface row that ties back to it, one realized by nothing) and one external-to-external relationship. THE READ MODEL: the three tiers come back id-ordered, the tie-back is joined from the interface registry with the side it ties on, the crossing nothing realizes comes back with an empty realization rather than being dropped, the entity name resolves for display without the crossing row restating it, and an interface row whose endpoint is outside the tree but which ties back to nothing is returned with the reason its own row records. THE RENDER: the census line matches the registry's real counts, every declared row reaches the diagram INCLUDING the party that holds no crossing, direction is read from the system's point of view (an inbound crossing heads at the system, an outbound one at the party), the unrealized crossing is marked in the markup and stated in the table, and the block splices ABOVE the module map rather than after it. VACUITY, DRIVEN BOTH WAYS: with no frame file the artifact is byte-identical to one generated before the view existed and the round trip proves it; a frame holding only the blank form's -000 example rows is equally vacuous; and a repository with a frame but no symbol inventory still earns the architecture tab. THE META REPO'S OWN FRAME is read as data, not as a picture: its locked table's counts, the one crossing deliberately left unrealized, and the three adjudicated no-tie-back rows with their recorded reasons.
- **Tier**: Smoke
- **Expected**: Satisfies the acceptance folded into LLR-200 (parent SR-168)
- **Automated**: Yes
- **Evidence**: tests/test_traj_parse.py::test_frame_context_is_none_without_a_declared_frame; tests/test_traj_parse.py::test_frame_context_drops_the_blank_form_s_example_rows; tests/test_traj_parse.py::test_frame_context_joins_the_tie_backs_and_keeps_id_order; tests/test_traj_parse.py::test_frame_context_reads_this_repo_s_own_locked_frame; tests/test_traj_views.py::test_context_view_renders_the_declared_frame; tests/test_traj_views.py::test_context_view_states_an_untied_external_endpoint_with_its_reason; tests/test_traj_views.py::test_context_view_renders_above_the_derived_structure; tests/test_traj_views.py::test_context_view_omitted_and_byte_identical_without_a_frame; tests/test_traj_views.py::test_a_template_only_frame_renders_the_vacuous_view; tests/test_traj_views.py::test_context_view_alone_earns_the_how_tab
- **Status**: Drafted
- **Phase**: 5

## SR-177 — Fan-out utilisation reported from the run's own telemetry


### LLR LLR-196 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **LLR-ID**: LLR-196
- **SR-Refs**: SR-177
- **Title**: The per-session telemetry a fan-out utilisation report would aggregate, not yet aggregated
- **Module**: project-trajectory/scripts/agent_common.py
- **CodeSymbol**: regenerate_index/per_turn_pace/per_turn_context
- **Detail**: Every session the coordinator launches already carries the columns a utilisation report would consume: `write_session_log` stamps `# wall-secs:`/`# api-secs:`/`# turns:`/`# tokens:`/`# cache-read:`/`# cache-create:` headers on its `docs/iteration/*.log` (wall measured by the coordinator's own clock, the rest parsed from the CLI's `type: result` JSON event — `agent_loop.parse_json_result`/the `meta` dict at its session-close path), and `regenerate_index` folds every log's header into one row per session in the generated `docs/iteration_index.md`, with `per_turn_pace`/`per_turn_context` already deriving the like-for-like speed and context-volume numbers SR-177's own rationale calls out as the existing telemetry to derive from. NOT DISCHARGED, stated rather than left to be discovered: nothing GROUPS these per-session rows by RUN or by LANE — `docs/iteration_index.md` is a flat session table, not a utilisation report — and nothing joins the `lanes` value `agent_loop` resolves at boot (CLI `--lanes` / `AGENT_LANES` / `docs/stack.ini [agent-loop] lanes`) against how many trains actually ran concurrently, so "lanes configured" versus "lanes occupied" and "work items integrated per wall-hour" are both unmeasured today. This row cites the real, already-collected columns and the real folding function the report would extend, rather than a fabricated aggregator that does not exist.
- **Rationale**: SR-177's own rationale already states the aggregation is the row's whole build gap; repeating that here without citing what exists would leave a reader to wonder whether even the inputs are real. They are: the per-session telemetry columns are shipped, tested, and committed on every session today, so the fan-out utilisation report SR-177 wants is a GROUPING pass over data already on disk, not a new instrumentation project — which is the concrete argument for `C`, not `B`, priority: the report is cheap precisely because this seam already carries what it needs. Citing the seam honestly, with the grouping gap stated rather than silently assumed solved, is what keeps this row from reading as discharged the day someone reads `docs/iteration_index.md` and mistakes a session table for a utilisation report.
- **TestRefs**: (see TC-191)
- **Status**: Drafted
- **Component**: CMP-008
- **Phase**: 5

### TC TC-191 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **TC-ID**: TC-191
- **Verifies**: SR-177;LLR-196
- **Level**: Integration
- **Method**: Runs the real folding path rather than reading it: `tests/test_generated_newlines.py::test_the_three_crash_paths_actually_run` calls `agent_common.regenerate_index` on a synthetic `docs/` tree and asserts the generated table exists (the row-per-session fold LLR-196 cites); `tests/test_agent_loop.py::test_done_exit_writes_logs_and_index` and `::test_stream_json_echo_and_result_parse` drive a live fake-CLI session end to end and assert the exact telemetry columns (`wall-secs`, `api-secs`, `turns`, `tokens`, `cache-read`, `cache-create`) land in the log header `regenerate_index`/`per_turn_pace`/`per_turn_context` read. No test here exercises a lanes-configured-vs-occupied or work-per-wall-hour GROUPING, because LLR-196 states plainly that grouping does not exist yet — this case verifies the inputs the future grouping pass would consume, not the report SR-177 ultimately asks for.
- **Tier**: Full
- **Expected**: Satisfies the acceptance folded into LLR-196 (parent SR-177) for the telemetry INPUT half only; the utilisation report itself remains the stated build gap
- **Automated**: Yes
- **Evidence**: tests/test_generated_newlines.py::test_the_three_crash_paths_actually_run; tests/test_agent_loop.py::test_done_exit_writes_logs_and_index; tests/test_agent_loop.py::test_stream_json_echo_and_result_parse
- **Status**: Drafted
- **Phase**: 5
