# Re-attestation brief — spine rows owing a human act

_GENERATED by `trace.py --approve modified` (WI-316) — do not hand-edit; cite the spine registries, not this rendering. One section per SR that is itself `Drafted`, has a `Drafted` LLR or TC anywhere in its chain, or whose chain has DRIFTED from the approved snapshot; each chain row shows only its CHANGED cells, before (the snapshot) vs after (the working tree), approved cells first — or, for a `Drafted` row with no cell diff, its full current content, since it owes only because it was never approved. `Status` itself is never listed — the marker is not the amendment. Rule on each section: bless → set `Status` to `Approved` (process.md §7) — and from the first signing onward, run `intake.py snapshot` in the SAME commit, or the record of what was blessed does not move._

_Baseline: `docs/archive/last_approved` — copied 2026-08-23 (2b7be11a), the commit that last wrote this record._

_Approval provenance: the last commit to move a `Status` cell in a snapshotted registry is 2b7be11a (2026-08-23) — the record's maturity cells have not moved since._


## SR-070 — Generated views are offline, deterministic and drift-checkable

> **Requirement.** The delivered generator set shall derive each artifact it produces from the tracked registries alone — self-contained and usable without network access, byte-stable across regeneration from unchanged sources, and carrying a freshness contract that fails when the committed copy has drifted from those sources — omitting a view whose source registries the adopting repository does not carry rather than emitting an empty one.

> **Rationale.** Realizes SN-010 and the dissolved edge expectation that a generated artifact drifting from its source fails its --check, by isolating one independently reviewable obligation formerly carried by SR-038; with SN-012, since a repository that carries no source registry pays nothing for the view that registry would have fed. ONE DECISION PER ROW: the INTEGRITY of a generated artifact and what the state view must SHOW are two unrelated obligations, and this row holds the first alone. They fail independently: a view can be perfectly offline, deterministic and drift-checkable while showing nothing about the work in flight or how the parts connect. The content half sheds along the conjunction SN-023 itself draws ("progress AND how its parts connect") to SR-168 and SR-169; this row keeps the artifact-integrity contract alone. The subject is "each artifact the generator set produces" rather than "each view" deliberately: the knowledge-export bundle is not a view, and a fourth row minted to hold it would be the filler tier the ruling forbids. The child/parent phase spread is untouched and is intended — this split is by observable class, not by phase, a partition that was considered and rejected.


### LLR LLR-199, Drafted — never approved
- **Detail**
  - before: The component view as a DERIVED registry rather than a hand-authored prose home. gen_components.py joins the four registries that already carry the facts - components.toml declares WHICH components exist, low-level-requirements.toml carries membership on its Component cell, system-requirements.toml is reached THROUGH that membership, interfaces.toml supplies the seams - and writes docs/requirements/components.derived.toml, declared in docs/stack.ini [generated] as kind components, freshness-gated by check.py's component-view step, regenerated on the trunk by trunk_step --regen, and stamped with the generated-but-live staleness header. Per component it emits the design rows that belong to it, the requirements those rows decompose, the perspectives that bear (trace.effective_hats over the members, so an SR-tier re-ruling propagates with no design cell edited), the modules, and the seams split internal from boundary by the components layer's own rule. THE THREE COVERAGE EDGES ARE ANSWERED IN THE OUTPUT, not in a comment: a requirement reaching no component is listed once in the unplaced table and counted, because nothing in any registry distinguishes a decomposition not yet written from a constraint over every component and a per-repo judgement has no place in shipped machinery; a requirement reaching several appears in EVERY one of them and additionally in that component's sr_shared_refs, so the view never implies exclusive ownership; a seam is placed by its Component tag UNION… [870 more chars — read the registry row]
  - after: gen_components.py derives the component view from four registries: components.toml declares components, low-level-requirements.toml provides Component membership, system-requirements.toml is reached through that membership, and interfaces.toml provides seams. It writes docs/requirements/components.derived.toml; docs/stack.ini [generated] declares kind components, check.py freshness-gates component-view, trunk_step --regen regenerates it on trunk, and the file carries the generated-but-live staleness header. For each component it emits member design rows, their decomposed requirements, perspectives from trace.effective_hats over members, modules, and seams split into internal and boundary by the components layer rule. A requirement with no component appears once in the counted unplaced table; registries cannot distinguish missing decomposition from an all-component constraint, and that judgement has no place in shipped machinery. A requirement in several components appears in each and in each component's sr_shared_refs, so the view never implies exclusive ownership. A seam is placed from its Component tag unioned with components resolved from endpoints through trace_text.norm_module; an unresolved seam stays in the unplaced table rather than being dropped. NO APPROVAL CELL APPEARS, EVER: the hand file declares the component and holds its maturity, while this file describes it. With no real CMP row and no view on disk, the generator exits 0 and writes nothing. NOT DISCHARGED: t… [230 more chars — read the registry row]
- **Title**
  - before: The generated component view: the join, its three coverage edges and the approval it may never carry
  - after: Component-view generator and coverage placement

### TC TC-195 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **TC-ID**: TC-195
- **Verifies**: SR-070;LLR-199;IF-139;IF-140;IF-141;IF-142;IF-143
- **Level**: Unit
- **Method**: The join, the three coverage edges, the standing constraint and the freshness contract, each driven against a two-component spine small enough to reason about by hand. THE JOIN: assert membership comes from the design tier's Component tag, that the requirement tier is reached through it, and that the modules follow. DERIVED NEVER COPIED: a design row carrying no hat cell still contributes its SR parent's perspectives, and re-ruling that one requirement moves the view with no design cell edited. EDGE 1: a requirement with no design child appears in the unplaced table, is counted there, and appears in no component section - visible rather than dropped. EDGE 2: a requirement reaching two components appears in both and is marked shared in both, while one reaching a single component is not. EDGE 3: seams enter the view, an interface with both endpoints inside a component is internal to it while one crossing two is a boundary of BOTH, an UNTAGGED interface is still placed through its endpoints, and one that resolves nowhere is reported as unplaced. NO APPROVAL: with one Approved and one Drafted component declared in the hand file, no maturity key and neither word appears in any emitted cell. FRESHNESS, DRIVEN BOTH WAYS: --check greens on a fresh view, reds after a registry row moves, and greens again once regenerated; a missing view reds; the written bytes carry no CRLF and two renders agree. VACUITY: a repo whose only CMP row is the inert -000 example writes nothing and exits zero… [15 more chars — read the registry row]
- **Tier**: Smoke
- **Expected**: Satisfies the acceptance folded into LLR-199 (parent SR-070)
- **Automated**: Yes
- **Evidence**: tests/test_gen_components.py::test_membership_and_the_requirement_reach; tests/test_gen_components.py::test_the_hat_union_is_DERIVED_from_the_parent_not_copied; tests/test_gen_components.py::test_a_parent_re_ruling_moves_the_view_with_no_design_cell_edited; tests/test_gen_components.py::test_a_childless_requirement_is_listed_ONCE_and_never_dropped; tests/test_gen_components.py::test_a_shared_requirement_appears_in_EVERY_component_it_reaches; tests/test_gen_components.py::test_seams_enter_the_view_split_internal_from_boundary; tests/test_gen_components.py::test_an_untagged_seam_is_placed_by_its_ENDPOINTS; tests/test_gen_components.py::test_an_unplaceable_seam_is_REPORTED_not_silently_excluded; tests/test_gen_components.py::test_the_generated_view_carries_NO_approval_cell; tests/test_gen_components.py::test_check_reds_on_a_stale_view_and_greens_once_regenerated; tests/test_gen_components.py::test_check_reds_when_the_view_is_missing_entirely; tests/test_gen_components.py::test_the_written_file_is_LF_and_reproducible; tests/test_gen_components.py::test_a_repo_with_no_real_component_is_VACUOUS; tests/test_gen_components.py::test_the_cli_runs_out_of_process
- **Status**: Drafted
- **Phase**: 5

## SR-146 — Prompts are reviewable files with a per-session audit trail

> **Requirement.** Every prompt the delivered loop launches shall be a shipped, reviewable file with strictly filled slots — listed by digest in a freshness-gated generated catalogue, each session recording which template it used and the fingerprint of what it rendered to.

> **Rationale.** Prose steers the sessions this loop launches and had been reviewable only by reading Python source, which makes the process trusted rather than inspectable. The audit trail is the part that makes the move useful rather than cosmetic: without a per-session digest, "which instruction did this session see" stays an inference. (The argv-vs-shell-string transport decision is design, and lives one tier down in LLR-163.) Hat-derived (hat.SECURITY): C-SEC-5 requires that content composed for dispatch to an external model runner carry a DECLARED inclusion rule rather than an implicit one — a prompt assembled inside source is an unreviewed egress path no write-side gate covers, and shipping it as a reviewable file with a digest is what makes that rule readable.


### TC TC-192, Drafted — never approved
- **Method**
  - before: `gen_prompt_catalog.render` is exercised directly rather than read: `test_the_catalogue_lists_every_shipped_prompt_with_its_digest` asserts every `prompts.KIT_PROMPTS` key and every `prompts.catalog_rows()` digest appears in the rendered text (the PURE-render claim, since it reads only the shipped templates); `test_the_catalogue_on_disk_is_FRESH` runs `catalog.main(["--check"])` and asserts exit 0 against the committed file (the freshness gate LLR-164 names); `test_a_template_edit_moves_its_digest_and_so_the_catalogue` asserts a real text edit moves the digest while a bare line-ending difference does not (the CRLF-vs-stale-catalogue claim `--check` exists to avoid); `tests/test_generated_freshness_wiring.py::test_prompt_catalog_step_reds_when_a_template_changes` drives the wired `[step:]` enforcer end to end — edits a template, asserts the commit-floor step reds, then runs `gen_prompt_catalog.py` and asserts it goes green — over a bootstrapped scaffold, not the source tree alone.
  - after: gen_prompt_catalog.render is exercised directly rather than read. test_the_catalogue_lists_every_shipped_prompt_with_its_digest asserts that the rendered text contains every prompts.KIT_PROMPTS key and every prompts.catalog_rows() digest. test_the_catalogue_on_disk_is_FRESH runs catalog.main(["--check"]) against the committed file and asserts exit 0 - the freshness gate LLR-164 names. test_a_template_edit_moves_its_digest_and_so_the_catalogue asserts that a real text edit moves the digest while a bare line-ending difference does not. tests/test_generated_freshness_wiring.py::test_prompt_catalog_step_reds_when_a_template_changes drives the wired [step:] enforcer end to end over a bootstrapped scaffold: edit a template, assert the commit-floor step reds, run gen_prompt_catalog.py, then assert it goes green.

## SR-157 — Spine and work-registry rules red the harness verdict

> **Requirement.** The delivered harness shall report every declared spine and work-registry rule violation — broken joins, integrity and schema faults, leftover placeholders, requirement-form findings and work-item coherence contradictions — naming the at-fault row and cell, gating the declared failure set at the declared gate while advisory classes never change the exit code.

> **Rationale.** Realizes SN-002 (the chain is mechanically verified, not asserted — this row is the boundary-level home of the verdict SN-002's acceptance asks for), SN-012 (optional registries and opt-outs cost a non-user nothing) and SN-025 (queue-overlap visibility). One row because the adopter-observable contract is one: registry rules produce named findings at a declared severity; which rules, at which severities, is the component detail the rows below it decompose. Deliberate pair (census F6): the PB-format invariant itself is SR-015's; this row's decomposition carries the checker. Fan-out re-stamp: the delivered contract is one — a named, row-and-cell-attributed finding at a declared severity, gating or advisory — while the rule inventory it ranges over is open by this row's own acceptance ("a rule added at one of those sites is in this row's scope by default"), so the child count tracks the number of declared rules rather than the number of decisions this row makes, and would climb back through any bound a split bought.


### LLR LLR-201, Drafted — never approved
- **Title**
  - before: The checker's cross-row coherence rules, sited below the engine that composes them
  - after: Checker cross-row coherence module below its engine

### TC TC-197 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **TC-ID**: TC-197
- **Verifies**: SR-157;LLR-201
- **Level**: Unit
- **Method**: Two claims, because the row makes two. THE RULES, each driven directly rather than through the engine: the orphan pass emits SR then LLR then TC then SN and returns exactly the at-fault ids of the findings it emitted; a Drafted SR stands the child-completeness rules down while its SN linkage still applies; a PB Refs cell resolves against an LLR Module path as well as against ids, and an empty one is a finding; a Component tag naming no CMP row is a finding while an empty component registry disarms the membership half entirely; a knowledge ref resolves only under the docs/knowledge prefix and a traversal ref never escapes the pack root; the phase scope covers the foundation phase under every filter and a blank Phase always; and the status criterion defers an out-of-phase row rather than failing it. THE BOUNDARY AND THE BAGS: assert that this module imports no sibling of the scripts directory, that the engine still answers to every moved name and that each definition lives here; that the loaded registry record is frozen, total, and constructed at exactly one site; that the findings record's list defaults are per instance and that the two fields filled after the pure pass returns are DECLARED rather than conjured at a call site; that the flags record accepts either itself or a CLI namespace; that no caller imports argparse to forge the engine's config; and that the composer stays a composer - measured line span, and no nested def, which is how the number this split removed got t… [8 more chars — read the registry row]
- **Tier**: Smoke
- **Expected**: Satisfies the acceptance folded into LLR-201 (parent SR-157)
- **Automated**: Yes
- **Evidence**: tests/test_trace_coherence.py::test_the_orphan_rules_emit_sr_then_llr_then_tc_then_sn; tests/test_trace_coherence.py::test_a_drafted_row_stands_the_child_rules_down_but_not_its_sn_link; tests/test_trace_coherence.py::test_off_spine_backlinks_resolve_against_ids_and_llr_module_paths; tests/test_trace_coherence.py::test_component_membership_is_checked_from_the_primitive_side; tests/test_trace_coherence.py::test_the_knowledge_ref_resolves_only_the_pack_prefix; tests/test_trace_coherence.py::test_the_phase_scope_always_covers_the_foundation_phase; tests/test_trace_coherence.py::test_the_status_criterion_defers_out_of_phase_and_stands_down_for_drafted; tests/test_trace_coherence.py::test_registries_is_frozen_and_total; tests/test_trace_coherence.py::test_the_loader_is_the_only_construction_site_of_registries; tests/test_trace_coherence.py::test_findings_list_defaults_are_per_instance; tests/test_trace_coherence.py::test_the_two_post_analyze_fields_are_declared_not_conjured; tests/test_trace_coherence.py::test_analysis_flags_takes_a_record_or_a_namespace; tests/test_trace_coherence.py::test_no_caller_forges_an_argparse_namespace_for_the_engine; tests/test_trace_coherence.py::test_the_coherence_rules_import_no_sibling_of_scripts; tests/test_trace_coherence.py::test_trace_re_exports_every_moved_name; tests/test_trace_coherence.py::test_analyze_is_a_composer_and_stays_one
- **Status**: Drafted
- **Phase**: 5

## SR-159 — Declared-architecture connectivity gaps are reported

> **Requirement.** The delivered harness shall report declared-architecture connectivity gaps — an unresolvable interface row, an undeclared endpoint or direction, a cross-component import no declared interface covers, an invalid spec interface citation, and a top view past its declared bound — warn-first with --strict gating, vacuous where the optional inventories are absent.

> **Rationale.** Realizes SN-023 (the parts' connections are as much of the truth as the requirement tree, checked mechanically warn-first — the need's own words), SN-002 (interface rows stay resolvable) and SN-012 (opt-out and vacuity keep the layer free for non-users). One row: the delivered contract is one connectivity verdict; the checkers below it are its decomposition.


### LLR LLR-041
- **Detail**
  - before: Loads interfaces.csv; validates IF ids, SR back-links and Component tags, and emits the best-effort endpoint-module advisory. Malformed or duplicate ids and unresolved SR/Component links are findings; an unmatched ThisProject endpoint stays advisory-only, and an absent interface registry is vacuous.
  - after: Loads the interface registry; validates IF ids, SR back-links and Component tags, and emits the best-effort endpoint-module advisory. Malformed or duplicate ids and unresolved SR/Component links are findings; an unmatched module-shaped Provider endpoint stays advisory-only, and an absent interface registry is vacuous.

## SR-160 — Front-door launchers for the two universal contributor actions

> **Requirement.** The delivered scaffold shall place both universal contributor actions — preparing the development environment, and resuming the automated development loop — as launchers at the repository root for each supported platform, each starting its action as a single step — locating a working interpreter by RUNNING a candidate and reporting clearly rather than crashing cryptically when none resolves or a Store alias exits nonzero — or documenting the one platform-required step where a single click is impossible.

> **Rationale.** Realizes SN-034, whose row is the declared ONE home for launcher-class obligations (root visibility, one action per platform, both universal actions). SN-034 had no citing SR — the launcher rows realized SN-001 and left the need an orphan the strict trace names. One row per (need, delivered property): (SN-034, scaffold capability). THE PER-LAUNCHER ROBUSTNESS OBLIGATION IS STATED HERE rather than cross-referenced, because a cross-reference to a row that no longer exists is not a home. It spans two audiences, the git hooks and the root launchers, and parts along that line: the hook half is machinery under SR-019/SR-020 (LLR-021), and the launcher half is an observable of THIS row, because a launcher that crashes cryptically has not started its action as a single step. The wiring obligation stays SR-113's, which is a live row.


### LLR LLR-193, Drafted — never approved
- **Detail**
  - before: pick_py probes each candidate interpreter twice, because 'found' and 'usable' are different questions: first that it RUNS at all (a Windows python3 on PATH is often the Microsoft-Store alias stub, which exists and executes nothing), then that its sys.version_info clears the 3.11 floor every kit script's tomllib import needs. Both .venv layouts are tried first (bin/ POSIX, Scripts/ a Windows-created venv), then python3, then python on PATH, so the pinned toolchain a contributor built wins over whatever ambient interpreter PATH happens to resolve. A refusal names every rejected candidate and why (PYWHY), never a bare 'not found', so the front door reports clearly instead of dying deep inside the engine at an obscure import. agent-resume.cmd's :pickpy label runs the identical two-stage probe for cmd.exe (.venv\Scripts\python.exe, .venv\bin\python, then python, then the py launcher), and agent-resume.command carries no probe of its own - its whole body delegates (exec ./agent-resume.sh) - so the policy reaches it by inheritance, not by a second copy. Together the three root files realize, on Windows/macOS/Linux alike, the loop-resume half of SN-034's two universal contributor actions as one action from the repository's front door. NOT DISCHARGED, stated rather than left to be discovered: SN-034 names TWO universal actions, and only this one - resuming the loop - has a root launcher today. The environment-preparation half's scripts (onboard.sh, dev-setup.sh) materialize under scri… [296 more chars — read the registry row]
  - after: pick_py distinguishes found from usable: it first runs each candidate, then checks that its sys.version_info meets the 3.11 floor every kit script's tomllib import needs. This rejects a Windows python3 on PATH that is the Microsoft-Store alias stub, which exists and executes nothing. It tries both .venv layouts, bin/ for POSIX and Scripts/ for a Windows-created venv, before python3 and python on PATH, so the pinned toolchain a contributor built wins over whatever ambient interpreter PATH resolves. On refusal it names every rejected candidate and its PYWHY reason rather than reporting a bare 'not found', so the front door reports clearly instead of dying deep inside the engine at an obscure import. agent-resume.cmd's :pickpy label runs the identical two-stage probe for cmd.exe, trying .venv\Scripts\python.exe, .venv\bin\python, python, then the py launcher. agent-resume.command carries no probe of its own; its whole body delegates (exec ./agent-resume.sh) and inherits the policy. Together the three root files realize, on Windows, macOS and Linux alike, the loop-resume half of SN-034's two universal contributor actions. NOT DISCHARGED: SN-034 names two universal actions and only loop resume has a root launcher today. The environment-preparation half's scripts (onboard.sh, dev-setup.sh) materialize under scripts/, not at the repository root SR-160's acceptance names (bootstrap.py's MAPPING; README.md's own 'Still owed' ledger states the gap), so the residual is recorded as a fin… [42 more chars — read the registry row]
- **Title**
  - before: Root launcher interpreter selection: the loop-resume half's probe-then-floor, the front door's own robustness
  - after: Loop-resume launcher interpreter selector

### TC TC-188 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **TC-ID**: TC-188
- **Verifies**: SR-160;LLR-193
- **Level**: Integration
- **Method**: Eleven live-launcher cases, run against a REAL interpreter with sys.version_info spoofed rather than a rehearsal of the probe string, so the assertion is on the selection RULE and not on a fake that merely fails to run. THE PREFERENCE: a valid project .venv beside a below-floor ambient python is taken over the ambient one, on both agent-resume.sh and agent-resume.cmd. THE REFUSAL: with nothing above the floor anywhere, the launcher exits nonzero, never reaches the engine, and names the floor (3.11) and each rejected candidate with why - the diagnostic repo-review 2026-08-19 H-01 asked for, since a bare 'not found' would misdescribe an interpreter sitting right there. THE OTHER DIRECTION: a PATH python that clears the floor is taken and runs the engine, on both platforms, so the refusal case above cannot be a launcher that simply never selects anything. THE FALLBACK: a stale below-floor .venv loses to a good PATH python rather than wedging the launcher - the venv is PREFERRED, not exempt - covering both the sh fallback and the cmd case of a present-but-unusable venv, plus cmd's alias-stub-to-py-launcher walk. THE INHERITANCE: the .command wrapper reaches the same venv preference as agent-resume.sh, and a companion assertion pins that its whole body delegates rather than holding a second copy of the policy.
- **Tier**: Full
- **Expected**: Satisfies the acceptance folded into LLR-193 (parent SR-160)
- **Automated**: Yes
- **Evidence**: tests/test_launcher_interpreter.py::test_sh_prefers_the_project_venv_over_a_below_floor_ambient_python[live]; tests/test_launcher_interpreter.py::test_sh_refuses_a_below_floor_interpreter_and_names_what_it_rejected[live]; tests/test_launcher_interpreter.py::test_sh_takes_a_path_python_that_satisfies_the_floor[live]; tests/test_launcher_interpreter.py::test_sh_falls_back_when_the_venv_itself_is_below_the_floor[live]; tests/test_launcher_interpreter.py::test_the_command_wrapper_inherits_the_venv_preference[live]; tests/test_launcher_interpreter.py::test_the_command_wrapper_declares_the_inheritance_and_holds_no_copy[live]; tests/test_launcher_interpreter.py::test_cmd_prefers_the_project_venv_over_a_below_floor_ambient_python[live]; tests/test_launcher_interpreter.py::test_cmd_refuses_a_below_floor_interpreter_and_names_what_it_rejected[live]; tests/test_launcher_interpreter.py::test_cmd_takes_a_path_python_that_satisfies_the_floor[live]; tests/test_launcher_interpreter.py::test_cmd_falls_back_when_the_venv_is_present_but_unusable[live]; tests/test_launcher_interpreter.py::test_cmd_walks_past_an_alias_stub_to_the_py_launcher[live]
- **Status**: Drafted
- **Phase**: 5

## SR-161 — Decompositions carry a perspective record

> **Requirement.** The delivered planning content shall leave, with every decomposition it produces, a machine-readable record of the declared review perspectives — each perspective's applicability decision and the requirements or explicit no-finding it produced — such that an applicable declared perspective missing from the record is a reportable finding.

> **Rationale.** Realizes SN-036 — a decomposition produced from one perspective can look complete while omitting security, operations or first-use failures, and the omission is invisible unless the record names what was considered. SN-036 had no citing SR. The declared-perspective inventory is the ruled hats roster; the record proves coverage and provenance while adequacy stays with the independent review, exactly as the need partitions it.


### LLR LLR-202, Drafted — never approved
- **Detail**
  - before: LLR-183 makes the perspective record RESOLVABLE and derives the effective set; nothing yet asks whether a still-resolvable record is still TRUE. staged_hat_refs_findings(root) is that question, and it is a second arm of the amend-without-flip guard rather than a new mechanism: it reads the one amendment set staged_spine_amendments already computes and reports each row whose approved half is non-empty while Hat-Refs is absent from its traced half - the substance moved, the perspective record did not. THE COMPARISON IS BY CELL CLASS, WHICH IS THE WHOLE DESIGN: the measured defect it is built against is a neighbouring check that blames the registry by LINE, where writing an informative cell re-dates the row and a blame time cannot tell an approved cell from a traced one, so this arm keys off split_changed_cells' §A5.1 split and is silent on a traced-only edit - the perspective backfill's own shape. Tier scope is STRUCTURAL, not an allowlist: traced_cells (extracted from spine_cell_class, which needed the set rather than one column's class) answers whether the registry declares the column at all, so the test-case tier is silent because it carries no such cell and a third registry that gains one is guarded with no edit here. THE BASELINE IS THE GUARD'S OWN - HEAD versus the index over rows reading the same approved-text Status on both sides - which leaves two honest vacuities inherited rather than re-litigated: a row minted in the same commit has nothing to compare, and a row belo… [515 more chars — read the registry row]
  - after: LLR-183 resolves the perspective record and derives its effective set; staged_hat_refs_findings(root) checks whether that record stayed TRUE. As a Hat-Refs arm of the amend-without-flip guard rather than a new mechanism, it reuses the amendment set staged_spine_amendments already computes and reports each row whose approved half is non-empty while Hat-Refs is absent from its traced half. THE COMPARISON IS BY CELL CLASS: a line-based comparison cannot distinguish an approved-cell amendment from an informative-cell edit that re-dates the row, so this arm keys off split_changed_cells' A5.1 split and stays silent on a traced-only edit - the perspective backfill's own shape. Scope is STRUCTURAL, not an allowlist: traced_cells, extracted from spine_cell_class, answers whether a registry declares the column at all, so the test-case tier is silent because it carries no such cell and a third registry that gains one is guarded with no edit here. Its baseline is the guard's own - HEAD versus the index over rows reading the same approved-text Status on both sides - which leaves two honest vacuities: a row minted in the same commit has nothing to compare, and a row below approval has blessed nothing to amend behind a human's back. Warn-first and never an exit code, printed only under --staged. NOT DISCHARGED: the warn fires once, at the commit that earns it, where a last_approved snapshot would make the finding STAND until answered; that baseline was declined on the timing argument this r… [121 more chars — read the registry row]
- **Title**
  - before: The perspective record going stale behind an amendment: the Hat-Refs arm of the amendment guard
  - after: Staged Hat-Refs amendment guard

### TC TC-198, Drafted — never approved
- **Method**
  - before: The arm driven over a real two-commit git repo whose HEAD holds an Approved SR-001 (and, for the tier case, an Approved LLR-001 + TC-001), with the change STAGED rather than committed - the hook's own question. FIRES: the requirement cell amended while Hat-Refs stands, one finding naming the row and the moved cells, with the amendment arm firing alongside it so the second arm is proven an addition rather than a re-spelling. SILENT ON TRACED-ONLY MOVEMENT, the regression case and a measured one: writing the Hat-Refs cell itself - literally the perspective backfill's edit - raises nothing, and neither does an SN-Refs re-point, because no approved cell moved. SILENT WHEN DISCHARGED: substance and Hat-Refs moving together raises nothing here while the amendment arm still fires. VACUOUS WHERE THERE IS NO BASELINE, both shapes: a row minted in the staged commit, and a row amended below approval on both sides. STRUCTURALLY SILENT ON A TIER WITH NO SUCH COLUMN: an LLR and a TC amended in the same commit yield exactly one finding, the LLR's, so the scoping cannot pass by the arm being silent everywhere.
  - after: Drive the arm over a real two-commit git repository whose HEAD holds an Approved SR-001 (and, for the tier case, an Approved LLR-001 and TC-001), with the change STAGED rather than committed - the hook's own question. FIRES: the requirement cell amended while Hat-Refs stands raises one finding naming the row and the moved cells, with the amendment arm firing alongside it so the second arm is proven an addition rather than a re-spelling. SILENT ON TRACED-ONLY MOVEMENT: writing the Hat-Refs cell itself - the perspective backfill's own edit - raises nothing, and neither does an SN-Refs re-point. SILENT WHEN DISCHARGED: substance and Hat-Refs moving together raises nothing here while the amendment arm still fires. VACUOUS WHERE THERE IS NO BASELINE, both shapes: a row minted in the staged commit, and a row amended below approval on both sides. STRUCTURALLY SILENT ON A TIER WITH NO SUCH COLUMN: an LLR and a TC amended in the same commit yield exactly one finding, the LLR's, so the scoping cannot pass by the arm being silent everywhere.

## SR-162 — Requirement boundary references resolve against the declared frame

> **Requirement.** The delivered harness shall resolve every system-requirement boundary reference, and every declared interface's endpoint pair and signal type, against the declared external frame — an unresolvable crossing failing the strict trace naming the row — reporting requirements naming no crossing, crossings named by no requirement, declared crossings with no realizing interface row, an interface declaring no endpoint pair or no signal type from the closed discrete/variable vocabulary, and a seam joined to a counterpart whose signal type is incompatible with its own, each as an advisory, and staying vacuous for a project declaring no frame.

> **Rationale.** Realizes SN-037 — every promised behavior's entry and exit locatable at a declared boundary, unresolved references mechanical findings, in the need's own words. SN-037 had no citing SR even though the machinery is already delivered (the frame registry, the boundary-reference field, the resolution and coverage rules); this row is the obligation that machinery enforces. Severity split deliberately: resolution hard, coverage advisory — the ruled warn-first sequencing while the corpus converges. The row also carries SN-037's SIGNAL-TYPING half — "every referenced interface identifies its boundary endpoints and whether its signal is discrete or variable ... incompatible signal types are mechanical findings". That half had ZERO textual occurrence in the SR layer while `orphans=0` held, even though `interfaces.toml` already carries a `signal` cell on every row (many of them typed `variable` by a recorded default with a re-type note, which is exactly the state a check is owed for). NAMED RESIDUAL, stated rather than quietly dropped: the need's last clause — that a reviewed change altering one side of the requirement/interface relationship carries or justifies the corresponding change on the other — is a REVIEW obligation this row does not claim to mechanize, and no SR states it yet.


### LLR LLR-187, Drafted — never approved
- **Detail**
  - before: The resolution half of the declared frame, sited in the trace pipe beside the spine's other reference rules. frame_findings resolves the frame registry's own three reference fields - a crossing's Entity and a relationship's From and To - against the declared entity ids, returning plain finding strings rather than the (at_fault_id, finding) pairs the older spine rules return, because no caller ever read the id. It is a FAILURE class, not an advisory: a tie-back to an entity that does not exist is a dangling reference exactly like a requirement citing a deleted need. Its one non-obvious rule is the vacuity guard: an empty entity set is vacuous ONLY when nothing references it, since crossings referencing entities that do not exist otherwise resolve to silence and the report then prints that every reference resolves - the false green this rule exists to prevent. sr_boundary_findings carries the requirement-side join and returns (findings, advisories) at TWO severities on purpose: an SR whose Boundary-Refs names an undeclared crossing is HARD and joins the --strict failure set, while the uncovered-requirement count, the crossings named by no requirement, and the realization gap (a declared crossing no interface row ties back to through InterfaceFromExternal/InterfaceToExternal) are each ONE summary advisory that never gates. Both rules are vacuous for a project declaring no frame, and _frame_report_section renders the resolved state or the findings into the report. THE INTERFACE-C… [1002 more chars — read the registry row]
  - after: In the trace pipe, frame_findings resolves a crossing's Entity and a relationship's From and To against declared entity ids. It returns finding strings, not the older spine rules' (at_fault_id, finding) pairs, because no caller uses the id. These are FAILURE findings. An empty entity set is vacuous only when nothing references it; otherwise a reference to a missing entity would falsely report full resolution. sr_boundary_findings joins the SR side and returns (findings, advisories): an SR Boundary-Refs value naming an undeclared crossing is HARD and joins --strict, while the uncovered-requirement count, crossings named by no requirement, and the realization gap - a declared crossing with no InterfaceFromExternal/InterfaceToExternal interface row - are one advisory each and never gate. Both rules are vacuous with no declared frame, and _frame_report_section renders their state or findings. At the IF entries in this module's required-field and closed-vocabulary tables, endpoint pairs and the closed discrete/variable signal vocabulary are declared as this row's observables; the generic checker executes them but remains owned by the schema row. NOT DISCHARGED: incompatible signal types on joined seams cannot be reported naming both rows because no join reads signal vocabulary; the carriage cell is the tier's only seam-to-seam edge and carries none. The cross-side requirement/interface amendment rule is also unimplemented.
- **Title**
  - before: Boundary references as resolvable cells: the frame's own joins, the SR-to-crossing rule, and the severity split that makes both adoptable
  - after: Frame-reference resolver and interface-cell checker

### TC TC-182 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **TC-ID**: TC-182
- **Verifies**: SR-162;LLR-187
- **Level**: Integration
- **Method**: Five claims, one per severity the row declares, driven over a scaffolded frame rather than over the live one so the negative cases can exist. RESOLUTION IS HARD: a crossing naming an undeclared entity, a relationship naming one, and an interface tie-back naming an undeclared crossing are each reported and each join the --strict failure set; a clean frame is silent. THE VACUITY GUARD IS NOT A PASS: a frame declaring crossings and relationships but NO entity is a finding in its own right, because the alternative reading returns silence and the report then claims every reference resolves - the false green the rule exists to prevent. VACUITY THAT IS REAL: a frame declaring nothing, and an absent frame registry, both report nothing at all, and the SR-side rule is vacuous for the same absence - a project that has drawn no boundary must not inherit a permanent warning. THE SR JOIN, BOTH SEVERITIES: a requirement naming an undeclared crossing is a HARD finding and a declared one is clean, while the uncovered-requirement count is asserted to be exactly one summary line that exits zero under --strict, and the realization gap is asserted reported and never gated. THE CARVED-OUT INTERFACE CELLS: a signal value outside the closed discrete/variable vocabulary is reported naming the row and the vocabulary, and stays warn-first - the observable is this parent's while the checker that runs it is the schema row's, so the assertion is on the message and the exit code together.
- **Tier**: Full
- **Expected**: Satisfies the acceptance folded into LLR-187 (parent SR-162)
- **Automated**: Yes
- **Evidence**: tests/test_external_frame.py::test_a_clean_frame_produces_no_finding; tests/test_external_frame.py::test_a_crossing_naming_an_undeclared_entity_is_a_FINDING; tests/test_external_frame.py::test_a_relationship_naming_an_undeclared_entity_is_a_FINDING; tests/test_external_frame.py::test_an_IF_tieback_naming_an_undeclared_crossing_is_a_FINDING; tests/test_external_frame.py::test_crossings_with_NO_entity_declared_is_a_FINDING_not_a_vacuous_pass; tests/test_external_frame.py::test_a_frame_declaring_NOTHING_stays_vacuous; tests/test_external_frame.py::test_the_frame_rules_are_VACUOUS_without_the_registry; tests/test_external_frame.py::test_an_SR_naming_an_undeclared_crossing_is_a_HARD_finding; tests/test_external_frame.py::test_an_SR_naming_a_DECLARED_crossing_is_clean; tests/test_external_frame.py::test_SR_boundary_COVERAGE_is_a_summary_advisory_and_never_an_error; tests/test_external_frame.py::test_the_realization_gap_is_REPORTED_and_never_gated; tests/test_external_frame.py::test_the_SR_boundary_rule_is_vacuous_without_a_frame; tests/test_trace.py::test_signal_refuses_an_unknown_value_as_a_warn
- **Status**: Drafted
- **Phase**: 5

## SR-164 — Stakeholder-need scope is a declared, checked value

> **Requirement.** Every non-example stakeholder-need row shall carry a scope value from the declared closed vocabulary (template / this repository / both), with a missing or out-of-vocabulary value reported by the harness naming the row.

> **Rationale.** Realizes SN-039 — a need whose audience is ambiguous gets enforced against the wrong repositories, and the scope must be a value a check can read, not inferred from prose. SN-039 had no citing SR. The field enters the SN carrier schema in the scheduled schema batch, per the need's own acceptance, rather than as a one-off column.


### LLR LLR-194, Drafted — never approved
- **Detail**
  - before: schema_findings already walks the real (non-placeholder) rows of one labelled table and reports the exact two failure classes SR-164 asks of scope: a column listed in REQUIRED_FIELDS[label] left empty names the row and the column, and a value present but outside ENUM_FIELDS[label]'s closed set names the row, the offending value and the allowed set. This is the mechanized shape SN-039's own acceptance text designates as scope's future home - 'the scope field enters the registry schema in the scheduled schema batch' - rather than a bespoke one-off column check. NOT DISCHARGED, stated rather than left to be discovered: SN carries no entry in REQUIRED_FIELDS or ENUM_FIELDS today, no dict, checker branch, or test anywhere in the shipped scripts names 'scope', and the whole-registry schema tier does not run over the needs registry at all. This row records the seam the scheduled batch is to ride, not machinery that discharges the obligation; a module or symbol implementing scope-checking does not exist to cite.
  - after: schema_findings scans the real, non-placeholder rows of one labelled table. A column listed in REQUIRED_FIELDS[label] left empty reports its row and column; a value present but outside ENUM_FIELDS[label]'s closed set reports its row, the offending value and the allowed set - the exact two failure classes SR-164 asks of scope. SN-039 designates this mechanism as scope's home rather than a one-off column check. NOT DISCHARGED: SN carries no REQUIRED_FIELDS or ENUM_FIELDS entry, no dict, checker branch or test in the shipped scripts names scope, and the whole-registry schema tier does not run over the needs registry at all. This row records the seam the scheduled work is to ride, not machinery that discharges the obligation; a module or symbol implementing scope-checking does not exist to cite.
- **Title**
  - before: The scope obligation's seam: the schema tier's required-field and closed-vocabulary check, not yet extended to SN
  - after: Scope's schema-tier seam, not yet extended to SN

### TC TC-189, Drafted — never approved
- **Method**
  - before: The seam's own two behaviours, driven over an existing REQUIRED_FIELDS/ENUM_FIELDS table (IF today, since no SN entry exists yet) because the shape being verified is the mechanism scope is scheduled to join, not a scope-specific checker: a column absent from a row raises a warn naming the row and the exact column, gated only under --strict and never by the bare read; and a value present but outside a closed vocabulary is reported as a schema finding naming the row, the offending value and the full allowed set, staying silent at --strict-schema alone and gating only when --strict joins it. Both are the identical shape SR-164's acceptance asks of scope - missing or out-of-vocabulary, naming the row - over the mechanism the field is scheduled to extend rather than a fabricated one.
  - after: The seam's own two behaviours, driven over an existing REQUIRED_FIELDS/ENUM_FIELDS table (IF today, since no SN entry exists yet), because the shape being verified is the mechanism scope is scheduled to join rather than a scope-specific checker. A column absent from a row raises a warn naming the row and the exact column, gated only under --strict and never by the bare read. A value present but outside a closed vocabulary is reported as a schema finding naming the row, the offending value and the full allowed set, staying silent under --strict-schema alone and gating only when --strict joins it. Both are the identical shape SR-164's acceptance asks of scope - missing or out-of-vocabulary, naming the row.

## SR-168 — The state view shows current progress and next work

> **Requirement.** The delivered generator set shall produce one state view from which a reviewer reads the adopted repository's completeness, its requirement decomposition, its planned and in-flight work, and any declared hold on that work, without consulting a second surface.

> **Rationale.** Realizes SN-023's FIRST conjunct — a reviewer sees the project's progress from one dashboard-like file — with SN-010 (what the reviewer reads there is the generated truth, never a hand-kept summary of it). ONE DECISION PER ROW: the integrity of a generated artifact (SR-070) and what that artifact must SHOW are two obligations that fail independently. This row is the progress half; SR-169 is the connection half, along the conjunction SN-023 itself draws. Deliberately NOT folded back into SR-070: a state view that regenerates deterministically, offline and drift-checkably while omitting the work in flight satisfies that row completely and this one not at all. Deliberately NOT folded into SR-169 either: a complete progress view can carry no seam graph whatsoever — the state this repository was in before its interface registry existed — so the two are separately observable and separately buildable. `phase` inherits the parent's 1 rather than tracking its children (which span 1 through 4), because a phase records WHEN a piece shipped, and child-later-than-parent is the ordinary case.


### LLR LLR-198, Drafted — never approved
- **Detail**
  - before: The state view's what-does-the-owner-still-owe derivation, in one module that renders no page and decides nothing about lanes. Three sources, all committed-tree-pure and clock-free: blocked_pending names every queued work item carrying a blockref together with the attestation page it waits on; spine_pending names every Drafted SR (a first approval is owed) and every SR whose text has drifted from its copy under docs/archive/last_approved (a re-attest is owed), one pointer line each naming the on-demand brief that carries the depth; pause_pending names the tracked docs/work/pause, fail-closed on a file it cannot read. pending_items joins the three into the TYPED model - one PendingItem per action carrying its kind and its rendered bullet - and the two shapes above it are declared once here rather than re-assembled per caller: pending_block is the model rendered, the byte-compared content of the generated owner surface's pending region, and owner_cards is the model minus the pause, which is what a drained-queue banner counts. THE SITING IS THE ROW: three modules read this derivation - the dashboard that renders the status snapshot, the generated owner surface, and the dispatcher's exit banner - and only the first is inside the render family, so a derivation defined there forced the other two to import the gen_trajectory facade for a state query and the dispatcher to reach in for two private names. traj_status.py re-exports all four under their former names, so no caller moved t… [213 more chars — read the registry row]
  - after: The pending-owner-action derivation, in one module that renders no page and decides nothing about lanes. Its three committed-tree-pure, clock-free sources are blocked_pending, which names every queued work item carrying a blockref together with the attestation page it waits on; spine_pending, which names each Drafted SR needing a first approval and each SR whose text has drifted from its copy under docs/archive/last_approved needing a re-attest, one pointer line each naming the on-demand brief that carries the depth; and pause_pending, which names the tracked docs/work/pause and fails closed on a file it cannot read. pending_items joins them into the TYPED model - one PendingItem per action carrying its kind and its rendered bullet. pending_block is that model rendered, the byte-compared content of the generated owner surface's pending region, and owner_cards is the model minus the pause, which is what a drained-queue banner counts. THE SITING IS THE ROW: the dashboard status snapshot, the generated owner surface and the dispatcher's exit banner all read this derivation, and only the first is inside the render family, so defining it there forced the other two through the gen_trajectory facade and into two private names. traj_status.py re-exports all four under their former names, so no caller moved to gain the layer and the generated surfaces are byte-identical - which is why that split sibling is named in this row's Module cell beside the read model: the shim is part of what… [35 more chars — read the registry row]
- **Title**
  - before: The pending-owner-action read model, sited below every module that reads it
  - after: Pending-owner-action read model and compatibility shim

### LLR LLR-200, Drafted — never approved
- **Title**
  - before: The depth-0 frame as a generated context view in the state view
  - after: State view depth-0 frame renderer

### TC TC-194, Drafted — never approved
- **Method**
  - before: Two claims, because the row makes two. THE BEHAVIOUR, unchanged by the move and still driven through the facade that used to own it: a blocked row projects its pointer, a Drafted or drifted SR projects the brief that carries the depth, a tracked pause projects one bullet and a malformed one still projects fail-closed, and the whole block is deterministic over two reads. THE MODEL AND THE SITING: assert every item carries a kind field, that owner_cards is exactly pending_items minus the pause, that every item's line appears in the rendered block, that the dispatcher's card read and the generated owner surface's block text each equal the read model's own answer, and that the facade still resolves all four former names to the read model's functions; then build the import graph of the shipped scripts INCLUDING imports written inside function bodies and assert that nothing imports the dashboard facade at all. IF-138, the loader seam this module carries, rides the blocked-row arms: they drive the derivation end to end through the validator's work-registry loaders over real temp repositories, so a loader that stopped answering would red here rather than silently projecting an empty owner queue.
  - after: Drive blocked rows, Drafted or drifted SRs, tracked pauses and malformed pauses through the facade that used to own the derivation. Assert that a blocked row projects its pointer, a Drafted or drifted SR projects the brief that carries the depth, a tracked pause projects exactly one bullet, a malformed one still projects fail-closed, and two reads agree. Assert every item carries a kind field, that owner_cards is exactly pending_items minus the pause, that every item's line appears in the rendered block, that the dispatcher's card read and the generated owner surface's block text each equal the read model's own answer, and that the facade still resolves all four former names to the read model's functions. Build the import graph of the shipped scripts INCLUDING imports written inside function bodies and assert that nothing imports the dashboard facade at all. IF-138, the loader seam this module carries, rides the blocked-row arms: they drive the derivation end to end through the validator's work-registry loaders over real temp repositories, so a loader that stopped answering reds here rather than silently projecting an empty owner queue.

### TC TC-196 — Drafted, never approved
_No cell differs from the approved snapshot; this row owes because its own `Status` has never been `Approved`._
- **TC-ID**: TC-196
- **Verifies**: SR-168;LLR-200
- **Level**: Unit
- **Method**: The derivation and the vacuity, both driven, over a hand-written frame small enough to reason about: two parties (one holding two crossings, one holding none), two crossings (one realized by an interface row that ties back to it, one realized by nothing) and one external-to-external relationship. THE READ MODEL: the three tiers come back id-ordered, the tie-back is joined from the interface registry with the side it ties on, the crossing nothing realizes comes back with an empty realization rather than being dropped, the entity name resolves for display without the crossing row restating it, and an interface row whose endpoint is outside the tree but which ties back to nothing is returned with the reason its own row records. THE RENDER: the census line matches the registry's real counts, every declared row reaches the diagram INCLUDING the party that holds no crossing, direction is read from the system's point of view (an inbound crossing heads at the system, an outbound one at the party), the unrealized crossing is marked in the markup and stated in the table, and the block splices ABOVE the module map rather than after it. VACUITY, DRIVEN BOTH WAYS: with no frame file the artifact is byte-identical to one generated before the view existed and the round trip proves it; a frame holding only the blank form's -000 example rows is equally vacuous; and a repository with a frame but no symbol inventory still earns the architecture tab. THE META REPO'S OWN FRAME is read as data, no… [161 more chars — read the registry row]
- **Tier**: Smoke
- **Expected**: Satisfies the acceptance folded into LLR-200 (parent SR-168)
- **Automated**: Yes
- **Evidence**: tests/test_traj_parse.py::test_frame_context_is_none_without_a_declared_frame; tests/test_traj_parse.py::test_frame_context_drops_the_blank_form_s_example_rows; tests/test_traj_parse.py::test_frame_context_joins_the_tie_backs_and_keeps_id_order; tests/test_traj_parse.py::test_frame_context_reads_this_repo_s_own_locked_frame; tests/test_traj_views.py::test_context_view_renders_the_declared_frame; tests/test_traj_views.py::test_context_view_states_an_untied_external_endpoint_with_its_reason; tests/test_traj_views.py::test_context_view_renders_above_the_derived_structure; tests/test_traj_views.py::test_context_view_omitted_and_byte_identical_without_a_frame; tests/test_traj_views.py::test_a_template_only_frame_renders_the_vacuous_view; tests/test_traj_views.py::test_context_view_alone_earns_the_how_tab
- **Status**: Drafted
- **Phase**: 5

## SR-177 — Fan-out utilisation reported from the run's own telemetry

> **Requirement.** The delivered loop content shall report, per run, the utilisation of the fan-out it commissions — the lanes configured, the lanes actually occupied, and the work integrated per unit of wall time — derived from the run's own recorded telemetry, reported and never gated, with no declared improvement target.

> **Rationale.** Hat-derived (hat.PERFORMANCE, C-PRF-1 — clause text in docs/plans/2026-08-16-blind-derivation-c-hats.md): the charter refuses a declared budget with no measurement behind it, and SN-027 is that finding inverted — the system's most complex machinery justified by a throughput claim no instrument measures, flagged unfalsifiable as written by three independent derivations. The intake proposal (docs/plans/2026-08-17-wi468-obligation-intake-options.md §3 option (b)) is DELIBERATELY LESS than the charter asks — the narrowing is the decision: C-PRF-1 wants a declared improvement over the serial semantic on a declared workload, but the wall time of an LLM loop is dominated by provider latency and model choice, so a numeric target would pin machine, provider and model conditions the kit does not control (one machine is one data point) — this row makes the throughput claim OBSERVABLE rather than BUDGETED. The concrete argument the instrument earns its keep: the lanes dial is undeclared in this repository and defaults to 1, so the machinery SN-027 justifies by fan-out runs SERIAL here and nothing today would say so — this report's first run would have printed lanes=1. SN-027's own `why` states the same structural claim, so the need and the instrument stop overclaiming together. NOT DECOMPOSED, stated rather than implied: nothing aggregates the existing per-session telemetry (wall seconds, api seconds, turns — the session-log headers and the iteration index) by lane or by run, so there is… [175 more chars — read the registry row]


### LLR LLR-196, Drafted — never approved
- **Detail**
  - before: Every session the coordinator launches already carries the columns a utilisation report would consume: `write_session_log` stamps `# wall-secs:`/`# api-secs:`/`# turns:`/`# tokens:`/`# cache-read:`/`# cache-create:` headers on its `docs/iteration/*.log` (wall measured by the coordinator's own clock, the rest parsed from the CLI's `type: result` JSON event — `agent_loop.parse_json_result`/the `meta` dict at its session-close path), and `regenerate_index` folds every log's header into one row per session in the generated `docs/iteration_index.md`, with `per_turn_pace`/`per_turn_context` already deriving the like-for-like speed and context-volume numbers SR-177's own rationale calls out as the existing telemetry to derive from. NOT DISCHARGED, stated rather than left to be discovered: nothing GROUPS these per-session rows by RUN or by LANE — `docs/iteration_index.md` is a flat session table, not a utilisation report — and nothing joins the `lanes` value `agent_loop` resolves at boot (CLI `--lanes` / `AGENT_LANES` / `docs/stack.ini [agent-loop] lanes`) against how many trains actually ran concurrently, so "lanes configured" versus "lanes occupied" and "work items integrated per wall-hour" are both unmeasured today. This row cites the real, already-collected columns and the real folding function the report would extend, rather than a fabricated aggregator that does not exist.
  - after: write_session_log writes # wall-secs:, # api-secs:, # turns:, # tokens:, # cache-read: and # cache-create: headers to each docs/iteration/*.log. The coordinator's own clock supplies wall time; agent_loop.parse_json_result reads the rest from the CLI's type: result JSON event into the meta dict at its session-close path. regenerate_index folds these headers into one row per session in docs/iteration_index.md; per_turn_pace and per_turn_context already derive the like-for-like speed and context-volume values SR-177 identifies as the existing telemetry to derive from. NOT DISCHARGED: docs/iteration_index.md is a flat session table with no RUN or LANE grouping, and nothing joins the lanes value agent_loop resolves at boot (CLI --lanes, AGENT_LANES, docs/stack.ini [agent-loop] lanes) against how many trains actually ran concurrently, so configured lanes, occupied lanes and work items integrated per wall-hour are all unmeasured today. This row cites the real, already-collected columns and the real folding function the report would extend; the missing aggregation is the stated build gap and no aggregator exists.
- **Title**
  - before: The per-session telemetry a fan-out utilisation report would aggregate, not yet aggregated
  - after: Per-session fan-out utilisation telemetry, unaggregated

### TC TC-191, Drafted — never approved
- **Method**
  - before: Runs the real folding path rather than reading it: `tests/test_generated_newlines.py::test_the_three_crash_paths_actually_run` calls `agent_common.regenerate_index` on a synthetic `docs/` tree and asserts the generated table exists (the row-per-session fold LLR-196 cites); `tests/test_agent_loop.py::test_done_exit_writes_logs_and_index` and `::test_stream_json_echo_and_result_parse` drive a live fake-CLI session end to end and assert the exact telemetry columns (`wall-secs`, `api-secs`, `turns`, `tokens`, `cache-read`, `cache-create`) land in the log header `regenerate_index`/`per_turn_pace`/`per_turn_context` read. No test here exercises a lanes-configured-vs-occupied or work-per-wall-hour GROUPING, because LLR-196 states plainly that grouping does not exist yet — this case verifies the inputs the future grouping pass would consume, not the report SR-177 ultimately asks for.
  - after: Runs the real folding path rather than reading it. tests/test_generated_newlines.py::test_the_three_crash_paths_actually_run calls agent_common.regenerate_index on a synthetic docs/ tree and asserts the generated table exists - the row-per-session fold LLR-196 cites. tests/test_agent_loop.py::test_done_exit_writes_logs_and_index and ::test_stream_json_echo_and_result_parse drive a live fake-CLI session end to end and assert that wall-secs, api-secs, turns, tokens, cache-read and cache-create land in the log header regenerate_index, per_turn_pace and per_turn_context read. No test here exercises a lanes-configured-versus-occupied or work-per-wall-hour GROUPING, because LLR-196 states plainly that the grouping does not exist yet: this case verifies the inputs the future grouping pass would consume, not the report SR-177 ultimately asks for.
