# Re-attestation brief — `Modified` spine rows

_Generated by `trace.py --ratify modified` (WI-316). One section per `Modified` SR (the attestation unit); each chain row shows only its CHANGED cells, before (the attested baseline revision) vs after (the working tree). The `Verified`→`Modified` Status flip itself is not listed — the marker is not the amendment. Baseline: `--since` when given, else the newest revision where the SR row still read `Verified` (correct under the amend+flip-same-commit rule the staged warn enforces — use `--since` for a pre-regime streak). Rule on each section: bless → `Verified`; evidence no longer verifies the amended text → `Planned` (process.md §7)._


## SR-003 — Placeholder and schema checks

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-003 read `Verified`._

### SR SR-003
- **Rationale**
  - before: Realizes SN-022 (a committed placeholder at a gate is a finding) and SN-002 (a real row missing a required cell breaks the trace).
  - after: Realizes the dissolved edge expectation that a committed placeholder at a gate is a finding and SN-002 (a real row missing a required cell breaks the trace).

## SR-006 — Gate/tier harness enforces required steps

_Baseline `ec5a8b983` (2026-08-13) — newest revision where SR-006 read `Verified`._

### SR SR-006
- **SN-Refs**
  - before: SN-004;SN-008;SN-014
  - after: SN-004;SN-008
- **Requirement**
  - before: check.py shall run the active gate's required steps and fail the gate when a required tool is missing, reporting SKIP(missing) rather than silently passing.
  - after: check.py shall run the required steps of the gate that must next be passed (the strictness selector cached in docs/gate) and fail that gate when a required tool is missing, reporting SKIP(missing) rather than silently passing.
- **Rationale**
  - before: Realizes SN-004 (gates enforce their bar), SN-008 (no false green) and SN-014 (a missing tool fails, never skips). The work-branch lane skip is SR-133's obligation (one row, one obligation).
  - after: Realizes SN-004 (gates enforce their bar), SN-008 (no false green) and the dissolved edge expectation that a missing tool fails, never skips. The work-branch lane skip is SR-133's obligation (one row, one obligation).
- **AcceptanceCriteria**
  - before: check.py --gate GN runs that gate's steps; a required-but-absent tool fails the gate with guidance (never a silent pass), and --lenient degrades it to SKIP; --tier selects the declared step subset.
  - after: check.py --gate <bar> runs that bar's steps; a required-but-absent tool fails the gate with guidance (never a silent pass), and --lenient degrades it to SKIP; --tier selects the declared step subset.

### LLR LLR-006
- **Component**
  - before: CMP-001
  - after: CMP-007

## SR-021 — Hook python probe

_Baseline `ec5a8b983` (2026-08-13) — newest revision where SR-021 read `Verified`._

### SR SR-021
- **SN-Refs**
  - before: SN-013
  - after: SN-001
- **Requirement**
  - before: The git hooks shall locate a working python3 by running a candidate and skip-or-report clearly when none resolves or a Store-alias exits nonzero.
  - after: The git hooks and the root launchers (dev-setup / agent-resume, the coordinator's entry) shall locate a working python3 by running a candidate and skip-or-report clearly when none resolves or a Store-alias exits nonzero.
- **Rationale**
  - before: Realizes SN-013 — no cryptic crash when the interpreter is absent or a Windows Store alias.
  - after: Realizes the dissolved edge expectation that the interpreter is probed by running a candidate — no cryptic crash when the interpreter is absent or a Windows Store alias.
- **AcceptanceCriteria**
  - before: With no working python3 the hook reports clearly and does not crash; with one, it runs the checks.
  - after: With no working python3 the hook and each root launcher report clearly and do not crash cryptically; with one, they run their action. (The launcher half folded in from retired SN-013's expectation at the 2026-08-13 dissolution — the review round caught that SR-021 alone carried only the hooks.)

### LLR LLR-021
- **Component**
  - before: CMP-004
  - after: CMP-007

## SR-023 — Arch-map generation

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-023 read `Verified`._

### SR SR-023
- **Rationale**
  - before: Realizes SN-010 and SN-021 — a generated view cannot silently rot; --check makes drift a red.
  - after: Realizes SN-010 and the dissolved edge expectation that a generated artifact drifting from its source fails its --check — a generated view cannot silently rot; --check makes drift a red.

## SR-025 — Skills index + checked per-agent fan-out

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-025 read `Verified`._

### SR SR-025
- **Rationale**
  - before: Realizes SN-012 (opt-in accelerators) and SN-021 — a hand-maintained index drifts from the skills it indexes, and an index that disagrees with its sources is worse than none, because it is trusted. Requiring each author to update the index by hand was rejected for the same reason the traceability matrix is generated: the discipline fails silently.
  - after: Realizes SN-012 (opt-in accelerators) and the dissolved edge expectation that a generated artifact drifting from its source fails its --check — a hand-maintained index drifts from the skills it indexes, and an index that disagrees with its sources is worse than none, because it is trusted. Requiring each author to update the index by hand was rejected for the same reason the traceability matrix is generated: the discipline fails silently.

## SR-026 — Coordinator resumes headless

_Baseline `1e4d7f0b0` (2026-08-12) — newest revision where SR-026 read `Verified`._

### SR SR-026
- **SN-Refs**
  - before: SN-006;SN-016
  - after: SN-006
- **Requirement**
  - before: agent_loop.py shall resume headless with stdin closed, never blocking on a prompt. Resume authority (one path - the serial resume loop is retired): a worker resumes from its explicit claimed assignment plus the committed trailer evidence on its branch, and the integrator (SR-132) derives claim/queue state from trunk history alone; docs/status.md is a generated reference surface (SR-059), never a session input.
  - after: agent_loop.py shall resume headless with stdin closed, never blocking on a
prompt.
Resume authority (one path - the serial resume loop is retired): a worker
resumes from its explicit claimed assignment plus the committed trailer evidence
on its branch, and the integrator (SR-132) derives claim/queue state from trunk
history alone.
docs/status.md is a generated reference surface (SR-059), never a session input.
- **Rationale**
  - before: Realizes SN-006 and SN-016 — an unattended run blocked on a prompt is indistinguishable from a hung one and holds its lane until a human notices. Keeping a separate serial resume path beside the dispatcher was rejected: two authorities over one decision drift, and the divergence surfaces as a run that resumes differently depending on how it was started.
  - after: Realizes SN-006 and the dissolved edge expectation that an unattended run never blocks on a prompt, at launch or mid-run — an unattended run blocked on a prompt is indistinguishable from a hung one and holds its lane until a human notices. Keeping a separate serial resume path beside the dispatcher was rejected: two authorities over one decision drift, and the divergence surfaces as a run that resumes differently depending on how it was started.
- **AcceptanceCriteria**
  - before: A worker reconciles from its assignment + committed evidence and the integrator from trunk history; every mode runs with stdin closed and a prompt cannot wedge it.
  - after: A worker reconciles from its assignment + committed evidence and the integrator from trunk history; every mode runs with stdin closed and a prompt cannot wedge it; a model rate limit backs off rather than failing the run, and a stall aborts to protect the budget. (The backoff/budget clauses folded in from retired SN-016's expectation at the 2026-08-13 dissolution — the review round caught that no SR carried them.)

### LLR LLR-026
- **Component**
  - before: CMP-004
  - after: CMP-008

### LLR LLR-143
- **Component**
  - before: CMP-004
  - after: CMP-008

## SR-027 — Coordinator preflight

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-027 read `Verified`._

### SR SR-027
- **Rationale**
  - before: Realizes SN-006 and SN-015 — a run that starts on a broken footing fails later and further from the cause, and an unattended loop cannot be asked what went wrong. Continuing with a warning was rejected: each of these conditions makes every subsequent step meaningless, so a typed nonzero exit is what lets the caller act on it.
  - after: Realizes SN-006 and the dissolved edge expectation that a preflight on a non-git working directory reports and exits nonzero rather than hanging — a run that starts on a broken footing fails later and further from the cause, and an unattended loop cannot be asked what went wrong. Continuing with a warning was rejected: each of these conditions makes every subsequent step meaningless, so a typed nonzero exit is what lets the caller act on it.

## SR-028 — Coordinator typed outcomes

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-028 read `Verified`._

### SR SR-028
- **Rationale**
  - before: Realizes SN-006, SN-019 and SN-020 — a coordinator ending in an untyped code forces its caller to parse prose; a session that produced no commit still has a HEAD to report, so without a guard an empty run is indistinguishable from a productive one in the record it leaves; and an all-ERROR run reported as a work stall sends a human looking for a blocked work item that does not exist. Treating an agent error as a stall was rejected for that reason: an unreachable agent and a blocked item need different responses, so they need different outcomes.
  - after: Realizes SN-006 and the dissolved edge expectations that a repository with no commits yet (HEAD absent) does not crash the loop and that an all-ERROR run is an unavailable agent rather than a work stall — a coordinator ending in an untyped code forces its caller to parse prose; a session that produced no commit still has a HEAD to report, so without a guard an empty run is indistinguishable from a productive one in the record it leaves; and an all-ERROR run reported as a work stall sends a human looking for a blocked work item that does not exist. Treating an agent error as a stall was rejected for that reason: an unreachable agent and a blocked item need different responses, so they need different outcomes.

## SR-029 — Coordinator lock auto-release

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-029 read `Verified`._

### SR SR-029
- **Rationale**
  - before: Realizes SN-017 — the dispatcher, each worker and an interactive sitting all take this lock, so a holder that dies without releasing it wedges every one of them behind an entry nobody can safely clear. A pid file plus a liveness check was rejected because it cannot decide the case it exists for: if the pid has been reused the check sees a live process and keeps a dead lock forever, and if it guesses the other way it clears a lock that is still held. A kernel advisory lock releases on process death by construction, so the question never arises.
  - after: Realizes the dissolved edge expectation that a coordinator killed mid-session leaves the next run un-wedged — the dispatcher, each worker and an interactive sitting all take this lock, so a holder that dies without releasing it wedges every one of them behind an entry nobody can safely clear. A pid file plus a liveness check was rejected because it cannot decide the case it exists for: if the pid has been reused the check sees a live process and keeps a dead lock forever, and if it guesses the other way it clears a lock that is still held. A kernel advisory lock releases on process death by construction, so the question never arises.

## SR-030 — Second coordinator refused

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-030 read `Verified`._

### SR SR-030
- **Rationale**
  - before: Realizes SN-018 and SN-006 — two coordinators in one checkout write the same working tree, and the loser's commits interleave into the winner's diff where no review will separate them. Coordinating by convention was rejected: the failure is silent and shows up only as a garbled history.
  - after: Realizes the dissolved edge expectation that a second coordinator launched in the same checkout is refused, and SN-006 — two coordinators in one checkout write the same working tree, and the loser's commits interleave into the winner's diff where no review will separate them. Coordinating by convention was rejected: the failure is silent and shows up only as a garbled history.

## SR-040 — Per-phase routing and review dial

_Baseline `2f0ee1f5c` (2026-08-07) — newest revision where SR-040 read `Verified`._

### SR SR-040
- **SN-Refs**
  - before: SN-006;SN-016
  - after: SN-006
- **Requirement**
  - before: The unattended coordinator shall select the agent command template per in-process session phase (PLAN/BUILD/REVIEW-A/REVIEW-B/DESIGN-CHECK/CRITIQUE - the coordinator's own activity; the retired docs/run-phase file, deleted by SR-059, is not an input) via AGENT_CMD_MAP/--cmd-map, falling back to the single AGENT_CMD; surface the declared reviewer dial (docs/review-policy, default 1) at run start without enforcing it; and warn (never block) when a lane resume surface exceeds the declared size threshold.
  - after: The unattended coordinator shall select the agent command template per
in-process session phase (PLAN/BUILD/REVIEW-A/REVIEW-B/DESIGN-CHECK/CRITIQUE -
the coordinator's own activity; the retired docs/run-phase file, deleted by
SR-059, is not an input) via AGENT_CMD_MAP/--cmd-map, falling back to the single
AGENT_CMD.
It shall surface the declared reviewer dial (docs/process.toml [policies]
review_rounds, default 1; the legacy docs/review-policy is still read through
the migration window) at run start without enforcing it.
It shall warn (never block) when a lane resume surface exceeds the declared size
threshold.
- **Rationale**
  - before: Realizes SN-006 and SN-016 — two samples of one model share blind spots, so a review by the family that built the change agrees with it for the wrong reasons; cross-family routing is what makes the second opinion independent, and one model for every phase was rejected on that basis. Dispatch stays keyed off the in-process session phase because the alternative, a per-phase code branch, makes adding a phase a code change and so discourages adding one. The resume surface's growth tripwire warns rather than fails because a growing resume surface is a smell and not a defect, and failing on a smell trains an operator to bypass the check.
  - after: Realizes SN-006 and the dissolved edge expectation that an unattended run never blocks on a prompt, at launch or mid-run — two samples of one model share blind spots, so a review by the family that built the change agrees with it for the wrong reasons; cross-family routing is what makes the second opinion independent, and one model for every phase was rejected on that basis. Dispatch stays keyed off the in-process session phase because the alternative, a per-phase code branch, makes adding a phase a code change and so discourages adding one. The resume surface's growth tripwire warns rather than fails because a growing resume surface is a smell and not a defect, and failing on a smell trains an operator to bypass the check.
- **AcceptanceCriteria**
  - before: A mapped session phase invokes its own command template and the default template does not fire; a broken map entry fails preflight before iteration 1; the banner surfaces docs/review-policy; an oversized status.md warns on stderr while the run proceeds; AGENT_STATUS_WARN_BYTES=0 silences.
  - after: A mapped session phase invokes its own command template and the default template does not fire; a broken map entry fails preflight before iteration 1; the banner surfaces the reviewer dial from docs/process.toml [policies] review_rounds; an oversized status.md warns on stderr while the run proceeds; AGENT_STATUS_WARN_BYTES=0 silences.

### LLR LLR-037
- **Component**
  - before: CMP-004
  - after: CMP-008

## SR-050 — Process reference view

_Baseline `1e4d7f0b0` (2026-08-12) — newest revision where SR-050 read `Verified`._

### SR SR-050
- **SN-Refs**
  - before: SN-010;SN-021
  - after: SN-010
- **Requirement**
  - before: gen_trajectory.py shall render a Process reference tab in PROJECT_STATE.html beside the existing views, presenting how the project is built as three linked panels: (1) artifact lifecycle x gates - Vision -> SN (G1) -> SR (G1->G2) -> LLR + architecture (G2) -> TC (G2->G3) -> code+tests (G3), each stage linked to its process-doc section and annotated with this repo live tier counts; (2) the resume loop - the managed agent_loop flow (read status -> PLAN -> BUILD -> REVIEW -> INTEGRATE -> commit -> hook/gate -> repeat) with its escalation edges; (3) slices -> phase -> gates - a per-WI slice ends at the commit bar, a phase closes at the gate bar, CI runs the same bar every push. Data-derived where a canonical source exists (docs/gate, the spine registries, the WI registry (docs/work/)); self-contained and byte-deterministic; a data-less repo renders byte-identically; --check freshness unchanged.
  - after: gen_trajectory.py shall render a Process reference tab in PROJECT_STATE.html
beside the existing views, presenting how the project is built as three linked
panels:
(1) artifact lifecycle x gates - Vision -> SN (DevBar-Reqs) -> SR (DevBar-Reqs->DevBar-Tests) -> LLR +
architecture (DevBar-Tests) -> TC (DevBar-Tests->DevBar-Release) -> code+tests (DevBar-Release), each stage linked to its
process-doc section and annotated with this repo live tier counts;
(2) the resume loop - the managed agent_loop flow (read status -> PLAN -> BUILD
-> REVIEW -> INTEGRATE -> commit -> hook/gate -> repeat) with its escalation
edges;
(3) slices -> phase -> gates - a per-WI slice ends at the commit bar, a phase
closes at the gate bar, CI runs the same bar every push.
The tab shall be data-derived where a canonical source exists (docs/gate, the
spine registries, the WI registry (docs/work/)); self-contained and
byte-deterministic; a data-less repo renders byte-identically; --check
freshness unchanged.
- **Rationale**
  - before: Realizes SN-010 and SN-021 — the dashboard shows project state but not the method that produced it, so a reader sees what is happening without seeing why it is allowed to happen. Restating the process docs in-view was rejected because it creates a second home that drifts: the view links out wherever a canonical home exists and restates only relationships no single doc states. Generating it is the ideal; a static-authored panel downgrades verification to adjudication by an independent critical eye.
  - after: Realizes SN-010 and the dissolved edge expectation that a generated artifact drifting from its source fails its --check — the dashboard shows project state but not the method that produced it, so a reader sees what is happening without seeing why it is allowed to happen. Restating the process docs in-view was rejected because it creates a second home that drifts: the view links out wherever a canonical home exists and restates only relationships no single doc states. Generating it is the ideal; a static-authored panel downgrades verification to adjudication by an independent critical eye.

### LLR LLR-051
- **Component**
  - before: CMP-002
  - after: CMP-009

## SR-055 — Ingest and human-decision process loops

_Baseline `1e4d7f0b0` (2026-08-12) — newest revision where SR-055 read `Verified`._

### SR SR-055
- **SN-Refs**
  - before: SN-010;SN-021
  - after: SN-010
- **Requirement**
  - before: The Process tab shall additionally render the project's two circular working loops as linked flow panels: (A) the intake loop - owner/agent intake -> triage into work items with spec detail -> the scheduler-derived ready frontier in the WI registry (docs/work/) -> build/review -> merge; and (B) the human-decision loop - open-items population (including the gate-ratification table) -> human review and ruling -> the log's Decisions record -> merge - with the LLM_Agent entry point rendered once and shared by both loops; each stage links to its canonical home (status.md, the WI registry (docs/work/), open-items.html, log.md); the panels stay data-derived where a canonical source exists, self-contained, byte-deterministic; a data-less repo renders byte-identically and --check freshness is unchanged.
  - after: The Process tab shall additionally render the project's two circular working
loops as linked flow panels:
(A) the intake loop - owner/agent intake -> triage into work items with spec
detail -> the scheduler-derived ready frontier in the WI registry (docs/work/)
-> build/review -> merge; and
(B) the human-decision loop - open-items population (including the
gate-ratification table) -> human review and ruling -> the log's Decisions
record -> merge
- with the LLM_Agent entry point rendered once and shared by both loops;
each stage links to its canonical home (status.md, the WI registry
(docs/work/), open-items.html, log.md);
the panels stay data-derived where a canonical source exists, self-contained,
byte-deterministic; a data-less repo renders byte-identically and --check
freshness is unchanged.
- **Rationale**
  - before: Realizes SN-010 and SN-021 — the Process tab shows the artifact lifecycle, the resume loop and the slice cadence, but not how work ENTERS or how the human decides, which are the two circular flows that close the method's loop; without them a reader sees the machine and not the way into it. Folding them into the existing panels was rejected: they are loops, and a linear lifecycle panel cannot show a cycle.
  - after: Realizes SN-010 and the dissolved edge expectation that a generated artifact drifting from its source fails its --check — the Process tab shows the artifact lifecycle, the resume loop and the slice cadence, but not how work ENTERS or how the human decides, which are the two circular flows that close the method's loop; without them a reader sees the machine and not the way into it. Folding them into the existing panels was rejected: they are loops, and a linear lifecycle panel cannot show a cycle.

### LLR LLR-056
- **Component**
  - before: CMP-002
  - after: CMP-009

## SR-070 — Offline project-state core

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-070 read `Verified`._

### SR SR-070
- **Rationale**
  - before: Realizes SN-010;SN-021 by isolating one independently reviewable obligation formerly carried by SR-038.
  - after: Realizes SN-010 and the dissolved edge expectation that a generated artifact drifting from its source fails its --check, by isolating one independently reviewable obligation formerly carried by SR-038.

## SR-071 — Conditional dashboard architecture and knowledge views

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-071 read `Verified`._

### SR SR-071
- **Rationale**
  - before: Realizes SN-010;SN-021 by isolating one independently reviewable obligation formerly carried by SR-038.
  - after: Realizes SN-010 and the dissolved edge expectation that a generated artifact drifting from its source fails its --check, by isolating one independently reviewable obligation formerly carried by SR-038.

## SR-072 — Deterministic responsive dashboard freshness

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-072 read `Verified`._

### SR SR-072
- **Rationale**
  - before: Realizes SN-010 and SN-021 — a generated dashboard that differs between runs cannot be freshness-checked, because every check reports a diff and the real staleness hides among them. Comparing rendered screenshots was rejected: it is expensive and still non-deterministic.
  - after: Realizes SN-010 and the dissolved edge expectation that a generated artifact drifting from its source fails its --check — a generated dashboard that differs between runs cannot be freshness-checked, because every check reports a diff and the real staleness hides among them. Comparing rendered screenshots was rejected: it is expensive and still non-deterministic.

## SR-079 — Agent pair-row routing

_Baseline `f5a423ae2` (2026-08-01) — newest revision where SR-079 read `Verified`._

### SR SR-079
- **SN-Refs**
  - before: SN-006;SN-016
  - after: SN-026;SN-006
- **Rationale**
  - before: Realizes SN-006;SN-016 by isolating one independently reviewable obligation formerly carried by SR-045.
  - after: Realizes SN-026;SN-006 and the dissolved edge expectation that an unattended run never blocks on a prompt, at launch or mid-run, by isolating one independently reviewable obligation formerly carried by SR-045.

### LLR LLR-044
- **Component**
  - before: CMP-004
  - after: CMP-008

## SR-080 — Managed review-session scheduling

_Baseline `f5a423ae2` (2026-08-01) — newest revision where SR-080 read `Verified`._

### SR SR-080
- **SN-Refs**
  - before: SN-006;SN-016
  - after: SN-026;SN-006
- **Rationale**
  - before: Realizes SN-006;SN-016 by isolating one independently reviewable obligation formerly carried by SR-045.
  - after: Realizes SN-026;SN-006 and the dissolved edge expectation that an unattended run never blocks on a prompt, at launch or mid-run, by isolating one independently reviewable obligation formerly carried by SR-045.

### LLR LLR-045
- **Component**
  - before: CMP-004
  - after: CMP-008

## SR-081 — Review substance scoring

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-081 read `Verified`._

### SR SR-081
- **Rationale**
  - before: Realizes SN-006;SN-016 by isolating one independently reviewable obligation formerly carried by SR-045.
  - after: Realizes SN-006 and the dissolved edge expectation that an unattended run never blocks on a prompt, at launch or mid-run, by isolating one independently reviewable obligation formerly carried by SR-045.

## SR-082 — Fixed review escalation

_Baseline `2f0ee1f5c` (2026-08-07) — newest revision where SR-082 read `Verified`._

### SR SR-082
- **SN-Refs**
  - before: SN-006;SN-016
  - after: SN-006
- **Requirement**
  - before: The coordinator shall apply fixed win-stay/lose-shift escalation and gate-policy paging for shared failure, contradictions, and tripwires.
  - after: The coordinator shall apply fixed win-stay/lose-shift escalation and page for shared failure, contradictions and tripwires per the declared ratification level, keyed on two separate dials: whether the tier in process is still the human's to ratify (human_ratification_through), and whether non-dependent work keeps running while a ratification is queued (keep_nondependent).
- **Rationale**
  - before: Realizes SN-006;SN-016 by isolating one independently reviewable obligation formerly carried by SR-045.
  - after: Realizes SN-006 and the dissolved edge expectation that an unattended run never blocks on a prompt, at launch or mid-run, by isolating one independently reviewable obligation formerly carried by SR-045.
- **AcceptanceCriteria**
  - before: Two failed gates swap family, a failed swap raises tier, and shared top-tier failure, contradictions, or tripwires page according to gate policy.
  - after: Two failed gates swap family, a failed swap raises tier, and shared top-tier failure, contradictions, or tripwires page according to the declared ratification level and the run's derived session_hold.

### LLR LLR-081
- **Detail**
  - before: Applies win-stay/lose-shift family swap, tier escalation, and gate-policy paging for shared failure, contradiction, and tripwires.
  - after: Applies win-stay/lose-shift family swap, tier escalation, and ratification-level-keyed paging for shared failure, contradiction, and tripwires.
- **Component**
  - before: CMP-004
  - after: CMP-008

### TC TC-084
- **Method**
  - before: Drive swap, tier-up, shared failure, contradiction, tripwire, and gate-policy escalation branches.
  - after: Drive swap, tier-up, shared failure, contradiction, tripwire, and the ratification-level escalation branches (all four human_held x keep_nondependent cells).
- **Evidence**
  - before: tests/test_agent_route.py::test_escalate_swap_then_tier_up_then_page; tests/test_agent_route.py::test_escalate_pages_on_top_tier_shared_failure; tests/test_agent_route.py::test_escalate_pages_on_tripwire_and_double_contradiction; tests/test_agent_route.py::test_failure_action_keyed_to_gate_policy; tests/test_agent_loop_review.py::test_changes_requested_swaps_implementer_family; tests/test_agent_loop_review.py::test_escalation_tiers_up_after_swap
  - after: tests/test_agent_route.py::test_escalate_swap_then_tier_up_then_page; tests/test_agent_route.py::test_escalate_pages_on_top_tier_shared_failure; tests/test_agent_route.py::test_escalate_pages_on_tripwire_and_double_contradiction; tests/test_agent_route.py::test_failure_action_keyed_to_the_ratification_level; tests/test_agent_loop_review.py::test_changes_requested_swaps_implementer_family; tests/test_agent_loop_review.py::test_escalation_tiers_up_after_swap

## SR-083 — Dual-plan planner-pair selection

_Baseline `f5a423ae2` (2026-08-01) — newest revision where SR-083 read `Verified`._

### SR SR-083
- **SN-Refs**
  - before: SN-006;SN-016
  - after: SN-026;SN-006
- **Rationale**
  - before: Realizes SN-006;SN-016 by isolating one independently reviewable obligation formerly carried by SR-045.
  - after: Realizes SN-026;SN-006 and the dissolved edge expectation that an unattended run never blocks on a prompt, at launch or mid-run, by isolating one independently reviewable obligation formerly carried by SR-045.

### LLR LLR-072
- **Component**
  - before: CMP-004
  - after: CMP-008

## SR-085 — Bounded critique rework loop

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-085 read `Verified`._

### SR SR-085
- **Requirement**
  - before: The coordinator shall iterate BUILD-to-CRITIQUE rework within AGENT_CRITIQUE_MAX and page through gate policy when the cap is exhausted.
  - after: The coordinator shall iterate BUILD-to-CRITIQUE rework within AGENT_CRITIQUE_MAX and page through the declared ratification level when the cap is exhausted.
- **AcceptanceCriteria**
  - before: CHANGES-REQUESTED reworks and re-critiques; approval stops; the configured cap pages through the declared policy.
  - after: CHANGES-REQUESTED reworks and re-critiques; approval stops; the configured cap pages per the declared ratification level.

## SR-089 — Tiered When roadmap view

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-089 read `Verified`._

### SR SR-089
- **Rationale**
  - before: Realizes SN-010;SN-021 by isolating one independently reviewable obligation formerly carried by SR-051.
  - after: Realizes SN-010 and the dissolved edge expectation that a generated artifact drifting from its source fails its --check, by isolating one independently reviewable obligation formerly carried by SR-051.

## SR-090 — Tiered How-SW hierarchy

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-090 read `Verified`._

### SR SR-090
- **Rationale**
  - before: Realizes SN-010;SN-021 by isolating one independently reviewable obligation formerly carried by SR-051.
  - after: Realizes SN-010 and the dissolved edge expectation that a generated artifact drifting from its source fails its --check, by isolating one independently reviewable obligation formerly carried by SR-051.

## SR-091 — Interface-wired hierarchy ports

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-091 read `Verified`._

### SR SR-091
- **Rationale**
  - before: Realizes SN-010;SN-021 by isolating one independently reviewable obligation formerly carried by SR-051.
  - after: Realizes SN-010 and the dissolved edge expectation that a generated artifact drifting from its source fails its --check, by isolating one independently reviewable obligation formerly carried by SR-051.

## SR-092 — One-layer hierarchy navigation

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-092 read `Verified`._

### SR SR-092
- **Rationale**
  - before: Realizes SN-010;SN-021 by isolating one independently reviewable obligation formerly carried by SR-051.
  - after: Realizes SN-010 and the dissolved edge expectation that a generated artifact drifting from its source fails its --check, by isolating one independently reviewable obligation formerly carried by SR-051.

## SR-108 — Dual-plan round disposition

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-108 read `Verified`._

### SR SR-108
- **Requirement**
  - before: The --dual-plan round shall commit SELECT as a docs-only disposition (children filed as queued specs, validated atomically with their generated views) and surface PAGE per gate policy, never silently.
  - after: The --dual-plan round shall commit SELECT as a docs-only disposition (children filed as queued specs, validated atomically with their generated views) and surface PAGE per the declared ratification level, never silently.
- **Rationale**
  - before: Realizes SN-024 and SN-016 — a disposition split across commits can land half-applied, and halting every lane for one round's outcome throws away the parallelism for work the outcome does not touch. Applying the disposition per-lane was rejected: the registries are shared, so concurrent writers conflict on every one.
  - after: Realizes SN-024 and the dissolved edge expectation that an unattended run never blocks on a prompt, at launch or mid-run — a disposition split across commits can land half-applied, and halting every lane for one round's outcome throws away the parallelism for work the outcome does not touch. Applying the disposition per-lane was rejected: the registries are shared, so concurrent writers conflict on every one.

## SR-112 — Checked per-agent skill fan-out

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-112 read `Verified`._

### SR SR-112
- **Rationale**
  - before: Realizes SN-021 — one skill maintained in three agent-specific copies rots into three different skills, and the copy an agent actually loads is the one nobody edited. Maintaining a single shared directory was rejected because each agent harness looks in its own path; the fan-out is forced, so the copies are generated and drift is a finding.
  - after: Realizes the dissolved edge expectation that a generated artifact drifting from its source fails its --check — one skill maintained in three agent-specific copies rots into three different skills, and the copy an agent actually loads is the one nobody edited. Maintaining a single shared directory was rejected because each agent harness looks in its own path; the fan-out is forced, so the copies are generated and drift is a finding.

## SR-122 — Dashboard freshness check

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-122 read `Verified`._

### SR SR-122
- **Rationale**
  - before: Realizes SN-021 — a committed dashboard that nobody re-checks becomes a confident picture of a project that has moved on, and the as-of stamp changes on every commit, so comparing it would make the check cry wolf and get disabled. Regenerating on read was rejected: the artifact is committed so it can be read without the toolchain.
  - after: Realizes the dissolved edge expectation that a generated artifact drifting from its source fails its --check — a committed dashboard that nobody re-checks becomes a confident picture of a project that has moved on, and the as-of stamp changes on every commit, so comparing it would make the check cry wolf and get disabled. Regenerating on read was rejected: the artifact is committed so it can be read without the toolchain.

## SR-125 — PAGE maps through the declared ratification level

_Baseline `2f0ee1f5c` (2026-08-07) — newest revision where SR-125 read `Verified`._

### SR SR-125
- **Title**
  - before: PAGE maps through gate policy
  - after: PAGE maps through the declared ratification level
- **Requirement**
  - before: The --dual-plan single-shot round shall map a PAGE through gate policy, hard-gating a human only under attended policy and otherwise reaching the pause-free attention (STALL) end state.
  - after: The --dual-plan single-shot round shall map a PAGE through the declared ratification level, hard-gating a human only while the tier in process is still HUMAN-HELD and otherwise reaching the pause-free attention (STALL) end state.
- **Rationale**
  - before: Realizes SN-016 — a PAGE that always stops for a human makes the unattended loop attended in practice, while one that never stops discards the judgement the round asked for. Two separate mappings for the dispatcher and the single-shot round were rejected: they drift, and the drift shows up as a loop that hangs on one path only.
  - after: Realizes the dissolved edge expectation that an unattended run never blocks on a prompt, at launch or mid-run — a PAGE that always stops for a human makes the unattended loop attended in practice, while one that never stops discards the judgement the round asked for. Two separate mappings for the dispatcher and the single-shot round were rejected: they drift, and the drift shows up as a loop that hangs on one path only.
- **AcceptanceCriteria**
  - before: A PAGE preserves evidence and follows attended/autonomous policy (attended -> NEEDS-HUMAN exit; autonomous/single-ratify -> STALL exit, no human gate).
  - after: A PAGE preserves evidence and follows the declared level (a human-held tier -> NEEDS-HUMAN exit; a loop-held tier -> STALL exit, no human gate); an unreadable level or stage resolves to human-held, so the failure direction is the gate, never past it.

### LLR LLR-132
- **Title**
  - before: PAGE gate-policy mapping
  - after: PAGE ratification-level mapping
- **Detail**
  - before: Applies gate-policy continuation semantics to a PAGE at both the dispatcher disposition and the --dual-plan single-shot round (attended pages, autonomous/single-ratify STALLs).
  - after: Applies the declared level's continuation semantics to a PAGE at both the dispatcher disposition and the --dual-plan single-shot round: `page_action(human_held, keep_nondependent)` over the four cells the retired enum's three words could not span — a human-held tier pages, a loop-held one STALLs, and `None` normalizes to the strictest cell rather than to `bool(None)`'s most permissive one.
- **Component**
  - before: CMP-004
  - after: CMP-008

### TC TC-098
- **Method**
  - before: Run dispatcher SELECT, attended PAGE, autonomous PAGE continuation, regeneration failure, and salvage cases, plus the dual-plan PAGE under attended, autonomous, and single-ratify at both the dispatcher and the --dual-plan single-shot entry.
  - after: Run dispatcher SELECT, human-held PAGE, loop-held PAGE continuation, regeneration failure, and salvage cases, plus the dual-plan PAGE on a human-held tier, on a loop-held tier, and with keep_nondependent, at both the dispatcher and the --dual-plan single-shot entry.
- **Evidence**
  - before: tests/test_dual_plan_round.py::test_full_round_unattended_selects_and_files; tests/test_dual_plan_round.py::test_arbiter_disagreement_pages; tests/test_dual_plan_round.py::test_arbiter_disagreement_autonomous_stalls_not_pages; tests/test_dual_plan_round.py::test_arbiter_disagreement_single_ratify_stalls_not_pages
  - after: tests/test_dual_plan_round.py::test_full_round_unattended_selects_and_files; tests/test_dual_plan_round.py::test_arbiter_disagreement_pages; tests/test_dual_plan_round.py::test_arbiter_disagreement_on_a_loop_held_tier_stalls_not_pages; tests/test_dual_plan_round.py::test_arbiter_disagreement_with_keep_nondependent_stalls_not_pages
