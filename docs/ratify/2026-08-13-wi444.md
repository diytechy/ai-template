# Re-attestation brief — `Modified` spine rows

_Generated by `trace.py --ratify modified` (WI-316). One section per `Modified` SR (the attestation unit); each chain row shows only its CHANGED cells, before (the attested baseline revision) vs after (the working tree). The `Verified`→`Modified` Status flip itself is not listed — the marker is not the amendment. Baseline: `--since` when given, else the newest revision where the SR row still read `Verified` (correct under the amend+flip-same-commit rule the staged warn enforces — use `--since` for a pre-regime streak). Rule on each section: bless → `Verified`; evidence no longer verifies the amended text → `Planned` (process.md §7)._


## SR-006 — Gate/tier harness enforces required steps

_Baseline `ec5a8b983` (2026-08-13) — newest revision where SR-006 read `Verified`._

### SR SR-006
- **SN-Refs**
  - before: SN-004;SN-008;SN-014
  - after: SN-004;SN-008
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Requirement**
  - before: check.py shall run the active gate's required steps and fail the gate when a required tool is missing, reporting SKIP(missing) rather than silently passing.
  - after: check.py shall run the required steps of the gate that must next be passed (the strictness selector cached in docs/gate) and fail that gate when a required tool is missing, reporting SKIP(missing) rather than silently passing.
- **Rationale**
  - before: Realizes SN-004 (gates enforce their bar), SN-008 (no false green) and SN-014 (a missing tool fails, never skips). The work-branch lane skip is SR-133's obligation (one row, one obligation).
  - after: Realizes SN-004 (gates enforce their bar), SN-008 (no false green) and the dissolved edge expectation that a missing tool fails, never skips. The work-branch lane skip is SR-133's obligation (one row, one obligation).
- **AcceptanceCriteria**
  - before: check.py --gate GN runs that gate's steps; a required-but-absent tool fails the gate with guidance (never a silent pass), and --lenient degrades it to SKIP; --tier selects the declared step subset.
  - after: check.py --gate <bar> runs that bar's steps; a required-but-absent tool fails the gate with guidance (never a silent pass), and --lenient degrades it to SKIP; --tier selects the declared step subset.
- **area**
  - before: Gate harness
  - after: (empty)

### LLR LLR-006
- **Component**
  - before: CMP-001
  - after: CMP-007

### LLR LLR-014 — ADDED since baseline
- **LLR-ID**: LLR-014
- **SR-Refs**: SR-006
- **Title**: Budget comparator
- **Module**: project-trajectory/scripts/check_perf.py
- **CodeSymbol**: evaluate
- **Detail**: Compares measured metrics to declared budgets and fails beyond tolerance at the row's block/warn level.
- **TestRefs**: (see TC-014)
- **Status**: Verified
- **Component**: CMP-007
- **Phase**: 1

### LLR LLR-016 — ADDED since baseline
- **LLR-ID**: LLR-016
- **SR-Refs**: SR-006
- **Title**: Stub marker detector
- **Module**: project-trajectory/scripts/check_stubs.py
- **CodeSymbol**: find_stubs/scan_source
- **Detail**: Walks the AST for stub bodies (pass-only/NotImplemented/ellipsis public functions) at the declared gate. A detected stub fails the run; clean source passes.
- **TestRefs**: (see TC-016)
- **Status**: Modified
- **Component**: CMP-007
- **Phase**: 1

### LLR LLR-098 — ADDED since baseline
- **LLR-ID**: LLR-098
- **SR-Refs**: SR-006
- **Title**: Per-module coverage-floor comparator
- **Module**: project-trajectory/scripts/check_coverage.py
- **CodeSymbol**: load_floors/load_report/evaluate
- **Detail**: Reads the docs/coverage-floors census (module + min percent; blank and # lines ignored) and coverage.py's JSON report (per-file percent_covered, path-normalized), fails on any module below its floor or absent from the report, SKIPs when the report is absent, is a no-op with an empty census, and fails loudly on a malformed floor line. A failure names the offending module, and all floors holding exits 0 — the per-module floor is what keeps the global aggregate from hiding a thin high-risk module.
- **TestRefs**: (see TC-101)
- **Status**: Modified
- **Component**: CMP-007
- **Phase**: 1

### TC TC-014 — ADDED since baseline
- **TC-ID**: TC-014
- **Verifies**: SR-006;LLR-014
- **Level**: Integration
- **Method**: Run the check-perf suite; a within-tolerance metric passes and a regression fails.
- **Tier**: Full
- **Expected**: Satisfies the acceptance folded into LLR-014 (parent SR-006)
- **Automated**: Yes
- **Evidence**: tests/test_check_perf.py
- **Status**: Verified
- **Phase**: 1

### TC TC-016 — ADDED since baseline
- **TC-ID**: TC-016
- **Verifies**: SR-006;LLR-016
- **Level**: Integration
- **Method**: Run the check-stubs suite; a stub at the declared gate fails and clean source passes.
- **Tier**: Full
- **Expected**: Satisfies the acceptance folded into LLR-016 (parent SR-006)
- **Automated**: Yes
- **Evidence**: tests/test_check_stubs.py
- **Status**: Verified
- **Phase**: 1

### TC TC-101 — ADDED since baseline
- **TC-ID**: TC-101
- **Verifies**: SR-006;LLR-098
- **Level**: Integration
- **Method**: Run the check_coverage suite; a module below its floor exits 1 naming it, a declared module absent from the report exits 1, all floors holding exits 0, an absent report SKIPs, an empty census is a no-op, and a malformed floor line fails loudly.
- **Tier**: Full
- **Expected**: Satisfies the acceptance folded into LLR-098 (parent SR-006)
- **Automated**: Yes
- **Evidence**: tests/test_check_coverage.py
- **Status**: Verified
- **Phase**: 1

## SR-015 — Performance-budget back-links

_Baseline `fd26a9669` (2026-08-14) — newest revision where SR-015 read `Verified`._

### SR SR-015
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Requirement**
  - before: The performance-budgets registry (PB-###) shall keep every row's Refs resolvable to a real SR/LLR/Module.
  - after: The delivered performance-budgets registry (PB-###) shall keep every row's Refs resolvable to a real SR/LLR/Module.
- **Rationale**
  - before: Realizes SN-002 — the off-spine budget rows stay traceable to the spine.
  - after: Realizes SN-002 — the off-spine budget rows stay traceable to the spine. Deliberate pair (census F6): this row states the delivered data invariant; the checker that polices it is the harness's, decomposing under SR-157 — an on-purpose split, not an echo.
- **Aspect**
  - before: (empty)
  - after: perf
- **area**
  - before: Perf comparator + budgets
  - after: (empty)

## SR-017 — Always-on secrets floor

_Baseline `fd26a9669` (2026-08-14) — newest revision where SR-017 read `Verified`._

### SR SR-017
- **Boundary-Refs**
  - before: (empty)
  - after: B-01;B-04
- **Requirement**
  - before: check_privacy.py shall scan every commit's staged diff, message, and outgoing range for secret patterns regardless of the privacy toggle, unless docs/process.toml [policies] secrets_scan is explicitly false (the legacy docs/secrets-scan is still read through the migration window).
  - after: When a session attempts to commit or push content matching a secret pattern, the system shall refuse at the hook floor — scanning every commit's staged diff, message and outgoing range regardless of the privacy toggle, and naming the finding — unless the declared secrets_scan dial is explicitly false.
- **area**
  - before: Secrets + privacy lint
  - after: (empty)

## SR-018 — Privacy gate two-axis

_Baseline `fd26a9669` (2026-08-14) — newest revision where SR-018 read `Verified`._

### SR SR-018
- **Boundary-Refs**
  - before: (empty)
  - after: B-01;B-04
- **Requirement**
  - before: check_privacy.py shall add PII/identity classes when docs/process.toml [policies] privacy_check is on (the legacy docs/privacy-check is still read through the migration window), exempting configured author identities.
  - after: When the declared privacy_check dial is on, the system shall additionally refuse, at the same hook floor, a commit or push carrying a PII/identity class — exempting configured author identities.
- **area**
  - before: Secrets + privacy lint
  - after: (empty)

## SR-019 — Pre-commit hook floor

_Baseline `fd26a9669` (2026-08-14) — newest revision where SR-019 read `Verified`._

### SR SR-019
- **Boundary-Refs**
  - before: (empty)
  - after: B-01;B-04
- **Requirement**
  - before: The pre-commit hook shall run the integrity + secrets floor on every commit, agent-neutrally.
  - after: The pre-commit hook shall run the integrity + secrets floor on every commit, agent-neutrally — a LOCAL floor, bypassable by the session (git commit --no-verify), so it discharges "no unchecked write enters governed state" only as a PAIR with the hosted re-run of the same bar (SR-151/SR-152), never alone.
- **AcceptanceCriteria**
  - before: The hook runs trace --strict-integrity and the secrets scan on staged content and blocks a commit that fails either; and the same git+CI floor shares one definition of passing per moment — the reference CI workflow invokes the same harness entry point at the tier the declared moment-to-tier table (docs/stack.ini [ci-tiers]) assigns to each trigger, pinned by tests/test_ci_tier_declaration.py (OI-24, 2026-08-13).
  - after: The hook runs trace --strict-integrity and the secrets scan on staged content and blocks a commit that fails either; a --no-verify commit bypasses it and is caught instead by the hosted re-run, which is what makes the pair — not this row — the honest unit of the claim.
- **area**
  - before: Git hooks
  - after: (empty)

## SR-021 — Hook python probe

_Baseline `ec5a8b983` (2026-08-13) — newest revision where SR-021 read `Verified`._

### SR SR-021
- **SN-Refs**
  - before: SN-013
  - after: SN-001
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Requirement**
  - before: The git hooks shall locate a working python3 by running a candidate and skip-or-report clearly when none resolves or a Store-alias exits nonzero.
  - after: The git hooks and the root launchers (dev-setup / agent-resume, the coordinator's entry) shall locate a working python3 by running a candidate and skip-or-report clearly when none resolves or a Store-alias exits nonzero.
- **Rationale**
  - before: Realizes SN-013 — no cryptic crash when the interpreter is absent or a Windows Store alias.
  - after: Realizes the dissolved edge expectation that the interpreter is probed by running a candidate — no cryptic crash when the interpreter is absent or a Windows Store alias. (The launcher half joined the hooks' at the 2026-08-13 SN dissolution.)
- **AcceptanceCriteria**
  - before: With no working python3 the hook reports clearly and does not crash; with one, it runs the checks.
  - after: With no working python3 the hook and each root launcher report clearly and do not crash cryptically; with one, they run their action.
- **area**
  - before: Git hooks
  - after: (empty)

### LLR LLR-021
- **Component**
  - before: CMP-004
  - after: CMP-007

## SR-026 — Coordinator resumes headless

_Baseline `1e4d7f0b0` (2026-08-12) — newest revision where SR-026 read `Verified`._

### SR SR-026
- **SN-Refs**
  - before: SN-006;SN-016
  - after: SN-006
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Requirement**
  - before: agent_loop.py shall resume headless with stdin closed, never blocking on a prompt. Resume authority (one path - the serial resume loop is retired): a worker resumes from its explicit claimed assignment plus the committed trailer evidence on its branch, and the integrator (SR-132) derives claim/queue state from trunk history alone; docs/status.md is a generated reference surface (SR-059), never a session input.
  - after: agent_loop.py shall resume headless with stdin closed, never blocking on a prompt: a worker resumes from its explicit claimed assignment plus the committed trailer evidence on its branch, and the integrator derives claim and queue state from trunk history alone — the generated status surface never a session input.
- **Rationale**
  - before: Realizes SN-006 and SN-016 — an unattended run blocked on a prompt is indistinguishable from a hung one and holds its lane until a human notices. Keeping a separate serial resume path beside the dispatcher was rejected: two authorities over one decision drift, and the divergence surfaces as a run that resumes differently depending on how it was started.
  - after: Realizes SN-006 and the dissolved edge expectation that an unattended run never blocks on a prompt, at launch or mid-run — an unattended run blocked on a prompt is indistinguishable from a hung one and holds its lane until a human notices. Keeping a separate serial resume path beside the dispatcher was rejected: two authorities over one decision drift, and the divergence surfaces as a run that resumes differently depending on how it was started.
- **AcceptanceCriteria**
  - before: A worker reconciles from its assignment + committed evidence and the integrator from trunk history; every mode runs with stdin closed and a prompt cannot wedge it.
  - after: A worker reconciles from its assignment + committed evidence and the integrator from trunk history; every mode runs with stdin closed and a prompt cannot wedge it; a model rate limit backs off rather than failing the run, and a stall aborts to protect the budget.
- **area**
  - before: Unattended coordinator
  - after: (empty)

### LLR LLR-026
- **Component**
  - before: CMP-004
  - after: CMP-008

### LLR LLR-061 — ADDED since baseline
- **LLR-ID**: LLR-061
- **SR-Refs**: SR-026
- **Title**: Explicit WI/train/worktree worker assignment
- **Module**: project-trajectory/scripts/agent_loop.py
- **CodeSymbol**: build_worker_assignment/worker_prompt
- **Detail**: Replaces internal --track assumptions with explicit --wi/--train/worktree assignment; assembles the worker prompt from AGENTS.md + WI row + SpecRef + predecessor context + train diff + rework finding (never the generated status surface); collision-safe per-assignment logs/review evidence naming the exact reviewed commit; the --track lane machinery is retired outright. The assignment is the branch integrate.py's claim cut, and the session tag defaults to the branch name; the prompt never includes docs/status.md; a worker branch never edits root status, another branch's claims, the root log or generated artifacts; the result channel is committed trailer evidence (WI: / Blocked-WI: + BlockRef:).
- **TestRefs**: (see TC-061)
- **Status**: Modified
- **Component**: CMP-008
- **Phase**: 4

### LLR LLR-143
- **Component**
  - before: CMP-004
  - after: CMP-008

### TC TC-061 — ADDED since baseline
- **TC-ID**: TC-061
- **Verifies**: SR-026;LLR-061
- **Level**: Integration
- **Method**: Run two concurrent workers from explicit --wi/--train/worktree assignments; assert no lane-local status/next/run-state file, non-colliding logs/review evidence naming the exact reviewed commit, no root-truth mutation, and --track refused as an unknown flag (retired).
- **Tier**: Full
- **Expected**: Satisfies SR-026 AcceptanceCriteria via the LLR-061 detail contract
- **Automated**: Yes
- **Evidence**: tests/test_agent_loop_worker.py
- **Status**: Verified
- **Phase**: 4

## SR-027 — Coordinator preflight

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-027 read `Verified`._

### SR SR-027
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Rationale**
  - before: Realizes SN-006 and SN-015 — a run that starts on a broken footing fails later and further from the cause, and an unattended loop cannot be asked what went wrong. Continuing with a warning was rejected: each of these conditions makes every subsequent step meaningless, so a typed nonzero exit is what lets the caller act on it.
  - after: Realizes SN-006 and the dissolved edge expectation that a preflight on a non-git working directory reports and exits nonzero rather than hanging — a run that starts on a broken footing fails later and further from the cause, and an unattended loop cannot be asked what went wrong. Continuing with a warning was rejected: each of these conditions makes every subsequent step meaningless, so a typed nonzero exit is what lets the caller act on it.
- **area**
  - before: Unattended coordinator
  - after: (empty)

## SR-028 — Coordinator typed outcomes

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-028 read `Verified`._

### SR SR-028
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Rationale**
  - before: Realizes SN-006, SN-019 and SN-020 — a coordinator ending in an untyped code forces its caller to parse prose; a session that produced no commit still has a HEAD to report, so without a guard an empty run is indistinguishable from a productive one in the record it leaves; and an all-ERROR run reported as a work stall sends a human looking for a blocked work item that does not exist. Treating an agent error as a stall was rejected for that reason: an unreachable agent and a blocked item need different responses, so they need different outcomes.
  - after: Realizes SN-006 and the dissolved edge expectations that a repository with no commits yet (HEAD absent) does not crash the loop and that an all-ERROR run is an unavailable agent rather than a work stall — a coordinator ending in an untyped code forces its caller to parse prose; a session that produced no commit still has a HEAD to report, so without a guard an empty run is indistinguishable from a productive one in the record it leaves; and an all-ERROR run reported as a work stall sends a human looking for a blocked work item that does not exist. Treating an agent error as a stall was rejected for that reason: an unreachable agent and a blocked item need different responses, so they need different outcomes.
- **area**
  - before: Unattended coordinator
  - after: (empty)

## SR-030 — Second coordinator refused

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-030 read `Verified`._

### SR SR-030
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Rationale**
  - before: Realizes SN-018 and SN-006 — two coordinators in one checkout write the same working tree, and the loser's commits interleave into the winner's diff where no review will separate them. Coordinating by convention was rejected: the failure is silent and shows up only as a garbled history.
  - after: Realizes the dissolved edge expectation that a second coordinator launched in the same checkout is refused, and SN-006 — two coordinators in one checkout write the same working tree, and the loser's commits interleave into the winner's diff where no review will separate them. Coordinating by convention was rejected: the failure is silent and shows up only as a garbled history.
- **area**
  - before: Parallel tracks
  - after: (empty)

### LLR LLR-029 — ADDED since baseline
- **LLR-ID**: LLR-029
- **SR-Refs**: SR-030
- **Title**: Kernel advisory lock
- **Module**: project-trajectory/scripts/agent_common.py
- **CodeSymbol**: acquire_lock/_take_os_lock
- **Detail**: Takes an OS advisory lock the kernel releases on process death, so no stale-pid file wedges the next run. The lock scope is per-worktree — the dispatcher, each worker and an interactive sitting take the same lock for their checkout.
- **TestRefs**: (see TC-029)
- **Status**: Modified
- **Component**: CMP-008
- **Phase**: 1

### TC TC-029 — ADDED since baseline
- **TC-ID**: TC-029
- **Verifies**: SR-030;LLR-029
- **Level**: Integration
- **Method**: Run the agent-loop suite's lock tests (ported from the retired tracks suite): a hard-exited holder's kernel lock is OS-released and the next run acquires it cleanly.
- **Tier**: Full
- **Expected**: Satisfies SR-030 AcceptanceCriteria via the LLR-029 detail contract
- **Automated**: Yes
- **Evidence**: tests/test_agent_loop.py
- **Status**: Verified
- **Phase**: 1

## SR-031 — Declared-policy readers agree

_Baseline `fd26a9669` (2026-08-14) — newest revision where SR-031 read `Verified`._

### SR SR-031
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Requirement**
  - before: The policy readers shall share one parse so the hooks, agent_loop and check_privacy agree on every declared value. Since SN-028 that parse is docs/process.toml, read by two grammars — tomllib and the hooks' pure sh — whose agreement over the file's checked line shape is itself driven; the legacy one-word files stay readable through the migration window, and a dial declared in both homes is REFUSED rather than resolved by precedence.
  - after: Every delivered enforcer of a declared policy dial shall read the same value for that dial as every other enforcer — one parse, one answer, across the hooks, the loop and the checkers alike.
- **Rationale**
  - before: Realizes SN-004 (policies drive the gate) and SN-005 (one source, every enforcer reads it the same way).
  - after: Realizes SN-004 (policies drive the gate) and SN-005 (one source, every enforcer reads it the same way). Partition with SR-137 (census): the one-home surface and its checked shape are SR-137's obligations at B-01/B-02; this row is the package-wide coherence property those mechanics serve. The shared-parse mechanism and the legacy migration-window read decompose to this row's LLRs.
- **AcceptanceCriteria**
  - before: Every enforcer reads the same value for the same dial; the hooks' sh and tomllib return the same answer over the adversarial file-shape table (including a trailing-comment decoy, which once made the privacy gate fail OPEN); a legacy file present alongside its key is a refusal naming both.
  - after: Every enforcer reads the same value for the same dial; the two delivered grammars (tomllib and the hooks' sh) return the same answer over the adversarial file-shape table, including the trailing-comment decoy that once made the privacy gate fail OPEN.
- **area**
  - before: Declared-policy readers
  - after: (empty)

## SR-035 — No language-specific token in the shipped scheme

_Baseline `fd26a9669` (2026-08-14) — newest revision where SR-035 read `Verified`._

### SR SR-035
- **Title**
  - before: Stack-agnostic across OSes
  - after: No language-specific token in the shipped scheme
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Requirement**
  - before: The process and ID scheme shall be stack-agnostic.
  - after: The delivered process and ID scheme shall carry no language-specific token in the shipped registries and ID vocabulary, such that a non-Python adopter's scaffold passes trace.py unmodified.
- **AcceptanceCriteria**
  - before: The ID scheme is language-neutral.
  - after: A non-Python-profile scaffold passes trace.py --strict-integrity --strict-schema unmodified; a token scan of the shipped registry templates and the ID scheme finds no language-specific token.
- **Verification**
  - before: Analysis
  - after: Test
- **Aspect**
  - before: (empty)
  - after: portability
- **area**
  - before: Portability
  - after: (empty)

### LLR LLR-171 — ADDED since baseline
- **LLR-ID**: LLR-171
- **SR-Refs**: SR-035
- **Title**: Language-neutral registry templates + ID scheme
- **Module**: project-trajectory/registries
- **CodeSymbol**: registry templates / ID vocabulary
- **Detail**: The shipped registry templates and the ID scheme (SN/SR/LLR/TC and the off-spine spaces) carry no language-specific token: a scaffold produced with a non-Python profile passes trace.py --strict-integrity --strict-schema unmodified, and a token scan over the shipped templates and the ID vocabulary finds no language-specific token.
- **Rationale**: The observable minted for SR-035 by owner ruling (13u rider): stack-agnosticism was a real delivered property with no observable, and this is the check that makes it falsifiable.
- **TestRefs**: (see TC-165)
- **Status**: Draft
- **Component**: CMP-009
- **Phase**: 1

### TC TC-165 — ADDED since baseline
- **TC-ID**: TC-165
- **Verifies**: SR-035;LLR-171
- **Level**: Integration
- **Method**: Scaffold with a non-Python bootstrap profile into a temp dir and run trace.py --strict-integrity --strict-schema over it unmodified; scan the shipped registry templates and the ID scheme vocabulary for language-specific tokens and assert none.
- **Tier**: Full
- **Expected**: Satisfies SR-035 AcceptanceCriteria
- **Automated**: Yes
- **Evidence**: tests/test_bootstrap.py (to be extended at implementation)
- **Status**: Draft
- **Phase**: 1

## SR-040 — Per-phase routing and review dial

_Baseline `2f0ee1f5c` (2026-08-07) — newest revision where SR-040 read `Verified`._

### SR SR-040
- **SN-Refs**
  - before: SN-006;SN-016
  - after: SN-006
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Requirement**
  - before: The unattended coordinator shall select the agent command template per in-process session phase (PLAN/BUILD/REVIEW-A/REVIEW-B/DESIGN-CHECK/CRITIQUE - the coordinator's own activity; the retired docs/run-phase file, deleted by SR-059, is not an input) via AGENT_CMD_MAP/--cmd-map, falling back to the single AGENT_CMD; surface the declared reviewer dial (docs/review-policy, default 1) at run start without enforcing it; and warn (never block) when a lane resume surface exceeds the declared size threshold.
  - after: The delivered coordinator shall route each in-process session phase (PLAN/BUILD/REVIEW-A/REVIEW-B/DESIGN-CHECK/CRITIQUE) through its declared per-phase command template, falling back to the single declared command — surfacing the declared reviewer dial at run start without enforcing it, and warning (never blocking) when a lane resume surface exceeds the declared size threshold.
- **Rationale**
  - before: Realizes SN-006 and SN-016 — two samples of one model share blind spots, so a review by the family that built the change agrees with it for the wrong reasons; cross-family routing is what makes the second opinion independent, and one model for every phase was rejected on that basis. Dispatch stays keyed off the in-process session phase because the alternative, a per-phase code branch, makes adding a phase a code change and so discourages adding one. The resume surface's growth tripwire warns rather than fails because a growing resume surface is a smell and not a defect, and failing on a smell trains an operator to bypass the check.
  - after: Realizes SN-006 and the dissolved edge expectation that an unattended run never blocks on a prompt, at launch or mid-run — two samples of one model share blind spots, so a review by the family that built the change agrees with it for the wrong reasons; cross-family routing is what makes the second opinion independent, and one model for every phase was rejected on that basis. Dispatch stays keyed off the in-process session phase because the alternative, a per-phase code branch, makes adding a phase a code change and so discourages adding one. The resume surface's growth tripwire warns rather than fails because a growing resume surface is a smell and not a defect, and failing on a smell trains an operator to bypass the check.
- **AcceptanceCriteria**
  - before: A mapped session phase invokes its own command template and the default template does not fire; a broken map entry fails preflight before iteration 1; the banner surfaces docs/review-policy; an oversized status.md warns on stderr while the run proceeds; AGENT_STATUS_WARN_BYTES=0 silences.
  - after: A mapped session phase invokes its own command template and the default template does not fire; a broken map entry fails preflight before iteration 1; the banner surfaces the reviewer dial from docs/process.toml [policies] review_rounds; an oversized status.md warns on stderr while the run proceeds; AGENT_STATUS_WARN_BYTES=0 silences.
- **Aspect**
  - before: (empty)
  - after: unattended-loop
- **Area**
  - before: Unattended loop
  - after: (empty)

### LLR LLR-037
- **Component**
  - before: CMP-004
  - after: CMP-008

## SR-046 — Run capability menu

_Baseline `fd26a9669` (2026-08-14) — newest revision where SR-046 read `Verified`._

### SR SR-046
- **SN-Refs**
  - before: SN-001
  - after: SN-001;SN-035
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Requirement**
  - before: The root run.cmd/run.sh/run.command launchers shall present every major
capability an evaluator runs from a single declaration - one name = command line
(plus an optional name.desc line) per capability in docs/stack.ini's [run]
section - via a stdlib scripts/run_menu.py that reads it:
no args gives a numbered interactive menu;
run_menu.py <name> launches one directly with exit-code passthrough;
and --list emits a stable name<TAB>desc machine listing (the agent surface).
An absent or empty [run] section prints the no-launch-command-wired-yet guidance
and exits 1.
The launch command is declared once (the duplicated RUN_CMD is retired) and
works on Windows and POSIX.
  - after: The root run.cmd/run.sh/run.command launchers shall present every major
capability an evaluator runs from a single declaration - one name = command line
(plus an optional name.desc line) per capability in docs/stack.ini's [run]
section - via a stdlib scripts/run_menu.py that reads it:
no args gives a numbered interactive menu;
run_menu.py <name> launches one directly with exit-code passthrough;
and --list emits a stable name<TAB>desc machine listing (the agent surface).
An absent or empty [run] section prints the no-launch-command-wired-yet guidance
and exits 1.
The launch command is declared once and works on Windows and POSIX.
- **area**
  - before: Onboarding + dev-setup
  - after: (empty)

## SR-049 — Derived gate from artifact states

_Baseline `fd26a9669` (2026-08-14) — newest revision where SR-049 read `Verified`._

### SR SR-049
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Requirement**
  - before: derive_gate.py shall compute the gate that must next be passed from the spine artifact states (SN
section-state + SR/LLR/TC Status maturity) and cache it to docs/gate rather than
accept a hand-set value, so a gate advances only when the mechanical states do.
Three Status values are recognized case-insensitively:
(1) Draft (DevBar-Below + decomposition exemptions);
(2) Verified (DevBar-Release); and
(3) Modified — a post-attestation amendment owing a re-attest, deriving DevBar-Tests
through the existing decomposed-unverified rung with no arithmetic of its own.
The cached basis line carries modified=N beside drafts=N so the pending state
never hides.
  - after: The delivered harness shall derive the gate the project must next pass from the spine artifact states alone, caching it as a derived value — never accepting a hand-set one — with a post-attestation amendment dropping the derived gate exactly as a newly introduced draft does, and the pending-amendment count carried beside the draft count so the pending state never hides.
- **AcceptanceCriteria**
  - before: derive_gate.py --print on the meta computes the per-phase gates from the current states (a Modified SR's phase reads DevBar-Tests until re-attested); --check exits nonzero when a state change is not reflected in the cached docs/gate basis; the compared line includes the modified=N count.
  - after: derive_gate.py --print on the meta computes the per-phase gates from the current states (an amended row's phase reads the decomposed-unverified rung until re-attested); --check exits nonzero when a state change is not reflected in the cached docs/gate basis; the compared basis line includes the pending-amendment count beside the draft count.

## SR-052 — Dashboard accessibility (rubric-adjudicated)

_Baseline `fd26a9669` (2026-08-14) — newest revision where SR-052 read `Verified`._

### SR SR-052
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Requirement**
  - before: PROJECT_STATE.html shall be operable and readable without a pointer or full color perception: every interactive element (tabs, expandable blocks, SVG nodes with detail panels) is keyboard-reachable and carries an accessible name (title/aria/visible text); no information is encoded by color alone (status/phase/type encodings carry a text or shape cue); text keeps readable contrast against its fill.
  - after: The state view the delivered generators produce shall be operable and readable without a pointer or full color perception: every interactive element (tabs, expandable blocks, SVG nodes with detail panels) is keyboard-reachable and carries an accessible name (title/aria/visible text); no information is encoded by color alone (status/phase/type encodings carry a text or shape cue); text keeps readable contrast against its fill.
- **Aspect**
  - before: (empty)
  - after: trajectory
- **area**
  - before: Trajectory
  - after: (empty)

## SR-053 — Dashboard UI uniformity (rubric-adjudicated)

_Baseline `fd26a9669` (2026-08-14) — newest revision where SR-053 read `Verified`._

### SR SR-053
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Requirement**
  - before: PROJECT_STATE.html shall read as one system across its tabs and views: one type scale and spacing rhythm; one status/phase/type color vocabulary applied consistently wherever the same concept renders; uniform node, edge, legend, and detail-panel styling across the SVG emitters; the same interaction idiom (expand, hover, detail) wherever the same structure appears.
  - after: The state view the delivered generators produce shall read as one system across its tabs and views: one type scale and spacing rhythm; one status/phase/type color vocabulary applied consistently wherever the same concept renders; uniform node, edge, legend, and detail-panel styling across the SVG emitters; the same interaction idiom (expand, hover, detail) wherever the same structure appears.
- **Aspect**
  - before: (empty)
  - after: trajectory
- **area**
  - before: Trajectory
  - after: (empty)

## SR-054 — Dashboard usability (rubric-adjudicated)

_Baseline `fd26a9669` (2026-08-14) — newest revision where SR-054 read `Verified`._

### SR SR-054
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Requirement**
  - before: PROJECT_STATE.html shall support its core reading tasks with low friction: a first-time reviewer finds the project state, the next work, and how the parts connect, each within one tab switch; views default to a legible density (start-collapsed per the greater-than-3 rule) and reveal detail without losing context; labels stay readable at default zoom with no clipped or overlapping text.
  - after: The state view the delivered generators produce shall support its core reading tasks with low friction: a first-time reviewer finds the project state, the next work, and how the parts connect, each within one tab switch; views default to a legible density (start-collapsed per the greater-than-3 rule) and reveal detail without losing context; labels stay readable at default zoom with no clipped or overlapping text.
- **Aspect**
  - before: (empty)
  - after: trajectory
- **area**
  - before: Trajectory
  - after: (empty)

### LLR LLR-055
- **Detail**
  - before: Adjudicated against the usability rubric (docs/rubrics/dashboard-usability.md) by the SR-084/SR-085 critique loop: labels stay readable at default zoom with no clipped or overlapping text, and the reading experience the rubric's remaining anchors describe. Byte-deterministic; --check unchanged. This row keeps ONLY what a test cannot pin: the mechanized cores have left for child rows that name the tests owning them — T2 start-collapse (LLR-099/TC-102), T3 breadcrumb-restores-parent (LLR-100/TC-103, the whole anchor, no residue here) and T1 labelled entry (LLR-115/TC-120, the whole anchor, no residue here). Do NOT restate a child's claim here: an anchor lives in exactly one row.
  - after: Adjudicated against the usability rubric (docs/rubrics/dashboard-usability.md) by the LLR-048/LLR-082 critique loop: labels stay readable at default zoom with no clipped or overlapping text, and the reading experience the rubric's remaining anchors describe. Byte-deterministic; --check unchanged. This row keeps ONLY what a test cannot pin: the mechanized cores have left for child rows that name the tests owning them — T2 start-collapse (LLR-099/TC-102), T3 breadcrumb-restores-parent (LLR-100/TC-103, the whole anchor, no residue here) and T1 labelled entry (LLR-115/TC-120, the whole anchor, no residue here). Do NOT restate a child's claim here: an anchor lives in exactly one row.

### LLR LLR-099
- **Detail**
  - before: The mechanized core of usability anchor T2 (docs/rubrics/dashboard-usability.md): a view whose scale EARNS tiering — a bundle spanning more than 3 OKF types, a module set above the component threshold (the SR-089 '>3' rule) — renders START-COLLAPSED as the shared type-tiered drill, one block per type or container, rather than a flat wall of nodes; at or below the threshold it stays flat. Covers the Knowledge, How-SW and landing What views; the What icicle start-collapses to one block per SN with descend-on-click, reusing _drill_layer_svg/_render_drill so the interaction idiom is single-sourced. SCOPE: T2's remaining 'reads legibly at default density' judgement stays perceptual under LLR-055/TC-055.
  - after: The mechanized core of usability anchor T2 (docs/rubrics/dashboard-usability.md): a view whose scale EARNS tiering — a bundle spanning more than 3 OKF types, a module set above the component threshold (the LLR-052 '>3' rule) — renders START-COLLAPSED as the shared type-tiered drill, one block per type or container, rather than a flat wall of nodes; at or below the threshold it stays flat. Covers the Knowledge, How-SW and landing What views; the What icicle start-collapses to one block per SN with descend-on-click, reusing _drill_layer_svg/_render_drill so the interaction idiom is single-sourced. SCOPE: T2's remaining 'reads legibly at default density' judgement stays perceptual under LLR-055/TC-055.

### LLR LLR-119
- **Rationale**
  - before: A label is illegible AS a label the moment its ink leaves the box it names, which is the half of this anchor a reader cannot argue with. Column right-sizing (LLR-057, SR-056) cannot absorb it: right-sizing clamps at MAX_TIER_COL, so a 400-character need asks for a 2000px column and gets 172 — the overflow is structural, not a tuning problem. A FIXED character budget was the shipped approach on the next-work card and is rejected: it spends the same budget at every width, cutting a title mid-word on a wide screen with the card half empty. Native <details> is the point of the reveal — operable by pointer AND keyboard with no script, in a file opened from disk, which is what makes the affordance assertable from the markup instead of needing a browser. The height bound is measured over 320 registry rows: median 44, p90 126, max 609 characters of leading clause.
  - after: A label is illegible AS a label the moment its ink leaves the box it names, which is the half of this anchor a reader cannot argue with. Column right-sizing (LLR-057) cannot absorb it: right-sizing clamps at MAX_TIER_COL, so a 400-character need asks for a 2000px column and gets 172 — the overflow is structural, not a tuning problem. A FIXED character budget was the shipped approach on the next-work card and is rejected: it spends the same budget at every width, cutting a title mid-word on a wide screen with the card half empty. Native <details> is the point of the reveal — operable by pointer AND keyboard with no script, in a file opened from disk, which is what makes the affordance assertable from the markup instead of needing a browser. The height bound is measured over 320 registry rows: median 44, p90 126, max 609 characters of leading clause.

### TC TC-055
- **Method**
  - before: A fresh, family-heterogeneous CRITIQUE session (SR-084/SR-085 loop) adjudicates the generated PROJECT_STATE.html against docs/rubrics/dashboard-usability.md, receiving the rubric + SN/SR intent + the rendered PNG matrix from scripts/dashboard-shots/shoot.mjs (widths x themes x tabs) as the artifact recipe and never the implementer's self-assessment; VERDICT machine line cites numbered anchors.
  - after: A fresh, family-heterogeneous CRITIQUE session (LLR-048/LLR-082 loop) adjudicates the generated PROJECT_STATE.html against docs/rubrics/dashboard-usability.md, receiving the rubric + SN/SR intent + the rendered PNG matrix from scripts/dashboard-shots/shoot.mjs (widths x themes x tabs) as the artifact recipe and never the implementer's self-assessment; VERDICT machine line cites numbered anchors.

### TC TC-102
- **Method**
  - before: Generate the dashboard against two fixture registries and assert the SR-089 '>3' rule mechanically: a bundle spanning > 3 OKF types yields the start-collapsed type-tiered drill (know_view returns layers; the Knowledge section renders blocks, not a flat concept graph), a <= 3-type bundle yields None and the flat graph, and the How-SW view collapses above the component threshold on the same rule.
  - after: Generate the dashboard against two fixture registries and assert the LLR-052 '>3' rule mechanically: a bundle spanning > 3 OKF types yields the start-collapsed type-tiered drill (know_view returns layers; the Knowledge section renders blocks, not a flat concept graph), a <= 3-type bundle yields None and the flat graph, and the How-SW view collapses above the component threshold on the same rule.

## SR-059 — Retire next-wi; generated status surface

_Baseline `fd26a9669` (2026-08-14) — newest revision where SR-059 read `Verified`._

### SR SR-059
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Requirement**
  - before: The migration shall delete docs/next-wi and docs/run-phase outright (fresh
scaffold and migrated repo) and remove every live dependency on them.
docs/status.md's derived snapshot is GENERATED (gen_trajectory --status) and
freshness-gated, never hand-copied.
(The generated docs/run-state surface this row once paired with retired with the
dispatcher at Phase 5 - the stop banner and exit codes carry the outcome.)
  - after: The delivered scaffold shall ship no hand-curated next-work or run-phase pointer surface — the status surface a session reads being generated and freshness-gated, never hand-copied.
- **AcceptanceCriteria**
  - before: A fresh scaffold ships no next-wi/run-phase and is parallel-by-default; no live instruction or executable surface reads, writes, validates, generates or links next-wi; a build-out lane's phase resolves from its {phase}-{gate} branch with run-phase absent.
  - after: A fresh scaffold ships no next-wi/run-phase pointer and is parallel-by-default; no live instruction or executable surface reads, writes, validates, generates or links such a pointer; a build-out lane's phase resolves from its branch name with no pointer file; the status snapshot regenerates mechanically and its freshness is gated.
- **area**
  - before: Parallel dispatch
  - after: (empty)

### TC TC-060
- **Verifies**
  - before: SR-059;LLR-060;SR-116;LLR-124
  - after: SR-059;LLR-060;SR-156;LLR-124
- **Expected**
  - before: Satisfies SR-059 and SR-116 AcceptanceCriteria
  - after: Satisfies SR-059 AcceptanceCriteria, and SR-156 AcceptanceCriteria via the LLR-124 detail contract

## SR-070 — Offline project-state core

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-070 read `Verified`._

### SR SR-070
- **SN-Refs**
  - before: SN-010
  - after: SN-010;SN-023;SN-012
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Requirement**
  - before: gen_trajectory.py shall generate one self-contained root HTML file containing completeness tiles, the SN-to-SR-to-LLR-to-TC hierarchy, and the WI roadmap DAG.
  - after: The delivered generator set shall produce the adopted repository's self-contained, offline, deterministic and freshness-checkable project-state views from the tracked registries alone.
- **Rationale**
  - before: Realizes SN-010;SN-021 by isolating one independently reviewable obligation formerly carried by SR-038.
  - after: Realizes SN-010 and the dissolved edge expectation that a generated artifact drifting from its source fails its --check, by isolating one independently reviewable obligation formerly carried by SR-038.
- **AcceptanceCriteria**
  - before: The root file renders all three registry-derived views without external fetches and remains usable offline.
  - after: The root state view renders its registry-derived views (completeness tiles, the SN-to-SR-to-LLR-to-TC hierarchy, the WI roadmap DAG) without external fetches and remains usable offline; regeneration from unchanged registries is stable; each generated view carries a --check freshness contract.
- **Aspect**
  - before: (empty)
  - after: trajectory
- **area**
  - before: Trajectory
  - after: (empty)

### LLR LLR-023 — ADDED since baseline
- **LLR-ID**: LLR-023
- **SR-Refs**: SR-070
- **Title**: Arch-map splice + drift check
- **Module**: project-trajectory/scripts/gen_arch_map.py
- **CodeSymbol**: build_map/splice_region
- **Detail**: Regenerates the symbol map inside the BEGIN/END marker block and fails --check when the committed map has drifted. --check passes when the committed map is current; regeneration rewrites only the marker block, leaving surrounding text untouched. The architecture-map target surface is ruled to retire; the obligation moves with the flows/dashboard rendering.
- **TestRefs**: (see TC-023)
- **Status**: Modified
- **Component**: CMP-006
- **Phase**: 1

### LLR LLR-039 — ADDED since baseline
- **LLR-ID**: LLR-039
- **SR-Refs**: SR-070
- **Title**: OKF bundle emitter
- **Module**: project-trajectory/scripts/gen_okf.py
- **CodeSymbol**: emit/concept/_doc_title_and_summary/on_disk
- **Detail**: emit() builds {relpath: content} for every real row (sorted, deterministic, no timestamps): frontmatter via JSON-quoted YAML scalars, bodies carrying the row prose plus up/down markdown links from locally built reverse maps, per-tier and root indexes, the UPSTREAM.md spec pin; on_disk() snapshots docs/okf for the byte-compare; write mode writes-if-changed and prunes; small loaders duplicated per the F5 rule (no new sibling import). Layer B2 adds a process-guides tier: _doc_title_and_summary derives each guide title+summary from the doc first heading/paragraph (inline links stripped so nothing dangles), emitted only after the vacuity gate so a fresh scaffold stays vacuous. Graph links resolve within the bundle; each Process Guide carries a resource pointer to its source doc, which is left unmodified. --check trips on a registry or doc edit, a missing bundle file and a hand-added extra, recovering after regeneration; docs/process.toml [checks] okf_export = false silences the step, with the legacy docs/okf-export sentinel still read through the SN-028 migration window; off and placeholder-only registries pass silently, and process guides emit only alongside a real spine.
- **TestRefs**: (see TC-042)
- **Status**: Modified
- **Component**: CMP-009
- **Phase**: 1

### LLR LLR-051 — ADDED since baseline
- **LLR-ID**: LLR-051
- **SR-Refs**: SR-070
- **Title**: Process reference tab
- **Module**: project-trajectory/scripts/traj_panels.py;project-trajectory/scripts/gen_trajectory.py
- **CodeSymbol**: process_panel/build_html
- **Detail**: Renders the Process tab as a fully self-contained conditional panel registered in build_html (the Knowledge-tab pattern: nav button + section id=process): (1) the artifact-lifecycle x gates panel derived from the live registries + docs/gate (current-gate highlight; per-tier counts from the spine), (2) the resume-loop panel (the managed agent_loop flow with its escalation edges), (3) the slices -> phase -> gates panel derived from the WI registry (docs/work/); links out to the process masters wherever a canonical home exists (bounded in-view restatement only where no single doc states the relationship); sorted inputs, no clocks; a data-less repo renders byte-identically; --check unchanged (the trajectory-map step). Process-doc link-outs resolve; the tab is part of the adopted toolkit's generated project-state view (REL-002 output), not a system output.
- **TestRefs**: (see TC-051)
- **Status**: Modified
- **Component**: CMP-009
- **Phase**: 2

### LLR LLR-052 — ADDED since baseline
- **LLR-ID**: LLR-052
- **SR-Refs**: SR-070
- **Title**: Tiered When roadmap
- **Module**: project-trajectory/scripts/traj_views.py
- **CodeSymbol**: when_view/_wi_phases
- **Detail**: Builds phase/workstream/WI tiers by threshold, surfaces delivery Phase, and aggregates parent edges from deduplicated child edges. Tiers collapse only above the threshold; leaf phases stay visible; each parent edge equals the deduplicated union of crossing child edges.
- **TestRefs**: (see TC-052)
- **Status**: Modified
- **Component**: CMP-009
- **Phase**: 2

### LLR LLR-056 — ADDED since baseline
- **LLR-ID**: LLR-056
- **SR-Refs**: SR-070
- **Title**: Process tab intake + human-decision loop panels
- **Module**: project-trajectory/scripts/traj_panels.py
- **CodeSymbol**: process_panel/_station_panel
- **Detail**: Extends LLR-051's process_panel with the project's two circular working loops as linked flow panels: (A) the intake loop - owner/agent intake -> triage into work items with spec detail -> the scheduler-derived ready frontier in the WI registry (docs/work/) -> build/review -> merge; (B) the human-decision loop - open-items population (incl. the gate-ratification table) -> human review and ruling -> the log's Decisions record -> merge - with the LLM_Agent entry node rendered once and shared by both loops. Each stage links to its canonical home (status.md, the WI registry (docs/work/), open-items.html, log.md); data-derived where a canonical source exists; a data-less repo renders byte-identically; --check unchanged (the trajectory-map step). Every stage link resolves to its canonical doc; generation is byte-deterministic; the loop panels are part of the adopted toolkit's generated project-state view (REL-002 output), not a system output.
- **TestRefs**: (see TC-056)
- **Status**: Modified
- **Component**: CMP-009
- **Phase**: 3

### LLR LLR-057 — ADDED since baseline
- **LLR-ID**: LLR-057
- **SR-Refs**: SR-070
- **Title**: Decomposition render polish (columns, arrows, persistent hover)
- **Module**: project-trajectory/scripts/traj_views.py
- **CodeSymbol**: when_view/_tier_column/_svg_node
- **Detail**: Polishes the tiered decomposition render: each tier column is sized no wider than its content within a declared bound (a per-tier max-width cap MAX_TIER_COL, not the current uniform column); one horizontal parent->child arrow is drawn per containment edge so containment is explicit rather than implied by nesting alone; and the hover/focus highlight persists on the last-hovered item until another takes it (a stateful highlight keyed to the last-hovered node id, killing the flash-on-exit). Self-contained, byte-deterministic; --check unchanged. Composes with LLR-052's tiering without changing its >3 thresholds. A data-less repo renders byte-identically; the polished decomposition views are part of the adopted toolkit's generated project-state view (REL-002 output), not a system output.
- **TestRefs**: (see TC-057)
- **Status**: Modified
- **Component**: CMP-009
- **Phase**: 3

### LLR LLR-078 — ADDED since baseline
- **LLR-ID**: LLR-078
- **SR-Refs**: SR-070
- **Title**: Conditional architecture and knowledge views
- **Module**: project-trajectory/scripts/traj_parse.py;project-trajectory/scripts/traj_panels.py
- **CodeSymbol**: sw_modules/cmp_rows/know_graph/_okf_nodes
- **Detail**: Renders software/component and OKF views only when their source bundles exist; omits each otherwise. A source-less repo remains byte-stable.
- **TestRefs**: (see TC-078)
- **Status**: Modified
- **Component**: CMP-009
- **Phase**: 1

### LLR LLR-079 — ADDED since baseline
- **LLR-ID**: LLR-079
- **SR-Refs**: SR-070
- **Title**: Deterministic responsive dashboard freshness
- **Module**: project-trajectory/scripts/gen_trajectory.py;project-trajectory/scripts/traj_parse.py
- **CodeSymbol**: main/_asof/ASOF_RE
- **Detail**: Produces deterministic responsive HTML. Repeated generation is byte-identical and mobile CSS is present in the output.
- **TestRefs**: (see TC-079)
- **Status**: Modified
- **Component**: CMP-009
- **Phase**: 1

### LLR LLR-080 — ADDED since baseline
- **LLR-ID**: LLR-080
- **SR-Refs**: SR-070
- **Title**: Declared-interface graph rendering
- **Module**: project-trajectory/scripts/gen_trajectory.py;project-trajectory/scripts/gen_arch_map.py;project-trajectory/scripts/traj_views.py
- **CodeSymbol**: sw_graph/build_html;build_module_map
- **Detail**: Renders IF-labeled seams in dashboard and architecture views with deterministic table fallback when absent. Declared seams produce labeled graph edges in both views; with no seams the table fallback is preserved. The architecture-map target surface is ruled to retire; the obligation moves with the flows/dashboard rendering.
- **TestRefs**: (see TC-081)
- **Status**: Modified
- **Component**: CMP-009
- **Phase**: 1

### LLR LLR-085 — ADDED since baseline
- **LLR-ID**: LLR-085
- **SR-Refs**: SR-070
- **Title**: Containerized How-SW rendering
- **Module**: project-trajectory/scripts/traj_views.py
- **CodeSymbol**: sw_containment/sw_graph
- **Detail**: Renders nested component membership and boundary-aggregated seams, falling back byte-identically to the flat view without containment. Crossing edges are deduplicated at the container boundary; nested membership is preserved.
- **TestRefs**: (see TC-087)
- **Status**: Modified
- **Component**: CMP-009
- **Phase**: 1

### LLR LLR-086 — ADDED since baseline
- **LLR-ID**: LLR-086
- **SR-Refs**: SR-070
- **Title**: Tiered How-SW hierarchy
- **Module**: project-trajectory/scripts/traj_views.py
- **CodeSymbol**: sw_containment/_descend
- **Detail**: Tiers component containment to modules and bounds the visible top level by TOP_VIEW_MAX. Uncontained inventories retain the flat view.
- **TestRefs**: (see TC-088)
- **Status**: Modified
- **Component**: CMP-009
- **Phase**: 2

### LLR LLR-087 — ADDED since baseline
- **LLR-ID**: LLR-087
- **SR-Refs**: SR-070
- **Title**: Hierarchy seam ports
- **Module**: project-trajectory/scripts/gen_trajectory.py
- **CodeSymbol**: _drill_svg/_drill_edges
- **Detail**: Attaches IF seams to visible input/output ports and aggregates cross-container seams at boundaries. At every visible tier seams terminate at the correct ports, and boundary aggregation adds no duplicates.
- **TestRefs**: (see TC-089)
- **Status**: Modified
- **Component**: CMP-009
- **Phase**: 2

### LLR LLR-088 — ADDED since baseline
- **LLR-ID**: LLR-088
- **SR-Refs**: SR-070
- **Title**: One-layer hierarchy navigation
- **Module**: project-trajectory/scripts/gen_trajectory.py
- **CodeSymbol**: _descend/_breadcrumb
- **Detail**: Handles pointer and keyboard one-layer descent and breadcrumb restoration in self-contained views. Double-click and Enter/Space each descend a focused container exactly one layer; breadcrumb actions restore prior levels.
- **TestRefs**: (see TC-090)
- **Status**: Modified
- **Component**: CMP-009
- **Phase**: 2

### LLR LLR-130 — ADDED since baseline
- **LLR-ID**: LLR-130
- **SR-Refs**: SR-070
- **Title**: Dashboard staleness check
- **Module**: project-trajectory/scripts/gen_trajectory.py;project-trajectory/scripts/traj_parse.py
- **CodeSymbol**: main/_asof/ASOF_RE
- **Detail**: Implements stale/missing --check with a git as-of stamp excluded from stamp-only comparison. As-of behavior is stable both inside and outside Git, the visible git-derived stamp excluded from stamp-only comparisons.
- **Rationale**: Split from the determinism row: determinism is what makes the check possible, but a deterministic artifact nobody re-checks still goes stale.
- **TestRefs**: (see TC)
- **Status**: Modified
- **Component**: CMP-009
- **Phase**: 1

### LLR LLR-139 — ADDED since baseline
- **LLR-ID**: LLR-139
- **SR-Refs**: SR-070
- **Title**: Pause surfaced in the pending block
- **Module**: project-trajectory/scripts/traj_status.py
- **CodeSymbol**: _pause_pending/pending_block
- **Detail**: Renders zero-or-one 'Paused since <since> - <reason>' bullet in the committed-tree-pure pending region from docs/work/pause - declared since verbatim, no clock, the since clause omitted when empty, fail-closed text on malformation pinned equal to the reader's placeholder. The bullet clears when the pause file is deleted; the pending surface is part of the adopted toolkit's generated project-state view (REL-002 output), not a system output.
- **Rationale**: An open pause is a visible accruing cost, never a forgotten one.
- **TestRefs**: (see TC-136)
- **Status**: Modified
- **Component**: CMP-009
- **Phase**: 4

### TC TC-023 — ADDED since baseline
- **TC-ID**: TC-023
- **Verifies**: SR-070;LLR-023
- **Level**: Integration
- **Method**: Run the gen-arch-map suite; --check fails on a stale map and regeneration rewrites only the marker block.
- **Tier**: Full
- **Expected**: A stale map fails --check and a current map passes; regeneration rewrites only the marker block.
- **Automated**: Yes
- **Evidence**: tests/test_gen_arch_map.py
- **Status**: Verified
- **Phase**: 1

### TC TC-042 — ADDED since baseline
- **TC-ID**: TC-042
- **Verifies**: SR-070;LLR-039
- **Level**: Integration
- **Method**: Run the gen-okf suite; typed linked concepts generate, Process Guide concepts emit for present docs with derived summaries and unmodified sources (and none on a vacuous scaffold), regeneration is byte-stable, --check trips on registry/doc edits/extras and recovers after regen, extras are pruned, placeholder-only is vacuous, off silences, and the okf step is wired at DevBar-Release.
- **Tier**: Full
- **Expected**: Typed linked concepts with resolvable graph links and derived Process Guides emit with sources unmodified; regeneration is byte-stable; --check trips on registry/doc edits, missing files and extras and recovers after regen; extras are pruned; placeholder-only is vacuous; off silences.
- **Automated**: Yes
- **Evidence**: tests/test_gen_okf.py
- **Status**: Verified
- **Phase**: 1

### TC TC-051 — ADDED since baseline
- **TC-ID**: TC-051
- **Verifies**: SR-070;LLR-051
- **Level**: Integration
- **Method**: Run the dashboard suite; the Process tab renders its three panels from the live registries + docs/gate (the current-gate highlight matches docs/gate and the tier counts match the spine), process-doc link-outs resolve, a bundle-/data-less repo renders byte-identically (no Process data joins), and generation stays byte-deterministic with --check tripping stale output.
- **Tier**: Full
- **Parameters**: tests/test_gen_trajectory.py (the process-tab cases)
- **Expected**: Process tab present with data-derived joins correct; links resolve; data-less render byte-identical; deterministic; --check trips.
- **Automated**: Yes
- **Evidence**: tests/test_traj_panels.py::test_process_tab_renders_three_panels_from_live_data tests/test_traj_panels.py::test_process_current_gate_highlight_follows_docs_gate tests/test_traj_panels.py::test_process_link_outs_prefer_the_scaffolded_docs tests/test_traj_panels.py::test_process_wi_counts_join_work_items tests/test_traj_panels.py::test_process_tab_omitted_and_byte_identical_without_gate tests/test_traj_panels.py::test_process_tab_is_byte_deterministic_and_check_stable tests/test_traj_panels.py::test_meta_process_tab_smoke
- **Status**: Verified
- **Phase**: 2

### TC TC-052 — ADDED since baseline
- **TC-ID**: TC-052
- **Verifies**: SR-070;LLR-052
- **Level**: Integration
- **Method**: Run When-view phase/workstream thresholds, delivery-phase labels, and parent-edge aggregation.
- **Tier**: Full
- **Expected**: Tier thresholds, phase display, and deduplicated parent edges match the declared hierarchy.
- **Automated**: Yes
- **Evidence**: tests/test_traj_views.py::test_when_view_tiers_by_phase_above_threshold; tests/test_traj_views.py::test_when_view_parent_edge_is_deduped_union_of_child_edges; tests/test_traj_views.py::test_when_view_nests_workstream_tier_inside_a_phase; tests/test_traj_views.py::test_leaf_wi_block_surfaces_delivery_phase
- **Status**: Verified
- **Phase**: 2

### TC TC-056 — ADDED since baseline
- **TC-ID**: TC-056
- **Verifies**: SR-070;LLR-056;IF-093;IF-094
- **Level**: Integration
- **Method**: Run the dashboard suite; the Process tab renders both working loops (intake loop A + human-decision loop B) as two intersecting hoops drawn in one self-contained SVG, each a directed closed cycle wired by arrow-headed edges and meeting at the single shared LLM_Agent hub, every stage's link resolving to its canonical doc, the gate-ratification stage present in loop B, a data-less repo rendering byte-identically, and generation byte-deterministic with --check tripping stale output.
- **Tier**: Full
- **Parameters**: tests/test_gen_trajectory.py (the process-loop cases)
- **Expected**: Both loops are explicit closed cycles (data-cycle=closed on each hoop disc); the LLM_Agent hub renders exactly once as the shared central junction of both hoops; every loop edge is directional (one floparrow marker each), 6 for the 5-stage intake loop + 5 for the 4-stage decision loop = 11; loop A stages = [intake -> triage-to-WI -> scheduler-derived ready frontier -> build/review -> merge]; loop B stages = [open-items population incl. gate-ratification -> human review/ruling -> log Decisions -> merge], each card keyed to its loop (data-node a-*/b-*); all stage links resolve; data-less render byte-identical; deterministic; --check trips.
- **Automated**: Yes
- **Evidence**: tests/test_traj_panels.py::test_process_tab_renders_the_station_cycle tests/test_traj_panels.py::test_station_outcomes_derive_from_the_integrator tests/test_traj_panels.py::test_station_slot_is_the_serial_waist tests/test_traj_panels.py::test_station_refresh_pins_the_bar_attestation tests/test_traj_panels.py::test_station_barrier_and_admission_arms_pin_to_the_dispatcher tests/test_traj_panels.py::test_station_intake_arm_pins_to_the_intake_mint tests/test_traj_panels.py::test_station_links_resolve tests/test_traj_panels.py::test_station_byte_identical_without_data
- **Status**: Verified
- **Phase**: 3

### TC TC-057 — ADDED since baseline
- **TC-ID**: TC-057
- **Verifies**: SR-070;LLR-057
- **Level**: Integration
- **Method**: Run the dashboard suite; the tiered decomposition views render one horizontal parent->child arrow per containment edge, each tier column no wider than the declared bound, and a persistent last-hovered highlight contract keyed to the last-hovered node; byte-deterministic with --check tripping stale output.
- **Tier**: Full
- **Expected**: Exactly one parent->child arrow per containment edge; each tier column width <= the declared bound MAX_TIER_COL (a named constant = the widest member label + a fixed padding - a declared value, not an adjective); the persistent-highlight attribute is present and keyed to the last-hovered node id (no flash-on-exit); deterministic; --check trips.
- **Automated**: Yes
- **Evidence**: tests/test_traj_views.py::test_decomposition_one_arrow_per_containment_edge tests/test_traj_views.py::test_tier_column_honors_declared_width_bound tests/test_traj_views.py::test_persistent_hover_highlight_keyed_to_last_node
- **Status**: Verified
- **Phase**: 3

### TC TC-078 — ADDED since baseline
- **TC-ID**: TC-078
- **Verifies**: SR-070;LLR-078
- **Level**: Integration
- **Method**: Generate with and without module/component and OKF bundles.
- **Tier**: Full
- **Expected**: Each conditional view appears only with its source and source-less generation is byte-identical.
- **Automated**: Yes
- **Evidence**: tests/test_traj_views.py::test_how_sw_view_renders_from_the_module_map; tests/test_traj_views.py::test_no_cmp_renders_flat_view_byte_identical; tests/test_traj_panels.py::test_knowledge_tab_renders_from_bundle; tests/test_traj_panels.py::test_knowledge_tab_omitted_and_byte_identical_without_bundle
- **Status**: Verified
- **Phase**: 1

### TC TC-079 — ADDED since baseline
- **TC-ID**: TC-079
- **Verifies**: SR-070;LLR-079;LLR-130
- **Level**: Integration
- **Method**: Run deterministic, responsive, stale/missing, Git as-of, and no-Git generation cases.
- **Tier**: Full
- **Expected**: Repeated generation is byte-identical with mobile CSS present; stale or missing output fails --check, and as-of behavior is stable in and outside Git.
- **Automated**: Yes
- **Evidence**: tests/test_gen_trajectory.py::test_mobile_responsive_shell; tests/test_gen_trajectory.py::test_generation_is_deterministic; tests/test_gen_trajectory.py::test_check_passes_fresh_and_trips_stale; tests/test_gen_trajectory.py::test_check_missing_html_is_stale; tests/test_traj_parse.py::test_asof_stamp_from_git_and_excluded_from_check; tests/test_traj_parse.py::test_no_git_means_no_stamp_and_stays_deterministic
- **Status**: Verified
- **Phase**: 1

### TC TC-081 — ADDED since baseline
- **TC-ID**: TC-081
- **Verifies**: SR-070;LLR-080
- **Level**: Integration
- **Method**: Generate dashboard and architecture views with and without declared seams.
- **Tier**: Full
- **Expected**: Seams render as labeled edges in both views; absent seams retain fallback; output is deterministic.
- **Automated**: Yes
- **Evidence**: tests/test_traj_views.py::test_how_sw_graph_renders_seams; tests/test_traj_views.py::test_how_sw_stays_a_table_without_seams; tests/test_gen_arch_map.py::test_contracts_and_if_edges
- **Status**: Verified
- **Phase**: 1

### TC TC-087 — ADDED since baseline
- **TC-ID**: TC-087
- **Verifies**: SR-070;LLR-085
- **Level**: Integration
- **Method**: Generate nested/flat How-SW containment and boundary aggregation cases.
- **Tier**: Full
- **Expected**: Contained views nest and deduplicate crossing seams; no containment stays byte-identical and deterministic.
- **Automated**: Yes
- **Evidence**: tests/test_traj_views.py::test_how_sw_containerizes_when_components_contain_modules; tests/test_traj_views.py::test_boundary_aggregation_dedupes_cross_component_edges; tests/test_traj_views.py::test_no_cmp_renders_flat_view_byte_identical; tests/test_traj_views.py::test_nested_components_render_inside_their_parent
- **Status**: Verified
- **Phase**: 1

### TC TC-088 — ADDED since baseline
- **TC-ID**: TC-088
- **Verifies**: SR-070;LLR-086
- **Level**: Integration
- **Method**: Run component-to-module How-SW tier and top-width-bound cases.
- **Tier**: Full
- **Expected**: Containment creates bounded component tiers that descend to modules; uncontained input remains flat.
- **Automated**: Yes
- **Evidence**: tests/test_traj_views.py::test_how_sw_collapses_above_component_threshold; tests/test_traj_views.py::test_tier_column_honors_declared_width_bound; tests/test_traj_views.py::test_no_cmp_renders_flat_view_byte_identical
- **Status**: Verified
- **Phase**: 2

### TC TC-089 — ADDED since baseline
- **TC-ID**: TC-089
- **Verifies**: SR-070;LLR-087
- **Level**: Integration
- **Method**: Render seams at hierarchy ports and crossing container boundaries.
- **Tier**: Full
- **Expected**: Visible endpoints use input/output ports and crossing seams aggregate without duplicates.
- **Automated**: Yes
- **Evidence**: tests/test_traj_views.py::test_when_view_seams_wire_to_block_ports; tests/test_traj_views.py::test_boundary_aggregation_dedupes_cross_component_edges
- **Status**: Verified
- **Phase**: 2

### TC TC-090 — ADDED since baseline
- **TC-ID**: TC-090
- **Verifies**: SR-070;LLR-088
- **Level**: Integration
- **Method**: Exercise pointer/keyboard descent and breadcrumb restoration.
- **Tier**: Full
- **Expected**: Each input descends one layer and breadcrumb restores the parent in the self-contained render.
- **Automated**: Yes
- **Evidence**: tests/test_traj_views.py::test_when_view_double_click_descends_a_layer; tests/test_traj_views.py::test_when_view_breadcrumb_restores_parent; tests/test_traj_render.py::test_a1_drill_leaf_blocks_are_keyboard_focusable
- **Status**: Verified
- **Phase**: 2

### TC TC-136 — ADDED since baseline
- **TC-ID**: TC-136
- **Verifies**: SR-070;LLR-139
- **Level**: Unit
- **Method**: Run the pending-surface pause suite: the block renders since/reason verbatim (no clock), omits the stamp when since is empty, matches the reader's fail-closed placeholder on malformation, clears on deletion, and is deterministic.
- **Tier**: Full
- **Expected**: The pending block renders 'Paused since <date> - <reason>' with the declared date verbatim (no clock), omits the stamp when since is empty, renders the pinned fail-closed placeholder on malformation, and clears when the pause file is deleted.
- **Automated**: Yes
- **Evidence**: tests/test_gen_trajectory_pending.py::test_tracked_pause_projects_reason_and_declared_since; tests/test_gen_trajectory_pending.py::test_pause_malformed_text_matches_the_coordinator_reader
- **Status**: Verified
- **Phase**: 4

## SR-112 — Checked per-agent skill fan-out

_Baseline `08c985cbc` (2026-08-13) — newest revision where SR-112 read `Verified`._

### SR SR-112
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Rationale**
  - before: Realizes SN-021 — one skill maintained in three agent-specific copies rots into three different skills, and the copy an agent actually loads is the one nobody edited. Maintaining a single shared directory was rejected because each agent harness looks in its own path; the fan-out is forced, so the copies are generated and drift is a finding.
  - after: Realizes the dissolved edge expectation that a generated artifact drifting from its source fails its --check — one skill maintained in three agent-specific copies rots into three different skills, and the copy an agent actually loads is the one nobody edited. Maintaining a single shared directory was rejected because each agent harness looks in its own path; the fan-out is forced, so the copies are generated and drift is a finding.
- **area**
  - before: Skills index + fan-out
  - after: (empty)

### LLR LLR-025 — ADDED since baseline
- **LLR-ID**: LLR-025
- **SR-Refs**: SR-112
- **Title**: Skills index generator
- **Module**: project-trajectory/scripts/gen_skills_index.py
- **CodeSymbol**: main
- **Detail**: Reads each SKILL.md applicability frontmatter and regenerates skills/INDEX.csv. The regenerated INDEX.csv reflects the current SKILL.md frontmatter, and a drifted index is detectable.
- **TestRefs**: (see TC-025)
- **Status**: Modified
- **Component**: CMP-009
- **Phase**: 1

### TC TC-025 — ADDED since baseline
- **TC-ID**: TC-025
- **Verifies**: SR-112;LLR-025
- **Level**: Integration
- **Method**: Run the skills-index suite; INDEX.csv regenerates from the SKILL.md frontmatter.
- **Tier**: Full
- **Expected**: The regenerated INDEX.csv matches the current SKILL.md frontmatter and a drifted index is detected.
- **Automated**: Yes
- **Evidence**: tests/test_skills_index.py
- **Status**: Verified
- **Phase**: 1

## SR-114 — Kit scripts run across the supported OSes

_Baseline `fd26a9669` (2026-08-14) — newest revision where SR-114 read `Verified`._

### SR SR-114
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Rationale**
  - before: Realizes SN-011 — a script that works only where its author ran it turns portability into a defect the adopter finds, and the path, line-ending and locking differences that break it are invisible on a single platform. Testing one platform and reading the others by inspection was rejected: every one of those differences has been a real failure here.
  - after: Realizes SN-011 — a script that works only where its author ran it turns portability into a defect the adopter finds, and the path, line-ending and locking differences that break it are invisible on a single platform. Testing one platform and reading the others by inspection was rejected: every one of those differences has been a real failure here. The macOS exclusion argument: the 3.11 floor is a platform-agnostic language/stdlib guarantee already exercised on Linux and Windows, while macOS's job is OS-specific behavior (fcntl.flock, paths, git worktrees) that does not vary by Python version — a redundant-coverage call, not a runner-availability workaround (3.11 has arm64 macOS builds).
- **AcceptanceCriteria**
  - before: The CI matrix spans Linux, Windows and macOS on Python 3.11 and latest (3.x) and is green; macOS runs on current Python only — the 3.11 floor is a platform-agnostic language/stdlib guarantee already exercised on Linux and Windows, while macOS's job is OS-specific behavior (fcntl.flock, paths, git worktrees) that does not vary by Python version, so the macOS+3.11 cell is excluded as a redundant-coverage call, not a runner-availability workaround (3.11 has arm64 macOS builds).
  - after: The CI matrix spans Linux, Windows and macOS on Python 3.11 and latest (3.x) and is green; the macOS+3.11 cell is excluded as a redundant-coverage call recorded in the rationale; the hosted matrix as an evidence surface is SR-152's contract.
- **Aspect**
  - before: (empty)
  - after: portability
- **area**
  - before: Portability
  - after: (empty)

## SR-129 — Registry representation migration

_Baseline `fd26a9669` (2026-08-14) — newest revision where SR-129 read `Verified`._

### SR SR-129
- **Boundary-Refs**
  - before: (empty)
  - after: B-05
- **Requirement**
  - before: The kit shall provide a mechanical converter between the work-item registry's home - the docs/work/ spec folder (status = directory, TOML frontmatter, Deliverable in the body) - and the retired docs/requirements/work-items.csv form, refusing any status outside the known vocabulary, any materialization into a non-empty target, and any conversion while an in-flight active/<branch>/ claim exists (conversion is a drained-stop operation), with the round-trip proven over the live registry before the authority flip.
  - after: The kit shall provide a mechanical converter between the work-item registry's home - the docs/work/ spec folder (status = directory, TOML frontmatter, Deliverable in the body) - and the retired docs/requirements/work-items.csv form, refusing any status outside the known vocabulary, any materialization into a non-empty target, and any conversion while an in-flight active/<branch>/ claim exists (conversion is a drained-stop operation), with the round-trip provable over the live registry.
- **Aspect**
  - before: (empty)
  - after: trajectory
- **area**
  - before: Trajectory
  - after: (empty)
