# Re-attestation brief — `Modified` spine rows

_Generated by `trace.py --ratify modified` (WI-316). One section per `Modified` SR (the attestation unit); each chain row shows only its CHANGED cells, before (the attested baseline revision) vs after (the working tree). The `Verified`→`Modified` Status flip itself is not listed — the marker is not the amendment. Baseline: `--since` when given, else the newest revision where the SR row still read `Verified` (correct under the amend+flip-same-commit rule the staged warn enforces — use `--since` for a pre-regime streak). Rule on each section: bless → `Verified`; evidence no longer verifies the amended text → `Planned` (process.md §7)._


## SR-129 — Registry representation migration

_Baseline `e1cf5743e` (2026-07-29) — newest revision where SR-129 read `Verified`._

### SR SR-129
- **Requirement**
  - before: The kit shall provide a mechanical converter between the work-item registry's two homes - the docs/work/ spec folder (status = directory, TOML frontmatter, Deliverable in the body) and the legacy docs/requirements/work-items.csv - refusing any status outside the known vocabulary and any materialization into a non-empty target, with the round-trip proven over the live registry before an authority flip.
  - after: The kit shall provide a mechanical converter between the work-item registry's two homes - the docs/work/ spec folder (status = directory, TOML frontmatter, Deliverable in the body) and the legacy docs/requirements/work-items.csv - refusing any status outside the known vocabulary, any materialization into a non-empty target, and any conversion while an in-flight active/<branch>/ claim exists (conversion is a drained-stop operation), with the round-trip proven over the live registry before an authority flip.
- **AcceptanceCriteria**
  - before: wi_convert --verify over a populated registry exits 0 reporting cell-exact equality; an unknown status refuses naming the row; folder->CSV->folder is parse-equal including a hand-filed spec with no order key.
  - after: wi_convert --verify over a populated registry exits 0 reporting cell-exact equality; an unknown status refuses naming the row; folder->CSV->folder is parse-equal including a hand-filed spec with no order key; an in-flight claim refuses conversion by name.

### LLR LLR-136
- **Detail**
  - before: Converts between the registry homes per the Phase 2a grammar (TOML +++ frontmatter, Deliverable in the body, status from the directory, order key only as a CSV-order migration artifact). --verify round-trips CSV->specs->CSV in a temp dir, exits nonzero on any cell mismatch, and reports byte identity with a reason. write_spec_file is the single spec writer (to_specs and plan_artifacts filing both go through it). Refuses unknown statuses by row and non-empty targets without --force.
  - after: Converts between the registry homes per the Phase 2a grammar (TOML +++ frontmatter, Deliverable in the body, status from the directory, order key only as a CSV-order migration artifact). --verify round-trips CSV->specs->CSV in a temp dir, exits nonzero on any cell mismatch, and reports byte identity with a reason. write_spec_file is the single spec writer (to_specs and plan_artifacts filing both go through it). Refuses unknown statuses by row, non-empty targets without --force, and any in-flight active/<branch>/ claim by name (status is the FIRST path component, the loaders' rule; conversion is a drained-stop operation per §3.2 - the Phase 4 claim protocol made the state reachable).
