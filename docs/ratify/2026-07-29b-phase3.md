# Re-attestation brief — `Modified` spine rows

_Generated by `trace.py --ratify modified` (WI-316). One section per `Modified` SR (the attestation unit); each chain row shows only its CHANGED cells, before (the attested baseline revision) vs after (the working tree). The `Verified`→`Modified` Status flip itself is not listed — the marker is not the amendment. Baseline: `--since` when given, else the newest revision where the SR row still read `Verified` (correct under the amend+flip-same-commit rule the staged warn enforces — use `--since` for a pre-regime streak). Rule on each section: bless → `Verified`; evidence no longer verifies the amended text → `Planned` (process.md §7)._


## SR-006 — Gate/tier harness enforces required steps

_Baseline `cb5102783` (2026-07-29) — newest revision where SR-006 read `Verified`._

### SR SR-006
- **Requirement**
  - before: check.py shall run the active gate's required steps and fail the gate when a required tool is missing, reporting SKIP(missing) rather than silently passing.
  - after: check.py shall run the active gate's required steps and fail the gate when a required tool is missing, reporting SKIP(missing) rather than silently passing; on a claimed work branch (one holding a docs/work/active/<branch>/ claim) the generated-artifact freshness steps shall SKIP with a stated trunk-lane reason - generated artifacts are trunk-only - while every non-freshness step still runs.
- **Rationale**
  - before: Realizes SN-004 (gates enforce their bar), SN-008 (no false green) and SN-014 (a missing tool fails, never skips).
  - after: Realizes SN-004 (gates enforce their bar), SN-008 (no false green) and SN-014 (a missing tool fails, never skips). The work-branch skip is not a weakening: a work branch cannot commit generated artifacts at all (concurrency-restructure §5.2), so demanding their freshness there would force the conflict the rule exists to delete; the skip is reported, never silent.
- **AcceptanceCriteria**
  - before: check.py --gate GN runs that gate's steps; a required-but-absent tool fails the gate with guidance (never a silent pass), and --lenient degrades it to SKIP; --tier selects the declared step subset.
  - after: check.py --gate GN runs that gate's steps; a required-but-absent tool fails the gate with guidance (never a silent pass), and --lenient degrades it to SKIP; --tier selects the declared step subset; on a claimed work branch the declared freshness steps SKIP with a §5.2 notice and exit code untouched while skills-sync, trajectory and registry-integrity still run.

### LLR LLR-006
- **CodeSymbol**
  - before: steps/run_step
  - after: steps/run_step/_work_branch
- **Detail**
  - before: Builds the required-step plan per gate/tier and reports SKIP(missing) with a nonzero exit when a required tool is absent.
  - after: Builds the required-step plan per gate/tier and reports SKIP(missing) with a nonzero exit when a required tool is absent. On a claimed work branch (_work_branch: current git branch with a docs/work/active/<branch>/ directory; memoized, fail-closed to full checks off-git or on '..' names) the _TRUNK_FRESHNESS_STEPS set SKIPs with a §5.2 notice, exit code untouched; the plan listing still names every step.
