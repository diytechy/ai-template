> **ARCHIVE** — design history as of 2026-08-13; not current guidance.

| Plan-WI | Title | Covers | Interfaces | Predecessors |
|---|---|---|---|---|
| P1 | Dual-plan round state machine: a pure, side-effect-free library modeling the round lifecycle (plan → coverage → cross-critique → revision → coverage → arbiter ×2 position-swapped → verdict) with typed step outcomes, the hard caps (one cross-critique round, one revision each, one coverage bounce), verdict-disagreement/cap-exhaustion → page dispositions, and a declared round-budget ledger whose exhaustion maps to the `docs/gate-policy` failure semantics. Unit-tested against injected fake step results. | C5;C7 | Proposed: a new pure round-lifecycle library provided to `scripts/agent_loop`; nearest is IF-053 (right contract shape — a side-effect-free library the dispatcher consumes, never touching git) but wrong provider module and wrong contract (frontier/classification, not round lifecycle) | |
| P2 | Redacted brief assembler + hat prompt-map keys: assembles planner/critic/arbiter prompts from an allowlist-only input set (goal brief, SR surface, IF registry, and a coverage-report slot for the critic/arbiter briefs) so `status.md`/`log.md`/self-assessments are unreachable by construction, and registers the three hat keys on the existing `--prompt-map`/`AGENT_PROMPT_MAP` override. Tested by asserting assembled briefs contain only allowlisted material. | C2 | IF-041; Proposed: `scripts/agent_loop` consumes `docs/requirements/interfaces.csv` + `system-requirements.csv` for brief assembly; nearest IF-057 consumes the same registries but its ThisProject is `scripts/plan_coverage`, not `agent_loop` | |
| P3 | Two-family planner routing: an `agent_route` planner-pair selection that returns two sessions from different Family lines where the enabled pool allows, and otherwise applies the degraded-availability rule (two fresh same-family sessions) emitting a recorded reason for the round artifacts/telemetry. Tested against `docs/agents.csv` fixtures for both branches. | C3 | IF-044; IF-045 | |
| P4 | Coverage step adapter: invokes `plan_coverage.py` headless after generation and after revision, captures the per-plan + pairwise report as the payload P2's critic/arbiter briefs embed, and maps the exit status to P1's typed dispositions — findings exit (1) yields bounce-to-author-for-mechanical-repair once, then page. Tested with fixture plans exercising clean, bounce-then-clean, and bounce-then-page paths. | C4 | Proposed: `scripts/agent_loop` invokes the `scripts/plan_coverage` CLI and consumes its report/exit contract; nearest IF-057 is plan_coverage's own consume of interfaces.csv — no declared seam has agent_loop as plan_coverage's consumer, and IF-041's counterpart is the external agent CLI, not a kit script | P1 |
| P5 | Round artifact filer: allocates `docs/plans/DP-NNN-<slug>/` and writes each stage artifact (briefs, plans, revisions, critiques, coverage reports, verdict) as tracked files, appends the verdict summary to `docs/log.md`, and files the selected plan's rows as queued WIs in `work-items.csv` such that `check_trajectory.py` passes on the result. Tested on a fixture repo end-to-end through the validator. | C6 | Proposed: the coordinator write-side of `work-items.csv`/`docs/log.md`/`docs/plans/`; nearest IF-054 and IF-023 are read-side consumers of `work-items.csv` (wrong direction) — this WI's outputs must satisfy those readers' contracts but the write seam itself is undeclared | |
| P6 | Coordinator wiring: `agent_loop` recognizes a queued WI declaring the dual-plan trigger (read through the schedule frontier) and enters a dual-plan round instead of a direct BUILD, driving P1's machine with sessions launched through the existing headless invocation path — so each session inherits the S8 per-session limits and telemetry — routed per P3, prompted per P2, coverage-checked per P4, filed per P5, and ending in the typed coordinator outcomes (including the page-the-human exits). Done when a fixture repo with a triggered WI runs the full round unattended. | C1;C7 | IF-015; IF-055; IF-041 | P1;P2;P3;P4;P5 |

## Notes

- **No clause excluded.** All of C1–C7 are covered above.
- **Declared C7 split:** P1 owns the round-total-budget semantics (a cap in the state machine, exhaustion following `docs/gate-policy`); P6 owns per-session limit/telemetry inheritance, which it gets structurally by launching every hat session through the existing IF-041 session path rather than a new invocation route. Neither half is deliverable by the other row.
- **C4 boundary:** P4 is the sole owner of C4. P2's brief templates carry a coverage-report slot as part of their input allowlist (a C2 construction concern); producing and routing the report into that slot, and the bounce/page disposition, are P4's deliverable. This is a template-parameter relationship, not an artifact dependency, so no P4↔P2 edge exists.
- **Assumption — trigger declaration:** the dual-plan trigger rides an existing WI-registry field surfaced through the schedule library (IF-054/IF-055 read path), consistent with SR-058's fail-closed classification (a dual-plan WI never packs into a multi-WI traincar). If a new registry column proves necessary, its `check_trajectory` tolerance is inside P6's scope, not a separate WI.
- **Assumption — paging mechanics exist:** `agent_loop` already exposes typed NEEDS-HUMAN-style outcomes (IF-015); P1/P4/P6 map round failures onto that existing surface per `docs/gate-policy` rather than building a new paging channel.
- **Assumption — WI-190 deliverables are stable:** `plan_coverage.py` (SR-044, IF-057) and the `--prompt-map`/`AGENT_PROMPT_MAP` override exist as shipped; this plan extends, never re-implements, them.
- **Edge honesty:** P4→P1 exists because the adapter emits P1's typed step-outcome constants (a consumed artifact). P2, P3, and P5 are deliberately edge-free — each is testable standalone — leaving P6 as the single integration fan-in.
