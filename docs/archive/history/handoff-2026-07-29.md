# Handoff — 2026-07-29, after Phase 5 (the restructure is executed)

**Read [status.md](../../status.md) first — it is the working surface; this file is
the wrap-up compilation of what remains, each item pointing at its home rather
than restating it.** The concurrency restructure
([concurrency-restructure.md](../../concurrency-restructure.md)) is executed through
all five phases (§7 records); the train/dispatcher machinery, the CSV registry
home, and their process prose are gone. Eleven Phase 5 commits,
`31ad569..739de32`; exit bar green (full suite 1634/7/0; `check.py` PASS at
the derived gate; `--strict` trajectory clean; census reconciled).

Sequencing rule, stated once: **item 1 blocks nothing but gates everything
else's G3 claim** — the queue is workable under `gate-policy: autonomous`, but
close the window first if a sitting is available (the standing
close-windows-fast lesson).

---

## 1. The owner sitting — EXECUTED (2026-07-29, same day)

**Closed:** the owner attested the Phase 5 amendment in-session (ruling in
[log.md](../../log.md)'s Decisions); all 26 `Modified` SRs + TC-133 blessed per
[ratify/2026-07-29d-phase5.md](../../ratify/2026-07-29d-phase5.md), gate re-derived
**G3 all four phases**. The account below is kept as the record of what the
sitting covered; nothing in it is an open task.

- 15 SRs superseded → SR-132 (`Superseded:` titles, the TC-099 pattern) —
  blessing them attests the *supersession record*, evidence being the Phase 5
  deletion commits + the green suite.
- 11 SRs re-grounded on the seam model — blessing attests the amended text
  against the surviving code (each section's after-cells name it).
- TC-133 `Draft` → ratify (its pinning test
  `test_phase5_supersession_rows_preserve_the_ratified_evidence_map` already
  runs green).

After the flips, `derive_gate.py` re-derives G3 and the ordinary
`gate-advance` skill flow applies (a reviewed Status-change commit + the log's
Decisions entry).

## 2. The buildable queue — EXECUTED (2026-07-29, the grind session)

**Closed:** every item below (plus WI-364/WI-365, filed and closed the same
day) was built, adversarially reviewed where risky, and closed in the
2026-07-29 grind session — records in the registry rows and the log's
session entry. The sequence below is kept as the account of what was queued;
nothing in it is an open task.

1. **WI-357** (P1) + **WI-359** — the two integrator-lifecycle defects from
   the Phase 4 acceptance (close-un-claims-lane; incomplete unload/worktree
   GC). Both rows say *design against the post-deletion loaders* — that
   precondition is now met, which is why they lead. WI-357's standing
   workaround (leave the emptied claim dir on disk) is in its row.
2. **WI-358** — R-D reds at merge when trunk status.md prose names an
   in-queue id; small, unblocks clean queue runs.
3. **WI-360** — the unpinned gate-scoped lookup assertion (REVIEW-A minor).
4. **WI-363** (quick) — gen_arch_map's dot-directory scan bug; downstream-
   facing, small, test shape stated in the row.
5. **WI-361** — re-home or retire the WI-286 venv-floor guarantee. A DESIGN
   decision, not a port: the row states both candidate seams and why the
   silent port was refused.
6. **WI-362** — the staleness clock is blind to a Title rename; measure
   before fixing (the row names the flag-test precedent).
7. **WI-331** — re-tune agent-facing guidance for the standard model,
   narrowed to findings 1+3 per the audit keep-ruling (finding 2 ruled out as
   self-generated complexity).
8. **WI-323 + WI-324 as ONE render batch** — the audit's keep-ruling: one
   critique dispatch covers both (WI-346 already closed at Phase 4). WI-323
   is attended work — read `LLR-120`'s Detail first and do NOT mechanize the
   T8 clause into a crossing-count proxy; WI-324 amends three Verified rows,
   so it lands AT a sitting (`sr-provenance-advisories` names the cells on
   every run).

## 3. Owner-only rulings still pending (do not grind these)

- **The §3 deferred-row retirements — RULED and EXECUTED 2026-07-29:**
  WI-271/108/061/063/158/187 retired per the
  [handoff-2026-07-28c.md](handoff-2026-07-28c.md) §3 dispositions (reasons
  in their archived rows; WI-271's un-defer trigger moved into
  `staged_findings`'s docstring). WI-280 stays deferred by its own
  keep-ruling, re-specified 2026-07-29 onto the post-deletion tree; WI-277
  stays hard-gated behind it.
- **The audit's §4 line 6** ("delete check_trajectory's never-fired rules,
  ~700 lines") was NOT executed — it was never in the Phase 5 compiled
  scope, and [concurrency-restructure.md](../../concurrency-restructure.md) §6's
  fate-table line about it is recorded as honest residue in the §7 Phase 5
  entry. It needs an explicit ruling before anyone deletes shipped checks.
- **Push → hosted CI → merge-to-main** (`push-policy: human`, unchanged):
  `dualplan-routing-fix` and `guardrails-fable-method` were already awaiting
  this; `ConcurrencyTrainRewrite` now joins the list. The 2026-07-27
  sitting's own finding applies: the shorter the window, the less it can
  hide — hosted CI has never run the post-restructure tree.

## 4. Standing rules that survive the restructure (homes, not restatements)

- Claims via `integrate.py claim`; merges via its fail-closed queue; pause =
  a tracked `docs/work/pause`; work-branch session records are `docs/log.d/`
  fragments ([process-options.md](../../../project-trajectory/PROCESS_OPTIONS.md)
  "Parallel work — the integration seam"; the `session-protocol` skill).
- Run `check_trajectory.py --strict` directly, unfiltered, before claiming
  anything done — the DEFAULTED pre-commit floor is warn-first by design.
- Never sanction a check or revert a real fix to green a step; census edits
  classify by reading ([status.md](../../status.md) standing rules).
- Measure on a tree whose line endings match the index; probe critique
  providers by PROVIDER before dispatch ([status.md](../../status.md)).
- One environment caveat of record, RESOLVED 2026-07-29: the ambient
  `PYTEST_DEBUG_TEMPROOT` pointing at a dot-prefixed directory no longer
  empties the arch-map scan — the WI-363 fix made the hidden-skip
  root-relative, pinned both directions.

## 5. What this handoff supersedes

The pre-restructure handoffs ([2026-07-28b](handoff-2026-07-28b.md) and
earlier, plus [wrap-up-plan.md](wrap-up-plan.md)) are history — read them for
the account, never as open tasks. [handoff-2026-07-28c.md](handoff-2026-07-28c.md)
stays live ONLY for its §3 table (the unruled dispositions above); everything
else in it is executed or superseded by the restructure spec.
