> **ARCHIVE** — design history as of 2026-08-23; not current guidance.

# Archive

**Convention (OI-56 ruled (a), WI-505):** every file under `docs/archive/**`
carries a one-time `ARCHIVE — design history as of <date>; not current
guidance.` banner at the top — an addition, never a content edit, and no
live-surface pointer (the owner declined that: it rots). Two classes are
exempt because a banner would break what reads them mechanically rather than
a human: the registry-parsed specs under `work/` (`kitlib.registry` requires
the `+++` frontmatter fence as line 0 and the body to start with `##
Deliverable`) and the byte-compared snapshot under `last_approved/`
(`baseline_snapshot` drift detection). The convention is enforced by nothing
else — this line is the whole of it, by ruling.

The kit's **design history** — kept for traceability, no longer working
surfaces. The go-forward homes are `docs/status.md` (what's next),
`docs/work/` (the WI registry's active workspace — status = directory), and
`docs/log.md` (the session/gate record); root holds only the live guides +
dogfooded `dev-setup.*`.

**The boundary, stated once.** *Anything that records a historical decision and
is no longer read by a script belongs here* — a spent plan, a resolved review, a
retired vocabulary, a closed spec. The counter-rule matters as much and is the
one people get wrong: *a generated or script-read surface is NOT history even
when it looks like it.* `docs/ratify/`, `docs/iteration/` and
`archive/last_approved/` all read like dated records and are all **machinery** —
something regenerates them or something reads them at run time — so archiving
one breaks a live check rather than tidying a shelf. When the two rules
disagree, the script wins: if a checker reads it, it is not history yet. (The
live-side half of this rule is stated in `PROCESS.md` §5, beside the
status.md-now / log.md-history split.)

| File | What it was |
|---|---|
| [`IMPROVEMENT_PLAN.md`](IMPROVEMENT_PLAN.md) | The kit's development plan — 53 design threads (each a Goal/Steps/Tests/Done-when spec) + the post-plan WI-1.x landed log. **Archived 2026-07-10** once the live homes (`status.md` + `work-items.csv` + `log.md`) superseded it; the files below were its inputs. |
| [`INTEGRATION_PLAN.md`](INTEGRATION_PLAN.md) | The ClaudeGuardChecks → kit integration plan (6 phases). Phases 1–5 landed (WI-045…049); copied in-repo 2026-07-10 so those WIs' spec-of-record resolves on a fresh checkout. Phase 2's upstream content enrichment is the one external remnant (done in the guardrails upstream, not this repo). Its source triage, `OVERVIEW.md`, stays in the external ClaudeGuardChecks reference checkout. |
| [`TEMPLATE_REVIEW.md`](TEMPLATE_REVIEW.md) | The original kit review the plan was derived from (resolved 2026-06-28). |
| [`kit-adoption-field-report.md`](kit-adoption-field-report.md) | Downstream-adoption field report (Finance-Auditor boot) that produced Threads 29–40 (processed 2026-07-04). |
| [`scratch.md`](scratch.md) | Backlog/stub notes mined into plan threads; searched when reviving a stub (see the `session-protocol` skill). |
| [`AXES_AND_WORKSTREAMS.md`](AXES_AND_WORKSTREAMS.md) | The static-structure design note (9 iterations) that ratified the `CMP-###` component model; built as WI-1.44 (schema bundle) and WI-1.52 (`PROJECT_STATE.html`). §12 records a handful of still-open, gated-on-need items (`consumes`/effort schema, typed-IF check, edge-vocab unification, the swBlock/CMP drift check, cyclic-graph rendering) — none scheduled. |
| [`AGENT_ROLES.md`](AGENT_ROLES.md) | The dynamic-layer (coordinator/reviewer) design note split out of AXES iteration 6; R1–R6 ratified and built as WI-1.49/WI-042 (`docs/review-policy`, `AGENT_CMD_MAP`, the status-size guard). `--prompt-map` remains deferred until role drift is observed. |
| [`THREAD_52_REVIEW.md`](THREAD_52_REVIEW.md) | Adversarial review of the trajectory/work-items layer (Thread 52); all 8 findings (F1–F8) resolved and landed (WI-1.43/1.45/1.46). |
| [`REVIEW_GRIND_A.md`](REVIEW_GRIND_A.md) · [`REVIEW_GRIND_B.md`](REVIEW_GRIND_B.md) · [`REVIEW_GRIND_FULL.md`](REVIEW_GRIND_FULL.md) | Three adversarial reviews (method/risk, process/trace, full-repo) of the 2026-07-10 grind batch. All 20 findings resolved and landed (WI-1.53). |
| [`AGENT_ROUTING_RESEARCH.md`](AGENT_ROUTING_RESEARCH.md) | Condensed three-pass research (repo seam map · review-scoring/routing literature · July-2026 ecosystem survey) that fed the proposed **S8** heterogeneous implementer/reviewer phase of the restructure spec (2026-07-10). |
| [`repo-review-2026-08-19.md`](repo-review-2026-08-19.md) | Repository-wide critical assessment (external "Reviewer" author, target `2b867c0f`): 1 Critical + 9 High + 17 Medium + 4 Low findings and 9 positives. **Triaged and archived 2026-08-19** — every actionable finding verified confirm-or-refute first (2 of M-08's five anchor claims refuted; M-10/M-17/H-09 already owned), then dispositioned: WI-473…WI-483 minted (the C-01 monotonic-floor design, the strict-seam red, launcher selection, the hygiene batch, the one-contract docs sweep, arch-map grammar, dashboard defensive rendering, toolchain SCA, performance budgets, the 3 stale LLR anchors, the decomposition successor program), OI-43/OI-44 raised (interface contract-test posture; publication/identity posture), OI-41/OI-42 corroborated, M-04/M-14 declined with reasons. Full disposition table: `docs/log.d/2026-08-19-repo-review-triage.md` (compiles into `log.md`). |
| [`repo-review-2026-07-12.md`](repo-review-2026-07-12.md) | Owner-directed full-repo deep review (2026-07-12). No critical findings; the confident fixes landed as WI-077 (parallel harness steps + batched hook floor, the commit-bar `-n auto` correction, the gen_trajectory layout dedup, dashboard/gitattributes debris). The four rulings it left open were **ruled 2026-07-12** and filed as backlog WI-078…082 (the `main()` decomposition is the `main-decomposition` campaign). |
| [`retired-vocabulary.md`](retired-vocabulary.md) | The `G0`/`G1`/`G2`/`G3`/`G-Release`/`G-Final` gate tags retired by the eight-rung stage ladder (OI-21, 2026-08-13) — the translation table for reading a historical record and the never-reword rule for attestations. **Moved out of `PROCESS.md` §4 on 2026-08-18**, which keeps a one-line pointer: the read-side aliases are `check.py`'s and `check_vocab.py`'s behaviour, so the load-bearing core carried a retirement's paperwork rather than a live rule. |
| [`Links.rtf`](Links.rtf) | Owner-authored reference links, **relocated from the repo root 2026-07-12** (was `L1` in the deep review — an opaque RTF is not diff-reviewable and the root stays live-only). Kept as-is, not opened or converted (owner content). |
| [`specs/`](specs/) | Closed work-item specs, archived per the spec lifecycle (close date appended to the filename, attributed WIs named inside). **2026-07-20:** the WI-251 sweep archived the **61** closed-WI specs that had accreted on the live surface (each absorb-verified first — every durable decision confirmed in a spine/architecture/process home; per-spec dispositions in `log.md`), and rule **R-F** now gates the steady state. First inhabitant: [`specs/working-surface-and-architecture-restructure.2026-07-11.md`](specs/working-surface-and-architecture-restructure.2026-07-11.md) — the fully-ruled S0–S8 campaign spec (WI-053…WI-059: the working-surface SSOT rules, the `IF-###` connectivity layer + its dogfood, cross-agent skill sync, heterogeneous implementer/reviewer scheduling). Also [`specs/WI-066.2026-07-11.md`](specs/WI-066.2026-07-11.md) — the OKF self-identification banner + `check_docs` doc-graph exclusion (WI-066) — and [`specs/capability-expansion.2026-07-11.md`](specs/capability-expansion.2026-07-11.md) — the fully-ruled C1–C4 campaign spec (WI-067…WI-070: the run capability menu, the `Critique` verification value + critique loop, the pair-row agent registry, the OKF knowledge tab) — and [`specs/owner-feedback-2026-07-11.md`](specs/owner-feedback-2026-07-11.md) — the owner-feedback batch (WI-071…WI-073: the campaign gate cadence, `OWNER_SCRATCHPAD.md` + the check_docs scan-scope, the ≤10-item How-SW containment + right-sizing rule) — and [`specs/campaign-binning-parallel-tests-resume-hardening.2026-07-11.md`](specs/campaign-binning-parallel-tests-resume-hardening.2026-07-11.md) — the campaign-binning batch (WI-074…WI-076: the `Campaign` grouping column + binned When-view, pytest-xdist parallel execution, the dirty-tree resume-hardening thin slice). |
| [`repo-lock-decisions-2026-08.md`](repo-lock-decisions-2026-08.md) | The lock program's **executed** decisions in full — D-1 (the attestation anchor moves onto the row), D-2, D-3 (one column name, one meaning), D-4 (supersession is deletion; ids are never reused), D-5 (the one TOML carrier), D-6 (`spine_carrier` amends F5), D-7 (the duplication census torn down) — plus the F-1…F-12 reference findings and the program's own log, including the claims it made and had to withdraw. **Moved out of [`../repo-lock.md`](../repo-lock.md) 2026-08-11**, which had grown to 2,343 lines and could no longer serve as a statement of what is still owed; that file keeps every `D-n` heading as a resolvable pointer, because ~20 live source and test files cite them in prose where nothing link-checks them. The still-owed decisions (D-1's anchor half, D-4's `SupersededBy` deletion, D-9's `Status` ladder) stayed behind. |
| [`history/`](history/README.md) | The docs-root **handoff chain, root review rounds, and spent root plans**, moved out 2026-08-13w on the post-sitting-2 census — per-file dispositions in [`history/README.md`](history/README.md). The root keeps only live surfaces plus the four files with live anchors (`concurrency-v2.md` = an open WI's specref; `concurrency-restructure.md` = named in a ratified SN acceptance cell + budgeted PROCESS_OPTIONS prose; `spine-restructure-2026-08-08.md` = three unruled §7 residues + the §8 dead-symbol table; `architecture.md` = ruled to die, but by its own program row). |
| [`work/`](work/) | **LIVE REGISTRY DATA — the other entry in this table that is not spent history.** The WI registry's TERMINAL history (`complete/`, `cancelled/`, `partial/`), relocated here whole 2026-08-22 (WI-504, OI-55 ruled (a)) so `docs/work/` holds only rows still in flight. Status is still the directory and nothing here is read-only-by-a-human the way the rest of this page is: `kitlib.registry.read_spec_rows` reads `docs/work/` and `docs/archive/work/` as ONE registry (`spec_roots`), so a closed row is still a live predecessor, trace link and dashboard count. Placed under the archive for the SAME reason `last_approved/` is — workspace hygiene, not retirement — and the same `check_vocab.EXEMPT_GLOBS` carve-out (`docs/archive/*`) that protects `last_approved/`'s frozen vocabulary also protects a closed spec's frozen prose here. |
| `last_approved/` | **FROZEN EVIDENCE READ BY LIVE CHECKS — not design history, and the one entry in this table that is not.** Byte-for-byte copies of the spine and the approval-carrying registries as they stood when a human last blessed them (owner directive 2026-08-15; design: [`../plans/2026-08-15-baseline-snapshot-design.md`](../plans/2026-08-15-baseline-snapshot-design.md)). Every "has this approved row changed?" question is answered by diffing the live registry against its copy here, so `trace.py --ratify`, `open-items.html`, `PROJECT_STATE.html` and the amendment adjudicator all read it. Same class as [`specs/`](specs/) — archive as live machinery input — and the placement is ACTIVELY load-bearing rather than incidental: `check_vocab.EXEMPT_GLOBS` exempts `docs/archive/*`, and this subtree legitimately holds the PREVIOUS vocabulary, so anywhere else would red the vocabulary enforcer on every signing. Written ONLY by `intake.py snapshot` (never by hand, never by the loop); `check_trajectory.staged_snapshot_findings` enforces that any touched file is byte-identical to its live counterpart in the same commit. **Absent until this repo's first signing, which is the honest state for a repo that has approved nothing yet** — which is also why this row does not LINK the directory: a link to a path that correctly does not exist is a broken link, and the absence is declared in `docs/declared-absences` instead. |
| [`plans/`](plans/README.md) | The 2026-08 sitting-preparation plan set + the closed DP-001 dual-plan round, **moved out of `docs/plans/` 2026-08-13** when the live decision surface consolidated into the two sitting documents that now own that directory. Per-file dispositions (what each still uniquely holds — including the STILL-LIVE D-9 migration checklist) in [`plans/README.md`](plans/README.md); the carry audit is sitting 2 §7. |
