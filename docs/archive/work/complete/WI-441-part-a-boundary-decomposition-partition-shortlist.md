+++
id = "WI-441"
title = "OI-14 part A program (ruled A3 + A6, 2026-08-13): derive the component partition from the system's declared inputs and outputs. In order: (1) declare the SYSTEM BOUNDARY — what is outside, what crosses, each crossing typed, the set declared complete — carrying OI-28's two seeds (the LLM-agent-in-a-direct-session actor whose crossings are instructions in and artifact edits out through the git-enforced hook floor, SR-019's rationale becoming the row's rationale; and the shipped template set as a traced product artifact class anchored at one SR with test_dogfood_sync as its verification); (2) first-cut functional decomposition from the needs, as signals; (3) score candidate partitions with ONE HOME PER BEHAVIOUR as a hard constraint and cut-count as tie-breaker; (4) produce a RANKED SHORTLIST with per-candidate rework sizing and behaviour-straddle lists; (5) PROVISIONALLY ADOPT the top candidate warn-first (owner's confirmed execution mode — Component is a traced cell, no re-attest opens) and package the shortlist for ratification at the owner's return. Also rule SR.Area explicitly (derive from Component or retire — never leave it authored-and-unread), and confirm the containment rule actually covers the 46 IF rows cross_component_findings is vacuous for. The boundary-rung enforcement lands as A6: applies-when, warn-first."
workstream = "lock-program"
sr_refs = []
needs = ["WI-440"]
buildtier = "strong"
safety_class = "spine"
priority = 1
+++

## Deliverable

Completed 2026-08-13, in three landed pieces plus the provisional adoption.

The DATA PACK (docs/plans/2026-08-13-part-a-data-pack.md): the 34-crossing
boundary inventory carrying OI-28's two seeds (19 crossings have no IF row —
Part B's intake), the functional decomposition, the coupling census (which
refuted the containment-covers-the-vacuous-rows hope: the 45 vacuous IF rows
are policed by NOTHING — Part B inherits that), the 12-behaviour multi-home
census (plus new live divergences B10 — fixed and pinned the same day — and
B11), and five candidate partitions with reproducible scoring.

The RANKED SHORTLIST AND RULING (docs/plans/2026-08-13-part-a-shortlist-
ruling.md): P5 narrow-waist adopted provisionally warn-first per the owner's
confirmed execution mode; P3 runner-up; P2 recorded as the measured trap;
the SR.Area verdict (retire the 31-value free-text column; six spanning
values become a closed aspect vocabulary in Part B's schema; Portability's
rows are depth-0 system obligations); the one-home constraint recognized as
a property of the code that WI-448 discharges, with D-8 superseding the
F5-era acceptance by recency.

The ADOPTION BUILD: CMP-006..009 minted (State planned, provisional note),
CMP-001..005 deleted under D-4, watermark 5→9; 149/149 LLR Component cells
and 54 IF Component cells re-pointed mechanically through the module map (the
old→new fallback declared and never needed); the WI-440 advisories dropped
15→0 and hard findings held 0→0 with all 31 cut seams IF-covered — the
pack's prediction reproduced exactly. No Status flipped anywhere. Surprises
recorded, not silenced: LLR-080 is the one residual multi-membership (its
lone edge stays policed and covered); the four non-arch-map modules (hooks,
shell templates) assigned; adjudicate_brief joins W3; the ratify brief
RENDERS traced-cell diffs (44 Component blocks) without opening any status
window; two partition-dependent test pins updated, and their fragility filed
as WI-450.

Deliberately NOT here, by the ruling's sequencing: the A6 boundary-rung
warn-first enforcement and the 19 missing boundary IF rows (Part B / WI-442
— they need the typed schema), and the Area conversion (rides the carrier
conversion). Owner ratification package: the sitting pack section 3.
Builder bars: full 2351 passed / 6 skipped; smoke 1005; strict integrity
clean at components=4.
