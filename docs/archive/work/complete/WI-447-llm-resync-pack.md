+++
id = "WI-447"
title = "OI-27 execution (ruled 2026-08-13, option e now with c as the named promotion target): build the LLM re-sync pack — a purpose-built LLM-facing surface: the clear procedure, the what-changed-over-time narrative of major kit shifts, the file-by-file deviation review against the MAPPING inventory, and the translation helper for concept renames (gate-to-stage is the inaugural entry, same content WI-445's adopter recipe produces). EVERY entry SHA-ANCHORED to the kit commit range it applies to — the non-negotiable discipline that makes promotion to the detector registry a mechanical lift. ONE HOME: the pack REPLACES the recipe half of ADOPTING section 6 and the downstream-resync skill's duplicated recipes (section 6 keeps the procedure and points at the pack). The promotion trigger is named in the ruling: first out-of-sphere adopter OR second observed drift incident. The old-kit test (WI-439's third defect) verifies the pack's procedure; if detectors ever ship piecemeal, record them in the pack's entries — the mechanisms compose."
workstream = "lock-program"
sr_refs = ["SR-036", "SR-111"]
needs = ["WI-439", "~WI-445"]
buildtier = "medium"
safety_class = "ordinary"
priority = 2
+++

## Deliverable

Completed 2026-08-13. The LLM re-sync pack ships at
project-trajectory/RESYNC_PACK.md: the bounded procedure (with SR-036's
operator judgement stated, not mechanized), the file-by-file deviation review
over the MAPPING inventory, 51 SHA-anchored entries (42 what-changed + 9
translation-helper; 100% anchor coverage, every anchor resolving to a real
commit, oldest at one commit after the repo's root — the pre-<sha> escape
implemented and tested but never needed), the reserved anchor-free
placeholder for the gate→stage entry that lands with WI-445, and the ruled
promotion trigger + detector-registry target. ONE HOME enforced by pins:
ADOPTING.md keeps framing and pointers (75,106 → 20,398 bytes; §6 804 → 31
lines) and may carry no [since] anchor, WI id, or recipe heading; the
downstream-resync skill sheds its last restatements (5,738 → 2,480) through
the materializer. One deliberate deviation accepted by the coordinator: the
pack is NOT a bootstrap MAPPING destination — a scaffolded copy would be
frozen exactly when a re-sync needs it fresh, the kit checkout every adoption
route carries is the pack's home, and a pin records the decision so re-adding
the row is a deliberate act; the kit-version stamp now names the pack. The
codex content-loss audit read all 34 old recipes against the new corpus:
32 fully carried, 0 lost, 2 weakened — both restored in the review commit
(the dual-carrier integrity-error warning on the WI-registry conversion; the
--sync only-the-skills-subtrees scope guarantee); one prose-date nit
superseded by the SHA anchor itself. Builder bars: smoke 1047; full 2389
passed / 6 skipped / 1 pre-existing containment failure (healed separately by
the LLR-168 seed); pack module 10 tests; real scaffold green with the pack
correctly absent.
