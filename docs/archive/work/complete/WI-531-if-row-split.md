+++
id = "WI-531"
title = "One row, one kind: split the 14 two-way rows, disposition the 35 bundled rows, re-point every citation (OI-67 slice 4)"
workstream = "architecture"
sr_refs = ["SR-159"]
needs = ["WI-530"]
buildtier = "strong"
safety_class = "ordinary"
priority = 2
+++

## Deliverable

The registry reads one row, one direction, one kind. Record:
[../../../log.d/2026-08-29-wi531-if-row-split.md](../../../log.d/2026-08-29-wi531-if-row-split.md);
the three-worker round whole at
[../../../reviews/2026-08-29-oi67-slice4/](../../../reviews/2026-08-29-oi67-slice4/README.md).

Twenty rows minted (`IF-145`–`IF-164`) from the slice-3 workers' split
worklist — the missing exit-code and argv halves, the file media that were
bundled under another owner (`docs/test/`, `docs/agents-enabled`,
`docs/status.md`, `docs/log.d/`), the write sides of `docs/work/` and
`docs/reviews/` — each with its `Contract IF-###:` body beside its owner; two
duplicate pairs collapsed (`IF-127` into `IF-075`, `IF-116` into `IF-101`);
three channels corrected and eight far sides re-measured from the code; the
three generated-document rows re-pointed from the artifact to its reader class.
136 rows became 154; the reference reads 73 sources / 150 seams / 150 stated.
`TC-161` re-pointed, the seam-TC allowlist pruned to 118 seeded plus 21
reasoned entries, `docs/log.d/README.md` exempted from fragment discovery.
Not split, stated: the harness's argv into each checker (one requestor for all
of them — recorded as a class in the fragment), and the arms the split itself
surfaced (`derive_stage --check`'s exit code, `schedule.py`'s CLI,
`integrate`'s in-process surface, `docs/test/report.html`,
`docs/test/perf-report.md`) — the next worklist, in the fragment.
