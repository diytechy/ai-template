+++
id = "WI-320"
title = "Roadmap DAG dependencies cannot be followed individually (T8, 121-CRITIQUE MAJOR) - in the When/Roadmap flat-DAG view several dependency edges share long coincident horizontal corridors between the phase-2, phase-3, unphased and phase-1 groups, then converge into dense port fans, so a reader cannot attribute a given line to one source and one target. Seen in 1680px-light-dag-full.png and 1280px-dark-dag-full.png. This is the FIRST finding to land on T8, the anchor whose perceptual clause (crossings minimized, in open space, not under a label) is one of the two the WI-300 ruling left genuinely perceptual - so scope this carefully: the MECHANIZABLE half (no edge passes through an unrelated node box) was hand-verified once during WI-273 and can be bound as a test; the ranked-lane routing the critic asks for is a layout change to _port_fan/the DAG router. Recommended split: bind the through-box rule first as the objective floor, then treat lane separation as its own design slice with before/after shots. RE-AFFIRMED against the amended SR-054 (2026-07-26, WI-321's prompt): the amendment removed the row's restatement of which critique anchors are live, moving that to the rubric alone. The requirement text this finding is judged against is unchanged, and the rubric still carries the anchor, so the finding stands as scoped."
workstream = "dashboard"
sr_refs = ["SR-054"]
buildtier = "medium"
safety_class = "ordinary"
order = 317
+++

## Deliverable

T8's OBJECTIVE half bound as LLR-120/TC-125, per the row's own recommended split: every emitted wire, sampled as the polyline a reader's eye follows, clears every node rect that is not its own endpoint - swept across the shipped dashboard plus one fixture per emitter. This is a DIFFERENT claim from the WI-253 router unit tests and had only been hand-verified once (WI-273): _detour_d returns a LEAST-BAD fallback when no lane clears within _MAX_LANES, so 'the algorithm detours' does not imply 'nothing shipped through a box'. Measured at the binding: 264 shipped wires, 0 through a box, 1 riding within the router's own 3px clearance. Proven to bite with the blocked-wire branch disabled. The lane-separation clause the critic actually reported is NOT claimed here - it is the reader-experience residue and is filed as WI-323 with its two measured starting points.
