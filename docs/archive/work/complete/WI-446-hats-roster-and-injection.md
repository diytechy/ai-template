+++
id = "WI-446"
title = "OI-19 execution (ruled 2026-08-13): ship the six-hat starting roster at docs/requirements/hats.toml plus its shipped template — SECURITY (always), FIRST-RUN-ADOPTER (template-scope), UNATTENDED-OPS (loop-executed), CROSS-PLATFORM (launchers/paths/shells), MAINTAINER (always), TEST-ENGINEER (always) — each row carrying applies_when, asks, and listens_for (a hat naming no failure class is refused as ceremony). Build the INJECTION first (the decomposition brief builder composes the applicable hats into the brief via the adjudicator seam), the coverage RECORD second (which hats were applied per decomposition — a new artifact class; injection changes what decompositions produce, the record only proves it happened). The roster is OWNER TEXT: shipped as drafted, marked for the owner's edit at return; adopters are expected to edit, which is what keeps applies_when honest. Owner's framing recorded on the OI-19 row: this SUPPORTS SN-040 by surfacing the rationale through the spine."
workstream = "lock-program"
sr_refs = []
needs = []
buildtier = "medium"
safety_class = "spine"
priority = 2
+++

## Deliverable

Completed 2026-08-13. The six-hat roster ships at docs/requirements/hats.toml
(owner text — shipped as drafted, marked for the owner's edit at return) with
the shipped template in registries/, mapped by bootstrap and verified by a
real scaffold bootstrap that parses the copied roster with the copied reader.
Each hat carries applies_when / asks / listens_for on a closed, evaluable
grammar whose honesty rule is that an undeclared field satisfies NO clause
(!= included) — so FIRST-RUN-ADOPTER's scope clauses stay silent until SN
rows carry a scope field, rather than firing on everything. The injection
point is the dual-plan PLANNER brief (the one mechanized decomposition brief;
the adjudicator seam honored as shape, not as file): a {{HAT_QUESTIONS}}
slot filled per round, every hat answered in ## Notes naming a Plan-WI or an
explicit no-finding. Absent roster = opt-out; malformed = PAGE. Deliberately
NOT built, per the ruling's sequencing: SN-036's per-decomposition coverage
RECORD (injection changes what decompositions produce; the record only proves
it happened — second slice). Builder totals: full 2357 passed / 6 skipped;
smoke 1033; scaffold + touched modules green; smoke-budget re-stamped
1030→1090 with a signed figure.
