# `last_approved` — the approval stamp

**This file is prose. Nothing parses it.** Every machine fact about the snapshot comes from the copied registry files beside it, or from `git log` over this directory.

## Refreshes recorded under an explicit approval

Each line below is a human's citation of the act that authorised a refresh absorbing ratified text (`intake.py snapshot --approves <ref>`). A refresh that absorbs nothing ratified, or that rides a `Status` flip, needs no entry.

- 2026-08-22 — refreshed under approval ref: **WORKLIST W-3, docs/reviews/2026-08-22-wi498-program-close (owner's standing fix directive; dial DevStg-Needs) - five prose cells re-authored against the live code after the sweep left them FALSE** (7 registry file(s)).
