# WI-308 — Triage the doc-reference residue, then wire the check in

**Status: queued** (filed 2026-07-25 at the close of WI-062).

## Why now

WI-062 split `check_doc_refs.py`'s path tier into **untraced** (explained by a
mechanical reason) and **dangling** (real rot). This repo dropped from **561
findings to 22**. That changes the economics: 561 was a number to tolerate, 22 is
a list to *fix* — and only once it is fixed is the check worth running on every
gate.

**Do the triage before the wiring.** Wiring `[step:doc-refs]` first would add 22
warns to every gate run, which is exactly how a check earns the ignore WI-062
just undid.

## The 22, in three classes

Regenerate the current list with:

```
python project-trajectory/scripts/check_doc_refs.py --root .
```

### (a) Real rot — fix the reference

- `docs/requirements/stakeholder-needs.md` names **`docs/next-wi`**, retired by <!-- path-ok: this spec QUOTES the offending references as its worklist -->
  WI-180. A live requirements doc pointing at a retired artifact is the rot this
  checker exists for.
- `docs/gate-policy.md`, `docs/knowledge/parallel-scheduling.md`,
  `docs/archive/ratify-WI-229-sr-split.md` and `stakeholder-needs.md` link specs that
  have since been **archived** (`docs/specs/WI-107.md`, `WI-229.md`, <!-- path-ok: this spec QUOTES the offending references as its worklist -->
  `parallel-wi-dispatch.md`). This is the **link-aware spec archival** class
  **WI-288** already owns — check whether WI-288 subsumes them before fixing
  by hand, so the two WIs don't fight over the same lines.

### (b) Scaffold-only paths this meta-repo deliberately lacks

`docs/process.md`, `docs/process-options.md`, `docs/kit-profile`, <!-- path-ok: this spec QUOTES the offending references as its worklist -->
`docs/kit-license`, `docs/release-checklist.md`, `.github/workflows/check.yml`, <!-- path-ok: this spec QUOTES the offending references as its worklist -->
`src/`. Every one is **already declared** in `tests/test_dogfood_sync.py`'s <!-- path-ok: this spec QUOTES the offending references as its worklist -->
`SCAFFOLD_OMISSIONS` (or is a generated stamp a scaffold gets and a meta-repo
does not). Two honest options:

1. mark the lines `path-ok` — cheap, but repeats a fact the omission list
   already states; or
2. **teach the checker the omission list** — a third untraced reason
   ("a scaffold destination this repo declares it does not carry"), which keeps
   the single source of truth in `SCAFFOLD_OMISSIONS`.

(2) is the better fit for the kit's anti-duplication rule, but it couples the
checker to a test module — so the seam would need to be a small declared file,
not an import. Rule this deliberately rather than defaulting to `path-ok`.

### (c) Declared-policy files where ABSENT means default

`docs/review-cadence`, `docs/subagent-gate`, `docs/components-check`. Naming one <!-- path-ok: this spec QUOTES the offending references as its worklist -->
in prose is *correct* — absence is a legal, documented state. These want a
**rule** (a fourth untraced reason, or a documented naming convention the path
tier skips), not a per-line fix.

## Done-when

- [ ] Each of the 22 is fixed, `path-ok`-marked, or covered by a new untraced
      reason — with the class it fell into recorded.
- [ ] `check_doc_refs.py --root . --strict` exits 0.
- [ ] `[step:doc-refs]` is wired into `docs/stack.ini` so the check runs at the
      gate, and the untraced count is visible in its output.
- [ ] The kit's `stack.ini.template` gains the same step, commented, so a
      downstream repo can opt in the same way.
