> **ARCHIVE** — design history as of 2026-07-24; not current guidance.

# WI-285 — the integrator's composed-tree bar never runs (stack.ini schema mismatch)

**WI:** WI-285 · **Status:** queued · **Priority 1** (integration-time gate
verification is silently OFF) · **Delivers:** SR-006-adjacent (the integrator's
combined bar) — harness layer, no new spine SR

## Problem

`agent_dispatch._run_combined_bar` — the composed-tree bar the serialized
integrator runs before publishing a train (spec §9, "combined bar always runs")
— reads the test command from `docs/stack.ini` **`[stack] test`**. This repo's
`stack.ini` has **no `[stack]` section**: it declares the harness under
**`[product] test = {py} -m pytest -q -n auto`** (with `[tiers] smoke = -m
smoke`), the form `check.py` reads and `{py}`/`{src}`/`{tests}`-substitutes.

Result (verified live, run `20260723T0202`): every integration in this repo
journals `integration-bar: "skipped (no declared test command)"` and treats it as
a **pass** — so **WI-275, WI-279, and every historical integration landed on
`llm/integration` without the composed-tree bar ever running.** Integration-time
gate verification is effectively disabled for the meta-repo (the per-commit
pre-commit floor still runs on worker commits, but the *combined* bar on the
merged tree — the one that catches cross-WI composition breakage — does not).

Two coupled defects:
1. **Schema mismatch:** the integrator's contract (`[stack] test`, raw
   `shlex.split`, no substitution) does not match the kit's own stack.ini schema
   (`[product] test` with `{py}` placeholders that `check.py` fills). The kit
   doesn't run its own integrator bar — a dogfood hole.
2. **Fail-OPEN default:** an absent command returns `(True, "skipped")` — a
   missing bar silently passes. For a walk-away integrator that is the wrong
   default when a command *should* exist.

## Approach

- **Run the harness the repo actually declares.** Make `_run_combined_bar` invoke
  the same bar `check.py` does — either read `[product] test` with the `{py}`/
  `{src}`/`{tests}` substitution (and choose the tier: at minimum smoke, ideally
  the gate bar `check.py --gate <derived>` on the composed tree), or shell out to
  `check.py` directly so the integrator and the commit floor share one definition
  (the WI-260 "one rule, read once" precedent).
- **Tighten the skip:** distinguish "no harness declared at all" (a genuinely
  stackless fixture → legitimately skip) from "a harness is declared but under a
  key I didn't read" (→ do not silently pass). Absent `[product] test` AND absent
  `[stack] test` = skip; either present = run it.
- **Dogfood + template decision:** decide whether the fix ships in the kit
  template (so downstream integrators run their declared bar too) and whether the
  template's stack schema needs a documented single test-command home; record it.
- Tests: a stack.ini with `[product] test` must cause the integrator bar to RUN
  (not skip); the stackless-fixture skip stays; a composed tree that fails the
  bar parks the train (not integrates).

## Interfaces

No cross-module seam — intra-module (`agent_dispatch._run_combined_bar` +
`check.py`/`stack.ini` reading, and tests). Nearest: SR-006 (gate/tier harness),
whose command home this aligns the integrator with.

## Done-when

- [x] The integrator runs the repo's declared bar on the composed tree (verified:
      a real integration in this repo journals a RUN result, not "skipped" —
      `test_product_schema_bar_runs_on_real_integration` asserts every
      `integration-bar` event reads `result: "pass"`, never `skipped …`).
- [x] A composed tree that fails the bar parks the train instead of integrating
      (`test_combined_bar_product_red_parks` + the existing red-`[stack]` path).
      A declared bar whose binary is absent/unrunnable also parks (fail closed,
      not an uncaught crash — SR-008): `test_combined_bar_missing_binary_fails_closed`
      + the end-to-end `test_missing_binary_combined_bar_parks_not_crashes`.
- [x] The "declared-but-unread key" case no longer fail-opens: `[product] test`
      now RUNS (`test_combined_bar_runs_product_schema_not_skips`), and a
      declared-but-EMPTY command fails closed rather than skipping
      (`test_combined_bar_declared_but_empty_fails_closed`).
- [x] Template/downstream disposition recorded (see Resolution below).
- [ ] Commit bar green; row `done`, spec archived. _(row-flip + archive are the
      integrator's close-ritual at done-flip; the worker ran the full suite green:
      1408 passed, 5 skipped.)_

## Resolution

**What ran vs. where the logic lives.** `_run_combined_bar` now resolves the
declared test command through a new `agent_common._declared_test_command(ini)`:
the kit schema `[product] test` (with the same `{py}/{src}/{tests}` substitution
`check.py` applies for its `tests+coverage` step), with the legacy raw `[stack]
test` kept as a fallback. The skip is honest — **only** a profile declaring
NEITHER key skips; a key present under a name the integrator didn't read (or an
empty command) no longer silently passes (empty → fail closed, never
`subprocess([])`). A declared command whose *binary* is absent/unrunnable is
caught (`except OSError`) and treated as a red bar too (SR-008) — the
`FileNotFoundError` no longer escapes `integrate_train` to crash the walk-away
dispatcher (exit 1) after the worker is ready; the train parks for rework
(REVIEW-A MAJOR rework).

**Why `agent_common`, not `import check`.** The first cut imported `check`
directly ("one rule, read once"), but `check.py` is CMP-001 and `agent_dispatch`
is CMP-004, so that in-process import is an undeclared CMP-004→CMP-001 edge that
`check_trajectory --strict` (the G3 gate) flags. The resolver is used *only* by
the integrator (never by `check.steps()`), so housing it in `check.py` would not
have shared one definition anyway — it would just have added a seam. Instead it
lives in `agent_common` (the CMP-004 shared-coordinator-primitives module the
integrator already imports), mirroring `check.py`'s stack schema the same way
`OWNER_ONLY_PATHS` mirrors `check_docs.SCRATCHPAD` rather than importing across
that boundary; the tokenizer is `agent_session.split_cmd` (no new duplicate), and
the tests pin the resolver against drift.

**Template / downstream.** No new schema is needed: `[product] test` is already
the single documented test-command home (`stack.ini.template`), and
`agent_dispatch.py` / `agent_common.py` are kit scripts that ship downstream
verbatim — so downstream integrators get this fix on re-sync with no migration.
`[stack] test` remains honored purely as a legacy fallback.

**Known residue (not this WI).** The SSOT ideal — `check.steps()` and the
integrator both deriving the base test command from one function — stays deferred
(`steps()` appends coverage/tier args and can't cleanly reuse the plain
resolver); worth a follow-up if the integrator↔harness coupling is later blessed
into a declared IF-### seam.

## Notes

- Filed 2026-07-23 from the run-babysit (WI-279's skipped bar). `Priority 1`:
  until this lands, integrations are not gate-verified — the reconcile that
  surfaced it had to run the full suite by hand to close the gap for WI-279.
- Interacts with WI-286 (worktree Python): the integrator bar must run under a
  floor-satisfying interpreter, or it re-introduces the ambient-3.8 risk.
