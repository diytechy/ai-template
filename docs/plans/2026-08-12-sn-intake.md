# The 2026-08-12 SN intake — batch record and sitting inputs

**What this is.** The plan-of-record for the stakeholder-need batch the owner
ruled in on 2026-08-12 ([repo-lock](../repo-lock.md) §8.3 ruled + §8.6 intake):
eight new Draft SNs (SN-033…SN-040), one Draft SR (SR-148), and one amendment
to the SN-029 *draft*. Everything here enters as **Draft** — attestation is
the sitting's act, and nothing in this batch opens a re-attest window (no
ratified text is touched).

**Review provenance (the §8.4 pattern).** The batch was adversarially
reviewed by OpenAI `gpt-5.6-sol` (medium effort) against six attack axes
(mis-levelling per the sitting's demotion precedent, duplication against
SN-001…SN-029, the D-7 evidence test, acceptance testability, internal
contradiction — including SR-126 and `human_ratification_through = 0` — and
plain-language self-compliance). Dispositions:

| draft | verdict | disposition |
|---|---|---|
| plain-language needs | AMEND | **accepted** — reviewer's text, restricted to SN `need` cells so it stops contradicting SR-126's script-name carve-out → SN-033 |
| launchers (dev-setup + agent-resume) | DEMOTE to SRs | **disputed** — kept at need tier (the owner's intake words them as needs, twice); the dispute is recorded in the row's acceptance for the sitting → SN-034 |
| run-menu launcher | DEMOTE to an SR-046 self-application amendment | **disputed** — same grounds; also fixed the draft's false claim about the registry NG section → SN-035 |
| intercept-at-stage | REJECT (duplicates SN-029; ordinal read backwards) | **accepted** — lands as an amendment to the SN-029 draft instead (below the reserved tier, approval is a *delegated, recorded* review, never skipped) |
| hats/perspectives | AMEND | **accepted** — reviewer's text (coverage-and-provenance evidence, adequacy stays a review judgment) → SN-036 |
| SRs against component-boundary interfaces | AMEND | **accepted** — reviewer's text (no pretence the component partition is settled) → SN-037 |
| everything-shipped chains back | AMEND | **accepted** — reviewer's text (declared inventory defines the coverage universe) → SN-038 |
| *missing need* — scope visibility | (reviewer-identified) | **adopted** → SN-039; the mechanical `Scope` field still lands in the §5 step-7 schema batch |
| *missing need* — reproducible decomposition | (reviewer-identified) | **adopted** → SN-040; the worked method is [`knowledge/system-decomposition-methods.md`](../knowledge/system-decomposition-methods.md) |
| loop-ordering SR | AMEND | **accepted** — reviewer's row: valid TOML, composed with SR-141's adjudication-first rule, holds keyed to the declared ratification level → SR-148 |

The reviewer also confirmed **no needs may be minted from repo-lock §8.6
items 5–7** (prompt-carrier format, common modules, guardrail delivery) —
those were questions, since answered as analyses, not stakeholder intent.

## Sitting input 1 — the launcher-tier dispute

The sitting's own precedent (2026-08-10: three needs demoted for decomposing
into one SR) supports the reviewer. The owner's intake supports need tier.
SN-034/SN-035 carry the dispute in their acceptance text; demotion, if ruled,
costs one id retirement each and re-homes the obligations as: dev-setup
placement → amendment under SN-001 (SR-032's surface), agent-resume launcher
→ amendment under SN-025, run-menu self-application → one-row amendment of
SR-046 plus the non-goal prose edit.

## Sitting input 2 — the edge-tier dissolution map (owner intent, §8.6 item 2)

Measured against `system-requirements.toml`: **8 of 10 edge SNs already share
their SR with a core need**, so dissolving the tier is mostly dropping the
edge id from `sn_refs` — only two SRs cite an edge SN alone and need
re-anchoring (the only judgment calls):

| edge SN | carried by | co-anchored core | act |
|---|---|---|---|
| SN-013 (no interpreter) | SR-021 | — | re-anchor → SN-001 or SN-011 (**judgment**) |
| SN-014 (missing tool) | SR-006 | SN-004, SN-008 | drop from `sn_refs` |
| SN-015 (not a git repo) | SR-027 | SN-006 | drop |
| SN-016 (no prompts) | SR-026 | SN-006 | drop |
| SN-017 (killed mid-session) | SR-029 | — | re-anchor → SN-006 or SN-027 (**judgment**) |
| SN-018 (second coordinator) | SR-030 | SN-006 | drop |
| SN-019 (zero-commit repo) | SR-028 | SN-006 | drop |
| SN-020 (agent CLI errors) | SR-028 | SN-006 | drop (same row as SN-019 — the §0 pair) |
| SN-021 (generated drift) | SR-023 · SR-025 | SN-010 / SN-012 | drop from both |
| SN-022 (placeholder at gate) | SR-003 | SN-002 | drop |

The edge rows are **ratified** text, so the dissolution is a spine amendment
inside the already-open window — same batch as the 25-row re-attest, not a
separate act. Ids retire and never re-mint (D-4). Afterwards `kind` sheds
`edge` (with D-9 consequence 3 shedding `draft`, the column retires at the
step-7 schema batch), and this coverage class is regenerated per-need by the
hats mechanism — the owner's stated intent.

## Sitting input 3 — the hats roster sketch (SN-036's machinery)

Proposed home `docs/requirements/hats.toml` (+ a shipped template), injected
by the decomposition brief builder; the decomposition record names the hats
worn (SN-036's acceptance). Sketch of the schema and this repo's starting
roster — a proposal, not an applied file:

- schema per row: `name` · `applies_when` (when the decomposer must wear it)
  · `asks` (THE question it puts to every need) · `listens_for` (the failure
  class it exists to catch — a hat naming no failure is ceremony, D-7).
- starting roster: SECURITY (always) · FIRST-RUN-ADOPTER (template-scope
  needs — the downstream-user hat the owner named) · UNATTENDED-OPS (needs
  the loop executes) · CROSS-PLATFORM (launchers/paths/shells) · MAINTAINER
  (always) · TEST-ENGINEER (always).

## What executed now vs what waits

Executed with this batch: the nine registry rows, the SN-029 draft
amendment, the watermark bump, regeneration. Waits for the sitting: every
attestation, the launcher-tier ruling, the edge dissolution, the hats file
itself (its carrier is ruled — TOML — but the roster contents deserve the
owner's eye with SN-036). Waits for step 7: the `Scope` schema field, the
edge columns' retirement.
