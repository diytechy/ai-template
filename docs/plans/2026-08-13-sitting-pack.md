# The consolidated sitting pack — everything the owner's return owes

Assembled by the coordinator during the 2026-08-13 charge-through (the
pre-absence batch ruling; the log's Decisions entry of that date is the
authority). **Read this file first**; each line points at the depth. The
machinery renders every owed re-blessing with its per-cell before/after in
[open-items.html](../open-items.html) — this index is the reading ORDER and
the decision list, not a restatement.

## 1. The re-attest batch (read FIRST, fatigue is the enemy)

- **The 12 previously-laundered rows, first** — the rows where a rewrite was
  once caught changing meaning: the verification table and per-row ledger are
  in [2026-08-13-wi444-batch-application.md](2026-08-13-wi444-batch-application.md)
  (part 1; 12/12 verified clean at application, token-multiset comparison).
- The rest of the prose batch (29 SN + 16 SR exact replacements), the OI-23
  stale-row amendments (15 rows), the OI-17 reframe (SN-025/034/035), the
  OI-18 dissolution (ten edge rows retired, two re-anchors), SN-005's OI-24
  narrowing — all applied per your apply-now direction; ledger in the same
  file, part 2.

## 2. Decisions the batch surfaced (each needs a yes/no, none blocks the rest)

1. **Six one-obligation-per-row form findings** the prepared prose batch
   introduced (SR-040/042/050/057/130/131 carry multiple `shall`s). A split
   mints ids — your act. Alternative: waive with a reason.
2. **SR-082's conditional rewrite** still unapplied — its condition ("the
   sitting confirms the drain-dial reading") is yours to confirm.
3. **Three Draft-status rows on shipped work** (SR-147, LLR-165, TC-160) —
   text corrected, status lift owed (lifting without you would launder Q11).
4. **~29 SR rationale cells** still cite retired SN-013..022 ids as history
   (D-4 reading). Sweep or keep — one call.
5. **WI-419 chain-flip scoping**: only rewritten rows flipped Modified
   (the batch is certified text-only). Confirm or order the wider flip.
6. **Four unblocked stale-text verdicts** (SR-082/085/108/125) — readable now
   that the `session_hold` rename killed the gate_policy ambiguity; they are
   OI-23-class amendments awaiting the same window.

## 3. The provisional partition (overturnable)

- **P5 narrow-waist adopted warn-first** — the ranked shortlist, the rework
  sizing, the SR.Area→aspect verdict, and what ratification means:
  [2026-08-13-part-a-shortlist-ruling.md](2026-08-13-part-a-shortlist-ruling.md);
  measured inputs in [2026-08-13-part-a-data-pack.md](2026-08-13-part-a-data-pack.md).
- The adoption build (CMP-006..009 minted, CMP-001..005 retired, 149 LLR
  re-tags, IF re-tags) — see its WI-441 close record in docs/work/complete/.

## 4. The hats roster is owner text

- [../requirements/hats.toml](../requirements/hats.toml) shipped as drafted —
  review the six, cut what does not earn its place, add what is missing.

## 5. Standing mechanical facts (no decision, just awareness)

- The gate reads G1 honestly; `modified` and `drafts` counts on the
  `docs/gate` basis line are the window you will close.
- Everything committed locally on `infra/mechanized-loop`; **push and
  merge-to-main stay yours** (`push = "human"`).
- Session-by-session account: the log's 2026-08-13 entries.
