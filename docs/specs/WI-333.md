# WI-333 — the G3 `lint` step is red on 20 pre-existing ruff findings

**Status: queued** (`P3`, surfaced 2026-07-27 while linting the WI-332 change —
filed rather than fixed, because none of it is that change's code).

## The observation

[`docs/stack.ini`](../stack.ini)'s `[step:lint]` runs
`ruff check {src} {tests}`. That command reports **20 findings today**, so the
step cannot pass the first time it is run. It has never run in anger: `lint` is a
**G3** step and the derived gate is G2, so nothing regressed — the debt has simply
never been visible to a gate.

| Rule | Count | Where |
|---|---|---|
| `F402` module `html` shadowed by a loop variable | 13 | `tests/test_gen_trajectory.py` |
| `F821` undefined name `pytest` | 2 | `tests/test_trace.py` (lines 1525 and 1672) |
| `F401` unused import | 5 | `gen_open_items.py` (`io`), 4 in test modules |

19 of the 20 are in `tests/`; one (`io`) is in a kit script.

## Why this is not purely cosmetic

**`F821` is a latent defect, not a style nit.** Both sites read:

```python
git = _sh.which("git")
if not git:
    pytest.skip("needs git on PATH")
```

`tests/test_trace.py` never imports `pytest`. The guard is there to skip when git
is absent — and on the machine where that is true it raises `NameError` instead
of skipping, so the *safety net itself* is what fails. It has never fired because
every machine that has run this suite had git on `PATH`. That is the same shape as
the traps this repo keeps paying for: a guard nobody has watched fail.

Triage `F821` **first**, and against the two sites rather than the rule name —
decide whether the import is missing or the branch is dead, and say which.

## Done when

- [ ] `F821` resolved at both sites, with the decision (import vs. dead branch)
      stated in the session log; if the import is added, the skip path is
      **exercised** (e.g. a temporary `PATH` without git) rather than assumed.
- [ ] `F402` cleared in `tests/test_gen_trajectory.py` — the loop variable is
      renamed, not the module import; a shadowed `html` means any later use of
      the module inside those tests silently reads a string.
- [ ] `F401` cleared (5 sites), each confirmed genuinely unused rather than
      re-exported for a caller.
- [ ] `ruff check project-trajectory/scripts tests` exits 0, and
      `python project-trajectory/scripts/check.py --gate G3` no longer fails at
      `[step:lint]` for this reason.
- [ ] Full suite green (`pytest -q -n auto`), totals pasted in the log.

## Interfaces (§8)

Single-module work, no seam: this edits test modules and deletes one unused
import inside `gen_open_items.py`. No `IF-###` citation.
