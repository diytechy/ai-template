# Python tooling artifacts from running/​testing the kit scripts
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
.coverage.*
coverage.json
.ruff_cache/

# Generated reports (downstream repos generate these into docs/test/)
docs/test/report.md
docs/test/report.html
docs/test/perf-report.md
docs/test/perf-metrics.json

# Raw unattended-session streams (agent_loop.py); the tracked, size-bounded
# copies live in docs/iteration/. Also covers the WI-540 adjudicator
# session-retention store (out/adjudicator/<FAMILY>.json) and its dedicated CLI
# homes (out/adjudicator/home/) — runtime state, per-checkout, never tracked.
out/

# macOS Finder metadata (appears anywhere you browse the tree in Finder)
.DS_Store

# Meta-only dashboard render-critique tooling (WI-189): the pinned Playwright
# install + its screenshot output are dev artifacts, not tracked. Dependency
# resolution is committed in package-lock.json so the documented npm ci works.
scripts/dashboard-shots/node_modules/
scripts/dashboard-shots/shots/
