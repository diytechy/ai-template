# AGENTS.md

The contributor guide for **this meta-repo** is [`CLAUDE.md`](CLAUDE.md) — read
it first. It governs editing the reusable kit in
[`project-trajectory/`](project-trajectory/). This stub exists so agents that
read `AGENTS.md` (Codex and others) land on that guide too; the content lives in
`CLAUDE.md`, not here.

> Note: the kit *ships* the inverse convention — downstream projects get a full
> `AGENTS.md` with `CLAUDE.md`/`GEMINI.md` stubs. Here `CLAUDE.md` is the home
> because that's the historical guide for this repo; this `AGENTS.md` dogfoods
> the cross-tool pointer pattern.
